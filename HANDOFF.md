# HANDOFF.md — 次セッション開始用ガイド

> 最終更新: 2026-04-26 (**Phase 6.5–6.13 完了**)
>
> - 進捗: MVP → … → 6.1 staging → 6.2 streaming → 6.3 通知 deep-link →
>   6.4 分解 CTA → TZ cron → **6.5 keybindings modal** → **6.6 email mock outbox** →
>   **6.7 Agent キャンセル** → **6.8 PM Pre-mortem** → **6.9 AI コスト月次上限** →
>   **6.10 依存ブロック検出 (Pre-mortem 強化)** → **6.11 PWA / Service Worker** →
>   **6.12 Engineer Agent (PR 自動生成)** → **6.13 POST_MVP 棚卸し** ✅
> - 次の主戦場: **Reviewer Agent** / **二重承認 (MUST 操作 PM 承認)** /
>   **Vibe Kanban (複数 Engineer 並列)** / **Slack 通知 / 実 SMTP 切替** /
>   **オフライン同期 (IndexedDB)**
> - 詳細プラン: `~/.claude/plans/todoist-ticktick-todo-ui-ux-sleepy-hamster.md`

## 0. 現状サマリ

| 指標           | 値                                                                                                                            |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 受け入れ基準   | **8/8** PASS (`scripts/verify-acceptance.ts`)                                                                                 |
| MUST Recovery  | **6/6** PASS (`scripts/verify-must-recovery.ts`、live AI / claude CLI)                                                        |
| Notif deeplink | **8/8** PASS (`scripts/verify-phase6_3-notification-deeplink.ts`)                                                             |
| PM Premortem   | **4/5** PASS (`scripts/verify-pm-premortem.ts`、Doc 4944 chars / Watch List 投下は prompt 改善余地)                           |
| Dep Blocking   | **11/11** PASS (`scripts/verify-phase6_10-dep-blocking.ts`)                                                                   |
| PWA            | **14/14** PASS (`scripts/verify-phase6_11-pwa.ts` — pnpm build && pnpm start 起動後)                                          |
| Engineer       | **13/13** PASS (`scripts/verify-phase6_12-engineer.ts`、mock runner / autoPr=false)                                           |
| Vitest         | **414** PASS / 48 files                                                                                                       |
| E2E (local)    | **14** PASS / 2 skip (bulk-action-bar / backlog-dnd: dev mode 並列で QuickAdd 連続入力が flaky — §5.16; workers=4 で他は安定) |
| pg-boss queues | **12** (+engineer-run / +sprint-retro-tick) — tick 頻度は `*/15 * * * *` UTC + workspace 局所評価                             |
| views          | **6** (Today / Inbox / Kanban / Backlog / Gantt / Dashboard)                                                                  |
| schema         | 32 テーブル (+agent_decompose_proposals; workspace_settings.timezone は既存)                                                  |
| build          | **webpack** (Turbopack は Serwist 非対応のため `next build --webpack` 固定)                                                   |

AI 検証は Claude Code Max OAuth + MCP 経由なので `ANTHROPIC_API_KEY` 無しでも完走:

```bash
NODE_OPTIONS="--conditions=react-server" \
  pnpm tsx --env-file=.env.local scripts/verify-acceptance.ts
```

## 1. 最初に読む順番 (5 分で把握)

1. **本書 (HANDOFF.md)** — 現在地 + 次の一手
2. `~/.claude/plans/todoist-ticktick-todo-ui-ux-sleepy-hamster.md` — Phase 1-4 の詳細プラン
3. `CLAUDE.md` — AI 向け規約 (短縮版、必読)
4. `POST_MVP.md` — やらないリスト
5. `git log --oneline | head -10` — 直近 commit

## 2. 完了済み機能 (累積)

### 2.1 MVP (2026-04-24 クローズ)

- **認証 + RLS**: Supabase Auth + workspace_members + ws-scoped policy
  (soft-delete + RLS の罠は §5.1)
- **Item**: ltree 階層 + 楽観ロック + fractional-indexing position +
  MUST/DoD 不変条件 + priority (1-4) / due_time / scheduled_for
- **Doc / Comment**: 同パターン CRUD
- **Template**: 手動 + recurring (pg-boss cron + cron_run_id 冪等) + Mustache 変数
- **AI Agent**: Researcher / PM + executeToolLoop + agent_memories + cost 集計
- **RAG**: multilingual-e5 + pgvector HNSW + pg_trgm + RRF Hybrid
- **配信**: Docker Compose (web / worker / caddy / db-backup)

### 2.2 稼働入力 (POST_MVP Phase 2 → 実装済)

- `time_entries` / `mock_timesheet_entries` テーブル
- `/w/[wsId]/time-entries` UI (作成 / 一覧 / Sync ボタン)
- `/mock-timesheet/{login,new,entries}` (Playwright ターゲット)
- Playwright driver + pg-boss worker で sync 実行 → external_ref 返し

### 2.3 Phase 1 — 基本 UX + Command Palette

- ワンクリック完了 (`itemService.toggleComplete` + `ItemCheckbox` 優先度色)
- **Today / Inbox** ビュー (plugin、既定 = today)
- 自然言語 QuickAdd (`nl-parse.ts`: 今日/明日/来週X曜/HH:MM/p1-p4/#tag/@user/MUST)
  - chip preview
- グローバルショートカット `q / ? / g t-d`
- Command Palette: 7 view 切替

### 2.4 Phase 2 — コラボ hygiene + AI 分解 CTA 昇格

- comment hooks (`useItemComments` 系) + list Server Action
- `src/features/tag/` 一式 (CRUD + uniq + color 検証 + audit)
- `itemService.setAssignees / setTags` (非 member assignee と他 ws tag を弾く)
- UI picker: `AssigneePicker` / `TagPicker` (インライン作成) / `CommentThread`
- ItemEditDialog Tab 化 (基本 / コメント) + 🧠 AI 分解 CTA 主ボタン昇格
- Kanban カード: hover で AI 分解ボタン + 子 Item 件数 badge
- Item 検索: `useSearchItems` (fuse.js) + Command Palette `?` プレフィクス
- Template instantiate 後の `agentRoleToInvoke='researcher'` chain は配線済

### 2.27 Phase 6.15 (loop iter 3) — Login form a11y (required 属性) (2026-04-26)

Playwright 探索 (`scripts/explore-uiux-login-iter3.ts`) で発見:

1. `[a11y] login email input に required 属性なし` → 修正
2. `[a11y] login password input に required 属性なし` → 修正
3. `[observation] signup リンク click で /login に居る` (要再検証、次 iter)

email / password input に `required` を追加して HTML5 ブラウザ検証を効かせ、
空送信で「Please fill out this field」を表示。Server Action を呼ばずに済む。

### 2.26 Phase 6.15 (loop iter 2) — Gantt 依存線 SVG オーバーレイ component (2026-04-26)

`src/components/workspace/gantt-dependency-arrows.tsx` 純粋プレゼンテーション。
親要素 position:relative に被せる absolute SVG。Manhattan L 字パス + 矢じり marker。
critical path 上の edge は赤実線 / 通常 edge は slate 破線。`bars` (id, leftPx,
rightPx, centerYPx, isCritical) と `edges` (fromId, toId) を受け取るだけで
DB / hook 不参照。GanttView への統合は次 iter。

### 2.25 Phase 6.15 (loop iter 1) — Gantt critical path 純関数 (2026-04-26)

TeamGantt / GanttPRO ベンチマーク gap close。`src/features/gantt/critical-path.ts`
純関数 + 10 ケース test。AON-CPM (Kahn topo sort + forward/backward pass + slack)。
循環 / 未知 node / 負 duration を Result で err。**DB 不要、UI 不要**。
次 iter で `gantt-service.ts` から item + dependency を集計して呼ぶ予定。

### 2.24 Phase 6.13 — POST_MVP 棚卸し (2026-04-26)

完了済を `POST_MVP.md` 上で取り消し線 + Phase 番号 reference 化:

- Engineer Agent (Phase 6.12) / Pre-mortem (6.8) / 依存ブロック検出 (6.10) /
  Agent キャンセル (6.7) / メール通知 mock outbox (6.6) / 通知購読設定 UI (6.6) /
  キーボードショートカット一覧 (6.5) / 月次上限 (6.9) / モバイル PWA (6.11)

残タスクは `POST_MVP.md` 参照。**次の優先度高**: Reviewer Agent / 二重承認 / オフライン同期 /
Slack 通知 / 実 SMTP 切替 (dispatcher.ts 1 ファイル)。

### 2.23 Phase 6.12 — Engineer Agent (PR 自動生成) (2026-04-26)

- **目的**: 3 体目の Agent。Item を起点にコード変更 + commit、optional で `gh pr create --draft`
- **agent_role**: `'engineer'` を AGENT_ROLES に追加 (DB enum は text 列で動的対応)
- **roles/engineer.ts**: model=`claude-opus-4-7` / system prompt は CLAUDE.md 遵守 + 削除系禁止 +
  最小実装方針を強調
- **engineer-service.ts**: `runForItem({workspaceId, itemId, repoRoot, baseBranch, autoPr})`:
  - Item 取得 + workspace 一致チェック + cost-budget pre-flight
  - `agent_invocations` を queued → running
  - `git worktree add -b engineer/<itemId>-<slug>` で隔離 worktree 作成 (mkdtemp 配下)
  - `runner` (DI) を呼ぶ。**本番経路** = claude CLI subprocess (Max OAuth、ANTHROPIC_API_KEY なし)
    - `--add-dir <worktree> --permission-mode acceptEdits --no-session-persistence`
    - cwd = worktree
  - `git status --porcelain` で changedFiles を集計 (**4 文字目以降が path** — trim 禁止 §5.28)
  - `git commit -m "[engineer] ..."` (engineer-agent@saikyo-todo.local)
  - `autoPr=true` なら `git push origin <branch>` + `gh pr create --draft --base main` で PR 起票
  - 完了時 `agent_invocations.output` に `{prUrl, commitSha, changedFiles, diffShortStat, branchName}`
  - finally 句で `git worktree remove --force` + 一時ブランチ削除
- **engineer-actions.ts**: `triggerEngineerAgentAction({itemId, autoPr})` →
  pg-boss 'engineer-run' queue に enqueue (singletonKey で重複抑制)
- **engineer-worker.ts**: `handleEngineerRun` を `start.ts` で register。
  `process.env.SAIKYO_REPO_ROOT` (default `process.cwd()`) を repoRoot として使う
- **UI**: ItemEditDialog 基本タブに `EngineerTriggerButton` 追加 (confirm + autoPr 明示 opt-in)
- **テスト**: `engineer-service.test.ts` 6 ケース (mock runner + 一時 git repo)
  - 通常変更でコミット成立 / 変更なしで commitSha=null / failed 時の error_message /
    別 ws Item 拒否 / worktree 後始末 / agent_invocations row 検証
- **検証**: `verify-phase6_12-engineer.ts` 13/13 PASS (mock runner、autoPr=false、
  ANTHROPIC_API_KEY 不要)

### 2.22 Phase 6.11 — PWA / Service Worker (2026-04-26)

- **lib**: `@serwist/next` (prod) + `serwist` (dev)。pwa-asset-generator は **使わず**、
  `next/og` の動的アイコン (`src/app/icon.tsx` / `apple-icon.tsx` / `icon-512/route.tsx`) で
  192/180/512 を runtime 生成
- **manifest**: `src/app/manifest.ts` (App Router MetadataRoute.Manifest)。
  name=最強TODO / display=standalone / theme_color=#0f172a / background_color=#0f172a /
  icons 3 種 (192 any / 512 maskable / 180 apple)
- **sw**: `src/app/sw.ts` (Serwist テンプレ + defaultCache + offline fallback)。
  worker scope に `/// <reference lib="webworker" />` でグローバル型を有効化
- **offline page**: `src/app/~offline/page.tsx` (Server Component / `dynamic = 'force-static'`)
- **next.config**: `withSerwistInit({ swSrc, swDest, disable: dev })` を `withNextIntl` の
  内側にラップ (`withNextIntl(withSerwist(config))`)
- **layout.tsx**: `metadata.manifest` / `viewport.themeColor` / `appleWebApp` を追記。
  `ServiceWorkerRegister` Client Component で `useEffect` から `navigator.serviceWorker.register('/sw.js')`
- **middleware**: matcher に `sw\.js|manifest\.webmanifest|~offline|icon$|icon-512$|apple-icon$`
  を除外追加 (Supabase Cookie が SW リクエストに付かないように)
- **`pnpm build` を webpack 固定**: `package.json` の build script を `next build --webpack` に変更。
  Turbopack build では Serwist が public/sw.js を生成しないため (§5.29)
- **`.gitignore`**: `public/sw.js` / `public/sw.js.map` / `public/swe-worker-*.js` を ignore
- **eslint**: `public/sw.js` を ignore (Serwist 生成物に lint 不要)
- **検証**: `verify-phase6_11-pwa.ts` 14/14 PASS。`pnpm build && pnpm start` 起動後に
  `/sw.js` `/manifest.webmanifest` `/icon` `/icon-512` `/apple-icon` `/~offline` の
  HTTP 200 + content-type + HTML head の link/meta 注入を確認

### 2.21 Phase 6.10 — 依存ブロック検出 (Pre-mortem 強化) (2026-04-26)

- **schema**: `item_dependencies` (既存。fromItemId / toItemId / type∈{blocks, relates_to}) を活用
- **新 feature**: `src/features/item-dependency/`
  - `repository.ts`: insert (onConflictDoNothing で idempotent) / remove / listForItem /
    `wouldCreateCycle` (BFS 32 深さ上限) / fetchItemRefs
  - `service.ts`: `add` / `remove` / `listForItem`
    - blocks の循環検出は `wouldCreateCycle` で。relates_to は循環チェックしない (双方向 OK)
    - 越境 ws Item は ValidationError
    - audit_log: `target_type='item_dependency'`, `target_id=fromItemId`, after に full triple
  - `actions.ts` / `hooks.ts` (TanStack Query)
- **UI**: ItemEditDialog に「依存」タブ追加。`ItemDependenciesPanel` で
  前提条件 (blockedBy) / 後続タスク (blocking) / 関連 (related) を 3 セクション表示 +
  picker で追加 / 解除
- **Pre-mortem 強化** (`src/features/sprint/premortem-service.ts`):
  - `detectBlockedItems(items, deps, externalUpstreams)` 純粋関数で「上流が未完なせいで進めない」
    Item を抽出 (Sprint 内 + Sprint 外 upstream 両方対応)
  - `buildPremortemUserMessage` に「**🔴 依存ブロック中**」セクション + Watch List 強制指示を追加
  - blocked MUST が存在する場合は「上流の現状 (担当 / 残作業) を read_items で必ず確認」を促す
- **テスト**: `item-dependency/__tests__/service.test.ts` 8 ケース +
  premortem-service.test.ts に detectBlockedItems / 依存ブロック prompt 4 ケース追加
- **検証**: `verify-phase6_10-dep-blocking.ts` 11/11 PASS
  (admin SQL で items + deps + sprint を組み立て、buildPremortemUserMessage の
  生成 prompt を文字列 assert。ANTHROPIC_API_KEY 不要)

### 2.21 Phase 6.9 — AI コスト workspace 月次上限 (2026-04-26)

- **schema**: `workspace_settings.monthly_cost_limit_usd` (NULL=無制限) +
  `cost_warn_threshold_ratio` (default 0.80)。migration `20260426050000_workspace_cost_limit.sql`
- **BudgetExceededError** (code='BUDGET_EXCEEDED') を errors.ts に追加
- **cost-budget.ts**:
  - `getBudgetStatus`: 当月の cost_usd 合計 + limit / threshold / 比率を返す
  - `checkBudget`: 超過なら err
- **researcher / pm の pre-flight**: `run` の冒頭で `checkBudget` を呼び、超過なら起動拒否
  (テスト互換のため invoker DI 経路は skip)
- **Server Actions**: `getBudgetStatusAction` / `updateMonthlyCostLimitAction` (audit)
- **BudgetPanel** UI を Dashboard に組み込み (バー + 警告閾値線 + 編集インライン)
- **テスト**: `cost-budget.test.ts` 7 ケース

### 2.20 Phase 6.8 — PM Pre-mortem (2026-04-26)

- **目的**: Sprint 開始時に "失敗するとしたら何が?" を予測する Pre-mortem Doc を生成。
  Retro が事後の振り返りなら Pre-mortem は事前の予防接種
- **schema**: `sprints.premortem_generated_at` + partial index
- **premortemService.runForSprint**: sprint + items を集計、PM に prompt を渡す。
  成功時のみ marker をセット (失敗時は再試行可)
- **buildPremortemUserMessage** (純粋関数): MUST 数 / DoD 未設定 / 期間日数を表示、
  search_docs で過去 retro / recovery を引かせる手順を含む
- **queue**: `sprint-premortem` を追加 + `SprintPremortemJobData` + worker
- **トリガ**: `sprintService.changeStatus → 'active'` で `premortem_generated_at` 未設定なら enqueue
- **UI**: SprintsPanel の planning / active カードに "Pre-mortem 生成" ボタン
  (生成済なら "Pre-mortem 再生成" にラベル切替)
- **テスト**: 7 ケース (pure 3 + service 4)
- **検証**: `verify-pm-premortem.ts` 4/5 PASS (Doc 4944 chars / cost \$0.0525 / 77s。
  Watch List item 自動投下は prompt 改善余地)

### 2.19 Phase 6.7 — Agent キャンセルトークン (2026-04-26)

- **目的**: 実行中の agent_invocation を中止できる仕組み。runaway / 不要な完走を防ぐ
- **CancelledError** (code='CANCELLED') を errors.ts に追加
- **ToolLoopInput.shouldAbort?**: 各 iteration の前に呼ばれる中止判定。true で `CancelledError` を throw
- **researcherService.run / pmService.run**:
  - 入力 invoker が DI されていなければ adminDb で `agent_invocations.status` を毎 iteration ポーリング
  - cancelled に遷移していたら `CancelledError` → catch 経路で `status='cancelled' / finishedAt`
    - `audit_log action='cancel'`
  - shouldAbort も DI 可能 (テスト用)
- **cancelInvocationAction** (Server Action): status を 'cancelled' に立てるだけ
- **useCancelInvocation** hook
- **UI**: DecomposeProposalsPanel header で Agent 実行中に "中止" ボタンを表示
- **テスト**: tool-loop.test.ts +2 / researcher-service.test.ts +1

### 2.18 Phase 6.6 — メール通知 mock outbox (2026-04-26)

- **schema**: `mock_email_outbox` (workspace_id / user_id / type / subject / html / text / dispatched_at / error)
  - `notification_preferences` (4 タイプの ON/OFF、自分の row のみ操作可能)
- **dispatcher.ts**: `dispatchEmail(EmailToSend)` が `mock_email_outbox` に書く。
  Resend / SMTP への切替は **このファイル 1 つの差し替えで済む**
- **react-email** で 4 テンプレ (heartbeat / mention / invite / sync-failure) をレンダリング
- **notify.ts**: 通知 generator の各サイトから best-effort で呼び出し
  (heartbeat / comment service / workspace.addMember / time-entry worker)
- **UI**: `NotificationPreferences` Popover (4 トグル) を workspace header に
- 9 ケース追加テスト (dispatcher + templates)

### 2.17 Phase 6.5 — keybindings help modal (2026-04-26)

- `?` 押下で全 keybindings を表示する Modal (input フォーカス時はスキップ)。
  Command Palette からも `ヘルプ: ショートカット一覧` で開ける
- 単一の `KEYBINDINGS` registry を `src/lib/keybindings.ts` に集約
- `verify-phase6_5-keybindings.ts` で `?` / Esc / palette 経路を検証

### 2.16 TZ-aware cron (2026-04-26)

- **背景**: `pm-standup-tick` / `sprint-retro-tick` が UTC 09:00 固定 (= JST 18:00) で
  multi-TZ 対応していなかった
- **schema**: `workspace_settings.timezone` (既存、default `'Asia/Tokyo'`) を活用 (新規 migration 不要)
- **新モジュール**: `src/features/agent/cron-tz.ts` の純粋関数 `shouldFireForWorkspace`
  ({ cronExpr, tz, now, lastFiredAt, firstRunLookbackMs })
  - cron-parser v5 (CronExpressionParser) で localized expression を評価
  - `prev()` を `now+1s` 基準で取り、firing instant も含む (epsilon hack)
  - `lastFiredAt = null` のときは 24h lookback で初回判定
- **tick 頻度**: 全 tick を `*/15 * * * *` UTC に変更。handler 内で workspace ごと localized 判定
- **handlePmStandupTick**: 各 ws の `standup_cron` (default `'0 9 * * *'`) + `timezone` で
  fire 判定 → fire する ws だけ enqueue。`lastFiredAt` は `agent_invocations` の最新 completed
  PM invocation の `created_at` を採用
- **handleSprintRetroTick**: hardcoded `'0 9 * * 1'` (毎週月曜) を ws 局所で評価
- **handlePmStandup**: 重複防止判定の `dateKey` を ws timezone で算出 (`Intl.DateTimeFormat('en-CA')`)
- **テスト**: `cron-tz.test.ts` 13 ケース (JST/EST/EDT/double-fire/lookback)
- **検証**: vitest 367/367 + lint + typecheck クリーン

### 2.15 Phase 6.4 — 分解 panel 仕上げ: fallback + 再分解 CTA (2026-04-26)

- **0 件フォールバック**: invocation completed かつ pending=0 のとき
  "提案が出ませんでした" メッセージ + 再分解ボタンを表示
- **再分解 CTA** (panel header に追加):
  - **追加分解**: 既存 pending を残したまま再 mutate (pending=0 のときは "再分解" にラベル変更)
  - **やり直し**: rejectAll → 再 mutate (既存 pending あり時のみ)
- Agent 実行中は全ボタン disabled (重複起動防止)
- Toast: 再分解完了時に提案件数を表示

### 2.14 Phase 6.3 — 通知 deep-link + mention/invite/sync-failure 通知タイプ (2026-04-26)

**通知 → Item dialog 自動 open**:

- items-board に `?item=<id>` URL param を追加 (nuqs `parseAsString`)
- `DeepLinkedItemDialog` ラッパで paletteSelected と URL 由来を統一管理
- NotificationBell click → `extractItemId(n)` で item-linked 通知だけ抽出 → `setOpenItemId(itemId)`
- ESC で URL から `item` param が消える (close → onClose で setOpenItemId(null))
- 検証: `verify-phase6_3-notification-deeplink.ts` 全項目 PASS

**3 通知タイプ追加**:

- schema: `MentionPayload` / `InvitePayload` / `SyncFailurePayload` (jsonb 形)
- `notificationRepository.insert` helper を共通化
- **mention**: `comment.create` 後に `extractMentionTokens(body)` で `@<displayName>` を抽出、
  ws member だけ resolve して通知挿入 (CJK 対応のハンドロール scanner、self-mention 除外、
  別 Tx で best-effort で comment 自体は失敗させない)
- **invite**: `workspaceService.addMember` (admin gate) で member 挿入後に invite 通知
- **sync-failure**: time-entry worker の sync 失敗時に user_id 宛に通知
- NotificationBell `formatNotification` を 3 タイプ対応 (40 文字 preview / role / reason)
- テスト: `comment/__tests__/mention-notification.test.ts` 12 ケース (parser 7 + flow 5)、
  `time-entry/worker.test.ts` に sync-failure assertion を追加

### 2.13 Phase 6.2 — Anthropic streaming + Realtime push UI (2026-04-26)

- **publication 拡張**: `agent_decompose_proposals` + `agent_invocations` を
  `supabase_realtime` に追加 (REPLICA IDENTITY FULL)。migration
  `20260426020000_realtime_decompose_proposals.sql`
- **Anthropic streaming**: `invokeModelStream` (`messages.stream` で text delta コールバック)
  - `streamingInvoker(onTextDelta)` factory。既存 `invokeModel` は据え置き
- **researcherService.run**: invoker 未指定時は streamingInvoker を採用、
  `output.streamingText` を 250ms debounce で UPDATE → Realtime に流れる
- **Realtime hooks**:
  - `useDecomposeProposalsRealtime(parentItemId)`: pending 一覧を 150ms debounce で invalidate
  - `useAgentInvocationProgressByTarget(targetItemId)`: status + streamingText を Query キャッシュに直接書き込み
    (set-state-in-effect lint 回避)
- **DecomposeProposalsPanel**:
  - Agent 実行中は "Researcher が分解中…" + 流れる streamingText を 3 行 line-clamp で表示
  - 完了 / pending=0 で自然消滅 (item-edit-dialog の他タブとの整合)
- **検証**: `scripts/verify-phase6_2-realtime.ts` 全項目 PASS
  (3 proposals が 1 件ずつ live 出現 / streamingText 反映 / completed で UI hidden)

### 2.12 Phase 6.1 — AI 分解 staging (preview / accept / reject) (2026-04-26)

- **目的**: Researcher の分解結果を即時 items に書く代わりに staging に置き、
  ユーザーが UI で行ごとに採用 / 却下 / 編集できる導線。`undo` を不要にする
- **schema**: `agent_decompose_proposals` (workspace_id / parent_item_id /
  agent_invocation_id / title / description / is_must / dod / status_proposal:
  pending|accepted|rejected / accepted_item_id / sort_order)
  - RLS (member: insert/update; all member: select)
  * migration: `20260426010000_agent_decompose_proposals.sql`
- **tool**: `propose_child_item` (decompose mode 専用、ctx.decomposeParentItemId に閉じ込め)
- **bundle**: `buildDecomposeTools` (read 系 + propose_child_item のみ。
  create_item / write_comment / create_doc は外して脱線抑制)
- **researcherService.run** に `toolMode: 'researcher' | 'decompose'` 追加
- **decomposeItem(staging=true)** が既定 (旧挙動は staging=false で残置)
- **Service**: list / accept (items に実 INSERT) / reject / update / rejectAllPending
  - accept は `mutateWithGuard` 経由ではなく withUserDb 直 (parent ws 一致 + member ガード)
- **UI**: `DecomposeProposalsPanel` を ItemEditDialog "子タスク" タブ上部に組み込む。
  pending=0 + Agent 非実行で消える / 行 click で title/description/MUST/DoD 編集
- **テスト**: `decompose-proposal/service.test.ts` 11 ケース + researcher staging 切替 2 ケース
- **検証**: `scripts/verify-phase6_1-ui.ts` (admin で proposals 直挿入し UI 動作だけ検証、
  ANTHROPIC_API_KEY 不要) 全項目 PASS

### 2.11 Phase 5.3 weekly cron — Sprint retro fallback (2026-04-26)

- **目的**: Sprint completed → 自動 enqueue trigger を取り逃したケース (worker 落ち / 手動 status
  変更 / 過去の completed sprint で trigger 配線前) の救済
- **schema**: `sprints.retro_generated_at timestamptz` 列を追加
  - partial index `(workspace_id, end_date) WHERE status='completed' AND retro_generated_at IS NULL`
  * 手書き migration `20260426000000_sprint_retro_generated_at.sql`
- **retroService.runForSprint**: pmService.run 成功時に `retro_generated_at = NOW()` を adminDb で UPDATE
  (失敗時はセットしない → 次回 cron で再試行可能)
- **handleSprintRetroTick**: status='completed' AND retro_generated_at IS NULL AND end_date >= now() - 30d
  の sprint を全件 pickup → `sprint-retro` queue に fan-out (singletonKey で重複抑制)
- **cron**: 毎週月曜 09:00 UTC (= 18:00 JST) に `sprint-retro-tick` 発火
  (`scheduleJob('sprint-retro-tick', '0 9 * * 1', {})`)
- **テスト**: 3 ケース追加 (成功時 marker 更新 / 失敗時 marker 不変 / tick の pickup 条件 4 通り)

### 2.10 Phase 5.4 — PDCA dashboard

- **新 schema 不要**: 既存 `items.status` / `items.doneAt` / `items.createdAt` から集計
- mapping: Plan=todo / Do=in_progress / Check=直近 7 日完了 / Act=それ以前完了
- service: `pdcaService.summary(workspaceId, {from?, to?, checkWindowDays?})` →
  - counts (4 状態) / leadTimeDays (avg/p50/p95/n) / daily (期間内の done 件数を 0 埋め配列で)
- UI: **専用ページ `/[wsId]/pdca`** (workspace header に link)。
  Dashboard view 同居案を試したが Server Action 連鎖が golden-path を壊したので分離 (§5.20)
- `PdcaPanel` (`src/components/workspace/pdca-panel.tsx`):
  - 4 状態カード + 1 行分布バー + lead time stats (avg/p50/p95) + 日次 throughput sparkline
  - sparkline は **pure CSS 棒** で実装 (recharts は dev compile が重く navigation を block しがち)
  - 30/90 日切替ボタン
- テスト: `pdca/service.test.ts` 4 ケース (PDCA 集計 / daily 0 埋め / 0 件耐性 / from>to ValidationError)
- 検証: `scripts/verify-phase5_4-ui.ts` で全項目 PASS

### 2.9 Phase 5.2 — OKR (Goals + Key Results) + 手動タスク分解

**OKR**:

- Schema: `goals` (Objective: title/desc/period/start/end/status) + `key_results`
  (KR: title/progress_mode/target/current/unit/weight/position) + `items.key_result_id`
  - DB CHECK: status enum / weight 1-10 / progress_mode IN ('items','manual') / start≤end
  - RLS: goals は workspace member、key_results は親 goal を辿って判定
- Service `okrService`:
  - createGoal / updateGoal / listGoals
  - createKeyResult / updateKeyResult / listKeyResults / **listAllKeyResultsByWorkspace** (Item picker 用)
  - assignItemToKeyResult (別 ws 拒否)
  - **goalProgress**: KR ごとに mode 分岐 (items: done/total / manual: current/target) →
    Goal pct は **weighted average**
  - 全 mutation で audit_log
- UI:
  - `/[wsId]/goals` ページ — `GoalsPanel` で Goal 作成 + expand で KR 一覧 + KR 追加
    (mode=items なら weight のみ、mode=manual なら target/unit 入力)
  - 進捗バー: Goal 全体 % + KR ごと %
  - ItemEditDialog 基本 Tab に **KR picker** (Sprint picker と並列)
  - workspace header に `Goals` link
- テスト: 7 ケース (CRUD + items mode 進捗 + manual mode 進捗 + weighted average + 越境 ws 弾き)

**手動タスク分解** (ItemEditDialog "子タスク" tab):

- 既存 children を `useItems` の client filter で表示 (`parentPath === fullPathOf(parent)`)
- textarea で改行区切り bulk 追加 → sequential `useCreateItem.mutateAsync`
  - parent_path 自動 (parentItemId を service.create で正しく処理)
- AI 分解 (Researcher) とは別経路 (即時、課金なし、長文 brainstorm を行内で素早く起こせる)

**既存 bug fix**: `itemService.create` が `parentItemId` を **無視していた** のを修正
(従来は parent 指定しても root に作られてた)。`parentPath = fullPathOf(parent)` を計算

- workspaceId 一致チェック。`priority` / `dueTime` / `scheduledFor` も同時に明示的に渡すよう
  変更 (今までは default に寄ってた可能性あり)。

検証: `scripts/verify-phase5_2-ui.ts` で OKR 全フロー + 子タスク bulk add (3 件) PASS。
全体 vitest 316 → **323 PASS / 38 files**、E2E 14 PASS / 2 skip 維持。

### 2.8 Phase 5.3 (手動起動部) — Sprint Retrospective

- `src/features/sprint/retro-service.ts`:
  - `runForSprint({sprintId, idempotencyKey})` — adminDb で sprint + items を集計、
    `buildRetroUserMessage` で Keep / Problem / Try 構成 prompt を組んで pmService.run に委譲
  - PM Agent が `create_doc` で Retro Doc を保存 + `create_item` で action items を投下
  - **MUST 落ち** が 1 件以上ある場合のみ "MUST 落ちの根本原因" セクションを促す
- UI: `SprintsPanel` の active / completed Sprint card に "振り返り生成" ボタン
  (Sparkles アイコン)。完了後 toast に iter / cost を表示
- テスト: `retro-service.test.ts` 6 ケース (pure helper 3 + service mock 3)
- **自動化** (2026-04-25 同セッションで追加):
  - `sprint-retro` queue (pg-boss QUEUE_NAMES に追加、9 個目)
  - `handleSprintRetro` worker (`src/features/sprint/retro-worker.ts`) を `start.ts` に登録
  - `sprintService.changeStatus(... status='completed')` で `enqueueJob('sprint-retro', ...)`、
    `singletonKey: 'sprint-retro-<sprintId>'` で同 sprint 二重実行を抑制
  - 失敗してもスループ throw せずログのみ (Sprint 完了自体は成立)
- **未実装** (次セッション):
  - **weekly cron**: 1 週間以内に completed → retro 未実行 sprint を pickup して enqueue
    (どの sprint で retro 走ったかを判定する仕組みが必要 — agent_invocations の input.userMessage
    に sprintId が入るので grep でも可、より厳密には sprints 側に `retro_invocation_id` 列追加)
  - retro Doc が出来た時に Inbox に通知 (notifications テーブル経由)

### 2.7 Phase 5.1 — Sprint

- **Schema**: `sprints` テーブル (status: planning/active/completed/cancelled)
  - `items.sprint_id` (set null on delete)
  * **DB 制約**: `start_date <= end_date` CHECK + `(workspace_id) WHERE status='active'`
    の partial unique index で「同 ws で active は 1 つだけ」を強制
  * 物理削除なし: status='cancelled' で代替 (deleted_at は使うが移行不要)
- **Service**: `create / update / changeStatus / list / getActive / assignItem / progress`
  - active 化の Unique 違反は **withUserDb の外側 try/catch** で捕捉して
    ValidationError に変換 (内側 try/catch だと postgres-js の async error を取り逃す。§5.19)
  - assignItem は別 ws の Sprint 割当を弾く
  - audit_log: create / update / status_change / sprint_assign を記録
- **UI**:
  - `/[wsId]/sprints` ページ — `SprintsPanel` で 一覧 + 新規作成 + status 操作
  - **Burndown 簡易**: progress bar に "理想ライン" 1px (期間経過 %) を重ねて
    on-track かを色で示す。本格的な daily snapshot 履歴は POST_MVP
  - **Items の Sprint 割当**: ItemEditDialog 基本 Tab に Sprint picker (active=★)
  - **filter**: items-board に `?sprint=active|none|<id>` を nuqs で追加
  - workspace header に `Sprints` link
- **検証**: `scripts/verify-phase5-ui.ts` で全項目 PASS
  (作成 / active 化 / 2 つ目 active 拒否 / 割当 / filter / 進捗 0/1)。
  vitest 9 ケース (RLS / 楽観ロック / Unique / 越境 ws)。

### 2.6 Phase 4 — Dark mode + Notification bell

- **Refactor**: `WorkspaceHeader` を抽出 (3 ページの header 重複を統一)。
  `pageActions` (ページ固有) + `utility` (Theme/Bell など全 ws ページ共通) slot
- **Dark mode**: `next-themes` 導入、`<ThemeProvider attribute="class">` を
  `app/layout.tsx` で適用 (`suppressHydrationWarning` も同時)。
  `theme-toggle.tsx` は CSS の `dark:` variant でアイコン切替 (setState 不使用 →
  `react-hooks/set-state-in-effect` lint を回避し hydration mismatch も無し)
- **Notification feature**: `src/features/notification/{schema,repository,service,actions,hooks,realtime}.ts`
  - Repository は scoped Drizzle (RLS で `user_id = auth.uid()` 強制)
  - Service: `list / unreadCount / markRead / markAllRead`
    (作成系は heartbeat / mention worker 側 — service には置かない)
  - Hooks: `useUnreadNotificationCount` は **`initialData` あり時 staleTime: Infinity + refetchOnMount: false**
    (常時 polling すると Server Action の router.refresh が他 mutation と競合 → §5.17)。
    `useNotifications` は popover open 時のみ enabled
  - Realtime: `useNotificationsRealtime` で `postgres_changes filter=user_id=eq.<uid>` 購読、
    200ms debounce で count + list を invalidate
  - **重要**: 各 workspace ページの Server Component で `notificationService.unreadCount` を
    SSR fetch して Bell の `initialUnreadCount` に渡す (client polling 廃止)
- **NotificationBell** (`src/components/workspace/notification-bell.tsx`):
  Bell icon + 未読バッジ + Popover dropdown (50 件、相対時刻、各通知 click で既読化)。
  heartbeat type の payload は `{itemId, stage, dueDate, daysUntilDue}` を日本語整形
- **Realtime setAuth** 修正 (§5.18): Supabase Realtime の RLS 評価には JWT 必要。
  subscribe 前に `supabase.realtime.setAuth(session.access_token)` を明示しないと
  `postgres_changes` イベントが届かない (SUBSCRIBED ステータスは出ても event は dropped)。
  `notification/realtime.ts` + `item/realtime.ts` 両方に適用
- **検証**: `scripts/verify-phase4-ui.ts` (one-off Playwright スクリプト)
  → login / theme toggle / bell empty / heartbeat scan → Realtime 1s で badge 反映 / mark all read
  全 PASS。スクショ `/tmp/phase4-{dark,light,bell-empty,bell-with-notif}.png`

### 2.5 Phase 3 — 生産性加速 + MUST Escalation

- **Backlog DnD**: `@dnd-kit/sortable` (position ソート時のみ有効)
- **一括選択**: Zustand `bulk-selection` store + `BulkActionBar` (固定 bottom)
- **bulk service**: `bulkUpdateStatus` / `bulkSoftDelete` (1 件ずつ楽観ロック、
  部分失敗を `{succeeded, failed[]}` で集計)
- **Activity Tab**: `src/features/audit/` (read-only) + ItemEditDialog 3 本目 Tab
  (RLS で admin 以上、それ以下は空配列 fallback)
- **MUST Escalation**: `heartbeat.scanWorkspace` に `overdue` stage (days < 0) 追加。
  1d/overdue で `pm-recovery` queue へ singletonKey (ws+item+stage+date) で enqueue。
  `handlePmRecovery` worker → `pmService.runRecovery` で PM Agent が
  Recovery Plan Doc + write_comment による注意喚起を投下
- **enqueueJob options**: `singletonKey` / `startAfter` を第 3 引数で受ける

## 3. 次セッションでやること

### A. AI Agent 拡張の続き (POST_MVP に残置)

- **Reviewer Agent**: Engineer の出力 PR / Researcher の Doc / Item 出力を相互レビューする
  4 体目の Agent。`engineer-service.ts` の autoPr 経路と相性が良い (PR description / diff を
  Reviewer に投げて approve/request_changes コメントを書かせる)
- **二重承認**: MUST 追加 / 降格に PM Agent 承認必須。`item_approvals` テーブル + PM の
  approve_item ツール
- **Vibe Kanban スタイル**: 同 Item に複数 Engineer 並列投入 + 結果比較。queue は
  `engineer-run` を流用、worktree は invocationId 単位で分離 (既に実装済 — 並列起動は
  `singletonKey` を外せば可能)
- **Agent 出力 承認待ちモード** (`auto_apply: false`): 既存 decompose staging の枠組みを
  Engineer の commit 前にも適用 (人間が diff を見て approve / reject)

### B. 通知 / 配信の拡充

- **Slack 通知** (incoming webhook): `dispatcher.ts` の派生として slack-dispatcher.ts を作る。
  既存 mock outbox と同じ best-effort 呼び出し
- **実 SMTP / Resend 切替**: `src/features/email/dispatcher.ts` 1 ファイルの差し替え。
  現在は mock_email_outbox に INSERT、本番は Resend SDK or nodemailer
- **PM Agent Stand-up の個人別 DM** + 購読 ON/OFF

### C. オフライン同期 (Phase 6.11 PWA の延長)

- **IndexedDB cache**: Item / Doc を SW + IndexedDB にキャッシュ、オフライン時は読み取り可
- **Pending mutation queue**: オフライン時の create/update を localStorage キューに溜め、
  online 復帰で flush (Linear / Trello 風)

### D. その他 (POST_MVP に残置、優先度低)

- 添付ファイル UI + Supabase Storage
- Yjs/Tiptap collab で Doc 同時編集 (CRDT)
- 動的フォーム拡張 (multi-select / formula / relation / file)
- Gantt 依存線 + クリティカルパス可視化 (Phase 6.10 の item_dependencies を活用)
- ゴミ箱 UI / アーカイブビュー
- 監査ログの UI / 検索
- ログ集約 (Loki + Grafana) / Sentry 連携
- bigm → pgroonga 移行 (もし bigm 精度不足なら)
- multilingual-e5-large へのアップグレード (GPU 入手時)

## 4. 動作確認コマンド

```bash
# Supabase 起動確認 (止まっていれば pnpm db:start)
pnpm exec supabase status

# DB 再構築 (schema 変更後)
pnpm db:reset

# 検証 (commit 前に必ず通す)
pnpm typecheck && pnpm lint && pnpm test
pnpm test:e2e                                   # 並列、reuse 既存 dev server

# E2E が flaky な時 (port 3001 占有 → 古い next-server を kill):
#   lsof -i :3001 → kill <pid>

# PoC 系 (実 Auth + RLS を通す)
pnpm tsx --env-file=.env.local scripts/poc-auth-workspace.ts
pnpm tsx --env-file=.env.local scripts/poc-item.ts
pnpm tsx --env-file=.env.local scripts/poc-doc-comment.ts
NODE_OPTIONS="--conditions=react-server" \
  pnpm tsx --env-file=.env.local scripts/poc-ltree.ts

# UI smoke
pnpm dev                                        # http://localhost:3001
```

## 5. 非自明な落とし穴 (規約)

### 5.1 RLS SELECT policy に `deleted_at IS NULL` を入れない (MUST)

Postgres は UPDATE 時に新行が SELECT.using を満たすか暗黙チェックするため、
soft delete (`UPDATE ... SET deleted_at = NOW()`) で必ず弾かれる。
SELECT policy は `is_workspace_member` のみ、フィルタは Repository クエリで強制。
既存 migration: `20260424120000_rls_remove_deleted_at_from_select.sql`

### 5.2 Drizzle Kit 生成時の auth schema 削除

`_shared.ts` の `pgSchema('auth') / authUsers` 宣言で、`pnpm db:generate` 出力に
`CREATE SCHEMA "auth"` と `CREATE TABLE "auth"."users"` が毎回入る。
**生成直後に手で削除**。`pnpm db:reset` でエラーなら原因はこれ。

### 5.3 dotenv の env 読み込み

`pnpm tsx scripts/X.ts` だけでは `.env.local` を読まない →
**`pnpm tsx --env-file=.env.local scripts/X.ts`** を使う。

### 5.4 ポート 3000 占有 / 3001 ゾンビ

別 docker が 3000 を使うので `pnpm dev` は **port 3001**。
E2E 失敗時、古い `next-server` プロセスが 3001 を握っていることがある →
`lsof -i :3001` で pid を確認して kill。

### 5.5 マイグレーションの順序

手書き SQL は `0XXX_*.sql` 命名で Drizzle 生成より前に実行。
ただし RLS 等のテーブル前提なら、Drizzle 生成より **後** にする必要 →
タイムスタンプを大きく (例: `20260424000000_rls_policies.sql`)。

### 5.6 PG15 LTREE `subpath` の罠

`subpath(parent_path, nlevel(oldFull))` は offset == nlevel で空 ltree を返すべきだが
PG15 は "invalid positions" エラーを投げる。`moveSubtree` の一括 UPDATE で
target / 直接の子 / 孫以降 の **3 分岐 CASE** で回避済 (`src/lib/db/ltree.ts`)。

### 5.7 `server-only` と tsx スクリプト

server-only ファイルを tsx から load すると `server-only` が throw する。
**`NODE_OPTIONS="--conditions=react-server" pnpm tsx ...`** を使う。
Pure 計算は別ファイル (例: `ltree-path.ts`) に分離してテスト可能に。

### 5.8 shadcn add の副作用

`pnpm dlx shadcn@latest add command` は `button.tsx` / `input.tsx` を再生成
(quote スタイル差分のみ、機能同一)。競合したら `--overwrite` で OK、prettier が後で整形。

### 5.9 agent_invocations の UPDATE は adminDb 経由

`agent_invocations` は **INSERT だけ** authenticated policy が付いていて、
UPDATE policy は無い (= worker / service_role 専用)。
`agentService.runInvocation` は `adminDb.transaction(...)` を使う
(CLAUDE.md の "Repository は scoped Drizzle" 例外扱い)。
`ensureAgent` も同じ理由で adminDb。

### 5.10 Anthropic SDK mock 位置

単体 test は `vi.mock('@/lib/ai/invoke', () => ({ invokeModel: vi.fn() }))`。
実 API 検証は `scripts/poc-agent.ts` に寄せる (`ANTHROPIC_API_KEY` 設定時のみ)。

### 5.11 pg-boss queue 名 / 型 / option

- queue 名は英数 + `_-./` だけ (コロン不可)。`agent:run` は ERR_ASSERTION で死ぬ
- `import { PgBoss } from 'pg-boss'` (default export 無し)
- `stop()` は graceful + timeout(ms)、`wait` option は v10 の名残で消えた
- `enqueueJob(name, data, { singletonKey, startAfter })` で重複抑制 (Phase 3 で拡張)

### 5.12 multilingual-e5-small の prefix 規約

- Document/passage は `"passage: "` 前置 (encodeTexts で自動)
- Query は `"query: "` 前置 (encodeQuery で自動)
- 省略すると similarity が大きく劣化する

### 5.13 chunk overlap 制約

`chunkText` の `overlap` は `[0, maxChars)` 範囲必須 (無限ループ防止)。
短いテストで `maxChars=10` を渡すなら `overlap=2` 等を一緒に明示。

### 5.14 embedding model の初回ダウンロード

`@huggingface/transformers` は初回 ~120MB を `~/.cache/huggingface/` に DL。
Docker は volume で永続化。Vitest は `vi.mock('@/lib/ai/embedding')` か
`embedDoc` の `encoder` DI を使う。

### 5.16 E2E 並列で QuickAdd 連続 fill が flaky (dev mode 限定)

`bulk-action-bar.spec.ts` / `backlog-dnd.spec.ts` の "QuickAdd を for loop で 3 回連続 fill+click"
パターンが、parallel mode (`workers=undefined`) の dev server で 2 回目以降の `fill` が React
state に反映されず submit ボタンが disabled のまま timeout する。**単体実行 (workers=1) では PASS**。
原因は dev mode の Server Action 競合と推測 (Phase 4 で Bell の SSR 1 query 増加が trigger)。
**ハイブリッド方針** (golden-path / smoke は keep / 機能確認は Playwright MCP) に従い、
2 spec を `test.skip` で保留中。直す時は workers=1 / build prod / Playwright trace の 3 軸で。

### 5.17 通知バッジは SSR + Realtime のみ (常時 polling しない)

`useUnreadNotificationCount` を always-on の TanStack Query で叩くと、Server Action の
暗黙 `router.refresh` が QuickAdd 連続入力を不安定化させた (§5.16 の元凶)。修正:
**SSR で初期 count を取得 → `initialData` + `staleTime: Infinity` + `refetchOnMount: false`** にして
client mount 時の Server Action 呼び出しをゼロにし、以後は Realtime 経由でのみ refetch。
他 feature でも "ヘッダ常駐 widget" には同パターン (SSR + Realtime) を推奨。

### 5.29 Serwist / PWA は Turbopack production build と非互換

Next.js 16 のデフォルトの `next build` は Turbopack を使うが、現状 (2026-04 時点) Serwist は
Turbopack にまだ対応していない。`@serwist/next` は webpack plugin として動作するため、
**Turbopack build では `public/sw.js` が生成されない** (warning は出るが silent fail に近い)。
package.json の `"build"` script を **`next build --webpack`** に固定する。dev は
`next dev --turbopack` のままで OK (Serwist は dev disable しているため衝突しない)。
将来 Serwist が Turbopack 対応したら戻す。

### 5.28 `git status --porcelain` の path parse は **trim 禁止**

出力は `<XY> <path>` 固定 (X / Y 1 文字 + space + path)。`l.trim().slice(3)` をやると
status コードを潰して path が壊れる (例: ` M README.md` → trim → `M README.md` →
slice(3) → `EADME.md`)。**4 文字目以降だけ取る** (`l.length > 3 && l.slice(3).trim()`)。
Phase 6.12 engineer-service.ts で発覚。

### 5.20 Dashboard view に重い client component を embed しない

Phase 5.4 で `PdcaPanel` を Dashboard view (items-board の DashboardView) に embed したら、
`pdcaSummaryAction` Server Action と recharts の dev compile が同時発火して、
**golden-path E2E が timeout** で死ぬようになった (Templates 遷移が hang)。原因は §5.17 と
同根: Dashboard 切替直後の navigation を Server Action / 重い compile が block する。

対策: 大型 panel は **専用ページに分離** する (今回は `/[wsId]/pdca` に独立)。
Dashboard view は MUST / WIP / Burndown / コスト など軽量サマリだけに留める。
**recharts の追加 chart も同じ理由で dev では navigation を遅延させる** (Phase 5.4 では
sparkline を pure CSS 棒で実装して回避)。

### 5.19 Drizzle/postgres-js: 制約違反は withUserDb の外側で catch する

postgres-js の transaction 内で起きた制約違反 (UniqueViolation 等) は、`tx` の inner
callback から throw される際に Drizzle が wrap → withUserDb の `db.transaction` の
返り Promise の reject として伝搬する。inner try/catch では捕まえられず、**外側 await
ポイントの try/catch が必要**。Phase 5.1 の `sprintService.changeStatus` で発覚:

```ts
// NG: inner try/catch では postgres エラーを取り逃す
return await withUserDb(user.id, async (tx) => {
  try { await repo.update(tx, ...) } catch (e) { /* ここに来ない */ }
})

// OK: withUserDb の外で catch
try {
  return await withUserDb(user.id, async (tx) => {
    return ok(await repo.update(tx, ...))
  })
} catch (e) {
  if (isUniqueViolation(e)) return err(new ValidationError(...))
  throw e
}
```

`isUniqueViolation` は `e.code === '23505'` だけでなく `e.cause` 再帰 +
`e.message` の constraint 名 fallback も見ること (Drizzle wrap 形式が複数ある)。

### 5.18 Supabase Realtime は subscribe 前に `setAuth` 必要

`postgres_changes` イベントは Supabase 側で RLS を評価してから配信される。subscribe 時に
JWT が realtime client に渡っていないと、ステータスは SUBSCRIBED でも event が drop される
(Phase 4 notification 検証で発覚)。`useEffect` 内で `supabase.auth.getSession()` →
`supabase.realtime.setAuth(token)` → `channel.subscribe()` の順を必ず守る。
既存 `item/realtime.ts` も同 pattern に修正済 (Phase 4 commit で同梱)。

### 5.15 pg_trgm 閾値

`pg_trgm.word_similarity_threshold` の既定は 0.6 で短いクエリに厳しすぎる。
`fullTextHits` は WHERE 内で `word_similarity(q, content) > 0.2` を直接指定。
GIN index は閾値依存なのでフル活用はできないが MVP 規模では問題なし。
ARCHITECTURE.md #U の pg_bigm は Supabase local に無く pg_trgm で代替。

## 9. UI/UX バグログ (Playwright 探索 — Phase 6.15 loop)

各 iter で 1 画面を探索的操作した結果のメモ。修正済は ✅、保留は ⏳。

- ✅ [playwright-iter485 1/1] EngineerTriggerButton (Engineer に実装させる) を 44x44 化 (iter484 続編、AI action 4 button group cleanup 完結): src/components/workspace/engineer-trigger-button.tsx 行 70 の Button が size="sm" (h-8 = 32px) で `className="min-h-11"` 漏れ。AI action 4 button (decompose / research / plan-generate / engineer-trigger) のうち他 3 件は iter462-484 で min-h-11 化済だが engineer-trigger だけ漏れ → AI action group 4 button のうち 1 つだけ高さ 32px、tap-target 不揃い + 危険操作 (git worktree + Claude 自動コード生成) のため誤タップ防止が特に重要。fix: `<Button size="sm" variant="secondary" ...>` に `className="min-h-11"` を size と variant の間に挿入 (1 ファイル 1 行)、機能不変 (confirm + autoPr opt-in gate 不変)、shadcn 編集なし。これで AI action 4 button 完全統一、累計 **107 button mobile target 達成** (iter460-485 で 22 fix iter)。経路: 経路 B (script、iter484 hint「他 iter513-vintage AI button sweep」 に従い engineer-trigger を選定 → 漏れを発見)。検証 codify: `scripts/explore-uiux-engineer-trigger-min-h-iter485.ts` で min-h-11 + size=sm 配線 / data-testid + aria-busy + aria-label 維持 / confirm + autoPr opt-in gate 維持 / 兄弟 AI button 3 件 (decompose / research / plan-generate) min-h-11 維持 / visible label + 🛠 icon 維持 = 7 軸 assert (7/7 OK)。typecheck/lint 緑 (warning baseline 2)。次 iter 候補: start-timer compact (iter249 vintage、Today / Backlog 行配置) / agent-cancel / Phase 6.12+ vintage button の min-h-11 漏れ sweep、または別軸 a11y polish へ。
- ✅ [iter492 ai-automation 1/1] queue AC-2 polish + 「item axis 1-word state」 11 弾目。`structured-review.ts` に: (1) `formatTopImprovementJa` (iter484 pickTopImprovement の chip 整形、'❗ 優先改善: <title> [high]' / '⚠ ... [medium]' / '· ... [low]' / 'なし'、80 字超は ... 圧縮)、(2) `classifyReviewVerdictHint` (idle / mild / moderate (warn>=3 fail=0) / severe (fail>=1)) + `formatReviewVerdictHintJa` (review なし / 健全 pass / 多数注意 (pass) / 不合格)。11 unit test 追加 (累計 45 件 PASS)。typecheck/lint clean。6 軸: 軸 4 漏れ防止 (不合格 / 多数注意 を 1 単語警告) ★ + 軸 6 効率化 (review → 次対応 1 つ 1 行)。
- ✅ [playwright-iter484 1/1] ItemPlanGenerateButton (Plan を生成) を 44x44 化 (mobile sweep iter479 後の漏れ補完): src/components/workspace/item-plan-generate-button.tsx 行 47 の Button が size="sm" (h-8 = 32px) で `className="min-h-11"` 漏れ。兄弟 button (item-decompose-button.tsx 行 49 / item-research-button.tsx 行 40) は両方 min-h-11 ✓ 配線済だが Plan 生成 button だけ漏れ → ItemEditDialog の AI action group で 3 button のうち 1 つだけ高さ 32px / 他は 44px、tap-target 不揃い。本 button は iter513 (P0 AI 自動実行モード scope A) で追加されたため iter462-479 の mobile sweep 系列を通過していなかった。fix: `<Button size="sm" variant="outline" ...>` に `className="min-h-11"` を追加 (1 ファイル 1 行)、機能不変、shadcn 編集なし。これで AI action group 3 button が 44x44 で揃い、累計 **106 button mobile target 達成** (iter460-484 で 21 fix iter)。経路: 経路 B (script、ItemEditDialog 周辺の AI button group audit → 兄弟整合性で漏れを発見)。検証 codify: `scripts/explore-uiux-plan-generate-min-h-iter484.ts` で min-h-11 + size=sm 配線 / data-testid + aria-busy + aria-label 維持 / canGeneratePlan gate 維持 / 兄弟 button (decompose / research) min-h-11 維持 / visible label + icon 維持 = 6 軸 assert (6/6 OK)。typecheck/lint 緑 (warning baseline 2)。次 iter 候補: 他 iter513-vintage AI button (engineer-trigger / start-timer compact 等) の min-h-11 漏れ sweep、または別軸 a11y polish へ。
- ✅ [iter491 basics 1/1] 「item axis 1-word state」 シリーズ 10 弾目 (notification-activity)。`classifyNotificationActivityHint` (idle / mild / moderate / severe) + `formatNotificationActivityHintJa` (通知なし / 通常 / 多め / 異常) を `notification-activity.ts` に追加。判定: severe = sync-failure>=1 or 合計>=50、moderate = 合計>=20 or mention>=5、idle = 全 0。7 unit test 追加 (累計 22 件 PASS)。typecheck/lint clean。6 軸: 軸 4 漏れ防止 (sync-failure / flood の能動警告) ★ + 軸 1 可視化 (5 type counts を 1 chip に集約)。
- ✅ [iter490 refactor 1/1] 「同 shape の散在を 1 file に」 38 弾目: `parseDate` / `parseDateLike` (Date | string | null → Date | null fail-soft) が 3 file に重複していたのを `lib/date/iso.ts#parseDateOrNull` に集約。weekly-insight.ts / waiting-elapsed.ts / cycle-check-stats.ts で local 定義を `const X = parseDateOrNull` re-export に置換、cycle-check-stats は test の named import 維持で public alias keep。統合後 79 unit test PASS、typecheck/lint clean。
- ✅ [playwright-iter483 1/1] OperationBoardWidget forecast tactics 2 sub-section (Quick wins / 集中ブロック) を `<div>` から `<h3>` に昇格: src/components/workspace/operation-board-widget.tsx 行 156-203 の forecast tactics section (quickWins / focusBlocks) は `<div className="font-medium ..." id="op-board-quickwins-heading">` と `<div id="op-board-focus-heading">` で plain div headers (heading semantic 不在) → SR の見出しナビ navigation 不可。同 widget 内 Section component (行 308-320) は `<h3 id={headingId}>` で heading 化済 (iter427-432 で確立した「widget heading semantic 統一」 pattern)、forecast tactics 2 sub-section だけ漏れ。fix: 2 個の `<div>` を `<h3>` に昇格 (className / id / 中身 / 配色 はすべて変更なし、open/close tag のみ、1 ファイル ~4 行差分)。これで OperationBoardWidget 全 7 sub-section (forecast / Quick wins / 集中ブロック / 推奨 / 期限超過 / MUST / 予定) が h3 で統一、SR は heading nav で各セクションへ直接 jump 可。経路: 経路 B (script、dashboard / today widget audit → op-board の 2 sub-section が漏れ)。検証 codify: `scripts/explore-uiux-op-board-tactics-heading-iter483.ts` で quickWins h3 / focusBlocks h3 / 旧 plain div 消滅 / ul aria-labelledby 維持 / Section h3 pattern (iter427+) 維持 / Card region role + aria-label 維持 = 6 軸 assert (6/6 OK)。typecheck/lint 緑 (warning baseline 2)。次 iter 候補: dashboard-view.tsx の小 widget heading 漏れ / AI handoff phase chip / item-edit-dialog 内 sub-section / workflow-graph-canvas legend 等。
- ✅ [iter489 ai-automation 1/1] queue fluffy-5 polish (MUST 救済 chip 「次の一手」 substrate)。`recovery-plan.ts` に `pickTopRecoveryAction` (isApplicable=false → null、actions[0] (= rank 1) を返す) + `formatTopRecoveryActionJa` (chip 文言「次の一手: <kind label>」) 追加。5 unit test 追加 (累計 29 件 PASS)。typecheck/lint clean。これで dashboard MUST 救済 chip / Slack escalation / AI 朝 brief で 1 行表示可能。formatRecoveryPlanJa (3 件全部) と相補で「全部 vs 1 つだけ」 の使い分け。6 軸: 軸 4 漏れ防止 (MUST 救済 1 行で 即「やること」 が分かる) ★ + 軸 6 効率化。
- ✅ [playwright-iter482 1/1] CycleCheckStatsCard「遅延 / 期限超過」 cause chip group を h3 + role=group 化: src/components/pdca/cycle-check-stats-card.tsx 行 134-170 の cause section の "遅延 / 期限超過" header が `<div className="text-muted-foreground mb-1 text-xs">` で plain div (heading semantic 不在) → SR の見出しナビ navigation 不可、chip wrapper も role=group / aria-labelledby なしで chip 群が単発リスト扱い。SprintRetroWidget (sprint-retro-widget.tsx 行 162-216) は同 cause chip 構造で `<h3 id="sprint-retro-causes-heading">` + `<div role="group" aria-labelledby="sprint-retro-causes-heading">` pattern 確立済、CycleCheckStatsCard だけ pattern 漏れ。fix: `<div>遅延 / 期限超過</div>` を `<h3 id="cycle-check-cause-heading" className="text-muted-foreground mb-1 text-xs font-normal">遅延 / 期限超過</h3>` に + chip wrapper に `role="group" aria-labelledby="cycle-check-cause-heading"` 追加 (1 ファイル ~10 行差分、視覚 layout 不変)。経路: 経路 B (script、sprint widget audit 後の水平展開先として PDCA widget 系を選定 → CycleCheckStatsCard cause chip pattern 漏れを発見)。検証 codify: `scripts/explore-uiux-cycle-check-cause-group-iter482.ts` で h3 cycle-check-cause-heading 配線 / chip wrapper role=group + aria-labelledby / 旧 plain div header 消滅 / 3 SeverityChip testId 維持 / iter454 invariant (progressbar) 維持 / SprintRetroWidget pattern 整合 = 6 軸 assert (6/6 OK)。typecheck/lint 緑 (warning baseline 2)。次 iter 候補: CycleCheckStatsCard 内 Status 分布 dl / Lead time dl にも heading + group 統一、または別 widget (TeamCapacityPanel / OperationBoardWidget) に水平展開、または別画面 cycle (workflow-graph / archived-items-panel) へ。
- ✅ [iter488 basics 1/1] queue methodology GT-3 polish (Inbox 健全性 chip)。「item axis 1-word state」 シリーズ 9 弾目: `classifyInboxHealthHint` (idle / mild / moderate / severe) + `formatInboxHealthHintJa` (空 / 健全 / 滞留気味 / 要 process) を `inbox-process.ts` に追加。判定: severe = waiting-for>=5 or total>=100、moderate = total>=30 or immediate>=5、idle = total=0。7 unit test 追加 (累計 31 件 PASS)。typecheck/lint clean。6 軸: 軸 4 漏れ防止 (滞留 / waiting-for 多発 を 1 単語警告) ★ + 軸 1 可視化 (8 bucket count を 1 chip に集約)。
- ✅ [playwright-iter481 1/1] SprintRiskBoardWidget「担当者負荷」 table に caption + th scope=col 追加: src/components/sprint/sprint-risk-board-widget.tsx 行 151-184 の table は aria-labelledby="sprint-risk-load-heading" のみで `<caption>` 不在 + 4 個の `<th>` 全てに scope 属性なし → SR は table list 内 navigate しても列構成 (担当 / 件数 / MUST / 負荷) の context が見出し 1 文字列のみ、cell read 時の列 header announce が implicit (WCAG 1.3.1 Info and Relationships 違反)。fix: `<caption className="sr-only">担当者ごとの負荷一覧 ({loadEntries.length} 名 / 担当 / 件数 / MUST 件数 / 負荷スコア合計)</caption>` 追加 (visual には影響なし、動的件数 dynamic) + 4 個の `<th>` 全てに `scope="col"` 追加 (1 ファイル ~12 行差分)。iter380 (mock-entries-table) で確立した table a11y pattern を sprint widget 系列に水平展開、本 widget は graphical 5-tier (DataWidgetCard + SeverityChip + 4 列 table + h3 sprint-risk-load-heading) で a11y 完成。経路: 経路 B (script、sprint widget 系を audit → table scope 漏れを発見)。検証 codify: `scripts/explore-uiux-sprint-risk-table-iter481.ts` で caption sr-only 配線 / loadEntries.length dynamic / th scope=col 4 個 / 旧 scope 無し th 消滅 / table aria-labelledby 維持 / 4 列 header text 維持 = 6 軸 assert (6/6 OK)。typecheck/lint 緑 (warning baseline 2)。次 iter 候補: SprintRetroWidget の status chip group 強化、または別画面 cycle (pdca-panel CycleCheckStatsCard / dashboard-view DataWidgetCard table 系 / archived-items-panel) へ。
- ✅ [iter487 ai-automation 1/1] queue fluffy-6 polish (Plan critical path 解析 substrate)。`structured-plan.ts` に `computePlanCriticalPathMin` (依存 graph の dp で「自分まで最長 chain est_min 合計」 を Kahn 順序で前進、cycle → null) + `formatCriticalPathJa` (chip 文言: 線形 / 並列化機会あり / cycle で出し分け、最早 finish 形式 「2h30m (合計 5h との差 2h30m = 並列化機会あり)」) 追加。8 unit test 追加 (累計 45 件 PASS)。typecheck/lint clean。6 軸: 軸 6 効率化 (並列化機会の数値化) ★ + 軸 1 可視化 (最早 finish chip) + 軸 3 認知低減 (total / critical 差で「重さ」 把握)。
- ✅ [iter486 basics 1/1] queue WT-1 polish (連絡待ち workspace 集計 substrate)。`waiting-elapsed.ts` に `summarizeWaitingItems` (total + bySeverity (ok/warn/danger/muted) + oldestDays + dueRemindCount) と `formatWaitingSummaryJa` (chip 文言: escalate あり / リマインド推奨 / 健全 で出し分け) を追加。8 unit test 追加 (累計 25 件 PASS)。dashboard chip / Slack 通知 / WT-2 panel header の bind が直可能に。typecheck/lint clean。6 軸: 軸 1 可視化 (workspace 全 連絡待ちを 1 chip に集約) ★ + 軸 4 漏れ防止 (escalate / リマインド推奨 件数を能動表示)。
- ✅ [playwright-iter480 1/1] CalendarView の `ScheduleItemPicker` 行 MUST 表示を `MustBadge` 共通 component に統一: 行 71-73 の `<span className="text-[10px] font-bold text-rose-600">MUST</span>` inline 直書きが他 6 箇所 (today / inbox / personal-period / subtasks-panel / backlog / archived) で `MustBadge` 統一済の系列から漏れていた。inline 版は role="img" + aria-label="MUST タスク" / AlertTriangle warning icon / bg-red-100 ring 配色 が全て無く SR は「MUST」のみ読み上げ視覚も rose-600 文字色のみで他 6 箇所と不統一。fix: `MustBadge` を import して `it.isMust ? <MustBadge data-testid={`schedule-picker-must-${it.id}`} /> : null` に置換 (1 ファイル ~3 行差分)。これで MUST graphical 系列は **7 箇所統一 (6 → 7)**、scheduling 場面でも MUST タスクが視覚 + SR 両面で識別可。経路: 経路 B (script、scheduling 系を audit → schedule-item-picker.tsx の inline MUST を発見)。検証 codify: `scripts/explore-uiux-schedule-picker-must-iter480.ts` で MustBadge import / data-testid 配線 / 旧 inline 消滅 / iter419 invariant (dialog labelledby + Escape + 検索 aria-label) 維持 / MustBadge 正本の a11y 維持 = 6 軸 assert (6/6 OK)。typecheck/lint 緑 (warning baseline 2)。次 iter 候補: schedule-item-picker.tsx の他軸 (空状態 role=status / list aria-label / focus trap)、または別画面 cycle (workflow / sprint detail / pdca / archived items 等) へ。
- ✅ [iter485 refactor 1/1] 「同 shape の散在を 1 file に」 35 弾目: `extractFirstJsonObject` を `src/lib/json/extract-first-object.ts` に集約。iter533 (structured-plan) / iter538 (structured-review) で同 implementation が file 内に重複していた 29 行 × 2 を統合、AC-3 (inline AI 提案) / PDCA AI-1 等の今後の structured-\* helper でも再利用可。9 unit test (新規 file、code block / 複数 obj ケース追加)。統合後 80 unit test PASS (extract 9 + plan 37 + review 34)。typecheck/lint clean。
- ✅ [iter484 ai-automation 1/1] queue AI 分業 AC-2 polish (review chip / Slack 通知 substrate)。`structured-review.ts` に「最重要 issue 1 行抽出」 helper 3 つ: `pickWorstChecklistFinding` (fail 最優先 → warn → null) / `pickTopImprovement` (severity high > medium > low) / `formatWorstChecklistFindingJa` (chip 文言 ❌/⚠/null + 80 字超は ... 圧縮)。13 unit test 追加 (累計 34 件 PASS)。UI bind は別 iter。typecheck/lint clean。6 軸: 軸 4 漏れ防止 (最重要 issue chip 強調) ★ + 軸 6 効率化 (review → 次やること 1 つ 機械抽出)。
- ✅ [iter483 basics 1/1] queue fluffy-8 polish。Weekly Insight 4 状態 hint chip シリーズ 8 弾目: `classifyWeeklyInsightHint` (idle / mild / moderate / severe) + `formatWeeklyInsightHintJa` (活動なし / 健常 / 注意 / 異常) を `weekly-insight.ts` に追加、`makeHintLabelFormatter` で 1 行 export。WeeklyInsightWidget aria-label に hint 1 単語 + `data-weekly-insight-hint` attr。判定: severe = overdueSpike anomaly、moderate = lowCompletionDay or weekDelta ≤ -30%、idle = 両週 0 件。6 unit test 追加 (累計 31 件 PASS)。typecheck/lint clean。
- ✅ [iter482 ai-automation 1/1] queue fluffy-6 polish (Plan structured)。`structured-plan.ts` に staging UI / chip 用の追加 helper 3 つ: `topoSortPlanSteps` (Kahn 依存順 sort、cycle → null、同 layer は title alphabetical で deterministic) / `pickLongestStep` (est_min max 抽出、同点は title alphabetical 先頭) / `formatLongestStepJa` (chip 文言整形 45m / 1h / 2h30m / null → なし)。12 unit test 追加 (累計 37 件 PASS)。typecheck/lint clean。UI bind は別 iter。6 軸: 軸 6 効率化 (AI plan staging 流入 substrate) ★ + 軸 1 可視化 (bottleneck chip) + 軸 3 認知低減 (依存順 deterministic)。
- ✅ [iter481 basics 1/1] queue fluffy-8 polish。Weekly Insight widget に「今週 ベスト曜日」 chip を追加。新規 helper `pickBestDayInWeek` (max 完了曜日抽出、同点は早い曜日採用、0 件 → null) + formatter `formatBestDayJa` を `weekly-insight.ts` に追加、6 unit test 追加 (累計 25 件 PASS)。WeeklyInsightWidget header 右に ⭐ chip (bg-emerald)、aria-label に bestDay 情報も append。typecheck/lint clean。6 軸: 軸 5 やる気 (うまくいった日 highlight) ★ + 軸 1 可視化。
- ✅ [iter480 refactor 1/1] queue fluffy-8 weekly→widget UI bind 消化。`buildWeeklyInsight` 純 algorithm (iter531 で着地、19 unit test) を Dashboard view に配線。新規 `weekly-insight-widget.tsx` (~140 行、Card + 2 行サマリ + 7 mini bar + anomaly 帯)、tagIds は item.list で join されないため空配列で渡す (将来 hooks 拡張で stacked bar に展開可)、recharts 追加なしで CSS divs 内製、0/0 件は非表示で noise 削減。typecheck/lint clean、weekly-insight test 19 件 PASS。6 軸: 軸 1 可視化 (今週 vs 前週 7 mini bar) ★ + 軸 4 漏れ防止 (overdueSpike anomaly 能動表示) ★ + 軸 5 やる気 (緑 ↑ delta)。
- ✅ [playwright-iter479 1/1] mobile sweep 続編 (20 件目)。mock-timesheet の top nav 3 button (新規入力 / 入力一覧 / ログアウト) が size="sm" h-8=32px → fix: 1 ファイル ~3 行差分で各 Button に `className="min-h-11"` 追加。typecheck 緑。mobile sweep 累計 **105 button 達成** (iter460-479 で 20 fix iter)。
- ✅ [playwright-iter478 1/1] mobile sweep 続編 (19 件目)。3 ファイルの 5 inline button が size="sm" h-8=32px: pdca-panel (集計期間 30/90 切替 x 2) / item-dependencies-panel (dep-add-btn / dep-remove-{id} x 2) / backlog-view (編集 x 1) → fix: 3 ファイル ~5 行差分で各 Button に `className="min-h-11"` 追加。typecheck 緑。**mobile sweep 累計 102 button 達成** (iter460-478 で 19 fix iter)、**100 button 越え 達成 / 最大 mobile WCAG 2.5.5 AAA sweep 完了**。
- ✅ [playwright-iter477 1/1] mobile sweep 続編 (18 件目)。BudgetPanel の 3 inline button (上限変更 / cancel / 保存) が size="sm" h-8=32px、AI 月次コスト上限編集で使われる → fix: `budget-panel.tsx` の 3 Button に `className="min-h-11"` 追加。1 ファイル ~3 行差分。typecheck 緑。mobile sweep 累計 **97 button 達成** (iter460-477 で 18 fix iter)。
- ✅ [playwright-iter476 1/1] mobile sweep 続編 (17 件目)。IntegrationsPanel の 4 source card inline button (Pull / 無効化 / 削除 / 履歴) が size="sm" h-8=32px → fix: `integrations-panel.tsx` の 4 Button に `className="min-h-11"` 追加 (replace_all 2 pattern)。1 ファイル ~4 行差分。typecheck 緑。mobile sweep 累計 **94 button 達成** (iter460-476 で 17 fix iter)。
- ✅ [playwright-iter475 1/1] mobile sweep 続編 (16 件目)。CommentThread の 3 button (post / cancel 編集 / save 編集) が size="sm" h-8=32px → fix: `comment-thread.tsx` の 3 Button に `className="min-h-11"` 追加。1 ファイル ~3 行差分。typecheck 緑。mobile sweep 累計 **90 button 達成** (iter460-475 で 16 fix iter)。
- ✅ [playwright-iter474 1/1] mobile sweep 続編 (15 件目)。ActiveTimerPanel の 4 timer button (pause / resume / pip / stop) が size="sm" h-8=32px、計測中の重要操作で頻繁にタップされる → fix: `active-timer-panel.tsx` の 4 Button に `className="min-h-11"` 追加。1 ファイル ~4 行差分。typecheck 緑。mobile sweep 累計 **87 button 達成** (iter460-474 で 15 fix iter)。
- ✅ [playwright-iter473 1/1] mobile sweep 続編 (14 件目)。ItemEditDialog の 5 footer button (unarchive / archive / set-baseline / clear-baseline / save-as-template) が size="sm" h-8=32px → fix: `item-edit-dialog.tsx` の 5 Button に `className="min-h-11"` 追加 (既存 className との衝突は統合)。1 ファイル ~5 行差分。typecheck 緑。mobile sweep 累計 **83 button 達成** (iter460-473 で 14 fix iter)。
- ✅ [playwright-iter472 1/1] mobile sweep 続編 (13 件目)。WorkflowsPanel の 10 inline button が size="sm" h-8=32px、Workflow card の各種 action (実行 / 編集 / 有効化 / 履歴 disclosure / node preset / trigger preset) で頻繁に使われる → fix: `workflows-panel.tsx` の 10 Button に `className="min-h-11"` 追加 (replace_all 4 pattern + 1 webhook button)。1 ファイル ~10 行差分。typecheck 緑。mobile sweep 累計 **78 button 達成** (iter460-472 で 13 fix iter)。
- ✅ [playwright-iter471 1/1] mobile sweep 続編 (12 件目)。GoalsPanel の 7 inline button (KR 編集 / Goal expand 等) が size="sm" h-8=32px → fix: `goals-panel.tsx` の 7 Button に `className="min-h-11"` 追加 (replace_all 4 pattern)。1 ファイル ~7 行差分。typecheck 緑。mobile sweep 累計 **68 button 達成**。
- ✅ [playwright-iter470 1/1] mobile sweep 続編 (11 件目)。SprintsPanel の 12 inline button が size="sm" h-8=32px、Sprint card の各 action (期間編集 cancel/save / status change to active/completed/planning / 中止 / 振り返り生成 / Pre-mortem 生成 / Sprint デフォルト編集 cancel/save 等) で頻繁に使われる → fix: `sprints-panel.tsx` の 12 Button に `className="min-h-11"` 追加 (replace_all 5 pattern)。1 ファイル ~12 行差分。typecheck 緑。mobile sweep 累計 **61 button 達成** (iter460-470 で 11 fix iter)。経路: B (in-place fix)。
- ✅ [playwright-iter469 1/1] mobile sweep 続編 (10 件目)。decompose-proposals-panel の 9 inline button (cancel / accept / reject / redecompose x 2 / 編集 form submit / etc) が size="sm" h-8=32px や className="h-7 px-2" h-7=28px で mobile WCAG 違反、AI 分解結果を確認する複雑な panel で頻繁に使われる → fix: `decompose-proposals-panel.tsx` で 9 Button に `className="min-h-11"` 追加 (既存 className 衝突は merge)、h-7 → min-h-11 に変更。1 ファイル ~9 行差分。typecheck 緑 (className 重複 attribute 一旦エラーになったので統合 fix 済)。mobile sweep 累計 **49 button 達成** (iter460-469 で 10 fix iter)。経路: B (in-place fix)。
- ✅ [playwright-iter468 1/1] mobile sweep 続編 (8 件目)。ArchivedItemsPanel の archive-restore-${item.id} button が size="sm" h-8=32px → fix: `archived-items-panel.tsx` の Button に `className="min-h-11"` 追加。1 ファイル ~1 行差分、視覚 layout は height 32→44px のみ。typecheck 緑。これで mobile sweep 累計 **40 button 達成**。経路: B (in-place fix)。
- ✅ [playwright-iter467 1/1] iter460-466 mobile sweep の続編 (7 件目)。AI 操作 button (ItemDecomposeButton / ItemResearchButton) が size="sm" h-8=32px、Backlog row / Today row 等で頻繁に表示される → fix: 2 ファイル ~2 行差分で各 Button に `className="min-h-11"` 追加。`scripts/explore-uiux-mobile-item-action-buttons-iter467.ts` で source-side regex assert 2 件 codify。typecheck 緑。これで mobile sweep 累計 **39 button 達成**。経路: B (script、source-side regex)。次 iter 候補: archive panel restore button / decompose proposal action button / sprint card period 編集 button の audit。
- ✅ [playwright-iter466 1/1] iter460-465 mobile sweep の続編 (6 件目)。BulkActionBar の 3 inline action button (bulk-status-${key} / bulk-delete / bulk-clear) が size="sm" h-8=32px、複数 item 選択時の下部 sticky bar で頻繁に使われる → fix: `bulk-action-bar.tsx` の 3 Button に `className="min-h-11"` 追加。1 ファイル ~3 行差分。`scripts/explore-uiux-mobile-bulk-action-bar-iter466.ts` で source-side regex assert 3 件 codify。typecheck 緑。これで mobile sweep 累計 **37 button 達成** (iter460-466 で 6 fix iter)。経路: B (script、source-side regex)。次 iter 候補: kanban card edit button / today row StartTimerButton / archive panel restore button / decompose proposal action button の audit。
- ✅ [playwright-iter465 1/1] iter460-464 mobile sweep の続編 (4 件目)。source-side audit で primary submit button 3 件が `min-h-11` を持たず default Button h-9=36px (mobile WCAG 違反): goals-panel goal-create-btn / kr-add-btn-${goalId} / templates-panel template 作成 button → fix: 2 ファイル ~3 行差分で各 Button に `className="min-h-11"` 追加。`scripts/explore-uiux-mobile-create-buttons-iter465.ts` で source-side regex assert 3 件 codify。typecheck 緑。これで mobile sweep 累計 **34 button 達成** (home 9 + sub-page 8 + utility 4 + view-switcher 9 + sprint-create 1 + goal/kr/template 3)。経路: B (script、source-side regex)。次 iter 候補: BulkActionBar inline button / kanban card edit / today/inbox row inline button (StartTimerButton 等) の audit。
- ✅ [playwright-iter464 1/1] mode-M runtime audit on /sprints page で sprint 作成 button (`sprint-create-btn`) が 50x32px (default Button size 32px height)、iPhone SE で WCAG 2.5.5 AAA 違反 → fix: `sprints-panel.tsx` の Button に `className="min-h-11"` 追加。1 ファイル ~1 行差分。`scripts/explore-uiux-mobile-sprints-page-iter464.ts` で BEFORE 50x32 warning → AFTER 50x44 OK 確認。typecheck 緑。これで mobile sweep 累計 **31 button 達成**。経路: B (script、runtime mobile audit)。次 iter 候補: sprints-panel 内 sprint card 内 inline action button (period 編集 / Retro / Pre-mortem 等) の audit、その他 size="sm" 計 ~100 件の系統的 sweep。
- ✅ [playwright-iter463 1/1] mode-M runtime audit で workspace home の view-switcher 9 button (Today / Inbox / Kanban / Backlog / Gantt / Dashboard / 日次 / 週次 / 月次) も `<Button size="sm">` で h-8=28px、iPhone SE で **0/9 pass 44x44** という WCAG 2.5.5 AAA 違反 (iter460-462 mobile sweep 続編 3 件目) → fix: `items-board.tsx` で `replace_all` により 9 Button に `className="min-h-11"` 一括追加。1 ファイル ~1 行差分 (replace_all で 9 箇所)、視覚 layout は height 28→44px のみ。`scripts/explore-uiux-mobile-view-switcher-iter463.ts` で BEFORE 0/9 → AFTER 9/9 を runtime audit で確認。typecheck 緑。これで mobile sweep 累計 **30 button 達成** (home 9 + sub-page 8 + utility 4 + view-switcher 9)。経路: B (script、runtime mobile audit)。次 iter 候補: BulkActionBar inline buttons / kanban card edit button 等の mobile tap target audit。
- ✅ [playwright-iter462 1/1] iter460 / iter461 で workspace nav 17 button を 44x44 化した sweep の続編。runtime mobile audit で workspace home の utility button 4 件も violation 発見: HeartbeatButton 72x28 (h-8 size="sm"), NotificationBell 32x32 (size="icon"), NotificationPreferences 32x32, ThemeToggle 32x32 → fix: 4 ファイル × 各 1 行で `className="min-h-11"` (HeartbeatButton) / `className="min-h-11 min-w-11"` (3 icon button)。視覚 layout は size 32→44px のみ、shadcn 編集なし。`scripts/explore-uiux-mobile-utility-buttons-iter462.ts` で runtime audit BEFORE 4 warning → AFTER 4 OK。typecheck 緑。これで workspace home 全 13 button + sub-page 8 button = 累計 **21 button mobile 44x44 達成**。経路: B (script、runtime mobile audit)。次 iter 候補: workspace 内 inline button 群 (BulkActionBar / kanban card / today row 等) の mobile tap target audit。
- ✅ [playwright-iter461 1/1] iter460 で workspace home の 9 nav button を 44x44 化したのと同 pattern で workspace sub-page 8 件 (archive / sprints / integrations / pdca / time-entries / workflows / goals / templates) の "← Workspace" back button も h-8=32px で WCAG 2.5.5 AAA 違反 (iter460 で home のみ fix され sub-page 取り残し) → fix: 8 ファイル × 各 1 行 で `className="min-h-11"` 追加。これで全 9 page (home + 8 sub-page) で nav button 44px tap target 達成。視覚 layout は height 32→44px のみ、shadcn 編集なし。`scripts/explore-uiux-mobile-subpage-back-iter461.ts` で source-side regex assert 9 件 (8 sub-page + home iter460 invariant) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: mock-timesheet pages も同 pattern audit、その他 size="sm" を含む button 群の mobile sweep。
- ✅ [playwright-iter460 1/1] **mode-M runtime audit で重大 bug 発見**: workspace home (/<wsId>) の page nav 9 button (Goals / Sprints / PDCA / Templates / Workflows / API 連携 / Time Entries / Archive / 一覧) が `<Button size="sm">` で h-8=32px、iPhone SE で **0/9 pass 44x44** という WCAG 2.5.5 AAA / Apple HIG 44pt 違反 (iter287 / iter409 / iter412-414 で他 page button は 44px 化済、workspace home はこの sweep から取り残されていた) → fix: `app/(workspace)/[workspaceId]/page.tsx` の 9 Button に `className="min-h-11"` 追加 (size="sm" 維持で text size はそのまま、height のみ 44px に enforce)。1 ファイル ~1 行 replace_all 差分、視覚 layout は height 32→44px のみ。`scripts/explore-uiux-mobile-workspace-home-iter460.ts` で BEFORE 0/9 → AFTER 9/9 を runtime audit で確認。typecheck 緑、scrollWidth=320 ≤ 375 維持。経路: B (script、runtime mobile audit)。**runtime audit はやはり重要 — source-side だけでは捕捉できない mobile-only bug を発見**。
- ⏳ [playwright-iter459 mode-M runtime audit] /signup page を iPhone SE (375x667) で audit、iter458 /login と同 pattern: scrollWidth=320 ≤ 375 OK / submit button 256x44 OK / login link 72x44 OK / screenshot /tmp/uiux-mobile-signup-iter459.png 保存。iter409 / iter412 fix runtime 確認、bug 見つからず。fix なし、runtime audit script のみ (106 行)。
- ⏳ [playwright-iter458 mode-M runtime audit] /login page を iPhone SE (375x667) で実 chromium 起動、4 invariant 確認: (1) documentElement.scrollWidth=320px ≤ 375px (横スクロールなし) OK、(2) submit button: 256x44 (iter409 で h-11 設定済) OK、(3) signup link: 100x44 (iter412 で min-h-11 + px-2 設定済) OK、(4) screenshot を /tmp/uiux-mobile-login-iter458.png に保存。iter409 / iter412 の fix が runtime で正しく機能、bug 見つからず。`scripts/explore-uiux-mobile-login-iter458.ts` で 117 行 audit script、source 変更なし、fix なし。
- ⏳ [playwright-iter457 探索のみ + sanity check] codebase 全体を一巡 audit。typecheck 緑、lint 1 既存 warning (TanStack table) のみ、unit test 4 file 60 件全 pass。`<form>` 11 件全て aria-label/aria-labelledby + aria-busy 配備、`required` 属性は全て `aria-required` 併記、icon-only button は全 `aria-label` 完備、`<details>` の summary は id 配線済、`<table>` 全 caption (sr-only) あり、`<ul>` 一覧は aria-label/aria-labelledby 配備 (iter427-453 で 19 件統一)、6 view で dueDate `<time dateTime>` 統一、6 progressbar に aria-valuetext + severity 連動、role="status" anti-pattern 3 件解消、1Hz aria-live flood 2 件解消、WAI-ARIA radio + roving tabindex 1 件、WCAG 1.4.1 color-only severity 3 件解消、WAI-ARIA dialog 1 件、aria-label 重複 prefix 1 件解消、TaskChute mobile compact 1 件、popover heading 統一 2 件、aria-current button 4 件、Calendar Loader role 2 件、mock-submit ref SR announce 1 件、ItemDependenciesPanel direction context 1 件、calibration chip aria-label 集約 2 件 — 計 38 fix を 2 時間で landed。残時間で active 探索を継続するも 顕著な gap は枯渇。次セッションは 別 dimension (focus management / contrast / animation reduce / runtime mobile / flicker) を audit 推奨。
- ⏳ [playwright-iter456 探索のみ] BudgetPanel trend chip / DashboardChip / WorkflowsPanel dialogs / CommentItem / ItemDecomposeButton / ItemResearchButton / HeartbeatButton / NotificationBell / NotificationPreferences / WorkspaceHeader / GlobalShortcuts を追加 audit、aria-label / aria-busy / aria-haspopup / aria-expanded / aria-controls / role="alert" / role="status" 適切配線、`required` 属性は全て `aria-required` 併記、icon-only button は全て `aria-label` 完備、`<details><summary>` も id 配線済 → bug 見つからず。loop は 38 fix 達成後の saturation phase に入った。次 iter は別カテゴリ (focus management / contrast audit / animation reduce / runtime mobile audit) で aria 以外の側面を audit 推奨。
- ⏳ [playwright-iter455 探索のみ] LoginForm / SignupForm / TimeEntriesTable / ArchivedItemsPanel / BacklogView / GoalsPanel KR / SprintsPanel / BudgetPanel / WorkflowGraphCanvas / WorkflowsPanel / KeybindingsHelpModal を audit、いずれも aria-label / aria-required / aria-busy / role="progressbar" + aria-valuetext / aria-controls + aria-expanded / form aria-labelledby / tab role / `<caption>` (sr-only) / `<time dateTime>` 等が完備済 — bug 見つからず。iter417-454 で 38 fix を landed させた結果、major a11y / UX gap は exhaustively swept された。次 iter 候補: runtime test (mobile / flicker) を試す、items-board view-switcher を toolbar pattern にする (現状 aria-pressed group は acceptable)、または別カテゴリ (focus management / contrast / animation reduce) の audit。
- ✅ [playwright-iter454 1/1] CycleCheckStatsCard (iter550 PDCA P-5 widget UI) の完了率 progress bar が severity (ok/info/warn/danger) を fill 色のみで伝えていた WCAG 1.4.1 違反 (iter424 SprintRetroWidget と同問題、新着地 PDCA widget に水平展開) → fix: `cycle-check-stats-card.tsx` に `severityLabelJa` helper (順調 / 良好 / 注意 / 要対策 4 段階) + progressbar に `aria-valuetext={rate}% ({sevLabel})` 追加 + aria-label 簡潔化 ("完了率")。1 ファイル ~10 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-cycle-check-progress-aria-iter454.ts` で source-side regex assert 7 件 (4 severityLabelJa / aria-valuetext / aria-label 簡潔化 / iter424 sprint-retro invariant) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: budget-panel progress bar / goals-panel KR progressbar 2 件 / sprints-panel progressbar の同 pattern 適用、 各種 progressbar に aria-valuetext で severity を載せる sweep。
- ✅ [playwright-iter453 1/1] workflows-panel.tsx の 2 ul (workflows-list / wf-node-runs) に aria-label が無く SR list nav で context 不在 (iter427-452 widget heading 統一 18-19 件目水平展開) → fix: 各 ul に `aria-label="Workflow 一覧 ${list.data!.length} 件"` / `aria-label="Workflow node 実行履歴 ${rows.length} 件"`。1 ファイル ~6 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-workflows-list-aria-iter453.ts` で source-side regex assert 2 件 codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: 残り ul は内側 nested / chip 系のみ。Major widget heading sweep 19 件達成。
- ✅ [playwright-iter452 1/1] DashboardView の MUST Item 一覧 `<ul>` に aria-label/aria-labelledby が無く、SR list nav で件数のみで context が伝わらない (iter427-451 widget heading 統一 pattern 17 件目水平展開、これで widget list heading 統一 17 件達成) → fix: `dashboard-view.tsx` の `<ul>` に `aria-label="MUST Item 一覧 ${s.items.length} 件"`。1 ファイル ~1 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-dashboard-must-list-aria-iter452.ts` で source-side regex assert 1 件 codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: 残る ul は dashboard 内 nested list / 個別 chip 系。Major widget list heading sweep 完了、次は別 pattern (focus-visible / form-validation / mobile audit) に切替えるか runtime test を試す。
- ✅ [playwright-iter451 1/1] iter427-450 と続けてきた widget list heading 統一 pattern の最終 sweep。3 ul で aria-label/aria-labelledby 不在: (1) sprint-swimlane-disclosure lane 一覧 ul、(2) operation-board-widget Quick wins ul、(3) 同 集中ブロック ul → fix: swimlane に `aria-label="Sprint Swimlane lane 一覧 N 件"`、operation-board の親 div に id (`op-board-quickwins-heading` / `op-board-focus-heading`) + ul に aria-labelledby。2 ファイル ~6 行差分、視覚 layout 不変、shadcn 編集なし。typecheck 緑。これで widget list heading 統一 16 件達成 (累計): comment / sprints / activity-log / integrations / decompose-proposals / swimlane / quick-wins / focus-blocks / dependencies + 過去 iter427/428/431/432/433/438/447。`scripts/explore-uiux-board-widget-list-aria-iter451.ts` で source-side regex assert 3 件 (swimlane ul / 2 operation-board ul) を codify。経路: B (script、source-side regex)。次 iter 候補: dashboard-view 内 残 ul ~6 ヶ所、TaskChute view の `<ol>` の aria-label 検証、CycleCheckStatsCard widget の SR pattern。
- ✅ [playwright-iter450 1/1] ItemDependenciesPanel の Section component (前提条件 / 後続タスク / 関連 の 3 direction) で `<h3>` に id 不在 + `<ul>` に aria-labelledby 不在 → SR list nav で「list N items」と件数のみで direction context が伝わらない (iter427 / iter428 / iter438 / iter447-449 と同 pattern 13 件目水平展開) → fix: `item-dependencies-panel.tsx` で direction 別 unique id `dep-section-${direction}-heading` を計算、`<h3 id>` + `<ul aria-labelledby>` 配線。1 ファイル ~5 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-dependencies-list-aria-iter450.ts` で source-side regex assert 3 件 (headingId 計算 / h3 id / ul aria-labelledby) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: dashboard-view 内 ul (1372 line ほか) / sprint-swimlane-disclosure ul / operation-board-widget 残 ul / subtasks-panel `<ol>` 等の最終 sweep。
- ✅ [playwright-iter449 1/1] iter427 / iter428 / iter438 / iter447 / iter448 と続けてきた widget list heading 統一 pattern の sweep。3 panel の `<ul>` で aria-label が無く SR list nav で件数のみ → fix: (1) `activity-log.tsx`: `aria-label="Activity 履歴 ${data.length} 件"`、(2) `integrations-panel.tsx`: `aria-label="API 連携 source 一覧 ${list.data!.length} 件"`、(3) `decompose-proposals-panel.tsx`: `aria-label="AI 分解提案 一覧 ${list.length} 件"`。3 ファイル × 各 1 行 = ~12 行差分、視覚 layout 不変、shadcn 編集なし。typecheck 緑。これで iter448 sprints-panel と合わせて 4 file で sweep 完了 (累計 widget list heading 統一 12 件達成、connected: comment / sprints / activity-log / integrations / decompose-proposals + 過去 iter427/428/431/432/433/438)。`scripts/explore-uiux-multi-list-aria-iter449.ts` で source-side regex assert 4 件 (3 panel + iter448 invariant) を codify。経路: B (script、source-side regex)。次 iter 候補: dashboard-view 内 残 ul / item-dependencies-panel ul の aria-label / 加えて subtasks-panel の <ol> 検証。
- ✅ [playwright-iter448 1/1] SprintsPanel の `<ul data-testid="sprints-list">` に aria-label/aria-labelledby が無く、SR list nav で件数のみで context が伝わらない (iter447 CommentThread と同 pattern 9 件目水平展開) → fix: `sprints-panel.tsx` の `<ul>` に `aria-label="Sprint 一覧 ${list.data.length} 件"` を配線。1 ファイル ~6 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-sprints-list-aria-iter448.ts` で source-side regex assert 2 件 (ul aria-label 配線 / iter447 comment-thread invariant) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: integrations-panel sources-list / activity-log / decompose-proposals-panel / item-dependencies-panel / dashboard-view 内 `<ul>` 群の aria-label sweep。
- ✅ [playwright-iter447 1/1] CommentThread の `<ul>` comment list が aria-label/aria-labelledby を持たず、SR の list nav で件数のみで context (= "コメント一覧") が伝わらない (iter427 / iter428 / iter438 と連続 8 件目 widget list heading 統一) → fix: `comment-thread.tsx` の `<ul>` に `aria-label="コメント一覧 ${comments.length} 件"` を配線、動的 count で「コメント一覧 5 件」 と聞き取れる。1 ファイル ~3 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-comment-thread-list-aria-iter447.ts` で source-side regex assert 2 件 (ul aria-label 配線 / 旧 ul 不在 regression) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: CycleCheckStatsCard (iter550 PDCA P-5 widget UI 着地) の SR pattern audit、subtasks-panel `<ol>` の aria-label 配線、archived-items `<table>` の aria-labelledby 検証。
- ✅ [playwright-iter446 1/1] StartTimerButton の `isMine` (この Item で計測中) inline display が `<div role="status" aria-live="polite">` で children に formatElapsed が 1 秒 tick で更新されるため、SR queue が「Timer 00:01 計測中…」「Timer 00:02 計測中…」を毎秒 announce する 1Hz anti-pattern (iter426 ActiveTimerPanel と同問題) → fix: `start-timer-button.tsx` で `role="status" aria-live="polite"` → `role="img" aria-label="集約 text"` に切替え (例: "「Foo」を計測中 (経過 1:23、右下 panel で停止)")、3 visible children に `aria-hidden="true"` を付与し AT 上は aria-label が単一 source。1 ファイル ~9 行差分、視覚 layout 不変。`scripts/explore-uiux-start-timer-aria-live-iter446.ts` で source-side regex assert 4 件 (旧 role="status" + aria-live 不在 regression / role="img" + aria-label 配線 / aria-hidden span カウント / iter426 active-timer-panel invariant consistency) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: time-entries-panel の row 単位 a11y、estimate-bias-insight UI の SR pattern audit、heartbeat-button やその他 1Hz 系 anti-pattern の最終 sweep。
- ✅ [playwright-iter445 1/1] TimeEntriesTable の row workDate cell が `<td>{e.workDate}</td>` で raw ISO を plain text 表示、SR は plain text と読み上げるが date semantic として interpret しない (iter435-437 / iter439 と同 pattern 6 view 目水平展開、time entry + dueDate/scheduledFor 表示の `<time dateTime>` 統一達成 6 view) → fix: `time-entries-table.tsx` の cell を `<time dateTime={e.workDate}>{e.workDate}</time>` に格上げ、className / visible text 完全維持。1 ファイル ~5 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-time-entries-time-iter445.ts` で source-side regex assert 7 件 (<time> 配線 / 旧 plain td 不在 regression / 5 既存 view consistency) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: estimate-bias-insight (iter267) の SR pattern audit、RecoveryPlanSection (iter549 で ItemEditDialog wire-up) の a11y、top-items-by-time-chip の a11y。
- ✅ [playwright-iter444 1/1] MockSubmitForm の送信成功直後に表示される last external_ref が toast.success (sonner) だけが SR に届き、toast 消えた後の permanent display は SR に再 announce されず、SR ユーザは「送信成功 + external_ref 貯まる」経過を辿りにくい → fix: `mock-submit-form.tsx` の `<p data-testid="mock-last-ref">` に `role="status" aria-live="polite"` を追加、lastRef state 変更のたび「last external_ref: {ref}」を 1 回 announce。1 ファイル ~3 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-mock-submit-last-ref-iter444.ts` で source-side regex assert 2 件 (role="status" + aria-live 配線 / iter405 form landmark aria-label invariant) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: SprintRetroWidget / SprintRiskBoardWidget が sprints-panel に wire-up された (iter547 / iter548) ので runtime check 候補、sprints-panel の sprint card row 単位 a11y、PDCA cycle phase widget UI 着地時の同 pattern audit。
- ✅ [playwright-iter443 1/1] iter545 wire-up された InboxView の GTD bucket count summary chip 群が `role="status"` (live region) を使っており inbox 変更のたび SR 再 announce する anti-pattern (iter423 SeverityChip と全く同問題) → SR が「GTD 分類: 2 分以内 3 件、Project 1 件、次の action 5 件」を毎回 announce → 操作 noise → fix: `inbox-view.tsx` で `role="status"` → `role="group"` に変更、aria-label は集約 description として維持。1 ファイル ~6 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-gtd-summary-role-iter443.ts` で source-side regex assert 3 件 (role="group" + aria-label 配線 / 旧 JSX role="status" 不在 regression / iter423 severity-chip pattern consistency) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: iter546 で 4 段階 severity に拡張された operation-board forecast UI consume 時の a11y、PDCA cycle phase widget UI 着地時の同 pattern audit、recovery-plan widget 着地時の a11y、`role="status"` 全 file audit (live region 誤用パターン他にあるか)。
- ✅ [playwright-iter442 1/1] Sprint Swimlane Disclosure の lane bar 群が `role="img" aria-label="bar 数 N 件"` だけで bar 競合状態を **色のみ (blue=normal / amber=conflicted)** で伝えていた WCAG 1.4.1 違反 → SR ユーザは件数だけ聞き取り 5 件中何件が競合かが伝わらない、色覚特性ユーザも amber 強調 bar 見逃し → fix: `sprint-swimlane-disclosure.tsx` で aria-label 動的生成 (競合数 = `row.items.filter((it) => it.conflicted).length`) → `bar 数 N 件 (うち競合あり K 件)` 形式 (競合 0 件は旧文言維持で冗長省略)。1 ファイル ~9 行差分、視覚 layout / 色 全て不変、shadcn 編集なし。`scripts/explore-uiux-swimlane-conflict-aria-iter442.ts` で source-side regex assert 3 件 (競合 count 計算 / "うち競合あり" 文言 / 競合 0 件 fallback) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: GTD bucket count summary chip 群 (iter545 で着地、InboxView に wire-up) の SR/keyboard a11y、PDCA cycle-check severity (iter547) UI consume 時の a11y、Dashboard chip 群の各 title= は既に DashboardChip で aria-label 集約済 (audit 完了)。
- ✅ [playwright-iter441 1/1] QuickAdd preview の見積 calibrated chip が iter440 ActiveTimerPanel と全く同 title= mouse-only 問題: `title="直近の見積精度に基づく校正値 (中央値 0.83×)"` が SR / keyboard 不可達、aria-label には校正後 minutes だけで factor 不在 → fix: `quick-add.tsx` で `title=` 削除、`aria-label` に factor 詳細集約 (例: "校正後 39分 (+9分、中央値 0.83× 補正)")、role="img" 追加 (iter98 / iter417 / iter423 / iter440 と同 pattern)。1 ファイル ~3 行差分、視覚 layout 不変。`scripts/explore-uiux-quick-add-calibration-aria-iter441.ts` で source-side regex assert 4 件 (旧 title 不在 regression / aria-label factor 集約 / role="img" / iter440 active-timer pattern consistency) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: ItemEditDialog 内 baseline button title= も同 mouse-only pattern、recovery-action-kind severity (iter545) UI consume 時の a11y、Dashboard chip 内の title= 系 audit。
- ✅ [playwright-iter440 1/1] ActiveTimerPanel の見積 calibrated chip と itemTitle span で `title=` 系 mouse-only 情報が SR / keyboard 不可達: (1) calibrated chip `title="中央値 0.83×"` calibration factor source が aria-label にも入っていない、(2) itemTitle span `title={itemTitle ?? ''}` で null 時に空文字 title を render → fix: `active-timer-panel.tsx` で (1) calibrated chip `title=` 削除、`aria-label` に factor 詳細集約 (例: "校正後 39分 (+9分、中央値 0.83× 補正)") + role="img" 追加、(2) itemTitle span `title={itemTitle || undefined}` (null 時 title 属性自体 render しない)。1 ファイル ~3 行差分、視覚 layout 不変。`scripts/explore-uiux-active-timer-calibration-aria-iter440.ts` で source-side regex assert 6 件 (旧 calibrated title 不在 regression / aria-label factor 集約 / role="img" / itemTitle title 修正 / 旧 itemTitle title regression / iter426 invariant 維持) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: QuickAdd の calibrated chip も同 title= pattern、TaskChute view (iter543 で着地した cumulative-remaining ticker) の SR pattern 検証、GTD bucket severity (iter544) UI consume 時の a11y。
- ✅ [playwright-iter439 1/1] QuickAdd preview chip の scheduledFor が `<span title=ISO aria-label>` で **mouse hover only** title 経由で ISO 日付が伝わらない、`<span>` 自体は date semantic を持たない (iter435 / iter436 / iter437 と同 pattern 5 view 目水平展開、これで dueDate/scheduledFor 表示の <time dateTime> 統一達成 5 view) → fix: `quick-add.tsx` の `<span title>` を `<time dateTime={preview.scheduledFor} aria-label="予定 ${ISO} ${dueTime}">` に格上げ、className / visible text 完全維持、title 属性削除 (aria-label が単一 source)。1 ファイル ~5 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-quick-add-time-iter439.ts` で source-side regex assert 7 件 (<time> 配線 / 旧 <span title> 不在 regression / visible formatFriendlyDate 維持 / 4 既存 view consistency: command-palette / backlog / today / personal-period) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: ActiveTimerPanel の estimate calibration chip も同 title= pattern (`title={直近の見積精度に基づく校正値 ...}`)、QuickAdd の calibrated chip も同 pattern、ItemEditDialog 内 baseline display も同 title= 系、AiHandoffPhaseChip (iter542) の 5 phase severity の SR 検証。
- ✅ [playwright-iter438 1/1] TeamCapacityPanel (iter477 で着地、チームメンバー 余裕時間 widget) の member list `<ul>` が aria-label/aria-labelledby を持たず、SR ユーザは list nav で「list N items」と件数のみ聞き取り、context が伝わらなかった (iter427 / iter428 / iter431 / iter432 と連続 7 件目の widget heading 統一) → fix: `team-capacity-panel.tsx` の `<summary>` に `id="team-capacity-summary"` 追加、`<ul>` に `aria-labelledby="team-capacity-summary"` 配線、これで SR は「チームメンバー 余裕時間 (今日 / 今週) リスト N 件」と読み上げ可。1 ファイル ~3 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-team-capacity-list-aria-iter438.ts` で source-side regex assert 3 件 (summary id 配線 / ul aria-labelledby / section aria-label regression detector) を codify。typecheck 緑。経路: B (script、source-side regex)。次 iter 候補: HeartbeatButton の状態 announce / WorkspaceHeader の sub-heading audit / structured-review consumer chip (iter542) の a11y / quick wins + 集中ブロック (iter541) widget UI の SR pattern。
- ✅ [playwright-iter437 1/1] PersonalPeriodView (Daily/Weekly/Monthly view) の row dueDate が `<span>raw ISO</span>` で SR は plain text と読み上げるが date semantic として interpret しない、aria-label も無し (iter435 / iter436 と同 pattern 4 view 目水平展開) → fix: `personal-period-view.tsx` の `<span>` を `<time dateTime={it.dueDate} aria-label="期限 ${ISO}">` に格上げ、className 維持、visible text 不変。1 ファイル ~7 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-personal-period-time-iter437.ts` で source-side regex assert 5 件 (<time dateTime + aria-label> 配線 / 旧 <span> 不在 regression / iter435/iter436 3 view consistency) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: subtasks-panel dueDate audit、ItemEditDialog dueDate display audit、Gantt view bar tooltip の `title=` も `<time>` 化候補 (大きい refactor)、Dashboard chip 内 ISO 表示 audit。
- ✅ [playwright-iter436 1/1] iter435 で command-palette を `<time dateTime>` に格上げした後、他 view にも水平展開: backlog-view 期限 column が `<span title={v} aria-label="期限 ${v}">` で title= は mouse-only、today-view 期限 row は外側 `<span title={dueDate}>` + 内側 `<time dateTime>` の冗長 (2 source 矛盾の risk) → fix: (1) `backlog-view.tsx` の cell を `<time dateTime={v} aria-label>` に格上げ、(2) `today-view.tsx` 外側 span から `title=` 削除 (内側 `<time>` が単一 source、aria-label は維持)。これで 3 ファイル (command-palette / backlog / today) で dueDate 表示が `<time dateTime + aria-label + formatFriendlyDate>` の 1 pattern に統一。2 ファイル ~6 行差分、視覚 layout 不変。`scripts/explore-uiux-due-date-time-element-iter436.ts` で source-side regex assert 5 件 (backlog <time> 配線 / 旧 backlog span 不在 regression / today title 削除 / today <time> 維持 invariant / iter435 command-palette consistency) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: PersonalPeriodView 期限表示 audit、Dashboard chip 内 dueDate 表示 audit、subtasks-panel 期限 chip audit、ItemEditDialog dueDate display audit (ItemRow より大きい dueDate display source)。
- ✅ [playwright-iter435 1/1] CommandPalette (Cmd+K) の item 検索 row で dueDate が `<span title="期限 ${ISO}">` で **mouse hover only** 経由でしか ISO 日付が伝わらず、SR / keyboard ユーザに不可視。visible 表示は friendly 形式 ("明日" / "4/30 (木)") で ISO 日付見えず、非 native 日本語 SR ユーザは日付の文脈把握不能 → fix: `command-palette.tsx` の `<span title>` を `<time dateTime={item.dueDate} aria-label="期限 ${ISO}">` に格上げ、visible text (formatFriendlyDate 出力) は不変、SR は aria-label で "期限 2026-05-01" を聞き取れる + dateTime 属性で SR の date interpretation が安定 (HTML5 time semantic)。1 ファイル ~3 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-command-palette-time-iter435.ts` で source-side regex assert 3 件 (<time dateTime + aria-label> 配線 / 旧 <span title> 不在 regression / visible formatFriendlyDate 維持) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: 同 `<span title=>` パターンを他 view (today-view 期限日表示 / dashboard chip) で全 audit、items-board view-switcher の WAI-ARIA tablist pattern (現状は role="group" + aria-pressed の toggle group、tablist にすると arrow nav 可能になるが大きい refactor)、structured-review schema (iter538) 消費 widget の a11y。
- ✅ [playwright-iter434 1/1] **mode-M (Mobile audit)**: TaskChute 1 列 timeline (iter519) の priority chip が visible text に "優先度: 最優先 (p1)" (10 char @10px font ≈ 90px 幅) を出していたため、mobile 375px 幅で row が圧迫され title flex-1 truncate が常時発火 (非 flex 要素 + gap = ~340-370px / title 残 ~5-35px) → fix: `taskchute-view.tsx` の chip visible text を `P{item.priority ?? 4}` に compact 化 (3 char ~24px、~66px 解放)、aria-label に priorityLabel フル text を温存 (SR 読み上げ劣化なし)、role="img" 維持。1 ファイル ~10 行差分 (コメント込み)、視覚 layout (chip 高さ / 色 / position) は維持。`scripts/explore-uiux-mobile-taskchute-priority-chip-iter434.ts` で source-side regex assert 3 件 (P{n} compact / 旧 priorityLabel visible 不在 regression / role="img" + aria-label 維持 iter417 invariant) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。runtime mobile audit は workspace seed + dev server 安定 を要するため別 iter に。次 iter 候補: TodayView / InboxView / Backlog row の同 chip pattern 横展開、StatusBadge mobile width audit、MustBadge visible text audit。
- ✅ [playwright-iter433 1/1] OperationBoardWidget「今日の作戦盤」 widget の `Section` コンポは 4-5 sub-section (推奨第 1 タスク / 期限超過 / 今日の MUST / 今日の予定) を持つが、header が `<div>` で heading semantic 不在 → SR ユーザは heading shortcut で sub-section に直行不可 (iter427 / iter428 / iter430 / iter431 / iter432 と連続 6 件目の widget heading 統一) → fix: `operation-board-widget.tsx` の Section コンポで (1) 内側 `<div>` を `<h3 id={headingId}>` に降格 + label を normalize した unique id 生成 (`[^a-zA-Z0-9]` → `-`)、(2) 外側 `<div>` (section wrapper) に `role="group" aria-labelledby` 配線、(3) count `<span>` を font-normal にして heading 文字列の太さ差分を出す。1 ファイル ~10 行差分、視覚 layout / 色 / icon / pl-5 indent 全て不変、shadcn 編集なし。`scripts/explore-uiux-operation-board-section-heading-iter433.ts` で source-side regex assert 5 件 (h3 id + className / headingId 計算 / wrapper role=group / count span font-normal / 旧 inner div 不在 regression) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: SprintRetroWidget の `<dl>` planned/delivered/差分 (delta 色 rose/emerald のみ伝達、+/- 文字あり) は acceptable、Backlog table column header の sortable 候補確認、PDCA cycle phase widget 着地時の同 pattern、Workspace home Card layout の sub-heading 検証。
- ✅ [playwright-iter432 1/1] iter527 で着地した SprintRetroWidget の sub-section 2 件 ("Status 分布" / "落ちた item の主因") header が `<div>` で heading semantic 不在 + 各 chip 群 (`flex flex-wrap`) も role/aria-labelledby 不在で SR ユーザに「これは status 分布の chip 群」 という context が伝わらない (iter431 連続水平展開) → fix: `sprint-retro-widget.tsx` で (1) "Status 分布" を `<h3 id="sprint-retro-status-heading">` 化 + chip wrapper に `role="group" aria-labelledby` 配線、(2) "落ちた item の主因" を `<h3 id="sprint-retro-causes-heading">` 化 + 4 SeverityChip wrapper に `role="group" aria-labelledby` 配線。1 ファイル ~12 行差分、視覚 layout / 色 / chip 表示 全て不変、shadcn 編集なし (iter427 / iter428 / iter430 / iter431 と連続 5 件で widget 群の heading semantic を統一)。`scripts/explore-uiux-retro-widget-headings-iter432.ts` で source-side regex assert 6 件 (Status 分布 h3 / Status chip role=group / 主因 h3 / Causes chip role=group / 旧 div 不在 regression / iter431 risk-board pattern consistency) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: OperationBoardWidget の `Section` component (Section の visible label 4 件 + count を heading semantic 化)、PDCA cycle phase-helpers (iter534) 消費 widget の a11y、GTD inbox-process classification (iter535) UI 着地時の a11y、weekly-completion-insight 消費 widget の a11y。
- ✅ [playwright-iter431 1/1] iter529 で着地した SprintRiskBoardWidget の section header 2 件 ("要警戒 上位 N 件" / "担当者負荷 (N 名)") が `<div>` で heading semantic 不在 → SR ユーザは widget 内 sub-section に heading shortcut で直行不可 + `<ul>` topRisk list / `<table>` 担当者負荷 が aria-label/aria-labelledby 不在で context が伝わらない + `<table role="table" aria-label>` は role 重複 → fix: `sprint-risk-board-widget.tsx` に (1) "要警戒 上位 N 件" を `<h3 id="sprint-risk-top-heading">` 化 + ul に `aria-labelledby` 配線、(2) "担当者負荷 (N 名)" を `<h3 id="sprint-risk-load-heading">` 化 (icon + text 内包、flex-items-center 維持) + table に `aria-labelledby` 配線、旧 role="table" + aria-label 削除。1 ファイル ~10 行差分、視覚 layout 不変、shadcn 編集なし (iter427 / iter428 / iter430 と同 pattern 連続水平展開、widget 群の heading semantic を統一)。`scripts/explore-uiux-risk-board-headings-iter431.ts` で source-side regex assert 5 件 (要警戒 h3 / ul aria-labelledby / 担当者負荷 h3 / table aria-labelledby + 旧 aria-label 削除 / 旧 div 不在 regression) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: SprintRetroWidget の "Status 分布" / "落ちた item の主因" header 2 件 (同 pattern)、OperationBoardWidget の Section component (推奨 / overdue / MUST today / 今日の予定 の 4 sub-heading)、PDCA cycle-check-stats (iter534) 消費 widget の a11y、Backlog table の同 column header 検証。
- ✅ [playwright-iter430 1/1] CalendarView の左/右 2 lane container が `<div>` で landmark / heading semantic を持たず、内側 lane 用 Loader2 spinner に role="status" 不在 → SR ユーザは landmark navigation で lane に直行不可 + 「左 lane / 右 lane」 direction 依存表記が SR / RTL ユーザに役立たず + 内側 spinner の "読み込み中" announce 無し (iter421 で外側 mutation Loader2 は対応済) → fix: `calendar-view.tsx` で iter101 Kanban / iter81 Workspace pattern を水平展開、(1) lane container `<div>` を `<section aria-labelledby="calendar-lane-{kind}-heading">` に、(2) lane title を `<h3 id="calendar-lane-{kind}-heading">` に + 文言を「想定タイムライン / 実測タイムライン」 (主) + 「左 lane / N 件」 (補助 muted) に分離、(3) 内側 Loader2 wrapper に `role="status" aria-live="polite"` + Loader2 自体に `aria-hidden="true"`。1 ファイル ~30 行差分、視覚 layout (rounded-lg border / grid-cols-2) 不変、shadcn 編集なし。`scripts/explore-uiux-calendar-lane-section-iter430.ts` で source-side regex assert 7 件 (2 section aria-labelledby / 2 h3 id / 想定実測タイムライン文言 / 内側 Loader2 role+aria-live x2 / aria-hidden x2) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: today-view 同 row pattern (button-in-button、ItemCheckbox + title button + StartTimerButton の 3 inner interactive で iter429 アプローチ難) は別アプローチ要、Kanban カード row pattern audit、structured-plan schema (iter533) 消費 widget の a11y、must-similar-history widget (iter532) 消費 UI の a11y。
- ✅ [playwright-iter429 1/1] InboxView row が `<div onClick>` で keyboard a11y 不在 (WCAG 2.1.1 Keyboard 違反 / 4.1.2 Name, Role, Value 違反) + 内部 `<button>` title と二重 interactive element (両方 setOpenItemId 同じ id を呼ぶ) で SR ユーザに混乱、outer を role="button" にすると button-in-button (WAI-ARIA 違反) → fix: `inbox-view.tsx` の row を (1) outer `<div>` に `role="button"` + `tabIndex={0}` + `onKeyDown` (Enter/Space で setOpenItemId) + `aria-label="${title} を編集ダイアログで開く"` + focus-visible ring class 追加 (単一 interactive element 化)、(2) inner `<button>` を `<span>` に降格 (button-in-button 解消)、5 行コメントで降格根拠を documenting。1 ファイル ~12 行差分、旧 hover:underline 削除、color / spacing / size は不変、shadcn 編集なし。`scripts/explore-uiux-inbox-row-keyboard-iter429.ts` で source-side regex assert 5 件 (outer interactive ARIA / Enter+Space handler / inner button 不在 regression / 降格 span / focus-visible ring) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: today-view 同 row pattern (`today-row-${it.id}` も同 button-in-button 構造) の水平展開、Backlog table の row 同 pattern audit (table cell + button)、Kanban card 同 audit。
- ✅ [playwright-iter428 1/1] NotificationPreferences popover header の "通知設定 (メール)" ラベルが `<p>` で heading semantic を持たず、SR ユーザは heading navigation (h ショートカット) で popover 内に直行できなかった。4 toggle `<ul>` も aria-label 不在で list navigation で「list 通知設定 4 件」が伝わらない (iter427 NotificationBell と同問題) → fix: `notification-preferences.tsx` に (1) "通知設定 (メール)" `<p>` を `<h2 id="notification-preferences-heading">` に変更、(2) 4 toggle `<ul>` に `aria-labelledby="notification-preferences-heading"` 配線。1 ファイル ~5 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-notification-preferences-heading-iter428.ts` で source-side regex assert 4 件 (h2 + id / 旧 p 不在 regression / ul aria-labelledby / iter427 bell pattern consistency) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: TaskChute view header summary helper (iter530 ai-automation で着地) の SR 出力検証、weekly-insight pure helper (iter531) を消費する WeeklyInsightWidget の a11y、popover 系 utility 群 (workspace アバター popover 等) の同 pattern audit、`<ul>` aria-labelledby pattern を Inbox / Today / Backlog list にも展開。
- ✅ [playwright-iter427 1/1] NotificationBell popover header の "通知" ラベルが `<span>` で heading semantic を持たず、SR ユーザは heading navigation (h ショートカット) で popover 内に直行できなかった。さらに通知 `<ul>` 自体も aria-label 不在で list navigation で「list 通知 N 件」が伝わらない → fix: `notification-bell.tsx` で (1) "通知" `<span>` を `<h2 id="notification-bell-heading">` に変更 (visual サイズ text-sm 維持、heading semantic だけ追加)、(2) 通知 `<ul>` に `aria-labelledby="notification-bell-heading"` 配線で list が「通知」を accessible name として持つ。1 ファイル ~5 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-notification-heading-iter427.ts` で source-side regex assert 3 件 (<h2 id> + 通知 / 旧 span 不在 regression / <ul> aria-labelledby 配線) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: notification-preferences の preferences toggle 群の WAI-ARIA radiogroup pattern (iter418 と同 pattern 横展開)、recovery-plan helper 消費 widget の a11y、TaskChute cumulative-remaining ticker (iter530 で着地) 表示時の aria-live / 1Hz flood 検証 (iter426 教訓水平展開)、popover内の Time `<time dateTime>` の SR 読み上げパターン audit。
- ✅ [playwright-iter426 1/1] ActiveTimerPanel の経過時間 span が `aria-live="polite"` + `aria-atomic="true"` を持っており、1 秒 tick ごとに SR の announce queue に積まれて **「00:01 00:02 00:03 ...」と無限に読み続ける** a11y アンチパターン状態 (timer / clock 表示に aria-live を使うのは WAI-ARIA / WCAG ベストプラクティス違反)。コンテナ `<div role="region" aria-label="タスクタイマー (経過 X)">` が aria-label に経過時間を含めて render しているため、SR ユーザは region に navigate すれば on-demand に現在値を聞ける → fix: `active-timer-panel.tsx` の elapsed span から `aria-live="polite"` + `aria-atomic="true"` 削除、代わりに `aria-hidden="true"` 付与 (container aria-label が単一 accessible name source に)、再導入 risk 防止に 5 行コメントで documenting。1 ファイル ~7 行差分、視覚 layout / 計測 / formatElapsed 表示 全て不変、shadcn 編集なし。`scripts/explore-uiux-active-timer-aria-live-iter426.ts` で source-side regex assert 3 件 (elapsed span aria-live="polite" 不在 / aria-hidden 配線 / コンテナ aria-label に formatElapsed 維持) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: notification-bell の SR pattern / 同 panel 内の `title={itemTitle ?? ''}` (空文字 tooltip)、PiP capability 検出後の動的 button text 切替 SR 音声、estimate 校正 chip の aria-label の単位表記統一 (「分」 vs minutes)。
- ✅ [playwright-iter425 1/1] iter527 で着地した React Flow 視覚プレビュー (`WorkflowGraphCanvas`) の container `aria-label` が node 種類を含まず SR ユーザに graph の本質情報が伝わらない: 現状 `Workflow graph: 5 nodes, 4 edges` のみで 8 node type (noop / http / ai / slack / email / script / branch / parallel) のうち何が混在しているか不明 → workflow 概要把握不能 (React Flow は SVG canvas 描画で内部 node を AT に expose せず container aria-label のみが頼り) → fix: `workflow-graph-canvas.tsx` に (1) useMemo で `nodeTypeSummary` 計算 (ASCII 昇順 sort + count 集約、例: "ai 1, http 1, noop 2, slack 1")、(2) aria-label に `(${nodeTypeSummary})` 埋込み (例: "Workflow graph: 5 nodes (ai 1, http 1, noop 2, slack 1), 4 edges")、(3) role="img" 維持。1 ファイル ~10 行差分、視覚 layout / color / styling 全て既存維持、shadcn 編集なし。`scripts/explore-uiux-workflow-graph-aria-iter425.ts` で source-side regex assert 3 件 (nodeTypeSummary useMemo / aria-label 埋込み / role="img" 維持) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: graph node 自体の AT expose (text alternative `<details><summary>` で node-edge tree 出力) / RiskBoardWidget の severity formatter (iter527) / DataWidgetCard の loading state role="status" は live region として正しい使い方なので維持確認 / WorkflowGraphCanvas 編集 mode (段階 B) 追加時の DnD keyboard a11y。
- ✅ [playwright-iter424 1/1] SprintRetroWidget (iter527 で着地、Sprint 数値レポート widget) の完了率 progress bar が severity (ok/info/warn/danger) を **fill 色のみ** で伝えていた: SR は「完了率 75%」と数値だけ聞き取り「OK か / 注意か / 要対策か」が伝わらない、WCAG 1.4.1 違反 → fix: `sprint-retro-widget.tsx` に (1) `severityLabelJa(sev)` helper (ok=順調 / info=良好 / warn=注意 / danger=要対策 の 4 段階)、(2) progressbar に `aria-valuetext={rate}% ({sevLabel})` 追加 → SR は「完了率: 75% (順調)」と severity 文字で聞き取れる、(3) aria-label 簡潔化 ("完了率") で valuetext と役割分担。1 ファイル ~10 行差分、視覚 layout / 色 / 数値表示 全て不変、shadcn 編集なし。`scripts/explore-uiux-sprint-retro-progress-aria-iter424.ts` で source-side regex assert 7 件 (4 severityLabelJa label / aria-valuetext 配線 / aria-label 簡潔化 / 既存 ARIA 属性 維持) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: workflow React Flow visual preview (iter527 で実装、graph node の SR 不可視性と DnD keyboard a11y)、SprintRetroWidget の `<dl>` planned/delivered/差分 で delta 色 (rose/emerald) のみ符号情報伝達 (但し +/- 文字あり)、StatusChip 5 種の chip ARIA pattern (色 + label のみ、role 無し)。
- ✅ [playwright-iter423 1/1] 共通 SeverityChip widget (iter523 で着地、DiffSummaryBar / dashboard 等で消費) の **static path** が `role="status"` (live region) を誤用していた問題: chip が DOM に出現するたび SR が再 announce、dashboard / diff-summary 等で chip 多数描画する画面では「ok ok warn ok danger」連続読み上げ → UX 破綻 / WAI-ARIA spec 違反 (role="status" は advisory text 用) → fix: `severity-chip.tsx` の static `<span>` を `role="status"` → `role="img"` (atomic group + aria-label、iter98 PDCA / iter417 TaskChute と同 pattern)、live region 性消滅。1 ファイル 1 行差分 (+ コメント 3 行)、視覚 layout / 色 / aria-label / button path 全て既存維持、shadcn 編集なし。`scripts/explore-uiux-severity-chip-role-iter423.ts` で source-side regex assert 4 件 (`<span role="status">` 不在 / role="img" + aria-label 配線 / button path 構造維持 / taskchute role="img" pattern consistency) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: DataWidgetCard shell の a11y pattern 検証、Severity 5 段階 chip の dashboard 内消費箇所一括 audit (古い ChipTone3 残存)、SprintRetroWidget (iter527) の数値表示 SR fallback。
- ✅ [playwright-iter422 1/1] OperationBoardWidget (iter522 で追加された「今日の作戦盤」 widget) の `ItemRow` が SR に視覚情報の半分しか届けていなかった: aria-label = `${item.title} を編集ダイアログで開く` のみで時刻 / 期限 / 状態 (overdue / done / 推奨) が伝わらない、状態は **色のみで伝達** = WCAG 1.4.1 違反 → fix: `operation-board-widget.tsx` の ItemRow に (1) statePrefix (`期限超過: ` / `完了済: ` / `推奨: ` / 空) を tone / muted / highlight から導出、(2) aria-label = `${statePrefix}${title}${time}${date} を編集ダイアログで開く` 形式に集約 (例: "期限超過: A タスク 期限 2026-04-25 を編集ダイアログで開く")、(3) visible 3 span (time / title / date) に `aria-hidden="true"` を付与し button aria-label が accessible name の単一 source に。1 ファイル ~12 行差分、視覚 layout 不変。`scripts/explore-uiux-operation-board-aria-iter422.ts` で source-side regex assert 6 件 (3 statePrefix / aria-label 集約 / aria-hidden カウント >=11 / button ariaLabel 配線) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: SeverityChip 共通 widget の SR 検証 (DiffSummaryBar 以外でも使われ始めている)、risk-board pure helper (iter526) を消費する RiskBoardWidget の a11y、DataWidgetCard shell の a11y pattern 検証、OperationBoardWidget の Section heading semantic (`<h3>` 不在)。
- ✅ [playwright-iter421 1/1] CalendarView (`calendar-view.tsx` 252 行) の日付 navigation で WCAG / SR 違反 4 件: (1) 前日 button (`<Button size="icon">` + `<ChevronLeft />`) が `aria-label` 不在で SR は「button」のみ用途不明 (WCAG 2.5.3 label-in-name)、(2) 同 翌日 button (ChevronRight) も `aria-label` 不在、(3) 日付表示が `<div>` で `<h2>` 等 heading semantic 不在 → SR で landmark / heading navigation で日付に直行不可、(4) mutation pending 中 `<Loader2>` icon が回るだけで `role="status"` / `aria-label` 不在で SR に「保存中」が伝わらない → fix: `calendar-view.tsx` に (1) 前日/翌日 button に `aria-label="前日"` / `"翌日"` + ChevronLeft/Right icon に `aria-hidden`、(2) 日付 `<div>` を `<h2>` 化 + `aria-live="polite"` + `aria-atomic="true"` で日付変更時 SR 自動 announce、(3) Loader2 に `role="status"` + `aria-label="保存中"` の 4 段。1 ファイル ~10 行差分、視覚 layout 不変、shadcn 編集なし。`scripts/explore-uiux-calendar-date-nav-iter421.ts` で source-side regex assert 4 件 (前日 button / 翌日 button / 日付 h2 + aria-live / Loader2 role + aria-label) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: lane container (左 lane / 右 lane) の `<section>` + `<h3>` landmark 化 (iter101 Kanban pattern 水平展開)、TimelineLane の SVG schedule bars SR 不可視 / DnD keyboard-only、loading state の `<Loader2>` 全 view 同 pattern 検証 (CalendarView 内部 lane 用 Loader2 もまだ aria-label 無し)。
- ✅ [playwright-iter420 1/1] Calendar view (iter497 二車線 timeline) の `DiffSummaryBar` (想定 vs 実測 chip 一覧) で WCAG + SR 違反 4 件: (1) severity (`ok` / `warn` / `danger` / `not_done` / `interrupt`) は **色 + icon のみ** で文字情報なし → WCAG 1.4.1 違反 (色覚特性ユーザは状態不明)、(2) 5 icon (CheckCircle2 / TrendingUp / AlertTriangle / MinusCircle / Zap) に `aria-hidden` 不在で SR 二重読み上げ、(3) `title` 属性 (mouse hover only) で「想定/実測」が冗長表示、visible span と重複、(4) button に severity 文字情報を持つ `aria-label` 不在で SR は "(不明) 1h → 1h30m +30m" のみ → fix: `diff-summary-bar.tsx` に (1) 5 SEVERITY_ICON entry に `aria-hidden="true"`、(2) SEVERITY_LABEL_JA 5 entry (想定通り / 注意 (1.1〜1.5x) / 危険 (1.5x 超過) / 未実施 / 割込み)、(3) 旧 title 削除、(4) button `aria-label` で sevLabel + title + 想定 + 実測 + 差分 集約 (例: "危険 (1.5x 超過): A タスク 想定 1h 実測 1h30m 差分 +30m")。1 ファイル ~15 行差分、視覚 layout 不変、色も既存維持。`scripts/explore-uiux-diff-summary-aria-iter420.ts` で source-side regex assert 12 件 (5 icon aria-hidden / 5 SEVERITY_LABEL_JA / button aria-label 集約 / 旧 title 削除) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: TimelineLane (`timeline-lane.tsx` 148 行) の `<svg>` schedule bars が SR 不可視 / DnD 操作不可 / focus-visible 漏れ、Calendar view 本体の workspace mode 切替 ARIA、SeverityChip widget (iter523 で追加された pure helper) の 5 段階 ARIA pattern 検証。
- ✅ [playwright-iter419 1/1] Calendar view (iter497 二車線 想定/実測 timeline) の `ScheduleItemPicker` modal dialog で WAI-ARIA 規約違反 4 件: (1) `aria-label`/`aria-labelledby` 不在で SR は「dialog」のみ読み上げ用途不明、(2) Escape キー handler 不在で modal 閉じられず、(3) 検索 input が placeholder のみで `aria-label` 不在、(4) 割込み note `<label>` が `htmlFor` で input に紐付いていない → fix: `schedule-item-picker.tsx` に (1) `aria-labelledby="schedule-picker-title"` + `<h2 sr-only>` 動的タイトル ("実績 / 割込みを記録する task を選択" or "想定する task を選択")、(2) dialog div `onKeyDown` で Escape → onCancel + e.stopPropagation で親 dialog bubble 防止、(3) 検索 input `aria-label` + Search icon `aria-hidden`、(4) 割込み note label `htmlFor` + Input 対応 `id` の 4 段。1 ファイル ~12 行差分、視覚 layout 不変。`scripts/explore-uiux-schedule-picker-dialog-iter419.ts` で source-side regex assert 4 件 (aria-labelledby + sr-only h2 / Escape handler / 検索 input aria-label + icon / 割込み label htmlFor + id) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: focus trap (Tab で modal 外へ漏れる、要 library or 実装)、Calendar view 本体 (`calendar-view.tsx` 252 行) の `<svg>` timeline lane の SR 読み上げ性、`TimelineLane` の DnD ハンドル keyboard-only 操作可否、`DiffSummaryBar` の data 表示 (chart) の table fallback。
- ✅ [playwright-iter418 1/1] WorkspaceModeSelector (iter518 で追加された方法論 mode 選択 UI) は `role="radiogroup"` + `role="radio"` + `aria-checked` を付与しているが、WAI-ARIA radiogroup spec が要求する **roving tabindex** + **矢印キー巡回** + mutation 中 **`aria-busy`** が未実装 → キーボードユーザは Tab で 3 option 全部に止まり矢印キーは無反応、NVDA / JAWS のラジオ群 navigation で破綻 → fix: `workspace-mode-selector.tsx` に (1) `tabIndex={selected ? 0 : -1}` で roving tabindex、(2) `handleKeyDown` で ArrowLeft/Right/Up/Down (wrap-around) + Home/End (jump) + 移動同時 select (WAI-ARIA「focus = 選択」 pattern)、(3) `useRef<HTMLButtonElement[]>` で ref array 保持 + `ref.focus()` で programmatic focus、(4) radiogroup div に `aria-busy={pending || undefined}` の 4 段。1 ファイル ~25 行追加、視覚 layout 不変。`scripts/explore-uiux-mode-selector-radiogroup-iter418.ts` で source-side regex assert 11 件 (roving tabIndex / aria-busy / 6 key handler / onKeyDown 配線 / useRef array / ref-focus 移動) を codify。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: TaskChute view 行 button (item title button + StartTimerButton) の Tab 順序 / focus-visible ring 検証 / mode selector の workspaceDefault と URL `?mode=` の競合時 SR announce / GTD mode UI 追加時の同 pattern 適用。
- ✅ [playwright-iter417 1/1] TaskChute view (iter519 で追加された方法論 mode の 1 列 timeline) の priority chip span が `aria-label={`優先度 ${'${priorityLabel(item.priority)}'}`}` と書かれており、`priorityLabel()` が既に "優先度: 最優先 (p1)" 形式を返すため SR で **「優先度 優先度: 最優先 (p1)」** と prefix 二重読み上げ。他 view (today / inbox / personal-period / command-palette) は全て `aria-label={priorityLabel(...)}` の単一 prefix で統一されており TaskChute だけが outlier → fix: `taskchute-view.tsx` の chip に (1) 冗長 prefix `優先度 ` 削除、(2) `role="img"` 追加 (iter98 PDCA bar pattern: container role="img" + 集約 aria-label) の 2 行差分。`scripts/explore-uiux-taskchute-priority-aria-iter417.ts` で source-side regex assert 6 件を codify (重複 prefix 不在 / role="img" + 単一 aria-label / 4 view の単一 prefix 維持)。typecheck / lint 緑。経路: B (script、source-side regex)。次 iter 候補: TaskChute chip の visible text "優先度: 最優先 (p1)" は inline pill としては verbose、`P{n}` 等の compact 表示への UX polish (mode-D or mode-M) / mode selector UI (iter518) の `radiogroup` ARIA 検証 / TaskChute 行 `<button>` の focus-visible ring と double tab order 検証。
- ✅ [playwright-iter255] login / signup form の submit button が pending 状態を SR に再 announce していなかった (iter180-222 の「pending SR 化」パターンの取り残し) → fix: `login-form.tsx` / `signup-form.tsx` の `<Button type=submit>` に `aria-label={isPending ? '<処理中文言>' : '<通常文言>'}` を追加。Docker 不在 cloud env のため Supabase 起動 fallback (login/signup 画面のみ走行) で `aria-label=null → "ログイン" / "アカウントを作成 (サインアップ)"` を確認。typecheck / lint 緑。
- ✅ [playwright-iter256 1/3] auth (login/signup) ページに `<main>` ランドマーク欠落 (`(auth)/layout.tsx` が `<div>` で children を包んでいた) → fix: 最外 `<div>` を `<main>` に置換 (1 行 ×open/close)。Playwright で `main=0→1` を /login と /signup 両方で確認。SR の skip-to-main 操作で form に直行可能に。
- ✅ [playwright-iter256 2/3] /login と /signup に programmatic heading (h1) 一切無し — shadcn `<CardTitle>` が `<div data-slot="card-title">` 実装で role=heading を持たないのが原因。shadcn 編集禁止のため fix: 両 `page.tsx` の CardTitle 使用箇所に `role="heading" aria-level={1}` のみ追加 (DOM はそのまま div、ARIA tree のみ heading 化)。Playwright で `aria-level=1=0→1` 確認。
- ✅ [playwright-iter256 3/3] /~offline (PWA SW network fallback) は h1 + 説明 2 段落のみで interactive element 0、ユーザは URL バー手動操作 / ブラウザ更新ボタン頼みだった → fix: `OfflineRetryButton` (client island、`location.reload()` トリガ + aria-label) と `<Link href="/">` (server-rendered + `buttonVariants({variant:'outline'})`) の 2 button row を追加。`force-static` / Server Action 不使用の原則は維持。Playwright で `link=0,button=0 → link=1,button=1` 確認。
- 経路メモ (iter256): 主に script 経路 (B) を使用。MCP server (`@playwright/mcp`) は `chrome` system 実行ファイル不在 (`/opt/google/chrome/chrome` not found) で起動不可、cloud env では `npx playwright install chromium` で入る Chromium binary を直接使う script 経路の方が確実。次 iter で MCP を使う場合は `@playwright/mcp` の `--executable-path` あたりの override を検討。
- ✅ [playwright-iter257 1/3] /login email input に `inputMode="email"` / `spellCheck=false` / `autoCapitalize="none"` 不在 → mobile キーボードで `@` 即押せず、autocorrect 誤変換 / 先頭大文字化の risk。`type="email"` だけでは iOS / Android の挙動が一貫しない。fix: `login-form.tsx` の IMEInput に 3 attr 追加 (3 行)。Playwright で `inputmode=email / spellcheck=false / autocapitalize=none` 出力確認。
- ✅ [playwright-iter257 2/3] /signup email input にも同 3 attr 不在 (signup は誤入力が復旧不能なため login 以上に影響大) → fix: `signup-form.tsx` の IMEInput に同パターン展開 (3 行)。Playwright で /signup の email も 3 attr 出力確認。
- ✅ [playwright-iter257 3/3] shadcn `<Card>` (= `<div data-slot="card">`) が SR の region/landmark 一覧に出ない (iter256 で heading aria-level=1 は付けたが Card 全体は無名 div のまま) → fix: 各 page で `<Card role="region" aria-labelledby="login-heading|signup-heading">` + 既存 CardTitle に `id` を付与。shadcn Card は `...props` を div に spread する実装 (`src/components/ui/card.tsx:18`) なので prop 追加で動作 (shadcn 編集禁止ルール内)。Playwright で `card[aria-labelledby]=0→1` / `heading id=null→login-heading|signup-heading` を確認、検証 assertion も `scripts/explore-uiux-auth-deep-iter257.ts` に codify。
- 経路メモ (iter257): 主に script 経路 (B)。`scripts/explore-uiux-auth-deep-iter257.ts` 1 ファイルで 3 fix 全部の before/after を assert。`pnpm tsx scripts/explore-uiux-auth-deep-iter257.ts` で warning=0 が現状の合格状態。残 bug 候補: (1) /~offline の heading に id + Card 化 / (2) auth layout に skip-to-main link / (3) login/signup の "アカウント未作成?" "サインアップ" link に aria-label を補強 (現状は visible text のみで context 不明)。
- ✅ [playwright-iter258 1/1] /login / /signup / /~offline の `document.title` が全部 root の "最強TODO" のまま → ブラウザ tab strip で 3 画面を見分けられず、SR は window 切替時に同じ announce で context lost。fix: 3 ページに `export const metadata: Metadata = { title: '<画面名> | 最強TODO' }` を追加 (各 5 行、Next.js metadata API なので head 注入は framework 任せ、機能追加なし)。Playwright で `/login → "ログイン | 最強TODO"` / `/signup → "サインアップ | 最強TODO"` / `/~offline → "オフライン | 最強TODO"` を確認、検証 assertion も `scripts/explore-uiux-auth-titles-iter258.ts` に codify (Supabase 不要、login screen only mode で完走)。
- ✅ [playwright-iter259 1/1] login footer の "サインアップ" / signup footer の "ログイン" Link に aria-label 不在 → 隣の "アカウント未作成?" / "アカウントあり?" は SR 上では別 node なので、link 単体に focus した時に画面と link 先の関係が伝わらず誤遷移 risk。fix: `<Link aria-label="アカウントをお持ちでない方はこちらでサインアップ">` (login) / `<Link aria-label="既にアカウントをお持ちの方はこちらでログイン">` (signup) を追加 (各 1 行、2 行合計、shadcn 編集なし、機能追加なし)。検証 codify: `scripts/explore-uiux-auth-footer-link-iter259.ts` で warning=0 確認。
- ✅ [playwright-iter260 1/1] /~offline の reload button + home link を包む `<div class="flex">` に role / aria-label が無く、SR は孤立コントロール 2 つに見える (「復帰アクション群」という関係性が伝わらない) → fix: 既存 wrapper に `role="group" aria-label="復帰アクション"` を追加 (3 行差分、機能追加なし、shadcn 編集なし)。検証 codify: `scripts/explore-uiux-offline-actions-iter260.ts` で `role=group=0→1` / `button-in-group=0→1` / `link-in-group=0→1` を assert、warning=0 確認。
- ✅ [playwright-iter261 1/1] login/signup Card に aria-describedby 不在 + CardDescription に id 不在 → SR が region 入境時の announce が heading のみで、説明文 ("最強TODO へようこそ" / "アカウントを作成して始めましょう") は別読みになり context が痩せる。fix: 各 Card に `aria-describedby="login-description|signup-description"` + CardDescription に対応 id を付与 (各 1 行 ×2 ファイル = 4 行差分)。iter257 の labelledby と同じ機構 (Card は `...props` を div spread)。shadcn 編集なし、機能追加なし。検証 codify: `scripts/explore-uiux-auth-card-described-iter261.ts` で warning=0 確認。
- ✅ [playwright-iter262 1/1] login/signup `<form>` に aria-busy 不在 → submit 中の busy state は button focus 時しか SR に伝わらず、focus 外れ / toast 読み上げ中は form 全体の進行状況が消える。fix: `<form aria-busy={isPending}>` を 2 form に bind (各 1 行)。RHF の useTransition 同期で初期 false / submit 中 true をリアルタイム送出。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-form-busy-iter262.ts` で初期 markup `aria-busy="false"` を assert。
- ✅ [playwright-iter263 1/1] /~offline `<main>` が無名 landmark のまま (h1 "オフラインです" は見えていても `aria-labelledby` で結びついていない) → SR の landmark 一覧で "main" としか出ず、複数 main 要素 (PWA で他ページ同居時) に「どの main か」見分けられない。fix: `<main aria-labelledby="offline-heading">` + h1 に `id="offline-heading"` を付与 (3 行差分、機能追加なし、shadcn 編集なし)。検証 codify: `scripts/explore-uiux-offline-main-label-iter263.ts` で warning=0 確認。
- ✅ [playwright-iter264 1/1] login/signup `<form>` に `noValidate` 不在 → `<input required>` だけだと空 submit 時にブラウザ native validation bubble (OS/ブラウザ言語依存、英語環境では "Please fill out this field"、スタイル不能) が発火し、zod の inline error / sonner toast と二重表示。fix: 各 form に `noValidate` を追加 (各 1 行)。required 自体は維持 (SR の aria-required 意味は残す)。これで native UI は完全に黙り、RHF + zod の日本語 error に一本化。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-novalidate-iter264.ts` で `form[novalidate]` 属性存在を assert。
- ✅ [playwright-iter265 1/1] /login と /signup を開いた直後カーソルが何処にも入らず、ユーザは Tab 1 回 or マウスクリックで最初の field に着地する必要があった → 「即入力したい」auth ページの中心 use case の取りこぼし。fix: /login email input / /signup displayName input にそれぞれ `autoFocus` を追加 (各 1 行 ×2 = 2 行)。SR 割り込み懸念は iter257 / iter261 の Card region aria-labelledby + aria-describedby で region 入境時に heading + description が一緒に announce されるため context 失われない (Twitter / GitHub login と同等の慣習)。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-autofocus-iter265.ts` で `document.activeElement.id` を assert。
- ✅ [playwright-iter266 1/1] signup `displayName` の zod は `min(1).max(50)` だが `<IMEInput>` 側に `maxLength` 不在 → ユーザは 51 文字以上打鍵可能、submit 時に zod が弾いて inline error が出るが既に消す手間。fix: `<IMEInput maxLength={50}>` を 1 行追加、zod schema と整合 (機能追加なし、shadcn 編集なし、影響面 1 ファイル)。検証 codify: `scripts/explore-uiux-signup-maxlength-iter266.ts` で `maxlength=50` を assert、warning=0 確認。
- ✅ [playwright-iter267 1/1] /login CardFooter / Link は iter4 で `relative z-10` を付けた (dev devtools overlay 等の stacking 対策) が /signup CardFooter / Link は対応無し → signup → login link が同条件で hit-test 不能になる潜在 risk + 単純な inconsistency。fix: /signup CardFooter / Link にも `relative z-10` を追加 (class 文字列 2 ヶ所のみ)。Playwright trial click は両画面で成功 (現環境では blocking 無し)、defensive 整合。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-signup-footer-z-iter267.ts` で CardFooter class に relative + z-10 を assert。
- ✅ [playwright-iter268 1/1] login/signup の pending visible text は "ログイン中..." / "作成中..." (ASCII dot ×3) なのに aria-label は "ログイン処理中…" / "アカウント作成中…" (Unicode 三点リーダー U+2026) で SR 文言と sighted 文言が不一致 → CJK 混在 typography 観点でも ASCII "..." はノイズ。fix: visible text を "ログイン中…" / "作成中…" に統一 (各 1 文字差替え)。aria-label と完全一致、機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-ellipsis-iter268.ts` (source grep — visible text に ASCII "..." が消え Unicode "…" のみ残ることを assert)。
- ✅ [playwright-iter269 1/1] `(auth)/layout.tsx` と `/~offline/page.tsx` の `<main>` が `min-h-screen` (=100vh) を使用 → iOS Safari / Android Chrome で「アドレスバー込みの高さ」を返す古典バグで、mobile で content がアドレスバー裏に隠れる / 不要 scroll が発生する。fix: 2 ファイルの `min-h-screen` を Tailwind v4 の `min-h-dvh` (dynamic viewport height、アドレスバー伸縮を反映) に置換 (各 1 行差替え、合計 2 行)。auth と offline は full-screen landing なので影響大、shadcn 編集なし、機能追加なし。dvh 非対応の古い browser は flex items-center の center 配置で fall back。検証 codify: `scripts/explore-uiux-auth-dvh-iter269.ts` で `min-h-screen=0 / min-h-dvh=1` を assert。
- ✅ [playwright-iter270 1/1] login/signup `<form>` 自体に accessible name (aria-labelledby / aria-label) 不在 → SR の form rotor / forms list (NVDA: Insert+F7) で「ログイン」/「サインアップ」と特定できない無名 form として並ぶ。Card region は heading に紐付けたが (iter257)、form もまた interactive container として固有の名前が必要。fix: `<form aria-labelledby="login-heading|signup-heading">` を 2 form に追加 (各 1 行)。Card region と同じ heading id を共有 (id 競合無し、ARIA spec 準拠)。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-form-name-iter270.ts` で `form[aria-labelledby]` 期待値一致を assert。
- ✅ [playwright-iter271 1/1] login/signup useForm に `mode` 指定無し → RHF default の 'onSubmit' で、ユーザは email field を離脱しても zod inline error を受け取れず submit ボタンを押して初めて表示。invalid email / missing field の打ち直し→submit→error サイクルが発生。fix: 各 useForm に `mode: 'onTouched'` を追加 (各 1 行) — 各 field を最初に blur した瞬間に validation 開始、以降は onChange で継続評価。RHF 公式推奨パターン、機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-validate-mode-iter271.ts` で email を invalid 値で埋めて password に focus 移動 → email-error が visible=true を assert。
- ✅ [playwright-iter272 1/1] /signup password Label が "パスワード (8 文字以上)" と inline ヒント混じり → Label がフィールド名 + ヒントの混合で再利用しにくい / aria-describedby が err 時のみセットでエラー前は SR が hint を察知できない。fix: Label を「パスワード」単独に、`<p id="signup-password-hint" class="text-muted-foreground text-xs">8 文字以上</p>` を新規 element として追加 (常時表示)、aria-describedby を「常に hint id を含む + err 時は error id を append」形式に修正 (5 行差分、機能追加なし、shadcn 編集なし)。検証 codify: `scripts/explore-uiux-signup-pw-hint-iter272.ts` で label text / hint 要素 / aria-describedby に hint id 含むことを assert。
- ✅ [playwright-iter273 1/1] /~offline 説明 2 段落で p1 のみ `max-w-md` 設定、p2 は無制限で wide screen (1280px+) で行長 448px / 601px と不一致 (p2 が 1 行 50-75 文字推奨を超え可読性低下)。fix: p2 にも `max-w-md` を追加 (1 行差替え、機能追加なし、shadcn 編集なし)。両段落で 448px に統一、reading-rhythm 整合。検証 codify: `scripts/explore-uiux-offline-text-width-iter273.ts` で各 p の class に max-w-md を assert (448 / 448 を確認)。
- ✅ [playwright-iter274 1/1] login/signup の server auth error 後 focus が disabled→enabled の submit button → BODY と移動し、誤入力修正に Tab で field 戻る手間 + SR で focus position 不明。fix: error path に `form.setFocus('password')` (login) / `form.setFocus('email')` (signup) を 1 行追加 (合計 2 行)。frequency-based: login は password 誤りが主、signup は email already-exists が主。RHF 公式 setFocus、機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-error-focus-iter274.ts` (Supabase 不在環境で /login に invalid creds で submit → server auth error 後 `document.activeElement === INPUT#password` を assert)。
- ✅ [playwright-iter275 1/1] auth schema (`email: z.string().email(...)` / `displayName: z.string().min(1).max(50)`) が `.trim()` を chain しておらず、`<input type="text">` displayName に paste した先頭末尾空白がそのまま server 側へ (" Hiro Tanaka ") 保存される。email も defensive に schema 層で trim しておくと server RPC 値ズレ無し。fix: SignupInputSchema (email/displayName) と LoginInputSchema (email) に `.trim()` を chain (3 行差替え)。password は trim せず intentional 末尾空白を許容。zod parse 時に自動 trim、機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-trim-iter275.ts` (zod schema を直 import して safeParse 結果で displayName/email trim 済 + password 元値保持を assert)。
- ✅ [playwright-iter276 1/1] /login submit button の `aria-label="ログイン"` (非 pending 時) が visible text "ログイン" と完全一致で重複 → SR は accessible name で aria-label を優先するため visible text を冗長に override。ARIA 推奨は「visible label が単独で十分なら aria-label を上書きしない」。fix: `aria-label={isPending ? 'ログイン処理中…' : undefined}` に変更 (1 行差替え) — 非 pending は visible text を SR が直接読み、pending 時のみ "ログイン処理中…" を override。/signup は aria-label "アカウントを作成 (サインアップ)" が visible "サインアップ" を含みつつ context を加える WCAG 2.5.3 適合パターンなので維持。検証 codify: `scripts/explore-uiux-auth-button-aria-iter276.ts` で /login submit aria-label=null を assert。
- ✅ [playwright-iter277 1/1] /~offline の home `<Link href="/">` で `aria-label="ホームに戻る"` が visible text "ホームに戻る" と完全一致 (iter256 で追加した時の冗長設定) → iter276 と同根の重複問題。fix: aria-label を削除 (1 行)。OfflineRetryButton 側は aria-label "ページを再読み込みして接続を再試行" が visible "再読み込みして再試行" を含みつつ context (ページ全体 / 接続再試行) を加える有用な拡張なので維持。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-offline-link-aria-iter277.ts` で home link aria-label=null を assert。
- ✅ [playwright-iter278 1/1] login/signup の各 input が `aria-invalid={Boolean(form.formState.errors.<name>)}` で形成され、エラー無し時に常に `aria-invalid="false"` が DOM に出ていた。ARIA spec: aria-invalid のデフォルト有効状態は「属性不在」が推奨で、"false" 明示は SR ノイズ (NVDA 一部 version で「有効」を冗長 announce)。fix: `aria-invalid={form.formState.errors.<name> ? true : undefined}` に書き換え (5 箇所、2 ファイル) — error 時のみ属性が出現。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-aria-invalid-iter278.ts` で全 input の aria-invalid attr が null を assert。
- ✅ [playwright-iter279 1/1] /signup displayName (type=text) に `spellCheck` 指定無し → browser 環境依存で default spellcheck=true、ユーザが "Hiroshi" 等の固有名詞 / nickname を入力すると spell-checker が赤下線 / 修正提案を出す。iter257 で email に同 fix を入れた pattern と整合。fix: `spellCheck={false}` を 1 行追加 (機能追加なし、shadcn 編集なし、影響面 1 ファイル)。autoCapitalize は意見が分かれるので維持。検証 codify: `scripts/explore-uiux-signup-displayname-spell-iter279.ts` で spellcheck="false" を assert。
- ✅ [playwright-iter280 1/1] login/signup password (form 最終 field) に `enterKeyHint` 指定無し → mobile キーボードの Enter ボタンが default で「改行」/ generic ラベルになり、ユーザに「ここで Enter すれば送信」が伝わらない。fix: 各 password input に `enterKeyHint="send"` を 1 行追加 (合計 2 行)。iOS / Android の仮想キーボードが Enter ボタンを「送信」/「Send」として表示。non-password field は default の "next" 相当が適切なので変更なし。機能追加なし、shadcn 編集なし。検証 codify: `scripts/explore-uiux-auth-enterkeyhint-iter280.ts` で password の enterkeyhint="send" を assert。
- ✅ [playwright-iter281 1/1] /login submit button の pending 時 visible="ログイン中…" / aria-label="ログイン処理中…" — "ログイン中…" は "ログイン処理中…" の連続 substring ではない (中央に "処理" が挟まる) → WCAG 2.5.3 (Label in Name, Level A) 違反。voice control / SR で visible label と accessible name の不整合。/signup 側は visible="作成中…" / aria-label="アカウント作成中…" で suffix substring 含むため OK、/login だけ非対称だった。fix: aria-label を `'ログイン中… (認証処理を実行中)'` に変更 (1 行差替え) — visible "ログイン中…" を accessible name の先頭 substring として配置 (WCAG 2.5.3 best practice 準拠)、"(認証処理を実行中)" の付加情報で iter276 の SR context 提供意図も保持。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で button HTML を取得 → 非 pending aria-label=null と visible="ログイン" を確認) → B (script に codify)。検証 codify: `scripts/explore-uiux-login-pending-aria-iter281.ts` で source 三項式 regex 抜き出し → visible "ログイン中…" を substring 含むこと assert + runtime で 非 pending aria-label=null assert (iter276 invariant 維持)。次 iter 候補: 他の pending state を持つ submit button (workspace 作成 / item 作成 等) で同 visible≠aria-label 不整合がないか確認。
- ✅ [playwright-iter282 1/1] login/signup form `aria-busy={isPending}` で書かれており React は boolean false を `aria-busy="false"` 属性として DOM 出力 → 非 pending 時 (大半の時間) 常時 `aria-busy="false"` が DOM に存在。ARIA spec 4.6.2: aria-busy default は false で属性自体が **不在** 推奨、"false" 明示は SR ノイズ。iter278 で aria-invalid を同 pattern (`Boolean(...)` → `... ? true : undefined`) で修正済みだったが aria-busy は見落としていた。fix: 各 form を `aria-busy={isPending || undefined}` に変更 (合計 2 行差替え、login-form.tsx + signup-form.tsx) — pending 時のみ `aria-busy="true"` が DOM に出現、非 pending は属性無し。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で /login + /signup form の aria-busy="false" を観測) → B (script に codify)。検証 codify: `scripts/explore-uiux-auth-aria-busy-iter282.ts` で /login と /signup の form aria-busy attr が null を assert。次 iter 候補: 他の boolean state 連動 ARIA 属性 (aria-disabled / aria-expanded / aria-pressed / aria-selected) で同種の "false" DOM 残留が無いか類似 audit。
- ✅ [playwright-iter283 1/1] /login と /signup ページで SR landmark が **同名で重複**: `region "ログイン"` (Card に role="region" + aria-labelledby="login-heading") と `form "ログイン"` (LoginForm の `<form aria-labelledby="login-heading">`、HTML5 spec で form に accessible name があると暗黙 landmark 化) が同 heading を name に共有。SR landmark navigation で「ログイン region / ログイン form」と冗長列挙、Card 内 form と完全 nest なので分けて navigate する意味は無い。codebase 内他の Card+role="region" は workspace panel 等で個別 unique aria-label を持ち form 重複 landmark は無い、auth 2 ページだけが非対称だった。fix: /login/page.tsx と /signup/page.tsx の Card から `role="region"` を削除 (1 行差替え × 2 ファイル)。aria-labelledby / aria-describedby は残す。SR landmark 総数 3→2 (main + form)、form という意味的により正確な landmark のみ残し semantic 改善。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で /login の landmark 列挙 → region "ログイン" と form "ログイン" 同名重複を観測) → B (script に codify)。検証 codify: `scripts/explore-uiux-auth-card-region-iter283.ts` で /login と /signup について role="region" 要素数=0 / form aria-labelledby="\*-heading" 維持 / heading-text と同名の landmark count=1 を assert。次 iter 候補: codebase 内他で `<form aria-labelledby>` + parent の role="region" が同 heading を共有する pattern が無いか類似 audit。
- ✅ [playwright-iter284 1/1] /~offline OfflineRetryButton の visible="再読み込みして再試行" / aria-label="ページを再読み込みして接続を再試行" — 文字単位で連続 substring 判定すると visible 中の "再読み込みして" は accessible 中で match (pos 4-10) するが、その直後 accessible は "接続を" を挟んで visible 後半 "再試行" まで連続しない → NOT contained、WCAG 2.5.3 (Label in Name) 違反。iter281 (login pending button "ログイン中…" vs "ログイン処理中…") と同種で iter277 自己レビュー時に substring vs subsequence を混同して見落としていた。fix: aria-label を `'再読み込みして再試行 (ページ全体を読み直して接続を回復)'` に変更 (1 行差替え) — visible "再読み込みして再試行" を accessible name の **先頭** substring として配置 (WCAG 2.5.3 best practice 準拠)、後半括弧内に context を保持。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で /~offline retry button HTML 取得 → contains() = false / indexOf = -1 を観測) → B (script に codify)。検証 codify: `scripts/explore-uiux-offline-retry-aria-iter284.ts` で aria-label が visible を連続 substring として含む (かつ先頭にあれば best practice 達成) を assert。次 iter 候補: iter276/277 で扱った他の aria-label = visible-with-context 箇所 (signup CardFooter link "既にアカウントをお持ちの方はこちらでログイン"、login CardFooter link "アカウントをお持ちでない方はこちらでサインアップ" 等) で同種 substring 取りこぼしが無いか再 audit。
- ✅ [playwright-iter285 1/1] /login と /signup の heading が `<CardTitle id role="heading" aria-level={1}>` で実装されており、shadcn CardTitle 内部は `<div>` のため native `<h1>` ではない。SR は ARIA で正しく heading-1 announce するが (1) ブラウザ document outline / a11y dev tool / Reader Mode が拾わない、(2) HTML5 semantic outline で h1 ゼロ、(3) /~offline は `<h1 id="offline-heading">` で native h1 を使用しており auth 2 ページだけ非対称。fix: CardTitle を native `<h1>` に置換 (合計 2 行 × 2 ファイル = 4 行差替え + 未使用 CardTitle import 削除 × 2 ファイル)。CardTitle base class `font-heading text-base leading-snug font-medium` + page override `text-2xl` の合算 visual を h1 className `font-heading text-2xl font-medium leading-snug` で再現、視覚は完全同一。form の aria-labelledby="\*-heading" は h1 id を refer して landmark name 維持。機能追加なし、shadcn 編集なし (CardTitle 自体は他 panel で使われているため残置)。経路: A (MCP browser_evaluate で /login の `[role="heading"]` が DIV、/~offline は H1 であることを観測 → 非対称を確認) → B (script に codify)。検証 codify: `scripts/explore-uiux-auth-native-h1-iter285.ts` で /login と /signup について <h1> 要素数=1 / id 一致 / text 一致 / role-aria-level の明示削除 / form aria-labelledby reference 維持 を assert。次 iter 候補: codebase 内他で `<CardTitle role="heading">` override されている箇所が無いか類似 audit (auth 以外には無い見込みだが念のため)。
- ✅ [playwright-iter286 1/1] /login と /signup の CardFooter Link が `text-primary ... underline-offset-4 hover:underline` で書かれており、非 hover 時は `text-decoration: none`。link 色 (text-primary、現テーマで rgb(23,23,23) ほぼ黒) と surrounding span 色 (text-muted-foreground、rgb(115,115,115) 中グレー) の contrast 3.78:1 で WCAG G183 technique (b) を辛うじて満たすが、(1) 色覚異常で luminance 差感知しにくい場合 / (2) hover 不可能な touch device / SR + 音声制御ユーザに link であることが伝わりにくい。WCAG 1.4.1 (Use of Color, Level A) ベスト適合は技術 (a) 「常時 underline」。fix: `hover:underline` → `underline` に変更 (1 行 × 2 ファイル)。`underline-offset-4` は元から付いており 4px オフセットで読みやすさ確保。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で text-decoration-line=none と link/surrounding 色 contrast 3.78 を観測) → B (script に codify)。検証 codify: `scripts/explore-uiux-auth-link-underline-iter286.ts` で /login と /signup 各 link の text-decoration-line === "underline" を assert。次 iter 候補: codebase 内他で `hover:underline` のみで in-text link を表現している箇所が無いか類似 audit (button-styled link は除外)。
- ✅ [playwright-iter287 1/1] /login と /signup CardFooter Link の touch target 寸法が 85.97 × 20 / 56 × 20 px → 高さ 20px が WCAG 2.5.8 (Target Size Minimum, Level AA、WCAG 2.2 で追加) の閾値 24×24 を下回り違反。"Inline" / "Equivalent control" / "Essential" のいずれの例外にも該当しない (CardFooter で flex item として配置)。fix: 各 Link className に `inline-flex items-center py-2` を追加 (1 行差替え × 2 ファイル)。inline-flex で flex 子要素として padding を適用、py-2 (上下 8px = 計 16px) で content 高 20 + padding 16 = **36px** に拡張。WCAG 2.5.8 AA (24×24) を上回り、AAA 2.5.5 (44×44) 未満だが手指 / touch で押しやすい comfortable hit area。視覚 layout の変化は CardFooter 行高 20→36px のみ、文言・色・配置不変。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で getBoundingClientRect → 高さ 20px を観測) → B (script に codify、修正後 36px を assert)。検証 codify: `scripts/explore-uiux-auth-link-target-size-iter287.ts` で /login と /signup 各 link の bounding rect 高さ・幅が WCAG 2.5.8 AA 閾値 24px 以上であることを assert。次 iter 候補: codebase 内他で短い text の inline link が touch target 24×24 未満になっている箇所が無いか類似 audit (table の title リンク等が候補だが Supabase 認証必要画面が多い)。
- ✅ [playwright-iter288 1/1] sonner Toaster の aria-label が default で英語 "Notifications alt+T" のまま、JP locale (html lang="ja") の app と不一致。sonner は `containerAriaLabel = 'Notifications'` を default として `${containerAriaLabel} ${hotkeyLabel}` で section の aria-label を構築 (node_modules/sonner/dist/index.mjs:920, 1082)。SR の landmark navigation で英語と日本語が混入し WCAG 3.1.2 (Language of Parts, Level AA) 適合性低下。fix: `src/app/layout.tsx` の `<Toaster ... />` に `containerAriaLabel="通知"` 追加 (1 行差替え) → "通知 alt+T" に変更。global Toaster なので /login / /signup / /~offline / 認証画面全てで改善。hotkey 部分 "alt+T" は universal 表記なので残置。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: A (MCP browser_evaluate で /login の section[aria-label] = "Notifications alt+T" を観測) → B (script に codify)。検証 codify: `scripts/explore-uiux-toaster-aria-label-iter288.ts` で /login, /signup, /~offline の Toaster section aria-label が "通知" で始まることを assert。次 iter 候補: Toaster の `closeButtonAriaLabel` (sonner default "Close notification") も英語のままなので localize 候補。
- ✅ [playwright-iter289 1/1] iter288 で container は localize 済みだが、各 toast の close button (× アイコン) の aria-label は sonner default `closeButtonAriaLabel = 'Close toast'` のまま (node_modules/sonner/dist/index.mjs:442)。toast 表示中に SR が close button を focus すると "Close toast button" と英語 announce → iter288 で残った英語ノイズ。WCAG 3.1.2 (Language of Parts, Level AA) 適合性向上のため localize。fix: `<Toaster>` に `toastOptions={{ closeButtonAriaLabel: '通知を閉じる' }}` を追加 (Toaster props 拡張で改行整形 5 行)。sonner は toastOptions の closeButtonAriaLabel を各 toast の close button aria-label に伝搬 (index.mjs:780, 1164)。global Toaster で全 page 改善。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 実 toast trigger には Supabase 必要のため runtime DOM 検証は環境依存 → source 側 regex assert (build-time) で fix verify + iter288 container aria-label 回帰防止 runtime check 併用。検証 codify: `scripts/explore-uiux-toaster-close-button-iter289.ts` で layout.tsx の closeButtonAriaLabel が日本語文字を含むこと assert + /login の section[aria-label] が "通知" で始まること (iter288 invariant) を assert。次 iter 候補: sonner の other ARIA strings (loading icon "Loading"、status announcement 等) も英語のまま localize 候補だが sonner i18n 機構は限定的。
- ✅ [playwright-iter290 1/1] /login と /signup の `<input type="email">` が `autoCorrect` 指定無しで iOS Safari の default "on" に従って auto-correct を発動。"foo@example.com" が "foo@example.con" 等に書き換えられて誤メールアドレス送信 → 認証失敗の典型的 mobile UX 失敗。spellCheck=false / autoCapitalize="none" / inputMode="email" は iter257 で揃えていたが autoCorrect は iOS 専用 attr で別軸の WHATWG 拡張属性、見落としていた。fix: 各 email input に `autoCorrect="off"` を追加 (1 行 × 2 ファイル)。Android Chrome / 他 browser は unknown attr として noop、iOS Safari でのみ effect。password は type="password" で iOS が auto-correct を default で disable するので変更不要、displayName は arbitrary 表示名で auto-correct が逆に役立つ場合もあるので残置。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で input#email の autocorrect attr=null を観測 → Chromium 上は noop だが iOS 仮想想定で fix 妥当性判定) → B (script に codify)。検証 codify: `scripts/explore-uiux-auth-email-autocorrect-iter290.ts` で /login と /signup の input#email の autocorrect attr === "off" を assert。次 iter 候補: codebase 内他で `<input type="email">` がある画面 (workspace member 招待 / profile 編集等) で同 autoCorrect="off" 適用が必要か類似 audit。
- ✅ [playwright-iter291 1/1] iter283 で Card から role="region" を削除した後、`aria-labelledby="login-heading"` / `aria-describedby="login-description"` を「将来 role 復活時に useful」と判断して残置していたが、role / tabindex 無しの `<div>` で aria-labelledby は SR landmark navigation に効かず focus も受領しないため完全 inert dead code。CardDescription の id も Card の aria-describedby から参照されるためだけに付いており dead。codebase 内他で Card+role="region" 使用例は個別 unique aria-label で landmark name 付与、aria-labelledby を CardTitle 経由で参照する pattern 無 → 「将来 role 復活時 useful」前提も成立しない。fix: /login/page.tsx と /signup/page.tsx の Card から aria-labelledby/aria-describedby を削除 + CardDescription から id を削除 (3 行差替え × 2 ファイル)。h1 id は form の aria-labelledby が依然 reference するので残置。機能追加なし、shadcn 編集なし。経路: A (MCP browser_evaluate で Card aria-attrs と CardDescription id が依然存在を観測 → iter283 後の dead 化を特定) → B (script に codify、削除後 null assert)。検証 codify: `scripts/explore-uiux-auth-card-aria-cleanup-iter291.ts` で /login と /signup について Card aria-labelledby/aria-describedby = null / CardDescription id = null / h1 id 維持 / form aria-labelledby 維持を assert。次 iter 候補: codebase 内他で role 削除歴史で残った dead aria-attrs が無いか grep audit 候補。
- ✅ [playwright-iter292 1/1] sonner Toaster に `theme` prop 未指定で sonner default "light" のまま固定。`<ThemeProvider attribute="class" defaultTheme="system" enableSystem>` で html.class が "dark"/"light" を切替えるが Toaster は light のまま → dark mode 利用ユーザに toast だけ light bg/dark text の Card が浮いて視覚的乖離、注意散漫の UX 悪化。sonner 内部 (index.mjs:942) は `theme="system"` 時 `prefers-color-scheme` を react.useState 初期値に取り込み data-sonner-theme で toast 要素に切替。fix: `<Toaster ... theme="system" ...>` を追加 (1 行)。sonner が OS preference を respect して dark/light を切替、ThemeProvider と system layer で同期。global Toaster なので全 page で dark mode UX 改善。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: layout.tsx 読み込みで Toaster に theme prop 無しを発見 → source-side regex assert で codify (実 toast trigger 困難)。検証 codify: `scripts/explore-uiux-toaster-theme-iter292.ts` で layout.tsx の Toaster block に `theme="system"` 文字列を含むこと assert。次 iter 候補: ThemeProvider と Toaster の完全同期 (user 明示切替時) には `useTheme().resolvedTheme` を Toaster の theme に渡す client wrapper が必要 (今回は system preference 同期で 95% カバー)。
- ✅ [playwright-iter293 1/1] iter292 の `theme="system"` は sonner が prefers-color-scheme を直接 query するため、user が in-app `<ThemeToggle>` で system 設定 (例: light) を override して dark に切替えても toast は light のまま (prefers-color-scheme は OS 値、user 選択は localStorage で next-themes 管理)。iter292 は **system 設定 = user 選択** の場合のみ valid で、明示切替時に in-app theme と toast theme が乖離する。fix: 新規 client component `src/components/shared/themed-toaster.tsx` を作成し、`useTheme().resolvedTheme` を取って Toaster の `theme` prop に渡す。resolvedTheme は user 選択 + system fallback を解決した最終値なので、in-app theme と完全同期。`src/app/layout.tsx` の `<Toaster ...>` を `<ThemedToaster ...>` に置換、Toaster 直 import 削除。ToasterProps を passthrough するので既存の richColors / position / closeButton / containerAriaLabel / toastOptions は変わらず。新規 1 ファイル + 編集 1 ファイル、約 18 行差分。機能追加なし、shadcn 編集なし。経路: sonner 内部 source 確認 (system query は prefers-color-scheme 直接) で iter292 の漏れを特定 → ThemedToaster wrapper で next-themes と橋渡し。検証 codify: `scripts/explore-uiux-themed-toaster-iter293.ts` で layout.tsx に <ThemedToaster> 存在 + sonner 直 import 無し / themed-toaster.tsx に 'use client' / useTheme / Toaster import / /login の section aria-label が "通知" で始まる (iter288 invariant) を assert。次 iter 候補: 他 next-themes 連動 library wrapper の同 pattern audit (例: charting library の dark-mode 切替)。
- ✅ [playwright-iter294 1/1] gantt-view.tsx に英語 aria-label が 2 箇所残存: (1) line 464 `aria-label="MUST item"` — must-badge.tsx で「MUST item (英) と MUST タスク (和) 混在を統一」した際の追従漏れで孤立、(2) line 474 `aria-label={\`baseline ${start} → ${end}\`}`— app 全体は「ベースライン」(katakana) 表記なので英語残留はノイズ。SR で gantt MUST badge focus 時 "MUST item" / baseline 線 Tab スキップ時 "baseline 2024-01-01 → 2024-02-01" と英語 announce → WCAG 3.1.2 (Language of Parts, Level AA) 適合性低下、JP locale 体験の一貫性低下。fix: (1)`aria-label="MUST タスク"`(must-badge.tsx と整合)、(2)`aria-label={\`ベースライン ${...}\`}`。visible 表示は変更なし。機能追加なし、shadcn 編集なし、影響面 1 ファイル、2 行差替え。経路: codebase 全体で `aria-label="[A-Z]`grep → 英語残留を発見 → must-badge.tsx の修正コメントから歴史的経緯確認 → source-side regex assert で codify (gantt-view は workspace 認証必須画面のため runtime 検証は環境依存)。検証 codify:`scripts/explore-uiux-gantt-aria-localize-iter294.ts` で "MUST item" / "baseline " 英語残留無し + "MUST タスク" / "ベースライン " 存在 を assert。次 iter 候補: 他の panel で aria-label 内英語フレーズ (sprint / goal / source 等) の localize 要否確認。
- ✅ [playwright-iter295 1/1] iter278 で /login と /signup の aria-invalid を Boolean(...) → ternary に統一したが、`src/components/workspace/create-workspace-form.tsx` の 2 箇所 (name + slug input) に Boolean(...) パターンが残存していた漏れ。HomePage の "最初の Workspace を作成" / "別の Workspace を作成" Card 内で表示時に常時 `aria-invalid="false"` が DOM 出力 → SR ノイズ (NVDA 等で「無効ではない」を冗長 announce)。ARIA spec: aria-invalid デフォルト有効状態は属性自体が **不在** が推奨。fix: 2 箇所を `aria-invalid={form.formState.errors.X ? true : undefined}` に書き換え (1 行 × 2 箇所、1 ファイル)。codebase 全体 grep で `aria-invalid={Boolean(` 残存 0 件を確認 (完全制覇)。機能追加なし、shadcn 編集なし。経路: codebase grep で `aria-invalid={Boolean` → create-workspace-form 漏れ発見。create-workspace-form は HomePage で workspace 一覧読み込み後に Card 内描画されるため Supabase 認証必須、source-side regex assert で codify。検証 codify: `scripts/explore-uiux-create-workspace-aria-invalid-iter295.ts` で create-workspace-form の Boolean(...) aria-invalid 残存無し + ternary 2 件存在 + codebase 全体 Boolean(...) 残存無し (iter278 invariant) を assert。次 iter 候補: 他で aria-busy={isPending} 等の boolean 属性 (iter282 pattern) で false 残留する form の sweep audit。
- ✅ [playwright-iter296 1/1] pdca-panel.tsx の PDCA 分布バーが `<div role="img" aria-labelledby="pdca-dist-label" aria-label="分布 (合計 X): Plan ... / Do ... / Check ... / Act ...">` の **両方** を持っており、ARIA AccName Computation 1.2 (Step 2A: aria-labelledby → Step 2B: aria-label) で aria-labelledby が優先されて aria-label が完全無視。実際の accessible name = "分布" のみ、開発者が意図した詳細 PDCA 各段階の値が SR user に伝わらず WCAG 1.1.1 (Non-text Content, Level A) chart alternative 不在。fix: `aria-labelledby="pdca-dist-label"` を削除 (1 行削除)、aria-label の詳細文字列が Step 2B で accessible name として有効化。視覚 "分布" label は別 div で残置 (sighted user の cue 維持)、`id="pdca-dist-label"` は将来 reference 時用に残置。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: `role="img"` を codebase grep → pdca-panel.tsx で aria-labelledby と aria-label の両指定を発見 → ARIA AccName 仕様で優先順位を確認 → 競合と判定。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-pdca-aria-conflict-iter296.ts` で role="img" + aria-labelledby + aria-label 三重指定の残存無し / aria-label に Plan/Do/Check/Act 全て含む詳細文字列維持 / visible 分布 label id 残置 を assert。次 iter 候補: codebase 内他で role="img" + aria-labelledby + aria-label の三重指定 audit。
- ✅ [playwright-iter297 1/1] gantt-dependency-arrows.tsx の `<svg>` 要素 (Gantt timeline の依存矢印描画) が `aria-hidden` 指定無しで a11y tree に露出していた問題。SVG path/marker の空コンテンツが SR a11y tree に流入してノイズに、依存情報自体は item-dependencies-panel 等で textual access 可能なので decorative。WCAG 1.1.1 (Non-text Content, Level A) 技術 G94: decorative element は a11y tree から除外推奨。codebase 内 item-checkbox.tsx の checkmark SVG は既に aria-hidden 設定済で、gantt-dependency-arrows のみ非対称だった。fix: `<svg>` に `aria-hidden="true"` を追加 (1 行追加)。SR 利用者は dependency 情報を item-dependencies-panel の textual UI から取得 (pre-existing access path)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase grep `<svg` で gantt-dependency-arrows と item-checkbox を発見 → 後者は設定済で前者のみ非対称と特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-gantt-arrows-aria-hidden-iter297.ts` で gantt-dependency-arrows の svg に aria-hidden="true" 含む + item-checkbox 既存設定維持 を assert。次 iter 候補: lucide-react 等 icon library 経由 SVG の aria-hidden 漏れ audit (visible なら親 button aria-label に統合される pattern が標準)。
- ✅ [playwright-iter298 1/1] ThemeToggle の Button は `aria-label="テーマ切替"` で accessible name 提供しているが、子要素 `<Sun />` `<Moon />` (lucide-react) には aria-hidden 指定無し。lucide-react defaultAttributes に aria-hidden 含まず (defaultAttributes.js: xmlns/width/height/viewBox/fill/stroke のみ)、SR 実装依存で "graphic" 等を冗長 announce する場合あり。codebase 内他 lucide 使用例 (async-states.tsx Loader2/AlertTriangle、must-badge.tsx AlertTriangle) は既に aria-hidden="true" 設定済で theme-toggle のみ非対称。fix: 各 icon に `aria-hidden="true"` 追加 (1 行 × 2 icon = 2 行)。Button の aria-label は維持。global ThemeToggle なので workspace 各 page header で恩恵。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase で lucide-react import grep → 各 file の icon usage 確認 → theme-toggle.tsx で aria-hidden 漏れを発見 (must-badge / async-states は既設定で対比)。source-side regex assert で codify。検証 codify: `scripts/explore-uiux-theme-toggle-aria-hidden-iter298.ts` で <Sun aria-hidden="true"> / <Moon aria-hidden="true"> 存在 + Button aria-label 維持 を assert。次 iter 候補: 他 lucide icon usage の aria-hidden 漏れ sweep audit (各 panel の close button、edit button、icon-only chip 等)。
- ✅ [playwright-iter299 1/1] iter298 lucide aria-hidden sweep 続編。budget-panel.tsx の budget exceeded alert (line 117-127) で `<AlertTriangle>` lucide icon に aria-hidden 指定無し。`role="alert"` の Live region が text 子 ("月次上限 (...) に達しました...") で意味を完全伝達済で icon は decorative、SR 実装依存の "graphic" 冗長 announce ノイズ。fix: `<AlertTriangle ... aria-hidden="true" />` 追加 (1 行差替え)。role="alert" の text 内容のみ SR に届く。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase grep で lucide JSX usage を icon 名で照合 → budget-panel.tsx で漏れを発見 (iter298 theme-toggle に続く 2 件目)。検証 codify: `scripts/explore-uiux-budget-panel-aria-hidden-iter299.ts` で <AlertTriangle aria-hidden="true"> 存在 + role="alert" 維持を assert。次 iter 候補: 他 panel (sprints / goals / templates / activity-log) の lucide icon aria-hidden 漏れ sweep 続行。
- ✅ [playwright-iter300 1/1] codebase 内 `<Button>` (shadcn) で `type` 属性未指定の 4 件 (item-edit-dialog.tsx 668/671 + comment-thread.tsx 217/220) を発見。HTML5 spec で `<button>` 未指定は default `type="submit"` で挙動するため、いずれも onClick 駆動 (Cancel / Save) で submit 意図ではないにも関わらず将来 form ancestor が増えた時に誤 submit を起こす **defensive bug**。fix: 各 Button に `type="button"` を明示 (1 行 × 4 箇所、2 ファイル)。codebase 全体 grep で type 未指定 + asChild 除外で 0 件達成。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: codebase grep `<Button ` で sweep → asChild (Link 化) を除外して 4 件特定 → 各 Button の onClick handler 確認で submit 意図でないことを verify。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-button-type-iter300.ts` で codebase 内 `<Button>` で type 未指定 (asChild 除く) が 0 件であることを assert。次 iter 候補: shadcn Button 自体に default `type="button"` 仕込む wrapper component を検討 (CLAUDE.md 規約で shadcn 編集禁止のため別 file 経由)。
- ✅ [playwright-iter301 1/1] goals-panel.tsx の KR (Key Result) 入力フォームの target / unit input で placeholder が英語 "target" / "unit (件 / %)" のまま、aria-label は "目標値" / "単位" で JP 統一済だが visible placeholder が英語で sighted user の入力像が掴みにくい (technical English term と Japanese-only ユーザの組合せ)。fix: placeholder を JP の **例示** 形式に変更: "target" → "例: 100"、"unit (件 / %)" → "例: 件 / %"。aria-label は維持。"例:" prefix で「実値ではなく example」が明確。機能追加なし、shadcn 編集なし、影響面 1 ファイル、2 行差替え。経路: codebase grep `placeholder=` で全 input sweep → goals-panel.tsx の英語 2 件のみ残留と特定 (他 placeholder は "例: 〜" / "team-a" / 説明文 で JP 統一)。検証 codify: `scripts/explore-uiux-goals-placeholder-iter301.ts` で 英語 placeholder 残存無し + JP "例: ..." 存在 + aria-label "目標値" / "単位" 維持を assert。次 iter 候補: 他 panel で英語 placeholder の sweep 続行。
- ✅ [playwright-iter302 1/1] integrations-panel.tsx の Source 作成フォーム project IDs input の placeholder が英語 mixed `"comma-separated (例: proj-a, proj-b)"`。"comma-separated" は技術用語で JP locale の "カンマ区切り" に統一する方が自然。example の `"(例: proj-a, proj-b)"` は project IDs が ASCII slug 様式なので維持。fix: placeholder = `"カンマ区切り (例: proj-a, proj-b)"` に変更 (1 行差替え)。iter301 (goals-panel) に続く placeholder sweep の 2 件目で、これで codebase 全体 placeholder が JP 統一 (slug/URL example の英語は意図的維持)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase grep `placeholder="[a-z]"` で英語 prefix sweep → integrations-panel.tsx 1 件特定 (他は team-a / https://example.com 等の意図的 example)。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-integrations-placeholder-iter302.ts` で 英語 "comma-separated" 残存無し + JP "カンマ区切り" 存在 + example slug 維持を assert。次 iter 候補: codebase 全体 placeholder sweep は本 iter で完了。他 text content (button / select option) の英語混在 audit は技術 / product noun 判別が必要なので別途。
- ✅ [playwright-iter303 1/1] create-workspace-form.tsx の `<form>` に `noValidate` 属性が無く、HTML5 native validation popup と RHF zod inline error が **dual validation** で重複表示。各 input には required / minLength / pattern が設定されており submit 時に popup と inline の両方が出る。auth pages (login / signup) は noValidate 設定済で RHF zod 単一に統一済 → create-workspace-form のみ非対称。schema vs input attribute の minLength 不整合 (slug schema 2 / input 1) も背景にある (popup 抑制で表面化を予防)。fix: `<form ... noValidate ...>` 追加 (1 行)。input の required / minLength / pattern 属性は SR への hint metadata として残存。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 各 form の noValidate 有無を grep 比較 → auth forms 設定済、create-workspace-form のみ非対称と特定。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-create-workspace-no-validate-iter303.ts` で create-workspace-form の <form noValidate> 存在 + auth forms (login / signup) の noValidate 維持 (回帰防止) を assert。次 iter 候補: 他 form (templates / sprints / goals 等) で noValidate 漏れの sweep / schema vs input attribute minLength 不整合の整合化。
- ✅ [playwright-iter304 1/1] sprints-panel.tsx の 3 form (line 189: 新規 Sprint 作成、line 464: 既存 Sprint 編集、line 741: workspace 全体 Sprint defaults) が noValidate 漏れで HTML5 native popup と zod inline の dual validation 重複表示。iter303 (create-workspace-form) の続編。fix: 各 form に noValidate を追加 (3 行追加、1 ファイル)。zod 単一 validation 経路に統一。input の required / minLength / pattern 属性は SR hint metadata として残存。機能追加なし、shadcn 編集なし。経路: codebase 全体 form sweep → sprints-panel が 3 form の最大規模で影響面が多いため優先選択。Supabase 認証必須画面のため source-side regex assert で codify (multiline `<form\n...\nnoValidate\n...>` を split + filter で検出)。検証 codify: `scripts/explore-uiux-sprints-no-validate-iter304.ts` で sprints-panel の <form> 出現数 = noValidate 付き form 数 (3 = 3) を assert。次 iter 候補: 残り form (decompose-proposals / goals × 2 / integrations / template-items-editor / templates / time-entry / mock-timesheet × 3) の noValidate 漏れ修正を続行 (mock-timesheet は test fixture なので除外候補)。
- ✅ [playwright-iter305 1/1] goals-panel.tsx の 2 form (line 125: Goal 作成、line 642: KR 追加) が noValidate 漏れで dual validation 重複表示。iter303 / iter304 の続編。fix: 各 form に noValidate 追加 (2 行)。zod 単一経路統一。これで auth + workspace 作成 + sprints + goals の 7 form 完了、残り 6 form は次 iter 候補。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase 全体 form sweep の 3 番目 panel。検証 codify: `scripts/explore-uiux-goals-no-validate-iter305.ts` で goals-panel の <form> 数 = noValidate 付き 数 (2 = 2) を assert。次 iter 候補: decompose-proposals / integrations / template-items-editor / templates / time-entry の noValidate 漏れ続行。
- ✅ [playwright-iter306 1/1] decompose-proposals-panel (line 329: AI proposal 編集) + integrations-panel (line 286: Source 作成) の各 1 form に noValidate 追加 (1 行 × 2 ファイル)。iter303-305 sweep 続編、9 form 統一達成 (auth × 2 + workspace 作成 + sprints × 3 + goals × 2 + decompose + integrations)。残り 3 production form (template-items-editor / templates / time-entry) は次 iter。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 各 1 form の panel を 2 ファイル合算で iter scope 内 batch 化。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-decompose-integrations-no-validate-iter306.ts` で 2 ファイル各々 <form> 数 = noValidate 付き 数を audit。次 iter 候補: template-items-editor / templates / time-entry の noValidate 漏れ続行。
- ✅ [playwright-iter307 1/1] template-items-editor.tsx (line 83: item 追加) + templates-panel.tsx (line 83: Template 作成) の各 1 form に noValidate 追加 (1 行 × 2 ファイル)。iter306 続編、11 form 統一達成 (production form 12 中 11 完了、残り 1 は time-entry)。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: iter306 と同 batch pattern。検証 codify: `scripts/explore-uiux-templates-no-validate-iter307.ts` で 2 ファイル各々 <form> 数 = noValidate 付き 数を audit。次 iter 候補: time-entry 1 form の noValidate 修正で production sweep 完結 → mock-timesheet (test fixture) は除外。
- ✅ [playwright-iter308 1/1] noValidate sweep の **最終回**。create-time-entry-form (line 52) の他、検証 script の codebase 全体 form sweep audit で追加発見した 2 form (template/instantiate-form line 74、workflow/workflows-panel line 88) を含めて 3 form に noValidate 追加。これで codebase 内 production form (mock-timesheet test fixture 除く) **15/15 件で noValidate 統一達成**、HTML5 native popup と zod inline の dual validation 重複表示が完全解消。zod 単一 validation 経路に集約 (single source of truth = zod schema)。input の required / minLength / pattern 属性は SR hint metadata として維持。機能追加なし、shadcn 編集なし、影響面 3 ファイル、3 行追加。経路: iter308 で time-entry を予定通り fix した後、検証 script で codebase 全 form を sweep audit して 2 件追加発見 → 同 iter で fix。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-time-entry-no-validate-iter308.ts` で time-entry 単体 + 全 production form (15/15) の noValidate 完了 invariant を assert。発展ステップ: 今後 form 追加時 noValidate default 付与の convention を CLAUDE.md / ARCHITECTURE.md に書く規約化検討候補 (再発防止)。
- ✅ [playwright-iter309 1/1] decompose-proposals-panel の `<Sparkles>` lucide icon (line 123) に aria-hidden 漏れ、iter298 ThemeToggle / iter299 budget AlertTriangle に続く lucide aria-hidden sweep の 3 件目。Sparkles は visible text ("Researcher が分解中…" / "AI 分解の提案 (N)") と並んで表示される decorative icon で、aria-hidden 未指定 → SR 実装によっては "graphic" 等を冗長 announce、WCAG 1.1.1 (Non-text Content, Level A) 技術 G94 「decorative element は a11y tree から除外推奨」非準拠。fix: `<Sparkles ... aria-hidden="true" />` を追加 (1 行)、visible 表示 / animate-pulse / text 内容は不変。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase 全体 lucide-react JSX usage を 6 代表 icon (Sparkles / AlertTriangle / Sun / Moon / Bell / Loader2) で sweep grep → decompose-proposals-panel.tsx 1 件特定 (他は iter298/299 で fix 済 + iter188/238 でも整備済)。Supabase 認証必須画面のため source-side regex assert で codify。検証 codify: `scripts/explore-uiux-decompose-sparkles-aria-hidden-iter309.ts` で decompose-proposals-panel の <Sparkles> aria-hidden="true" 存在 + iter298/299 invariant 維持 + lucide sweep 漏れ 0 件を assert。次 iter 候補: lucide 残 50+ icon 名で sweep を拡張する dedicated audit script の整備。
- ✅ [playwright-iter310 1/1] StartTimerButton (size='sm') の otherActive 時 visible label "別 Item を停止して計測開始" (12 文字超) が Today / Backlog 行幅を圧迫し折返し / レイアウト崩れの原因に。iter250 で size='sm' を Today/Backlog 行に配置した時の取り残し UX gap (size='default' は dialog 用で幅余裕、'sm' のみ問題)。fix: compact 時のみ visible label を「切替して開始」(7 文字) に短縮、aria-label は full hint「「<前 Item>」のタイマーを停止して「<新 Item>」の計測を開始」を維持、`title` 属性で hover tooltip 化。WCAG 2.5.3 (Label in Name, Level A) 適合性は「切替して開始」が full aria-label の意味的 prefix で維持 (SR / 音声操作で full hint を使えば match 可能)。default size の dialog では従来の長文 visible label を保持 (幅余裕あり)。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 16 行差替え (visibleLabel / fullHint 中間変数で重複削減)。経路: iter249/250 commit で長文 label が compact 行に流入していたのを source 読みで発見 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-start-timer-compact-label-iter310.ts` で `isCompact ? '切替して開始'` 三項分岐の存在 + full hint と title attr 維持 + default size の長文 label 残置を assert。次 iter 候補: 他 compact-only label 短縮候補 (engineer-trigger-button / heartbeat-button / standup-button など) の sweep audit。
- ✅ [playwright-iter311 1/1] items-board の "新規 Item (クイック追加)" CardTitle に heading semantic 不在 (iter256 で auth pages の CardTitle 経路は h1 直書きに置換済だが workspace 内 CardTitle は未対応)。shadcn `<CardTitle>` は `<div data-slot="card-title">` 実装で role=heading を持たないため、workspace home の主要 entry point である QuickAdd Card の見出しが SR の heading navigation (NVDA H key / VoiceOver Cmd+Opt+H) で skip 不可、SR ユーザは線形 read を強要され WCAG 2.4.6 (Headings and Labels, Level AA) 非対応。fix: CardTitle に `role="heading" aria-level={2}` を追加 (DOM そのまま div、ARIA tree のみ heading 化)。h1 (workspace title in WorkspaceHeader) の sub-section なので level=2。shadcn 編集禁止ルールは props 経由 attr 追加なので維持。機能追加なし、影響面 1 ファイル、3 行差替え。経路: shadcn card.tsx の CardTitle 実装を確認 → workspace 内 CardTitle 25 件のうち最も visible な entry point である items-board (workspace home QuickAdd 上) を選定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-quick-add-card-heading-iter311.ts` で items-board CardTitle に role="heading" + aria-level={2} が併存 + iter256 invariant (login/signup h1 id 残置) を assert。次 iter 候補: 残 24 件の workspace CardTitle (sprints / goals / dashboard / pdca / today / personal-period / budget / team-context) に同 heading semantic を sweep 展開、各 panel の文脈 (h1 直下なら level=2、ネスト Card なら level=3) で aria-level を調整。
- ✅ [playwright-iter312 1/1] budget-panel の月次上限編集 UI が `<form>` 要素無しの `<div>` ラッパで、2 Input + Save/Cancel button (onClick 駆動) のため Enter キー submit 不可、`aria-busy` も無し。iter38-41 (sprint/goal/template の <form> 化) と iter303-308 (noValidate sweep) で確立した pattern が漏れていた。Tab → Save button → Enter の冗長な keyboard UX、SR は pending 中も "busy" 通知無しで処理進捗が掴めない。fix: 編集モードの `<div>` を `<form noValidate aria-busy={update.isPending || undefined} onSubmit={...preventDefault → saveEdit}>` に置換、Save button を `type="submit"` 化 (onClick 削除)、Cancel button に明示 `type="button"` 付与で誤 submit 防止 (HTML5 default は submit)。これで Input 内 Enter で保存実行、pending 中は SR が "busy" を報告。zod schema は無いが saveEdit 内で値検証、noValidate で HTML5 native popup を抑制し toast で error 通知に統一。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 16 行差替え (form 開閉 + button type 変更 + aria-busy)。経路: budget-panel.tsx の grep で `<form` 不在を発見、iter38-41 / iter303-308 の pattern と差分を比較して該当判定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-budget-panel-form-iter312.ts` で <form onSubmit> + noValidate + aria-busy + Save type="submit" + Cancel type="button" 5 invariants を assert。次 iter 候補: 他 onClick-only mini form (notification-preferences / team-context-editor / kanban-modal の inline editor 等) で同 pattern 漏れ sweep。
- ✅ [playwright-iter313 1/1] team-context-editor Textarea に Cmd/Ctrl+Enter 保存ショートカット展開 (iter228/iter229 で comment-thread に確立した pattern を AI プロンプト inject 用 chamber に展開)。旧 UX は Tab → Save button → Enter の 2-step、Slack / GitHub / Notion 標準の Cmd/Ctrl+Enter での即時送信が漏れていた。textarea 内 Enter は newline で必須なので `<form>` 化は不可、modifier 併用が唯一の選択肢。fix: Textarea に `onKeyDown` で `(metaKey || ctrlKey) + Enter` かつ `!nativeEvent.isComposing` (IME 確定中の誤発火回避) かつ `dirty` (未変更時は無反応) かつ `!upd.isPending` (二重送信防止) の 4 ガードで `handleSave()` を呼出。`preventDefault()` で newline 挿入を抑制。placeholder と aria-label の文末に "Cmd/Ctrl+Enter で保存" を append (visible / SR 両方で hint)。機能追加なし、shadcn 編集なし、影響面 1 ファイル、12 行差替え。経路: comment-thread.tsx の iter228/229 実装を参照しながら同 pattern を team-context-editor に水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-team-context-cmd-enter-iter313.ts` で Cmd/Ctrl+Enter 判定 + IME ガード + hint 文言 + iter228 invariant (comment-thread Cmd/Enter 維持) を assert。次 iter 候補: personal-period-view.tsx (Goal textarea) / sprints-panel.tsx (retro textarea) / goals-panel.tsx (description textarea) など Textarea + Save button が同居する panel に同 pattern 展開。
- ✅ [playwright-iter314 1/1] personal-period-view (週次 / 月次 / 四半期 ゴール) の Textarea にも Cmd/Ctrl+Enter 保存を展開、iter313 team-context-editor の続編。週次 / 月次 / 四半期 ゴール は workspace の主要 OKR-style 入力で頻繁編集される (Period_Goal の上書き)。旧 UX は Tab → Save button → Enter で同じ漏れ。fix: Textarea に `onKeyDown` で 4 ガード (Cmd/Ctrl+Enter 判定 + IME 確定中 skip + dirty + !upsertGoal.isPending) を追加、`preventDefault()` で newline 抑制、`handleSave()` 起動。placeholder / aria-label に "Cmd/Ctrl+Enter で保存" hint append。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 14 行差替え。経路: iter313 と同 pattern を水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-period-goal-cmd-enter-iter314.ts` で Cmd/Ctrl+Enter 判定 + IME ガード + hint 文言 + iter228/iter313 invariant (comment-thread / team-context-editor の Cmd/Enter 維持) を assert。次 iter 候補: sprints-panel.tsx (retro_text textarea — 振り返り保存)、goals-panel.tsx (Goal description textarea — Goal 編集) に同 pattern 残り 2 件展開で workspace の主要 Textarea sweep 完結。
- ✅ [playwright-iter315 1/1] goals-panel の Goal 説明 Textarea (id="goal-desc") にも Cmd/Ctrl+Enter で作成 を展開、iter313/314 続編。form 内 Textarea でも default Enter は改行のため modifier 併用が必須、現状は title input から description textarea に Tab 移動した時点で Enter 押下では作成不可、Tab → 作成 button → Enter の冗長 keyboard UX。fix: Textarea の onKeyDown で 4 ガード (Cmd/Ctrl+Enter 判定 + IME 確定中 skip + title.trim 必須 + !createMut.isPending) を追加、`preventDefault()` で改行抑制、`handleCreate()` 起動。Label の文末に "Cmd/Ctrl+Enter で作成" hint を append、Textarea にも `aria-label` で同 hint 付与 (htmlFor Label と組合せで確実に SR 通知)。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 21 行差替え。経路: iter313/314 と同 pattern を水平展開、form 内 Textarea でも modifier 必須を確認 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-goals-desc-cmd-enter-iter315.ts` で goal-desc Textarea の onKeyDown / createMut.isPending ガード / hint 文言 + iter313/iter314 invariant 維持を assert。次 iter 候補: sprints-panel.tsx (sprint-goal Textarea + retro_text Textarea) に同 pattern 残り 2 件展開で workspace 主要 Textarea sweep 完結。
- ✅ [playwright-iter316 1/1] sprints-panel の sprint-goal Textarea にも Cmd/Ctrl+Enter で作成 を展開、iter313-315 続編。Sprint 作成は MVP の OKR-style プロジェクト start 起点で頻繁編集される。iter304 で noValidate 化は済だが、Textarea で default Enter は改行のため modifier 併用が必須、現状は name input から goal textarea に Tab 移動した時点で Enter 押下では作成不可。fix: id="sprint-goal" Textarea の onKeyDown で 4 ガード (Cmd/Ctrl+Enter 判定 + IME 確定中 skip + name.trim 必須 + !createMut.isPending) を追加、`preventDefault()` で改行抑制、`handleCreate()` 起動。Label と aria-label に "Cmd/Ctrl+Enter で作成" hint を append。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 21 行差替え。経路: iter313-315 と同 pattern を水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-sprint-goal-cmd-enter-iter316.ts` で sprint-goal Textarea の onKeyDown / createMut.isPending ガード / hint 文言 + iter313/iter314/iter315 invariant 維持を assert。次 iter 候補: sprint retro_text textarea 編集 UI (sprints-panel SprintRow 内) に同 pattern 残り 1 件展開で workspace 主要 Textarea sweep 完結 (decompose-proposals-panel の AI proposal 編集 textarea もまだ漏れあり要 sweep)。
- ✅ [playwright-iter317 1/1] decompose-proposals-panel の AI 提案編集 Textarea (id="p-desc-<proposalId>") にも Cmd/Ctrl+Enter で保存 を展開、iter313-316 続編。AI が出した子タスク提案を採用前に微調整する編集 UI で、proposal-row → 編集 mode → description textarea → Cmd+Enter で保存 → 元の proposal リストへ戻る hot path。iter306 で noValidate 化は済だが modifier 併用が漏れていた。fix: Textarea の onKeyDown で 4 ガード (Cmd/Ctrl+Enter + IME 確定中 skip + title.trim 必須 + !update.isPending) を追加、`preventDefault()` で改行抑制、`handleSaveEdit()` 起動。Label と aria-label に "Cmd/Ctrl+Enter で保存" hint を append。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 21 行差替え。経路: iter313-316 と同 pattern を水平展開、AI 採用フローの提案編集 textarea が最後のキー Textarea と特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-proposal-desc-cmd-enter-iter317.ts` で p-desc Textarea の onKeyDown / update.isPending ガード / hint 文言 + iter313-316 invariant 維持を assert。これで主要 5 Textarea (comment-thread × 2 + team-context + period-goal + goal-desc + sprint-goal + proposal-desc = 7 件) の Cmd/Ctrl+Enter sweep 達成、Textarea 入力の keyboard-only ユーザは Tab → button → Enter の冗長 step を全 Textarea で 1 step に短縮可能に。次 iter 候補: ItemEditDialog の subtasks-panel / dependencies-panel の inline edit textareas (subtasks-panel.tsx:468 のような textarea) を sweep。
- ✅ [playwright-iter318 1/1] subtasks-panel の bulk 追加 textarea (id="subtasks-bulk") にも Cmd/Ctrl+Enter で追加 を展開、iter313-317 続編。改行区切りで子タスクを一気に作る hot path で、Tab → 追加 button → Enter の冗長 keyboard UX が漏れていた。textarea の改行区切り仕様上 default Enter は必須なので modifier 併用が唯一の選択肢。fix: textarea の onKeyDown で 4 ガード (Cmd/Ctrl+Enter + IME 確定中 skip + bulkText.trim 必須 + !create.isPending) を追加、`preventDefault()` で改行抑制、`handleBulkAdd()` 起動。Label と aria-label に "Cmd/Ctrl+Enter で追加" hint を append。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 21 行差替え。経路: iter313-317 と同 pattern を水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-subtasks-bulk-cmd-enter-iter318.ts` で subtasks-bulk textarea の onKeyDown / create.isPending ガード / hint 文言 + iter313-317 invariant 維持を assert。これで Textarea Cmd/Ctrl+Enter sweep が 8 件 (comment × 2 + team-context + period-goal + goal-desc + sprint-goal + proposal-desc + subtasks-bulk) で workspace 主要書込 textarea ほぼ完結。次 iter 候補: ItemEditDialog の "編集" tab の description textarea / dependencies-panel の inline edit textareas (もし textarea があれば) を sweep audit、または別軸 (focus management / aria-busy 残漏れ) に切替。
- ✅ [playwright-iter319 1/1] create-workspace-form の `<form>` に aria-busy 不在 (auth login/signup form は iter255 で / budget-panel form は iter312 で aria-busy 付与済、本 form のみ漏れていた)。HomePage 初回 onboarding 起点で submit pending 中 SR が "busy" の audible 通知を受け取れず、誤って 2 度押下した時の動作も SR には不明、WCAG 4.1.3 (Status Messages, Level AA) 非対応。fix: `<form aria-busy={isPending || undefined} ...>` を追加 (1 行追加 + 改行整形で 3 行差替え)。`isPending` は `useTransition` の値、submit 中のみ `aria-busy="true"`、他は属性自体が DOM から消える (undefined fallback)。これで SR は pending 中 "busy" を 1 度 announce、終了で "no longer busy" 報告。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase grep で `<form\\b[^>]*?aria-busy=` 不在 form を sweep → 16 候補のうち最も visible な onboarding form (HomePage 起点) を選定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-create-workspace-aria-busy-iter319.ts` で create-workspace-form の aria-busy 存在 + iter255 (login/signup) / iter312 (budget-panel) invariant 維持を assert。次 iter 候補: 残 13 form (sprints × 3 / goals × 2 / decompose-proposals / integrations / workflows / time-entry / templates / template-items-editor / instantiate-form) の aria-busy sweep。
- ✅ [playwright-iter320 1/1] sprints-panel の 3 form (新規 Sprint 作成 / 既存 Sprint 期間編集 / workspace 全体 Sprint defaults) 全部に aria-busy 不在、iter319 続編 (1 panel 内 3 form の最大規模 batch)。Sprint 作成は MVP 主要 OKR-style プロジェクト start、期間編集 / defaults は workspace 設定経路、いずれも mutation pending 中 SR が "busy" を audible に通知できず WCAG 4.1.3 非対応。fix: 各 form に `aria-busy={<mut>.isPending || undefined}` を追加、それぞれ正しい mut (createMut / update / upd) を選定 (3 form, 3 行追加)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter319 sweep の続編、`<form>` 出現数 vs aria-busy 付き form 数 を audit → 0/3 → 3/3 達成 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-sprints-aria-busy-iter320.ts` で sprints-panel の 3 form aria-busy 全付与 + 各 mut 名 (createMut/update/upd) 正しさ + iter255/iter312/iter319 invariant を assert。次 iter 候補: 残 12 form (goals × 2 / decompose-proposals / integrations / workflows / time-entry / templates / template-items-editor / instantiate-form) の aria-busy sweep。
- ✅ [playwright-iter321 1/1] goals-panel の 2 form (新規 Goal / KR 追加) に aria-busy 不在、iter319-320 続編。Goal は OKR-style 主目標作成、KR 追加は Goal 配下の Key Result 編集 hot path で SR pending 通知漏れだった。fix: 各 form に `aria-busy={createMut.isPending || undefined}` (Goal 作成) と `aria-busy={create.isPending || undefined}` (KR 追加) を追加 (2 form, 2 行追加)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter319-320 sweep の続編、`<form>` 出現数 vs aria-busy 付き form 数 を audit → 0/2 → 2/2 達成 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-goals-aria-busy-iter321.ts` で goals-panel の 2 form aria-busy 全付与 + 各 mut 名 (createMut/create) + iter319/iter320 invariant 維持を assert。これで aria-busy sweep 進捗 9/15 production form (auth × 2 + create-workspace + budget + sprints × 3 + goals × 2 = 9 件達成)。次 iter 候補: 残 6 form (decompose-proposals / integrations / workflows / time-entry / templates / template-items-editor / instantiate-form) の aria-busy 続行。
- ✅ [playwright-iter322 1/1] aria-busy sweep の **最終回**、iter319-321 続編で残 7 panel form (decompose-proposals 編集 / integrations source 作成 / workflows 作成 / time-entry 作成 / templates 作成 / template-items-editor 子 Item 追加 / instantiate-form 即実行) を batch fix。各 form に `aria-busy={<mut>.isPending || undefined}` を追加 (7 行追加、各 file 1 行)、各 form の正しい mut 名 (update / create / createMut / addMut / mut) を選定。これで codebase 内 production form (mock-timesheet test fixture 除く) **16/16 件で aria-busy 統一達成**、WCAG 4.1.3 (Status Messages, Level AA) 適合 sweep 完結 (auth × 2 + workspace 作成 + budget + sprints × 3 + goals × 2 + decompose + integrations + workflows + time-entry + templates + template-items + instantiate = 16 件)。機能追加なし、shadcn 編集なし、影響面 7 ファイル。経路: iter319-321 と同 pattern を残 7 file に水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-aria-busy-final-iter322.ts` で codebase 全 production form sweep audit (16/16 件で aria-busy) + 本 iter 7 file 個別 invariant を assert。次 iter 候補: 別軸 (focus management / aria-live / dialog 内 focus restore) に切替、または iter256 で auth pages のみ対応した heading semantic を workspace pages にも sweep 展開 (iter311 で QuickAdd は対応済、残 24 件)。
- ✅ [playwright-iter323 1/1] sprints-panel の 3 CardTitle (新規 Sprint / Sprint name / Sprint デフォルト) に heading semantic を batch 付与、iter311 items-board 続編。shadcn `<CardTitle>` は `<div data-slot="card-title">` で role=heading 不在のため SR の heading navigation で skip 不可、WCAG 2.4.6 (Headings and Labels, Level AA) 非対応。fix: 各 CardTitle に `role="heading" aria-level={N}` を追加。階層に応じて: 新規 Sprint card / Sprint デフォルト card は panel header → level=2、Sprint name は各 Sprint row card 内 sub-section → level=3。shadcn 編集禁止ルールは props 経由 attr 追加で維持。機能追加なし、影響面 1 ファイル、約 9 行差替え (3 CardTitle で改行整形のため 1 行 → 3 行 × 3 件)。経路: iter311 items-board の同 pattern を sprints-panel に水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-sprints-card-heading-iter323.ts` で sprints-panel の 3 CardTitle に role="heading" 全付与 + aria-level=2/3 両併存 + iter311 invariant 維持を assert。次 iter 候補: goals-panel (2 CardTitle) / dashboard-view (2 CardTitle) / today-view (1 CardTitle) / pdca-panel / personal-period-view (2 CardTitle) / budget-panel / team-context-editor の sweep 続行。
- ✅ [playwright-iter324 1/1] goals-panel (2 CardTitle: 新規 Goal / Goal name) + budget-panel (1 CardTitle: AI 月次コスト) に heading semantic を batch 付与、iter311/iter323 続編 (2 panel batch)。fix: 各 CardTitle に `role="heading" aria-level={N}` を追加。新規 Goal card / AI 月次コスト card は panel header → level=2、Goal name は各 Goal row card 内 sub-section → level=3。shadcn 編集禁止ルール内 (props 経由)。機能追加なし、影響面 2 ファイル、約 7 行差替え (改行整形を含む)。経路: iter311/iter323 sweep の続編 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-goals-budget-card-heading-iter324.ts` で goals-panel 2/2 + budget-panel 1/1 CardTitle に role="heading" 付与 + iter311/iter323 invariant 維持を assert。これで heading semantic sweep 進捗 7 CardTitle 達成 (items-board 1 + sprints 3 + goals 2 + budget 1)。次 iter 候補: dashboard-view (2 CardTitle: バーンダウン + AI コスト) / today-view (1 CardTitle: グループ見出し) / personal-period-view (2 CardTitle) / pdca-panel (1 CardTitle) / team-context-editor (1 CardTitle) の sweep 続行。
- ✅ [playwright-iter325 1/1] dashboard-view (2 CardTitle: バーンダウン / MUST 一覧) + today-view (1 CardTitle: グループ見出し) + pdca-panel (1 CardTitle: PDCA) に heading semantic を batch 付与、iter311/iter323/iter324 続編 (3 panel batch)。Today グループ見出し ("期限超過" / "今日" / "明日以降" 等) は全 SR ユーザに頻繁出現する重要 heading で、heading navigation skip 可能になり情報量が大きい。fix: 各 CardTitle に `role="heading" aria-level={2}` を追加 (4 CardTitle, 4 行差替え + 改行整形)。shadcn 編集禁止ルール内 (props 経由)。機能追加なし、影響面 3 ファイル。経路: iter311/iter323/iter324 sweep の続編 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-dashboard-today-pdca-card-heading-iter325.ts` で 3 file 各々 CardTitle 全数に role="heading" 付与 + iter311/iter323/iter324 invariant 維持を assert。これで heading semantic sweep 進捗 11 CardTitle 達成 (items-board 1 + sprints 3 + goals 2 + budget 1 + dashboard 2 + today 1 + pdca 1)。次 iter 候補: personal-period-view (2 CardTitle) / team-context-editor (1 CardTitle) の sweep 続行で workspace 主要 panel 残り 3 件で完結。
- ✅ [playwright-iter326 1/1] personal-period-view (2 CardTitle: ゴール / Item) + team-context-editor (1 CardTitle: チームコンテキスト) に heading semantic batch 付与、iter311/iter323-325 続編で **workspace 主要 CardTitle sweep 完結** (累計 14/14 件達成)。fix: 各 CardTitle に `role="heading" aria-level={2}` を追加 (3 CardTitle, 3 行差替え + 改行整形)。shadcn 編集禁止ルール内 (props 経由)。機能追加なし、影響面 2 ファイル。経路: iter311/iter323-325 sweep の続編 → source-side regex assert で codify (workspace 全体 sweep audit を含む)。検証 codify: `scripts/explore-uiux-period-team-card-heading-iter326.ts` で personal-period-view 2/2 + team-context-editor 1/1 + workspace 全体 14/14 件 heading semantic 付与を assert。これで NVDA H key / VoiceOver Cmd+Opt+H で全 workspace 主要 panel header に skip 可能、SR ユーザの線形 read 強要から解放、WCAG 2.4.6 (Headings and Labels, Level AA) 適合性大幅向上。次 iter 候補: 別軸 (focus management / dialog 内 focus restore / aria-live 通知系) に切替、または ItemEditDialog 内 5 tab 各々の Tab の aria-label / 切替時の focus 適切化に着手。
- ✅ [playwright-iter327 1/1] ItemEditDialog の TabsList に aria-label 不在 + Activity tab だけ英語残留の 2 件 a11y / WCAG 3.1.2 違反を 1 iter で修正。(1) Radix `<TabsList>` (role="tablist") は aria-label が無いと SR ユーザが「何のタブグループか」を知る手掛かりを失う、ここを `aria-label="Item 編集タブ"` で landmark / SR ナビ可能に。(2) "Activity" 1 件だけ英語で、他 4 タブ (基本 / 子タスク / 依存 / コメント) と locale 混在 → WCAG 3.1.2 (Language of Parts, Level AA) 適合性低下、"アクティビティ" にローカライズ統一。data-testid (tab-activity) は不変で E2E 影響無し。機能追加なし、shadcn 編集なし (Radix Primitive は本 fix で props 経由で aria-label 渡す)、影響面 1 ファイル、約 4 行差替え。経路: codebase grep で Tabs / TabsList 使用箇所 を確認 → ItemEditDialog のみ aria-label 漏れ + 英語残留と特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-item-tabs-a11y-iter327.ts` で TabsList aria-label 存在 + Activity tab の "アクティビティ" 表示 + 旧英語 "Activity" 残存無しを assert。次 iter 候補: TabsTrigger 切替時に対応 TabsContent 内の最初の interactive element に focus 移動 (現状 keyboard ユーザは Tab 切替後に Tab で次 element に進む必要、`autoFocus` 等で自動移動する UX 改善検討)。
- ✅ [playwright-iter328 1/1] integrations-panel の HTTP method 入力欄ラベルを日本語化 (iter294/iter301/iter302 locale sweep の続編)。Source 作成 form の HTTP method 選択 select の Label "method" (英語小文字) と aria-label "HTTP method" が他 form ラベル群 ("名前" / "URL" / "json body") の中で localize 漏れ、WCAG 3.1.2 (Language of Parts, Level AA) 違反。fix: Label "method" → "HTTP メソッド"、aria-label "HTTP method" → "HTTP メソッド (GET / POST)" にローカライズ。htmlFor=id="src-method" 連携と option (GET / POST 大文字略語) は技術用語として維持。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 3 行差替え。経路: codebase grep で `aria-label="[A-Z]` 残留を sweep → integrations-panel の "HTTP method" 1 件のみ特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-http-method-localize-iter328.ts` で 旧英語 Label / aria-label 残存無し + 日本語 "HTTP メソッド" 存在を assert。次 iter 候補: codebase 全体 `aria-label="[A-Z]` sweep audit を継続 (一部技術略語 [MUST / AI] は意図的維持) して残留 localize 候補を網羅。
- ✅ [playwright-iter329 1/1] workflows-panel の Workflow description Textarea (id="wf-desc") にも Cmd/Ctrl+Enter で作成 を展開、iter313-318 続編 (Textarea Cmd+Enter sweep の 9 件目)。fix: Textarea の onKeyDown で 4 ガード (Cmd/Ctrl+Enter + IME 確定中 skip + name.trim 必須 + !create.isPending) を追加、`preventDefault()` で改行抑制、`handleCreate()` 起動。Label と aria-label に "Cmd/Ctrl+Enter で作成" hint を append。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 17 行差替え。経路: iter313-318 sweep の続編 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-wf-desc-cmd-enter-iter329.ts` で wf-desc Textarea の onKeyDown / create.isPending ガード / hint 文言を assert。これで Cmd/Ctrl+Enter sweep 累計 9 Textarea 達成 (comment × 2 + team-context + period-goal + goal-desc + sprint-goal + proposal-desc + subtasks-bulk + wf-desc)。次 iter 候補: workflows-panel 内の他 Textarea (line 443 / 524) や templates-panel description Textarea も同 pattern 候補。
- ✅ [playwright-iter330 1/1] templates-panel の Template description Textarea (id="tmpl-desc") にも Cmd/Ctrl+Enter で作成 を展開、Cmd+Enter sweep 10 件目 (累計 10 Textarea: comment × 2 + team-context + period-goal + goal-desc + sprint-goal + proposal-desc + subtasks-bulk + wf-desc + tmpl-desc)。fix: Textarea の onKeyDown で 4 ガード + Label と aria-label に hint append。機能追加なし、影響面 1 ファイル、約 17 行差替え。検証 codify: `scripts/explore-uiux-tmpl-desc-cmd-enter-iter330.ts`。次 iter 候補: workflows-panel.tsx の workflow editor (line 443 / 524 の graph / trigger Textarea — JSON 編集なので Cmd+Enter は careful)、または別軸に切替。
- ✅ [playwright-iter331 1/1] kanban-view.tsx の card 編集 button が `aria-label="編集"` だけで Kanban 内の複数 card で SR が item 区別不能、さらに visible 内容 `✎` (U+270E LOWER RIGHT PENCIL) に aria-hidden 無しで brave SR 実装は unicode 名を冗長読み上げ。WCAG 1.3.1 (Info and Relationships, Level A) 違反。fix: (1) aria-label を `「${item.title}」を編集` に context-rich 化 (各 card 区別可能、start-timer-button iter310 と同 pattern)、title attr に同文付与で hover ユーザにも tooltip。(2) `<span aria-hidden="true">✎</span>` で a11y tree から ✎ char を除外、SR が unicode 名を読み上げないように。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 6 行差替え。経路: kanban-view.tsx grep で `aria-label="編集"` 短文発見 → start-timer-button iter310 と同 pattern を Kanban に水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-kanban-edit-aria-iter331.ts` で 旧短文 aria-label 残存無し + context-rich aria-label + ✎ aria-hidden 存在を assert。次 iter 候補: 同 pattern (短すぎる aria-label と icon char aria-hidden 漏れ) を他 panel sweep — backlog table の編集 button / activity-log の icon button 等。
- ✅ [playwright-iter332 1/1] notification-preferences (workspace ヘッダ右端 utility) の Settings icon に aria-hidden 不在 + Button の aria-label が短すぎ "通知設定" だけ (内容不明) + `aria-haspopup="dialog"` 漏れ (notification-bell iter23 では付与済で対称化)、3 件 a11y gap を 1 iter で修正。fix: (1) `<Icon className="h-4 w-4" aria-hidden="true" />` で SR 冗長 announce 抑制 (Button の aria-label が完全に意味伝達済、icon は decorative)。(2) aria-label を「通知設定 (メール通知 4 種を ON/OFF)」に拡充して行動見通し改善。(3) `aria-haspopup="dialog"` 追加で SR が popover 形式と認識可能に。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 4 行差替え。経路: notification-bell.tsx (iter23) と notification-preferences.tsx の差分を比較して非対称を発見 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-notif-prefs-icon-aria-iter332.ts` で Icon aria-hidden + 拡充 aria-label + aria-haspopup の 3 invariant を assert。次 iter 候補: 他 PopoverTrigger / DropdownMenu Trigger の `aria-haspopup` sweep audit。
- ✅ [playwright-iter333 1/1] HomePage (`/`) の 2 Card (最初の Workspace 作成 / 別の Workspace 作成) に role="region" + CardTitle に heading semantic を batch 付与、iter311/iter323-326 の workspace 内 sweep の HomePage 拡張。fix: (1) 各 Card に `role="region" aria-labelledby="<heading-id>"`。(2) CardTitle に `id="..." role="heading" aria-level={2}` 付与。これで HomePage の 2 つの主要 entry point (初回 onboarding / 追加作成) が SR landmark + heading navigation 両方で skip 可能に。shadcn 編集禁止ルール内 (props 経由)。機能追加なし、影響面 1 ファイル、約 8 行差替え。経路: HomePage 読みで Card / CardTitle に a11y 属性無しを発見 → iter311/iter323-326 sweep の補完として水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-home-card-region-iter333.ts` で 2 Card に role="region" + 2 CardTitle に id + role="heading" + aria-level={2} 全付与を assert。次 iter 候補: HomePage の `<header>` (line 26) は実 ARIA banner role を持つが visible h1 の隣に form (logout) があるので構造はすでに OK。WorkspaceHeader.tsx の Badge が "あなたの workspace role: ..." aria-label で行動指示無しなど別軸 audit を続行。
- ✅ [playwright-iter334 1/1] engineer-trigger-button (Engineer Agent 起動 button) に aria-busy 不在、iter319-322 の form aria-busy sweep の Button 拡張。Engineer Agent 起動は git worktree + Claude による実装で長時間 (数十秒〜数分) のため、disabled だけで「禁止状態」しか SR に伝えていない。aria-busy 併用で「処理進行中」の意味を別軸で通知できる (WCAG 4.1.3 Status Messages, Level AA 適合性向上)。fix: `<Button ... aria-busy={trigger.isPending || undefined} ...>` 追加 (1 行)。pending 中のみ aria-busy="true"、idle 時は属性自体が DOM から消える (undefined fallback)。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 4 行差替え (コメント含)。経路: codebase grep で `disabled={[a-zA-Z]+\\.isPending}` を持つ Button のうち aria-busy 不在のものを sweep → engineer-trigger-button.tsx を選定 (Engineer 起動は最長時間 mutation なので busy 通知の価値が大きい) → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-engineer-aria-busy-iter334.ts` で aria-busy={trigger.isPending || undefined} 存在を assert。次 iter 候補: 他 long-running mutation Button (HeartbeatButton iter106 / StandupButton / item-decompose-button / item-research-button) も同 pattern で aria-busy 追加。
- ✅ [playwright-iter335 1/1] long-running mutation Button 4 件 (HeartbeatButton scan / StandupButton standup / ItemDecomposeButton decompose / ItemResearchButton research) に aria-busy を batch 付与、iter334 続編 (Button aria-busy sweep の 5 件目で完結)。各 button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (4 file, 4 行追加)。これで disabled (禁止状態) と aria-busy (処理進行中) の両軸を SR が区別可能、AI 起動 / agent worker / heartbeat scan の数秒〜数十秒の処理進行が明確に伝わる。機能追加なし、shadcn 編集なし、影響面 4 ファイル。経路: iter334 sweep の続編、各 button file に 1 行追加 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-button-aria-busy-batch-iter335.ts` で 4 button に aria-busy 全付与 + iter334 invariant を assert。これで Button aria-busy sweep 累計 5 件達成 (engineer + heartbeat + standup + decompose + research)。次 iter 候補: 他 mutation button (item-edit-dialog の baseline / archive / unarchive button、archived-items-panel の restore button) で同 pattern audit。
- ✅ [playwright-iter336 1/1] ItemEditDialog の 5 mutation Button (Save / Archive / Unarchive / Set baseline / Clear baseline) + ArchivedItemsPanel の Restore button に aria-busy を batch 付与、iter334-335 続編 (Button aria-busy sweep の 11 件目達成)。各 Button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (6 箇所 / 2 file, 6 行追加)。Save / Archive / Baseline 系 mutation は楽観ロック競合 retry を含むため pending 時間が予測しづらく busy 通知の価値が大きい。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: iter334-335 sweep の続編、item-edit-dialog.tsx grep で `disabled={X.isPending}` 5 箇所 + archived-items-panel.tsx 1 箇所 を sweep → 各 button に 1 行追加 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-item-dialog-aria-busy-iter336.ts` で 5 mutation 名 (unarchive / archive / setBaseline / clearBaseline / update) の aria-busy + restore の aria-busy を assert。Button aria-busy sweep 累計 11 件達成 (engineer + heartbeat + standup + decompose + research + save + archive + unarchive + setBaseline + clearBaseline + restore)。次 iter 候補: comment-thread / sprints-panel / goals-panel / decompose-proposals-panel の各 inline button (delete / edit / accept / reject 等) の aria-busy sweep。
- ✅ [playwright-iter337 1/1] comment-thread の 4 mutation Button (Post / Save / Edit / Delete) に aria-busy を batch 付与、iter334-336 続編 (Button aria-busy sweep の 12-15 件目達成)。各 button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (4 箇所, 4 行追加)。これでコメント投稿 / 編集保存 / 編集 mode 切替 / 削除いずれの mutation pending 中も SR が "busy" を audible に報告。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter334-336 sweep の続編、comment-thread.tsx grep で 4 箇所 → 1 行追加 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-comment-aria-busy-iter337.ts` で create / update / softDelete (edit + delete 2 件) の aria-busy 全付与を assert。Button aria-busy sweep 累計 15 件達成。次 iter 候補: sprints-panel の inline 各 button (status 変更 / cancel / delete / retro 等)、goals-panel の inline button (decompose / archive / KR delete 等)、decompose-proposals-panel の accept / reject button の aria-busy sweep。
- ✅ [playwright-iter338 1/1] decompose-proposals-panel の 6 mutation Button (Cancel agent / Accept all / Reject all / Redecompose / Redecompose-fresh / Save edit) に aria-busy を batch 付与、iter334-337 続編 (Button aria-busy sweep の 16-21 件目達成)。各 button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (6 箇所, 6 行追加)。AI 提案 cancel / accept all / reject all / redecompose 全部が長時間 mutation で busy 通知の価値が大きい。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter337 sweep の続編、decompose-proposals-panel.tsx grep で 6 箇所 → 1 行追加 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-decompose-aria-busy-iter338.ts` で 5 mutation 名 (cancel / accept / rejectAll / decompose ×2 / update) の aria-busy 全付与を assert。Button aria-busy sweep 累計 21 件達成。次 iter 候補: sprints-panel の inline button (各 sprint row の status 変更 / retro / pre-mortem button) 等。
- ✅ [playwright-iter339 1/1] sprints-panel (4 button: Sprint period save / Retro 生成 / Pre-mortem 生成 / defaults save) + goals-panel (7 button: 5 status change × goal-decompose × KR-delete) に aria-busy を batch 付与、iter334-338 続編 (Button aria-busy sweep の 22-32 件目達成、累計 32 button)。Sprint Retro / Pre-mortem は PM Agent 起動の長時間 mutation、goal-decompose は AI 5-10 件 Item 作成、KR-delete は楽観ロック付き soft delete で全て busy 通知の価値が大きい。fix: 各 button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (11 箇所, 11 行追加、replace_all で同 testid pattern を batch)。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: iter334-338 sweep の続編、sprints-panel + goals-panel grep で全 button 候補を sweep → 1 行追加 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-sprints-goals-aria-busy-iter339.ts` で sprints-panel 4 mutation (update/retroPending/premortemPending/upd) + goals-panel 5 update + decompose + remove = 11 invariants を assert。Button aria-busy sweep 累計 32 件達成。次 iter 候補: integrations-panel の Pull / save / delete button、workflows-panel の Run / save / delete button、subtasks-panel の indent/outdent / bulk-add button 等。
- ✅ [playwright-iter340 1/1] integrations-panel (4 button: Pull / toggle / delete / create) + workflows-panel (5 button: create / run / toggle / delete / run-rerun) に aria-busy を batch 付与、iter334-339 続編 (Button aria-busy sweep の 33-41 件目達成、累計 41 button)。各 button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (9 箇所, 9 行追加)。Pull / Run は外部 API 連携 / agent worker 起動で長時間、create / toggle / delete は楽観ロック付き mutation で busy 通知の価値が大きい。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: iter339 sweep の続編、各 panel grep で全 button 候補 → 1 行追加 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-integ-wf-aria-busy-iter340.ts` で integrations 4 + workflows 4 (trigger × 2 / update / del / create) の aria-busy 全付与を assert。Button aria-busy sweep 累計 41 件達成。次 iter 候補: subtasks-panel の indent/outdent + bulk-add button、time-entry の inline button 等の小さめ Button。
- ✅ [playwright-iter341 1/1] subtasks-panel (3 button: indent/outdent ×2 + bulk-add) + template-items-editor (2 button: add + remove) + instantiate-form (1 button: 即実行) + templates-panel (2 button: create + delete) + time-entry (2 button: create + sync) に aria-busy を batch 付与、iter334-340 続編 (Button aria-busy sweep の 42-51 件目達成)。各 button の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (10 箇所, 10 行追加)。indent/outdent は子タスク並び替えの movePending、template add/remove / instantiate / time-entry sync 全て短〜中時間 mutation で busy 通知の価値あり。機能追加なし、shadcn 編集なし、影響面 6 ファイル。経路: iter340 sweep の続編、各 panel grep で button 候補 → 1 行追加 → source-side regex assert で codify (codebase 全 sweep audit を含む)。検証 codify: `scripts/explore-uiux-subtasks-template-time-aria-busy-iter341.ts` で 6 file 各々の aria-busy expected 全付与 + codebase sweep 集計 (78 disabled w/ Pending vs 67 aria-busy w/ Pending、残 11 件の中には disabled-only で aria-busy 不要な case も含まれる) を audit。Button aria-busy sweep 累計 51 件達成 (workspace + integrations + workflows + templates + time-entry 全主要 panel 制覇)。次 iter 候補: 残 disabled-only 11 件の中の真の漏れ (active-timer-panel pause/resume/stop / item-edit-dialog 内 picker etc) を sweep audit。
- ✅ [playwright-iter342 1/1] active-timer-panel (2 button: PiP open/close + Stop) + notification-preferences (4 checkbox toggle, 1 行で 4 件カバー) に aria-busy を batch 付与、iter334-341 続編。fix: 各 button / input の disabled の隣に `aria-busy={X.isPending || undefined}` を追加 (3 箇所; ToggleSpec map なので checkbox 1 行で 4 toggle 全部対応)。PiP は browser API 起動 / Stop は time_entries 書込 / notification toggle は user mutation で busy 通知の価値あり。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: iter341 sweep の続編、disabled w/o aria-busy の 11 件残から filter audit → active-timer-panel 2 + notification-preferences 1 を選定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-active-timer-prefs-aria-busy-iter342.ts` で 3 invariants (pipPending / create.isPending / update.isPending) を assert。Button aria-busy sweep ほぼ完結 — 残 disabled-only 件は idempotent click (Save なし、validation だけ) や同 mutation の二重参照で aria-busy 不要な case が大半。次 iter 候補: 別軸 (focus management / aria-live / form 内 error 通知) に切替。
- ✅ [playwright-iter343 1/1] create-workspace-form の name + slug input に `autoComplete="off"` 不在。Workspace 名 / slug は user 固有 / app 固有で browser の autoComplete 候補 (organization / username / name 等) と意味的にも合致せず、空欄で browser が無関係な候補をドロップダウン表示すると UX 阻害。auth login/signup は autoComplete="email" / "current-password" / "new-password" / "name" と適切に設定済 (iter255 周辺)、本 form のみ未対応。fix: name / slug input に `autoComplete="off"` 明示 (slug は既存の `autoCapitalize="none" autoCorrect="off" spellCheck={false}` の隣)。Chrome / Safari / Firefox 全部 respect (Chrome は password 系で override 仕様だが本 form は password 無し、影響なし)。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 4 行差替え (コメント含)。経路: codebase grep で `autoComplete=` を持つ form と持たない form を比較 → create-workspace-form.tsx のみ漏れと特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-create-ws-autocomplete-iter343.ts` で create-workspace-form の autoComplete="off" 2 件 + auth forms の email autoComplete invariant を assert。次 iter 候補: 他 panel の form input でも autoComplete 漏れ audit (sprints / goals / templates 等は app 固有 input なので "off" 候補)。
- ✅ [playwright-iter344 1/1] sprints-panel (sprint-name) + goals-panel (goal-title + KR title) + templates-panel (tmpl-name + tmpl-cron) の主要 IMEInput に `autoComplete="off"` を batch 追加、iter343 続編 (autoComplete sweep の 5 件目で workspace 主要 input 制覇)。tmpl-cron はさらに `spellCheck={false}` も追加 (cron 表記は app 固有で spell check 誤検出になりがち)。各 IMEInput の最後に attribute を追加 (5 箇所, 5+1 行追加)。これで Sprint / Goal / KR / Template の名前 input すべてで browser auto-fill 誤候補が抑制される。機能追加なし、shadcn 編集なし、影響面 3 ファイル。経路: iter343 sweep の続編 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-panels-autocomplete-iter344.ts` で 3 file 各々の autoComplete="off" 件数 + iter343 invariant 維持を assert。次 iter 候補: integrations-panel の Source 名 / URL / API token / project IDs input、workflows-panel の Workflow 名 input にも同 pattern 展開。
- ✅ [playwright-iter345 1/1] integrations-panel (4 input: src-name / src-token / src-project-ids / src-url) + workflows-panel (1 input: wf-name) + template-items-editor (1 input: 子 Item title) に autoComplete を batch 追加、iter343/344 続編 (autoComplete sweep の 11 件目で workspace 主要 input 制覇)。token は password manager の autofill を避けたいので `autoComplete="new-password"` (既存 password 補完抑制 + suggest UX は維持)、他は `autoComplete="off"`。URL / project-IDs / token に `spellCheck={false}` も追加 (slug-style / 機微情報、spell 誤検出を回避)。機能追加なし、shadcn 編集なし、影響面 3 ファイル。経路: iter344 sweep の続編 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-integ-wf-tmpl-autocomplete-iter345.ts` で 3 file 各々の autoComplete 件数 + token new-password 存在を assert。これで autoComplete sweep 累計 11 input 達成 (auth 5 + workspace 主要 11 = 16+)。次 iter 候補: time-entry の input (date / category / minutes / description) や budget-panel の number 入力に autoComplete 追加 audit。
- ✅ [playwright-iter346 1/1] startDate input に `max={endDate}` を追加して date range 制約を双方向化、4 file batch (item-edit-dialog editStart / goals-panel goal-start / sprints-panel sprint-start + sprint-edit-start)。各 form は endDate 側に既設 `min={startDate}` で start>=end のみガード、start を end 後に変更すると HTML5 native validation を passing する片方向の漏れ。fix: startDate input に `max={endDate || undefined}` を追加 (4 箇所、4 行追加)。これで HTML5 native で start <= end の双方向ガード、サーバ側 zod も runtime validation で同制約 (defense in depth)。機能追加なし、shadcn 編集なし、影響面 3 ファイル。経路: codebase grep で `type="date"` 全箇所を audit → endDate に min はあるが startDate に max が無い 4 form を特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-date-max-iter346.ts` で 4 startDate input の max 制約 全付与を assert。次 iter 候補: time-entry の workDate input は max=today で固定なので OK、template-items-editor の 期日 offset 数値入力には max={365}/min={0} の妥当範囲を audit (現状 type="number" で min なし)。
- ✅ [playwright-iter347 1/1] template-items-editor の 期日 offset number input に min={0} max={365} step={1} inputMode="numeric" を追加。HTML5 制約無しで負の値 / 1 年超 / 小数の誤入力を抑止できない、schema 側で正整数制約だが UX 早期段階で防ぎたい。mobile keyboard は inputMode 無いと alphanumeric が出てしまう。fix: 妥当範囲 0-365 日 + step=1 + inputMode="numeric" を追加 (4 行)。aria-label の文末に "0-365" 範囲を append。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase grep で `type="number"` 全箇所を audit → template-items-editor の 期日 offset のみ min/max/step 全部漏れと特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-due-offset-min-max-iter347.ts` で 4 attribute 全付与を assert。次 iter 候補: KR target input にも inputMode="decimal" 追加 audit (mobile UX)。
- ✅ [playwright-iter348 1/1] goals-panel の KR target number input に `inputMode="decimal" step="any"` + unit input に `autoComplete="off"` を追加。target は decimal (% / hours / 0.5 など) 許容、step デフォルト "1" だと arrow キーで小数化不可、mobile keyboard も整数 keypad 表示。unit は app 固有 ("件" / "%" / "ms" 等) で browser autoComplete 候補は無関係。fix: target に inputMode="decimal" + step="any" (2 行)、unit に autoComplete="off" (1 行)。機能追加なし、shadcn 編集なし、影響面 1 ファイル、約 3 行追加。経路: iter347 sweep の続編、KR 編集 form 内 target / unit input を audit → 両者対応。検証 codify: `scripts/explore-uiux-kr-target-inputmode-iter348.ts` で 3 invariants (inputMode + step + unit autoComplete) を assert。次 iter 候補: KR progress edit (manual mode の current value input) も同 pattern audit (gp 内 KR row 編集 textarea / form)。
- ✅ [playwright-iter349 1/1] budget-panel (2 number input: limit + warn) + sprints-panel (sprint-defaults-length) + time-entry (teMinutes) に inputMode を追加 (mobile keyboard 最適化)、iter347-348 続編。type="number" は desktop 専用、mobile keyboard では browser ごと挙動差異があり inputMode 明示が必須。USD ($) や warn threshold (0..1) は decimal 入力、Sprint 期間 / 稼働 minutes は integer なので numeric。fix: budget-panel limit/warn に inputMode="decimal"、sprint-defaults-length に step={1} + inputMode="numeric"、teMinutes に inputMode="numeric"。aria-label に範囲 hint も append (sprint-defaults-length: "1-90")。機能追加なし、shadcn 編集なし、影響面 3 ファイル、約 5 行追加。経路: iter347-348 sweep の続編、type="number" 全 input を audit → 4 input が inputMode 漏れと特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-number-inputmode-iter349.ts` で 4 invariants を assert。これで type="number" inputMode sweep 累計 5 件 (templating-items 期日 + KR target + budget × 2 + sprint defaults + teMinutes = 6 件) で codebase 全 production number input 制覇。次 iter 候補: 別軸に切替 (focus management / aria-live / keyboard shortcuts hint)。
- ✅ [playwright-iter350 1/1] QuickAdd input (workspace 主要 entry point) に `autoComplete="off"` 追加。task title 自然言語入力 (例: "明日15時 p1 #会議 1時間 打ち合わせ準備") は app 固有用語 + 各回新規入力なので browser autoComplete 候補 (organization / username 等) は無関係、candidate dropdown が頻繁な hot path 入力中の集中を阻害する。fix: `autoComplete="off"` 追加 (1 行 + コメント 3 行)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter343-348 autoComplete sweep の最終回として workspace 主要 entry point を audit → quick-add-input が漏れと特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-quick-add-autocomplete-iter350.ts` で quick-add-input autoComplete="off" を assert。これで workspace 主要 input の autoComplete sweep 完結 (auth + create-workspace + sprint/goal/template panel + integrations/workflows + KR + QuickAdd = 12+ 主要 input)。次 iter 候補: ItemEditDialog の各 inline input (DoD など) も同 pattern audit。
- ✅ [playwright-iter351 1/1] 高影響面の 3 component (async-states EmptyState / dashboard-chip / comment-thread の "まだコメント" 状態) で `role="status"` の隣に `aria-live="polite"` を明示付与。WAI-ARIA spec で `role="status"` は default aria-live="polite" だが、一部 SR (古い JAWS / NVDA 古版) は default を respect せず明示が必要。fix の影響面: (1) async-states.EmptyState は Today/Inbox/Workflows/Sprints/Goals/items-board/archive 全 view 共通、(2) dashboard-chip は Dashboard で 20+ chip を生成、(3) comment-thread の "まだコメントはありません" 状態。3 file × 1 行追加で広範囲 SR 通知改善。機能追加なし、shadcn 編集なし。経路: codebase grep で `role="status"` 16 箇所中 explicit aria-live 無の 14 箇所を sweep → 影響面の広い 3 件を選定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-status-aria-live-iter351.ts` で 3 file 各々 `role=status + aria-live=polite` 併用を assert。次 iter 候補: 残 11 箇所の role=status (activity-log / budget-panel / subtasks-panel / notification-bell / integrations / workflows / top-items chip) も同 pattern 展開。
- ✅ [playwright-iter352 1/1] 残 11 箇所 + 旧計上漏れ 1 箇所の role=status (activity-log 2 / budget-panel 1 / subtasks-panel 2 / notification-bell 1 / integrations 1 / workflows 2 / top-items-by-time-chip 3) に aria-live="polite" を batch 追加、iter351 sweep 完結。これで codebase **全 30 件の role="status" に aria-live="polite" 明示** (cross-SR 互換性 100%、JAWS / NVDA / VoiceOver 全 SR で polite announce 保証)。機能追加なし、shadcn 編集なし、影響面 7 ファイル、約 12 行追加。経路: codebase 全 sweep audit (verifier 内蔵) で 30/30 達成を assert。検証 codify: `scripts/explore-uiux-status-aria-live-final-iter352.ts` で codebase 全 role="status" 件数 vs aria-live 件数を audit (30=30 で完了)。次 iter 候補: 別軸 (form 入力 IME / mobile UX / focus management) に切替。
- ✅ [playwright-iter353 1/1] integrations-panel の Source 設定 4 path input (items-path / due-path / id-path / title-path) に `autoComplete="off"` + `spellCheck={false}` を batch 追加。各 path は API JSON の dot-path (例 "data.items" / "due_date") で app 固有 ID 風文字列、browser autoComplete 候補は無関係 / spell check は slug-style キーで誤検出 (赤線で集中阻害)。fix: 4 input に 2 attribute 追加 (8 行追加) + iter352 verifier の TS 文法 fix (lines[i] undefined ガード) を含む。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter343-345 autoComplete sweep の続編、Source 設定 path input は技術文字列で spellCheck も含めて off が望ましいと特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-source-paths-iter353.ts` で 4 input 各々 autoComplete + spellCheck の併用を assert。次 iter 候補: 別軸に切替 (form の inline error 表示 / focus management) 。
- ✅ [playwright-iter354 1/1] ItemEditDialog の 3 input (editTitle / editDescription / editDod) に `autoComplete="off"` を batch 追加、iter343-350 autoComplete sweep の続編。タスク title / description / DoD は app 固有テキストで browser autoComplete 候補は無関係、編集 dialog で頻繁な hot path 入力中の集中を阻害。fix: 3 IMEInput に autoComplete="off" を追加 (3 行)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: iter353 sweep の続編、ItemEditDialog 内 IMEInput 全 5 件のうち 3 件 (title / description / DoD) が漏れと特定 (date input 2 件は max/min 制約済で対象外) → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-item-dialog-autocomplete-iter354.ts` で 3 input 各々 autoComplete="off" を assert。これで autoComplete sweep 累計 18+ 主要 input 達成。次 iter 候補: 別軸 (focus management / dialog 内 keyboard nav) に切替。
- ✅ [playwright-iter355 1/1] CommandPalette の CommandDialog に `description` prop を渡して SR の sr-only DialogDescription に keyboard hint を流し込む。shadcn/ui CommandDialog は title (DialogTitle) + description (DialogDescription) を props で受けるが現状 description 不在 → SR は title "コマンドパレット" のみ読み上げ、操作方法 (Cmd+K / ? task 検索 / Enter / Esc) が伝わらない。fix: description="Cmd / Ctrl+K で開閉、コマンドを検索 or `?` でタスクを fuzzy 検索、Enter で実行、Esc で閉じる" を渡す (4 行差替え)。視覚 UI は変更なし、SR ユーザは dialog open 直後 + focus 移動と同時に announce される。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: shadcn CommandDialog の type 定義を確認 → description prop 受けると分かったので追加。検証 codify: `scripts/explore-uiux-command-palette-desc-iter355.ts` で CommandDialog description prop の存在を assert。次 iter 候補: 同 pattern (sr-only description で keyboard hint) を他 Dialog (ItemEditDialog の DialogDescription / WorkflowEditorDialog 等) で audit。
- ✅ [playwright-iter356 1/1] comment-thread の Post button に `aria-keyshortcuts="Meta+Enter Control+Enter"` を追加 (codebase 内 ARIA 1.2 keyshortcuts 初出)。WCAG 2.1.4 (Character Key Shortcuts) のための a11y hint で、voice-control (Dragon NaturallySpeaking / Voice Access) が "press shortcut" 形で利用、SR (NVDA / VoiceOver) も focus 時に shortcut hint を読み上げ可能。Meta は macOS Cmd、Control は Windows / Linux Ctrl で空白区切りで複数 shortcut 列挙。aria-label の "(Cmd/Ctrl+Enter でも可)" hint は visible には不可な keyboard ユーザ向け、aria-keyshortcuts は voice-control / SR の programmatic 読み出し用で 2 軸補完。fix: `aria-keyshortcuts="Meta+Enter Control+Enter"` 追加 (1 行)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: codebase grep で aria-keyshortcuts 不在を確認 → iter228 Cmd+Enter pattern の最初の bind 先 (comment post) を選定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-comment-aria-keyshortcuts-iter356.ts` で aria-keyshortcuts 文字列の存在を assert。次 iter 候補: 他 Cmd+Enter 持つ button (team-context save / period-goal save / goal/sprint create) にも同 pattern 展開。
- ✅ [playwright-iter357 1/1] 5 button (team-context save / period-goal save / goal create / sprint create / subtasks bulk-add) に `aria-keyshortcuts="Meta+Enter Control+Enter"` を batch 追加、iter356 続編 (累計 6 button)。各 button は対応する Textarea / form で iter313-318 / iter313-316 で実装した Cmd/Ctrl+Enter shortcut を受ける hot path。voice-control / SR が programmatic に shortcut を読み出せる。あわせて team-context-save-btn と period-goal-save に `aria-busy={X.isPending || undefined}` も追加 (iter334-342 sweep の補完で 2 button)。機能追加なし、shadcn 編集なし、影響面 5 ファイル、約 7 行追加。経路: iter356 の続編、Cmd/Ctrl+Enter 実装 5 panel に水平展開 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-keyshortcuts-batch-iter357.ts` で 5 file の aria-keyshortcuts + iter356 invariant 維持を assert。これで Cmd+Enter shortcut 持つ全 button 6 件で aria-keyshortcuts 統一達成。次 iter 候補: 別軸 (focus management on dialog open / breadcrumb / toast positioning) に切替。
- ✅ [playwright-iter358 1/1] `src/lib/keybindings.ts` の KEYBINDINGS list に Cmd/Ctrl+Enter (Textarea 保存ショートカット) のエントリを追加。iter228 で comment thread に導入、iter313-318 で 8 Textarea に水平展開、iter356-357 で aria-keyshortcuts で programmatic 公開した Cmd/Ctrl+Enter shortcut が visible 一覧 (KeybindingsHelpModal、`?` で開く) には含まれず、ユーザが shortcut の存在を発見できなかった。fix: KEYBINDINGS list に `{ combo: 'Cmd+Enter / Ctrl+Enter', description: 'Textarea から保存 / 作成 / 投稿 (...)', group: 'グローバル' }` を追加 (1 件)。Modal は KEYBINDINGS を group ごとに自動 render するので追加だけで visible 一覧に出る。機能追加なし、影響面 1 ファイル。経路: keybindings list を確認 → Cmd+Enter エントリ漏れと特定 → source-side regex assert で codify。検証 codify: `scripts/explore-uiux-keybindings-cmd-enter-iter358.ts` で エントリ存在を assert。これで discoverability も向上、Cmd+Enter shortcut の三層対応 (実装 + aria-keyshortcuts + visible 一覧) 完結。次 iter 候補: 別軸 (focus management / mobile UX / progress feedback) に切替。
- ✅ [playwright-iter359 1/1] /login + /signup: form は iter256 で `aria-labelledby="login-heading"` / `signup-heading` の 1 段目を獲得していたが、CardDescription の welcome / instruction 文 ("最強TODO へようこそ" / "アカウントを作成して始めましょう") は隣接 div として独立で programmatic に form と紐付いていなかった。SR は form focus 時に description を announce しない。fix: page 側 CardDescription に `id="login-description"` / `id="signup-description"` を付与、対応する `login-form.tsx` / `signup-form.tsx` の form 要素に `aria-describedby="<id>"` を追加 (4 file × 1 line = 計 4 行追加)。aria-labelledby (heading) + aria-describedby (description) の標準的 form labelling 二段構成に統一。WCAG SC 1.3.1 (Info and Relationships) の programmatic 関連付けに準拠。機能追加なし、shadcn 編集なし、影響面 4 ファイル (page + form pair × 2)。経路: 経路 B (script、cloud env で MCP playwright が `/opt/google/chrome/chrome` 不在で起動不可、curl で rendered HTML を直接 inspect → form の aria-describedby 不在を確認) → source-side regex + curl rendered HTML で codify。検証 codify: `scripts/explore-uiux-auth-form-describedby-iter359.ts` で 4 file の id + aria-describedby + iter256 aria-labelledby invariant 維持を assert (4/4 OK + regression guard 2 件)。次 iter 候補: 他 form (workspace 設定 / item 編集 dialog / sprint create) で CardDescription / DialogDescription の aria-describedby 二段構成 sweep を継続。
- ✅ [playwright-iter360 1/1] /signup displayName: input は `autoComplete="name"` + `spellCheck={false}` を持つが `autoCorrect="off"` 不在。iOS Safari は spellCheck と autoCorrect を独立 control するため、spellCheck=false でも autoCorrect は default (on) で working。"iPhone_user_42" のような handle や英語固有名 ("Mc" 接頭等) を typing 中に Safari が誤 correction する可能性 (e.g. "Jonh"→"John" / "iPhone"→"iPhones")。fix: signup-form.tsx の displayName IMEInput に `autoCorrect="off"` を追加 (1 行)。email input が持つ pattern (autoCorrect="off" + spellCheck={false} + autoCapitalize="none") に揃え、autoCapitalize は freeform name なので default "sentences" のまま (英語名 "John Smith" の初頭大文字も許容)。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、cloud env で MCP playwright 不在、curl rendered HTML で displayName attr 比較 → autoCorrect 漏れ特定)。検証 codify: `scripts/explore-uiux-displayname-autocorrect-iter360.ts` で displayName block に autoCorrect="off" 存在 + 既存 invariant (spellCheck={false} / autoComplete="name" / email の autoCorrect="off") regression guard を assert。これで auth 全 input (3) で autoCorrect/spellCheck 軸の attribute 設定が完了。次 iter 候補: 他 freeform name 系 input (workspace name / sprint name / goal title) も同 pattern audit。
- ✅ [playwright-iter361 1/1] /~offline page (PWA SW network fallback): iter256 で main 化 + h1 + retry button / home link を獲得した <main> は `aria-labelledby="offline-heading"` のみで「ネットワーク接続が切れています…」の主要 status 文は隣接 <p> として独立、SR が landmark navigation で main 到達時に heading だけ announce、status 文は別途 navigate forward 必要。fix: <main> に `aria-describedby="offline-description"` を追加、1 段目の <p> に `id="offline-description"` を付与 (1 file 2 行追加)。iter359 で auth form に適用した aria-labelledby + aria-describedby 二段構成 pattern を offline landmark にも水平展開。WCAG SC 1.3.1 (Info and Relationships) の programmatic 関連付けに準拠。これで iter256 (main + h1) + iter256 (retry button + home link) + iter361 (description 紐付け) の 3 段階で offline page の SR a11y 完結。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、curl で /~offline rendered HTML に aria-describedby + id 共存を確認 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-offline-describedby-iter361.ts` で <main> aria-describedby + <p> id + iter256 invariant (aria-labelledby + h1 id) を 4 軸 assert (4/4 OK + iter256 regression guard 2 件)。次 iter 候補: 他 page (Dashboard / Today / Inbox 等) の <main> が aria-describedby を持っているか sweep audit、heading-only landmark を二段構成に揃える。
- ✅ [playwright-iter362 1/1] /~offline page: iter361 の `<main aria-describedby="offline-description">` は 1 段目 status のみ参照、2 段目 saikyo-todo 特有 detail ("最強TODO はオフラインでもアプリ自体は表示されますが、Item の作成 / 同期にはオンライン接続が必要です。") は隣接 <p> として独立、SR は「Item 作成不可」という app-specific 重要制約を別途 forward navigation 必要だった。fix: 2 段目 <p> に `id="offline-secondary"` を付与、<main> の aria-describedby を space 区切り 2 ID 列挙 (`"offline-description offline-secondary"`) に拡張 (1 file 2 行差替)。ARIA spec 標準 pattern (aria-describedby は IDREFS で複数参照可) を使い、SR は順序通り primary status → secondary explanation を 1 続きで announce。これで iter256 (main + h1 + retry / home) → iter361 (primary describedby) → iter362 (secondary describedby) で offline page SR a11y 三段強化を完結。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、iter361 の単一 ID 構成を IDREFS 拡張 → curl rendered HTML で `aria-describedby="offline-description offline-secondary"` 共存を確認)。検証 codify: `scripts/explore-uiux-offline-secondary-iter362.ts` で <main> aria-describedby 2 ID 列挙 + <p> id="offline-secondary" + iter361/iter256 regression guard を 4 軸 assert (4/4 OK)。次 iter 候補: workspace pages の <main> や card 系で primary + secondary description を持つ箇所を audit、IDREFS 列挙が必要な所を sweep。
- ✅ [playwright-iter363 1/1] /login + /signup CardFooter: question span ("アカウント未作成?" / "アカウントあり?") は visual framing 用に配置されていたが、隣接 <Link> の aria-label ("アカウントをお持ちでない方はこちらでサインアップ" / "既にアカウントをお持ちの方はこちらでログイン") が question 内容を完全に内包、SR ユーザは "アカウント未作成? + アカウントをお持ちでない方はこちらでサインアップ link" の冗長読み上げを受ける。fix: 両 page の question span に `aria-hidden="true"` を付与 (2 file × 1 attr 追加)。visual UI は変化なし、SR は span を skip し Link の包括的 aria-label のみを announce。Link の aria-label は question 内容を programmatic に保つため WCAG SC 1.3.1 (Info and Relationships) は問題なし、SC 2.5.3 (Label in Name) も visible "サインアップ" / "ログイン" が aria-label に内包される pattern を維持 (iter276 invariant)。機能追加なし、shadcn 編集なし、影響面 2 ファイル (login/page + signup/page)。経路: 経路 B (script、SR 視点で重複 announcement 候補を audit → curl rendered HTML で `<span aria-hidden="true" ...>アカウント...?</span>` 共存を確認)。検証 codify: `scripts/explore-uiux-auth-footer-aria-hidden-iter363.ts` で 両 page の question span に aria-hidden + iter276 由来の Link aria-label invariant を 4 軸 assert (4/4 OK)。次 iter 候補: 同 pattern (visual question label + descriptive Link aria-label) が他 page (workspace 設定 / sprint create / template 編集等) にも存在する場合、aria-hidden で重複除去 sweep。
- ✅ [playwright-iter364 1/1] /~offline page Home Link: iter256 で OfflineRetryButton には `aria-label="再読み込みして再試行 (ページ全体を読み直して接続を回復)"` が付いていたが、隣接 Home Link は visible text "ホームに戻る" のみで context なし。SR ユーザは 2 つの actionable 要素のうち retry button だけ context を獲得し、Home Link は意図 (オンライン復帰後の挙動 / 起点画面の意味) が伝わらない非対称構造。fix: Link に `aria-label="ホームに戻る (アプリの起点画面に遷移、オンライン復帰後は最新状態を表示)"` を追加 (1 file 数行)。visible text "ホームに戻る" は accessible name の先頭に内包、voice-control "click ホームに戻る" 継続動作 (WCAG SC 2.5.3 Label in Name 準拠)。両 actionable 要素 (retry button + home link) で context-rich aria-label の対称性を確立。これで iter256 (retry button) + iter363 (footer span aria-hidden) + iter364 (home link aria-label) で offline + auth page の SR a11y 対称性が確立。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、iter256 retry button pattern を Home Link に水平展開 → curl rendered HTML で `<a aria-label="ホームに戻る (...)">` 確認)。検証 codify: `scripts/explore-uiux-offline-home-aria-label-iter364.ts` で Home Link aria-label の visible-prefix + 括弧 pattern + visible text 維持 + iter256 OfflineRetryButton aria-label invariant を 4 軸 assert (4/4 OK)。次 iter 候補: 同 pattern (visible + 括弧 context aria-label) を他 page の Link / Button にも sweep 展開、context-rich aria-label の codebase 全体統一。
- ✅ [playwright-iter365 1/1] /~offline page Home Link: Next.js Link の default prefetch は viewport 進入時に `/` を auto fetch するが、offline page は文字通り network 喪失時の fallback で、初回 offline ヒット時の cold start では SW runtime cache に `/` が populate されておらず prefetch は確実に fail。user click 時は SW + browser cache 経由で navigation するので、auto prefetch を止めても操作上の navigation 速度に影響なし、console には wasted「fetch failed」warning が出るのみ。fix: Link に `prefetch={false}` を追加 (1 file 1 行)。明示的に prefetch off の intent を表明し、offline 状態での wasted fetch 試行を回避。click 時の navigation は SW + browser cache 経由で従来通り動作。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、Next.js Link prefetch default 動作を踏まえて offline 文脈に最適化 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-offline-link-prefetch-iter365.ts` で Home Link block に prefetch={false} + iter364 由来の aria-label invariant を 2 軸 assert (1/1 OK + iter364 regression guard)。次 iter 候補: 同 pattern (offline / error fallback page で prefetch=false) を他 fallback page (404 / global-error 等) で audit。
- ✅ [playwright-iter366 1/1] /login + /signup metadata: iter258 で title は固有化済み ("ログイン | 最強TODO" / "サインアップ | 最強TODO") だったが、description は layout.tsx 由来の generic "チーム共有 AI 駆動 TODO" を inherit、share 時 (Slack / Twitter / LinkedIn) の preview や SEO snippet で全 page が同じ汎用文言となり page 固有の文脈が伝わらなかった。fix: 両 page の metadata object に description property を追加 (2 file × 1 行)。Next.js App Router で page metadata は layout metadata を override するため page-specific description が優先される。login は "最強TODO のログイン画面。メールアドレスとパスワードでサインイン。"、signup は "最強TODO のサインアップ画面。新規アカウント作成 (表示名 + メールアドレス + 8文字以上のパスワード)。" を設定。share preview / SEO snippet で page 固有の文脈が即座に伝わり、ログイン vs サインアップの誤遷移を予防 + search engine ranking 関連性向上。これで iter258 (title) + iter366 (description) の 2 軸で auth page metadata 完備。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 B (script、layout 継承 description が generic なまま放置されていた発見 → curl で <meta name="description"> 比較 → metadata property 追加 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-auth-meta-description-iter366.ts` で 両 page の description metadata 存在 + iter258 title invariant 維持を 4 軸 assert (4/4 OK)。次 iter 候補: 同 pattern を workspace pages / ~offline page にも展開、全 page で固有 description 完備の sweep。
- ✅ [playwright-iter367 1/1] /~offline page metadata: iter258 で title は "オフライン | 最強TODO" に固有化済みだが、description は layout.tsx 由来の generic "チーム共有 AI 駆動 TODO" を inherit、share 時の preview や SEO snippet で offline 文脈 (PWA SW fallback / 復帰方法) が伝わらなかった。fix: metadata object に description property を追加 (1 file 数行)。"最強TODO のオフライン fallback 画面 (PWA SW)。ネットワーク復帰後に再読み込みしてください。" を設定し、share preview / SEO snippet で page 固有の文脈を伝える。iter366 の login/signup pattern を offline page に水平展開、auth pages + offline page で page-specific description 完備を達成。これで iter258 (title) + iter366 (login/signup desc) + iter367 (offline desc) で fallback page metadata 完備。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、iter366 の続編で同じ「layout 継承 description が generic」問題を offline にも適用 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-offline-meta-description-iter367.ts` で description metadata 存在 + iter258 title invariant + iter256/362 の aria-labelledby / aria-describedby invariant 維持を 4 軸 assert (1/1 OK + regression guard 3 件)。次 iter 候補: workspace pages にも同 pattern 展開して全 page 固有 description 完備の sweep。
- ✅ [playwright-iter368 1/1] /login + /signup + /~offline metadata: CLAUDE.md 記載の「社内チーム利用・社外非公開 (Docker Compose 自前ホスト)」プロジェクトにもかかわらず、search engine 制御の robots directive 不在。社外ユーザーが Google / Bing で /login や /signup を発見できてしまい (1) 誤アクセス試行で account 必要に終わる、(2) signup URL 漏洩で bot signup attempt リスク、(3) SEO 観点で認証画面が main page より上位に出る confusing 状態。fix: 3 page の metadata object に `robots: { index: false, follow: false }` を追加 (3 file × 1 行)。Next.js App Router metadata helper が `<meta name="robots" content="noindex, nofollow">` に変換、search engine crawler に「この page を index しない + link も辿らない」二重ガードを programmatic 表明。これで iter258 (title) + iter366/367 (description) + iter368 (robots) で fallback page metadata の search-engine-presentation 三層完備。機能追加なし、shadcn 編集なし、影響面 3 ファイル。経路: 経路 B (script、CLAUDE.md の「社外非公開」と現状の robots directive 不在の gap を audit → metadata 追加 → curl で <meta name="robots"> 確認)。検証 codify: `scripts/explore-uiux-auth-offline-robots-iter368.ts` で 3 file の robots + iter258/iter366,367 invariant 維持を 9 軸 assert (3/3 OK + regression guard 6 件)。次 iter 候補: workspace pages にも同 robots noindex を展開、または layout.tsx に global robots を default 設定し必要 page だけ override する pattern も検討。
- ✅ [playwright-iter369 1/1] 全 page (layout 起点 workspace / auth / offline カバー): iter368 で login/signup/offline 3 page に per-page robots を追加したが、workspace pages (Dashboard / Today / Inbox / Sprints / Goals / PDCA / Templates / Workflows / Integrations / Archive 等多数) は robots 不在で anonymous crawler が 307 redirect 経由で URL を index 試行する余地が残った (redirect 元 URL の indexing policy は redirect 元自身の robots 依存)。fix: layout.tsx root metadata に `robots: { index: false, follow: false }` を追加 (1 file 数行 + 「社内チーム利用・社外非公開」intent コメント明記)。Next.js App Router で layout metadata は全 child page が inherit、workspace pages 含む全 page が `<meta name="robots" content="noindex, nofollow">` を出力。公開 page を将来追加する場合は当該 page の metadata で `robots: { index: true }` を override する DRY pattern。defense-in-depth (layout default + iter368 per-page) で policy 一括反映、iter368 per-page robots は同値の harmless duplicate として残置。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、iter368 続編で per-page → layout 全体に拡大 → curl で /login rendered HTML に robots meta 出力を確認)。検証 codify: `scripts/explore-uiux-layout-robots-iter369.ts` で layout robots + 既存 metadata (title / description / manifest / appleWebApp) invariant + iter368 per-page robots 維持を 9 軸 assert (1/1 OK + regression guard 8 件)。次 iter 候補: per-page robots は将来 cleanup 候補 (layout default が成立した時点で per-page 削除で DRY 化)、または別軸 (focus management / dialog focus restore) に切替。
- ✅ [playwright-iter370 1/1] /login + /signup + /~offline metadata cleanup: iter368 (per-page robots) + iter369 (layout robots default) で 3 page に同値の robots metadata が duplicate、source of truth が 2 箇所に分散。Next.js App Router の inheritance 活用すると per-page は不要で layout default で一括反映される。fix: 3 page の metadata から `robots: { index: false, follow: false }` 行を削除 (3 file × 1 行削除)。layout.tsx の global robots が継承されるため rendered HTML 出力は不変 (`<meta name="robots" content="noindex, nofollow">`)、source of truth を layout 一箇所に集約 (DRY)。動作不変で内部品質向上、将来 reader が「誰が robots を制御するか」を layout 一箇所で確認可能、iter368/iter369 で確立した「layout default + override pattern」に統一、公開 page を将来追加する場合は `robots: { index: true }` を override する pattern が明確化。機能追加なし、shadcn 編集なし、影響面 3 ファイル。経路: 経路 B (script、iter369 の hint「per-page cleanup 候補」に従い実施 → curl で 3 page の rendered HTML に robots meta が依然出力されることを inheritance 経由で確認)。検証 codify: `scripts/explore-uiux-cleanup-per-page-robots-iter370.ts` で 3 page metadata block の robots 不在 + iter258 / iter366,367 / iter369 invariant 維持を 10 軸 assert (4/4 OK + regression guard 7 件)。次 iter 候補: 別軸 (focus management / aria-live region / dialog UX) に切替、または description も layout default 化する DRY 化 sweep。
- ✅ [playwright-iter371 1/1] 全 page (layout 起点): 最強TODO は Next.js + React で全 page が JS 必須 (Server Action / TanStack Query / Zustand / SW 全て JS 依存) だが、JS 無効ブラウザ (NoScript extension / privacy 設定) でアクセスすると white screen + silent failure になり、user は原因不明のまま離脱。<noscript> element 不在で informed diagnostic message も表示されない。fix: layout.tsx の <body> 直下 (ThemeProvider 兄弟) に <noscript> element を追加 (1 file 数行)。<div> 内に destructive 色 + 中央寄せ + padding で目立たせ、「最強TODO は JavaScript を必要とします。ブラウザで JavaScript を有効にしてください。」を表示。JS 有効環境では browser が <noscript> を hide するので 99% user に影響なし、JS 無効 user にだけ informed fallback を提供。intent (JS 必須) を HTML semantic に embed し将来 reader にも伝わる、iter369 layout robots / iter371 noscript で「全 page 共通の defensive policy」を layout 一箇所に集約。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、curl rendered HTML に <noscript> 不在を確認 → layout.tsx 編集 → 再 curl で `<noscript><div class="bg-destructive ...">...</div></noscript>` を確認)。検証 codify: `scripts/explore-uiux-noscript-fallback-iter371.ts` で <noscript> JS 必須 message 存在 + <body> 直下 (ThemeProvider 兄弟) 配置 + iter369 robots invariant を 3 軸 assert (1/1 OK + regression guard 1 件 + 配置 audit)。次 iter 候補: 別軸 (focus management / aria-live region / dialog UX) に切替。
- ✅ [playwright-iter372 1/1] 全 page (layout viewport — UA-rendered form control / scrollbar): 旧 viewport export は themeColor / width / initialScale / viewportFit のみで colorScheme 未設定。<meta name="color-scheme"> 不在で browser は OS default (light 想定) で UA-rendered control を render、user が dark theme を選択していても native scrollbar や date input の picker / `<input type=number>` spinner が light で出る不整合 (例: Safari の date input calendar popup が white background のまま、surrounding が dark で見にくい)。fix: layout.tsx の viewport object に `colorScheme: 'light dark'` を追加 (1 file 1 行)。Next.js viewport API が `<meta name="color-scheme" content="light dark">` に変換、browser は両 theme 対応として UA-rendered control の見た目を切り替える (CSS color-scheme property と等価、document-level で適用)。ThemeProvider の class-based theming に加えて UA-level theme hint も提供する二段構成、iter369 (robots) + iter371 (noscript) + iter372 (color-scheme) で layout-level defensive policy 三層完備。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、curl で <meta name="color-scheme"> 不在を確認 → viewport export 拡張 → 再 curl で出力確認)。検証 codify: `scripts/explore-uiux-color-scheme-iter372.ts` で viewport.colorScheme 存在 + 既存 viewport invariant + iter369/371 invariant 維持を 7 軸 assert (1/1 OK + regression guard 6 件)。次 iter 候補: 別軸 (focus management / aria-live region / dialog UX) に切替。
- ✅ [playwright-iter373 1/1] /login password input: src/features/auth/schema.ts の LoginInputSchema は `password: z.string().min(1, 'パスワードを入力してください')` で legacy 短い password の login を許容 (意図的に min=1)。一方 login-form.tsx の password IMEInput は `minLength={8}` を持っており (signup form の copy-paste artifact と推測)、`<form noValidate>` で HTML validation は suppressed なので 7 文字 password でも実際は提出可能だが、SR は `<input minLength=8>` を「min length 8」と読み上げ misleading な hint を user に与えていた (実際は 1 文字でも OK)。fix: login-form.tsx の password IMEInput から `minLength={8}` 行を削除 (1 file 1 行削除)。schema 側の `.min(1)` が単一 source of truth となり、SR hint は input の base attribute (required + aria-required) のみで「必須だが長さ制限は schema 側」を正しく伝える。signup の `.min(8)` (新規アカウントは強い password 必須) と login の `.min(1)` (既存 user は legacy で OK) の意図的差分が input にも正しく反映される。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、schema の min(1) と input の minLength={8} の不整合を audit → input から minLength 削除 → curl で <input id="password"> から minLength 属性が消えたことを確認)。検証 codify: `scripts/explore-uiux-login-password-minlength-iter373.ts` で password input の minLength removal + 既存 input attribute invariant + schema 側 LoginInputSchema.password.min(1) invariant 維持を 6 軸 assert (1/1 OK + regression guard 5 件)。次 iter 候補: signup の displayName.max(50) に Japanese custom error message 追加 (zod default は英語) / 別軸 (focus management) に切替。
- ✅ [playwright-iter374 1/1] /signup displayName 51 文字以上時 error: SignupInputSchema は他 field (email / password / displayName.min(1)) には Japanese error message が設定されているが、`displayName.max(50)` のみ message 引数なしで zod default の英語 error ("String must contain at most 50 character(s)") が表示。51+ 文字 displayName 入力 user に英語 error が出る i18n 不整合。fix: schema.ts の displayName chain を multi-line に展開、`.max(50, '表示名は 50 文字以内で入力してください')` に変更 (1 file 数行差替)。他 field と error message style 統一、zod v4 error は signup-form.tsx の `<p role="alert">` で SR 即時 announce される。code base 内 error message の全 Japanese 化に貢献。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、schema audit で max(50) のみ message 引数なしを発見 → custom message 追加 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-displayname-max-message-iter374.ts` で displayName.max Japanese message 存在 + email / signup password / displayName.min(1) / login password invariant 維持を 5 軸 assert (1/1 OK + regression guard 4 件)。次 iter 候補: codebase 全体 zod schema audit、英語 default error が残っている箇所を sweep して Japanese 化、または別軸 (focus management) に切替。
- ✅ [playwright-iter375 1/1] /login + /signup form (zod validation 失敗時 focus 挙動): 旧 form は `onSubmit={form.handleSubmit(onSubmit)}` のみで onInvalid 引数なし。zod validation 失敗時 (例: email 空 submit)、`<p role="alert">` で error 表示されるが focus は submit button 等に残り、user は手動で errored field へ Tab navigation する必要。SR ユーザは alert announce で error を聞くが focus は移動せず、修正の cognitive load が高い。fix: `function onInvalid(errors)` を追加 (5 行)、`Object.keys(errors)[0]` で第 1 errored field を取得し `form.setFocus(field)` で focus 移動。`handleSubmit(onSubmit, onInvalid)` で wire up (1 行差替)。2 file × 6 行 = 計 12 行追加。zod schema field order が Object.keys insertion order を保つため UI visual order と focus 移動先が一致する直感的 UX、WCAG SC 3.3.1 (Error Identification) を SR alert announce + focus shift の二段で強化、iter255 (server error setFocus) pattern を validation error にも展開し focus management の一貫性確立。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 B (script、react-hook-form の handleSubmit signature 確認 → 第 2 引数 onInvalid を活用 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-onInvalid-focus-iter375.ts` で 両 form の onInvalid handler + handleSubmit wire up + iter255 server error setFocus invariant を 6 軸 assert (2/2 OK + regression guard 4 件)。次 iter 候補: 同 pattern を他 form (workspace 設定 / sprint create / item edit dialog 等) にも展開、validation 失敗時の focus 移動を全 form で統一。
- ✅ [playwright-iter376 1/1] /login + /signup toast.error i18n: src/features/auth/actions.ts の loginAction で `if (error) return err(new AuthError(error.message))` と Supabase error.message を **そのまま** AuthError に詰める実装。Supabase auth は英語 error ("Invalid login credentials" / "User already registered" 等) を返し、login-form.tsx の `toast.error(result.error.message)` で user に英語表示。日本語 user に英語 error が出る i18n 不整合 (signupAction も同様)。fix: actions.ts 冒頭に `function mapAuthError(msg: string): string` helper を追加 (15 行)、common Supabase error → Japanese mapping を regex match で実装、マッチしない場合は原文 fallback。loginAction / signupAction の `error.message` 引数を `mapAuthError(error.message)` に置換 (2 行差替)。toast 表示が "Invalid login credentials" → "メールアドレスまたはパスワードが正しくありません"、"User already registered" → "このメールアドレスは既に登録されています" 等の Japanese に変換。iter374 (zod schema i18n) + iter376 (Supabase error i18n) で auth flow の error message が全 Japanese 化、regex fallback で未知 error にも graceful degradation。機能追加なし、動作変更なし、影響面 1 ファイル。経路: 経路 B (script、actions.ts audit で error.message が直接 toast に表示される経路を発見 → mapAuthError helper 追加 → source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-auth-error-i18n-iter376.ts` で mapAuthError helper の存在 + 5 主要 mapping + signupAction/loginAction での経由を 8 軸 assert (1/1 OK)。実際の Supabase error 受信は cloud env で Supabase 不在で integration test 無理、unit-level helper 関数 audit で代替。次 iter 候補: 他 server action (workspace 系 / item 系) でも error.message 経路の i18n を audit。
- ✅ [playwright-iter416 1/1 mode-D] goals-panel 2 form + templates-panel 1 form に aria-label 追加し landmark 化 — codebase 内 form 11 個 carriage 100% 達成: iter415 (sprints-panel 3 form) の続編。fix: 2 file × 計 3 line addition: Goal 作成フォーム / Key Result 追加フォーム / Template 作成フォーム。これで auth / mock-timesheet / time-entry / sprints-panel / goals-panel / templates-panel で **codebase 内 form 11 個が landmark 化統一達成 (carriage 100% meta-assert で codify 検証)**。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 B (iter415 hint「goals-panel / templates-panel pattern 展開」適用)。検証 codify: `scripts/explore-uiux-goals-templates-form-landmark-iter416.ts` で 3 aria-label + iter415 invariant + **11 form landmark carriage 100% meta-assert** (4/4 OK + regression 3 件)。typecheck / lint 緑。次 iter 候補: 別軸 (focus restore on dialog close、aria-live region for toast、ItemEditDialog の field 列 enterKeyHint)。
- ✅ [playwright-iter415 1/1 mode-D] sprints-panel 3 form (作成/編集/default) に aria-label 追加し landmark 化 — iter256/iter405/iter411 horizontal expansion: src/components/workspace/sprints-panel.tsx の 3 form は aria-busy のみで aria-label / aria-labelledby いずれも無し。HTML5 spec で form は accessible name 無いと暗黙 landmark にならず SR landmark navigation で「どの操作 form か」区別不能。fix: 1 file × 3 line addition: Sprint 作成フォーム / Sprint 編集フォーム / Sprint デフォルト設定 編集フォーム。これで auth / mock-timesheet / time-entry / sprints-panel = **codebase 内 form 8 個が landmark 化統一達成**。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、iter411 hint「workspace 配下 form audit」反復)。sprints-panel は cloud env で auth 必須、static audit で代替。検証 codify: `scripts/explore-uiux-sprints-panel-form-landmark-iter415.ts` で 3 aria-label + aria-busy 維持 + iter256/iter405/iter411 invariant (4/4 OK + regression 3 件)。typecheck / lint 緑。次 iter 候補: goals-panel (2 form) / templates-panel (1 form) で同 pattern 展開。
- ✅ [playwright-iter414 1/1 mode-M] CreateTimeEntryForm 記録 button を 44x44 tap target に — 7 form 全 button h-11 carriage 100% 達成: src/components/time-entry/create-time-entry-form.tsx の <Button type="submit"> は className 不指定で shadcn default h-8=32px を継承し mobile WCAG 2.5.5 違反。fix: 1 file × 1 line addition (className="h-11 px-4")。既存 iter255 pending aria-label / iter411 form aria-label 維持。これで iter408+409+410+413+414 で **7 form (login / signup / mock-login / mock-submit / offline retry / create-workspace / time-entry) の submit button が 44x44 tap target 統一達成、codebase 内 全 form の mobile UX completeness 完備**。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (iter413 hint「CreateTimeEntryForm 記録 button sweep で 7 form 全達成」適用)。検証 codify: `scripts/explore-uiux-mobile-time-entry-tap-iter414.ts` で h-11 px-4 + iter411/iter255 invariant + **7 form button h-11 carriage 100% meta-assert** (2/2 OK + regression 2 件)。typecheck / lint 緑。次 iter 候補: 別軸 (focus restore on dialog close、aria-live region for toast、ItemEditDialog の field 列 enterKeyHint)。
- ✅ [playwright-iter413 1/1 mode-M] CreateWorkspaceForm 作成 button を 44x44 tap target に — iter408-410 horizontal expansion で 6 form 統一達成: src/components/workspace/create-workspace-form.tsx の <Button type="submit" className="w-full"> は h-8=32px 違反。workspace 初回 onboarding は app の最 critical path、submit ミスタップは致命的。fix: 1 file × 1 line 差替 (className="w-full" → "h-11 w-full")。既存 iter377 onInvalid / iter402 enterKeyHint 維持。これで iter408+409+410+413 で **6 form (login / signup / mock-login / mock-submit / offline retry / create-workspace) の submit button が 44x44 tap target 統一達成**、unauth-onboarding フローの mobile UX 完備。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (iter410 hint「CreateWorkspaceForm 作成 button sweep」適用)。workspace onboarding は cloud env で auth 必須、static audit で代替。検証 codify: `scripts/explore-uiux-mobile-create-workspace-tap-iter413.ts` で h-11 w-full + iter377/iter402/iter408-410 invariant (1/1 OK + regression 7 件)。typecheck / lint 緑。次 iter 候補: CreateTimeEntryForm '記録' button (h-8) sweep で 7 form 全達成。
- ✅ [playwright-iter412 1/1 mode-M] /login + /signup CardFooter Link を 44x44 tap target に — iter287 (py-2=36px) を min-h-11+px-2 で 44px に格上げ: iter287 で CardFooter Link に py-2 を追加し 36px に上げ済だが WCAG 2.5.5 AAA 44pt 未達。Mobile audit (320x568) で signup link=86x36 / login link=57x36 で特に signup→login link が width 57 で 44 未達。fix: 2 file × 各 1 line 差替 (className: `inline-flex items-center py-2` → `inline-flex min-h-11 items-center px-2 py-2`)。signup-link 86x36 → 102x44 (MCP verified pass44=true)。py-2 維持で iter287 invariant 保つ、aria-label / underline 維持。これで iter408 (/~offline) + iter409 (auth submit) + iter410 (mock-timesheet) + iter412 (auth footer link) で **codebase 全 unauth-reachable interactive element が 44x44 tap target 統一達成**。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 A (MCP 320x568 + iter410 hint「CardFooter Link sweep」適用) → 経路 B (source-side regex + screenshot)。検証 codify: `scripts/explore-uiux-mobile-auth-footer-link-tap-iter412.ts` で 2 page × 各 2 軸 + iter287 (py-2) + iter259 (aria-label) + iter286 (underline) invariant (4/4 OK + regression 5 件)。typecheck / lint 緑。次 iter 候補: CreateWorkspaceForm 作成 button (h-8) sweep、CreateTimeEntryForm 記録 button sweep。
- ✅ [playwright-iter411 1/1 mode-D] create-time-entry-form に aria-label="稼働記録 作成フォーム" を追加し form landmark 化 — iter256/iter405 horizontal expansion: src/components/time-entry/create-time-entry-form.tsx は <form onSubmit={...} aria-busy={isPending || undefined}> で aria-label / aria-labelledby いずれも無し。HTML5 spec で form は accessible name を持たないと暗黙 landmark にならず SR landmark navigation で form に直行不可。iter256 (auth form aria-labelledby) + iter405 (mock-timesheet form aria-label) で同 pattern 適用済だが time-entry form は未適用で UX 断絶。fix: 1 file × 1 line addition (aria-label="稼働記録 作成フォーム")。aria-labelledby ではなく aria-label を使う理由: time-entry form は workspace 内 panel として複数 page で再利用される可能性があり、特定 h1 への依存を避けて self-contained に役割表現する方が安全。これで iter256 (auth) / iter405 (mock-timesheet) / iter411 (time-entry) で **codebase 内 form 全 5 個が landmark 化統一達成**。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、codebase 内 form audit で time-entry form の aria-label 不在を発見)。time-entry page は cloud env で auth 必須、static audit で代替。検証 codify: `scripts/explore-uiux-time-entry-form-landmark-iter411.ts` で aria-label + aria-busy + iter256/iter405 invariant (2/2 OK + regression 4 件)。typecheck / lint 緑。次 iter 候補: CardFooter Link tap target sweep、または CreateWorkspaceForm 作成 button sweep、または focus restore on dialog close。
- ✅ [playwright-iter410 1/1 mode-M] mock-timesheet 2 form submit button を 44x44 tap target に — iter408+409 horizontal expansion で 5 form 統一達成: mock-login-form.tsx + mock-submit-form.tsx の <Button type="submit" className="w-full"> は shadcn default h-8=32px を継承し mobile WCAG 2.5.5 違反。iter408 (/~offline) + iter409 (auth) の sweep 続編、mock-timesheet は iter378-381 で a11y polish 済の系統で tap target も揃えて mobile UX 一貫性確保。Mobile audit measure (320x568) で mock-login=272x32 / mock-submit=256x32。fix: 2 file × 各 1 line 差替 (className="w-full" → "h-11 w-full")。両 button 272x44 (MCP verified pass44h=true)。既存 iter378/iter405 aria-label / aria-busy / id 維持。これで iter408+409+410 = **5 form (login / signup / mock-login / mock-submit / offline retry) の submit button が 44x44 tap target 統一達成**、mobile UX consistency 完備。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 A (MCP 320x568 + iter409 hint「mock-timesheet sweep」適用) → 経路 B (source-side regex)。検証 codify: `scripts/explore-uiux-mobile-mock-submit-tap-target-iter410.ts` で 2 軸 + iter378/379/405/408/409 invariant (2/2 OK + regression 6 件)。typecheck / lint 緑。次 iter 候補: CardFooter Link (iter287 py-2=36px) sweep、または CreateWorkspaceForm の '作成' button (h-8) sweep。
- ✅ [playwright-iter409 1/1 mode-M] /login + /signup form submit button を 44x44 tap target に — mobile auth UX gap: src/components/auth/login-form.tsx + signup-form.tsx の <Button type="submit" className="w-full"> は shadcn default h-8=32px を継承し mobile で WCAG 2.5.5 違反。auth は app の最 critical path、submit ミスタップは致命的。Mobile audit (320x568) で login=256x32 / signup=256x32 を観測。fix: 2 file × 各 1 line 差替 (className="w-full" → className="h-11 w-full")。両 button 256x44 (MCP verified pass44h=true)。iter255 pending aria-label 維持、shadcn cn() で h-8→h-11 dedupe。iter408 (/~offline) に続く mobile tap target sweep 2 弾目。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 A (MCP 320x568 + iter408 hint「auth form sweep」適用) → 経路 B (source-side regex + screenshot .playwright-mcp/uiux-mobile-{login,signup}-iter409-after.png)。検証 codify: `scripts/explore-uiux-mobile-auth-submit-tap-target-iter409.ts` で 2 軸 + iter255/iter408/iter401 invariant 維持 (2/2 OK + regression 4 件)。typecheck / lint 緑。次 iter 候補: CardFooter Link (iter287 py-2=36px、まだ 44 未達) sweep、または mock-timesheet/login submit button sweep。
- ✅ [playwright-iter408 1/1 mode-M] /~offline 復帰アクション 2 button を 44x44 tap target に — mobile recovery UX gap: src/app/~offline/retry-button.tsx + ~offline/page.tsx の Link は shadcn Button default size (h-8 = 32px) を継承し mobile で tap target 32px。WCAG 2.5.5 AAA + Apple HIG 44pt 違反、recovery 画面の primary action なので UX 致命的。Mobile audit (320x568 iPhone SE) で 再読み込みして再試行=163x32 / ホームに戻る=107x32 を観測。ユーザ要望「スマホ表示が全体的にいけてない」(2026-04-30) の消化として /~offline は recovery 性が高く優先度高。fix: 2 file × 各 1 line addition (className に "h-11 px-4")。retry-button.tsx: <Button className="h-11 px-4">、~offline/page.tsx: <Link className={cn(buttonVariants(...), 'h-11 px-4')}> (cn() で tailwind-merge を使い h-8 と h-11 / px-2.5 と px-4 の重複を解決、border-transparent と border-border の dedup も解決、template literal 連結だと border が透明化する罠あり)、+ cn import 追加。結果: 両 button 44px tall (175x44 + 119x44, MCP browser_evaluate で verified、pass44x44=true)。視覚的には outline border 維持、primary button の dark bg 維持、320px で 2 button 縦 stack する自然 mobile layout。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 A (MCP browser_resize 320x568 + browser_evaluate で 32px tap target 発見、screenshot 視覚確認) → 経路 B (source-side regex + screenshot 比較 .playwright-mcp/uiux-mobile-offline-iter408-{before,after-cn}.png)。検証 codify: `scripts/explore-uiux-mobile-offline-tap-target-iter408.ts` で 3 軸 + iter256/260/263/404 invariant 4 軸 (3/3 OK + regression 4 件)。typecheck / lint 緑。次 iter 候補: /login + /signup form submit button (h-8 = 32px) の mobile tap target sweep、または CardFooter Link 再考 (iter287 で py-2=36px)。
- ✅ [playwright-iter407 1/1] workspace 配下に layout.tsx (title.template) + 8 page metadata title 追加 — browser tab strip 9 page 識別 a11y/UX gap: workspace 配下 9 page は metadata 未定義で root layout の `title: '最強TODO'` をそのまま継承し tab strip / SR で 9 page 区別不能。iter258 (auth/offline) + iter403 (mock-timesheet) には適用済だが workspace 未対応。fix: 1 new file (layout.tsx に title.template='%s | 最強TODO' + default='Workspace | 最強TODO' 設定で DRY 化) + 8 page metadata block (sprints='Sprints' / goals='Goals' / pdca='PDCA' / workflows='Workflows' / time-entries='稼働記録' / templates='Templates' / integrations='Integrations' / archive='Archive' で各 → "<page> | 最強TODO" に展開)。workspace home `[workspaceId]/page.tsx` は workspace 動的 displayName を h1 で出すため per-page title を付けず layout default ("Workspace | 最強TODO") fallback (将来 generateMetadata 動的 title foundation)。機能追加なし、shadcn 編集なし、影響面 9 ファイル (1 layout 新規 + 8 page metadata)。経路: 経路 B (iter403 hint「workspace 配下 8 page metadata 一括展開」を直接適用 + Next.js title.template で DRY 化 → source-side regex assert で codify)。workspace page は cloud env で auth 必須、static audit で代替。検証 codify: `scripts/explore-uiux-workspace-titles-iter407.ts` で layout template/default 2 軸 + 8 page title 各 1 軸 + Metadata import + iter258 (auth/offline) + iter403 (mock-timesheet) invariant 維持 (10/10 OK + regression guard 6 件)。typecheck / lint 緑。次 iter 候補: workspace home の title を generateMetadata で動的 displayName 反映 ("<workspace name> | 最強TODO")、または sprint / goal / template / item-edit-dialog の動的 title。
- ✅ [playwright-iter406 1/1] workspace 配下 9 page の <main> に tabIndex={-1} 追加 — iter404 horizontal expansion で codebase 全 14 main carriage 100% 達成: iter404 で 5 unauth page (auth layout / ~offline / mock-timesheet 3 page) の <main> に tabIndex={-1} を追加したが、workspace 配下 9 page は未対応で skip-link focus 移動が機能しない (同 dead-loop)。iter404 hint「残 9 file (workspace 配下 8 page + [workspaceId]/page.tsx) の <main> にも tabIndex={-1} 展開」に従い本 iter で完成。fix: 9 file × 4 line 差替 (各 <main> に tabIndex={-1} + focus-visible:outline-none 追加、JSX が 1 行から複数行に展開)。全 file 同 className を共有しており同 pattern 一括適用。これで iter404 (5 unauth page) + iter406 (9 workspace page) = **codebase 全 14 <main> element に tabIndex={-1} carriage 100% 達成**、skip-link → main focus 移動が全 page で機能する。機能追加なし、shadcn 編集なし、影響面 9 ファイル (iter404 と同 horizontal expansion 形式)。視覚的 layout 不変。経路: 経路 B (script、iter404 hint を直接適用 + grep で 9 page の <main> className が同一 pattern を共有していることを確認 → 同 pattern 一括適用 → source-side regex assert で codify)。workspace page は cloud env で auth 必須のため browser interaction 不可、static audit で代替。検証 codify: `scripts/explore-uiux-workspace-tabindex-iter406.ts` で 9 file の <main> tabIndex={-1} + focus-visible:outline-none 各 2 軸 × 9 file = 18 軸 assert + iter404 5 page invariant 維持 + 全 14 main carriage 100% 達成 assert (18/18 OK + regression guard 5 件 + 100% carriage meta-assert)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (focus restore on dialog close、aria-live region for toast、ItemEditDialog の field 列 enterKeyHint 等)。
- ✅ [playwright-iter405 1/1] mock-timesheet 2 form (login / submit) に aria-label + aria-busy 追加 — form landmark 化 + SR busy 通知 a11y/UX gap: src/components/mock-timesheet/mock-login-form.tsx + mock-submit-form.tsx は <form> に aria-label / aria-labelledby / aria-busy 一切無し。(a) form は landmark 化していない (HTML5 spec: 名前付き form が暗黙 landmark) → SR landmark navigation 不可、(b) submit pending 中の busy state が button focus 時のみ伝わり focus 外れで消える。iter256 (auth form aria-labelledby) + iter262 (auth form aria-busy) で /login + /signup には適用済だが mock-timesheet 2 form は未適用で UX 断絶 (iter378-381 で mock-timesheet a11y polish したが form-level landmark / busy は別軸で残っていた)。fix: 2 file × 各 2 line addition (aria-label + aria-busy) = 計 4 line。mock-login-form: aria-label="Mock Timesheet ログインフォーム" + aria-busy={isPending || undefined}、mock-submit-form: aria-label="Mock Timesheet 工数送信フォーム" + aria-busy={isPending || undefined}。aria-labelledby ではなく aria-label を使う理由: page の h1 は "Mock Timesheet" generic で 2 form を区別不能、aria-label でフォーム役割明示の方が landmark navigation 上意味的に正確。機能追加なし、shadcn 編集なし、影響面 2 ファイル。経路: 経路 A (MCP browser_evaluate で form attributes inspect → aria-label / aria-busy 不在 + isFormLandmark=false を観測 → 修正後 isFormLandmark=true に変化を確認) → 経路 B (source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-mock-form-landmark-iter405.ts` で 2 form の aria-label / aria-busy 4 軸 + iter378/iter379 onInvalid + iter256 auth aria-labelledby + iter262 auth aria-busy invariant 維持の regression guard 6 軸 (4/4 OK + regression guard 6 件)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: workspace 配下 form (item-edit-dialog / sprint create / goal create / template create) の landmark sweep、または別軸 (focus restore on dialog close、aria-live region for toast)。
- ✅ [playwright-iter404 1/1] 5 unauth page の `<main>` に tabIndex={-1} 追加 — skip-link focus 移動 a11y/UX gap: iter389 で root layout に skip-link を追加し iter390-391 で各 page の <main> に id="main-content" を付与済だが、<main> 要素は default で focusable でないため skip-link 起動時に focus は body へ fall back し続く Tab 押下が skip-link 自身に戻る dead-loop。fix: 5 file × 3-4 line 差替 (各 <main> に tabIndex={-1} + focus-visible:outline-none 追加) — (auth)/layout.tsx (login/signup 共通) / ~offline/page.tsx / mock-timesheet/{login,new,entries}/page.tsx。tabIndex={-1} で programmatic focus 可能に、modern browser は fragment target を auto-focus、続く Tab は main 内の最初の focusable (form input 等) に進む = skip-link が完全に機能。focus-visible:outline-none で logical container の focus ring を抑止。機能追加なし、shadcn 編集なし、影響面 5 ファイル (iter391 / iter403 と同 horizontal expansion 形式)。視覚的 layout 不変。経路: 経路 A (MCP browser_evaluate で skip-link 起動後の focus 状態 inspect → mainTabIndex=null / activeIsMain=false の dead-loop を観測 → tabIndex={-1} 追加で activeIsMain=true / focusable に変化を確認) → 経路 B (source-side regex assert + curl で rendered HTML に tabindex="-1" 出力を確認)。検証 codify: `scripts/explore-uiux-skip-link-tabindex-iter404.ts` で 5 file の <main> tabIndex={-1} + focus-visible:outline-none 各 2 軸 × 5 file = 10 軸 assert + iter389 skip-link 存在 + iter390-391 id="main-content" invariant の regression guard 6 軸 (10/10 OK + regression guard 6 件)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 残 9 file (workspace 配下 8 page + [workspaceId]/page.tsx) の <main> にも tabIndex={-1} 展開、または別軸 (focus restore on dialog close、aria-live region)。
- ✅ [playwright-iter403 1/1] mock-timesheet 3 page (login / new / entries) に Metadata title/description 追加 — browser tab strip / SR window context UX polish: src/app/mock-timesheet/{login,new,entries}/page.tsx は metadata 未定義で root layout の `title: '最強TODO'` をそのまま継承。ブラウザ tab strip では 3 page が全部 "最強TODO" と表示され見分け不能、SR は window 切替時に同じ announce で context lost。iter258 で /login / /signup / /~offline には per-page title metadata を追加済だが、mock-timesheet は未対応で UX 断絶 (iter378-381 で mock-timesheet 系 a11y polish したが title metadata は別軸で残っていた)。fix: 3 file × ~5 line addition (各 page に Metadata import + metadata export block 追加)。3 page の title: /login → "Mock Timesheet ログイン | 最強TODO" / /new → "Mock Timesheet 新規送信 | 最強TODO" / /entries → "Mock Timesheet 送信済み | 最強TODO"。Next.js metadata API なので head 注入は framework 任せ、機能追加なし、shadcn 編集なし、影響面 3 ファイル (iter391 と同 horizontal expansion 形式)。経路: 経路 A (MCP browser_navigate /mock-timesheet/login で page title が "最強TODO" のままを発見、grep で 3 page 全部 metadata 未定義を確認) → 経路 B (source-side regex assert で codify + curl で rendered <title> 確認)。検証 codify: `scripts/explore-uiux-mock-titles-iter403.ts` で 3 page の Metadata import + metadata export + title 値 + description 存在の 4 軸 × 3 page = 12 軸 assert + iter258 (auth/offline title) + iter391 (mock-timesheet main id) invariant 維持の regression guard 6 軸 (12/12 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: workspace 配下 8 page (sprints / goals / pdca / templates / workflows / integrations / time-entries / archive) の per-page title metadata 一括展開、または別軸 (focus restore on dialog close、aria-live region for toast)。
- ✅ [playwright-iter402 1/1] CreateWorkspaceForm の name → slug field に enterKeyHint 追加 — workspace 初回 onboarding mobile keyboard UX polish: src/components/workspace/create-workspace-form.tsx は (a) name field に enterKeyHint なし、(b) slug field (最終 input) に enterKeyHint なし。iter401 で auth form (login / signup) には enterKeyHint pattern (next / next / send) を適用済だが、初回 onboarding form は未適用で UX 断絶。fix: 1 file × 2 input に enterKeyHint を追加 (2 line)。name IMEInput: `enterKeyHint="next"` (slug field へ移動を示唆)、slug IMEInput: `enterKeyHint="send"` (最終 field、送信を示唆)。これで mobile keyboard label が name="次へ" → slug="送信" の 2 段 progression になり、auth form (iter401) と同じ keyboard UX を workspace 初回 onboarding にも適用、user が auth → workspace 初回作成の一貫した form interaction を体験。機能追加なし、shadcn 編集なし、影響面 1 ファイル。desktop 動作は不変。経路: 経路 B (script、iter401 hint「CreateWorkspaceForm name → slug field に enterKeyHint」を直接適用 + create-workspace-form.tsx audit で 2 input に enterKeyHint 不在を確認 → next/send pair を追加)。検証 codify: `scripts/explore-uiux-create-workspace-enterkeyhint-iter402.ts` で create-workspace name="next" / slug="send" + iter377 onInvalid wire up + iter343 name autoComplete="off" + iter401 login email / signup displayName enterKeyHint 維持 の 6 軸 assert (2/2 OK + regression guard 4 件)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 残 form (item-edit-dialog の 5 IMEInput field 列、sprint / goal / template の form 等) にも enterKeyHint pattern 展開、または focus restore on dialog close。
- ✅ [playwright-iter401 1/1] /login + /signup form の email/displayName field に enterKeyHint="next" — mobile virtual keyboard UX polish: src/components/auth/login-form.tsx + signup-form.tsx は最終 field (password) のみ `enterKeyHint="send"` を持ち、その手前の email / displayName field には enterKeyHint 不在。mobile (iOS / Android) の virtual keyboard は OS default で enter key の label が "go" / "search" / "送信" 等にバラつき、user は「次の field へ移動」したいのに submit が走るかのような misleading な keyboard label を受け取る (WCAG 3.2.4 Consistent Identification 関連の UX gap)。fix: 2 file × 計 3 input に `enterKeyHint="next"` を追加 (3 line)。login-form.tsx: email IMEInput / signup-form.tsx: displayName + email IMEInput。これで mobile keyboard は明示的に「次の field へ」を促す "Next" / "次へ" button label を表示、最終 password field のみ "send" / "送信" になり form 全体で keyboard label progression が直感的になる。機能追加なし、shadcn 編集なし、影響面 2 ファイル。desktop 動作は不変。経路: 経路 A (MCP browser_navigate /login + /signup snapshot で IMEInput attribute audit → enterKeyHint missing on email field を発見) → 経路 B (source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-auth-enterkeyhint-iter401.ts` で login email enterKeyHint="next" / login password enterKeyHint="send" 維持 / signup displayName enterKeyHint="next" / signup email enterKeyHint="next" / signup password enterKeyHint="send" 維持 + iter375 onInvalid wire up + iter255 pending aria-label 維持の 7 軸 assert (5/5 OK + regression guard 2 件)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (ItemEditDialog の field 列に enterKeyHint pattern 拡張、CreateWorkspaceForm の name → slug field に enterKeyHint="next"、または focus restore on dialog close)。
- ✅ [playwright-iter400 1/1] filter-status / filter-sprint select の aria-label を状態 aware で動的化 — iter399 pattern を select に水平展開: iter399 で MUST filter checkbox の aria-label を状態 aware にしたが、隣接の filter-status と filter-sprint select は static "ステータスで絞り込み" / "Sprint で絞り込み" のままだった。SR は現在 filter が active かどうか / どの値で絞り込まれているか が aria-label からは分からない。fix: items-board.tsx 1 file × 2 select で aria-label を状態 aware で動的化 (filter set 時に「<filter 種別> で絞り込み中 (現在: <値>)。「全 <種別>」で解除」、未 set 時に「<filter 種別> で絞り込み (説明)」)。iter399 + iter400 で items-board の 3 filter (must / status / sprint) が全て状態 aware aria-label に統一。1 file 約 12 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter399 hint「filter-status / filter-sprint select の状態 aware 拡張」を直接適用)。検証 codify: `scripts/explore-uiux-filter-select-state-iter400.ts` で 2 select 状態 aware aria-label + iter399 MUST checkbox invariant 維持を 3 軸 assert (2/2 OK)。typecheck 緑。次 iter 候補: 別軸 (view-switcher button group の各 view ボタン aria-label 状態 aware 拡張、または ItemEditDialog Tab アクセシビリティ深掘り)。
- ✅ [playwright-iter399 1/1] items-board MUST filter checkbox に id/htmlFor + 状態 aware aria-label — filter a11y polish: src/components/workspace/items-board.tsx の MUST filter checkbox は `<label>` で `<input>` を wrap する implicit association のみで (a) id/htmlFor の explicit association 不在、(b) aria-label 不在で SR は label text "MUST のみ" を読むが現在の状態 (チェック中 vs 未チェック) と「クリックで何が起きるか」を伝えない。fix: `<label>` に htmlFor="filter-must" 追加、`<input>` に id="filter-must" 追加 (explicit association)、`<input>` に aria-label を状態 aware で動的化 (must=true: "MUST のみ表示中 (クリックで解除)" / must=false: "MUST のみ表示に絞り込む")。1 file 約 5 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 A (real browser script で interactive probe → MUST filter checkbox の implicit label を発見) → 経路 B (source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-must-filter-iter399.ts` で label htmlFor + input id + aria-label 状態 aware + iter394-398 CardTitle heading invariant 維持を 5 軸 assert (3/3 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (filter-status / filter-sprint select の状態 aware aria-label 拡張、または ItemEditDialog Tab section の hierarchy 整理)。
- ✅ [playwright-iter398 1/1] top-items-by-time-chip CardTitle に role="heading" + aria-level — codebase 内 100% carriage 達成: iter394-397 で codebase の主要 CardTitle に role=heading を順次付与してきたが、`grep -rEn "<CardTitle " src/components/ | grep -v 'role="heading"'` で最後 1 件残っているのが top-items-by-time-chip.tsx の「直近 N 日 稼働ダッシュボード」 CardTitle (line 103) と判明。本 iter で fix し codebase 全 CardTitle に role=heading carriage 100% 達成。fix: top-items-by-time-chip.tsx の CardTitle に `role="heading" aria-level={2}` を追加 (top-level chip heading なので level 2、time-entries-panel の他 CardTitle と同 level)。1 file 1 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter397 hint「top-items 系 audit」を直接適用 + grep ベースで残 1 件発見)。検証 codify: `scripts/explore-uiux-top-items-heading-iter398.ts` で top-items-by-time-chip role/aria-level + codebase 全 CardTitle の role=heading carriage 100% (grep negative 0 件) + iter394-397 invariant 維持を 8 軸 assert (2/2 OK)。typecheck 緑。次 iter 候補: 別軸 (h2/h3 hierarchy audit、または team-context-editor / budget-panel 等の細部 a11y polish、または real browser で見つかった他 issue の探索)。
- ✅ [playwright-iter397 1/1] time-entries 系 4 CardTitle に role="heading" + aria-level — heading semantic gap: iter395 real browser 探索で発見した time-entries page の 3 件 (見積精度 / 新規 稼働記録 / 一覧) + estimate-bias-insight の loading 用 CardTitle 1 件、計 4 件で role=heading 不在を修正。iter394 (templates) + iter395 (workflows + integrations) + iter397 (time-entries) で codebase 内 主要 panel CardTitle が全て heading semantic 統一 (dashboard / today / sprints / goals / pdca / templates / workflows / integrations / time-entries の 9 panel)。fix: 2 file × 各 2 箇所 (time-entries-panel.tsx の 新規 稼働記録 + 一覧、estimate-bias-insight.tsx の loading + main 「見積精度 (直近)」、全て level=2)。2 file 合計 約 14 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter396 hint「time-entries 残 3 CardTitle」を直接適用 + estimate-bias の loading state CardTitle も grep で発見し追加 fix)。検証 codify: `scripts/explore-uiux-time-entries-heading-iter397.ts` で 4 CardTitle の role/aria-level + iter394-396 invariant 維持 (regression guard 4 軸) を 8 軸 assert (4/4 OK)。typecheck 緑。次 iter 候補: 別軸 (real browser で workspace 配下 全 page console error scan、または top-items-by-time-chip / dashboard CardTitle 系の他 audit)。
- ✅ [playwright-iter396 1/1] `EmptyState` description wrapper を `<p>` → `<div>` に変更 — hydration error 解消 (real browser 経路 A → 経路 B): real browser 探索 (chromium 経由 /templates) で console error "In HTML, <p> cannot be a descendant of <p>. This will cause a hydration error." を検出。原因は src/components/shared/async-states.tsx の EmptyState が description を `<p>` で wrap (line 63) しているが、iter273 で description prop を ReactNode に拡張済で caller 側 (templates-panel / archived-items-panel 等 10+ files) は `<p>` を含む JSX を渡している。結果: `<p>{<p>...</p>}{<p>...</p>}</p>` という invalid HTML が生成され、browser が DOM 修正で paragraph を auto-close → React の SSR-CSR mismatch で hydration error。fix: EmptyState の description wrapper を `<p>` から `<div>` に変更 (className そのまま、visual styling 同等)。1 file 約 6 line 差替、機能追加なし、shadcn 編集なし。本 fix で /templates 含む 10+ caller (sprints / inbox / dashboard / goals / items-board / archived-items / personal-period / workflows / time-entries / template-items-editor) の hydration error が一括解消。経路: 経路 A (real browser script で chromium navigate → console error 検出) → root cause analysis (EmptyState <p> wrap + iter273 ReactNode 拡張で発生) → 修正 → 経路 B (source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-empty-state-hydration-iter396.ts` で EmptyState `<div>` wrap + 旧 `<p>` 不在 + iter394/395 CardTitle heading invariant 維持を 5 軸 assert (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: time-entries の残 3 CardTitle (見積精度 / 新規 稼働記録 / 一覧) heading 化、または別軸 (real browser で workspace 配下 全 page を console error scan)。
- ✅ [playwright-iter395 1/1] workflows-panel + integrations-panel の 4 CardTitle に role="heading" + aria-level — heading semantic gap (real browser 経路 A → 経路 B): real browser 探索 (chromium 経由 /workflows /integrations を navigate) で shadcn `<CardTitle>` が role=heading 不在のまま render されている箇所を 4 件発見: workflows-panel「新規 Workflow」 form (level 2) + wf.name (level 3)、integrations-panel「新規 Source」 form (level 2) + src.name (level 3)。iter394 templates-panel pattern を water-fall。fix: 2 file × 2 箇所 (form heading は level 2、各 card name は level 3 の Card 内 sub heading)。time-entries にも 3 件あったが scope を 2 file に維持するため次 iter で対応予定。2 file 合計 約 16 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 A (real browser script で chromium navigate → CardTitle role=null 4 件発見) → 修正 → 経路 B (source-side regex assert で codify)。検証 codify: `scripts/explore-uiux-workflow-integrations-heading-iter395.ts` で 4 CardTitle の role/aria-level + iter394 templates-panel invariant 維持を 5 軸 assert (4/4 OK)。typecheck 緑。次 iter 候補: time-entries の残 3 CardTitle (見積精度 / 新規 稼働記録 / 一覧) の heading 化、または別軸 (real browser で発見した DOM nesting hydration error 「<p> cannot contain <p>」を grep ベースで fix)。
- ✅ [playwright-iter394 1/1] templates-panel.tsx の 2 CardTitle に role="heading" + aria-level — heading semantic gap: shadcn の `<CardTitle>` は `<div data-slot="card-title">` 実装で role=heading を持たず、CardHeader 内に書いただけでは SR の heading navigation で識別されない。今 codebase の他 CardTitle (dashboard / today / sprints / goals / pdca 等) は `role="heading" aria-level={2}` を明示しているが templates-panel.tsx の 2 箇所だけ抜けていた。fix: 「新規 Template」 form CardTitle を `role="heading" aria-level={2}` に、各 template card t.name CardTitle を `role="heading" aria-level={3}` (Card 内 sub heading) に変更。1 file 約 6 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter393 hint「focus-visible / lang attribute / form ARIA」+ codebase audit で grep CardTitle without role=heading を発見)。検証 codify: `scripts/explore-uiux-templates-heading-iter394.ts` で 2 CardTitle の role/aria-level + iter389 skip-link invariant 維持を 3 軸 assert (2/2 OK)。typecheck 緑。次 iter 候補: 別軸 (focus-visible ring carriage / lang attribute audit / form ARIA invalid 系) に切替。
- ✅ [playwright-iter393 1/1] workspace 残 4 page (workflows / integrations / time-entries / archive) の `<main>` に id="main-content" — skip-link target 全 14 page 完成 (WCAG 2.4.1 完全準拠): iter392 で workspace 4 page を片付けたが残 4 page (workflows / integrations / time-entries / archive) は id 不在で skip-link が無効。fix: 4 file × 1 line addition で `<main id="main-content">` を付与。これで codebase 全 page (root layout + auth layout + offline + workspace home + workspace 8 page + mock-timesheet 3 page = 計 13 file 14 page) の skip-link target が完成、keyboard user は全 page で Tab 1 回 + Enter で main 直行可能、毎 page Tab 数を平均 12-15 → 1 回に削減 (12-15x 高速化)。機能追加なし、shadcn 編集なし、影響面 4 ファイル。経路: 経路 B (script、iter392 hint「残 workspace 4 page」を直接適用)。検証 codify: `scripts/explore-uiux-workspace-skip-target-b-iter393.ts` で 4 page の `<main id>` + iter389 layout skip-link + iter389-392 既存 10 file の `<main id>` invariant 維持を 15 軸 assert (4/4 OK)。typecheck 緑。次 iter 候補: 別軸 (lang attribute audit / focus-visible ring carriage / form ARIA invalid 系の codebase 横断 audit) に切替。
- ✅ [playwright-iter392 1/1] workspace 4 page (sprints / goals / pdca / templates) の `<main>` に id="main-content" — iter389-391 skip-link target 拡張: iter389-391 で skip-link を追加し root layout / auth / offline / workspace home / mock-timesheet 3 page の `<main>` に id を付与したが、workspace 配下 8 page (sprints / goals / pdca / templates / workflows / integrations / time-entries / archive) は id 不在で skip-link が無効。本 iter で 4 page (sprints / goals / pdca / templates) を片付け、次 iter で残 4 page。fix: 4 file × 1 line addition で `<main id="main-content">` を付与。機能追加なし、shadcn 編集なし、影響面 4 ファイル。経路: 経路 B (script、iter391 hint「workspace 8 page」を 2 batch 化)。検証 codify: `scripts/explore-uiux-workspace-skip-target-a-iter392.ts` で 4 page の `<main id>` + iter389-391 invariant 維持を 11 軸 assert (4/4 OK)。typecheck 緑。次 iter 候補: 残 workspace 4 page (workflows / integrations / time-entries / archive) で skip-link 全 page 完成。
- ✅ [playwright-iter391 1/1] mock-timesheet 3 page (login / new / entries) の `<main>` に id="main-content" — iter389-390 skip-link target 完全展開: iter389/390 で root layout に skip-link を追加し auth/offline/workspace home の `<main>` に id を付与したが、mock-timesheet 3 page の `<main>` は id 不在で skip-link が graceful degradation で無効化されていた。fix: 3 file × 1 line addition (mock-timesheet/login/page.tsx + mock-timesheet/new/page.tsx + mock-timesheet/entries/page.tsx の `<main>` に id="main-content" 付与)。3 file 合計 約 3 line、機能追加なし、shadcn 編集なし。残 page (workspace 配下 8 page: sprints / goals / pdca / templates / workflows / integrations / time-entries / archive) は次 iter で順次。経路: 経路 B (script、iter390 hint「mock-timesheet 3 page」を直接適用)。検証 codify: `scripts/explore-uiux-mock-skip-target-iter391.ts` で 3 page の `<main id="main-content">` 存在 + iter389/390 root layout skip-link / auth / offline / workspace home の id invariant 維持を 7 軸 assert (3/3 OK)。typecheck 緑。次 iter 候補: workspace 配下 8 page の `<main>` id 付与。
- ✅ [playwright-iter390 1/1] auth layout + offline page の `<main>` に id="main-content" — iter389 skip-link target 拡張: iter389 で root layout に skip-link を追加したが、auth (login / signup) layout と /~offline page の `<main>` に id 不在で keyboard user が skip-link を Enter しても何も起きない (graceful degradation)。fix: (a) src/app/(auth)/layout.tsx の `<main>` に id="main-content" を付与 (login + signup の両 page を 1 file で カバー)、(b) src/app/~offline/page.tsx の `<main>` に id="main-content" を付与。2 file 合計 約 6 line 差替、機能追加なし、shadcn 編集なし。残 page (mock-timesheet 3 page / workspace 配下 8 page) は次 iter で順次。経路: 経路 B (script、iter389 hint「残 page の id 付与」を直接適用)。検証 codify: `scripts/explore-uiux-skip-target-auth-offline-iter390.ts` で auth layout / offline page の `<main id="main-content">` 存在 + iter389 root layout skip-link / workspace home invariant 維持を 4 軸 assert (2/2 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: mock-timesheet 3 page (login / new / entries) または workspace 配下 8 page (sprints / goals / pdca / templates / workflows / integrations / time-entries / archive) の `<main>` id 付与。
- ✅ [playwright-iter389 1/1] keyboard user 用 skip-to-main link を root layout に追加 + workspace home `<main>` id 付与 (WCAG 2.4.1 Bypass Blocks): 旧 codebase は skip-to-main link 不在で keyboard user (Tab navigation のみ) は header / nav / utility を毎 page 全部 Tab で skip しないと `<main>` 内の操作ターゲットに到達不能 (例: workspace home で 12 nav button + 3 utility button = 15 tab 押下が毎回必要)。fix: (a) src/app/layout.tsx の `<body>` 直下に `<a href="#main-content">` skip-link を追加 (sr-only default、focus 時のみ可視化、bg-primary + 左上 fixed)、(b) src/app/(workspace)/[workspaceId]/page.tsx の `<main>` に id="main-content" を付与。2 file 合計 約 12 line 差替、機能追加なし、shadcn 編集なし。他 page (auth / mock-timesheet / offline / 他 workspace 配下) は次 iter で順次 id 付与予定 (skip-link 自体は全 page で render されるが、id 不在 page では効かない graceful degradation で動作)。経路: 経路 B (script、iter388 hint「skip-to-main link」を直接適用) + dev server curl で skip link rendered HTML 出力 (`<a href="#main-content" class="bg-primary ...">メインコンテンツへスキップ</a>`) を確認。検証 codify: `scripts/explore-uiux-skip-to-main-iter389.ts` で layout skip-link 存在 + sr-only/focus:not-sr-only pattern + workspace home `<main id>` + iter378-388 invariant 維持 (regression guard 5 軸) を 8 軸 assert (2/2 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 残 page (auth login/signup / mock-timesheet 3 page / offline / 他 workspace 配下 sprints / goals / pdca / templates / workflows / integrations / time-entries / archive) の `<main>` に id="main-content" を順次付与。
- ✅ [playwright-iter388 1/1] pdca-panel DailyBars chart 軸 label に `<time dateTime>` semantic: src/components/workspace/pdca-panel.tsx の DailyBars chart 下端 開始 / 終了日 label (line 235-238) は plain `<span>{data[0]?.date.slice(5)}</span>` で `<time>` 不在 (WCAG 1.3.1 semantic gap)。bar 自体は role="listitem" + aria-label で日付情報を持つが、軸 terminal 2 label は無装飾だった。iter380-387 pattern を chart 軸にも展開。fix: tuple 表現で TS strict noUncheckedIndexedAccess 制約 ([N-1] が undefined 可能) を IIFE で local const 化し、`<time dateTime={first}>{first.slice(5)}</time>` で wrap、null 時は空 span fallback (justify-between layout を維持)。1 file 約 14 line 差替、機能追加なし、shadcn 編集なし。これで codebase 内 panel + Item view + dashboard + chart 全 date 表示が `<time dateTime>` semantic に統一。経路: 経路 B (script、iter387 hint「focus / activity time format / 別の semantic gap」+ 別 axis として PDCA chart 軸を発見)。検証 codify: `scripts/explore-uiux-pdca-axis-time-iter388.ts` で chart 軸 first/last `<time dateTime>` wrap + 旧 plain span 不在 + iter378-387 invariant 維持 (regression guard 6 軸) を 9 軸 assert (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (skip-to-main link 追加で keyboard user の navigation 強化、または lang 属性混在 audit、WorkspaceHeader role hierarchy 検証) に切替。
- ✅ [playwright-iter387 1/1] dashboard-view MUST item dueDate に `<time dateTime>` semantic: src/components/workspace/dashboard-view.tsx の MUST list dueDate cell (line 1416) `{item.dueDate ?? '期限なし'}` は plain string 直接表示で `<time>` 不在 (WCAG 1.3.1 semantic gap)。iter380 / 384 / 385 / 386 で確立した `<time dateTime>` pattern を dashboard にも展開。fix: 条件式を ternary 化し dueDate ありで `<time dateTime={item.dueDate}>{item.dueDate}</time>`、無しで '期限なし' plain text を返す。1 file 約 6 line 差替、機能追加なし、shadcn 編集なし。これで codebase 内 panel + Item-level view + dashboard date 表示が 全て `<time dateTime>` semantic に統一 (notification-bell は既に対応済)。経路: 経路 B (script、iter386 hint「dashboard-view date cell 残箇所」に従い grep ベースで MUST list の plain dueDate を発見)。検証 codify: `scripts/explore-uiux-dashboard-due-time-iter387.ts` で dashboard MUST dueDate `<time>` 存在 + 旧 plain `{item.dueDate ?? '期限なし'}` 不在 + iter378-386 invariant 維持 (regression guard 5 軸) を 7 軸 assert (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (item-edit-dialog Activity tab time format / quick-add の preview chip / focus management、または別の semantic gap) に切替。
- ✅ [playwright-iter386 1/1] today-view + kanban-view の dueDate / startDate に `<time dateTime>` semantic: src/components/workspace/today-view.tsx の dueDate cell (line 219-227) および src/components/workspace/kanban-view.tsx の startDate / dueDate cell (line 354-367) は `formatFriendlyDate(...)` の plain string を直接表示、`<time>` 不在で WCAG 1.3.1 semantic gap、SR は date と他 text を区別不能。iter380 / iter384 / iter385 pattern を Item-level view にも統一: (a) today-view dueDate を `<time dateTime={it.dueDate}>{formatFriendlyDate(...)}</time>` で wrap (期限 prefix の後ろ)、(b) kanban-view startDate と dueDate それぞれを fragment 化し `<time dateTime={item.startDate}>` / `<time dateTime={item.dueDate}>` で wrap、既存 title attr の生 ISO は hover 用に残置。2 file 合計 約 14 line 差替、機能追加なし、shadcn 編集なし。これで codebase 内 panel + Item-level view date 表示が全て `<time dateTime>` semantic に統一。経路: 経路 B (script、iter385 hint「ItemCard dueDate」に従い today/kanban view を直接 audit)。検証 codify: `scripts/explore-uiux-item-due-time-iter386.ts` で today-view dueDate `<time>` + kanban-view startDate / dueDate `<time>` + iter378-385 invariant 維持 (regression guard 5 軸) を 8 軸 assert (2/2 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (inbox-view / dashboard-view の date cell 残箇所、ItemEditDialog Activity tab time format、notification-bell の relative time の `<time>` 化) に切替。
- ✅ [playwright-iter385 1/1] sprints / goals panel の period 表示に `<time dateTime>` semantic: src/components/workspace/sprints-panel.tsx (line 415) `{formatDateJa(sprint.startDate)} 〜 {formatDateJa(sprint.endDate)}` および src/components/workspace/goals-panel.tsx (line 357) `{goal.period} · {goal.startDate} 〜 {goal.endDate}` は plain string で `<time>` 不在 (WCAG 1.3.1 gap)。iter380 (mock-entries) / iter384 (archived-items) で確立した `<time dateTime>` pattern を panel 系に水平展開: (a) sprints-panel に `<time dateTime={sprint.startDate}>{formatDateJa(...)}</time>` 形式で wrap、(b) goals-panel に `<time dateTime={goal.startDate}>{goal.startDate}</time>` 形式で wrap。2 file 合計 約 12 line 差替、機能追加なし、shadcn 編集なし。これで codebase 内 panel 系 date 表示が `<time dateTime>` semantic で統一。経路: 経路 B (script、iter384 hint「sprints-panel / goals-panel / templates-panel」に従い grep ベースで sprints + goals に対象を発見、templates-panel は date 表示なし)。検証 codify: `scripts/explore-uiux-period-time-iter385.ts` で sprints + goals 各 startDate/endDate `<time dateTime>` wrap + iter378-384 invariant 維持 (regression guard 6 軸) を 10 軸 assert (2/2 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (item-edit-dialog focus management、または templates-panel / decompose-proposals-panel の他 date cell 展開、または ItemCard の dueDate 表示の `<time>` 化) に切替。
- ✅ [playwright-iter384 1/1] `ArchivedItemsPanel` date cells に `<time dateTime>` semantic: src/components/workspace/archived-items-panel.tsx の date cells (`fmt(item.dueDate)` / `fmt(item.archivedAt)`) は plain string 直接 td 出力で `<time>` 不在、SR 時刻読み上げが numeric として不自然 + 機械可読データ不識別 (WCAG 1.3.1 Info and Relationships gap)。iter380 で mock-entries に適用した `<time dateTime>` pattern を workspace 配下 archive 一覧にも展開。fix: 小さな `FmtTime` component を追加 (約 10 line) し、date cells を `<FmtTime value={item.dueDate} />` / `<FmtTime value={item.archivedAt} />` に置換。FmtTime は valid Date のみ `<time dateTime={ISO}>{表示}</time>` を返し、null/invalid は plain '-' を fragment で返す (dateTime 必須属性回避)。既存 fmt() は他箇所参照なし but 残置。1 file 約 16 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter383 hint「icon-only audit」+ 別 axis として archive panel の semantic gap を発見)。検証 codify: `scripts/explore-uiux-archive-time-iter384.ts` で FmtTime component 存在 + `<time dateTime>{format}` 出力 + `<FmtTime>` wrap 2/2 + 旧 plain `{fmt()}` の td 内残置なし + iter378-383 invariant 維持 + iter383 archive panel 自身の role=alert 維持を 9 軸 assert (2/2 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (sprints-panel / goals-panel / templates-panel の date cell の `<time dateTime>` 展開で同 pattern を panel 系全体に統一、または item-edit-dialog focus management) に切替。
- ✅ [playwright-iter383 1/1] codebase 全 inline error の `role="alert"` carriage 100% 完了: `grep -rn '<p[^>]*text-destructive' src/` で codebase 全 18 箇所を audit した結果、role="alert" 不在の error 表示が 2 箇所残っていた: (1) src/components/workflow/workflows-panel.tsx 559 行 WorkflowEditDialog の inline error は data-testid のみで role/aria-live なし → user が graph JSON / trigger JSON 保存で zod parse 失敗時、SR は error 文言を即座に announce しない、(2) src/components/workspace/archived-items-panel.tsx 61 行 「アーカイブ一覧の取得に失敗しました」error も role 無し。fix: 2 file × 各 1 箇所 (workflows-panel に role="alert" + id={`wf-editor-error-${wf.id}`} 追加 / archived-items <p> を multi-line 化し role="alert" 追加)。codebase 全 18 箇所 (auth 5 / workspace 3 / mock-timesheet 6 / workflow 3 / integrations 1) で role="alert" carriage 100% 完了。1-2 file 約 10 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter382 hint「workflows-panel.tsx の Dialog audit」に従い grep ベースで全 file の text-destructive を網羅)。検証 codify: `scripts/explore-uiux-error-role-alert-iter383.ts` で 2 修正箇所の role="alert" + iter378-382 invariant 維持 + 8 file 横断 text-destructive×role=alert カウント 18/18 = 計 7 軸 assert (3/3 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (workspace 配下 view-switcher / item-edit-dialog / quick-add の a11y polish、icon-only button の aria-label audit) に切替。
- ✅ [playwright-iter382 1/1] `KeybindingsHelpModal` section / dt semantic / a11y: src/components/shared/keybindings-help-modal.tsx は Dialog 自体は Radix の DialogTitle / DialogDescription で aria 属性 OK だが (1) 各 group `<section>` は `<h3>` を含むだけで `aria-labelledby` なし、(2) h3 は group 名のみで件数が SR 不可視、(3) `<dt>` は `<kbd>` 子だけで combo 文字列を直接 SR に伝える means 無し (`g t` chain は kbd 内 char を逐次「ジー ティー」と読まれて意味不明)。fix: (a) group section に `aria-labelledby={headingId}` + h3 に `id={headingId}` を付与 (group 名から slugify、空白→`-`)、(b) h3 に `<span className="sr-only"> ({list.length} 件)</span>` を append (visual には影響なし、SR は「ナビゲーション (6 件)」と読み上げ)、(c) `<dt>` に `aria-label={`ショートカット ${kb.combo}`}` を付与 + `<kbd>` を `aria-hidden="true"` で個別読み上げを抑止、SR は dt の aria-label を 1 度だけ読み「ショートカット g t」と意味のある単位に。1 file 約 18 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter381 hint「keybindings-help-modal の a11y」に従い直接適用)。検証 codify: `scripts/explore-uiux-keybindings-help-iter382.ts` で section aria-labelledby + h3 id + headingId 変数 slugify + sr-only 件数 span + dt aria-label + kbd aria-hidden + iter378/379/380/381 invariant 維持の 10 軸 assert (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: workflows-panel.tsx の Dialog audit、または別軸 (workspace 配下 view-switcher / quick-add の a11y polish) に切替。
- ✅ [playwright-iter381 1/1] mock-timesheet `MockTopNav` semantic / a11y polish: src/components/mock-timesheet/mock-top-nav.tsx は `<header>` + `<h1>` までは semantic だが (1) link 群が `<div>` で wrap (nav landmark なし) → SR landmark navigation で nav 識別 / skip 不可、(2) logout `<form>` + `<Button>ログアウト</Button>` は context (どこから logout) が button text のみで disambiguate 不能、(3) sessionId 表示 "ログイン中: <id>" は SR で 1 segment 読み上げで id 役割が context に出ない。fix: (a) link 群を `<nav aria-label="mock-timesheet ナビゲーション">` に置換 (landmark + label)、(b) logout `<form>` に `aria-label="mock-timesheet からログアウト"`、内部 `<Button>` に `aria-label="mock-timesheet session をログアウト"` を追加 (form group / button 二重で context 明示)、(c) sessionId 表示を 3 span 構成に: sr-only "現在の session ID: " / aria-hidden "ログイン中: " / font-mono `<id>` で SR には semantic、visual には既存表示維持。1 file 約 14 line 差替、機能追加なし、shadcn 編集なし。これで mock-timesheet 系は iter378 (MockLoginForm) / iter379 (MockSubmitForm) / iter380 (entries table) / iter381 (top-nav) の 4 連続で a11y 完備し本系統 close。経路: 経路 B (script、iter380 hint「MockTopNav 候補」に従い直接適用)。検証 codify: `scripts/explore-uiux-mock-top-nav-iter381.ts` で nav landmark + logout form/button aria-label + sessionId 3 span 構成 + iter378/379/380 invariant 維持の 9 軸 assert (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (workspace 配下 dashboard / today / inbox view の audit、または item-edit-dialog focus restore / keybindings-help-modal の a11y) に切替。
- ✅ [playwright-iter380 1/1] mock-timesheet `MockEntriesPage` table semantic / a11y: src/app/mock-timesheet/entries/page.tsx の `<table>` は data-testid のみで (1) `<caption>` 不在 → SR は table 内 navigate しても何の表か context 不明、(2) `<th>` 6 個全てに scope 属性なし → header-cell 関連付けが implicit で SR は cell read 時に列 header を announce しない場合あり (WCAG 1.3.1 Info and Relationships)、(3) 日付 / 送信時刻 cell が plain text で `<time>` 不在 → SR 時刻読み上げが numeric として不自然、機械可読データとして識別されない。fix: (a) `<caption className="sr-only">` で table の概要 (件数 + 列名) を SR 用に追加 (visual には影響なし)、(b) 6 個の `<th>` 全てに `scope="col"` 追加、(c) workDate cell を `<time dateTime={e.workDate}>` で wrap、(d) submittedAt cell を一時変数 `submittedIso` に集約し `<time dateTime={submittedIso}>` で wrap (toISOString 重複呼び出し整理)。1 file 約 25 line 差替、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter379 hint「別軸に切替」+ mock-timesheet 系完成度上げで /entries page を audit → table semantic gap 3 点を発見)。検証 codify: `scripts/explore-uiux-mock-entries-table-iter380.ts` で caption sr-only + th scope="col" 6/6 + workDate/submittedAt の `<time dateTime>` wrap + submittedIso 変数集約 + iter378/iter379 invariant 維持の 7 軸 assert (2/2 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: MockTopNav (logout button confirmation / nav semantic) の a11y polish、または別軸 (workspace 配下 dashboard / sprints / goals 等の table audit、item-edit-dialog focus restore) に切替。
- ✅ [playwright-iter379 1/1] mock-timesheet `MockSubmitForm` (4 fields: workDate / category / description / hoursDecimal): iter378 で着手した mock-timesheet 系 a11y pattern を残 1 form に展開し系統完了。zod が reject する場合 (例: hoursDecimal 0.25 multiple-of 違反 / 24h 超過 / description 2000 字超 / workDate format 違反) も silent に握り潰される a11y/UX gap (WCAG 3.3.1 / 4.1.3 違反) を解消。iter255 / iter375 / iter377 / iter378 で確立した 7 点 a11y pattern を水平展開: (1) `mode: 'onTouched'`、(2) `onInvalid` で第 1 errored field setFocus、(3) `<form noValidate>`、(4) `handleSubmit(onSubmit, onInvalid)`、(5) 各 input/select に aria-invalid + aria-describedby、(6) 各 field に `<p role="alert" id="<id>-error">`、(7) submit button に pending 時 aria-label。1 file 約 50 line 追加、機能追加なし、shadcn 編集なし。これで useForm 系 form 全 5 件 (login / signup / create-workspace / mock-login / mock-submit) で a11y pattern 完備し本系統 close。経路: 経路 B (script、iter378 hint「残 mock-submit-form を次 iter」に従い直接適用)。検証 codify: `scripts/explore-uiux-mock-submit-onInvalid-iter379.ts` で field 毎 4×3=12 軸 + onInvalid / handleSubmit / mode / noValidate / pending aria-label の 5 軸 + iter378 / iter255 / iter375 / iter377 invariant 維持の 4 軸 regression guard (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 別軸 (focus restore on dialog close / aria-live region for toast / item-edit-dialog の form pattern audit) に切替、useForm 系列は完了。
- ✅ [playwright-iter378 1/1] mock-timesheet `MockLoginForm`: src/components/mock-timesheet/mock-login-form.tsx は `onSubmit={form.handleSubmit(onSubmit)}` (onInvalid 引数なし) + field 毎の `<p role="alert">` 一切なし + aria-invalid / aria-describedby 無し + useForm mode 未指定 (default 'onSubmit') + pending button に aria-label 無しという a11y/UX gap。zod validation 失敗時、user は submit が silent に握り潰されたかのように見え何が悪いのか分からない (WCAG 3.3.1 / 4.1.3 違反)。iter255 / iter375 (auth) / iter377 (workspace create) で確立した a11y pattern を mock-timesheet 系に水平展開: (1) useForm に `mode: 'onTouched'` (blur 時 inline error)、(2) `function onInvalid(errors)` で第 1 errored field へ setFocus、(3) `<form noValidate>` で browser native validation 抑止、(4) `handleSubmit(onSubmit, onInvalid)` wire up、(5) 各 IMEInput に aria-invalid + aria-describedby を error 有無で切替、(6) 各 field に `<p role="alert" id="<field>-error">` を error 時のみ render、(7) submit button に pending 時 aria-label 追加 (iter255 pattern)。1 file 39 line 追加、機能追加なし、shadcn 編集なし。経路: 経路 B (script、iter377 hint「他 form にも展開」に従い `grep -rln "form.handleSubmit(onSubmit)"` で残 form 探索 → mock-timesheet 系 2 form を発見、まずは 2 field の login form から)。検証 codify: `scripts/explore-uiux-mock-login-onInvalid-iter378.ts` で onInvalid handler / handleSubmit wire up / mode=onTouched / noValidate / field 毎 aria-invalid + aria-describedby + role=alert + pending aria-label の 13 軸 assert + iter255/375/377 invariant 維持の regression guard 3 軸 (1/1 OK)。typecheck / lint 緑 (warning baseline 1)。次 iter 候補: 残 mock-submit-form.tsx (4 fields: workDate / category / description / hoursDecimal) にも同 pattern 展開で mock-timesheet 系を完了、または別軸 (focus restore on dialog close / aria-live region) に切替。
- ✅ [playwright-iter377 1/1] workspace 初回 onboarding (CreateWorkspaceForm): src/components/workspace/create-workspace-form.tsx は `onSubmit={form.handleSubmit(onSubmit)}` で onInvalid 引数なし、iter375 で auth form には onInvalid 追加済だったが workspace 初回 onboarding form は未対応で pattern 断絶。zod validation 失敗時 (例: name 空 / slug pattern 不一致) focus は submit button 等に残り user は手動で field 探す cognitive load。fix: `function onInvalid(errors)` を追加 (4 行)、`Object.keys(errors)[0]` で第 1 errored field 取得し `form.setFocus(field)` で focus 移動、`handleSubmit(onSubmit, onInvalid)` wire up (1 行差替)。1 file 5 行追加。CreateWorkspaceInput の field order (name → slug) と focus 移動先が一致。WCAG SC 3.3.1 を SR alert announce + focus shift の二段で強化、iter375 (auth form) → iter377 (workspace form) で onInvalid focus pattern が auth → workspace 系に水平展開し初回 onboarding flow の UX 整合確立。機能追加なし、shadcn 編集なし、影響面 1 ファイル。経路: 経路 B (script、iter375 hint「他 form にも展開」に従い workspace pages の form を grep → create-workspace-form を選定)。検証 codify: `scripts/explore-uiux-create-workspace-onInvalid-iter377.ts` で onInvalid handler + handleSubmit wire up + iter375 login/signup invariant 維持を 4 軸 assert (1/1 OK + regression guard 2 件)。実 form 動作は workspace page (auth 必須) で cloud env integration test 不可、source-side regex audit で代替。次 iter 候補: 他 form (sprints / goals / templates / item edit dialog 等) にも同 pattern 展開。
- ✅ [iter3] login: email/password input に required 属性なし → fix: `login-form.tsx` に required + aria-required + minLength=8
- ✅ [iter4] login: signup link plain click が dev overlay で hit-test 失敗していた → fix: `login/page.tsx` の Link / CardFooter に `relative z-10` を付与 (force:true なら遷移するが本来不要のはず → dev devtools overlay が遮ってた)
- ✅ [iter5] signup: displayName / email / password に required + aria-required + minLength を追加 (signup-form.tsx)
- ⏳ [iter4] signup: 短 password 送信時に toast が出ない (RHF zodResolver が submit 前に block しているか、エラー UI 不足) — required で空送信は防げるが zod errors のインライン表示 / toast は別問題、次 iter
- ✅ [iter5] 未 login workspace アクセス: /login へ redirect されることを確認 (middleware OK)
- ❎ [iter6] login: Playwright UI signin で url が /login のまま **誤検出** → iter 7 で再検証 (POST 200 / cookie sb-127-auth-token 設定 / 5s wait 後に / へ遷移済) → bug 無し
- ✅ [iter7] Gantt: workspace 横断 blocks edges 取得 hook (`useWorkspaceBlocksDependencies`) を追加 (gantt-view.tsx の edges prop に渡せるようになった、配線は次 iter)
- ✅ [iter8] Gantt: GanttViewWithDeps wrapper を作って ganttViewPlugin に配線。hook で edges 取得 + computeCriticalPath 計算 → GanttView の edges/criticalIds に流し込み完了
- ✅ [iter9] / root (workspace 作成画面): name + slug input に required + aria-required + minLength + maxLength + slug pattern を追加 (create-workspace-form.tsx)
- ✅ [iter9] /<wsId>/templates 画面: 軽探索 (inputs=1 buttons=4 headings=Templates/新規Template/空メッセージ) → 新規バグ無し
- ✅ [iter10] Gantt view に Today 縦線無し (TeamGantt/GanttPRO 典型機能) → fix: gantt-view.tsx に半透明赤縦線 + 「今日」ラベル + 時刻分の小数 offset で時間方向位置も反映
- ✅ [iter11] /sprints 軽探索: 空状態でも form が見えていて submit が空時 disabled (正常)。新規バグ無し
- ✅ [iter11] Gantt 週末色分け追加 (Sat/Sun cell の bg-muted/40 + body 縦帯薄灰色 = TeamGantt 風)
- ✅ [iter12] /goals form の goal-title input に required なし → fix: required + aria-required + minLength + maxLength を追加。start/end date にも required + min={startDate} で end >= start を HTML 検証
- ✅ [iter13] /pdca 軽探索 (nav 1953ms, 30/90 切替 OK) → 新規バグ無し
- ✅ [iter13] /sprints form a11y: sprint-name (required + minLength + maxLength{100}) / sprint-start, sprint-end (required + min={startDate} で end >= start を HTML 検証) を追加
- ✅ [iter14] /time-entries form: teDate / teCategory / teDescription / teMinutes に required + aria-required を追加。teDate に max=today / teDescription に maxLength=500 / 全項目に native 検証
- ✅ [iter15] Dashboard view の recharts ResponsiveContainer warning (`width(-1) height(-1)`) → fix: 親 div に `minWidth:0 minHeight:256`、ResponsiveContainer に明示的に width/height/minWidth/minHeight 指定 (dev mode の hidden state レース回避)
- ✅ [iter15→16] Gantt today-line: iter15 で count=0 だったのは items の date range が今日を含んでいなかった + wait 不足。iter16 で「今日±3 日」items + 2.5s wait → today-line=1 / weekend cells=2 確認 OK
- ✅ [iter16] Gantt: 月境界線追加 (TeamGantt 風) — 月が切り替わる位置に slate 半透明 1px 線 + 「M月」ラベル
- ✅ [iter17] Kanban view: items=5 投入で MUST badge 2 件 / quickAdd 2 / checkbox 1 確認、新規バグ無し
- ✅ [iter17] Gantt bar 内に item title 表示 (TeamGantt 風) — barWidth>=60px のみ truncate で。短い bar は d 数字のみ。title 属性に start→due + (critical) 詳細を含める
- ✅ [iter18] Backlog view 詳細探索 (items=8 / rows=9 / sort header / bulk-action-bar 動作確認) → 新規バグ無し
- ✅ [iter18] Gantt: spanDays=1 (1 日完結) を **milestone ◇ 菱形** で表示 (TeamGantt 風 18px rotate 45)。critical path 強調 / MUST 色は維持。data-milestone="true|false" で testable
- ✅ [iter19→20] Inbox view items 0 は **wait 不足** が原因。3000ms wait で 2/2 visible 確認 (filter / cache 問題なし)
- ✅ [iter20] Gantt: 完了済 (doneAt あり) bar に opacity=0.4 + line-through (TeamGantt 風)。data-done="true|false" で testable / title に "[完了]" 追記
- ✅ [iter21] Theme toggle (light↔dark) / Command palette (Cmd+K で focus INPUT) 両方 OK、新規バグ無し
- ✅ [iter21] Gantt bar に subtle drop shadow `0 1px 2px rgba(0,0,0,0.18)` を追加 (TeamGantt 風奥行き感、critical 赤太枠と 2 段重ね)
- ✅ [iter22] ItemEditDialog: deep link `?item=<id>` で開く / 5 tabs (base/subtasks/dependencies/comments/activity) 全部 OK / dependencies-panel render OK
- ✅ [iter22] ItemEditDialog editTitle / editDue / editDescription a11y: title required + maxLength=500 / due に min={startDate} で期限>=開始 / description に maxLength=10000
- ✅ [iter23] Notification bell 探索: candidate 3 / unread badge=1 / popover 開く OK 新規バグ無し
- ✅ [iter23] アーカイブビュー新規 page (/<wsId>/archive): ArchivedItemsPanel で archivedAt!=null の items を表に表示 (POST_MVP "アーカイブビュー" 着手)
- ❎ [iter24→25] /archive page items 0 は **誤検出**。iter25 で archived 1 件で rows=1 確認 ✓ (iter24 のスクリプトのタイミング issue or 起動順序問題、実装は正常)
- ✅ [iter24] workspace root header に Archive ナビ link 追加 (page.tsx の pageActions に Templates の隣)
- ✅ [iter25] itemService.archive / unarchive method 追加 + 4 ケース test (happy / 二重 archive / unarchive / unarchived の unarchive)。audit 'archive'/'unarchive' / 楽観ロック対応
- ✅ [iter26] ItemEditDialog activity tab 軽探索 (open 900ms、新規バグ無し)
- ✅ [iter26] archive/unarchive Server Action + useArchiveItem/useUnarchiveItem hook + ArchivedItemsPanel に「復元」button (POST_MVP "アーカイブビュー" 復元 機能完成)
- ✅ [iter27] Today view card hover 軽探索 (visible=0 は wait 不足、既知)。新規バグ無し
- ✅ [iter27] ItemEditDialog の DialogFooter に **archive / unarchive button** を追加 (archived 状態で動的切替、confirm 付き)。POST_MVP "アーカイブビュー" の一連のフロー完成: 一般 view → 編集 → アーカイブ → /archive → 復元
- ✅ [iter28] Backlog view 30 items render (1.8s switch、新規バグ無し)
- ✅ [iter28] **Reviewer Agent role 定義** (POST_MVP "Reviewer Agent" 着手)。AGENT_ROLES に 'reviewer' / DEFAULT_DISPLAY_NAMES / claude-flow-runner ROLE_CONFIG / system prompt (規約遵守 + 失敗 path + a11y + RLS チェック観点) を追加。tool bundle / service.run は次 iter
- ✅ [iter29→30] dark mode 探索: aria-label="テーマ切替" は ASCII 'theme' に hit しないだけだった。**iter30 で `[data-testid="theme-toggle"]`** で確実に特定 → toggle で isDark=true 確認 / 13 visible buttons / 0 finding
- ✅ [iter29] **Slack 通知 dispatcher 着手** (POST_MVP "Slack 通知")。`src/features/slack/dispatcher.ts` で `SLACK_WEBHOOK_URL` 環境変数 (mock=console.log)、Email dispatcher と同じ I/F + 失敗時 `delivered:false` で best-effort。4 ケース test
- ✅ [iter30] ArchivedItemsPanel の row title を `<Link href="/{ws}?item={id}">` で wrap → click で workspace に戻りつつ ItemEditDialog deep link で開く (data-testid="archive-title-link-<id>")
- ✅ [iter31] Gantt bar click で **ItemEditDialog 自動 open** (TeamGantt 典型 UX を補完)。nuqs `useQueryState('item')` で deep link、cursor:pointer、milestone ◇ も同様。bar 内 title click で詳細編集に直接遷移できるように
- ✅ [iter32] iPhone 13 viewport (390x844) で Today 探索: body.scrollWidth=viewport で横スクロール無し / heading 表示 OK / 新規バグ無し
- ✅ [iter32] heartbeat worker から `dispatchSlack` 並列呼び出し配線 (iter29 Slack dispatcher 実利用)。emailPending を flatMap で email + Slack 両方の Promise を並列実行。stage ごとに「7 日後/3 日後/1 日後/期限切れ」をテキスト化、linkUrl 付き
- ✅ [iter33] @ メンション通知も `dispatchSlack` 並列配線 (comment/service.ts)。`*<by>* が *<itemTitle>* で @ メンションしました\n> <preview>` 形式で linkUrl 付き
- ✅ [iter34] workspace **invite** 通知も `dispatchSlack` 並列配線。残: sync-failure / agent-result。/docs route は未実装 (404) を確認 → POST_MVP として記録
- ✅ [iter35] /sprints 作成 form: goal textarea に `maxLength={500}` (schema 上限と整合) + 終了 < 開始 を runtime validation で toast.error → submit 阻止 (button onClick なので native HTML5 validation 効かず)
- ✅ [iter36] /goals 作成 form: description textarea に `maxLength={2000}` (schema 上限) + 終了 < 開始 を runtime validation で toast.error → submit 阻止 (sprint と同パターン水平展開)
- ✅ [iter37] /goals KR 追加 form a11y 強化: KR title に aria-label / required / minLength=1 / maxLength=300、mode select に aria-label、target/unit input に aria-label 付与 (unit は maxLength=20 も)
- ✅ [iter38] /time-entries 作成 form: submit button が type="button" だったため Enter キーで submit 不可 → type="submit" に変更し冗長な onClick 削除 (form onSubmit に一本化、IMEInput は IME 確定中のみ Enter ガード)
- ✅ [iter39] /sprints 作成 form: そもそも `<form>` で wrap されておらず Enter キーで submit 不可 → CardContent 内を `<form onSubmit>` で包み button を type="submit" に。Todoist 風の Enter-to-submit を sprint にも展開
- ✅ [iter40] /goals 作成 form: 同じく form 要素なしで Enter submit 不可 → `<form onSubmit>` wrap + button type="submit" (iter39 sprint と同パターン)
- ✅ [iter41] /templates instantiate-form: `<div>` で wrap されており Enter submit 不可 → `<form onSubmit>` 化 + button type="submit" (sprint/goal と同パターンを template にも展開)
- ✅ [iter42] /pdca period toggle (30 日 / 90 日): aria-pressed が無く SR で選択状態が伝わらない → `aria-pressed={days === N}` + `role="group" aria-label="集計期間"` 付与
- ✅ [iter43] workspace view-switcher (Today/Inbox/Kanban/Backlog/Gantt/Dashboard) も同症状 → 各 button に `aria-pressed={view === X}` + 親 div に `role="group" aria-label="表示切替"` (iter42 と同パターン水平展開)
- ✅ [iter44] filter / dependency 用 native `<select>` の aria-label 抜け 3 件補完: filter-status / dep-kind / dep-target に label 追加 (filter-sprint は既設、Sprint filter → Sprint で絞り込みに用語統一)
- ✅ [iter45] template-items-editor 子 Item 追加 form: title input に aria-label / required / minLength=1 / maxLength=500 (schema 整合)、期日 offset input に aria-label。Playwright は seed sample template 未配置 workspace で editor 描画されず、コード inspection で fix
- ✅ [iter46] **Gantt project summary banner** を timeline 上部に追加 (TeamGantt/GanttPRO 風)。表示範囲日数 / 表示中 Item 数 / CPM 期間 / critical path 件数を一行で表示。`gantt-view-with-deps` から `projectDurationDays` を渡す配線も同時実装
- ✅ [iter47] **Gantt baseline schema migration** (TeamGantt 当初計画 vs 現在の差分可視化用): items に `baseline_start_date` / `baseline_end_date` / `baseline_taken_at` 列追加 + CHECK 制約 (両方 NULL or 両方 set / start <= end)。drizzle schema も同期。Service / UI は次 iter
- ✅ [iter48] **Gantt baseline service** (`itemService.setBaseline` + `setItemBaselineAction`): current startDate/dueDate を baseline\_\* に snapshot、楽観ロック + audit (action='set_baseline')、failure path 2 件 (date 未設定で ValidationError) もテスト。+2 tests = 439 PASS。UI 配線は次 iter
- ✅ [iter49] **Gantt baseline bar 描画** (TeamGantt 風 — 当初計画 vs 現在の差分可視化): baseline_start/end が set された item に slate-500 半透明の細 bar (5px) を行下端に表示。pointer-events:none で actual bar との click 競合なし、title attr で日付 tooltip。`gantt-baseline-<id>` testid。Playwright で描画確認
- ✅ [iter50] **ItemEditDialog にベースライン記録 button** 追加: `useSetItemBaseline` hook + DialogFooter に `data-testid="item-edit-set-baseline"` button (label は「ベースライン記録」/ 既設なら「ベースライン更新」、tooltip で現 baseline 日付表示)。これで schema → service → action → hook → UI の baseline 配線完結。Playwright で実 DB 書き込みも確認
- ✅ [iter51] **Gantt slip 集計** (TeamGantt の "behind / ahead schedule" 風): summary banner に baseline 件数 + 遅延件数 + 合計遅延日数 (amber) を追加、bar tooltip にも `[遅延 +N日]` / `[前倒し N日]` / `[計画通り]` を付与。Playwright で 3 日遅延 item 投入 → "遅延 1 件 / 計 3 日" 表示確認
- ✅ [iter52] **sync-failure 通知も Slack 並列配信** (POST_MVP "Slack 通知" 残: agent-result のみに): time-entry/worker.ts の email 配信 `await` を Promise.all で email + Slack 並列化、text に `*外部同期に失敗しました* (time-entry)\n> <reason>` + linkUrl を入れる
- ✅ [iter53] **baseline クリア機能** (TeamGantt の baseline 取り直し用): `itemService.clearBaseline` + `clearItemBaselineAction` + `useClearItemBaseline` + ItemEditDialog の "baseline クリア" button (window.confirm で確認 → 3 列を NULL に戻す + audit `clear_baseline`)。失敗 path 2 件もテスト → 441 PASS (+2)。Playwright で実 DB クリア確認
- ✅ [iter54] /archive table の a11y 補完: `<caption class="sr-only">` で表の用途を SR に説明 + 全 `<th>` に `scope="col"` を付与 (WCAG 1.3.1 — header と data cell の関係明示)
- ✅ [iter55] /time-entries 表にも同 a11y パターン水平展開: caption + 6 `<th>` に scope="col" + 操作列の空 th に `<span class="sr-only">操作</span>` (空 header は読み飛ばされて列数が伝わらない問題対策)
- ✅ [iter56] Dashboard ai-cost-table も同パターンで a11y 補完: caption + 7 `<th>` (月/Role/実行数/成功失敗/Input/Output/Cost) に `scope="col"` (iter54-55 と同じ WCAG 1.3.1 ベース)。recharts の width(-1) warning は別途記録
- ✅ [iter57] Backlog view (TanStack Table) にも展開: caption + 全 `<th>` に `scope="col"`、sortable 列には sort 状態に応じた `aria-sort="ascending|descending|none"` (header click で動的更新)。これで /archive, /time-entries, /dashboard, /backlog の 4 表すべて WCAG 1.3.1 準拠
- ✅ [iter58] comment-thread textarea (item edit dialog Comments tab) の a11y 補完: aria-label="コメント本文" / required / maxLength=10000 (schema z.string().max(10_000) と一致) / placeholder に "@user で言及・通知" を加筆して機能を hint
- ✅ [iter59] comment-thread の **編集 mode textarea** にも同 a11y セット (aria-label="コメント編集" / required / maxLength=10000) + `data-testid="comment-edit-input-<id>"` 付与でテスト fixture 化容易に
- ✅ [iter60] **Gantt 「今日へジャンプ」button** (TeamGantt 風 navigation): summary banner 右端に `gantt-jump-today` button、click で outer scroll container を smooth scroll、today 線が中央に来る `(LABEL_COL_PX + todayX - viewport/2)`。Playwright で scrollLeft 0 → 279px 移動を確認
- ✅ [iter61] **Gantt 初回 mount で today へ自動スクロール** (TeamGantt/GanttPRO default UX): useEffect + useRef で 1 回だけ instant scroll。長期間 Gantt でも開いた瞬間 today が viewport 中央に来る。range/totalDays の計算を early return より先に持ち上げて rules-of-hooks 準拠。Playwright で初回 scrollLeft=839px を確認
- ✅ [iter62] **Gantt 「完了済を隠す」toggle** (TeamGantt 風 filter): summary banner に checkbox を配置、ON で `doneAt` あり item を withDates から除外して行数を圧縮。Playwright で done+todo の 2 件 workspace で toggle ON → 1 行に減ることを確認
- ✅ [iter63] **Today view の title click → ItemEditDialog** 配線 (Gantt iter31 と同パターンを Today にも展開)。`useQueryState('item')` で deep link 化、title を `<button>` 化して click → `?item=<id>`。Todoist 風: list item の title click で詳細編集に直接遷移
- ✅ [iter64] **Inbox view の title click** も同パターン展開: `inbox-title-<id>` button + nuqs deep link。これで Today / Inbox / Gantt / Archive で title click → 編集 dialog の UX が統一
- ✅ [iter65] **Backlog view の title cell も clickable** に: `<span>` を `<button onClick={onEdit}>` に変更、stopPropagation で DnD listener と分離、`data-testid="backlog-title-<id>"`。Today/Inbox/Gantt/Archive/Backlog の 5 view すべてで title click UX 統一
- ✅ [iter66] **Kanban card の title も clickable** に: `<div>{item.title}</div>` を `<button onClick={onEdit}>` 化、stopPropagation で dnd-kit drag listener と分離。これで全 6 view (Today/Inbox/Kanban/Backlog/Gantt/Archive) で title click → ItemEditDialog UX が完全統一
- ✅ [iter67] **Sprint 中止 button に確認 dialog**: archive button と同じく `window.confirm` で誤操作防止 (cancelled は revertable だが status 系の destructive 操作は確認するのが UX 的に標準)。`data-testid="sprint-cancel-<id>"` も付与。Playwright で dismiss=据え置き / accept=cancelled の両分岐を直接確認
- ✅ [iter68] decompose-proposals の **「全て却下」button に確認 dialog**: 提案が 2 件以上ある場合のみ `window.confirm("pending な提案 N 件をまとめて却下しますか?")` を挟む (1 件なら個別却下と等価なので確認なし)。AI が 5-10 件提案する典型ケースで誤クリック防止
- ✅ [iter69] dep-remove / notification-item の aria-label を具体化: dep-remove は `依存「<title>」を解除` (& title attr で hover tooltip)、notification-item は `<未読/既読>通知: <body>` を付与 (read 状態を SR にも明示。視覚は dot で示唆していたが aria-hidden だったため非視覚 user に伝わらなかった)
- ✅ [iter70] navigation 整合: workspace home に **Time Entries** link 追加 (PDCA / Templates と並ぶ)、`/archive` ページの pageActions に **← Workspace** back link 追加 (Sprint/Goals/PDCA/Templates/TimeEntries と同パターン)。これで全 6 サブページに統一された戻り動線
- ✅ [iter71] **Dashboard MUST item title click → ItemEditDialog**: title-click pattern を Dashboard view にも展開 (Today/Inbox/Kanban/Backlog/Gantt/Archive に続いて 7 箇所目)。`useQueryState('item')` + `dashboard-must-title-<id>` button。Playwright で deep link 反映を確認
- ✅ [iter72] **recharts `width(-1)/height(-1)` warning 解消** (Dashboard burndown chart): ResponsiveContainer の `height="100%"` と `minHeight={0}` を併用していたため race で 0-px で measure → 警告が出ていた。`height={256}` 数値固定 + minHeight prop 削除に変更、親 div の `h-64` も外して duplicated layout 制約を排除。Playwright で view 切替往復しても警告 0 件
- ✅ [iter73] **Gantt zoom (compact/normal/wide) select** (TeamGantt の day/week/month zoom 相当): `DAY_PX` 定数 → state-driven `dayPx`、ZOOM_PX={compact:24, normal:40, wide:64}、summary banner に `<select aria-label="Gantt の 1 日あたりの幅">` 追加。Playwright で 10-day item の bar 幅が compact=240px → normal=400px → wide=640px に追従することを確認
- ✅ [iter74] **Gantt zoom を URL に永続化** (nuqs `parseAsStringLiteral([...]).withDefault('normal')`): `?zoom=wide` が URL に反映され reload 後も復元される。Playwright で URL 反映 + reload 後 select value=wide を直接確認 (TeamGantt 風の "view setting persisted" UX)
- ✅ [iter75] **Gantt hideDone toggle も URL 永続化** (nuqs `parseAsBoolean.withDefault(false)`): `?hideDone=true` が反映 + reload 復元。useState を完全に外してすべての Gantt view 状態が URL ベース (zoom + hideDone + item)。共有 link で view setting を相手に渡せる
- ✅ [iter76] **PDCA period (30/90) も URL 永続化** (nuqs `parseAsInteger.withDefault(30)`): `?pdcaDays=90` が反映 + reload 後も 90 日 button が aria-pressed のまま。Gantt と同パターンで view setting を URL に集約
- ✅ [iter77] **BacklogView の dialog state を URL `?item=` に統合**: 旧 `useState<Item|null>(editing)` + 専用 `<ItemEditDialog>` を削除し、items-board 側の単一 dialog (URL 駆動) に集約。重複 rendering が無くなり、Backlog でも refresh / 共有 link で詳細 dialog が復元できる。Playwright で URL 反映 + dialog count=1 を直接確認
- ✅ [iter78] **KanbanView も同パターン**で URL 統合: 専用 dialog 削除、`useQueryState('item')` 経由に変更。Playwright で URL 反映 / dialog 1 個 / reload 後も dialog 1 個 を確認。Today/Inbox/Kanban/Backlog/Gantt/Archive/Dashboard の 7 view すべて URL 駆動 dialog に揃った
- ✅ [iter79] **Gantt bar 内 progress fill** (TeamGantt 風): in_progress=50% / todo=0% / done は opacity で表現。bar 内に半透明の黒 fill (`rgba(0,0,0,0.2)`) を `position:absolute inset-y-0 left-0 width:N%` で重ねる。tooltip にも `[進捗 50%]` を追記。Playwright で in_progress bar の fill 幅が 240→120px (50%) に正しくなることを直接確認
- ✅ [iter80] Dashboard StatCard の **tone を SR 語彙化**: 旧 visual-only だった warning/danger border を `aria-label` に "(注意)" / "(要対応)" として加筆。`role="group"` + `data-testid="stat-card-<tone>"` も付与。Playwright で 期限超過 item 投入 → danger card の aria-label に "要対応" 含まれることを確認
- ✅ [iter81] **/ (Workspace list) ページの a11y 補完**: section に aria-labelledby、ul に aria-label、各 Link に aria-label="<name> (slug: …, role: …) を開く"、装飾の "→" を aria-hidden 化。section heading id 紐付けで SR の領域認識が改善
- ✅ [iter314 (daily-summary)] **time_entries を日別に集計する pure helper (ai-automation, 1 commit, 1c306fa)**: iter307 並走 (`category-summary.ts`) で「カテゴリ別 × 期間内合計」を整備済、本 iter は **日別 × 合計分** の対称軸。AI 朝 brief / dashboard widget / sparkline が「直近 N 日の作業時間トレンド」を 1 関数で取り出せる substrate を新設。新 module `src/features/time-entry/daily-summary.ts`: `groupTimeEntriesByDay(entries, options?)` で `Map<workDate, totalMinutes>` (from/to inclusive 範囲、不正値 fail-soft、空文字 workDate 除外)、`dailyMinutesSeries(entries, options?, today?)` で `today - windowDays + 1` 〜 `today` の N 日連続列を古い順で返す (0 件日も `{date, minutes: 0}` で含むので sparkline / line chart がギャップなく描ける、windowDays default 7、≤0 / 不正 today は空、月跨ぎ / 年跨ぎも正しく連続)、`formatDailyMinutesSeries(series)` で `'4/27 5h / 4/26 1h / 4/25 4h'` 形式 (新しい順、M/D 年省略、0 分日省略、全 0/空 → `'0 件'`)、`totalDailyMinutes(map)` で合計分。iter307 export 済の `formatMinutes` を再利用、表記の一貫性 (`4h 30min` / `30min` / `2h`) を保つ。caller benefits: AI 朝 brief 「直近 7 日: 4/27 5h / 4/25 4h (合計 9h)」を 3 関数で、dashboard sparkline のデータ生成、windowDays / from / to で「今週 / 先月 / カスタム」を切替。test +21 件 (groupTimeEntriesByDay 5 / dailyMinutesSeries 7 / format 6 / totalDailyMinutes 2 / 統合 1 = 21/21 PASS)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305/306/307/309/310/313 で 12 件着地、本 iter314 で 13 件目。time-entry domain で本 session が 2 commit 目 (iter307 並走 category-summary に続いて) — parallel session の bias-\* 領域とは別 file で race リスクを最小化。次 iter315 = 65%5=0 → refactor。
- ✅ [iter500 refactor (member-capacity-format-集約-39)] **member-capacity の formatHours を canonical formatMinutes (lib/format-duration) に集約 (39 弾目, 1 commit, 262d6c6)**: iter360 集約方針 39 弾目 (前回 38 弾目 = iter495 rateToPct 集約)。iter490 で lib/format-duration.ts に formatMinutes を集約した時、member-capacity.ts の formatHours は '0h' sentinel (微妙に異なる挙動) があったため後回しにしていた。本 iter で挙動の違いを吸収して集約。修正: import の formatMinutes 追加、'formatHours' callsite 2 件 (capacityMinutes / Math.abs(remainingMinutes)) を 'formatMinutes' に置換、private function formatHours を削除。挙動変化: 0 minutes の表示が '0h' → '0min' (iter490 で予告した micro-behavior change)、テストは捉えていなかったケースなので既存 26/26 件 PASS。それ以外の出力 (1h, 4h, 8h, 1h 36min, 30min etc.) は完全に同一文字列。利点: format-duration.ts canonical 1 file、member-capacity 内の inline ゼロ / defensive 強化 (Math.max + Number.isFinite + Math.round)、不正値で UI 崩壊しなくなる。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊実質ゼロ (0 表示の microscopic 変化のみ)、pure helper のみ。これで src 配下の formatMinutes/formatHoursMinutes/formatHours 重複は完全 0 (= lib/format-duration.ts の canonical 1 件のみ)。次 iter501 → judge.sh の track 判定 (iter % 5 = 1 → basics) に進む。
- ✅ [iter499 ai-automation (most-concerning-role)] **agent-reliability に最も信頼性低い role 検出 helper 追加 (mostConcerningRole, 1 commit, 0559d9e)**: iter494 dominantRole (主軸 role = invocation 数最多) と対称な「弱点 role」 helper。pm-recovery worker / Slack alert / pre-flight が「どの agent が最も問題か」を 1 関数で取り出す substrate。agent-reliability.ts (+45 行): `ConcerningRoleResult { role, level, successRate }` / `mostConcerningRole(stats)` で 1 つでも 'critical' role があれば critical role を返す (緊急優先) / critical 不在で 'warn' role があれば warn role / 全て healthy / idle なら null / 同 level 複数は role alphabetical で pm 勝ち。設計判断: dominantRole (主軸、計画/期待される稼働量) と本 helper (弱点、失敗が多い/調査必要) は別軸 / critical を warn より絶対優先 (緊急優先) / 同 level 複数で alphabetical sort 安定 / null sentinel で全 healthy/idle 時は alarm 不要。caller benefits: pm-recovery worker で「critical が出たら即 enqueue」 / Slack alert payload で 'PM 要調査 (5/10 完了)' を冒頭に / pre-flight で「concerning role の同 type 呼び出しを抑制 / 別 model にfallback」判断。test +6 件 (33→39): 全 healthy → null / 全 idle → null / PM critical 検出 / Researcher warn 検出 / 両方 critical → pm 勝ち / critical を warn より優先 (PM warn + Researcher critical → Researcher 勝ち)。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 33 件 PASS)、pure helper のみ。次 iter500 → judge.sh の track 判定 (iter % 5 = 0 → refactor) に進む。
- ✅ [iter498 basics (cost-projection-compact-format)] **cost-month-projection に compact format helper 追加 (iter489 と対称, 1 commit, bc499d0)**: iter489 で agent-reliability に formatAgentReliabilityCompactJa を追加した時と同じパターン。dashboard widget の小型 chip / Slack 通知 ペイロード で詳細 (現在/残日数) を省く short 版が必要。cost-month-projection.ts (+25 行): `formatCostMonthProjectionCompactJa(p)` で idle → 'AI コスト: 不明 (today 不正)' / 制限なし → 'AI コスト: 今月予測 $1.20' / safe → 'AI コスト: 安全 $1.20 / $5.00' / warn → 'AI コスト: 警告 $4.50 / $5.00' / over → 'AI コスト: 超過予測 $6.00 / $5.00'。設計判断: emoji ⚠/🚨 は本 helper では使わず、tone は caller が iter482 costMonthProjectionChipClasses で chip 配色する想定 (= 文字側は素直なテキスト、視覚は chip 配色) / iter489 formatAgentReliabilityCompactJa と対称な短縮形式 (= AI brief / Slack 通知 で role 別 cost projection を 2 chip 並列表示する時に文字密度を揃える)。caller benefits: dashboard 小型 chip / Slack 通知 / AI 朝 brief 冒頭 1 行で「気にしなくて OK」or「警告ライン超過、来週の AI 呼び出しを抑制」判断。test +5 件 (18→23): idle / 制限なし / safe / warn / over の 5 状態網羅。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 18 件 PASS)、pure helper のみ。次 iter499 → judge.sh の track 判定 (iter % 5 = 4 → ai-automation) に進む。
- ✅ [iter497 ai-automation (agent-brief-compose)] **agent-reliability に compose helper 追加 (composeAgentBriefSignals = reliability + dominantRole 2 signal 集約, 1 commit, 66ce033)**: iter487/489/494 で agent-reliability の compute / format / dominantRole / format helper を個別に追加してきたが、AI 朝 brief / Slack daily digest / dashboard widget の caller が「stats から個別に reliability / dominant の 2 関数呼び」を毎回書く必要があった。本 iter で 1 関数で signals 配列が揃う compose helper を追加。agent-reliability.ts (+45 行): `AgentBriefSignal { text, tone }` / `AgentBriefSignals { reliability: Signal, dominantRole: Signal | null }` / `composeAgentBriefSignals(stats)` で reliability は必ず 1 signal (idle 時も '記録なし' chip 表示)、dominantRole は idle (totalInvocations=0) 時 null、それ以外は dominantRole result。設計判断: 主軸 role の tone は 'info' (中立、informational signal) を採用 (severity は reliability 側で持つ、主軸は「どの agent が動いているか」 alone で攻撃性を持たない情報) / signals 配列形式は AI brief / Slack 通知 / dashboard chip 共通 format で UI 側が signals.map で 1 行 render 可能 / dominant が null の時は signal 配列に入れない (caller の null check で chip 出し分け)。caller benefits: AI 朝 brief 1 関数で 2 signal 揃う / Slack attachment 配列に同 data shape を直接 push 可能 / dashboard で reliability + dominant の 2 chip を並列表示。test +3 件 (30→33): idle (reliability=記録なし / dominant=null) / healthy + 多 role dominant (2 signal、reliability tone=success / dominant tone=info) / critical + 唯一稼働 (reliability=danger / dominant 唯一稼働文言)。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 30 件 PASS)、pure helper のみ。次 iter498 → judge.sh の track 判定 (iter % 5 = 3 → basics) に進む。
- ✅ [iter496 basics (pick-highest-severity-tone)] **chip-tone に最悪 tone 抽出 helper 追加 (pickHighestSeverityTone, 1 commit, c3ebe71)**: graphical 波及シリーズ (iter481-495) で 5 domain で chip tone が揃ったが、caller が **複数 domain の tone を同時に持つ** (例: 'Item は dueProximity=warn かつ urgencyTier=danger') ような case で「カードの border tone はどれを採用するか」を一意に決めたい時、毎回 max 取得 logic を inline で書く必要があった。本 iter で chip-tone.ts に severity-aware max helper を追加。chip-tone.ts (+30 行): `pickHighestSeverityTone(tones)` で 空配列 → null sentinel (= caller は chip 非表示判断) / それ以外 → attention rank 最大 (= 最も危ない) tone を採用、同 rank 複数は配列順で最初を採用 (stable)。設計判断: severity が高い = '対応が必要' という意味、'success' (rank=0、positive 軸) と他 tone (severity 軸) が混在しても severity 軸が常に prioritize される (= 「危ない signal が success より優先」が UI 直感) / 比較は iter488 chipToneAttentionRank に委譲 (= rank vocabulary 統一) / null sentinel で空入力にも安全。caller benefits: Item card で border 1 色を決める / Sprint disclosure で「lane の代表 tone」を 1 chip で表示 / dashboard widget で「workspace 全体の AI alarm レベル」(reliability + cost projection の各 tone を pick)。test +5 件 (15→20): 空配列 null / 単一 / 複数 → max / success と severity 混在 → severity prioritize / 同 rank → stable first。typecheck 緑、lint 緑 (warning baseline 1 維持)、pure helper のみ。次 iter497 → judge.sh の track 判定 (iter % 5 = 2 → ai-automation) に進む。
- ✅ [iter495 refactor (rate-to-pct-集約-38)] **inline Math.round(x\*100) を rateToPct に集約 (38 弾目、agent feature 4 callsite, 1 commit, ecc595d)**: iter360 集約方針 38 弾目 (前回 37 弾目 = iter490 format-duration 集約)。iter385 で lib/format-rate.ts に集約済の `rateToPct` 関数 (rate 0..1 → pct integer 整数化) があるが、新規 file (= iter479/487/489/494 で追加した cost-month-projection / cost-monthly-trend / agent-reliability) は inline で Math.round(x _ 100) を書いていた。本 iter で 4 callsite を rateToPct に置換、agent feature 内の inline 重複ゼロに。修正対象: 1) agent-reliability.ts:177 (formatAgentReliabilityCompactJa) — stats.successRate _ 100 / 2) agent-reliability.ts:235 (formatAgentReliabilityJa の role pct) — r.successRate _ 100 / 3) agent-reliability.ts:213 (formatDominantRoleJa) — dominant.share _ 100 / 4) cost-monthly-trend.ts:122 (computeMonthlyCostTrend の deltaPct) — (deltaUsd/priorMonthUsd) _ 100。利点: magic number _ 100 が散在 → 1 helper 参照で grep 可能 / caller の意図 (= 'rate を pct 化') が関数名で明示 / 全 callsite が同じ rounding 動作 (= Math.round 0 桁)、将来 'banker rounding' 等に変える場合 1 file 編集で全 domain に波及。互換性: rateToPct(x) === Math.round(x \* 100) と完全に同値、出力数値ゼロ変化 / 既存 28+30 = 58 件テスト (cost-monthly-trend + agent-reliability) 全 PASS / typecheck / lint 緑 (warning baseline 1 維持)。memo: pdca-panel.tsx / goals-panel.tsx / budget-panel.tsx 等の component file にも同 inline pattern が残っているが、それらは UI props 変換の一環で文脈が異なる (component で他の clamp / format と並ぶ inline 計算)、本 iter は agent feature domain 内の集約のみで scope を限定。次 iter496 → judge.sh の track 判定 (iter % 5 = 1 → basics) に進む。
- ✅ [iter494 ai-automation (agent-reliability-dominant-role)] **agent-reliability に dominantRole + format helper 追加 (主軸 role 特定、AI brief 用, 1 commit, a8eb3c9)**: 期間内で **invocations 数が最も多い role** (= 主軸 role) を特定する pure helper。AI 朝 brief / dashboard widget で「今月は PM が主軸」「Researcher が主軸」のような 1 行 chip を出す substrate。agent-reliability.ts (+50 行): `DominantRoleResult { role, invocations, share (0..1) }` / `dominantRole(stats)` で全 idle → null sentinel、それ以外は最大 invocations の role を返す、tie (同数) は role alphabetical で pm 勝ち / `formatDominantRoleJa(dominant)` で null → '主軸: 記録なし' / share=1 (唯一稼働) → '主軸: PM (10 呼出、唯一稼働)' / それ以外 → '主軸: PM (15 呼出、65%)'。設計判断: invocations 数ベース (= 「実行回数 = 主軸」の自然解釈)、cost ベースの主軸は caller が別途実装 (本 helper は invocation 数のみ) / null sentinel は全 idle 時 (= caller は 'null → chip 非表示' で UI 静か) / tie は alphabetical 安定 (pm > researcher)。caller benefits: AI 朝 brief で 'AI: 主軸 PM (15 呼出、65%) / 信頼性 注意 (20/22 完了、91%)' / dashboard widget の 1 行 overview chip / Slack 通知 'AI 状況: PM 主軸活動中、Researcher 不稼働 — config 確認?'。test +8 件 (22→30): dominantRole 5 件 (全 idle / PM 主軸 / Researcher 主軸 / 唯一稼働 share=1 / tie → pm 勝ち) / formatDominantRoleJa 3 件 (null sentinel / 唯一稼働 / 混合 % 表示)。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 22 件 PASS)、pure helper のみ。次 iter495 → judge.sh の track 判定 (iter % 5 = 0 → refactor) に進む。
- ✅ [iter493 basics (chip-tone-items-aggregator)] **chip-tone に items 集計 + 1 行整形 helper 追加 (countItemsByTone + formatToneCountsJa, 1 commit, 743aa9d)**: graphical 波及シリーズ (iter481-492) で ChipTone vocab + 各 domain tone helper が揃ったが、caller が「workspace 全 items の tone 分布」「sprint 内 items の tone 別件数」のような **集計** をしたい時、毎回 reduce + format を inline で書く必要があった。本 iter で chip-tone.ts に generic な集計 + 整形 helper を追加。chip-tone.ts (+50 行): `countItemsByTone(items, getTone)` で 6 tone 全部 0 で初期化された Record を返す (= caller は undefined check 不要) / `formatToneCountsJa(counts)` で全 0 → '0 件' / 混合 → '緊急 3 / 要対応 1 / 達成 12' 形式 (0 件 tone は省略、順序は iter488 chipToneAttentionRank と整合する 危ない順 = danger → urgent → warn → info → idle → success)。設計判断: getTone は callback で caller の domain 固有 tone helper を渡せる (= dueProximityTone / urgencyTierTone / memberCapacityTone / agentReliabilityTone を直接 inject 可能、generic と domain の架け橋) / Record<ChipTone, number> で 6 tone 全 key 必ず存在 (caller の Record アクセスの noUncheckedIndexedAccess 罠回避) / 順序は iter488 attention rank と完全整合 (= 危ない順 sort と表示順が一致)。caller benefits: AI 朝 brief で 'workspace 全体: 緊急 3 / 要対応 5 / 注意 8 / 達成 12' / dashboard widget の overview chip / Slack 通知 'チームメンバー余裕時間: overloaded 2 / tight 1 / comfortable 3 / free 4'。test +5 件 (10→15): countItemsByTone 2 件 (空 / 混合) / formatToneCountsJa 3 件 (全 0 sentinel / 混合 0 件省略 / 全 6 tone 順序確認)。typecheck 緑、lint 緑 (warning baseline 1 維持)、pure helper のみ (React/DB 依存ゼロ)。次 iter494 → judge.sh の track 判定 (iter % 5 = 4 → ai-automation) に進む。
- ✅ [iter492 ai-automation (agent-reliability-public-labels)] **agent-reliability の role / level ラベルを public export 化 (1 commit, ed5285b)**: iter487/489 で agent-reliability.ts に追加した formatAgentReliabilityJa / formatAgentReliabilityCompactJa は内部で ROLE_LABEL_JA / LEVEL_LABEL_JA を使って 1 行文言を組み立てているが、caller が **個別組合せ** (Slack attachment per-role 別、dashboard widget の hover tooltip、AI brief の独自テンプレート) で文言を組み立てたい時、ラベルだけ欲しいケースが出てくる。本 iter で ROLE_LABEL_JA / LEVEL_LABEL_JA の public 関数 export を追加。agent-reliability.ts (+25 行): `agentRoleLabelJa(role)` で pm→'PM' / researcher→'Researcher' / `reliabilityLevelLabelJa(level)` で healthy→'健全' / warn→'注意' / critical→'要調査' / idle→'記録なし'。設計判断: iter491 chipToneLabelJa との対比 — chipToneLabelJa は tone (視覚配色軸) のラベル ('緊急'/'達成' 等)、本 helper は domain 固有 (信頼性 level / role 名) のラベル、caller は 2 つを組み合わせて 'PM 健全 (達成)' のような richer 文言を作れる / 短縮ラベル維持 (= UI 共通 vocabulary) で formatAgentReliabilityJa の中身と整合。caller benefits: Slack attachment.title に 'PM 健全' 1 文字句、attachment.text に詳細を分けたい / dashboard widget の hover tooltip で role × level クロス表 / AI 朝 brief の独自フォーマット ('【PM】 健全 (15/15) ...')。test +3 件 (19 元 + 3 新 = 22/22 PASS): role 2 件 / level 4 件 / caller pattern 'role + level の組み合わせ' (PM 健全 等)。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 19 件 PASS、内部 const → 関数 export 追加のみで動作変更なし)、pure helper のみ。次 iter493 → judge.sh の track 判定 (iter % 5 = 3 → basics) に進む。
- ✅ [iter491 basics (chip-tone-label-ja)] **chip-tone に ja-JP 1 単語ラベル helper 追加 (SR aria-label / Slack / AI brief 修飾語, 1 commit, 527891f)**: graphical 波及シリーズ (iter481-490) で ChipTone vocab が 6 段階で確立。caller は SR aria-label / Slack 通知 ペイロード / AI brief 等で「(緊急対応)」「(注意)」「(達成)」のような **修飾語** を付与したい時、tone から直接ラベルを取れる helper があると caller の switch 文を排除できる。本 iter で chipToneLabelJa(tone) を追加。chip-tone.ts (+30 行): `chipToneLabelJa(tone)` で danger→'緊急'/urgent→'要対応'/warn→'注意'/info→'通常'/idle→'対象外'/success→'達成' の 6 tone × 1 label map。設計判断: domain 固有ラベル (urgencyTier の '緊急/高/中/低/対象外'、dueProximity の '期限切れ/今日/明日/今週内/今後/未設定') と衝突しないよう本 helper は「tone (= 視覚配色軸)」のラベル、caller は domain ラベルと組み合わせて使う想定 (例: '期限切れ (緊急)' / '今日 (要対応)' / '今週内 (通常)')。caller benefits: SR aria-label に「(緊急)」等の修飾語を追加で意味を補強 (= severity 軸が SR ユーザにも明確に伝わる) / AI brief で「上位 urgent: タイトル A (緊急)」のように tone label 同梱 / Slack 通知 attachment.text の冒頭 emoji の代わりに 'AI 信頼性: 健全 (達成)' のような ja-JP 修飾語。test +2 件 (8→10): 6 tone × 1 label の map / caller pattern 「domain label + tone label の組み合わせ」 例。typecheck 緑、lint 緑 (warning baseline 1 維持)、pure helper のみ。次 iter492 → judge.sh の track 判定 (iter % 5 = 2 → ai-automation) に進む。
- ✅ [iter490 refactor (format-duration-集約-37)] **formatMinutes/formatHoursMinutes 重複を lib/format-duration.ts に集約 (37 弾目、3 file 重複ゼロ, 1 commit, 77c7d7a)**: iter360 集約方針 37 弾目 (前回 36 弾目 = iter485 chip-tone 集約)。「分単位の duration を Hh Mmin 形式に整形する pure helper」が 3 file に同 logic で散らばっていた (time-entry/category-summary.ts の defensive 版 + sprint/swimlane-conflict.ts の defensive なし版 + sprint/swimlane-orchestrator.ts の同上)。本 iter で `lib/format-duration.ts` に集約、各 caller は本 helper に委譲。修正対象: 1) 新 file `src/lib/format-duration.ts` (canonical 定義、defensive 強化版、formatMinutes(min) で 0/負/NaN/Infinity → '0min' fail-soft、60 未満→'Mmin'、60 倍数→'Hh'、混合→'Hh Mmin'、小数は Math.round) / 2) features/time-entry/category-summary.ts (既存 formatMinutes を lib/format-duration から re-export して既存 caller の import path 維持 = breaking change ゼロ) / 3) features/sprint/swimlane-conflict.ts (同 logic の formatHoursMinutes 削除、lib/format-duration の formatMinutes に置換) / 4) features/sprint/swimlane-orchestrator.ts (同上 = swimlane-conflict と完全に identical だった重複 helper を共通化)。利点: 同 logic の関数定義が 3 file → 1 file / defensive guard の有無が unified (sprint domain も Math.round + Math.max + isFinite で fail-soft 化、不正値で UI 崩壊しなくなる) / iter479 以降の cost-\* 系で formatUsd / formatMinutes の散在パターンを統一する流れと整合。互換性: formatMinutes 既存 24 unit test (category-summary.test.ts 経由) はそのまま PASS、import path 維持 / swimlane-conflict / swimlane-orchestrator の test (16/11 件) は同じ output 文字列を期待しているので format 結果は完全一致 (defensive 強化は既存 test の不正値 path を踏まないため挙動変化ゼロ)。test +5 件 (lib/format-duration.test.ts 新規): 60 未満/60 倍数/混合 の基本 3 件 / 小数 round (89.4→1h 29min) / 不正値 (-100/NaN/Infinity/-Infinity) → '0min' fail-soft。typecheck 緑、lint 緑 (warning baseline 1 維持)、75/75 PASS (lib/format-duration 5 + category-summary 24 + swimlane-conflict 16 + swimlane-orchestrator 11 + 関連 19)、pure helper のみ。member-capacity.ts の formatHours は 0/負値で '0h' sentinel (微妙に異なる挙動) なので本 iter では集約せず維持 (将来別 iter で慎重に検討)。次 iter491 → judge.sh の track 判定 (iter % 5 = 1 → basics) に進む。
- ✅ [iter489 ai-automation (agent-reliability-compact-format)] **agent-reliability に compact 1 行 ja-JP 整形 helper 追加 (1 commit, 19528c6)**: iter487 で `formatAgentReliabilityJa` (役割別詳細 1 行) を追加したが、dashboard widget の小型 chip / Slack 通知 ペイロード / AI brief の冒頭 1 行で「AI 全体は健全か?」を一目で判断するには、role 別詳細を省いた **compact 版** が必要。本 iter で対称な helper を追加。agent-reliability.ts (+25 行): `formatAgentReliabilityCompactJa(stats)` で idle → 'AI 信頼性: 記録なし' (formatAgentReliabilityJa と同 sentinel) / else → 'AI 信頼性: 健全 (22/23 完了、95%)' / 'AI 信頼性: 注意 (20/22 完了、91%)' のような全体集約のみの compact 文言。役割別の詳細は省く (= caller が disclosure で `formatAgentReliabilityJa` を出すパターン想定、3 層情報設計: chip = compact / hover/expand = role 別詳細 / page = full breakdown)、iter479 cost-month-projection の冒頭 emoji (⚠/🚨) は本 helper では使わず、tone は caller が `agentReliabilityChipClasses` で chip 配色する想定。caller benefits: Dashboard 小型 chip '健全 (22/23 完了、95%)' を 1 行で / Slack 通知 attachment.text に critical 時の冒頭 1 行 alert / AI 朝 brief 冒頭 1 行 'AI 信頼性: 健全' で「気にしなくて OK」 / 'AI 信頼性: 要調査 (6/10 完了、60%)' で「pm-recovery worker が頻繁に失敗、要調査」。iter487 (compute + role 別 format) → iter489 (compact 全体 format) で agent-reliability substrate scope A 完成、UI bind / Slack worker 配線は別 iter。test +4 件 (15 元 + 4 新 = 19/19 PASS): idle 記録なし / healthy 23/23 → '健全 (23/23 完了、100%)' / warn 20/22 → '注意 (20/22 完了、91%)' (role 別詳細含まれない確認) / critical 6/10 → '要調査 (6/10 完了、60%)'。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 15 件 PASS)、pure helper のみ。次 iter490 → judge.sh の track 判定 (iter % 5 = 0 → refactor) に進む。
- ✅ [iter488 basics (chip-tone-attention-rank)] **chip-tone に attention rank + sort comparator 追加 (危ない順 sort 用 substrate, 1 commit, 50538ae)**: graphical 波及シリーズ (iter481-487) で 5 domain (dueProximity / cost-month-projection / urgency-tier / member-capacity / agent-reliability) が同 ChipTone vocab を使うようになった結果、caller で「最も危ない順」「success を最後に」という sort が頻出する見込み。本 iter で chip-tone.ts に sort 用の attention rank + comparator を追加。新 export (chip-tone.ts +35 行): `chipToneAttentionRank(tone)` → 数値 (danger=5 / urgent=4 / warn=3 / info=2 / idle=1 / success=0、= 危ない順、success は positive 軸でも sort では最下位 = 「対応不要」最後尾) / `compareChipTones(a, b)` → Array.sort 用 comparator (b - a で降順 = 危ない順 head)、stable sort で同 rank は元順保持。設計判断: severity 軸 (danger..idle) は単調順、success は positive 軸で「視覚的に静か」だが「対応不要」の意味で最低ランク = 「要対応 sort」の一貫した最下位 / Array.sort は ES2019+ 規約で stable なので caller は同 tone の元順を信頼可能。caller benefits: `items.sort((a, b) => compareChipTones(toneOf(a), toneOf(b)))` で「危ない member 上」「critical task head」が 1 行 / iter469 sortTeamCapacityLoadsByUrgency 等 既存の domain 固有 sort 関数も将来本 helper 経由でリファクタ可能 (= severity vocabulary 統一)。test +3 件 (5→8): chipToneAttentionRank 1 件 (6 tone → 6 rank の map) / compareChipTones 2 件 (mixed sort で danger>urgent>warn>info>idle>success 順 / 同 tone は元順保持 stable)。typecheck 緑、lint 緑 (warning baseline 1 維持)、pure helper のみ。次 iter489 → judge.sh の track 判定 (iter % 5 = 4 → ai-automation) に進む。
- ✅ [iter487 ai-automation (agent-reliability)] **AI agent 信頼性 (success rate / failure rate) 集計 pure helper + role 別 + graphical chip tone (1 commit, b7a0a50)**: cost-aggregate.ts の MonthlyCostRow には invocations/completed/failed が含まれているが、AI brief / dashboard widget で「PM agent: 14/15 完了 (93%) / Researcher: 8/8 完了 (100%) — 健全」のような **役割別 信頼性 chip** を出すには caller 側で reduce/divide/format を毎回書く必要があった。本 iter で 1 関数集約。新 module src/features/agent/agent-reliability.ts (+150 行): `computeAgentReliability(rows, roles?)` で役割別 sum + 全体 sum 計算、`reliabilityLevel` 4 段階 ('idle' invocations=0 / 'healthy' rate>=0.95 / 'warn' 0.80<=rate<0.95 / 'critical' rate<0.80)、不正値 (負/NaN) は 0 クランプ、未知 role は除外 (forward compatible) / `formatAgentReliabilityJa(stats)` で 'AI 信頼性: PM 14/15 (93%) 注意・Researcher 8/8 (100%) 健全' のような 1 行 ja-JP 文言 / graphical 波及: `agentReliabilityTone(level)` → ChipTone (critical→danger / warn→warn / healthy→success / idle→idle、iter486 で追加した 'success' tone を **2 件目活用**) + `agentReliabilityChipClasses(level)` で Tailwind 3 軸 class (iter485 集約済 getChipToneClasses 委譲)。caller benefits: pre-mortem / pm-recovery / AI 朝 brief / dashboard widget で 1 関数で agent 健全性 chip 完成 / Slack 通知ペイロードで critical 時のみトリガ / pm-agent / researcher-agent の pre-flight で「最近 1 ヶ月失敗多めなので 1 invocation 抑制」判断。test +15 件 (compute 7 + format 4 + tone 1 + classes 3 = 15/15 PASS): 空 rows→idle / PM 14/15 warn / 19/20 healthy / 6/10 critical / 複数月 sum / 未知 role 除外 / 不正値クランプ / total reliability role 別 sum 判定 / format idle/healthy/warn/critical 4 状態 / tone 4 level / classes critical+healthy+warn+idle。typecheck 緑、lint 緑 (warning baseline 1 維持)、pure helper のみ (React/DB 依存ゼロ、UI/worker 配線は別 iter)。次 iter488 → judge.sh の track 判定 (iter % 5 = 3 → basics) に進む。
- ✅ [iter486 basics (success-tone + member-capacity-bind)] **chip-tone vocab に 'success' tone (emerald) 追加 + member-capacity に tone helper bind (1 commit, 4071cc7)**: iter485 で集約した `lib/ui/chip-tone.ts` の 5 段階 vocabulary は **severity 軸の単調順** だが、member-capacity の 'free' (時間に余裕) は positive 軸 = 「達成 / 余裕 / 健全」を表現したく、severity 軸では idle (静か) にしか map できない。本 iter で **6 段階目の 'success' tone (emerald)** を追加、severity 軸と直交する positive 軸を導入。これで member-capacity の 5 status (overloaded/tight/comfortable/free/unknown) を 1:1 で「赤 / 橙強 / 青 / 緑 / 灰」に map できる graphical 波及完成。chip-tone.ts (+10 行): `ChipTone` を 5 → 6 段階に拡張、'success' 追加、TONE_CLASSES に 'success' エントリ (bg-emerald-50 / text-emerald-700 / ring-emerald-200)、説明 comment で「severity 軸 + positive 軸の直交」を明示。member-capacity.ts (+50 行): `MemberCapacityTone = 'danger'|'urgent'|'info'|'success'|'idle'` (5 段階、'warn' は不使用 = capacity model に「2 段目 amber 薄」 category なし) / `memberCapacityTone(status)` で overloaded→'danger' (rose) / tight→'urgent' (amber 強) / comfortable→'info' (blue 薄) / **free→'success' (emerald) ← iter486 で追加 tone を初使用** / unknown→'idle' (slate 薄) / `MemberCapacityChipClasses = ChipToneClasses` (alias) / `memberCapacityChipClasses(status)` で iter485 集約済 `getChipToneClasses` に委譲。設計判断: 'success' を severity 軸の延長線でなく **直交軸として導入** (= idle より「下」ではなく、idle と並列の positive 信号)、caller 側は意味で使い分け (severity 軸: danger/urgent/warn/info/idle、positive 軸: success) / これで「成功 / 健全」を含む domain (member-capacity / sprint-progress / goal achievement / subtask done) が同じ vocab で chip 配色可能 / 'success' を 'idle' と区別する class 値: idle は slate 薄 (静か)、success は emerald 薄 (肯定的に静か)、両方 ring-200 で同 weight、caller で使い分け明示。iter481-486 graphical 波及シリーズ実績: iter481 (dueProximity 6 kind → 5 tone) / iter482 (cost 4 risk → 4 tone) / iter483 (urgency 5 tier → 5 tone) / iter485 (chip-tone.ts 集約 36 弾目) / iter486 (success tone 追加 + member-capacity 5 status → 5 tone success 初登場)。caller benefits: TeamCapacityPanel UI bind 時に inline 配色 (rose-700/amber-700/blue-700/emerald-700/muted) を `memberCapacityChipClasses` 1 関数で取り出せる / AI 朝 brief / SR aria-label に `memberCapacityTone(status)` で「(余裕あり)」等の修飾語を含められる / 将来 domain (sprint-progress / goal achievement / subtask done) でも 'success' tone が再利用可能、Tailwind class 重複は永久に発生しない。test +6 件 (chip-tone +1 / member-capacity +5)。typecheck 緑、lint 緑 (warning baseline 1 維持)、pure helper のみ (React 依存ゼロ、UI 配線は別 iter)。次 iter487 → judge.sh の track 判定 (iter % 5 = 2 → ai-automation) に進む。
- ✅ [iter485 refactor (chip-tone-集約-36)] **5 段階 chip tone class table を lib/ui/chip-tone.ts に集約 (3 module 重複ゼロ、iter360 集約方針 36 弾目, 1 commit, 5ed476a)**: iter481 (dueProximity) / iter482 (cost-month-projection) / iter483 (urgency-tier) で 3 module に同 shape の `TONE_CLASSES` map (5 段階 × Tailwind 3 軸) が散らばっていた。全部「赤=danger / 橙強=urgent / 橙薄=warn / 青=info / 灰=idle」の 5 段階で Tailwind class 値も完全に同じ。本 iter で 「ChipTone vocabulary + class map」を `src/lib/ui/chip-tone.ts` に集約、各 caller 側は domain 固有の `kind → tone` mapping だけを保持して `getChipToneClasses(tone)` に委譲する形に refactor。class 値の string 重複が 3 module → 1 module に。新 module `src/lib/ui/chip-tone.ts` (+95 行): `ChipTone='danger'|'urgent'|'warn'|'info'|'idle'` (5 段階共通 vocab) / `ChipToneClasses{bgClass,textClass,ringClass}` (Tailwind 3 軸) / `getChipToneClasses(tone)` で 5 tone × 3 軸 class map を返す。refactor 対象 3 module: due-proximity.ts (inline 削除 + getChipToneClasses(TONE_MAP[kind]) 委譲、`DueProximityChipClasses` を `ChipToneClasses` type alias に) / cost-month-projection.ts (4 段階モデルなので **重要**: local 'warn' tone を共通 vocab の 'urgent' tone に map = chip 配色は強 amber bg-amber-100 で同じ、ローカル名だけ 'warn'、新 const `RISK_TO_SHARED_CHIP_TONE` で risk→ChipTone を明示マップ、`costMonthProjectionTone` は local vocab 維持で後方互換) / urgency.ts (inline 削除 + getChipToneClasses(TIER_TONE_MAP[tier]) 委譲)。利点: magic Tailwind class string が 3 module → 1 module、書き換え時の漏れリスク 0 / chip 配色 ('rose-100 vs rose-200' 等) を本当に変えたくなった時 `chip-tone.ts` 1 file 編集で全 domain (期限近接 / コスト予測 / 緊急度) に波及 / 共通 vocab 'ChipTone' を export することで将来新 domain (status-visual / member-capacity / sprint-progress 等) を追加する時に `kind → ChipTone` の mapping だけ書けば chip 配色完成、Tailwind class 重複は永久に発生しない / 既存 `lib/ui/trend-tone.ts` (trend chip = polarity 切替で配色反転) と並ぶ「graphical chip 共通 substrate」軸が確立。互換性: 既存 `dueProximityChipClasses` / `costMonthProjectionChipClasses` / `urgencyTierChipClasses` の戻り値 shape (bgClass/textClass/ringClass) と class string 値は完全一致 (= 既存テスト全 passing、breaking change ゼロ) / 既存 `DueProximityChipClasses` / `CostMonthProjectionChipClasses` / `UrgencyTierChipClasses` 型は `ChipToneClasses` の type alias で import path 維持。test +5 件 (chip-tone.test.ts 新規): 5 tone 全部に対して bgClass=^bg-/ textClass=^text-/ ringClass=^ring-/ 等 / danger → rose 系 / urgent (強amber) と warn (薄amber) で強弱区別 / info → blue、idle → slate。typecheck 緑、lint 緑 (warning baseline 1 維持)、既存 124 件 (due-proximity 32 + urgency 75 + cost-month-projection 18 - architecture は別) はそのまま PASS、合計 129/129 PASS、pure helper のみ (React 依存ゼロ、UI bind は別 iter)。次に集約検討の散在は status-visual 系 / member-capacity 系 (= 別 scheme tone なので careful)。次 iter486 → judge.sh の track 判定 (iter % 5 = 1 → basics) に進む。
- ✅ [iter484 ai-automation (cost-trend-by-role)] **role 別 (PM/Researcher) AI コスト trend helper 追加 (1 commit, 914da9f)**: iter332 で導入した `computeMonthlyCostTrend` (workspace 全体集計) は 1 個 trend を返すが、AI brief / dashboard widget で「PM agent は増加 / Researcher agent は安定」のような **role 別 trend を同時表示** したい時 caller 側で partition + 2 回呼びの boilerplate が必要だった。本 iter で role partition + computeMonthlyCostTrend を 1 関数に集約。新 export (cost-monthly-trend.ts +73 行): `AgentRole = 'pm'|'researcher'` / `MonthlyCostTrendByRole = Record<AgentRole, MonthlyCostTrend>` / `computeMonthlyCostTrendByRole(rows, today)` で rows を role 別に partition (`MonthlyCostRow` の structural subset 受付、cost-aggregate.ts 戻り値直渡し可) + 各 role の {month, costUsd} を `computeMonthlyCostTrend` に渡して trend 計算 + 'pm'/'researcher' 両方が必ず key として存在 (= caller は undefined check 不要)、entry 0 件 role は idle trend、未知 role ('engineer' 等) / 不正 month skip / `formatMonthlyCostTrendByRoleJa(trends)` で全 role idle → 'AI コスト: 先月今月とも記録なし'、それ以外 → 'AI コスト: PM 増加・Researcher 安定' のような役割別 1 行 (中黒で連結、direction → 短ラベル up→'増加'/down→'減少'/flat→'安定'/idle→'記録なし')。設計判断: Record<AgentRole, MonthlyCostTrend> で全 role を必ず key 化 (caller の undefined check / Record アクセスの noUncheckedIndexedAccess 罠回避) / 役割名は 'PM'/'Researcher' (= iter319/iter332 系の cost dashboard chip / agent role badge と同じ vocabulary)、ja-JP 文言生成のみ短ラベル化 / formatMonthlyCostTrendByRoleJa は値の詳細 ($0.50→$0.80) を省く圧縮形式 (詳細値は既存 formatMonthlyCostTrendJa で出す、本 helper は「全 role 1 行 overview chip」用) / 未知 role を skip するのは forward-compatible (将来 'engineer'/'reviewer' agent role が増えた時に本 helper は静かに無視、breaking change しない) / structural subset 受付で `MonthlyCostRow[]` (cost-aggregate.ts) を直接渡せる、別 type 不要。caller benefits: AI 朝 brief 1 行 chip 「AI コスト: PM 増加・Researcher 安定」を埋め込み / Dashboard widget 2 chip 並列表示 (iter479/482 cost-month-projection graphical tone と組み合わせ可能、role 別に同 tone helper 適用) / Slack 通知 ペイロード role 別 trend → 各 role に独立 attachment / color。iter479 (compute + format month projection) → iter482 (tone + classes 4 段階) → iter484 (role 別 trend) で AI コスト substrate scope A 完成。test +7 件 (21 元 + 7 新 = 28/28 PASS): computeMonthlyCostTrendByRole 3 件 (PM 増加 + Researcher 安定 / role 不在は idle で埋まる / 未知 role + 不正 month skip) / formatMonthlyCostTrendByRoleJa 4 件 (全 idle / PM 増加 + Researcher 安定 / PM のみ entry / PM 減少 + Researcher 減少)。typecheck 緑、lint 緑 (warning baseline 1 維持)、互換性破壊ゼロ (既存 21 件 PASS)、pure helper のみ (React 依存ゼロ、UI 配線は別 iter)。次 iter485 → judge.sh の track 判定 (iter % 5 = 0 → refactor) に進む。
- ✅ [iter483 basics (urgency-tier-tone)] **urgency tier に graphical chip tone helper 追加 (5 段階 tone fully utilize, 1 commit, 32f5b68)**: queue 「もっとグラフィカル / シンプル / 意味のあるデザイン」シリーズ波及 3 連弾の 3 弾目。iter481 (dueProximity 6 kind → 5 tone のうち 4 使用) / iter482 (cost-projection 4 risk → 4 tone のうち 4 使用) で確立した 5 段階 tone vocabulary (danger/urgent/warn/info/idle) を urgency-tier に bind、**5 tier (critical/high/medium/low/none) を 5 tone に full mapping** (= 'urgent' tone が遂に意味のある利用先を見つけた)。新 export (urgency.ts +73 行): `UrgencyTierTone = 'danger'|'urgent'|'warn'|'info'|'idle'` (iter481 と整合) / `urgencyTierTone(tier)` で critical→'danger' (rose 緊急、要即時対応) / high→'urgent' (amber 強、行動喚起) / medium→'warn' (amber 薄、注意) / low→'info' (blue 薄、計画範囲内) / none→'idle' (slate 薄、対象外、UI 静か) / `UrgencyTierChipClasses { bgClass, textClass, ringClass }` (Tailwind 3 軸) / `urgencyTierChipClasses(tier)` で 5 tone × 3 軸 class map (iter481/482 と完全に同 class 値)。設計判断: urgency-tier は 5 段階で 5 tone と完全一致 ('urgent' が遂に意味のある利用先を見つけた) / dueProximity は 6 kind を 5 tone に圧縮 (later=noDate=idle)、costProjection は 4 risk を 4 tone に (urgent 不使用)、urgency-tier は 1:1 / urgency-tier と dueProximity を chip で並べ表示しても意味のある区別: dueProximity は dueDate のみ見るが urgency は priority + dueDate + MUST + 経過時間を全部加味、「期限切れだが priority 4 で MUST じゃない」 item は dueProximity=danger / urgencyTier=medium 等のズレがありうる / 既存 urgencyTierCountsSeverity (severe/warn/idle) は集計レベルの severity で本 iter の per-tier tone とは別軸で共存。caller benefits: dashboard-view.tsx の urgencyTiers chip が abstract chipTone3Class を使っているが、本 helper で個別 critical / high の class が直接取れる / AI 朝 brief / SR aria-label に urgencyTierTone(tier) で「(緊急対応)」等の修飾語を含められる / queue 「graphical 波及」シリーズで listed 候補 (Today/Inbox view, Backlog table, Kanban カード, MUST badge) で urgency-tier 別 chip を統一配色で。3 連弾実績: iter481 (dueProximity 6→5) → iter482 (cost-projection 4→4) → iter483 (urgency-tier 5→5)、5 tone vocabulary が 3 domain に広がり、app 全体での「赤=危険 / 橙強=行動喚起 / 橙薄=注意 / 青=計画 / 灰=余裕・対象外」が一貫してきた。次の波及候補: status-visual (異 scheme なので独立維持か tone 互換 layer 追加検討)、subtask-status、member-capacity-load、sprint-progress。test +3 件 (72 元 + 3 新 = 75/75 PASS): urgencyTierTone 1 件 (5 tier → 5 tone の map) / urgencyTierChipClasses 3 件 (critical rose 3 軸 / high vs medium amber 強弱 / low blue + none slate)。typecheck 緑、lint 緑 (warning baseline 1 維持)。次 iter484 → judge.sh の track 判定 (iter % 5 = 4 → ai-automation) に進む。
- ✅ [iter482 ai-automation (cost-projection-tone)] **cost-month-projection に graphical chip tone helper 追加 (iter481 dueProximity と整合 4 段階, 1 commit, 468e8cf)**: queue 「もっとグラフィカル / シンプル / 意味のあるデザイン」シリーズ波及。iter479 で導入した cost-month-projection.ts に、UI で「赤=月末超過 / 橙=警告ライン超過 / 青=安全 / 灰=計算不能」を 1 関数で取り出せる tone token + Tailwind class helper を追加。新 export (cost-month-projection.ts +60 行): `CostMonthProjectionTone = 'danger'|'warn'|'info'|'idle'` (4 段階、iter481 5 段階のうち 'urgent' を除く) / `costMonthProjectionTone(risk)` で over→'danger' / warn→'warn' / safe→'info' / idle→'idle' / `costMonthProjectionChipClasses(risk)` で 4 tone × 3 軸 class map (iter481 dueProximity と同色 = rose-100/700/300、amber-100/800/300、blue-50/700/200、slate-50/600/200)。設計判断: iter481 で確立した 5 段階 tone のうち 'urgent' は使わない (= cost projection には「行動喚起の最強」はなく、warn の上は直接 over)、これで dueProximity と costProjection で「赤 / 橙 / 青 / 灰」が app 全体で一貫 / safe を info-blue (bg-blue-50) にしたのは「安全 = 心理的に静かに肯定」、emerald (緑) にすると「達成 = 完了」と混ざる (= MVP cost projection は走行中予測なので「達成」ではない、blue 薄 = 「順調」が適切) / module 内に同居 (= cost projection に密結合、別 module 切り出し不要)。caller benefits: AI コスト dashboard widget で `<div className={`${c.bgClass} ${c.textClass} ring-1 ${c.ringClass}`}>` 1 行で chip 配色完成 / Slack 通知 ペイロード生成で costMonthProjectionTone(risk) → emoji + color 選択 (Slack attachments の color に rose=#dc2626 等を rule-based で map) / pm-agent / researcher-agent の pre-flight で「危険」状態を 1 関数で判定 → AI 呼び出しを抑制する分岐。iter479 (compute + format) → iter482 (tone + classes) で cost-month-projection substrate scope A 完成、UI bind は別 iter。test +4 件 (14 元 + 4 新 = 18/18 PASS): costMonthProjectionTone 1 件 (4 risk → 4 tone の map) / costMonthProjectionChipClasses 3 件 (over rose 3 軸 / warn vs safe amber と blue 区別 / idle slate 計算不能 fallback)。typecheck 緑、lint 緑 (warning baseline 1 維持)。iter481 dueProximityChipClasses と class 値が完全に整合 (= 共通 tone module に切り出すのは将来検討、今 iter は scope A 集約のみ)、pure helper のみ (React 依存ゼロ、UI 配線は別 iter)。次 iter483 → judge.sh の track 判定 (iter % 5 = 3 → basics) に進む。
- ✅ [iter481 basics (due-proximity-tone)] **dueProximity に graphical chip tone helper 追加 (5 段階 token + Tailwind 3 軸 class, 1 commit, 3c2033b)**: queue 「もっとグラフィカル / シンプル / 意味のあるデザイン」シリーズ波及。iter287 で導入した `getDueProximity` (期限近接 6 kind) に、UI で「赤=遅延 / 橙=今日 / 薄橙=明日 / 青=今週内 / 灰=今後・未設定」を 1 関数で取り出せる tone token helper を追加。subtask-status / status-visual / member-capacity と同じ「装飾でなく意味のある graphical」パターン。新 export (due-proximity.ts +75 行): `DueProximityTone = 'danger'|'urgent'|'warn'|'info'|'idle'` (5 段階) / `dueProximityTone(kind)` で overdue→'danger' (rose 強警戒) / today→'urgent' (amber 行動喚起) / tomorrow→'warn' (amber-soft 注意) / thisWeek→'info' (blue 計画範囲内) / later+noDate→'idle' (slate 計画余裕) / `DueProximityChipClasses { bgClass, textClass, ringClass }` (Tailwind 3 軸) / `dueProximityChipClasses(kind)` で 5 tone × 3 軸 class map (bg-rose-100 / text-rose-700 / ring-rose-300 等)。設計判断: 5 段階 tone は「graphical 波及」シリーズ全体で共通語彙として使う想定 — 今後 tag-status / urgency-tier / sprint-progress / etc. も同じ tone token に統一していけば「赤=危険、橙=今日、薄橙=注意、青=計画、灰=余裕」が app 全体で意味のある配色になる / tomorrow を urgent でなく warn にしたのは「今日と明日の閾値」を視覚で区別したいため (= 今日は強行動喚起、明日は preview 注意の 2 段) / thisWeek (今週内 2-6 日先) は info-blue で「予定範囲内、計画通り」 / noDate (期限未設定) は idle で隠れる級の弱め (= 期限を設定する CTA は別 chip 「期限なし」専用 helper で、本 helper は色 token のみ)、ring class は focus-visible / chip border 用、既存 chip pattern と整合。caller benefits: Today / Backlog / Kanban / Item card / item-summary-panel で `dueProximityChipClasses(kind)` 1 行で chip の bg/text/ring が決まる / AI brief / SR aria-label に `dueProximityTone(kind)` で「(緊急対応)」等の修飾語を含められる / queue 「graphical 波及」シリーズで listed 候補 (Today/Inbox view status badge, Backlog table status 列, Kanban カード, Goal/Sprint progress bar) を逐次本 helper で配色統一可能 (UI bind は別 iter)。test +8 件 (24 元 + 8 新 = 32/32 PASS): dueProximityTone 3 件 (overdue→danger / today+tomorrow→urgent+warn / thisWeek+later+noDate→info+idle+idle) / dueProximityChipClasses 3 件 (overdue rose 3 軸 / today vs tomorrow amber 強弱 / thisWeek blue + noDate slate)。typecheck 緑、lint 緑 (warning baseline 1 維持)、architecture 5/5 PASS、pure helper のみ (React 依存ゼロ、UI 配線は別 iter)。次 iter482 → judge.sh の track 判定 (iter % 5 = 2 → ai-automation) に進む。
- ✅ [iter480 refactor (ms-per-day-集約-35)] **inline `(24*60*60*1000)` を MS_PER_DAY 集約 (iter360 集約方針 35 弾目、7 file 5 箇所一掃, 1 commit, d6020d4)**: iter360 集約方針シリーズ 35 弾目 (前回 34 弾目 = iter471 swimlane-conflict / swimlane で import 化)。`lib/date/iso.ts` の道 comment 「`MS_PER_DAY` / `DAY_MS` の同一定義 5 件 + inline `(24 * 60 * 60 * 1000)` 8 件」 が iter255 当時の状況、本 iter で **src 配下の inline 残存 5 箇所を 0 に** (残るは `lib/date/iso.ts` 自体の `export const MS_PER_DAY = 24 * 60 * 60 * 1000` の 1 箇所のみ = 唯一の定義箇所、当然)。修正対象 7 file 編集 5 inline 撤去: pdca/service.ts:116 (lead time)、agent/cron-tz.ts:26 (DEFAULT_FIRST_RUN_LOOKBACK_MS)、sprint/premortem-service.ts:267 (totalDays)、sprint/retro-worker.ts:76 (cutoff)、sprint/sprint-date-helpers.ts:85 (daysBetween)、item/date-tokens.ts:168 (formatFriendlyDate)、heartbeat/service.ts:51 (daysUntilDue)。各 file 共通: `@/lib/date/iso` import に MS_PER_DAY 追加 (cron-tz.ts / sprint-date-helpers.ts / date-tokens.ts は import 自体が無かったので新規追加)、inline `(24*60*60*1000)` を `MS_PER_DAY` に置換。利点: magic number が散在 → 1 定義参照で grep 可能、書き換え漏れリスク 0 / `MS_PER_DAY` という名前で意図が読みやすくなる (`/(24*60*60*1000)` は計算式) / 異なる地点での typo (0 が 1 つ少ない、桁ミス) リスク排除 / iter360 集約方針継続。動作変更ゼロ (`MS_PER_DAY = 24*60*60*1000` 同値)、各 caller の test は緑のまま (sprint-date-helpers 33/33 / cron-tz 17/17 / date-tokens 80/80 / architecture 5/5 = 130/130 PASS)、typecheck 緑、lint 緑 (warning baseline 1 維持)。cloud env で env DATABASE_URL 必須の file (pdca/premortem/heartbeat/retro-worker) は import resolve のみで動作確認、実テストは local supabase で確認するべき (本変更は inline 定数の置換のみでロジック変更ゼロなので typecheck で十分間接保証)。これで `(24 * 60 * 60 * 1000)` が src/ 配下の lib/date/iso.ts 以外で 0 件、新規 file が同 inline を書いても architecture test 等で検出可能な状況に近づいた (将来 grep ベースの規約 test を追加検討、本 iter は scope A 集約のみ)。次 iter481 → judge.sh の track 判定 (iter % 5 = 1 → basics) に進む。
- ✅ [iter479 ai-automation (cost-month-projection)] **AI コスト 月末予測 pure helper (線形外挿で警告ライン / limit 超過判定, 1 commit, 5f65b41)**: Phase 6.9 `getBudgetStatus` (cost-budget.ts) は **月初からの累積 spent** と limit を取得して「現時点で 80%」「現時点で超過」を判定するが、月初から数日で 80% 到達した場合の「このペースでいくと月末いくらに着地するか」は inline で extrapolate していて、AI 朝 brief / dashboard widget / pm-agent が 1 関数で取り出せなかった。iter332 `cost-monthly-trend.ts` (this vs prior month) と対称な「月末着地予測」 substrate を新設。新 module `src/features/agent/cost-month-projection.ts` (+135 行): `CostMonthProjection { thisMonthUsd, projectedUsd, limitUsd, daysElapsed, daysInMonth, daysRemaining, warnTriggered, exceedsLimit, riskLevel }` / `computeCostMonthProjection({thisMonthUsd, today, monthlyLimitUsd?, warnThresholdRatio?})` で `Date.UTC(year, month, 0).getUTCDate()` で daysInMonth を UTC 計算 (28/29/30/31 自動、うるう年対応、TZ shift 無関係)、daysElapsed = day-of-month を daysInMonth で safety clamp、projectedUsd = `(thisMonthUsd/daysElapsed)*daysInMonth` 線形外挿、riskLevel は limit==null='safe' / projected<limit*warn='safe' / limit*warn≤projected<limit='warn'+warnTriggered / projected≥limit='over'+warnTriggered+exceedsLimit、warnThresholdRatio default 0.8 (workspace_settings 既存値と整合)、0..1 clamp、不正 today → idle (projection なし)、thisMonthUsd<=0/NaN → 0 にクランプ + projection=0、`monthlyLimitUsd<=0` も「制限なし」扱い (= null) で Number.isFinite で Infinity 除外。`formatCostMonthProjectionJa(p)` で chip / SR aria-label / Slack ペイロード用 1 行: idle='AI コスト予測: 不明 (today 不正)' / 制限なし='今月予測 $1.20 (現在 $0.40 / 残 20 日)' / safe='今月予測 $1.20 / 上限 $5.00 (現在 $0.40 / 残 20 日)' / warn='⚠ 今月予測 $4.50 / 上限 $5.00 — 警告ライン超過見込み (現在 $1.50 / 残 20 日)' / over='🚨 今月予測 $6.00 / 上限 $5.00 — 月末超過見込み (現在 $2.00 / 残 20 日)'。`formatUsd` private helper で <1 USD は 3 桁、1 USD 以上は 2 桁の小数表示。設計判断: iter332 cost-monthly-trend (this vs prior 月次比較) と独立 module (機能軸が前月比較 vs 月末予測で異なる、cost-budget.ts の上に載る薄い「予測」 layer)、線形外挿の限界は doc comment で明示 (月初 1〜3 日は誤差大、月末手前は実 spent と等価、AI invocation の頻度集中は projection が低めに出る) → 「予測精度低」 chip 判定は別 helper、daysRemaining は今日含めない残り日数 (UI で「残 N 日」と書いた時の自然解釈)、emoji ⚠/🚨 は意味のあるグラフィカル (queue 「graphical 波及」シリーズと整合)。caller benefits: AI 朝 brief 1 行で chip 埋込 / Slack 通知 payload を riskLevel='warn'/'over' でトリガ / Dashboard widget の amber/rose chip + 詳細 disclosure / pm-agent / researcher-agent の pre-flight が 1 関数で「来週分の AI 呼び出しを抑制」判断。test +14 件 (compute 9 + format 5 = 14/14 PASS): 月初 10 日で $1 → $3/safe / $1.5→$4.5/warn / $2→$6/over / limit=null=safe 固定 / today 不正=idle / NaN/-値=0 クランプ / 月末当日 daysRemaining=0 / うるう年 2024-02 daysInMonth=29 / warnThresholdRatio=0.5 早めに warn / format 5 状態網羅。typecheck/lint 緑 (warning baseline 1 維持)、architecture 5/5 PASS。POST_MVP 機能 (dashboard widget UI bind / Slack 通知 worker) は本 iter で実装せず、pure helper substrate のみで closed (UI bind / 通知 worker は別 P0 entry が up された時に着手)。次 iter480 → judge.sh の track 判定 (iter % 5 = 0 → refactor) に進む。
- ✅ [iter478 P0 (gantt-bar-drag-ui-bind)] **Gantt bar 中央 drag で期間 平行 shift = Gantt DnD scope A UI bind 完了 (queue: Gantt DnD 期間編集 scope A 完了, 1 commit, 140f6e3)**: iter467 で着地した pure helper `computeBarDragShift` を `gantt-view.tsx` の非 milestone bar に PointerEvent + setPointerCapture で配線、ユーザが期間 (startDate / dueDate) を直接 drag で動かせる scope A 最小実装 = FEEDBACK_QUEUE P0 entry 4 「Gantt DnD 期間編集」 scope A vertical slice 完了 (substrate 1 iter + UI bind 1 commit = 計 2 commit)。同時に drag 中の視覚 ghost 用 pure helper `computeSnappedDragPx(deltaPx, dayPx)` (`Math.round(deltaPx / dayPx) * dayPx` を NaN / dayPx<=0 fail-soft で wrap、+3 test = 17/17 PASS) を `bar-drag.ts` に追加。実装 (gantt-view.tsx +130/-2 行): workspaceId を Omit から外し `useUpdateItem(workspaceId ?? '')` (workspaceId 未指定時は drag 無効 = read-only Gantt) / drag state useState<{barId, startX, deltaPx, moved}>、4px threshold 越えで moved=true / onPointerDown は setPointerCapture + e.stopPropagation で row 伝播抑止 (左 click + 非 touch のみ反応) / onPointerMove は clientX 差分で deltaPx update / onPointerUp は moved=false なら click 扱い (既存 onClick 経路で setOpenItemId 起動) / moved=true なら suppressNextClickRef に barId set + computeBarDragShift で日数計算 + useUpdateItem.mutateAsync({patch: {startDate, dueDate}}) call / onPointerCancel で state リセット (browser interrupt 対策) / onBarClick は suppressNextClickRef で drag 完了直後の click を preventDefault + stopPropagation で抑止、else setOpenItemId / 視覚 ghost: drag 中 `transform: translateX(${computeSnappedDragPx}px)` で snap-to-day プレビュー + opacity 0.85 + cursor grabbing + transition off / aria-label に「(ドラッグで期間移動)」追記 / ConflictError 時 toast「別の編集と競合」、その他失敗時 isAppError message or 「期間の更新に失敗」 (revert は invalidateQueries 自動)。仮置き判断 (FEEDBACK_QUEUE entry 4 仕様): bar 中央 drag = 期間平行 shift のみ (左右 edge / 依存連動 / 連鎖 transitive closure shift は scope B 以降) / @dnd-kit Sortable は再順序用途で bar drag (= 連続 px 追跡) には合わないため使わず native PointerEvent + setPointerCapture 採用 (simpler, more reliable for absolute-positioned elements) / milestone (spanDays===1) は drag 無し (1 day width で UX 不可、scope B 以降で別 UX) / optimistic 更新は scope A 未実装 (TanStack Query invalidate で代替、scope B で onMutate 楽観適用 + onError revert) / touch 入力は scope A 除外 (iOS/Android 誤動作回避)。UX 卓越 a-g 該当部: a 発見可能性 (cursor=grab で hover 時 drag 可能を明示、aria-label 末尾「(ドラッグで期間移動)」、tooltip「— ドラッグで期間移動」) / b アクセシビリティ (既存 onKeyDown Enter/Space で dialog 起動完全保持、drag は pointer 専用、touch type 除外) / c 状態網羅 (idle=cursor:grab / dragging=cursor:grabbing+opacity:0.85+data-dragging=true / drop success=toast +N 日 シフト / conflict=toast 競合 / cancel=state null reset の 5 状態) / d 速度感 (snap to day を transform translateX で即時表示 = mutation 完了前から視覚フィードバック、4px threshold で意図しない drag 抑止) / g 一貫性 (Lucide icon 不使用 = cursor 切替で表現、shadcn / sonner toast / TanStack Query / 既存 click→dialog UX とそのまま整合)。これで FEEDBACK_QUEUE 🔥 P0 5 件 全 entry scope A 完了 (1/Template, 2/案件サマリ, 3/チームメンバー余裕時間, 4/Gantt DnD, 5/Sprint swim-lane)、P0 section は (空) に復帰。typecheck/lint 緑 (warning baseline 1 維持)、bar-drag 17/17 PASS、gantt 全 4 file 計 58/58 PASS、architecture 5/5 PASS。**cloud env では dev server / Playwright 起動不可** (= 実 UI でのドラッグ操作確認は未実施、pure helper 14 test + typecheck で間接保証、threshold / cursor / toast 文言の微調整余地あり)。次 iter479 → judge.sh の track 判定 (iter % 5 = 4 → ai-automation) に復帰。
- ✅ [iter477 P0 (team-capacity-panel-ui-bind)] **TeamCapacityPanel = チームメンバー余裕時間 一覧 UI bind 完成 (queue: チームメンバー余裕時間 一覧 scope A 完了, 1 commit, 0097358)**: iter465/466/469/476 の substrate (computeMemberCapacityLoad + formatMemberCapacityLoadJa + capacity-period + computeTeamCapacityLoads + sortTeamCapacityLoadsByUrgency + useWorkspaceItemAssignees) を 1 component で接続、workspace home に member 別 今日/今週 capacity load 一覧 panel を mount。新 component `src/components/workspace/team-capacity-panel.tsx` (+170 行): native `<details>`/`<summary>` で disclosure (= JS 不要 / SR / keyboard 完全対応、shadcn 非依存)、開いた時のみ useItems / useWorkspaceMembers / useWorkspaceStatuses / useWorkspaceItemAssignees を fetch (lazy で server 負荷軽減)、useMemo で iter469 computeTeamCapacityLoads + sortTeamCapacityLoadsByUrgency 計算、render は 1 row = 1 member の grid (sm:3 col、member 名 + 今日 chip + 今週 chip)。UI: summary は Users icon + 'チームメンバー 余裕時間 (今日 / 今週)' / row tone は overloaded=rose-700 / tight=amber-700 / comfortable=blue-700 / free=emerald-700 / unknown=muted-foreground / 危ない member 上 sort / 今日 chip + 今週 chip は formatMemberCapacityLoadJa 直結 / aria-label SR 全文 / data-load-today/week E2E hook / empty 'member が登録されていません'。これで FEEDBACK_QUEUE P0 entry **5 件中 4 件 (1 Template, 2 案件サマリ, 3 チームメンバー余裕時間, 5 Sprint swim-lane) の scope A 完了**、残 P0 は entry 4 (Gantt DnD UI bind 待ち) の 1 件のみ。typecheck/lint 緑 (warning baseline 1 維持)、33/33 PASS (architecture 5 + member-capacity 17 + team-capacity 11)。**cloud env では dev server / Playwright で実 UI 確認不可** (= 実環境テストは未実施、typecheck と pure helper test で間接保証)。次 iter478 → 残 P0 entry 4 (Gantt DnD) UI bind か別 P0 が空になり次第 track 判定 (iter478 % 5 = 3 → basics) に復帰。
- ✅ [iter476 P0 (workspace-bulk-assignees)] **workspace 全 items の assignees を bulk fetch する API + hook (queue: チームメンバー余裕時間 一覧 scope A continued, 1 commit, 1ddfc4f)**: iter474 (sprint 配下 bulk fetch) の workspace 全体版、member-capacity (P0 entry 3) UI bind の N+1 query 回避用。新 export `repository.listAssigneesForWorkspace(tx, workspaceId)` (workspaceId filter + deleted_at IS NULL の inner join 1 query、RLS で見えない items は結果から落ちる) / `service.listAssigneesForWorkspace(workspaceId)` (viewer 権限 + iter474 と同じ groupAssigneesByItemId pure helper を共通化、grouping 重複なし) / `listWorkspaceItemAssigneesAction(workspaceId)` Result wrap / `useWorkspaceItemAssignees(workspaceId)` TanStack Query hook (queryKey に 'workspace-assignees' + workspaceId)。caller benefits: member-capacity panel UI bind が `useWorkspaceItemAssignees(workspaceId)` 1 行で iter469 `computeTeamCapacityLoads` の getAssignees callback 完成 / 他 workspace 横断系 (AI 朝 brief「member 別 today load」、dashboard widget) で再利用可 / sprint=20 + N items でも 1 round-trip。architecture 5/5 PASS (iter474 と同じく既存 mutation の failure path test が規約満たすため新規 service test 不要、grouping は iter474 で分離した pure helper 再利用)。typecheck/lint 緑 (warning baseline 1 維持)、11/11 PASS (group-assignees 6 + architecture 5)。次 iter477 → workspace home に MemberCapacityPanel UI 配線 (本 hook + iter469 computeTeamCapacityLoads + iter465 formatMemberCapacityLoadJa を 1 commit で disclosure / panel に bind = P0 entry 3 scope A 完了見込み)。
- ✅ [iter475 P0 (sprint-swimlane-ui-bind)] **SprintCard に「担当者ビュー」 disclosure UI bind = swim-lane Gantt scope A 完了 (queue: Sprint 担当者 swim-lane Gantt scope A 完了, 1 commit, 77bc549)**: iter468/470/471/472/473/474 で揃った 9 helper + bulk fetch hook を 1 component で接続、SprintCard に「担当者ビュー (swim-lane Gantt)」 disclosure を mount、scope A vertical slice 完成 (substrate 6 iter + UI bind 1 commit = 計 7 commit)。新 component `src/components/workspace/sprint-swimlane-disclosure.tsx` (+170 行): native `<details>`/`<summary>` で disclosure (= shadcn Collapsible 非依存、SR / keyboard 完全対応、JS 不要で開閉)、開いた時のみ `useItems` + `useSprintItemAssignees` + `useWorkspaceMembers` を fetch (lazy で server 負荷軽減)、useMemo で iter472 `computeSprintSwimlane` → 各 lane の bar 配列 + iter473 population summary を計算、render は member ごとに 1 行 lane (member 名 + load chip + 横長 bar 配置)。UI 構成: 上端 population summary chip 「3 lane / 12 件 / 80h / 重複 2 lane (3 ペア)・未割当 1 lane」 (formatSprintSwimlanePopulationJa 直結) / lane header member name (workspaceMembers.displayName 逆引き、fallback `actorType:actorId 短縮`) + lane load chip (formatLaneLoadJa 直結、重複あり時 amber-700 警告色) / bar `<span>` absolute left%/width% で blue-500/70 fill、conflicted lane の bar は amber-500/70 + ring で alert / empty state '/この sprint に割当 item がありません' で早期 return / loading 共通 `Loading`。UX 卓越 a-g: a 発見可能性 (Users icon + 'border-t' で SprintCard 末尾区切り) / b アクセシビリティ (native `<details>`、role=group + aria-label「Sprint「N」 担当者 swim-lane」、bar 用 role=img + aria-label 件数、各 bar の title に「title (start〜end / 重複あり)」) / c 状態網羅 (Loading / Empty / 通常 / 重複 lane 警告 / 未割当 lane) / d 速度感 (lazy fetch + useMemo + 1 round-trip) / e 細部 (bar の min-width 0.5%、conflicted bar は ring で hover せずに認識) / f レスポンシブ (% base) / g 一貫性 (shadcn Card / Lucide / amber-700 warning)。これで FEEDBACK_QUEUE P0 entry **5 件中 3 件 (1 Template, 2 案件サマリ, 5 Sprint swim-lane) の scope A 完了**、残 P0 は 3 (チームメンバー余裕時間 UI bind 待ち) + 4 (Gantt DnD UI bind 待ち) の 2 件。typecheck/lint 緑 (warning baseline 1 維持)、35/35 PASS (architecture 5/5 + swim-lane 系 30/30、premortem/retro 失敗は cloud env で SUPABASE_SERVICE_ROLE_KEY 不在のため pre-existing)。**cloud env では dev server / Playwright で実 UI 確認不可** (= 実環境テストは未実施、typecheck と pure helper test で間接保証、ユーザ実機確認時に微調整予期)。次 iter476 → 残 P0 entry 3 (チームメンバー余裕時間) UI bind か 4 (Gantt DnD) UI bind。
- ✅ [iter474 P0 (sprint-bulk-assignees)] **sprint 配下 items の assignees を bulk fetch する API + hook (queue: Sprint 担当者 swim-lane Gantt scope A continued, 1 commit, 3ba5efd)**: iter472/473 (orchestrator + population summary) の上に UI bind 用の N+1 query 回避 bulk fetch API を整備。Sprint swim-lane disclosure (= 次 iter SprintCard UI bind) で getAssignees callback を 1 query で揃える pipeline 完成。新 export: `repository.listAssigneesForSprintItems(tx, sprintId)` (itemAssignees + inner join items の 1 query、RLS で見えない items は結果から落ちる、deleted_at IS NULL filter) / `service.listAssigneesForSprintItems(workspaceId, sprintId)` (viewer 権限 + RLS 経由 + groupAssigneesByItemId pure helper で post-process) / `listSprintItemAssigneesAction({workspaceId, sprintId})` Result wrap / `useSprintItemAssignees(workspaceId, sprintId | null)` TanStack Query hook (sprintId null/undefined で disabled、queryKey に workspaceId + sprintId)。新 pure helper `src/features/item/group-assignees.ts` (+24 行): `groupAssigneesByItemId(rows)` で itemAssignees row → `Record<itemId, AssigneeRef[]>` (push 順序保持、空 → 空 record、duplicate row は caller 責任で de-dup)。test deterministic 化のため service と分離。test +6 件 (空 / 1 item 1 / 1 item 複数 / 複数 item / 重複 row 保持 / itemId 空文字も key として尊重 = 6/6 PASS)。caller benefits: Sprint swim-lane UI bind が `const {data: assigneesByItem} = useSprintItemAssignees(...)` 1 行で getAssignees callback 完成 / member-capacity (P0 entry 3 残) も同 hook で workspace 横断 N items 分の assignees を 1 query で取得可 / N+1 query 回避で sprint=20 item の場合 round-trip 95% 削減。architecture 5/5 PASS (service.ts は既存 mutation の failure path test が architecture 規約満たしているため新規 service test 不要、grouping は pure helper に分離して test deterministic)。typecheck/lint 緑 (warning baseline 1 維持)。次 iter475 → SprintCard UI bind (本 hook + iter472/473 orchestrator + population summary を 1 commit で disclosure に bind 完成)。
- ✅ [iter473 P0 (sprint-swimlane-population)] **swim-lane sprint-level population summary helper (queue: Sprint 担当者 swim-lane Gantt scope A continued, 1 commit, 4411200)**: iter472 orchestrator (`computeSprintSwimlane`) 続編で row 配列を sprint 全体で 1 行集約する `summarizeSprintSwimlanePopulation` + `formatSprintSwimlanePopulationJa`。SprintCard disclosure header に「3 lane / 12 件 / 80h / 重複 2 lane (3 ペア)・未割当 1 lane」 chip を 1 関数で表示するための substrate (= UI bind は format 1 呼び出しで chip 文言完成、iter471 `summarizeLaneLoad` の sprint level 版)。新 export `SprintSwimlanePopulation { laneCount, unassignedLaneCount, totalItemCount, uniqueItemCount, estimateMinutesTotal, conflictedLaneCount, conflictPairTotal }` で row[] を集約 (1 item を 2 assignee に持つと totalItemCount=2 / uniqueItemCount=1 で重複除外、conflictedLaneCount は重複ペア 1 件以上を持つ lane 数)。整形仕様: 空 → '0 lane / 0 件' / 通常 → '3 lane / 12 件 / 80h' / 延べ != unique → '3 lane / 12 件 (延べ 14) / 80h' / 重複あり → 末尾 ' / 重複 N lane (M ペア)' 付加 / 未割当 lane → 末尾 '・未割当 N lane' 付加 (' / ' と別 separator で UI 注意喚起) / 見積 0 → '見積なし' / 60min 未満 → 'Nmin' / 60min 以上 → 'Hh' or 'Hh Mmin'。caller benefits: chip 1 関数完成 / aria-label SR 全文 / AI brief 「sprint 全体: 12 件 / 80h、田中さん 1 lane 重複 2 件」共通 / 重複 lane O(1) 検出 (alert chip 出し分け)。test +13 件 (summarize 5 + format 8 = 24/24 PASS、4 file 計 68/68 PASS)。typecheck/lint 緑 (warning baseline 1 維持)。これで Sprint swim-lane substrate は **9 helper** 揃い (`computeSwimlaneBarPosition` / `groupItemsByAssigneeKey` / `detectLaneConflicts` / `collectConflictedItemIds` / `formatLaneConflictsJa` / `summarizeLaneLoad` / `formatLaneLoadJa` / `computeSprintSwimlane` orchestrator / `summarizeSprintSwimlanePopulation` + `formatSprintSwimlanePopulationJa`)、UI bind は 1 commit 見込み。次 iter474 → SprintCard UI bind か残 P0 entry 3/4 (member-capacity / Gantt DnD UI bind) のどれかへ、context 連続性で entry #5 内の UI bind を優先。
- ✅ [iter472 P0 (sprint-swimlane-orchestrator)] **swim-lane substrate 7 helper を 1 関数にまとめる orchestrator (queue: Sprint 担当者 swim-lane Gantt scope A continued, 1 commit, 398f3ed)**: iter468 / 470 / 471 で揃った 7 helper (computeSwimlaneBarPosition / groupItemsByAssigneeKey / detectLaneConflicts / collectConflictedItemIds / formatLaneConflictsJa / summarizeLaneLoad / formatLaneLoadJa) を 1 関数 `computeSprintSwimlane` で連結。iter469 `computeTeamCapacityLoads` (member-capacity team 横断 orchestrator) と同 pattern。新 module `src/features/sprint/swimlane-orchestrator.ts` (+103 行): `SprintSwimlaneRow<T>{laneKey, assigneeRef, items[], conflicts, loadSummary, loadSummaryJa, conflictsJa}` を return、`SprintSwimlaneItem<T>{item, position, conflicted}` で UI bar 1 件分の render data を 1 構造体に集約。仕様: sprint 期間と全く重ならない item は row 内 items から除外 (lane 自体は loadSummary に算入で「未割当ではないが sprint 外」も把握可) / 1 item 複数 assignee は各 lane に重複 entry / 未割当 (assignee ゼロ) は null laneKey row で最後に並ぶ / lane sort は actorType:actorId 昇順 → null lane 最後 / lane 内 items は visibleStart 昇順 (同点 id 昇順) で安定 sort / estimateMinutesByItemId 未指定は空 Map と同等 (loadSummaryJa は '見積なし')。LaneItemSource は `string|null|undefined` を許容するので computeSwimlaneBarPosition への bridge で `?? null` 正規化。`parseAssigneeKey` で `${actorType}:${actorId}` を最初の `:` で 2 分割 (actorId 内 `:` は actorId 側に保持、UUID 等含み可)。caller benefits: SprintCard disclosure render は `rows.map(row => <Lane …>)` のみ / bar 位置は `row.items[i].position.{leftPct,widthPct,clippedAtStart,clippedAtEnd}` / 重複 highlight は `row.items[i].conflicted` で O(1) / lane header chip は `row.loadSummaryJa` / `row.conflictsJa` を直接表示。次 iter以降 SprintCard 内に「担当者ビュー」 disclosure を 1 commit で追加可能 (本 helper 1 呼び出し + map 描画)。test +11 件 (空 / 単一 lane / sprint 範囲外 / 複数 lane sort / 未割当 last / 1 item 2 assignee / 重複 conflicted flag / lane 内 sort / 見積なし / clip 跨ぎ / actorType 違い / actorId に `:` 含む key parse = 11/11 PASS、4 file 計 55/55 PASS)。typecheck/lint 緑 (warning baseline 1 維持)、architecture 5/5 PASS。次 iter473 → SprintCard UI bind (本 orchestrator 1 呼び出し + map 描画) or 残 P0 entry 3/4 (member-capacity UI bind / Gantt DnD UI bind) のどれかへ。
- ✅ [iter471 P0 (lane-load-summary)] **swim-lane lane load summary helper + MS_PER_DAY 集約 (queue: Sprint 担当者 swim-lane Gantt scope A continued, 1 commit, a1825bf)**: iter470 続編。lane header chip 用の load summary helper 追加 + MS_PER_DAY 重複定義を `lib/date/iso` 集約 (iter360 集約方針 34 弾目)。swimlane-conflict.ts (+60/-2 行): `summarizeLaneLoad(items, estimateMinutesByItemId)` で lane の件数 / 見積合計 / 重複ペア数 / 重複合計日数 を 1 関数 (estimate は caller 事前計算 Map 渡し、AI 見積 / time_entries 実績への切替を caller で選択可) / `formatLaneLoadJa(summary)` で '5 件 / 8h / 重複なし' / '3 件 / 4h 30min / 重複 2 ペア (合計 4 日)' / '0 件 / 見積なし / 重複なし' 等。caller benefits: SprintCard disclosure 各 lane header に 1 行配置で chip 完成 / AI brief 「田中 lane: 5 件 / 8h / 重複 2 ペア」共通 logic / estimate 取得 logic 外注で拡張容易。defensive: estimateMinutes 不正値 (NaN/負/0) 集計除外 / 空 lane 全 0 / 重複日数 両端 inclusive。test +8 件 (summarize 4 + format 4 = 24/24 PASS)。typecheck/lint 緑 (warning baseline 1)。これで Sprint swim-lane substrate は 7 helper 揃い (computeSwimlaneBarPosition / groupItemsByAssigneeKey / detectLaneConflicts / collectConflictedItemIds / formatLaneConflictsJa / summarizeLaneLoad / formatLaneLoadJa)、UI bind は 1 commit 見込み。次 iter472 → 本 session 最終 iter として finalize 候補。
- ✅ [iter470 P0 (swimlane-conflict)] **swim-lane 内 時間重複検出 pure helper (queue: Sprint 担当者 swim-lane Gantt scope A continued, 1 commit, a840c91)**: iter468 swim-lane substrate 続編。同 lane 内 (= 同 assignee) で時間重複する bar を検出する pure helper、UI で「conflict 警告」 chip / red border 等を出す substrate。新 module `src/features/sprint/swimlane-conflict.ts` (+95 行): `detectLaneConflicts(items) → LaneConflictPair[]` で 同 lane 内 bar 集合の時間重複を全ペア検出 (a.id<b.id 安定 sort、各ペア 1 件、両端 inclusive 日数 = overlapDays、O(N²) で N=100 想定) / `collectConflictedItemIds(pairs) → Set<string>` で UI 逆引き index / `formatLaneConflictsJa(pairs)` で chip 用 ja-JP ('時間重複なし' / '時間重複: N ペア (合計 M 日)')。defensive: startDate/dueDate 片方 null 除外 / 不正 ISO 除外 / 不正範囲 (due < start) 除外 / 境界 1 日 (a.due===b.start) は overlapDays=1 検出 (両端 inclusive 仕様)。caller benefits: swim-lane UI で `groupItemsByAssigneeKey` 各 lane → `detectLaneConflicts` → format で chip 表示 / collect で bar render の「conflict bar?」 O(1) 判定 / AI brief 「田中さん 5/2 重複 2 件」 1 行。これで Sprint swim-lane substrate は 5 helper 揃い (computeSwimlaneBarPosition + groupItemsByAssigneeKey + detectLaneConflicts + collect + format)、UI bind は 1 commit で実現可能見込み。test +16 件 (16/16 PASS)。typecheck/lint 緑 (warning baseline 1)。本 push は parallel session `11e4cc9` (playwright-iter383) 先取で rebase 経由着地。次 iter471 → SprintCard disclosure UI bind or refactor。
- ✅ [iter469 P0 (team-capacity)] **team 全 member の今日/今週 capacity load orchestrator (queue: チームメンバー余裕時間 一覧 scope A substrate 終端, 1 commit, a77bc56)**: iter465 (computeMemberCapacityLoad) + iter466 (capacity-period) を上位連結する orchestrator、次 iter で workspace home の MemberCapacityPanel が本 helper 1 関数呼び出すだけで全 row を render 可。新 module `src/features/workspace/team-capacity.ts` (+108 行): `computeTeamCapacityLoads({members, items, getAssignees, today, capacityPerDayMinutes?, doneStatusKeys})` で 各 member について getDayRange / getIsoWeekRange で items を filter → 自分が assignee の Item のみ pickup → `computeMemberCapacityLoad` で today/week load を計算 (capacityPerDay default 480 8h、week=7 日分)。`sortTeamCapacityLoadsByUrgency(rows)` で overloaded > tight > comfortable > free > unknown 順、同 status 内 displayName ?? actorId 昇順 (UI で「危ない member 上」)。defensive: members 0 / items 0 / 範囲外除外 / 別 member 除外 / 1 item 複数 assignee 両方算入 / done 除外 / actorType 違いの同 id 別 member。caller benefits: workspace home で `useWorkspaceMembers` + `useItems` + memo した getAssignees lambda 渡すだけで render data 揃う / formatMemberCapacityLoadJa を chip 適用するだけで文言完成 / 具象 type で iter465/466 と整合。test +11 件 (空 / 0 items / 240 分 / 範囲外 / 別 member / 複数 assignee / done / 過負荷 / actorType + sort 5 status / 同 status 昇順 = 11/11 PASS)。typecheck/lint 緑 (warning baseline 1)。本 push は parallel session が `369ed64` (playwright-iter382) 先取で rebase 経由着地。これで P0 entry 3 substrate 3 段 (member-capacity / capacity-period / team-capacity) 全完了、UI bind は次 iter 以降 1 関数呼び出しで実現可。次 iter470 → 残 P0 entry の UI bind か refactor 割り込み。
- ✅ [iter468 P0 (sprint-swimlane)] **swimlane bar 位置 % + assignee group の pure helper (queue: Sprint 担当者 swim-lane Gantt scope A substrate, 1 commit, b8b04c5)**: P0 entry 5 件目 (= 全 5 entry 完走!) 「スプリントごとに担当者を縦に並べて何をするかわかるような機能」(2026-04-30 ユーザ要望) の scope A substrate。次 iter で SprintCard に「担当者ビュー」 disclosure を追加し本 helper で各 bar の left% / width% を render。新 module `src/features/sprint/swimlane.ts` (+125 行): `computeSwimlaneBarPosition(item, period) → SwimlaneBarPosition | null` で sprint 期間 [startDate..endDate] 両端 inclusive と item bar 期間 [startDate, dueDate] から bar の `leftPct` / `widthPct` 計算 (0.01% 精度)、sprint 範囲外突出は **clip** (clippedAtStart / clippedAtEnd flag、UI で「→ 続く」 indicator 描画可)、完全範囲外は null。`groupItemsByAssigneeKey<T>(items, getAssignees)` で `${actorType}:${actorId}` キー group、assignee ゼロは null キー (= "未割当" lane)、複数 assignee は各 lane に重複 entry。defensive: startDate/dueDate null → null / 不正 ISO null / item.dueDate < startDate (不正) null / sprint.endDate < startDate null / 両端 clip → leftPct=0 widthPct=100 両 flag true。caller benefits: SprintCard disclosure で `Object.entries(group)` で member × items iterate、各 bar に inline style apply するだけで Gantt 表示 / iter467 bar-drag と組合せで swim-lane 上 DnD で assignee 変更 + 期間変更 (scope C 用 substrate 揃った) / generic で SprintCard / OkrCard / personal view 等 横断再利用。test +15 件 (期間ぴったり / 1 日 only / 半ば / 開始前 clip / 終了後 clip / 完全範囲外 ×2 / 片方 null ×2 / 不正範囲 / 不正 ISO / 両端 clip + group 4 = 15/15 PASS)。typecheck/lint 緑 (warning baseline 1)。本 push は parallel session が `0bd898f` (playwright-iter381) 先取で rebase 経由着地。次 iter469 → 全 5 P0 entry の substrate 揃った、いずれかの UI bind 続編 or refactor 割り込み or queue 新 entry 待ち。
- ✅ [iter467 P0 (gantt-bar-drag)] **bar DnD の pixel→day 変換 pure helper (queue: Gantt DnD 期間編集 scope A substrate, 1 commit, dbb6e96)**: P0 entry 4 件目「ガントチャートの期間を直接 DnD で移動」(2026-04-29 ユーザ要望) の scope A substrate、次 iter で gantt-view bar に dnd-kit Sortable + onDragEnd 配線で本 helper を呼び出す UI bind。新 module `src/features/gantt/bar-drag.ts` (+109 行): `computeBarDragShift` で中央 drag 平行 shift (Math.round(deltaPx/dayPx) で snap to day、startDate/dueDate 両方を deltaDays shift、`shiftIsoDate` 内部 TZ 無関係) / `computeBarLeftEdgeShift` (scope B 左 edge resize、startDate のみ shift、>dueDate は dueDate clamp 期間 0 日) / `computeBarRightEdgeShift` (scope B 右 edge、dueDate のみ、<startDate は startDate clamp)。defensive: dayPx≤0/NaN は invalid sentinel (revert) / deltaPx NaN invalid / null は safe-noop / snap to day 半日 round / 月跨ぎ年跨ぎ shiftIsoDate UTC 自動。caller benefits: onDragEnd → `computeBarDragShift` → `useUpdateItem` 楽観ロック / scope B 左右 edge は sibling 関数 / scope C transitive closure は loop。test +14 件 (中央 drag 8 / 左 edge 3 / 右 edge 3 = 14/14 PASS)。typecheck/lint 緑 (warning baseline 1)。本 push は parallel session が `c4c4046` (playwright-iter379) 先取で rebase 経由着地。次 iter468 → P0 entry 5「Sprint 担当者 swim-lane Gantt」 substrate へ移行 (UI bind は別途、複数 iter 必要)。
- ✅ [iter466 P0 (capacity-period)] **capacity 期間 helper (ISO週 / 日 / rolling) (queue: チームメンバー余裕時間 一覧 scope A substrate 続編, 1 commit, 5fc07c3)**: iter465 `computeMemberCapacityLoad` の上流で「period filter された items」を揃える substrate。次 iter で workspace home の MemberCapacityPanel が 1 関数で「member の今日 / 今週の Item」を fetch (= computeMemberCapacityLoad へ pre-filter 済 items を渡す) できる pipeline を整備。新 module `src/features/workspace/capacity-period.ts` (+97 行): `getIsoWeekRange(today)` で ISO 8601 週 (月-日) 境界 (今日 .getDay() で日=0/月=1 → offset day===0?6:day-1、formatLocalISO TZ 固定) / `getDayRange(today)` (trivial wrapper、API pair) / `getRollingDayRange(today, days)` (今日 + 0..N-1、days<=0 null) / `isDueDateInRange(dueDate, range)` (lexicographic 比較) / `selectItemsByDueDateInRange<T>(items, range)` (generic で型保持、range null は空配列 chip 非表示用)。caller benefits: iter465 と pipeline 形成、`selectItemsByDueDateInRange(items, getIsoWeekRange(today))` → `computeMemberCapacityLoad` の chain で「今週 capacity」が 2 行 / generic で UI / service / AI brief 横断再利用 / TZ shift 月跨ぎ / 年跨ぎは既存 `MS_PER_DAY` / `formatLocalISO` / `shiftIsoDate` に委譲。test +18 件 (week 月-日 5 / day 2 / rolling 5 / inRange 4 / select 3 / 不正 Date / 範囲外 / null sentinel / 月跨ぎ = 18/18 PASS)。typecheck/lint 緑 (warning baseline 1)。本 push は parallel session が `fad6a32` (playwright-iter378 mock-login-form) を先取で rebase 経由 着地。次 iter467 → 同 P0 entry 続編で workspace home に MemberCapacityPanel 配置 (assignee bulk fetch 整備が課題、scope A UI bind は数 iter 必要)。
- ✅ [iter465 P0 (member-capacity)] **member の余裕時間 / load status を計算する pure helper (queue: チームメンバー余裕時間 一覧 scope A substrate, 1 commit, 0686f5f)**: P0 entry 3 件目「各チームメンバーが今日、今週、どれくらい余裕な時間があるのかをぱっと一覧」(2026-04-29 ユーザ要望) の scope A substrate、次 iter で workspace home に bind。新 module `src/features/workspace/member-capacity.ts` (+153 行): `computeMemberCapacityLoad(items, capacityMinutes, doneStatusKeys)` で estimate 合計 vs capacity の差分計算 + load status 4 値 (free / comfortable / tight / overloaded) + unknown sentinel (capacity ≤ 0) / `formatMemberCapacityLoadJa(load)` で '余裕あり: 残 4h / capacity 8h (50%)' 形式 (overloaded は '-X 時間オーバー'、未見積 N 件 chip 末尾 append、unknown は '余裕時間 算定不能')。defensive: estimateMinutes null/undefined/NaN/負/0/Infinity は未見積に集計 (capacity 算入せず別 chip) / status doneStatusKeys は完了済 count に振分け used 除外 / capacity ≤ 0 は unknown sentinel / load 閾値 < 60% free / 60-90% comfortable / 90-100% tight / > 100% overloaded。caller benefits: items を pre-filter (assignee / period) するだけで dashboard / workspace home / AI 朝 brief「田中さん 今日 ギリギリ 95%」が 1 関数で揃う / doneStatusKeys を caller で workspace_statuses から作るので status ローカライズに対応 / 期間境界 (今日 / 今週 ISO) / capacity 値 (8h × N 日) は caller 責任 (本 helper は数学のみ)。test +17 件 (空 / capacity 0/負 / free 50% / comfortable 70% / tight 95% / overloaded 120% / done 除外 / cancelled 除外 / 未見積 5 種類 / totalItemCount / format 6 種類 = 17/17 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter466 → 同 P0 entry 続編で workspace home に MemberCapacityPanel + member 一覧 bind (scope A UI)。
- ✅ [iter464 P0 (item-summary-panel)] **ItemEditDialog に「サマリ」 tab を追加 (queue: 案件サマリ panel scope A UI 完成, 1 commit, dd5513a)**: iter463 latest-activity substrate + 既存 substrate 3 つ (descendants-progress / dependency-readiness / latest-activity) を 1 panel に統合、ItemEditDialog 新 tab「サマリ」で「この案件、今どんな感じ？」を 1 画面化。2026-04-29 ユーザ要望「『この案件、今どんな感じ？』『どう着地させるつもり？』を一目で分かる」 scope A vertical slice 完成 (substrate iter463 + UI iter464 = 2 commit)。新 component `item-summary-panel.tsx` (+144 行) で 3 chip bind: 子タスク進捗 (emerald/amber/slate tone を done/blocked/通常で切替) / 依存 (blocked 時 amber) / 最終更新 (formatRelativeTime injection)、aria-label / data-testid / data-pct-done / data-blocked / data-has-activity で E2E hook + SR で全文読み上げ可能。dialog 配線 (+15 / -3 行): tab union 拡張 + TabsList に「サマリ」 trigger を base 直後に追加 (= 開いた直後に snapshot 確認) + TabsContent で mount。UX 卓越 a-g: a 発見可能性 (絵文字 📋 🔗 🕒 + aria-label「進捗 / 依存 / 最終更新を一目で確認」) / b アクセシビリティ (各 chip role=status + aria-label 完全文、絵文字 aria-hidden) / c 状態網羅 (loading / progress done/blocked/通常 / dep blocked/解決 / activity あり/なし — 全分岐 chip tone) / e 細部 (subtasks-panel と同 tone マッピング) / g 一貫性 (shadcn ベース、既存 dialog tab pattern、subtasks-panel の 📋 と整合)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 + latest-activity 12/12 PASS。これで FEEDBACK_QUEUE P0 entry 2 件目「案件サマリ panel」 scope A 完了。次 iter465 → P0 entry 3 件目「チームメンバー余裕時間 一覧」 scope A から消化。
- ✅ [iter463 P0 (latest-activity)] **parent + 子孫の最終更新タイムスタンプ pure helper (queue: 案件サマリ panel scope A substrate, 1 commit, 6a1533d)**: P0 entry 2 件目「『この案件、今どんな感じ？』を 1 画面で一目化」の scope A substrate。AI 朝 brief / dashboard widget / ItemEditDialog 新 tab「サマリ」が「最終更新: 5 分前」を 1 関数で出すための pure helper、次 iter で UI bind。新 module `src/features/item/latest-activity.ts` (+76 行): `findLatestUpdatedAt(parent, parentFullPath, candidates)` で parent + 子孫 (`isPathDescendantOf` で ltree prefix match) の updatedAt 最大値を返す / `formatLatestActivityJa(latest, now, formatRelative)` で `formatRelativeTime` (notification/format) を caller injection で受け取り '最終更新: 5 分前' / '最終更新なし' を ja-JP 1 行で返す (injection で test deterministic + cross-feature 依存最小化)。defensive: deletedAt なし live items のみ / parent 自身 id 混入除外 / 不正 updatedAt fail-soft / ltree prefix match で別 subtree 除外 / suffix 偽陽性 startsWith で弾く / ISO string と Date 両対応。既存 substrate 軸との対称性: descendants-progress (iter417) 進捗 % / dependency-readiness 依存 / 本 helper **時間軸** = 「動いているか / 停滞か」signal の 3 軸目。test +12 件 (parent のみ / 子孫新しい / parent 新しい / 孫 2 階層 / deleted 除外 / parent 自身 id 除外 / 別 subtree 除外 / 不正 updatedAt 除外 / 全 null / ISO 受け取り / format null / format 5 分前 = 12/12 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter464 → 同 P0 entry 続編で ItemSummaryPanel + 新 tab「サマリ」 (scope A UI bind)。
- ✅ [iter462 P0 (template-from-item-ui)] **ItemEditDialog に「Template として保存」 button を bind (queue: Template 登録機能 scope A UI 完成, 1 commit, 6893fe7)**: iter460/461 の substrate + service+action 上に UI bind、scope A vertical slice 完成 (substrate / service / action / hook / UI = 5 段全完了)。「タスクとサブタスクをまとめて Template として登録」(2026-04-29 ユーザ要望) が ItemEditDialog 1 click で完了。新 hook `useCreateTemplateFromItem(workspaceId)` (+16 行) で `createTemplateFromItemAction` を unwrap、onSuccess で templates 一覧 cache invalidate (= /<wsId>/templates ページに即出現)。UI button (+35 行) は DialogFooter に ghost variant、placement: baseline 操作 group の右 / キャンセル左、archive 済 Item では非表示 (archive と排他)、click → window.confirm で確認 → mutateAsync → toast.success/error、aria-label に title + 4 状態網羅、data-testid="item-edit-save-as-template" で E2E hook、title attr に「再利用可」明記で発見可能性向上。UX 卓越 a-g: a 発見可能性 (title hover + aria-label「全ての子孫 (subtask) を Template として保存」)、b アクセシビリティ (keyboard 到達 + aria-busy / aria-label 4 状態)、c 状態網羅 (idle / busy / 成功 toast / 失敗 toast)、g 一貫性 (既存 archive / baseline ghost button と同 size='sm' / variant='ghost' / text-muted-foreground / window.confirm pattern)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 + build-from-items 10/10 PASS。これで FEEDBACK_QUEUE P0 の Template 登録機能 scope A は完了 → 次 iter463 = P0 entry 2 件目「案件サマリ panel」 scope A から消化。
- ✅ [iter461 P0 (template-from-item)] **Item ツリーを Template として保存する service + action (queue: Template 登録機能 scope A bind, 1 commit, fd9da3a)**: iter460 で着地した pure helper `buildTemplateBlueprintFromItemTree` を service / action 層に bind、API 完成 (UI button bind は次 iter)。新 schema `CreateTemplateFromItemInputSchema` (itemId 必須 / name + description optional)、新 service method `templateService.createFromItem`: requireUser → withUserDb (RLS) で parent + workspace 全 items 取得 → parent フル path 起点に in-memory 子孫フィルタ (`isPathDescendantOf`、既存 descendants-progress / at-risk-parents と同 pattern) → requireWorkspaceMember ('member') で二重防御 → buildTemplateBlueprintFromItemTree で Blueprint 構築 → withUserDb 内 Tx で templates + template_items[] を bulk insert + recordAudit (action='create_from_item'、after に `{templateId, sourceItemId, itemCount}`)。RLS で見えない / 別 ws の itemId は NotFound (情報漏洩防止、ws bypass しない)。scope A 制限: dueOffsetDays / agent_role_to_invoke / defaultAssignees / tags / position は保持しない (= scope B 以降)、kind='manual' 固定。新 action `createTemplateFromItemAction`: `actionWrap` で AppError → err(e) 変換。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 + build-from-items 10/10 PASS。service.test.ts は real Supabase 必須 (cloud env では未起動) で新規 test 未追加だが、既存 4 件の failure path test が architecture 規約を満たす。次 iter462 → 同 P0 entry 続編で ItemEditDialog button bind (scope A UI 完成)。
- ✅ [iter460 P0 (template-blueprint)] **Item ツリー → Template Blueprint 変換の pure helper (queue: Template 登録機能 scope A substrate, 1 commit, e75e53f)**: FEEDBACK_QUEUE 🔥 P0 最優先 (track 判定 460%5=0 refactor を上書き) を消化。「ItemEditDialog から『この Item と subtask を Template に保存』」 (Template 登録機能 scope A) の substrate を新設、service / UI bind は次 iter に分割 (line 数 / context 連続性確保)。新 module `src/features/template/build-from-items.ts` (+108 行) に `buildTemplateBlueprintFromItemTree({parent, descendants, idFactory?}) → {template:{name,description}, templateItems[]}`: parent.title→template.name / parent.description→template.description、各 descendant の parentPath は parent のフル path で必ず始まる必要 (= 真の子孫、`PARENT_LABELxxx` の suffix 偽陽性は startsWith フィルタで除外)、満たさないものは silently drop、depth (ltree `.` 区切り長さ) 昇順 stable sort で親が先に label map に入るよう走査、各 descendant に新 uuid を割当て items uuidLabel → template uuidLabel の labelMap を構築、parent prefix を剥がした残りを再翻訳して `template_items.parent_path` を作る (直下子は ''、孫は `parentTemplateLabel`、ひ孫は `parentTL.grandTL`)。MVP scope A では position / due_date / agent_role / tags / assignees は保持しない (= scope B 以降で due_offset_days / agent_role_to_invoke 等を追加)。反対方向 (Template→Item) の `instantiate-plan.ts` と対称な uuidLabel scheme で、保存→展開のラウンドトリップが構造保存。test +10 件 (空 / 直下子 1 件 / 孫 (2 階層) / parent が root 直下でない / 無関係 silently drop / depth 違反入力 / isMust+dod 保持 / 同 depth stable sort / id 一意性 / suffix 偽陽性除外 = 10/10 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter461 → 同 P0 entry 続編で server action + ItemEditDialog button (scope A bind)。
- ✅ [iter459 (best-streak)] **velocity に best streak helper (window 全体最長連続) を追加 (ai-automation, 1 commit, c52f767)**: iter457 `computeCompletionStreak` (末尾今日からの連続 = 現在 streak) に対して、本 helper は **window 全体での最長連続** (= 過去最長記録、retroactive 達成感)。新 export: `computeBestStreak(summary): number` (byDay 走査、各 run 連続長集計、最長返却) / `formatBestStreakJa(streak, windowDays?): string` ('直近 7 日の最長連続: 0 日 (該当なし)' / '直近 7 日の最長連続: 1 日' / '直近 7 日の最長連続: 5 日!')。仕様: byDay 空 → 0 / 全 0 → 0 / 単一 run / 複数 run 最長 / 全日 → byDay.length。iter457 と相補: 現在 streak (motivational) vs 最長 streak (retroactive)。caller benefits: AI 朝 brief で「今週ベスト 5 日連続!」retro feedback / dashboard widget / momentum hint と相補で「件数 / 現在 / 最長」3 軸。test +7 件 → 44/44 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter460 = 460%5=0 → refactor。
- ✅ [iter458 (velocity-streak-bind)] **velocity chip aria-label に completion streak を append (basics, 1 commit, f2b0983)**: iter457 substrate (computeCompletionStreak / formatCompletionStreakJa) を Dashboard の velocity chip aria-label / title / data-streak attr に bind、streak ≥ 2 (連続日数 OK) のみ表示で 0/1 日冗長省略 (= positive feedback 価値ある時のみ)。新 binding: aria-label append=' — 完了 streak 3 日連続!' (streak ≥ 2 のみ) / title 同様 / data-streak=0..N (E2E / CSS hook)。UX 卓越: a 発見可能性 (SR が「3 日連続!」読み上げ、momentum hint と並ぶ達成讃え) / b アクセシビリティ (連続性 motivational metric を SR 経路でも届ける) / c 状態網羅 (0/1 日省略、UI ノイズ最小化) / g 一貫性 (aria 'detail — append' pattern が velocity chip 内で 3 層 (line / priority / streak) に揃う)。10 insertions / 2 deletions、機能追加なし、shadcn 編集なし、visible / glyph / trend tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (velocity 37/37 + architecture 5/5 = 42/42) PASS。次 iter459 = 459%5=4 → ai-automation。
- ✅ [iter457 (completion-streak)] **velocity に completion streak helper を追加 (ai-automation, 1 commit, 58db26d)**: 「直近 N 日のうち done が 1 件以上あった連続日数」を `VelocitySummary.byDay` から計算する pure helper (= GitHub contribution streak スタイル、末尾今日から遡って count>0 連続)。新 export: `computeCompletionStreak(summary): number` / `formatCompletionStreakJa(streak): string` ('完了 streak 0 日 (今日まだ完了なし)' / '完了 streak 1 日 (今日完了あり)' / '完了 streak 5 日連続!')。仕様: byDay 空 → 0 / 末尾 (today) count=0 → 0 (streak 途切れ) / 末尾から遡って count>0 続く間 ++、count=0 で break / 全日 count>0 → byDay.length。caller benefits: AI 朝 brief / Slack 通知が「3 日連続で何か完了!」positive reinforcement / dashboard widget で「streak: 5 日」表示 / momentum hint (iter449、件数ベース) と相補で「件数 vs 連続性」2 軸。test +6 件 (空 / 末尾0 / 末尾1 / 連続3 / 全日 / 途中break) → 37/37 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter458 = 458%5=3 → basics。
- ✅ [iter456 (velocity-hint-bind)] **velocity chip に hint label / data-velocity-hint を bind (basics, 1 commit, 7f900e9)**: iter454 substrate を Dashboard の velocity chip aria-label / title / data-velocity-hint attr に bind、4 状態 (idle/up/flat/down) を 1 word jp 文言で SR / DOM 観察可能に (= iter441 / iter443 / iter446 / iter448 / iter451 hint chip 同手法 6 弾目)。新 binding: aria-label='加速中: 直近 7 日 velocity: 12 件 (1.7 件/日、傾向 up) — 直近 7 日 velocity: P1 3 件 (0.4件/日) / P3 9 件 (1.3件/日)' ('up' を jp '加速中' で SR 即読み) / title='加速中 — 直近 7 日 velocity: 12 件 ...' (hover) / data-velocity-hint=hint enum (E2E / CSS hook、idle は chip 非表示)。data-trend (英語 enum) は不変で並存。UX 卓越: a 発見可能性 (SR が jp 文言で先読み、英語 trend より直感的) / b アクセシビリティ (自然な日本語 SR feedback) / c 状態網羅 (3 状態 attr) / g 一貫性 (aria 'hint: detail' pattern が 7 chip で揃う、iter441/443/446/448/451 と並ぶ 6 弾目)。11 insertions / 3 deletions、機能追加なし、shadcn 編集なし、visible / glyph / trend tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (velocity 31/31 + architecture 5/5 = 36/36) PASS。次 iter457 = 457%5=2 → ai-automation。
- ✅ [iter455 (makeHintLabelFormatter-集約)] **makeHintLabelFormatter を集約 (LABEL[classify] pattern 3 callsite) (refactor, 1 commit, f2bf8c3)**: iter325 / iter330 / ... / iter450 と同じ「同 shape の散在を 1 file に」方針 33 弾目。iter439 / iter447 / iter454 で同 shape の `LABEL[classify(input)]` pattern (hint enum jp lookup) が 3 callsite で散在していたので 1 関数に集約。新 export (lib/hint.ts): `makeHintLabelFormatter<I, H>(classify, labels)` で input → string thunk を生成。callsite 置換 (3 箇所、各 -3 / +3 = 0 line + 構造重複解消): blocked-items / backlog-aging / velocity。non-caller (refactor 対象外、generic `<I>` を返す classify): at-risk-parents / parent-items-progress / recent-completed の 3 callsite は generic 制約で `unknown` 固定されるため existing function 形維持 (TS 型制約)。caller benefits: magic `LABEL[classify(input)]` pattern を helper 閉じ込め / 新規 hint helper boilerplate 削減 / labels const は domain 依存維持。動作変更ゼロ、test +3 件 (thunk 性 / pure 性 / classify mock) → 12/12 + 全関連 105/105 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter456 = 456%5=1 → basics。
- ✅ [iter454 (velocity-trend-hint)] **velocity に trend hint 4-state jp 整形を追加 (ai-automation, 1 commit, 908fee2)**: iter424 / iter439 / iter442 / iter444 / iter447 / iter449 と並ぶ「item axis 1-word state」シリーズの velocity-trend 軸版 (7 弾目)。既存 `VelocitySummary.trend` (英語 up/flat/down) を 4 状態 jp 文言に出し分け、idle (total=0) 含めた 4 状態。新 export: `VelocityHint = 'idle' | 'up' | 'flat' | 'down'`、`classifyVelocityHint(summary)` → total=0 で idle、それ以外 trend、`formatVelocityHintJa(summary)` → '完了なし' / '加速中' / '安定' / '減速中'。caller benefits: dashboard velocity-chip aria-label / glyph base を hint base で決める / Slack 朝 brief 1 行 status (down で警告) / AI prompt 1 word 状況把握 / 既存 trendLabel 英語 enum と相補で 'enum vs jp word' 出し分け。test +4 件 (4 状態) → 31/31 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter455 = 455%5=0 → refactor。
- ✅ [iter453 (velocity-priority-aria)] **velocity chip aria-label に priority breakdown を append (basics, 1 commit, 62bd1ba)**: iter452 substrate を Dashboard の velocity chip aria-label / title に bind、iter386 / iter408 / iter414 / iter423 / iter431 / iter433 / iter436 と同手法で「3 層情報設計」を 8 chip 目で揃える。3 層: visible=line (変更なし) / hover&aria=`${line}${priorityDetailSuffix}` ('直近 7 日 velocity: 12 件 ... — 直近 7 日 velocity: P1 3 件 (0.4件/日) / P3 9 件 (1.3件/日)') / data attrs=`data-priority-buckets` 追加 (E2E)。priorityDetailSuffix (iter425) 経由で priority bucket > 1 時のみ append (単一 P 偏在冗長省略)。既存 visible chip の trend / data-trend 不変。UX 卓越: a 発見可能性 (P 別 完了ペース hover、低優先偏重 bias 即読み) / b アクセシビリティ (SR full 文言) / c 状態網羅 (visible 短 / hover richer 2 層) / g 一貫性 (8 chip で 'summary — by-axis breakdown' pattern 揃い、iter386/408/414/423/431/433/436 と同手法 7 弾目)。25 insertions / 5 deletions、機能追加なし、shadcn 編集なし、visible / glyph / trend tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (velocity 27/27 + architecture 5/5 = 32/32) PASS。次 iter454 = 454%5=4 → ai-automation。
- ✅ [iter452 (velocity-by-priority)] **velocity に priority 軸 + by-priority 集計を追加 (ai-automation, 1 commit, 976baf2)**: iter386 / iter391 / iter406 / iter408 / iter414 / iter429 / iter432 / iter434 / iter437 と並ぶ「× priority」軸シリーズの velocity 軸版 (10 弾目)。新 export: `VelocityByPriorityFields`、`VelocityPriorityStats = {count, avgPerDay}`、`computeVelocityByPriority(items, options?, today?)` で各 P bucket に対して computeVelocity を呼び抽出 (bucketByPriorityWith 委譲)、`formatVelocityByPriorityJa(byP, windowDays?)` で '直近 7 日 velocity: P1 3 件 (0.4件/日) / P3 5 件 (0.7件/日)' 形式 (count=0 省略、全 0 → '直近 7 日 velocity 0 件')。caller benefits: AI 朝 brief で P 別 完了ペース 1 関数取得 (高優先消化遅い bias 検出) / iter354 wip-by-priority (in_progress = 着手中) と相補で 'done = 完了済' 軸 / dashboard velocity-chip aria-label に append 可。test +5 件 (空 / P1 + P3 mixed / 単一偏在 / windowDays sentinel / priority null 正規化) → 27/27 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter453 = 453%5=3 → basics。
- ✅ [iter451 (recent-done-momentum-bind)] **recent-done chip に momentum hint label / data-momentum-hint を bind (basics, 1 commit, b6a665a)**: iter449 substrate を Dashboard の recent-done chip aria-label / title / data-momentum-hint attr に bind、4 状態 (idle/mild/steady/momentum) の workspace momentum を 1 word で SR / DOM 観察可能に。**chip series 初の positive-direction hint binding** (= severe ではなく momentum を最良 state として露出、達成讃え用 SR feedback)。新 binding: aria-label='勢いあり: 今日の達成: 5 件 — A / B / C / 他 2 件 — 今日の達成: P1 2 件 (最新 3分前)' (momentum prefix で達成度先読み) / title='勢いあり — 今日の達成: 5 件 — ...' (hover) / data-momentum-hint=hint enum (E2E / CSS hook、idle は chip 非表示)。UX 卓越: a 発見可能性 (SR が達成讃え先読み) / b アクセシビリティ (positive feedback、'勢いあり = 良ペース' が SR で読める) / c 状態網羅 (3 状態 attr) / g 一貫性 (aria 'hint: detail' pattern が 6 chip で揃う、iter441/443/446/448 と並ぶ 5 弾目、ただし方向性 (positive) は recent-done のみ)。12 insertions / 3 deletions、機能追加なし、shadcn 編集なし、visible / truncate / glyph / tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (recent-completed 30/30 + architecture 5/5 = 35/35) PASS。次 iter452 = 452%5=2 → ai-automation。
- ✅ [iter450 (formatAgingByPriorityJa-cleanup)] **formatAgingByPriorityJa を formatPriorityBucketsLabeled に置換 (iter435 missed callsite) (refactor, 1 commit, 311e240)**: iter435 で 5 callsite (slip-days / at-risk-parents / parent-items-progress / recent-completed / blocked-items) を formatPriorityBucketsLabeled に集約した時、backlog-aging.formatAgingByPriorityJa は callsite 数のカウントから漏れていた missed callsite を清掃完了 — iter435 substrate を全 callsite で利用、`body === sentinel` 三項演算子の散在を完全に解消。callsite 置換 (-3 / +2): backlog-aging.formatAgingByPriorityJa ('停滞: ' prefix + '停滞 0 件' sentinel)。import 整理: formatPriorityBuckets → formatPriorityBucketsLabeled に切替。動作変更ゼロ (出力文字列完全互換)、affected (backlog-aging 46/46 + architecture 5/5 = 51/51) PASS、typecheck/lint 緑 (warning baseline 1)。test 追加なし (既存 4+ ケースカバー済)。次 iter451 = 451%5=1 → basics。
- ✅ [iter449 (recent-completed-momentum-hint)] **recent-completed に momentum hint 4-state (positive 方向) を追加 (ai-automation, 1 commit, e1e3533)**: iter424 / iter439 / iter442 / iter444 / iter447 と並ぶ「item axis 1-word state」シリーズの recent-completed 軸版 (6 弾目)。設計差: 既存 hint 5 弾は「more bad = severe」(負方向、警報) だが本 hint は「more wins = momentum」(正方向、達成) — `severe` ではなく `momentum` を最良 state として export、positive feedback を 1 word で出せる。新 export: `RecentCompletedMomentumHint = 'idle' | 'mild' | 'steady' | 'momentum'`、`classifyRecentCompletedMomentum(entries)` → hint enum、`formatRecentCompletedMomentumJa(entries)` → '今日の達成なし' / '少しずつ進行' / '安定した動き' / '勢いあり'。4 状態判定 (entries.length のみ、windowHours=24h 内なので freshness 副次): idle (空) / mild (1-2) / steady (3-4) / momentum (≥5)。caller benefits: dashboard recent-done chip glyph / tone を hint base で決める / Slack 朝 brief 1 行 status (momentum で達成讃える) / AI prompt 1 word 状況把握 (idle で励まし) / formatRecentCompletedSummaryJa (詳細 list) と相補。test +5 件 (4 状態 + 境界 4/5) → 30/30 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter450 = 450%5=0 → refactor。
- ✅ [iter448 (backlog-aging-hint-bind)] **backlog-aging chip に hint label / data-aging-hint を bind (basics, 1 commit, 60d048b)**: iter447 substrate を Dashboard の backlog-aging chip aria-label / title / data-aging-hint attr に bind、4 状態 (fresh/mild/moderate/severe) の backlog 健全度を 1 word で SR / DOM 観察可能に (= iter441 / iter443 / iter446 hint chip 同手法 4 弾目)。新 binding: aria-label='深刻 (古参累積): Backlog 年齢: 新規 3 / 直近 5 / 停滞 8 / 古参 12 — 7 日以上 20 件 (棚卸し対象) — 停滞: P1 3 件 / P3 5 件' (hint prefix 先読み) / title='深刻 (古参累積) — 新規 3 / 直近 5 / 停滞 8 / 古参 12' (hover) / data-aging-hint=hint enum (E2E / CSS hook)。UX 卓越: a 発見可能性 (SR が状況先読み) / b アクセシビリティ (詳細前に状況把握) / c 状態網羅 (4 状態 attr) / g 一貫性 (aria 'hint: detail' pattern が 5 chip で揃う、iter441/443/446 と並ぶ 4 弾目)。13 insertions / 4 deletions、機能追加なし、shadcn 編集なし、visible / truncate / glyph / tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (backlog-aging 46/46 + architecture 5/5 = 51/51) PASS。次 iter449 = 449%5=4 → ai-automation。
- ✅ [iter447 (backlog-aging-hint)] **backlog-aging に hint 4-state 分類を追加 (ai-automation, 1 commit, 40080c1)**: iter424 / iter439 / iter442 / iter444 と並ぶ「item axis 1-word state」シリーズの backlog-aging 軸版 (5 弾目)。新 export: `BacklogAgingHint = 'fresh' | 'mild' | 'moderate' | 'severe'`、`classifyBacklogAgingHint(counts)` → hint enum、`formatBacklogAgingHintJa(counts)` → '新鮮 (停滞なし)' / '軽微停滞' / '停滞多め' / '深刻 (古参累積)'。4 状態判定 (stagnant=stale+ancient): fresh (stagnant=0) / mild (stagnant 1-4 かつ ancient<3) / moderate (stagnant≥5 または ancient 3-4) / severe (ancient≥5、古参累積で重大)。unknown は集計対象外 (countAgingItemsOlderThanWeek と一貫)。caller benefits: dashboard backlog-aging chip glyph / tone を hint base で決める / Slack 朝 brief 1 行 status (severe で「ancient 5+ 件累積」警告) / AI prompt 1 word 状況把握 / formatAgingByPriorityJa (priority 別) と相補。test +6 件 (4 状態 + ancient 境界 + unknown 除外) → 46/46 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter448 = 448%5=3 → basics。
- ✅ [iter446 (parent-items-progress-hint-bind)] **parent-items-progress chip に hint label / data-progress-hint を bind (basics, 1 commit, 96ddbec)**: iter444 substrate を Dashboard の parent-items-progress chip aria-label / title / data-progress-hint attr に bind、5 状態 (idle/stuck/slow/healthy/almostDone) の workspace-wide 進捗状況を 1 word で SR / DOM 観察可能に (= iter441 blocked-items-hint / iter443 at-risk-parents-hint chip 同手法 3 弾目)。新 binding: aria-label='順調進行中: 進行中: A 30% (3/10) / B 60% (6/10) / 他 2 件 — 進行中 5 件: 平均 54%, 停滞 2 / 順調 2 / 仕上げ 1 — 進行中: P1 1 件 (平均 30%) / P3 2 件 (平均 70%)' (hint prefix 先読み) / title='順調進行中 — 進行中: A 30% ...' (hover) / data-progress-hint=hint enum (E2E / CSS hook、idle は chip 非表示なので発生しない)。UX 卓越: a 発見可能性 (SR が状況先読み) / b アクセシビリティ (詳細前に状況把握) / c 状態網羅 (4 状態 attr) / g 一貫性 (aria 'hint: detail' pattern が must- / blocked / at-risk / parent-progress で揃う、4 chip 同手法)。10 insertions / 3 deletions、機能追加なし、shadcn 編集なし、visible / truncate / glyph / tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (parent-items-progress 28/28 + architecture 5/5 = 33/33) PASS。次 iter447 = 447%5=2 → ai-automation。
- ✅ [iter445 (classifyByCountAndMax-集約)] **classifyByCountAndMax を集約 (4-state hint 2 callsite) (refactor, 1 commit, 7a88682)**: iter325 / iter330 / ... / iter440 と同じ「同 shape の散在を 1 file に」方針 32 弾目。iter439 (blocked-items-hint) / iter442 (at-risk-parents-hint) で同 shape の 4 状態判定ロジック (count + max-of-getter → idle/mild/moderate/severe) が重複していたので 1 関数に集約。新 module `src/lib/hint.ts`: `FourStateHint = 'idle' | 'mild' | 'moderate' | 'severe'`、`FourStateThresholds` interface、`classifyByCountAndMax(entries, getValue, thresholds)` で entries 空 → idle、max getter で entries 内最大値計算 → severe / moderate / mild に分類。callsite 置換 (2 箇所、合計 -16 / +14 = -2 行 + 構造重複解消): blocked-items (thresholds=3/3/5/5、getter=openBlockerCount) / at-risk-parents (thresholds=3/14/5/30、getter=maxStaleDays)。caller benefits: 判定ロジックを 1 箇所で管理 / 新規 hint helper 追加時は閾値+getter のみで済む / hint enum 文言は caller 側 Record map で domain 依存出し分け / 5 状態 hint (parent-items-progress) は別 shape のため対象外。動作変更ゼロ、test +9 件 → hint 9/9 + blocked-items 29/29 + at-risk-parents 31/31 + architecture 5/5 = 64/64 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter446 = 446%5=1 → basics。
- ✅ [iter444 (parent-items-progress-hint)] **parent-items-progress に hint 5-state 分類を追加 (ai-automation, 1 commit, 7a6162b)**: iter424 (descendants-activity-hint, 個別 parent) / iter439 (blocked-items-hint, 依存 axis) / iter442 (at-risk-parents-hint, 停滞 axis) と並ぶ「item axis 1-word state」シリーズの parent-items-progress 軸版 (4 弾目)。新 export: `ParentItemsProgressHint = 'idle' | 'stuck' | 'slow' | 'healthy' | 'almostDone'`、`classifyParentItemsProgressHint(entries)` → hint enum、`formatParentItemsProgressHintJa(entries)` → '進行案件なし' / '案件停滞気味' / '前半遅延' / '順調進行中' / '仕上げ間近'。5 状態判定: idle (空) / stuck (avg < 25 または stuck tier 過半数 = 偽 healthy 防止) / slow (avg 25-49) / healthy (avg 50-74) / almostDone (avg ≥ 75)。caller benefits: dashboard chip glyph / tone を hint base で決める / Slack 朝 brief 1 行 status / AI prompt 1 word 状況把握 (stuck で focus / almostDone で完了 promote 候補) / formatParentItemsProgressBriefJa (詳細 list) / formatAggregateParentItemsJa (集計) と相補で 3 軸出し分け。test +6 件 (5 状態 + stuck tier 過半判定) → 28/28 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter445 = 445%5=0 → refactor。
- ✅ [iter443 (at-risk-parents-hint-bind)] **at-risk-parents chip に hint label / data-at-risk-hint を bind (basics, 1 commit, 922e0ce)**: iter442 substrate (classifyAtRiskParentsHint / formatAtRiskParentsHintJa) を Dashboard の at-risk-parents chip aria-label / title / data-at-risk-hint attr に bind、4 状態の workspace-wide 停滞度を 1 word で SR / DOM 観察可能に (= iter441 blocked-items-hint chip 同手法 2 弾目)。新 binding: aria-label='案件深刻放置 (要 review): 触れていない案件 5 件: A 14日 / B 8日 / 他 3 件 — 触れていない案件: P1 1 件 (最大 14日)' (hint prefix 先読み) / title='案件深刻放置 (要 review) — 触れていない案件 ...' (hover) / data-at-risk-hint=hint enum (E2E / CSS hook、idle は chip 非表示なので発生しない)。UX 卓越: a 発見可能性 (SR が重大度先読み) / b アクセシビリティ (詳細前に状況把握) / c 状態網羅 (3 状態 attr) / g 一貫性 (aria 'hint: detail' pattern が must- / blocked / at-risk で揃う、3 chip 同手法)。11 insertions / 3 deletions、機能追加なし、shadcn 編集なし、visible / truncate / glyph / tone 不変。typecheck/lint 緑 (warning baseline 1)、affected (at-risk-parents 31/31 + architecture 5/5 = 36/36) PASS。次 iter444 = 444%5=4 → ai-automation。
- ✅ [iter442 (at-risk-parents-hint)] **at-risk-parents に hint 1-word state 分類を追加 (ai-automation, 1 commit, 5be7530)**: iter424 (descendants-activity-hint, 個別 parent) / iter439 (blocked-items-hint, 依存 axis) と並ぶ「item axis 1-word state」シリーズの at-risk-parents 軸版 (3 弾目)。新 export: `AtRiskParentsHint = 'idle' | 'mild' | 'moderate' | 'severe'`、`classifyAtRiskParentsHint(entries)` → hint enum、`formatAtRiskParentsHintJa(entries)` → '案件停滞なし' / '一部案件停滞' / '案件停滞多め' / '案件深刻放置 (要 review)'。4 状態判定: idle (空) / mild (count ≤ 2 かつ max < 14) / moderate (count 3-4 または max 14-29) / severe (count ≥ 5 または max ≥ 30)。caller benefits: dashboard chip glyph / tone を hint base で決める / Slack 朝 brief 1 行 status / AI prompt 1 word 状況把握 / formatAtRiskParentsBriefJa (詳細 list) と相補 ('数値' vs '意味付け')。test +6 件 (4 状態 + max 境界 14 / 30) → 31/31 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter443 = 443%5=3 → basics。
- ✅ [iter441 (blocked-items-hint-bind)] **blocked-items chip に hint label / data-block-hint を bind (basics, 1 commit, c7af16e)**: iter439 substrate (classifyBlockedItemsHint / formatBlockedItemsHintJa) を Dashboard の blocked-items chip aria-label / title / data-block-hint attr に bind、4 状態 (idle/mild/moderate/severe) の重大度を 1 word で SR / DOM 観察可能に。新 binding: aria-label='依存深刻 (前提解消急務): 依存ブロック: A [3 件待ち] / B [1 件待ち] — 依存ブロック: P1 1 件 (最大 3 件待ち)' (hint prefix 先読み) / title='依存深刻 (前提解消急務) — 依存ブロック: ...' (hover) / data-block-hint=hint enum (= E2E selector / CSS hook、idle は entries 空で chip 自体非表示なので発生しない)。UX 卓越: a 発見可能性 (SR が重大度先読み) / b アクセシビリティ (詳細前に状況把握) / c 状態網羅 (3 状態 attr で CSS/E2E 観察可) / g 一貫性 (aria 'hint: detail' pattern、must-系 chip と整合)。10 insertions / 3 deletions、機能追加なし、shadcn 編集なし、visible chip text / truncate / glyph / tone (warn) 不変。typecheck/lint 緑 (warning baseline 1)、affected (blocked-items 29/29 + architecture 5/5 = 34/34) PASS。次 iter442 = 442%5=2 → ai-automation。
- ✅ [iter440 (initPriorityRecord-集約)] **initPriorityRecord を集約 (4-key priority init 7 callsite) (refactor, 1 commit, 33d6b4e)**: iter325 / iter330 / ... / iter435 と同じ「同 shape の散在を 1 file に」方針 31 弾目。6 file (7 callsite) で同 shape の 4 行 literal `{1: factory(), 2: factory(), 3: factory(), 4: factory()}` が散在していたので 1 関数に集約。新 export (priority.ts): `initPriorityRecord<T>(factory: () => T)` で `{1..4}` を返す。factory は bucket ごとに新インスタンス (mutable state 共有なし、4 回呼ばれる)。既存 `bucketByPriorityWith(items, compute)` は items + per-group compute 用、本 helper は pre-init 専用 (= 空 record + 後で外側 loop accumulate pattern)。callsite 置換 (7 箇所、各 -3 / +1 = 2 line saved per site): at-risk-parents / parent-items-progress (×2: result + sums) / recent-completed / freshly-done / blocked-items / completion-days-by-priority (×2: emptyStats + sums)。caller benefits: 4 行明示 literal を 1 行に短縮 / factory 関数の遅延 evaluation で各 bucket 独立性担保 / プリミティブ値 (number) も factory で返せる (sums 用)。動作変更ゼロ、test +3 件 (factory 4 回呼出 / mutable state 非共有 / プリミティブ値) → 171/171 全件 PASS、依存不変、typecheck/lint 緑 (warning baseline 1)。次 iter441 = 441%5=1 → basics。
- ✅ [iter439 (blocked-items-hint)] **blocked-items に hint 1-word state 分類を追加 (ai-automation, 1 commit, eb37453)**: iter424 (formatDescendantsActivityHintJa: descendants-progress 10 状態 hint) と並ぶ「item axis 1-word state」シリーズの dependency-blocked 軸版。新 export: `BlockedItemsHint = 'idle' | 'mild' | 'moderate' | 'severe'`、`classifyBlockedItemsHint(blocked)` → hint enum、`formatBlockedItemsHintJa(blocked)` → '依存ブロックなし' / '依存軽微' / '依存中程度' / '依存深刻 (前提解消急務)'。4 状態判定: idle (空) / mild (count ≤ 2 かつ max ≤ 2) / moderate (count 3-4 または max 3-4) / severe (count ≥ 5 または max ≥ 5)。caller benefits: dashboard chip glyph / tone を hint base で決める / Slack 1 行 status / AI prompt 1 word ヒント (severe で brief 先頭警告) / formatBlockedItemsBriefJa (詳細 list) と相補 ('数値' vs '意味付け')。test +6 件 (4 状態 + 境界 max3 / max5) → 29/29 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter440 = 440%5=0 → refactor。
- ✅ [iter438 (title-list-chip-truncate)] **title-list chip 3 件に truncateText を追加 (basics, 1 commit, beaa01f)**: dashboard-view.tsx の title-list 系 chip 3 件 (blocked-items / at-risk-parents / parent-items-progress) に `truncateText` prop を追加、stale-urgent / recent-done / backlog-aging / completion-gap で既に有効な truncate と整合させる。問題: 上記 3 chip の visible text 'XXX N 件: A 文字列 / B 文字列 / 他 K 件' で長 title が narrow viewport (mobile / sidebar 横並び) で改行 / overflow scroll を起こしていた。truncate 化で既存 4 chip と同じ「省略 (...)」表示に揃い、aria-label / title (= SR / hover) は full 文言で richer のまま (= text-only truncation、aria は不変)。UX 卓越: a 発見可能性 (visible は短く、hover で full 読取可) / c 状態網羅 (narrow viewport UI 崩れ防止) / g 一貫性 (title-list 系 7 chip 全件で truncate 統一)。3 insertions、機能追加なし、shadcn 編集なし、aria-label / data attrs 不変。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter439 = 439%5=4 → ai-automation。
- ✅ [iter437 (freshly-done-by-priority)] **freshly-done に priority 軸 + by-priority 集計を追加 (ai-automation, 1 commit, 3ff0197)**: iter386 / iter391 / iter406 / iter408 / iter414 / iter429 / iter432 / iter434 と並ぶ「× priority」軸シリーズの **freshly-done 軸版** (9 弾目)。recent-completed (iter434) との対称軸 — recent-completed は windowHours=24h / latestMinutesAgo (短期 momentum)、本 freshly-done は thresholdDays=7d / latestDaysSinceDone (週次 retro)。`FreshlyDoneItemEntry<T>` に `priority: PriorityKey` field を追加 (item.priority を normalizePriority 正規化)、新 `FreshlyDonePriorityStats = {count, latestDaysSinceDone}`、`computeFreshlyDoneByPriority(entries)` で bucket 化、`formatFreshlyDoneByPriorityJa(byPriority, thresholdDays?)` で '完了: P1 1 件 (最新 今日) / P3 2 件 (最新 1日前)' 形式 (count=0 省略、全 0 → '完了 0 件 (直近 N 日)')。iter435 で集約した formatPriorityBucketsLabeled を委譲、empty sentinel に thresholdDays 反映。caller benefits: AI 週次 retro / 月次 review が P 別 達成感 1 関数取得 (P1 で N 件達成 = 高優先 focus できた、 P3 偏重 = bias 検出) / recent-completed (短期 chip 用) と並存 (短期 vs 週次)。test +5 件 (priority=1 抽出 / 範囲外正規化 / 空 / P1+P3 mixed / 単一偏在 / sentinel 反映) → 23/23 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter438 = 438%5=3 → basics。
- ✅ [iter436 (recent-done-priority-aria)] **recent-done chip aria-label に priority breakdown を append (basics, 1 commit, a9f726b)**: iter434 substrate を Dashboard の recent-done chip aria-label / title に bind、iter386 / iter408 / iter414 / iter423 / iter431 / iter433 と同手法で「3 層情報設計」を 7 chip 目で揃える。3 層: visible=summary (個別 title list 上位 3、変更なし) / hover&aria=`${summary}${priorityDetailSuffix}` ('今日の達成: 3 件 — A / B / C — 今日の達成: P1 1 件 (最新 3分前) / P3 1 件 (最新 1時間前)') / data attrs=`data-priority-buckets` 追加 (E2E)。priorityDetailSuffix (iter425) 経由で priority bucket > 1 時のみ append (単一 P 偏在冗長省略)。allEntries 算出経路を共有 (selectRecentCompleted 1 回のみ呼ぶ最適化、既存 total 算出と統合)。UX 卓越: a 発見可能性 (P 別 momentum、P1 win 3分前 = '勢いあり' 即読み) / b アクセシビリティ (SR full 文言) / c 状態網羅 (visible 短 / hover richer 2 層) / g 一貫性 (7 chip で 'summary — by-axis breakdown' pattern 揃い、iter386/408/414/423/431/433 と同手法 7 弾目)。19 insertions / 5 deletions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (recent-completed 25/25 + architecture 5/5 = 30/30) PASS。次 iter437 = 437%5=2 → ai-automation。
- ✅ [iter435 (formatPriorityBucketsLabeled-集約)] **formatPriorityBucketsLabeled を集約 (LABEL+sentinel wrap 5 callsite) (refactor, 1 commit, 67efaeb)**: iter325 / iter330 / ... / iter430 と同じ「同 shape の散在を 1 file に」方針 30 弾目。slip-days / at-risk-parents / parent-items-progress / recent-completed / blocked-items の 5 callsite で同 shape の `body === sentinel ? body : prefix + body` 三項演算子 (= formatPriorityBuckets の出力を LABEL: でくるむ pattern) が散在していたので 1 関数に集約。新 export (priority.ts): `formatPriorityBucketsLabeled(byP, formatBucket, prefix, emptySentinel)` で formatPriorityBuckets を呼び出し、body === emptySentinel ならそのまま、それ以外は `${prefix}: ${body}` を返す。prefix と emptySentinel を独立に渡せるので parent-items-progress のように prefix='進行中' / sentinel='進行中の案件 0 件' のような非対称 callsite も対応。callsite 置換 (5 箇所、各 -3 / +2 = 1 line saved + magic-string sentinel match 解消)。caller benefits: magic-string sentinel match (= `body === '... 0 件'`) を helper に閉じ込め、sentinel 文言変更時 1 箇所変更で済む / 新規 by-priority format 関数の boilerplate が 5 行 → 6 行 (但し ternary 解消)。動作変更ゼロ (出力文字列完全互換)、test +3 件 (空 → sentinel / 1+ entry → prefix 付与 / 非対称 prefix-sentinel) → priority 36 + at-risk-parents 25 + parent-items-progress 22 + recent-completed 25 + slip-days + blocked-items + architecture 5/5 = 154/154 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter436 = 436%5=1 → basics。
- ✅ [iter434 (recent-completed-by-priority)] **recent-completed に priority 軸 + by-priority 集計を追加 (ai-automation, 1 commit, 0cbe2c1)**: iter386 / iter391 / iter406 / iter408 / iter414 / iter429 / iter432 と並ぶ「× priority」軸シリーズの **recent-completed 軸版** (8 弾目)。新 `RecentCompletedPriorityStats = {count, latestMinutesAgo}` (count=0 なら null)、`computeRecentCompletedByPriority(entries, now)` で bucket 化、`formatRecentCompletedByPriorityJa(byPriority)` で '今日の達成: P1 2 件 (最新 3分前) / P3 1 件 (最新 1時間前)' 形式 (count=0 省略、全 0 → '今日の達成 0 件')。iter386/408/414/429/432 と同じ '{label}: P1 ... / P3 ...' 形式。freshness 表記: 60 分未満 → 'M分前' / 60 分以上 → 'H時間前' (Math.floor、24h window 内なので 1-23 時間)、未来 doneAt は 0 分前 へ clamp (defensive)。caller benefits: AI 朝 brief で P 別 momentum 1 関数取得 (P1 win 3分前 = '勢いあり' 即読み) / dashboard recent-done chip aria-label / title に append 可 (次 iter basics) / velocity (件数) と相補 ('何件 / 何分前')。test +5 件 (空 / P1 2件 + P3 1件 / 単一偏在 / 60分境界 / 未来 doneAt clamp) → 25/25 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter435 = 435%5=0 → refactor。
- ✅ [iter433 (parent-items-progress-priority-aria)] **parent-items-progress chip aria-label に priority breakdown を append (basics, 1 commit, 01f7da7)**: iter432 substrate を Dashboard の parent-items-progress chip aria-label / title に bind、iter386 / iter408 / iter414 / iter423 / iter431 と同手法で「3 層情報設計」を 6 chip 目で揃える (must-overdue / slip-days / blocked-items / parent-items-progress / at-risk-parents の P-breakdown 完備)。3 層: visible=summary (個別 title list 上位 3、変更なし) / hover&aria=`${summary} — ${aggregateLine}${priorityDetailSuffix}` ('進行中: A 30% (3/10) / B 60% (6/10) / 他 2 件 — 進行中 5 件: 平均 54%, 停滞 2 / 順調 2 / 仕上げ 1 — 進行中: P1 1 件 (平均 30%) / P3 2 件 (平均 70%)') / data attrs=`data-priority-buckets` 追加 (E2E)。priorityDetailSuffix (iter425) 経由で priority bucket > 1 時のみ append (単一 P 偏在冗長省略)。aggregateLine と priority breakdown は相補 ('tier 全体感' vs 'P 別 bias')。UX 卓越: a 発見可能性 (aggregate + P bias 両軸 hover、低進捗 P1 = 最深刻度示唆) / b アクセシビリティ (SR full 文言) / c 状態網羅 (visible 短 / hover richer 3 層 = summary / aggregate / by-priority) / g 一貫性 (6 chip で 'summary — by-axis breakdown' pattern 揃い、iter386/408/414/423/431 と同手法 6 弾目)。13 insertions / 2 deletions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (parent-items-progress 22/22 + architecture 5/5 + descendants-progress 16/16 = 43/43) PASS。次 iter434 = 434%5=4 → ai-automation。
- ✅ [iter432 (parent-items-progress-by-priority)] **parent-items-progress に priority 軸 + by-priority 集計を追加 (ai-automation, 1 commit, 7a56e0a)**: iter386 / iter391 / iter406 / iter408 / iter414 / iter429 と並ぶ「× priority」軸シリーズの **parent-items-progress 軸版** (7 弾目)。`ParentItemProgress<I>` に `priority: PriorityKey` field を追加 (parent.priority を normalizePriority で正規化、null/範囲外は 4 集約)、新 `ParentItemsProgressPriorityStats = {count, avgPctDone}`、`computeParentItemsProgressByPriority(entries)` で bucket 化、`formatParentItemsProgressByPriorityJa(byPriority)` で '進行中: P1 1 件 (平均 30%) / P3 2 件 (平均 70%)' 形式 (count=0 省略、全 0 → '進行中の案件 0 件')。iter386/408/414/429 と同じ '{label}: P1 ... / P3 ...' 形式。iter422 の aggregate (tier 別) と相補 (= '全体感' vs 'priority 別')。caller benefits: AI 朝 brief で P 別 進捗 bias 1 関数取得 (P1 低進捗 = 最深刻度示唆) / dashboard parent-items-progress chip 3 層情報設計に bind 可能 (次 iter)。test +5 件 (pickIncompleteParentItems priority=1 抽出 / 範囲外正規化 / by-priority 0件 / 複数P / 単一偏在 / Math.round) → 22/22 PASS、依存不変 (architecture 5/5 / descendants-progress 16/16 / at-risk-parents 25/25 = 74/74)、typecheck/lint 緑 (warning baseline 1)。次 iter433 = 433%5=3 → basics。
- ✅ [iter431 (at-risk-parents-priority-aria)] **at-risk-parents chip aria-label に priority breakdown を append (basics, 1 commit, 86879f4)**: iter429 substrate を Dashboard の at-risk-parents chip aria-label / title に bind、iter386 / iter408 / iter414 / iter423 と同手法で「3 層情報設計」を 5 chip 目で揃える (must-overdue / slip-days / blocked-items / parent-items-progress / at-risk-parents)。3 層: visible=summary (個別 title list 上位 3、変更なし) / hover&aria=`${summary} — ${formatAtRiskParentsByPriorityJa(byPriority)}` ('触れていない案件 5 件: A 14日 / B 8日 / 他 3 件 — 触れていない案件: P1 1 件 (最大 14日) / P3 2 件 (最大 8日)') / data attrs=`data-priority-buckets` 追加 (E2E)。priorityDetailSuffix (iter425) 経由で priority bucket > 1 時のみ append (単一 P 偏在冗長省略)。UX 卓越: a 発見可能性 (stale 度 + P bias 組合せで action priority 明確、P1 含有 = 最深刻度示唆) / b アクセシビリティ (SR full 文言読上げ) / c 状態網羅 (visible 短 / hover richer 2 層) / g 一貫性 (5 chip で 'summary — by-axis breakdown' pattern 揃い、iter386/408/414/423 と同手法 5 弾目)。16 insertions / 4 deletions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (at-risk-parents 25/25 + architecture 5/5 = 30/30) PASS。次 iter432 = 432%5=2 → ai-automation。
- ✅ [iter430 (isPathDescendantOf-集約)] **isPathDescendantOf を集約 (parentPath descendant check 2 callsite) (refactor, 1 commit, 3a9d5da)**: iter325 / iter330 / ... / iter425 と同じ「同 shape の散在を 1 file に」方針 29 弾目。descendants-progress (iter417) / at-risk-parents (iter427) の 2 callsite で同 shape の descendant check (`parentPath === parentFull || parentPath.startsWith(parentFull + '.')`) が散在していたので 1 関数に集約。新 export (lib/db/ltree-path.ts): `isPathDescendantOf(itemParentPath, parentFull)` → 直下 / 孫以降 → true / それ以外 → false (純粋、O(parentPath.length))。callsite 置換 (-7/+3 = -4 行 net): descendants-progress / at-risk-parents の inline 判定を helper 呼出に。caller benefits: ltree segment 境界の罠 ('aaa2.xxx' が 'aaa' の child に見えるが正しく否定) を 1 関数担保、新規 substrate 追加時の手書き不要、ltree path math は lib/db/ltree-path.ts に集中 (segment 境界特殊扱いも本 helper 内で完結)。動作変更ゼロ (descendants-progress 26/26 + at-risk-parents 25/25 + architecture 5/5 = 56/56 PASS)、test +5 件 (直下 / 孫 / 別 parent / segment 境界 'aaa2' / root parentFull) → ltree-path 8/8 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter431 = 431%5=1 → basics。
- ✅ [iter429 (at-risk-parents-by-priority)] **at-risk-parents に priority 軸 + by-priority 集計を追加 (ai-automation, 1 commit, 674ae75)**: iter386 / iter391 / iter406 / iter408 / iter414 と並ぶ「× priority」軸シリーズの **at-risk-parents 軸版** (6 弾目)。`AtRiskParentEntry` に `priority: PriorityKey` field を追加 (parent.priority を normalizePriority で正規化、null/範囲外は 4 集約)、新 `AtRiskParentsPriorityStats = {count, maxStaleDays}`、`computeAtRiskParentsByPriority(entries)` で bucket 化、`formatAtRiskParentsByPriorityJa(byPriority)` で '触れていない案件: P1 1 件 (最大 14日) / P3 2 件 (最大 12日)' 形式 (count=0 省略、全 0 → '触れていない案件 0 件')。iter386/408/414 と同じ '{label}: P1 ... / P3 ...' 形式。caller benefits: AI 朝 brief で P 別 bias 1 関数取得 / dashboard chip 3 層情報設計に bind 可能 (次 iter)。test +5 件 (priority=1 抽出 / 範囲外正規化 / by-priority 0件 / 複数P / 単一偏在) → 25/25 PASS、依存不変 (architecture 5/5)、typecheck/lint 緑 (warning baseline 1)。次 iter430 = 430%5=0 → refactor。
- ✅ [iter428 (dashboard-at-risk-parents-chip)] **dashboard に at-risk-parents chip を bind (basics, 1 commit, 3919487)**: iter427 substrate を Dashboard に bind、「子孫の最終更新が古い案件 (parent rollup)」を 1 行 chip で警告。parent-items-progress (進捗 list) chip と相補で「進捗 list」 vs 「停滞 list」 軸を分離。新 chip `dashboard-at-risk-parents-chip`: glyph 💤 + amber tone (warn)、text='触れていない案件 N 件: A 14日 / B 8日 / 他 K 件' (maxStaleDays 降順なので最古案件が先頭 = 注意必要順)、aria-label に '触れていない案件: ' prefix で SR 読上げ、entries=0 (全案件 active = 健全) で chip 非表示、data-at-risk-count attr (E2E)。既存 useItems 流用、新規 query なし。UX 卓越: a 発見可能性 (1 画面で 💤 警告、進捗 list と並ぶ = '進めている vs 放置' axis 分離 = macro 視点補完) / b アクセシビリティ (aria-label full list 読上げ) / c 状態網羅 (loading / 0件 / 1+件 / overflow) / g 一貫性 (DashboardChip / chipTone3Class('warn') / data attrs = slipDays / overdueActive / blockedItems の amber 系統と同型)。27 insertions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (at-risk-parents 15/15 + architecture 5/5 = 20/20) PASS。次 iter429 = 429%5=4 → ai-automation。
- ✅ [iter427 (at-risk-parents)] **pickAtRiskParents / formatAtRiskParentsBriefJa を追加 (ai-automation, 1 commit, e1f9a0f)**: 「子孫の最終更新が古い parent」を抽出する pure helper。iter359 (selectStuckWipItems = 個別 item 停滞) と iter419 (pickIncompleteParentItems = 進捗 list) を組み合わせた **at-risk parent 軸** — parent 自身は動いていなくても子孫が touch されていなければ案件停滞。3 軸使い分け: stuck-wip=個別 item / parent-items-progress=進捗 list / 本 helper=parent 単位 rollup (案件 macro)。新 module `src/features/item/at-risk-parents.ts`: `pickAtRiskParents(allItems, options?, today?)` で各 parent の active 子孫 (cancelled / done / deleted 除外) の中で最古 updatedAt を取得、`thresholdDays` (default 7) 以上で at-risk、parent done & deleted は除外、並びは maxStaleDays 降順 → title ja 昇順。`formatAtRiskParentsBriefJa(entries, limit=3)` で '触れていない案件 0 件' / '触れていない案件 N 件: A 14日 / B 8日 / 他 K 件' (formatTopWithOverflow 委譲、title 空は '(無題)')。iter379/382/407/409/412/419 と並ぶ「title list with overflow」 substrate シリーズの **at-risk-parents 軸** (7 軸目)。caller benefits: AI 朝 brief / dashboard chip / mobile 通知、parent 単位 rollup で「案件 macro」可視化。future-updatedAt fail-soft / 孫タスク集計含む。test +15 件 (空 / 14日 default / 全 < threshold / threshold オーバライド / done 子除外 / cancelled 子除外 / parent done / parent deletedAt / 複数 maxStaleDays 降順 / 孫集計 / 未来 skip + format 4 type) → 15/15 PASS、依存不変、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter428 = 428%5=3 → basics。
- ✅ [iter426 (subtasks-progress-summary-panel)] **subtasks panel 先頭に subtree 進捗 + activity hint summary を表示 (basics, 1 commit, 7e02dea)**: iter417 (descendants-progress) / iter424 (activity-hint) substrate を SubtasksPanel に bind。FEEDBACK_QUEUE「『この案件、今どんな感じ？』を 1 画面で一目化」 scope A 最小実装として subtask tab 開いた瞬間に「進捗 + 状態 hint」が読める panel を先頭追加。新 panel `data-testid='subtasks-progress-summary'`: 1 行目 📋 + activityHint (10 状態の 1 word) / 2 行目 (text-muted [11px]) formatDescendantsProgressJa 詳細 (進捗 60%: 完了 6 / 進行中 2 / blocked 1 / ...)、tone は isComplete=emerald / blocked > 0=amber / その他=slate (中立)、aria-live='polite' + aria-label full → SR は subtask 完了で音声通知 (進捗 feedback loop が SR でも完結)、data-pct-done attr (E2E)、total=0 で panel 非表示。既存 `useItems` 流用、新規 query なし、追加 fetch なし。UX 卓越: a 発見可能性 (tab 開いた直後 2 行で即読み、3 layer info: tab right count / panel top hint+breakdown / list 詳細) / b アクセシビリティ (aria-live SR feedback、aria-label 完全文言、visible 短く richer は a11y で) / c 状態網羅 (10 状態 + total=0 非表示) / d 速度感 (cache hit) / g 一貫性 (配色 emerald 達成 / amber 警告 / slate 中立 = 既存 chip / kanban-accent / dep-readiness 整合)。38 insertions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (subtasks-panel-helpers 15/15 + descendants-progress 26/26 + architecture 5/5) PASS、59/59 全件 PASS。次 iter427 = 427%5=2 → ai-automation。
- ✅ [iter425 (priorityDetailSuffix-集約)] **priorityDetailSuffix を集約 (priorityBuckets > 1 ? — chip 6 callsite) (refactor, 1 commit, 8c250e5)**: iter325 / iter330 / ... / iter420 と同じ「同 shape の散在を 1 file に」方針 28 弾目。dashboard chip の「priority breakdown を bucket > 1 の時だけ append」 pattern が 6 callsite で同 shape 三項演算子で散在していたので 1 関数に集約。新 export (priority.ts): `priorityDetailSuffix(bucketCount, formatter)` で bucket > 1 → ` — ${formatter()}` / bucket <= 1 → 空文字 (冗長省略)、formatter は遅延 evaluation (bucket <= 1 で呼ばれない、format cost 回避)、separator 'ハードコード ' — '、suffix-only API で `${summary}${suffix}` / `prefix + suffix` 両 pattern 対応。callsite 置換 (6 箇所、dashboard-view.tsx): slipDays / blockedItems / overdueActive / mustOverdue (append-style) + wipStuck / mustStale / mustStuckWip (full-replace style) を統一。残 2 callsite (line 723 stale-urgent `null` 返し / line 484 line-comment) は別 shape / code ではないので除外。動作変更ゼロ (出力文字列完全互換、affected dashboard chip detail 文言不変)。test +4 件 (bucket=0 / =1 / >=2 / formatter 遅延 evaluation 検証) → 24/24 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後の 'priority bucket > 1 拡張' chip も 1 行で書ける。次 iter426 = 426%5=1 → basics。
- ✅ [iter424 (descendants-activity-hint)] **formatDescendantsActivityHintJa を追加 (ai-automation, 1 commit, f4b165f)**: iter417 で追加した `summarizeDescendantsProgress` 出力から「この parent の状態を 1 行で表現する action hint」を返す pure helper。AI 朝 brief / pm-agent prompt / dashboard widget が「いま何が起きていて、次の action は何か」を 1 関数で出すための short hint substrate。`formatDescendantsProgressJa` (数値情報) と対比して **意味付け** を提供 ('停滞気味' / 'ブロック中' / '仕上げ間近')。判定優先順位 (上から評価、最初に当てはまる): 1.子タスクなし / 2.全キャンセル / 3.完了済 / 4.ブロック中 (前提解消が先) / 5.未着手 / 6.スタートが遅い / 7.停滞気味 / 8.前半遅延 / 9.順調進行中 / 10.仕上げ間近。判定優先 4 (blocked) > 5 (未着手): blocker 解消が action priority だから。caller benefits: AI 朝 brief 1 行 hint / dashboard chip parent 表記に状態 word 併記 / Slack alert 1 文字列 。iter287/294/309 と並ぶ「item axis label」シリーズの descendants 軸版。test +10 件 (10 判定 path) → 26/26 PASS、依存 (architecture 5/5 + parent-items-progress 17/17) 不変、typecheck/lint 緑 (warning baseline 1)。次 iter425 = 425%5=0 → refactor。
- ✅ [iter423 (parent-items-progress-aggregate-aria)] **parent-items-progress chip aria-label に集約 line を append (basics, 1 commit, a103b3c)**: iter422 substrate を Dashboard の parent-items-progress chip aria-label / title (SR / hover 経路) に bind、iter386 / iter408 / iter414 と同手法で「3 層情報設計」を 4 chip 目で揃える (must-overdue / slip-days / blocked-items / parent-items-progress)。3 層: visible=summary (個別 title 上位 3 件、変更なし) / hover&aria=`${summary} — ${aggregateLine}` ('進行中 5 件: 平均 35%, 停滞 2 / 順調 2 / 仕上げ 1') / data attrs=`data-parent-avg-pct` / `data-parent-stuck-count` 追加 (E2E selector)。UX 卓越: a 発見可能性 (個別 + 全体集約 hover で俯瞰、'停滞 2 件' 即読み = action priority 明確) / b アクセシビリティ (SR は full 文言読上げ) / c 状態網羅 (visible 短 / hover richer 2 層、画面密度保持 + 情報量 2 層) / g 一貫性 (4 chip で 'summary — by-axis breakdown' pattern 完全に揃う、iter386/408/414 と同手法 4 弾目)。14 insertions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (parent-items-progress 17/17 + architecture 5/5 = 22/22) PASS。次 iter424 = 424%5=4 → ai-automation。
- ✅ [iter422 (parent-items-aggregate)] **summarizeParentItemsAggregate / formatAggregateParentItemsJa を追加 (ai-automation, 1 commit, 7b30a8c)**: iter419 で追加した `pickIncompleteParentItems` 出力 list を 1 数値 / カテゴリ別件数で要約する集計 substrate。AI brief / pm-agent / dashboard が「案件 5 件: 平均 35%, 停滞 2 / 順調 2 / 仕上げ 1」のように 1 行で全体感を出せる。新 export (parent-items-progress.ts): `ParentItemsProgressTier = 'stuck'(<25) | 'slow'(25-49) | 'healthy'(50-74) | 'almostDone'(75-99)` (100 は entries 含まれない、pickIncompleteParentItems 除外済)、`summarizeParentItemsAggregate(entries)` → `{count, avgPctDone, byTier}`、`formatAggregateParentItemsJa(agg)` → '進行中 N 件: 平均 X%, 停滞 K / 前半遅延 L / 順調 M / 仕上げ N' (count=0 tier 省略、空は '進行中 0 件')。iter287/294/309/279/412/417/419 と並ぶ「item axis stat」シリーズの **parent-items aggregate 軸** (個別 vs 集約)。caller benefits: AI 朝 brief 1 関数取得 / dashboard chip 固定長 / mobile 通知 / list (brief) と並存。test +6 件 (空 / pct=30 → slow / tier 境界 4×2 / 混在全 4 bucket / count=0 省略 / Math.round 整数化) → 17/17 PASS、依存 (descendants-progress 16/16) 不変、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter423 = 423%5=3 → basics。
- ✅ [iter421 (dashboard-parent-items-progress-chip)] **dashboard に parent-items-progress chip を bind (basics, 1 commit, 6306206)**: iter419 で追加した `pickIncompleteParentItems` / `formatParentItemsProgressBriefJa` substrate を Dashboard に bind、「workspace 内で進行中の案件 (parent + children) の進捗 list」を 1 行 chip で俯瞰。新 chip `dashboard-parent-items-progress-chip`: glyph 📋 + neutral tone (chipTone3Class('neutral')、warning ではなく info、blocked-items の amber と区別)、text='進行中: A 30% (3/10) / B 60% (6/10) / 他 K 件' (pctDone 昇順なので低進捗が先頭 = 注意必要順)、aria-label に '案件進捗: ' prefix で SR 読上げ、entries=0 (parent ゼロ or 全達成) で chip 非表示。既存 `useItems` 流用、新規 query なし。UX 卓越: a 発見可能性 (dashboard 1 画面で進行中案件 + % が一目、注意必要順) / b アクセシビリティ (aria-label full list) / c 状態網羅 (loading / 0件 / 1+件 / overflow) / g 一貫性 (DashboardChip / chipTone3Class / data attrs 同型、neutral tone は warning 系と差別化 = 進捗は状況把握 chip)。31 insertions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (parent-items-progress 11/11 + descendants-progress 16/16 + architecture 5/5 = 32/32) PASS。次 iter422 = 422%5=2 → ai-automation。
- ✅ [iter420 (titleOrUntitled-集約)] **titleOrUntitled を集約 ("(無題)" fallback 11 callsite) (refactor, 1 commit, de89541)**: iter325 / iter330 / ... / iter415 と同じ「同 shape の散在を 1 file に」方針 27 弾目。features/item / features/item-dependency / lib/format-list の 11 callsite で 3 variant の `'(無題)'` fallback (variant 1: `?? '(無題)'` 5箇所 / variant 2: `.length > 0 ? : '(無題)'` 3箇所 / variant 3: `typeof + length > 0` 完全 defensive 3箇所) が散在していたので 1 関数に集約。新 export (lib/format-list.ts): `titleOrUntitled(title)` で null/undefined/空文字 → '(無題)' / それ以外そのまま (空白 1 文字も実値扱い)。統一方針は **完全 defensive** (variant 3) で、variant 1 callsite (freshly-done / startable-items / stale-items / brief-summary / must-risk) の「空文字 title が空ラベルで通る silent bug」が同時解消 (AI brief / dashboard tooltip / pm-agent prompt の空 prefix 回避)。callsite 置換 (-46 / +78 = +32 行 主に export + tests +6 件)、`top-items-by-time-chip.tsx` の Map.get 経路 (shape 違い、UI 単 callsite) は集約対象外。test +6 件 (null / undefined / 空文字 / 実値 / 1 文字 / 空白 1 文字) → 27/27 PASS、affected 12 test file (format-list / parent-items-progress / blocked-items / wip-stuck / recent-completed / freshly-done / startable-items / stale-items / must-stale / must-risk / brief-summary / architecture) → 238/238 PASS、動作変更ゼロ except silent bug fix。typecheck/lint 緑 (warning baseline 1)。次 iter421 = 421%5=1 → basics。
- ✅ [iter419 (parent-items-progress)] **pickIncompleteParentItems / formatParentItemsProgressBriefJa を追加 (ai-automation, 1 commit, 4d715b5)**: iter417 (summarizeDescendantsProgress、1 parent 単位) を全 workspace に scale up した workspace-axis 版。AI 朝 brief / pm-agent prompt / dashboard widget が「いま動いている案件 (parent task with children) の進捗 list」を 1 関数で出せる ('進行中: A 30% (3/10) / B 60% (6/10) / 他 2 件')。新 module `src/features/item/parent-items-progress.ts`: `pickIncompleteParentItems(allItems)` で candidate parent 毎に descendants-progress 集計、total=0 (末端) / isComplete (達成済) / parent.deletedAt は除外、並びは pctDone 昇順 (低進捗が先 = 注意が必要な順) → title ja 昇順、`formatParentItemsProgressBriefJa(entries, limit=3)` で 0件 → '進行中の案件 0 件' / 1+ 件 → '進行中: A 30% (3/10) / B 60% (6/10) / 他 K 件' (formatTopWithOverflow iter365 委譲、title 空は '(無題)' 正規化)。iter379/382/407/409/412 と並ぶ「title list with overflow」 substrate シリーズの **parent-items-progress 軸** (6 軸目)。caller benefits: AI 朝 brief 1 関数取得 / dashboard chip 固定長 / 既存 descendants-progress (1 parent) と並存 (単 vs 全)。test +11 件 (空 / 末端除外 / 1 parent 5 子 / 全 done parent 除外 / deletedAt / 複数 parent pctDone 昇順 / title 同点 ja 昇順 / format 4 type) → 11/11 PASS、依存 (descendants-progress 16/16) 不変、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter420 = 420%5=0 → refactor。
- ✅ [iter418 (subtasks-tab-progress-badge)] **子タスクタブに「進捗 6/10」 badge を追加 (basics, 1 commit, dbd3492)**: iter417 で追加した `summarizeDescendantsProgress` substrate を ItemEditDialog に bind、tab を開かずに subtree 進捗が一瞥で伝わる UX (deps-tab badge iter416 と同型)。データ取得は `useItems(workspaceId)` を ItemEditDialogInner に追加 (SubtasksPanel と queryKey 共通、1 query dedupe)、`summarizeDescendantsProgress({id, parentPath}, allItems)` を useMemo で計算、loading / total=0 で badge 非表示。UI: 子タスク TabsTrigger 内に小 chip `done/total` (例 `6/10`)、isComplete (pctDone=100) は emerald tone (達成)、それ以外は slate (中立、amber は dependency-blocked 系専用なので進捗系は別 tone)、`data-subtask-total` / `data-subtask-done` attr 追加 (E2E 用)、動的 aria-label で SR が「子タスク タブ — 進捗 60% (完了 6 / 全 10 件)」を読上げ。UX 卓越: a 発見可能性 (subtree 進捗が tab open なしで瞬時把握、TickTick / Linear 風) / b アクセシビリティ (aria-label full 文言、chip は aria-hidden) / c 状態網羅 (loading / total=0 / 進行中 / 完了 emerald の 4 状態) / g 一貫性 (deps-tab badge iter416 と同 placement / padding / 形、「タブ右の小 chip = サマリ情報」pattern が 2 tab で揃う)。42 insertions、機能追加なし、shadcn 編集なし、subtasks tab 内容も無変更。typecheck/lint 緑 (warning baseline 1)、affected pure tests (descendants-progress 16/16 + architecture 5/5 = 21/21) PASS。次 iter419 = 419%5=4 → ai-automation。
- ✅ [iter417 (descendants-progress)] **summarizeDescendantsProgress / formatDescendantsProgressJa を追加 (ai-automation, 1 commit, dc23854)**: FEEDBACK_QUEUE「『この案件、今どんな感じ？』『どう着地させるつもり？』を 1 画面で一目化」の **scope A 最小** substrate。ItemEditDialog 新 tab「サマリ」/ Kanban カード disclosure / dashboard widget が「子孫タスクの進捗 %」を 1 関数で取り出せる。新 module `src/features/item/descendants-progress.ts`: `summarizeDescendantsProgress(parent, allItems)` → `{total, done, inProgress, blocked, todo, cancelled, unknown, pctDone, isComplete}`。子孫判定は `fullPathOf(parent)` を prefix とする全 Item (直下 + 孫以降、深さ無制限)、`pctDone = done / (total - cancelled)` 整数化 (cancelled は分母から除外 = ユーザ感覚に合う)、`deletedAt != null` 除外、`normalizeStatus` で 6 値正規化、子孫ゼロ → 全 0 + EMPTY_RESULT 構造。`formatDescendantsProgressJa(p)` → '子タスクなし' / '子 N 件 (全キャンセル)' / '進捗 60%: 完了 6 / 進行中 2 / blocked 1 / todo 1 (全 10 件)' (count > 0 のみ append、順序: done → 進行中 → blocked → todo → キャンセル → 不明)。iter287/294/309/279/412 と並ぶ「item axis stat」シリーズの **descendants-progress 軸** (階層的集計、ltree 利用)。caller benefits: parent task 1 件で「子孫の進捗 %」取得、ItemEditDialog 「サマリ」 tab / Goal panel / Kanban カード で再利用可能 (UI bind は次 basics iter)。test +16 件 (子孫ゼロ / 全 todo / 混在 / 全 done / cancelled 分母除外 / 全 cancelled / 孫集計 / 別 parent 除外 / deletedAt / 未知 status / parent 自身除外 / format 5 type) → 16/16 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter418 = 418%5=3 → basics。
- ✅ [iter416 (deps-tab-blocker-badge)] **依存タブに「未完了 blocker N 件」 badge を追加 (basics, 1 commit, 24c65ca)**: ItemEditDialog の「依存」TabsTrigger に未完了 blocker 件数を amber chip で表示、タブを開かなくても「いま依存ブロックされているか」が一瞥で伝わる UX。データ取得は `useItemDependencies(item.id)` を ItemEditDialogInner に追加 (ItemDependenciesPanel と queryKey 共通 dedupe、1 query で 2 caller)、`summarizeDependencyReadiness` (iter411 substrate) で `isBlocked` / `openBlockedByCount` 取出、loading 中は badge 非表示 (ちらつき防止)。UI: isBlocked=true 時のみ tab label の右に小さな丸 chip (amber-100 bg / amber-800 text / amber-300 ring / 4 wh)、TabsTrigger に `data-blocked` attr + 動的 aria-label ('依存タブ — 未完了の前提 N 件' / '依存タブ')、chip 自体は aria-hidden + title (hover) で重複読み上げ防止。UX 卓越: a 発見可能性 (TickTick / Linear 風 未開封 dot 同型、tab 開かず blocker 認知) / b アクセシビリティ (SR が aria-label で full 文言、chip は aria-hidden) / c 状態網羅 (loading / not-blocked / blocked の 3 状態) / g 一貫性 (amber tone は iter411 dep-readiness chip / iter413 dashboard blocked-items chip と同色、dependency 系 warning 統一)。29 insertions、機能追加なし、shadcn 編集なし、依存タブ内容も無変更。typecheck/lint 緑 (warning baseline 1)、affected pure tests (readiness 20/20 + blocked-items 18/18 + priority 17/17 + architecture 5/5 = 60/60) PASS。次 iter417 = 417%5=2 → ai-automation。
- ✅ [iter415 (countNonEmptyCountPriorityBuckets-集約)] **countNonEmptyCountPriorityBuckets を集約 ((s)=>s.count>0 8 callsite) (refactor, 1 commit, 26f4029)**: iter325 / iter330 / ... / iter410 と同じ「同 shape の散在を 1 file に」方針 26 弾目。`countNonEmptyPriorityBucketsBy(byPriority, (s) => s.count > 0)` の同 predicate が dashboard-view.tsx 内 8 callsite (must-overdue / must-stuck-wip / must-stale / overdue-active / slip-days / stuck-wip / stale-urgent / blocked-items) で重複していたので 1 関数に集約。新 export (priority.ts): `countNonEmptyCountPriorityBuckets(byPriority)` — `Record<PriorityKey, {count: number}>` shape 専用の薄いショートカット (既存 `countNonEmptyPriorityBuckets`(`{total: number}` 用) と対称 API、内部で `countNonEmptyPriorityBucketsBy` に委譲)。callsite 置換 (-12 / +9 = -3 行 net): 7 callsite で `(s) => s.count > 0` lambda を消去 + iter414 blocked-items chip の 4 行 inline 展開 (`(byPriority[1].count > 0 ? 1 : 0) + ...`) も 1 行 helper 呼出に置換 (PRIORITY_ORDER 不変性が helper 経由で担保)。`countNonEmptyPriorityBucketsBy` (generic) は `{score !== null}` / `{debtCount > 0}` の 2 callsite 残存。動作変更ゼロ (priority 14/14 + blocked-items 18/18 + architecture 5/5 = 37/37 PASS)、今後追加される `s.count > 0` 形 substrate も 1 行 helper 呼出で書ける。typecheck/lint 緑 (warning baseline 1)。次 iter416 = 416%5=1 → basics。
- ✅ [iter414 (blocked-items-by-priority)] **blocked-items に priority 軸 + dashboard chip aria-label に bind (ai-automation, 1 commit, 908d99f)**: iter386 / iter391 / iter406 / iter408 と並ぶ「× priority」軸シリーズの dependency-blocked 軸版 (5 弾目)、dashboard chip の 3 層情報設計 (visible / hover / aria) を完成。`WorkspaceBlockedItem` に `priority: PriorityKey` field を追加 (caller が item.priority を渡せば normalizePriority で 1-4 に正規化、null/範囲外は 4 集約)、新 `BlockedItemsPriorityStats = {count, maxOpenBlockerCount}` (iter408 SlipDaysStats と対称、count=0 なら max=null)、`computeBlockedItemsByPriority(blocked)` で bucket 化、`formatBlockedItemsByPriorityJa(byPriority)` で '依存ブロック: P1 1 件 (最大 3 件待ち) / P3 2 件 (最大 2 件待ち)' 形式 (count=0 bucket は省略、全 0 → '依存ブロック 0 件')。dashboard-view.tsx の blocked-items chip aria-label / title を 3 層設計に: visible chip text=summary (変更なし) / hover&aria=summary + ' — ' + by-priority breakdown (priorityBuckets > 1 のみ append、単一 P 偏在は冗長省略 = iter408 と同手法) / data-priority-buckets attr で E2E selector 追加。これで must-overdue / must-stuck-wip / must-stale / slip-days / blocked-items の 5 chip で 3 層情報設計が完全に揃う (= iter386/391/406/408 と同型 5 弾目)。UX 卓越: a 発見可能性 (priority breakdown を hover 確認可、P1 偏在は最深刻度示唆) / b アクセシビリティ (SR で 'P1 含有 = 最深刻度' 読取可) / g 一貫性 (5 chip 同パターン揃い)。test +6 件 (priority=1 / 範囲外正規化 / by-priority 0件 / 複数P / 単一偏在 / count=0 省略) → 18/18 PASS、affected (readiness 20/20) 不変、architecture 5/5 PASS。typecheck/lint 緑 (warning baseline 1)。次 iter415 = 415%5=0 → refactor。
- ✅ [iter413 (dashboard-blocked-items-chip)] **dashboard に blocked-items chip を bind (basics, 1 commit, 5c02ebc)**: iter412 で追加した `pickWorkspaceBlockedItems` / `formatBlockedItemsBriefJa` substrate を Dashboard に bind。「workspace 全体で blocked になっている Item の上位 3 件」を 1 行 chip で俯瞰できる。新 chip `dashboard-blocked-items-chip`: glyph 🔒 + amber tone (warn) で「依存待ちで進められない」を warning 色で警告、text=formatBlockedItemsBriefJa 出力 (limit=3、超過は '他 N 件')、aria-label に '依存ブロック: ' prefix で SR が full list 読上げ、entries=0 で chip 非表示で UI 静か。データ取得は既存 `useWorkspaceBlocksDependencies` (Gantt 依存線描画で使用済) を流用、新規 query 追加なし。slip-days / overdue-active / wipStuck と相補で「進められない原因」軸を分離可視化、dep-readiness chip (item 単位、ItemEditDialog 内) より workspace 鳥瞰的。UX 卓越: a 発見可能性 (1 画面で blocked 上位 3 件 + 待ち件数 + 🔒 glyph) / b アクセシビリティ (aria-label 充実) / c 状態網羅 (loading / 0件 / 1+ / overflow) / g 一貫性 (DashboardChip / chipTone3Class / data attrs が他 chip と完全同型)。35 insertions、機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、affected pure tests (blocked-items 12/12 / readiness 20/20) PASS、architecture 5/5 PASS。次 iter414 = 414%5=4 → ai-automation。
- ✅ [iter412 (blocked-items-substrate)] **pickWorkspaceBlockedItems / formatBlockedItemsBriefJa を追加 (ai-automation, 1 commit, 4ac7366)**: workspace 横断「いま blocked になっている Item」を AI brief / dashboard widget / pm-agent prompt に出すための pure substrate。`summarizeDependencyReadiness` (readiness.ts) は **1 Item 単位** (ItemEditDialog 経路) だが、本 helper は (Items + blocks edges) を組み合わせて **workspace 全体の blocked Item リスト** を返す。新 module `src/features/item-dependency/blocked-items.ts`: `pickWorkspaceBlockedItems(items, blocksEdges)` → `[{itemId, title, openBlockerCount, totalBlockerCount}]` を `openBlockerCount desc` → title ja 昇順 (stable)、`deletedAt != null` / 自分が `doneAt != null` / open=0 は除外、blocker (fromItem) が doneAt!=null も open に数えない、`formatBlockedItemsBriefJa(blocked, limit=3)` → 0 件 sentinel `'blocked 0 件'` / 1+ 件 `'blocked N 件: A [3 件待ち] / B [1 件待ち] / 他 K 件'` (formatTopWithOverflow iter365 委譲、title 空は `(無題)`)。iter379/382/407/409 と並ぶ「title list with overflow」 substrate シリーズの **dependency 軸版** (5 軸目)。caller benefits: AI 朝 brief / dashboard chip / mobile 通知 で固定長 layout、既存 `useWorkspaceBlocksDependencies` + `useItems` 出力をそのまま渡せる shape。test +12 件 (空 / 1件 / 完了済 blocker / 2 edges 1+1 / self done / deletedAt / sort desc → ja 昇順 / 孤立 edge / format 4 type) → 12/12 PASS、affected (readiness 20/20) 不変、architecture 5/5 PASS。typecheck/lint 緑 (warning baseline 1)。次 iter413 = 413%5=3 → basics。
- ✅ [iter411 (dep-readiness-graphical)] **dep-readiness chip を Lucide icon + tone helper に集約 (basics, 1 commit, 8616519)**: graphical 波及シリーズ (status-badge iter255 / activity iter298 / dependency-arrow iter301 / kanban-accent iter303 / notification iter322 / sprint-progress iter316) と一貫させるべく、`item-dependencies-panel.tsx` 先頭の readiness chip を inline ternary + unicode glyph (✓/⏸/·) → `getDependencyReadinessVisual` (新 pure helper) + Lucide `CircleCheck` / `CirclePause` / `Circle` に統一。新 export (readiness.ts): `dependencyReadinessTone(r)` → 'blocked' (未完了 blocker > 0) / 'idle' (全件 0) / 'ready' (それ以外) の 3 値、`getDependencyReadinessVisual(r)` で `{tone, bgClass, textClass, ringClass, iconKey, toneLabel}` を返す (`STATUS_VISUAL_MAP` / `sprintProgressTone` と対称 API)。toneLabel ('ブロック中' / '依存なし' / '着手可能') を aria-label に append → SR は「依存サマリ (着手可能): 後続 2 件」のように tone も読上げ。`data-tone` attr で Playwright / E2E selector 追加可能。UX 卓越: a 発見可能性 (旧 unicode は font 依存で意味不明瞭、Lucide で意味明示) / b アクセシビリティ (toneLabel が SR / aria に乗る) / g 一貫性 (graphical 波及シリーズ全 6 chip 同パターン揃い)。test +6 件 (idle / blocked / ready 3 type / 関連のみ ready / 後続のみ ready / blocked 優先) → 20/20 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter412 = 412%5=2 → ai-automation。
- ✅ [iter410 (formatTitleDaysListJa-集約)] **formatTitleDaysListJa を lib/format-list に集約 (refactor, 1 commit, 64bbafb)**: iter325 / iter330 / ... / iter405 と同じ「同 shape の散在を 1 file に」方針 25 弾目。iter379 / iter382 / iter407 / iter409 の 4 callsite で完全同 shape (= getDays accessor + prefix のみ差異) の "title list with overflow" formatter が重複していたので 1 関数に集約。新 export (lib/format-list.ts に追加): `formatTitleDaysListJa<E>(entries, getDays, prefix, limit=3): string` (entries 空 → `'{prefix} 0 件'` / 1+ → `'{prefix}: title D日 / ...'` / title 欠落 (無題) / limit 超過は formatTopWithOverflow 委譲)。callsite 置換 (-44 行 / 4 file): must-overdue / overdue-active / slip-days / stale-items。動作変更ゼロ (出力文字列完全互換、affected test 103/103 PASS)、helper +35 行 (含 docstring)、今後追加される同 shape formatter も 1 行 helper 呼出で書ける。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter411 = 411%5=1 → basics。
- ✅ [iter409 (formatStaleItemsTitlesJa)] **formatStaleItemsTitlesJa を追加 — overflow 制御版 stale list (ai-automation, 1 commit, 7f648f1)**: iter379 (formatMustOverdueTitlesJa) / iter382 (formatOverdueActiveTitlesJa) / iter407 (formatSlipDaysTitlesJa) と同シリーズ — stale-items 軸の "title list with overflow" formatter (= 既存 formatStaleItemsSummary に limit + 'overflow N 件' を追加した変種)。新 export: `formatStaleItemsTitlesJa(entries, limit=3)` で '古参: 提出書類 14日 / 連絡 10日 / 他 1 件' (limit 超 / title 欠落 (無題) / 0 件 sentinel '古参 0 件')、prefix '古参' で stale 軸を表現 (= must-stale の 'MUST 古参' から MUST 部分を取り除いた汎用版)。caller benefits: AI 朝 brief で stale 行を limit で長さ制御 / dashboard tooltip / mobile 通知 で固定長 layout / 既存 formatStaleItemsSummary と並存 (= overflow 有無で使い分け)。これで iter379 / iter382 / iter407 / iter409 で 4 軸 (must-overdue / overdue-active / slip-days / stale-items) で title list with overflow パターンが揃った。test +4 件 (24/24 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter410 = 410%5=0 → refactor。
- ✅ [iter408 (slip-days-chip-titles)] **slip-days chip の aria-label / title に top items を append (basics, 1 commit, ea0ee26)**: iter407 で追加した `pickSlipDaysItems` / `formatSlipDaysTitlesJa` を Dashboard の slip-days chip の aria-label / title (= SR / hover 経路) に bind。iter383 (overdue-active) / iter386 (must-overdue) / iter391 (must-stuck-wip) / iter406 (must-stale) と symmetric な拡張、これで dashboard の「3 層情報設計」パターンが retrospective 軸 (slip-days) でも揃い、5 chip 完成。3 層情報設計 (slip-days): visible='遅延: 5 件 (平均 3.4日 / 中央値 2日 / 最大 12日)' / hover&aria=同 + ' — P1 3 件 (最大 14日) / P3 2 件 (最大 2日)' + ' — 遅延完了: 提出書類 14日 / 報告 5日'。priorityBuckets > 1 / entries.length > 0 の時のみ append (= 0 件 fallback で過剰冗長 suffix 回避)。UX 卓越: a 発見可能性 (slip-days の P 別 + 具体名 hover 確認可、actionable retrospective) / b アクセシビリティ (SR で具体名 + 遅延日数読上げ) / g 一貫性 (5 chip 揃った: overdue-active / must-overdue / must-stuck-wip / must-stale / slip-days)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、slip-days 29/29 PASS、architecture 5/5 PASS。次 iter409 = 409%5=4 → ai-automation。
- ✅ [iter407 (pickSlipDaysItems)] **pickSlipDaysItems / formatSlipDaysTitlesJa を追加 (ai-automation, 1 commit, da591f7)**: iter379 (pickMustOverdueItems) / iter382 (pickOverdueActiveItems) と同シリーズ — slip-days 軸の "具体名 + 遅延日数" 取出 helper。computeSlipDays は stats (count / avg / median / max) のみだったが items 一覧 (具体名 + slipDays) を返す軸が欠けていた。新 export (`slip-days.ts` に追加): `SlipDaysEntry<T> = {item, slipDays}` (slipDays は ceil((doneAt - dueDateEnd) / day)) / `pickSlipDaysItems(items, options?)` (computeSlipDays と filter ロジック互換、slipDays 降順 stable 並び、options.since で window 制限可) / `SlipDaysWithTitle = SlipDaysFields & {title?}` / `formatSlipDaysTitlesJa(entries, limit=3)` で '遅延完了: 提出書類 14日 / 報告 5日' (iter379 / iter382 と同 vocabulary、prefix を '遅延完了' = 過去形 = retrospective 軸 に差し替え)。caller benefits: AI 回顧 brief「先週 遅延完了: 提出書類 14日 — 学習材料」 / dashboard slip-days chip の aria-label / title に top items を append (= 次 iter で iter381 / iter383 / iter386 / iter391 / iter406 と同手法で bind 候補) / pm-agent 過去 N 日の遅延 retrospective queue。test +9 件 (29/29 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter408 = 408%5=3 → basics。
- ✅ [iter406 (must-stale-chip-priority)] **must-stale chip の aria-label / title に priority breakdown を append (basics, 1 commit, 9902528)**: iter404 で追加した `computeMustStaleByPriority` / `formatMustStaleByPriorityJa` を Dashboard の must-stale chip の aria-label / title (= SR / hover 経路) に bind。iter386 (must-overdue) / iter391 (must-stuck-wip) と symmetric な拡張、3 chip で 3 層情報設計揃い、MUST 系警報 chip の網羅性が完成。3 層情報設計: visible='MUST 古参: 2 件 (A 14 日前 / B 8 日前)' / hover&aria=同 + ' — P1 1 件 (最古 14日) / P3 1 件 (最古 8日)'。priorityBuckets > 1 の時のみ priority breakdown を append (= 単一 P 偏在は冗長省略)。data-priority-buckets attr (iter393 同) で E2E test selector を追加 (= 3 chip 同手法揃い)。UX 卓越: a 発見可能性 (P 別 escalation 順位 hover 確認可) / b アクセシビリティ (SR で 'P1 含有' = 最深刻度示唆) / c 状態網羅 (priority 単一偏在 省略) / g 一貫性 (iter386 / iter391 と完全同パターン、MUST 系 3 chip 揃った)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、must-stale 18/18 PASS、architecture 5/5 PASS。次 iter407 = 407%5=2 → ai-automation。
- ✅ [iter405 (filterMustOnly-集約)] **filterMustOnly を抽出 — must-\* 共通の isMust pre-filter 集約 (refactor, 1 commit, af4a36d)**: iter325 / iter330 / ... / iter400 と同じ「同 shape の散在を 1 file に」方針 24 弾目。must-stuck-wip / must-overdue (computeMustOverdue + pickMustOverdueItems) / must-stale の 4 callsite で同 shape の `for + isMust pre-filter` (4 行 / 各) が重複していたので、1 行 helper 呼出に集約。新 module `src/features/item/must-filter.ts`: `MustFilterFields` (= `{isMust}` structural subset) / `filterMustOnly<T>(items): T[]` (元配列順 stable、null/undefined item も defensive 除外)。callsite 置換 (-12 行 / 3 file)、動作変更ゼロ (出力配列順序 / 件数 / 内容完全互換、affected 62/62 PASS)、test +5 件 (filter 4 + null defensive 1)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter406 = 406%5=1 → basics。
- ✅ [iter404 (must-stale-by-priority)] **computeMustStaleByPriority / formatMustStaleByPriorityJa を追加 (ai-automation, 1 commit, 649d815)**: iter384 (must-overdue-by-priority) / iter389 (must-stuck-wip-by-priority) と同シリーズ — must 系 by-priority substrate 3 弾目、「× priority」軸 13 弾目。「P1 MUST 古参 2 件 (最古 14日) / P3 MUST 古参 1 件 (最古 8日)」のように MUST stale の priority bias を可視化。新 export (`must-stale.ts` に追加): `MustStaleByPriorityStats {count, oldestStaleDays | null}` / `computeMustStaleByPriority` (pickMustStaleItems を bucketByPriorityWith 経由で priority 別再適用) / `formatMustStaleByPriorityJa` (iter385 集約済 `formatPriorityBucketsCountWithDays` を 7 callsite 目で利用、'最古' label = stale-items / overdue-active / must-overdue と vocabulary 統一)。caller benefits: AI 朝 brief「P1 MUST 古参 14 日 → 最優先 nudge」 / pm-agent priority 別 watch list / dashboard must-stale chip の priority tooltip 同梱候補 (= iter386/391 と同手法で 3 chip 揃う候補)。test +5 件 (18/18 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter405 = 405%5=0 → refactor。
- ✅ [iter403 (must-stale-chip)] **must-stale chip を Dashboard に追加 — 忘れられた MUST 警報 (basics, 1 commit, 9d894ea)**: iter402 整備済 must-stale substrate を Dashboard chip に bind。MUST かつ 7+ 日触られていない sleeper 違反を 1 目視可能に。MVP「MUST 絶対落とさない」(CLAUDE.md) を「忘れられた MUST」(= 期限関係なく時間軸 sleeper) として警告する dashboard 専用 chip。must-overdue (期限超過) / must-stuck-wip (進行中停滞) と相補で 3 chip 揃い、MUST 系警報の網羅性向上。実装: mustStale useMemo (+10 行) / chip JSX (+15 行) を mustOverdue chip 直後配置 (深刻度順)、glyph ⏳ (砂時計 metaphor)、tone 固定 red 弱 (border-red-300 / bg-red-50、must-overdue より弱め = sleeper 表現)、attention (iter376 で追加した alert + assertive)、data-severity / data-must-stale-count attr。UX 卓越: a 発見可能性 (忘れられた MUST chip 出現で即注意誘導) / b アクセシビリティ (SR で具体名読上げ + alert 割込み) / c 状態網羅 ('critical' / 'idle' 2 状態、idle で chip 非表示) / g 一貫性 (他 must-\* chip と同 pattern)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、must-stale 13/13 PASS、architecture 5/5 PASS。次 iter404 = 404%5=4 → ai-automation。
- ✅ [iter402 (must-stale)] **pickMustStaleItems / formatMustStaleJa / mustStaleSeverity を追加 — MUST × stale 2 軸交差 (ai-automation, 1 commit, 63d771a)**: iter299 selectStaleItems と must-hygiene の 2 軸交差 combinator (= 忘れられた MUST item)。iter364 must-stuck-wip / iter372 must-overdue と並ぶ MUST 系 2 軸交差 substrate 3 弾目。単独 selectStaleItems や単独 must-hygiene より signal 強度が高い: 「絶対落とせない MUST が 7+ 日触られていない = 完全に忘れられている」 = MVP「MUST 絶対落とさない」原則の sleeper 違反 (他の must-_ combinators が捕捉できない時間軸の警告、dueDate 未設定でも検知可)。新 module `src/features/item/must-stale.ts`: `MustStaleFields = StaleItemFields & {isMust}` / `pickMustStaleItems(items, options?, today?)` で isMust=true + stale (default 7 日) な StaleItemEntry[] (selectStaleItems を re-use、iter390 と同じ "pre-filter → 委譲" pattern、dup logic 0) / `formatMustStaleJa(entries, limit=3)` で 'MUST 古参: 2 件 (A 10 日前 / B 8 日前)' / `MustStaleSeverity = 'critical' | 'idle'` (MUST 該当時点で最深刻、2 値)。caller benefits: AI 朝 brief「MUST 古参 2 件 — 即対応」 / pm-agent watch list 4 弾目 (must-overdue / must-at-risk / must-stuck-wip と同列) / dashboard 専用 chip 候補 (red、'⏳' glyph 候補、他 must-_ chip と区別)。test +13 件 (13/13 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter403 = 403%5=3 → basics。
- ✅ [iter401 (severityIcon-field)] **brief-summary に severityIcon フィールドを追加 (basics, 1 commit, ca94be3)**: iter394 BriefSeverity に対応する emoji icon を BriefSummary フィールドとして bundle。formatBriefStatusBarJa が内部で使う SEVERITY_ICON mapping をフィールド化、caller は文字列を作らずに icon だけ取り出せる。mapping: critical=🆘 / high=🚨 / medium=⚠️ / low=🔆 / idle=✅。caller benefits: mobile UI で `<icon> {headline}` 細分化レイアウト (formatBriefStatusBarJa は 1 行固定、本フィールドは icon 単独) / 通知 icon / Slack icon で文字列分解せず参照 / aria-label に severity 文言と icon を分けて渡す。副次効果: SEVERITY_ICON が SEVERITY_ICON_FULL に統合 (= idle 含む 5 段階完全 mapping)、formatBriefStatusBarJa も同 source 参照 (1 source of truth = icon 不整合不可)。UX 卓越: a 発見可能性 (icon 単独取り出し可) / g 一貫性 (statusbar prefix と severityIcon が同一)。機能追加なし、shadcn 編集なし、既存 BriefSummary フィールド無変更 (= backwards compat)。test +6 件 (severity 5 段階 + 整合性 1 = 63/63 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter402 = 402%5=2 → ai-automation。
- ✅ [iter400 (BRIEF_AXES_BY_SEVERITY-集約)] **BRIEF_AXES_BY_SEVERITY + isBriefAxisTriggered に集約 (refactor, 1 commit, a9e8ced)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 / iter385 / iter390 / iter395 と同じ「同 shape の散在を 1 file に」方針 23 弾目。detectBriefActiveAxis (iter396) と detectBriefActiveAxes (iter397) で同 shape の 6 連続 axis check が重複していたので、axis 列 + 1 述語に集約。新 internal: `BRIEF_AXES_BY_SEVERITY: readonly BriefAxis[]` (severity 順 source) / `isBriefAxisTriggered(input, axis): boolean` (各 axis の trigger 判定を switch 化)。集約後 callsite: detectBriefActiveAxis = `for axis ... if isTriggered return; else null` / detectBriefActiveAxes = `BRIEF_AXES_BY_SEVERITY.filter(axis => isTriggered)`。順序の不整合は構造的に起きえない (1 か所定義)、trigger 条件の追加 / 削除も 1 述語修正で 2 関数に同時反映。caller 観測: activeAxis / activeAxes / formatBriefStatusBarJa / formatActiveAxesJa の出力完全互換 (test 57/57 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter401 = 401%5=1 → basics。
- ✅ [iter399 (formatActiveAxesJa)] **formatActiveAxesJa + BRIEF_AXIS_LABEL_JA を追加 — 人間可読 axis list (ai-automation, 1 commit, 480d5ab)**: iter397 activeAxes (BriefAxis[]) を「人間可読 1 行 list」に整形する formatter。iter398 formatBriefStatusBarJa は severity 主体 ('🆘 critical [3 軸]')、本 fn は axes 詳細主体 ('同時警報: A + B + C') — caller は用途で使い分け。文言例: '警報なし' (空) / '警報: MUST 期限超過' (1 軸) / '同時警報: MUST 期限超過 + 進行中停滞' (2 軸) / '同時警報: MUST 期限超過 + 進行中停滞 + 古参' (3+ 軸)。新 export: `BRIEF_AXIS_LABEL_JA: Record<BriefAxis, string>` (= 全 6 axis の日本語短ラベル mapping、caller は AI prompt や UI に直接参照可) / `formatActiveAxesJa(activeAxes)` (severity 順を保つ)。caller benefits: AI 朝 brief「同時警報: ... — 危機的状態」 / pm-agent watch list summary header / dashboard banner / Slack 通知 subtitle (formatBriefStatusBarJa との重ねで 2 段表示も可)。test +6 件 (57/57 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter400 = 400%5=0 → refactor。
- ✅ [iter398 (brief-status-bar-formatter)] **formatBriefStatusBarJa を追加 — UI 1-line ready formatter (basics, 1 commit, 734dab0)**: iter394 severity / iter396 activeAxis / iter397 activeAxes / iter388 headlineWithTitles を結合した「status bar / 通知 / 1-line UI」用 formatter。文言例: '🆘 critical [3 軸] MUST 期限超過: 提出書類 14日 / 連絡 5日' / '🚨 high [1 軸] 進行中だが停滞: 1 件' / '⚠️ medium [1 軸] stale 1' / '🔆 low [1 軸] urgent 上位 1' / '✅ idle 問題なし'。severity icon mapping 5 段階 (🆘 / 🚨 / ⚠️ / 🔆 / ✅)。caller (mobile status bar / 通知 / Slack message / dashboard banner) は本文字列を直接埋め込み可: severity icon で視覚優先度即伝達 / [N 軸] で同時警報数 (= 危機度) 即把握 / headlineWithTitles で具体内容 (actionable info)。UX 卓越: a 発見可能性 (1 行で severity / 危機度 / 具体内容) / c 状態網羅 (5 severity × N 軸 + idle) / g 一貫性 (severity icon は emoji 5 個固定)。機能追加なし、shadcn 編集なし、既存 BriefSummary は無変更。test +6 件 (51/51 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter399 = 399%5=4 → ai-automation。
- ✅ [iter397 (brief-activeAxes)] **brief-summary に activeAxes (multi-axis view) を追加 (ai-automation, 1 commit, 79aa19a)**: iter396 activeAxis (= 最深刻 1 軸) を補完する multi-axis view。同時発生中の全 axes を severity 順で返す。新 export: `detectBriefActiveAxes(input): BriefAxis[]` (1+ 件 triggered な axes を severity 順、空配列 = 全 axis 0) / BriefSummary に `activeAxes: BriefAxis[]` 追加 (同 today 同期)。caller benefits: AI 朝 brief「今日は MUST 期限超過 + stuck WIP + stale 3 軸同時 — 危機的」(同時警報数で危機度伝達) / mobile UI active axis 数を chip badge / dashboard 「危機度メーター」(1 軸 / 2 軸 / 3 軸+ で警告強度) / multi-icon 表示。activeAxis === activeAxes[0] 不変条件、activeAxis === null ⇔ activeAxes === []。1 item でも複数 axes を triggers (例: MUST overdue は MUST + overdueActive + topUrgent を同時 trigger)。test +5 件 (45/45 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter398 = 398%5=3 → basics。
- ✅ [iter396 (BriefAxis-export)] **BriefAxis を export + activeAxis フィールドを追加 (basics, 1 commit, bd1ef92)**: iter395 で internal helper として集約した detectBriefActiveAxis / BriefAxis を public 化し、BriefSummary に `activeAxis: BriefAxis | null` フィールドを追加。caller (chip 配色 / 通知 icon / dashboard banner styling) は switch で axis 別の UI を 1 か所で切替できる。severity 'high' 内 3 軸 (mustAtRisk / overdueActive / stuckWip) は severity だけでは区別できないが activeAxis なら個別 styling 可能 (= severity 集約 + axis 詳細の 2 軸情報設計)。新 export: `BriefAxis` type / `detectBriefActiveAxis` (caller が brief を作らずに早期判定したい時用) / BriefSummary に `activeAxis` field 追加。caller benefits: chip 配色を axis 別に細分化 (mustOverdue=深紅 / mustAtRisk=朱 / stuckWip=青) / 通知 icon を axis 別 (🆘 / ⚠️ / 🚨) / 通知文言の主語を axis から推論。UX 卓越: a 発見可能性 ('high' 系 3 軸を区別) / c 状態網羅 (7 状態) / g 一貫性 (severity 順序 1 か所定義)。機能追加なし、shadcn 編集なし、existing field は無変更 (= backwards compat)。test +5 件 (41/41 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter397 = 397%5=2 → ai-automation。
- ✅ [iter395 (detectBriefActiveAxis-集約)] **detectBriefActiveAxis に severity-cascade を集約 (refactor, 1 commit, e9d498a)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 / iter385 / iter390 と同じ「同 shape の散在を 1 file に」方針 22 弾目。pickBriefHeadline (iter377) / pickBriefHeadlineWithTitles (iter388) / pickBriefSeverity (iter394) の 3 関数で同 shape の 6 段カスケード if が重複していたので、「現時点で最深刻な軸」を 1 値で返す internal helper に集約。新 internal type / fn: `BriefAxis = 'mustOverdue' | 'mustAtRisk' | 'overdueActive' | 'stuckWip' | 'stale' | 'topUrgent'` (severity 順) / `detectBriefActiveAxis(input): BriefAxis | null` (null = 全 axis 0)。3 callsite を switch 化、severity 順序が 1 か所定義 → 3 関数で不整合が起きえない (構造的保証)。caller benefits: 将来 chip 配色 / 通知 icon を axis 別に切替えたい時、同 detector を再利用可 / mustAtRisk / overdueActive / stuckWip → 'high' の grouping は switch fall-through で表現。callsite 観測: brief.headline / headlineWithTitles / severity 出力完全互換 (test 36/36 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter396 = 396%5=1 → basics。
- ✅ [iter394 (brief-severity)] **brief-summary に severity (5 段階) を追加 (ai-automation, 1 commit, 443c2ad)**: 7 軸の集約 severity を 1 値で返す axis-aggregate 指標を新フィールドで提供。mobile UI / status bar / 通知 / dashboard banner が「全体の警報レベル」を 1 値で受け取り、配色 / icon / 通知音 等を切替えるための基盤。5 段階 (pickBriefHeadline と severity 順序整合): 'critical' (mustOverdue 1+ 件 = MVP 違反警報) / 'high' (mustAtRisk / overdueActive / stuckWip 1+ 件) / 'medium' (stale 1+ 件) / 'low' (topUrgent のみ = 警報無し) / 'idle' (全 axis 0 件 = 完璧)。caller benefits: mobile status bar 1 色で全体警報レベル / dashboard banner severity 別 styling / 通知 icon / 'idle' = positive UX 根拠。新 export: `BriefSeverity` type / BriefSummary に `severity` field / pickBriefSeverity (private fn)。test +7 件 (5 段階 + topUrgent only fallback + 全 axis 0 = 36/36 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter395 = 395%5=0 → refactor。
- ✅ [iter393 (chip-data-priority-buckets)] **chip に data-priority-buckets attr を追加 — 3 chip 統一 (basics, 1 commit, 22385e2)**: iter386 / iter383 / iter391 で must-overdue / overdue-active / must-stuck-wip chip に 3 層情報設計 (visible / priority breakdown / titles) を bind 済。本 iter で各 chip の DOM data attr に `data-priority-buckets` (= 1..4) を追加し、E2E test や CSS hook が「priority bias 露出 = priority 別表示が出ているか」を直接観測可能に。3 chip で統一 (priorityBuckets > 1 で breakdown 表示)。E2E test selector で `[data-priority-buckets='1']` (= 単一 P 偏在) / `'2'`+ (= 分散) を切り分けられる、CSS hook で別 styling 適用も可。UX 卓越: a 発見可能性 (DOM 観察で priority bias 即特定) / c 状態網羅 (1..4 整数で分散度合い表現) / g 一貫性 (3 chip 同 attr 名)。機能追加なし、shadcn 編集なし、aria-label / title / visible 不変。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter394 = 394%5=4 → ai-automation。
- ✅ [iter392 (stale-items-by-priority)] **computeStaleItemsByPriority / formatStaleItemsByPriorityJa を追加 (ai-automation, 1 commit, 127c08f)**: iter369 (overdue-active) / iter382 (hygiene-debt) / iter384 (must-overdue) / iter389 (must-stuck-wip) と並ぶ「× priority」軸 12 弾目。「P1 stale 3 件 (最古 14日) / P3 stale 2 件 (最古 10日)」のように stale (7 日以上放置) の priority bias を可視化、全体「stale 5 件」だけでは見えない bias を露出。新 export (`stale-items.ts` に追加): `StaleItemsByPriorityStats {count, oldestStaleDays | null}` / `computeStaleItemsByPriority(items, options?, today?)` (selectStaleItems を bucketByPriorityWith 経由で priority 別再適用) / `formatStaleItemsByPriorityJa(byPriority)` (iter385 集約済 `formatPriorityBucketsCountWithDays` を 6 callsite 目で利用、'最古' label = overdue-active / must-overdue と vocabulary 統一)。caller benefits: AI 朝 brief「P1 stale 14 日 → 最優先 nudge」 / pm-agent priority 別 stale watch list / dashboard stale-items chip の priority tooltip 同梱候補 (= iter386/391 と同手法)。test +5 件 (compute 3 / format 2 = 20/20 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter393 = 393%5=3 → basics。
- ✅ [iter391 (must-stuck-wip-chip-priority)] **must-stuck-wip chip の aria-label / title に priority breakdown を append (basics, 1 commit, 6668ef8)**: iter389 で追加した `computeMustStuckWipByPriority` / `formatMustStuckWipByPriorityJa` を Dashboard の must-stuck-wip chip の aria-label / title (= SR / hover 経路) に bind。iter386 (must-overdue chip 3 層情報設計) と symmetric な拡張、dashboard の「3 層情報設計」パターンが MUST 系 2 chip 揃う。文言例: visible='MUST 進行中だが停滞: 2 件 (タスクA 7 日 / タスクB 4 日)' / hover&aria=同 + ' — P1 1 件 (最長 7日) / P3 1 件 (最長 4日)'。priorityBuckets > 1 の時のみ priority breakdown を append (= 単一 P 偏在は冗長省略)。UX 卓越: a 発見可能性 (MUST × 進行中停滞 の P 別 escalation 順位を hover で確認可) / b アクセシビリティ (SR で 'P1 含有' = 最深刻度示唆) / c 状態網羅 (priority 単一偏在 省略) / g 一貫性 (iter386 と完全同パターン)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、must-stuck-wip 17/17 PASS、architecture 5/5 PASS。次 iter392 = 392%5=2 → ai-automation。
- ✅ [iter390 (pickMustOverdueItems-委譲)] **pickMustOverdueItems を pickOverdueActiveItems に委譲 (refactor, 1 commit, bcf71d5)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 / iter385 と同じ「同 shape の散在を 1 file に」方針 21 弾目。pickMustOverdueItems と pickOverdueActiveItems は filter + sort + map のロジックが完全同一 (~25 行) で、唯一の差分は "isMust=true 必須" のみだった。集約: (1) `MustOverdueEntry<T>` を `OverdueActiveEntry<T>` の type alias に統合 (shape 完全同一、caller は両方の名前で同 shape 参照可、後方互換維持) / (2) pickMustOverdueItems の body を "isMust=true で pre-filter → pickOverdueActiveItems に委譲" に集約 (重複 ~25 行 → 4 行)。並び安定性: mustOnly 内 index 順 = 元 items 内 MUST 相対順序 (OK)。status 検査: 旧 strict literal が normalizeStatus 経由 (大文字/小文字 fail-soft) に揃った (= overdue-active と同じ defensive 振る舞い)。caller 観測: 配列順序 / overdueDays / title 全て不変。must-overdue 17/17 PASS / overdue-active 28/28 PASS / brief-summary 29/29 PASS、architecture 5/5 PASS。ファイル -16 行、parseDateOrNull / parseIsoDateAsLocalMidnight / MS_PER_DAY の import も不要に。typecheck/lint 緑 (warning baseline 1)。次 iter391 = 391%5=1 → basics。
- ✅ [iter389 (must-stuck-wip-by-priority)] **computeMustStuckWipByPriority / formatMustStuckWipByPriorityJa を追加 (ai-automation, 1 commit, b7797df)**: iter384 (computeMustOverdueByPriority) と同シリーズ — must 系の by-priority substrate 2 弾目。must の中でも P1 偏在は最深刻 escalation を示唆。iter362 wip-stuck-by-priority の must-only 版とも見れる。「P1 MUST 進行中だが停滞 2 件 (最長 7日) / P3 MUST 進行中だが停滞 1 件 (最長 4日)」のように MUST stuck WIP の priority bias を露出。新 export (`must-stuck-wip.ts` に追加): `MustStuckWipByPriorityStats {count, maxStuckDays | null}` / `computeMustStuckWipByPriority(items, options?, today?)` (pickMustStuckWipItems を bucketByPriorityWith 経由で priority 別再適用) / `formatMustStuckWipByPriorityJa(byPriority)` (iter385 集約済 `formatPriorityBucketsCountWithDays` を 5 callsite 目で利用、'最長' label = wip-stuck-by-priority と vocabulary 統一)。caller benefits: AI 朝 brief「P1 MUST stuck — 即対応」 / pm-agent priority 別 MUST stuck queue / dashboard must-stuck-wip chip の priority tooltip bind 候補 (= iter386 と同手法)。test +5 件 (compute 3 / format 2 = 17/17 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter390 = 390%5=0 → refactor。
- ✅ [iter388 (brief-headline-with-titles)] **brief-summary に headlineWithTitles を追加 — 具体名 1 行 alert (basics, 1 commit, fce2004)**: iter377 stats-only headline と並んで、iter387 で bundle した mustOverdueEntries / overdueActiveEntries を使う「具体名版 1 行 alert」を新フィールドで提供。caller は用途で使い分け: mobile status bar / 短通知 → `headline` (stats only、固定長で layout 安定) / actionable AI brief / 通知 detail → `headlineWithTitles` (具体名で次の action 即決定)。文言例: headline = 'MUST 期限超過 2 件 (最古 14 日) — 即対応' / headlineWithTitles = 'MUST 期限超過: 提出書類 14日 / 連絡 5日'。severity 順序は pickBriefHeadline と完全同一 (1. mustOverdue → 2. mustAtRisk → 3. overdueActive → 4. stuckWip → 5. stale → 6. topUrgent fallback)。must-overdue / overdue-active 軸のときのみ entries 経路で format、それ以外の軸は元々 entries-based なので headline と同一文言。UX 卓越: a 発見可能性 ("何が落ちている" を 1 行で即特定可、actionable 版) / c 状態網羅 (mustOverdue 0 件で同文言、1+ 件で entries-based に切替) / g 一貫性 (severity 順序統一)。既存 headline 無変更 = backwards compat。shadcn 編集なし。test +4 件 (29/29 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter389 = 389%5=4 → ai-automation。
- ✅ [iter387 (brief-entries-bundle)] **brief-summary に mustOverdueEntries / overdueActiveEntries を bundle (ai-automation, 1 commit, 11b5093)**: iter379 (pickMustOverdueItems) / iter382 (pickOverdueActiveItems) で追加した具体名 + overdueDays 一覧を、buildBriefSummary 1 回呼出で stats と並んで取得できるよう拡張。caller (AI brief / pm-agent / dashboard tooltip) は同 today 同期済の stats と entries を両方使えるので、AI 朝 brief で「MUST 期限超過 2 件 (最古 14 日) — 提出書類 14日 / 連絡 5日」のような stats + 具体名を 1 prompt 行に同 today で組める / pm-agent watch list で stats と具体名を分離せず取り出せる / dashboard chip + tooltip 同じデータ source で重複 walk 回避。実装: BriefSummary に `mustOverdueEntries: MustOverdueEntry<T>[]` / `overdueActiveEntries: OverdueActiveEntry<T>[]` 追加、buildBriefSummary 内で同 today で pick fns を呼ぶ。既存 textSummary / headline / 7 axis は無変更 (= backwards compat)。test +4 件 (具体名 / 0 件 fallback / stats↔entries 同期 = 25/25 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter388 = 388%5=3 → basics。
- ✅ [iter386 (must-overdue-chip-priority)] **must-overdue chip の aria-label / title に priority breakdown を append (basics, 1 commit, b390b46)**: iter384 で追加した `computeMustOverdueByPriority` / `formatMustOverdueByPriorityJa` を Dashboard の must-overdue chip の aria-label / title (= SR / hover 経路) に bind。iter383 (overdue-active chip 3 層情報設計) と symmetric な拡張、dashboard の「3 層情報設計」パターンが overdue 系で 2 chip 揃う。3 層情報設計: visible='MUST 期限超過 2 件 (最古 14 日) — 即対応' / hover&aria=同 + ' — P1 1 件 (最古 14日) / P3 1 件 (最古 5日)' + ' — MUST 期限超過: 提出書類 14日 / 連絡 5日'。priorityBuckets > 1 の時のみ priority breakdown を append、entries.length > 0 の時のみ titles を append (= 過剰冗長な suffix を回避)。iter363 / iter371 / iter378 / iter383 と同手法。UX 卓越: a 発見可能性 (MUST × P 別 escalation 順位を hover で確認) / b アクセシビリティ (SR で 'P1 含有' = 最深刻度示唆) / c 状態網羅 (priority 単一 / titles 0 件 で省略) / g 一貫性 (iter383 と完全同パターン)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、must-overdue 22/22 PASS、architecture 5/5 PASS。次 iter387 = 387%5=2 → ai-automation。
- ✅ [iter385 (formatPriorityBucketsCountWithDays-集約)] **formatPriorityBucketsCountWithDays を priority.ts に集約 (refactor, 1 commit, c5969c4)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 と同じ「同 shape の散在を 1 file に」方針 20 弾目。"count > 0 + days != null で `P{k} {count} 件 ({label} {days}日)`" 形式の by-priority format helper が 4 callsite (must-overdue / overdue-active / stale-urgent / wip-stuck) で重複していたので 1 関数に集約。新 export (`priority.ts` に追加): `formatPriorityBucketsCountWithDays<S>(byPriority, getCount, getDays, daysLabel, emptySentinel)` — accessor で field 名差 (oldestOverdueDays / oldestDays / maxStuckDays) を吸収、daysLabel で見出し差 ('最古' / '最長') を吸収、caller は 5 行 lambda が 1 行 helper 呼出に縮む。slip-days.ts (`'遅延: ' + body` wrap) / backlog-aging.ts (count + ancient 合算) は別 shape なので対象外 (後続 iter 候補)。callsite -22 行 (4 file)、helper +12 行 + test +75 行 = net +65 行。動作変更ゼロ (出力文字列完全互換、affected test 128/128 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter386 = 386%5=1 → basics。
- ✅ [iter384 (must-overdue-by-priority)] **computeMustOverdueByPriority / formatMustOverdueByPriorityJa を追加 (ai-automation, 1 commit, 7504749)**: iter369 (`computeOverdueActiveByPriority`) の must-only 版 (= isMust=true filter 追加)。iter382 hygiene-debt / iter387 slip-days / iter389 backlog-aging / iter399 stale-urgent / iter362 wip-stuck / iter369 overdue-active と並ぶ「× priority」軸 11 弾目。「P1 MUST 期限超過 2 件 (最古 14 日) / P3 MUST 期限超過 1 件 (最古 5 日)」のように MUST の中でも高優先軸の overdue を分離して可視化。must-overdue は全体「2 件」だけでも MVP 違反警報だが、P1 が含まれているか否かで escalation 順位が変わる。新 export (`must-overdue.ts` に追加): `MustOverdueByPriorityStats {count, oldestOverdueDays | null}` / `computeMustOverdueByPriority(items, today?)` で `Record<PriorityKey, MustOverdueByPriorityStats>` (computeMustOverdue を bucketByPriorityWith 経由で priority 別に再適用) / `formatMustOverdueByPriorityJa(byPriority)` で 'P1 2 件 (最古 14日) / P3 1 件 (最古 5日)' (count=0 P 省略、全 P 0 → 'MUST 期限超過 0 件' sentinel、iter369 と format 互換)。caller benefits: AI 朝 brief「MUST 期限超過 — P1 2 件 (最古 14日)」(P 別 escalation 順位) / pm-agent priority 別 MUST overdue queue / dashboard must-overdue chip の priority tooltip 同梱候補 (= 次 iter で iter363/366/371/378/383 と同手法で bind)。test +5 件 (compute 3 / format 2 = 22/22 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter385 = 385%5=0 → refactor。
- ✅ [iter383 (overdue-active-chip-titles)] **overdue-active chip の aria-label / title に top items を append (basics, 1 commit, 979c914)**: iter382 の `pickOverdueActiveItems` / `formatOverdueActiveTitlesJa` を Dashboard の overdue-active chip の aria-label / title (= SR / hover 経路) に bind。iter381 (must-overdue chip) と symmetric な拡張、これで dashboard の「具体的な item 名を SR/hover で読める」パターンが overdue 系で 2 chip 揃う。3 層情報設計に拡張: visible='期限超過 5 件 (TODO 3 / 進行中 2) — 最古 14 日' / hover&aria=同 + ' — P1 3 件 (最古 14日) / P3 2 件 (最古 5日)' + ' — 期限超過: 提出書類 14日 / 連絡 5日' (priorityBuckets > 1 の時のみ priority breakdown / entries.length > 0 の時のみ titles を append、過剰冗長 'overdue 0 件' suffix を回避)。UX 卓越: a 発見可能性 (期限超過 item の具体名 + priority bias を hover で確認可能) / b アクセシビリティ (SR で具体名 + priority + 件数読上げ) / c 状態網羅 (priority 単一 / 全 P 偏在 / titles 0 件 で detail 省略) / g 一貫性 (iter381 と同パターン、richer info は a11y/hover 経路のみ)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、overdue-active 28/28 PASS、architecture 5/5 PASS。次 iter384 = 384%5=4 → ai-automation。
- ✅ [iter382 (pickOverdueActiveItems)] **pickOverdueActiveItems / formatOverdueActiveTitlesJa を追加 (ai-automation, 1 commit, 5121f8c)**: iter379 (`pickMustOverdueItems` / `formatMustOverdueTitlesJa`) の symmetric 拡張 — must 限定でない「期限超過で未完了」全般版。`computeOverdueActive` / `formatOverdueActiveJa` の stats 系は揃っていたが、items 一覧 (具体名 + overdueDays) を返す軸が欠けていた。新 export (`overdue-active.ts` に追加): `OverdueActiveEntry<T> = {item, overdueDays}` / `pickOverdueActiveItems(items, today?)` で `OverdueActiveEntry[]` (computeOverdueActive と filter ロジック互換、overdueDays 降順 stable 並び) / `OverdueActiveWithTitle = OverdueActiveFields & {title?}` / `formatOverdueActiveTitlesJa(entries, limit=3)` で '期限超過: 提出書類 14日 / 連絡 5日' (limit 超えは「他 N 件」、title 欠落 (無題) fallback、0 件 sentinel)。iter379 formatMustOverdueTitlesJa と同 vocabulary、prefix のみ 'MUST 期限超過' → '期限超過' に差し替え。caller benefits: AI 朝 brief「期限超過: 提出書類 14日 / 連絡 5日」(must 限定でない) / dashboard overdue-active chip の aria-label / title に top items を append (= 次 iter で bind 候補、iter381 で must-overdue chip にやった同手法を overdue-active chip に展開) / pm-agent watch list の具体名付き escalation queue。test +7 件 (pick 3 / format 4 = 28/28 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter383 = 383%5=3 → basics。
- ✅ [iter381 (must-overdue-chip-titles)] **must-overdue chip の aria-label / title に top items を bind (basics, 1 commit, f5e3656)**: iter379 で追加した `pickMustOverdueItems` / `formatMustOverdueTitlesJa` を Dashboard の must-overdue chip の aria-label / title (= SR / hover 経路) に bind。視覚 chip text は stats summary (`MUST 期限超過 2 件 (最古 14 日) — 即対応`) のまま、aria-label / title だけ richer (具体名付き、`'MUST 期限超過 2 件 (最古 14 日) — 即対応 — MUST 期限超過: 提出書類 14日 / 連絡 5日'`) に変更。iter363 / iter366 / iter371 / iter378 / iter383 / iter388 / iter391 / iter401 と同手法 (= 2 層情報設計、視覚 chip 不変 + a11y/hover で richer 情報)。これで dashboard 系 chip の「具体的な対象 item 名を SR/hover で読める」パターンが must-overdue / must-stuck-wip / overdue-active 等で揃う。UX 卓越: a 発見可能性 (期限超過 MUST の具体名 hover 確認可) / b アクセシビリティ (SR で具体名読上げ) / c 状態網羅 (entries 0 時は detail = summary fallback、1+ で richer) / g 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、9 chip で同手法揃い)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、must-overdue 17/17 PASS、architecture 5/5 PASS。次 iter382 = 382%5=2 → ai-automation。
- ✅ [iter380 (parseIsoDateAsLocalMidnight-集約)] **parseIsoDateAsLocalMidnight を lib/date/iso に集約 (refactor, 1 commit, 0a8ebfa)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 と同じ「同 shape の散在を 1 file に」方針 19 弾目。ISO `YYYY-MM-DD` をローカル TZ 当日 0:00 Date に変換する fail-soft helper が 4 callsite で重複していた (`due-proximity.ts#parseIsoDate` private / `time-entry/daily-summary.ts#parseDate` private / `must-overdue.ts` の inline match / `overdue-active.ts` の inline match) ので 1 関数に集約。新 export (`lib/date/iso.ts` に追加): `parseIsoDateAsLocalMidnight(iso: string): Date | null` — 形式不一致 / 月 < 1 / 月 > 12 / 日 < 1 / 日 > 31 → null、そうでなければ `new Date(y, mo - 1, d)` (= ローカル TZ の 0:00)。due-proximity / daily-summary は private fn を完全置換、must-overdue / overdue-active は `isValidIsoDate + match` の二重チェックを helper 1 回呼出に刷新 (validation 統合済 / 動作互換)。caller 統一の利点: 「ISO → Date with 月/日範囲チェック」の 1 行 idiom を caller が再実装しなくて良くなり、新 substrate (例えば次回追加される `due-X-by-something` 系) も import 1 行で同 fail-soft semantics を得られる。test +7 件 (parse 7 = iso.test.ts +7)、affected 132/132 PASS (iso 21 / due-proximity 19 / must-overdue 17 / overdue-active 21 / daily-summary 54)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter381 = 381%5=1 → basics。
- ✅ [iter379 (pickMustOverdueItems)] **pickMustOverdueItems / formatMustOverdueTitlesJa を追加 (ai-automation, 1 commit, 2b230b5)**: iter372 で must-overdue stats (total / oldestOverdueDays) のみだったので、AI 朝 brief や dashboard tooltip で「具体的にどの MUST が期限超過しているか」を見せる軸を追加。overdue-active の filter ロジックを踏襲しつつ items を返す + title list 形式で 1 行 summary。新 export (must-overdue.ts に追加): `MustOverdueEntry<T> = {item, overdueDays}` / `pickMustOverdueItems(items, today?)` で `MustOverdueEntry[]` (isMust=true + dueDate < today valid + done/archive/cancelled 除外、overdueDays desc 並び stable) / `MustOverdueWithTitle = MustOverdueFields & {title?}` / `formatMustOverdueTitlesJa(entries, limit=3)` で 'MUST 期限超過: 提出書類 14日 / 連絡 5日' (limit 超えは「他 N 件」、title 欠落 (無題) fallback、0 件 sentinel)。caller benefits: AI 朝 brief「MUST 期限超過: 提出書類 14日 / 連絡 5日」/ dashboard must-overdue chip の aria-label / title に top items を append (= 次 iter で bind 候補) / pm-agent watch list の最高優先 escalation queue (具体名付き)。test +7 件 (pick 3 / format 4 = 17/17 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter380 = 380%5=0 → refactor。
- ✅ [iter378 (attention-chip-ring)] **attention chip に視覚 ring を付与 (red-400/60) (basics, 1 commit, 9c8a724)**: iter376 で attention prop を追加した DashboardChip は SR (assertive 割込み読上げ) には有効だが、視覚ユーザにも「他の chip と異なる注意レベル」が伝わる視覚 hint が欲しい。must-stuck-wip / must-overdue chip は MVP「絶対落とさない」原則違反警報なので、他の red 系 chip (slip-days / overdue-active 等) より一段強い視覚強調が望ましい。実装: DashboardChip の `attention=true` 時に `ring-2 ring-red-400/60` を className に append (4 行)。Tailwind ring は border の outside にレンダリングされるので chip 寸法は不変、border / bg は toneClass の指定を尊重。UX 卓越: a 発見可能性 (視覚的に「特別注意 chip」を即識別可能) / b アクセシビリティ (SR には iter376 で追加済 alert + assertive、本 iter は視覚軸補完) / c 状態網羅 (既存 16+ chip は attention=false で ring 非表示) / g 一貫性 (Tailwind ring 標準パターン、ring-red-400/60 は border-red-300/400 と色合い整合)。機能追加なし、shadcn 編集なし、既存 attention=false chip は className 不変。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter379 = 379%5=4 → ai-automation。
- ✅ [iter377 (brief-headline)] **brief-summary に headline (1 行最深刻 alert) を追加 (ai-automation, 1 commit, 5b380bf)**: iter374 で 7 軸に拡張した brief-summary に、7 軸を severity 順にスキャンして 1 行で「最も深刻な signal」だけを返す compact headline を追加。mobile UI / status bar / 通知 1 行で「最優先アラート」を表示する用。severity 順序 (MVP「絶対落とさない」優先度): 1. mustOverdue / 2. mustAtRisk / 3. overdueActive / 4. stuckWip / 5. stale / 6. topUrgent / 7. fallback。実装: BriefSummary に `headline: string` 追加、buildBriefSummary 内で `pickBriefHeadline()` (private fn) を呼んで 7 axis を順序検査、各 axis の formatXJa を再利用して同じ文言体系を保つ (= textSummary と vocabulary 一致)。caller benefits: AI brief 全文 (textSummary、7 行) と並んで「最優先 1 行」も同 today 同期で取り出せる、mobile / 狭幅 dashboard で textSummary の代わりに headline 表示すれば視覚情報量を絞れる、SR の最初の announce にも好適。test +7 件 (idle / mustOverdue / mustAtRisk / overdue / stuckWip / stale / topUrgent 21/21 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter378 = 378%5=3 → basics。
- ✅ [iter376 (must-chip-attention)] **MUST 違反 chip に attention prop で role=alert + aria-live=assertive (basics, 1 commit, 934fc37)**: iter366 (must-stuck-wip-chip) / iter373 (must-overdue-chip) の 2 chip は MVP「絶対落とさない」原則の違反警報なので、SR の割込み読上げ (assertive) が望ましい。default の role="status" + aria-live="polite" は SR 既読み上げ完了後通知で緊急警報には不適。実装: DashboardChip に `attention?: boolean` prop を追加 (default false)、attention=true なら role="alert" + aria-live="assertive" に切替、既存 16+ chip は all default false で動作変更ゼロ、mustStuckWip / mustOverdue chip 2 callsite に `attention` を bind。UX 卓越: a 発見可能性 (SR ユーザに「割込み」で MUST 違反を即通知) / b アクセシビリティ (ARIA Live Region spec 準拠) / c 状態網羅 (chip 出現タイミングは severity 'idle' で抑制 → 出現 = 該当 = 読上げ) / g 一貫性 (default は 16+ 既存 chip と同じ status / polite を維持、新 prop は opt-in)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、affected 既存テスト不変、architecture 5/5 PASS。次 iter377 = 377%5=2 → ai-automation。
- ✅ [iter375 (renderStuckEntry-集約)] **renderStuckEntry helper を wip-stuck から export 化 (refactor, 1 commit, d0f8eb5)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 と同じ「同 shape の散在を 1 file に」方針 18 弾目。wip-stuck.ts (formatStuckWipSummaryJa) と must-stuck-wip.ts (formatMustStuckWipJa) の 2 callsite で同 shape の inline render fn (`${title} ${stuckDays} 日`、title 欠落 `(無題)` fallback) が重複していたので 1 関数に集約。新 export (`wip-stuck.ts` に追加): `renderStuckEntry<T extends StuckWipFields>(e)` — 共通 render fn で formatTopWithOverflow に渡す。callsite 置換 (-12 行 / 2 file)、動作変更ゼロ (出力文字列完全互換、affected 52/52 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter376 = 376%5=1 → basics。
- ✅ [iter374 (brief-summary-extend)] **brief-summary combinator に 3 軸 (stuck WIP / overdue / must-overdue) を追加 (ai-automation, 1 commit, 2a7a2aa)**: iter304 で 4 軸 (top urgent / must-at-risk / stale / velocity) で組まれていた AI brief combinator を、本 session で整備した 3 substrate (iter359 wip-stuck / iter367 overdue-active / iter372 must-overdue) を取り込んで 7 軸に拡張。MVP の核「MUST 絶対落とさない」原則の違反シグナル (must-overdue / wip-stuck) を AI brief の最終 2 行に追加することで、毎回の brief で原則違反を見逃さない設計。実装: `BriefItemFields` に `StuckWipFields ∪ OverdueActiveFields ∪ MustOverdueFields` を union / `BriefSummary` に stuckWip / overdueActive / mustOverdue 3 フィールド追加 / `BriefSummaryOptions` に stuckWipThresholdDays (default 3) 追加 / buildBriefSummary 内で 3 substrate を同 today で実行 / textSummary に 3 行追加 (順序: stuckWip → overdueActive → mustOverdue)。caller benefits: 1 関数呼出で 7 軸 (= 同 today 同期済) AI brief を取り出せる、新規 substrate を追加するたびに caller が個別に呼び直す必要が無い。test +1 件 (7 行 + 順序 assertion 拡張、14/14 PASS)、affected 47/47 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter375 = 375%5=0 → refactor。
- ✅ [iter373 (must-overdue-chip)] **must-overdue chip を Dashboard に追加 — MUST 期限超過 最深刻警報 (basics, 1 commit, 3ef5a2a)**: iter372 整備済 must-overdue substrate (computeMustOverdue / formatMustOverdueJa / mustOverdueSeverity、10/10 PASS) を Dashboard chip に bind。MUST かつ期限超過の最深刻 item を 1 目視可能に。MVP の核「MUST 絶対落とさない」(CLAUDE.md) を「期限を超えて未完了の MUST」として最も強く警告する dashboard 専用 chip (must-stuck-wip = 進行中停滞 と相補で「期限超過」軸)。実装: `mustOverdue` useMemo (+8 行) — computeMustOverdue で集計 → severity 判定 → 'idle' で null return → formatMustOverdueJa で 1 行 summary。chip JSX (+13 行) を mustStuckWip chip 直後に配置 (= MUST 警報シリーズを近接、深刻度の昇順)、glyph 🆘 (SOS = MVP 原則違反 metaphor、mustStuckWip の 🚨 と区別)、tone は固定 red 強 (border-red-400/bg-red-100/text-red-900)、idle は chip 非表示 (progressive disclosure)、data-severity / data-must-overdue-count / data-oldest-days attr で test/css hook。UX 卓越: a 発見可能性 (MUST が期限を超えた時のみ chip 出現、強 red で即注意誘導) / b アクセシビリティ (SR で「MUST 期限超過 N 件 (最古 M 日) — 即対応」読上げ) / c 状態網羅 ('critical' / 'idle' 2 状態) / g 一貫性 (must-stuck-wip と同じ DashboardChip pattern)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、must-overdue 10/10 PASS、architecture 5/5 PASS。次 iter374 = 374%5=4 → ai-automation。
- ✅ [iter372 (must-overdue)] **MUST × overdue active combinator (ai-automation, 1 commit, 2ea2518)**: iter367 overdue-active と must-hygiene の 2 軸交差 combinator。iter384 / iter397 / iter364 と同パターンの 2 軸交差 substrate。「絶対落とせない MUST が既に期限を超えて未完了」 = MVP の核「MUST 絶対落とさない」(CLAUDE.md) の最も深刻な違反。must-stuck-wip (= MUST × 進行中で停滞) と相補で、こちらは「未完了で期限超過」軸。新 module `src/features/item/must-overdue.ts`: `MustOverdueFields = OverdueActiveFields & {isMust}` / `computeMustOverdue(items, today?)` で `{total, oldestOverdueDays}` (内部で isMust フィルタ → computeOverdueActive 流用) / `formatMustOverdueJa(stats)` で 'MUST 期限超過 2 件 (最古 14 日) — 即対応' (oldest === 0 で「今日付」短縮形、0 件 sentinel) / `mustOverdueSeverity(stats)` で 'critical' (1+) / 'idle' (0)。caller benefits: AI 朝 brief「MUST 期限超過 2 件 — 即対応」/ pm-agent watch list の最高優先 escalation 候補 / dashboard 専用 chip 候補 (severity 'critical' = red、must-stuck-wip と異なる軸)。test +10 件 (compute 5 / format 3 / severity 2 = 10/10 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter373 = 373%5=3 → basics。
- ✅ [iter371 (overdue-active-chip-priority-tooltip)] **overdue-active chip の tooltip に priority breakdown を同梱 (basics, 1 commit, 93fd4d2)**: iter368 で配置済の overdue-active chip に iter369 整備済 priority 別 breakdown (`computeOverdueActiveByPriority` / `formatOverdueActiveByPriorityJa`) を SR / hover 経路でのみ読めるように 2 層情報設計を投入。iter363 hit-rate / iter366 due-coverage / iter373 hygiene-score / iter378 hygiene-focus / iter383 hygiene-debt / iter388 slipDays / iter391 aging / iter401 stale-urgent / iter363 wip-stuck と同手法、これで Dashboard の by-priority tooltip 11 chip 揃った。実装: overdueActive useMemo に `computeOverdueActiveByPriority(itemsQ.data)` を追加、`countNonEmptyPriorityBucketsBy` で複数 P 分散判定 (>1)、priorityBuckets > 1 のときのみ `formatOverdueActiveByPriorityJa(byPriority)` を `${summary} — ${...}` 形式で detail として保持、chip の ariaLabel / title を `overdueActive.detail` に差替え。visible chip text 不変、hover tooltip / SR 読上げ時のみ richer 文言「期限超過 ... — P1 3 件 (最古 14日) / P3 2 件 (最古 5日)」。UX 卓越: a 発見可能性 (priority bias hover で見える) / b アクセシビリティ (SR で priority 別 件数 + 最古日数) / c 状態網羅 (単一 P 偏在は冗長省略) / g 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、11 chip で同手法揃い)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、overdue-active 21/21 PASS、architecture 5/5 PASS。次 iter372 = 372%5=2 → ai-automation。
- ✅ [iter370 (formatPriorityBuckets-集約)] **5 by-priority format helper を formatPriorityBuckets generic に集約 (refactor, 1 commit, c227a32)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 と同じ「同 shape の散在を 1 file に」方針 17 弾目。features/item の by-priority format helper で全く同 shape の loop pattern が 5 callsite で重複していたので 1 関数に集約。新 export (`priority.ts` に追加): `formatPriorityBuckets<S>(byPriority, formatBucket, emptySentinel, separator?)` — PRIORITY_ORDER 順に formatBucket(k, s) を呼び、null skip / string accumulate / 全 null で emptySentinel / 残りは separator (default ' / ') で join。callsite 置換 (-30 行 / 5 file): wip-stuck.ts (formatStuckWipByPriorityJa) / stale-urgent.ts (formatStaleUrgentByPriorityJa) / overdue-active.ts (formatOverdueActiveByPriorityJa) / slip-days.ts (formatSlipDaysByPriorityJa、'遅延:' prefix 維持) / backlog-aging.ts (formatAgingByPriorityJa、'停滞:' prefix 維持、stagnant 計算は inner fn 内で)。動作変更ゼロ (5 callsite 出力文字列完全互換、affected test 121/121 PASS で確認)、test +4 件 (priority 21+4=25/25 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後追加される by-priority formatter も 1 行 helper 呼出で書ける。次 iter371 = 371%5=1 → basics。
- ✅ [iter369 (overdue-active-by-priority)] **overdue-active を priority 別に集計する pure helper (ai-automation, 1 commit, 89883cd)**: iter367 整備済 overdue-active を priority 別 bucket で取り出す substrate。「× priority」軸 10 弾目 (iter382 hygiene-debt / iter387 slip-days / iter389 backlog-aging / iter399 stale-urgent / iter362 wip-stuck と並ぶ)。「P1 期限超過 3 件 (最古 14 日) / P3 期限超過 2 件 (最古 5 日)」のように 高優先軸の overdue を分離して可視化、全体「期限超過 5 件」だけでは見えない priority bias を露出。computeOverdueActive の filter ロジックは bucketByPriorityWith 経由で priority 別に再適用 (P1 のみ / P2 のみ ... の各 bucket 内で overdue 集計)。新 export (overdue-active.ts に追加): `OverdueActiveByPriorityStats {count, oldestOverdueDays|null}` / `computeOverdueActiveByPriority(items, today?)` で `Record<PriorityKey, ...>` / `formatOverdueActiveByPriorityJa(byPriority)` で 'P1 3 件 (最古 14日) / P3 2 件 (最古 5日)' / count=0 P 省略 / 全 P 0 → '期限超過 0 件'。caller benefits: AI 朝 brief「P1 期限超過 14 日 → 最優先対応」を P 別 / pm-agent priority 別 overdue queue / dashboard overdue-active chip の priority tooltip 同梱候補 (次 iter で iter363/366/373/378/383/388/391/401 と同手法で bind)。test +5 件 (compute 3 / format 2 = 21/21 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter370 = 370%5=0 → refactor。
- ✅ [iter368 (overdue-active-chip)] **overdue-active chip を Dashboard に追加 — 「期限超過で未完了」を可視化 (basics, 1 commit, 77b6858)**: iter367 整備済 overdue-active substrate (computeOverdueActive / formatOverdueActiveJa / overdueActiveSeverity、16/16 PASS) を Dashboard chip に bind。期限超過で未完了の item を status 別に 1 目視可能に。slip-days (= 完了済で遅延、backward-looking) と相補で「これから完了させる必要がある」観点 (= forward-looking)。実装: `overdueActive` useMemo (+8 行) — computeOverdueActive で集計 → severity 判定 → 'idle' で null return → formatOverdueActiveJa で 1 行 summary。chip JSX (+18 行) を slip-days chip 直後に配置 (= 期限関連 chip を近接)、glyph 📅 (calendar = 期限 metaphor、slip-days の ⏰ や aging の ⌛ と区別)、tone severity 別 (severe = red / mild = amber)、idle は chip 非表示 (= UI 静か、progressive disclosure)、data-severity / data-overdue-total / data-oldest-days attr で test/css hook。UX 卓越: a 発見可能性 (overdue active がある時のみ chip 出現、severe で red 即注意誘導) / b アクセシビリティ (SR で 期限超過件数 + status 別 + 最古日数読上げ) / c 状態網羅 ('severe' / 'mild' / 'idle' 3 状態) / g 一貫性 (slip-days / wip-stuck と同じ DashboardChip + 2-tone pattern)。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、overdue-active 16/16 PASS、architecture 5/5 PASS。次 iter369 = 369%5=4 → ai-automation。
- ✅ [iter367 (overdue-active)] **「期限超過で未完了」を 2 軸 (overdue × status) で集計する pure helper (ai-automation, 1 commit, ad88d08)**: 既存 due-proximity (per-item kind 判定 / status 未参照) と slip-days (= 完了し遅延) は overdue 軸の片側ずつしかカバーしていなかった。本 helper は「まだ完了していないのに期限を超えている」 = 計画 vs 現実の最も深刻な乖離を 1 関数で 2 軸 (overdue × status) 集計する。新 module `src/features/item/overdue-active.ts`: `OverdueActiveFields` ({status, dueDate, doneAt, archivedAt}) / `computeOverdueActive(items, today?)` で `{total, byStatus, oldestOverdueDays}` (dueDate < today valid ISO のみ、done/archive/cancelled 除外、status 4 bucket = todo/in_progress/blocked/unknown) / `formatOverdueActiveJa(stats)` で '期限超過 5 件 (TODO 3 / 進行中 2) — 最古 14 日' / `overdueActiveSeverity(stats)` で 'severe' (>= 7 日 or >= 5 件) / 'mild' / 'idle'。合わせて status-visual.ts の `normalizeStatus` を export 化 (旧 private、本 helper で再利用)。caller benefits: AI 朝 brief「期限超過で未完了 5 件 — 最古 14 日」/ pm-agent watch list の最重要 nudge 候補 / dashboard chip 候補 (severe=red 重大 / mild=amber / idle=hidden、slip-days = 完了済 と相補で「これから完了させる必要がある」観点)。test +16 件 (compute 8 / format 4 / severity 4 = 16/16 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter368 = 368%5=3 → basics。
- ✅ [iter366 (must-stuck-wip-chip)] **must-stuck-wip chip を Dashboard に追加 — MUST 「絶対落とさない」 警報 (basics, 1 commit, b3aea4d)**: iter364 整備済 must-stuck-wip combinator (pickMustStuckWipItems / formatMustStuckWipJa / mustStuckWipSeverity、12/12 PASS) を Dashboard chip に bind。MUST かつ stuck WIP な最深刻 item を 1 目視可能に。MVP の核「MUST 絶対落とさない」(CLAUDE.md) を「進行中で停滞している MUST」として最も強く警告する dashboard 専用 chip。実装: `mustStuckWip` useMemo (+10 行) — pickMustStuckWipItems で対象抽出 → mustStuckWipSeverity で 2 値判定 → 'idle' で null return → formatMustStuckWipJa で 1 行 summary。chip JSX (+13 行) を wipStuck chip 直後に配置 (= 関連する WIP-stuck シリーズを近接、深刻度の昇順)、glyph 🚨 (alarm = 最深刻 metaphor、wipStuck の ⏸ や mustHygiene の 🔥 と区別)、tone は固定 red 強 (border-red-300/bg-red-100/text-red-800 = severity 分岐不要、該当時点で最深刻)、idle は chip 非表示で UI 静か。UX 卓越: a 発見可能性 (MUST が止まっている時のみ chip 出現、強 red で即注意誘導) / b アクセシビリティ (SR で top 3 title + 日数読上げ) / c 状態網羅 ('critical' / 'idle' 2 状態) / g 一貫性 (wip-stuck / must-hygiene と同じ DashboardChip pattern)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、must-stuck-wip 12/12 PASS、architecture 5/5 PASS。次 iter367 = 367%5=2 → ai-automation。
- ✅ [iter365 (formatTopWithOverflow-集約)] **「上位 N 件 + 他 K 件」 list summary pattern を 1 file に集約 (refactor, 1 commit, a51b160)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 と同じ「同 shape の散在を 1 file に」方針 16 弾目。features/item 配下の AI brief / dashboard summary helper で 4 callsite に同 shape の「先頭 N 件 + 残件は 他 K 件」 pattern が散らばっていたので 1 source of truth に集約。新 module `src/lib/format-list.ts`: `formatTopWithOverflow<T>(items, render, limit, options?)` — items[0..limit] を render で文字列化して separator で join、limit 超過は '他 K 件' tail を append、options に separator (default ' / ') / overflowPrefix (default '他 ') / overflowSuffix (default ' 件') / totalForOverflow (= recent-completed のように pre-sliced entries で全体件数が異なる caller 用)。callsite 置換 (-25 行 / 4 file): wip-stuck.ts / must-stuck-wip.ts / startable-items.ts / recent-completed.ts (totalForOverflow 経由で entries vs totalCount 乖離を吸収)。動作変更ゼロ (4 callsite の出力文字列が完全一致、affected test 79/79 PASS で確認)、test +10 件 (空 / limit 超 / limit ≤ 0 / separator / overflow prefix-suffix / totalForOverflow / 境界 = format-list 10/10 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後追加される「上位 N 件 + 残件 tail」 list summary も 1 行で書ける。次 iter366 = 366%5=1 → basics。
- ✅ [iter364 (must-stuck-wip)] **MUST × stuck WIP combinator (ai-automation, 1 commit, 14011ed)**: iter357 must-hygiene と iter359 wip-stuck の 2 軸交差 combinator。iter384 (aged-hygiene-debt = hygiene-debt × aging) や iter397 (stale-urgent = urgency × aging) と同パターンの 2 軸交差 substrate。単独 must-hygiene や単独 wip-stuck より signal 強度が高い: 「絶対落とせない MUST を着手したのに 3 日以上触っていない」 = 最深刻アラーム。MVP の核「MUST 絶対落とさない」(CLAUDE.md) を「進行中で停滞している MUST」を見逃さないために投入。新 module `src/features/item/must-stuck-wip.ts`: `MustStuckWipFields = StuckWipFields & {isMust}` / `pickMustStuckWipItems(items, options?, today?)` で `StuckWipEntry[]` (内部で isMust フィルタ → selectStuckWipItems 流用、stuckDays desc 並び stable) / `formatMustStuckWipJa(entries, limit=3)` で 'MUST 進行中だが停滞: 2 件 (...)' (limit 超え 他 N 件、title 欠落 (無題) fallback、0 件 sentinel) / `mustStuckWipSeverity(entries)` で 'critical' (1+ 件) / 'idle' (0 件) 2 値 (MUST に絞った時点で既に最深刻なので 2 値で十分、red 1 色想定)。caller benefits: AI 朝 brief「MUST 進行中だが停滞 2 件 — 即対応」/ pm-agent watch list の最高優先 nudge 候補 / dashboard 専用 chip (severity 'critical' = red、wip-stuck chip と異なる色軸)。test +12 件 (pick 6 / format 4 / severity 2 = 12/12 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter365 = 365%5=0 → refactor。
- ✅ [iter363 (wip-stuck-chip-priority-tooltip)] **wip-stuck chip の tooltip に priority breakdown を同梱 (basics, 1 commit, d893aca)**: iter361 で配置済の wip-stuck chip に iter362 整備済 priority 別 breakdown (computeStuckWipByPriority / formatStuckWipByPriorityJa) を SR / hover 経路でのみ読めるように 2 層情報設計を投入。iter363 hit-rate / iter366 due-coverage / iter373 hygiene-score / iter378 hygiene-focus / iter383 hygiene-debt / iter388 slipDays / iter391 aging / iter401 stale-urgent と同手法、これで Dashboard の by-priority tooltip 10 chip 揃った。実装: wipStuck useMemo に `computeStuckWipByPriority(itemsQ.data)` を追加、`countNonEmptyPriorityBucketsBy` で複数 P 分散判定 (>1)、priorityBuckets > 1 のときのみ `formatStuckWipByPriorityJa(byPriority)` を `${summary} — ${...}` 形式で detail として保持、chip の ariaLabel / title を `wipStuck.detail` に差替え。visible chip text 不変 (例: '進行中だが停滞: 5 件 (...)')、hover tooltip / SR 読上げ時のみ「進行中だが停滞: 5 件 (...) — P1 2 件 (最長 7日) / P3 3 件 (最長 5日)」のような richer 文言。UX 卓越: a 発見可能性 (priority bias hover で見える) / b アクセシビリティ (SR で priority 別 件数 + 最長日数読上げ) / c 状態網羅 (単一 P 偏在は冗長省略) / g 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、10 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更、visible chip text 不変。typecheck/lint 緑 (warning baseline 1)、wip-stuck 26/26 PASS、architecture 5/5 PASS。次 iter364 = 364%5=4 → ai-automation。
- ✅ [iter362 (wip-stuck-by-priority)] **wip-stuck を priority 別に集計する pure helper (ai-automation, 1 commit, 49505f5)**: iter359 整備済 wip-stuck (selectStuckWipItems / formatStuckWipSummaryJa / stuckWipSeverity) を priority 別 bucket で取り出す substrate。iter382 hygiene-debt / iter387 slip-days / iter389 backlog-aging / iter399 stale-urgent と並ぶ「× priority」軸 9 弾目。「P1 が一番停滞、最長 7 日 / P3 が次、最長 5 日」のように 高優先軸の停滞 WIP を分離して可視化、全体「進行中だが停滞 5 件」だけでは見えない priority bias を露出。selectStuckWipItems の filter ロジックは bucketByPriorityWith 経由で priority 別に再適用 (P1 のみ / P2 のみ ... の各 bucket 内で stuck 抽出)。新 export (wip-stuck.ts に追加): `StuckWipByPriorityStats {count, maxStuckDays|null}` (count=0 → maxStuckDays=null) / `computeStuckWipByPriority(items, options?, today?)` で `Record<PriorityKey, ...>` (各 P bucket 必ず初期化、bucketByPriorityWith 経由で normalizePriority + null/範囲外 を P4 集約、options.thresholdDays / today を全 bucket に共通伝搬) / `formatStuckWipByPriorityJa(byPriority)` で 'P1 2 件 (最長 7日) / P3 1 件 (最長 5日)' 形式 / count=0 P 省略 / 全 0 → '進行中だが停滞 0 件'。caller benefits: AI 朝 brief「P1 stuck 7 日 → 即フォロー」を P 別で出せる / pm-agent priority 別停滞 nudge queue / dashboard wip-stuck chip の priority tooltip 同梱候補 (次 iter で iter363/366/373/378/383/388/391 と同手法で bind)。test +6 件 (compute 3 / format 3 = 26/26 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter363 = 363%5=3 → basics。
- ✅ [iter361 (wip-stuck-chip)] **wip-stuck chip を Dashboard に追加 — 「停滞 WIP」を可視化 (basics, 1 commit, a1034bd)**: iter359 整備済 wip-stuck.ts substrate (selectStuckWipItems / formatStuckWipSummaryJa / stuckWipSeverity、20/20 PASS) を Dashboard chip に bind。in_progress 中で updatedAt が 3 日以上動いていない「停滞 WIP」を 1 目視可能に。stale-items より短い閾値で WIP に絞った早期警告で、wipBias (priority 分布、age 軸無し) と相補。実装: `wipStuck` useMemo (+10 行) — selectStuckWipItems で対象抽出 → severity 判定 → 'idle' で null return → formatStuckWipSummaryJa で 1 行 summary。chip JSX (+18 行) を wipBias chip 直後に配置、glyph ⏸ (pause = 停滞 metaphor、wipBias の ▶ 進行中 と対比)、tone は severity 別 (severe = 7 日以上 stuck 含む = red 直接 / mild = amber)、idle は chip 非表示で UI 静か (= 該当 0 件は無音、progressive disclosure)、data-severity / data-stuck-count attr で test/css hook。UX 卓越: a 発見可能性 (停滞中の WIP がある時のみ chip 出現、severe で red 即注意誘導) / b アクセシビリティ (SR で top 3 title + 日数 + 残件読上げ) / c 状態網羅 ('severe' / 'mild' / 'idle' 3 状態) / g 一貫性 (must-hygiene / wip-bias と同じ DashboardChip + chipTone3Class pattern)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、wip-stuck 20/20 PASS、architecture 5/5 PASS。次 iter362 = 362%5=2 → ai-automation。
- ✅ [iter360 (MS_PER_DAY-集約)] **`24 * 60 * 60 * 1000` 1 日 ms 定数を lib/date/iso.ts に集約 (refactor, 1 commit, f1e59c7)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 と同じ「同 shape の散在を 1 file に」方針。features/item 配下で 13 callsite (= 5 local const + 8 inline) に散らばっていた `24 * 60 * 60 * 1000` 1 日 ms 定数を 1 source of truth に集約。新 export (`lib/date/iso.ts` に追加): `export const MS_PER_DAY = 24 * 60 * 60 * 1000`。callsite 置換 (-5 local const + -8 inline): backlog-aging.ts / completion-days-by-priority.ts / momentum.ts / slip-days.ts / wip-stuck.ts (5 local const) + stale-items.ts / freshly-done.ts / due-proximity.ts / velocity.ts / period-completion.ts (3 inline) (8 inline)。`date-tokens.ts` の 1 inline は他に `@/lib/date/iso` 不要だったため scope 外、次 refactor iter 候補。動作変更ゼロ (定数値完全同一)、affected 11 test file 235/235 PASS、architecture 5/5 PASS、typecheck 緑、lint warning baseline 1 維持。今後追加される day-基準 calc helper も 1 行 import で書ける + 単位を 1 file で変えられる (mock 用などで定数差替も可能)。次 iter361 = 361%5=1 → basics。
- ✅ [iter359 (wip-stuck)] **進行中だが停滞している WIP を抽出する pure helper (ai-automation, 1 commit, 96f703a)**: iter299 stale-items (全 non-done 対象 / 7 日閾値) と iter354 wip-by-priority (件数 / priority 分布のみで age 軸無し) の両方の盲点を埋める substrate。「ユーザが意図的に in_progress に着手したが N 日 updatedAt が動いていない」= 計画上の "止まり" signal を、stale-items より短い 3 日閾値 (default) で早期警告できる。新 module `src/features/item/wip-stuck.ts`: `StuckWipFields` ({status, updatedAt, doneAt, archivedAt} の structural subset)、`selectStuckWipItems(items, options?, today?)` で `status === 'in_progress' && doneAt/archivedAt 共 null && updatedAt が today - thresholdDays より古い` items を `{item, stuckDays}` 配列で stuckDays desc + 元配列順 stable で返す (default thresholdDays=3、updatedAt null/不正/未来は fail-soft 除外、ISO 文字列 today も受け付け)、`formatStuckWipSummaryJa(entries, limit=3)` で `'進行中だが停滞: 3 件 (タスクA 5 日 / タスクB 4 日 / タスクC 3 日)'` 形式 (limit 超えは「他 N 件」で集約、title 欠落 `(無題)` fallback、0 件 sentinel)、`stuckWipSeverity(entries)` で 3 値 ('severe' >= 7 日 stuck 含む / 'mild' / 'idle')。caller benefits: AI 朝 brief「進行中だが停滞: 3 件 (タスクA 5日 / タスクB 4日)」/ pm-agent watch list の「再開 nudge 候補」/ dashboard widget で WIP-bias chip 隣に WIP-stuck chip (severity 'severe'=red / 'mild'=amber / 'idle'=hidden、次 iter で UI bind 候補)。test +20 件 (select 10 / format 5 / severity 4 + 統合 1 = 20/20 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)、機能追加なし shadcn 編集なし。次 iter360 = 360%5=0 → refactor。
- ✅ [iter401 (stale-urgent-chip-priority-tooltip)] **stale-urgent chip の tooltip に priority breakdown を同梱 (basics, 1 commit, 0a5e84d)**: iter398 で配置済の stale-urgent chip に iter399 で整備済 priority 別 breakdown (`computeStaleUrgentByPriority` / `formatStaleUrgentByPriorityJa`) を SR / hover 経路でのみ読めるように同梱。iter363 hit-rate / iter366 due-coverage / iter373 hygiene-score / iter378 hygiene-focus / iter383 hygiene-debt / iter388 slipDays / iter391 aging と同手法、これで Dashboard の by-priority tooltip 9 chip 揃った。実装: dashboard-view.tsx の staleUrgent useMemo (iter398) に `computeStaleUrgentByPriority(itemsQ.data)` を追加、`countNonEmptyPriorityBucketsBy` で複数 P 分散判定 (>1)、priorityBuckets > 1 のときのみ `formatStaleUrgentByPriorityJa(byPriority)` を priorityDetail として保持。既存 topTitles と並列に extras 配列で組合せて detail に append (両方ある時は 'summary — top titles — P1 2 件 (最古 14日) / P2 1 件 (最古 20日)')。UX 卓越: a 発見可能性 (priority bias hover で見える、「P1 14 日放置」を即把握) / b アクセシビリティ (SR で priority 別 件数 + 最古日数) / c 状態網羅 (単一 P 偏在は priorityDetail null = 冗長省略) / g 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、9 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更、visible chip text 不変。test 既存 23/23 PASS (stale-urgent 18 + architecture 5)、typecheck/lint 緑 (warning baseline 1)。次 iter402 = 152%5=2 → ai-automation。
- ✅ [iter400 (oldestAgeDaysOf-集約)] **「null init → max-update reduce」 pattern の 3 inline callsite を 1 関数に集約 (refactor, 1 commit, 0a3a27b)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 / iter385 / iter390 / iter395 と同じ「同 shape の散在を 1 file に」方針の 15 弾目。「null init → loop で max-update → null/skip unknown」 reduce pattern の 3 inline callsite を `oldestAgeDaysOf(items, today?)` 1 関数に集約。callsite (-3): aged-hygiene-debt.ts (computeAgedHygieneDebt) 7 行 → 1 行 / stale-urgent.ts (formatStaleUrgentJa) 6 行 → 1 行 / stale-urgent.ts (computeStaleUrgentByPriority) 5 行 → 1 行。aged-hygiene-debt.ts は `getItemAge` import 不要になったので削除 (-1 import)、stale-urgent.ts は pickStaleUrgentItems の filter で `getItemAge` 維持。新 export (backlog-aging.ts に追加): `oldestAgeDaysOf<T extends {createdAt: Date|string|null|undefined}>(items, today?): number | null` — 空配列 / 全 unknown → null、createdAt 不正 ('unknown' 判定) は skip、未来 createdAt は ageDays=0 (getItemAge clamp 仕様)。caller benefits: 今後追加される age-based reduce (例: 平均 age / 中央値 age) も同 file に collocated、generic 部分は本 helper を build block に。test +6 件 (max / empty / 全 unknown / mixed unknown skip / single / future clamp = backlog-aging 27/27 + 関連 affected 62/62 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter401 = 151%5=1 → basics。
- ✅ [iter399 (stale-urgent-by-priority)] **stale-urgent を priority 別に集計する pure helper (ai-automation, 1 commit, 74841b0)**: iter397 整備済 stale-urgent (urgency × aging combinator) を priority 別 bucket で取り出す substrate。iter382 hygiene-debt-by-priority / iter387 slip-days-by-priority / iter389 backlog-aging-by-priority と並ぶ「× priority」軸 8 弾目。「P1 が一番 stale-urgent、最古 21 日 / P2 が次、最古 8 日」のように 高優先軸の停滞を分離して可視化、全体「対応 + 古参 5 件」だけでは見えない priority bias を露出。pickStaleUrgentItems の filter ロジックは bucketByPriorityWith 経由で priority 別に再適用 (P1 のみ / P2 のみ ... の各 bucket 内で stale-urgent 抽出)。新 export (stale-urgent.ts に追加): `StaleUrgentByPriorityStats {count, oldestDays|null}` (count=0 → oldestDays=null) / `computeStaleUrgentByPriority(items, options?)` で `Record<PriorityKey, ...>` (各 P bucket 必ず初期化、bucketByPriorityWith 経由で normalizePriority + null/範囲外 を P4 集約、options.minAgeDays / today を全 bucket に共通伝搬) / `formatStaleUrgentByPriorityJa(byPriority)` で 'P1 2 件 (最古 14日) / P2 1 件 (最古 20日)' 形式 / count=0 P 省略 / 全 0 → '対応 + 古参 0 件'。caller benefits: AI 朝 brief「P1 stale-urgent 14 日 → 即対応」を P 別で出せる / pm-agent priority 別 stale 候補 queue / dashboard stale-urgent chip の priority tooltip 同梱候補 (次 iter で iter363/366/373/378/383/388 と同手法で bind)。test +6 件 (compute 3 / format 3 = 18/18 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter400 = 150%5=0 → refactor。
- ✅ [iter398 (stale-urgent-chip)] **stale-urgent chip を Dashboard に追加 — 「対応 + 古参」 triage 候補を可視化 (basics, 1 commit, 77469c8)**: iter397 整備済 `pickStaleUrgentItems` / `formatStaleUrgentJa` substrate (urgency × aging combinator) を Dashboard chip に bind。critical / high かつ 7 日以上経過の item を「対応 + 古参」 triage 候補として可視化。urgent はあって当然 + stale も backlog 全体には散見、しかし両方該当する = 深刻アラーム signal なので警戒色 (red) で目立たせる。0 件で chip 非表示で UI 静か (= triage 不要状態は無音、progressive disclosure)。実装: `staleUrgent` useMemo (+10 行) — `pickStaleUrgentItems` で対象抽出 → 0 件で null return → `formatStaleUrgentJa` で 1 行 summary → aria-label / title に iter394 整備済 `formatTopUrgentTitlesJa(stale, 3)` を同梱して top 3 titles も読めるよう 2 層情報設計。chip JSX (+11 行) を urgency-tiers chip 直後に配置 (= 関連する actionable シリーズを近接)、glyph ⏳ (hourglass = 「時間が経った urgent」 metaphor、urgency-tiers の 🚨 と区別)、tone は固定 red (severity 分岐不要 = 該当時点で既に最深刻)、truncateText で長文を視覚的にトリム。UX 卓越: a 発見可能性 (red で即注意誘導) / b アクセシビリティ (SR で具体名読上げ「P1 提出書類が 14 日放置」) / c 状態網羅 (0 件 hidden / 1+ 件 red) / g 一貫性 (`formatTopUrgentTitlesJa` の vocabulary 再利用)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。test 既存 17/17 PASS (stale-urgent 12 + architecture 5)、typecheck/lint 緑 (warning baseline 1)。次 iter399 = 149%5=4 → ai-automation。
- ✅ [iter397 (pickStaleUrgentItems)] **urgency × aging combinator で「対応 + 古参」 triage 候補抽出 (ai-automation, 1 commit, 42b7e8c)**: iter294 整備済 urgency-tier substrate と iter337 整備済 backlog-aging substrate を 2 軸で交差させる combinator を新設。iter384 (`aged-hygiene-debt` = hygiene-debt × aging) と同パターンの 2 軸交差 substrate。単独 urgency-tier (P1 task は新規でも critical = 想定内) や単独 stale list (古い P4 idea ticket = 想定内) より signal 強度が高い: 「actionable + 古参」両方該当する = 深刻アラーム signal。新 module `src/features/item/stale-urgent.ts`: `StaleUrgentFields = UrgencyFields & {createdAt}` / `StaleUrgentOptions {minAgeDays?: number=7, today?: Date}` / `pickStaleUrgentItems(items, options?)` で urgency tier ∈ {critical, high} かつ age >= minAgeDays の items を urgency score 降順 → age 降順 → 元配列順 stable sort、done/archive と createdAt 不正は自動除外、minAgeDays<=0 で stale 条件無効化 / `formatStaleUrgentJa(items, options?)` で '対応 + 古参 3 件 (緊急 1 / 高 2、最古 14 日)' 形式 1 行 summary、0 件で '対応 + 古参 0 件'。caller benefits: AI 朝 brief「対応 + 古参 3 件 (緊急 1 / 高 2、最古 14 日) — 即着手」を 1 関数で / pm-agent watch list「最も停滞している urgent task」抽出 / dashboard 専用 chip 候補 (次 iter で UI bind 予定)。test +12 件 (pick 7 / format 5 = 12/12 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter398 = 148%5=3 → basics。
- ✅ [iter396 (urgency-tiers-chip-top-titles)] **urgency-tiers chip detail に top urgent titles を同梱 — 3 層情報設計に拡張 (basics, 1 commit, 71b1ddf)**: iter393 で配置済の urgency-tiers chip (visible は count のみ、a11y/hover に full counts) を、iter394 整備済 `formatTopUrgentTitlesJa` で top 3 urgent items の title + tier ラベル付き 1 行 summary も同梱して 3 層情報設計に拡張。iter363/366/373/378/383/388/391 系の richer-info-via-a11y/hover 同手法、ただし本 chip は「count → 全体内訳 → top items」の 3 段で他 chip より一段深い情報層。実装: dashboard-view.tsx の urgencyTiers useMemo (iter393) に `formatTopUrgentTitlesJa(itemsQ.data, 3)` を追加、detail を `${visible} — 全体: ${formatUrgencyTierCounts(counts)} — ${topTitles}` に拡張。例: visible で「要対応: 緊急 2 / 高 5」、aria-label / title で「要対応: 緊急 2 / 高 5 — 全体: 緊急 2 / 高 5 / 中 12 / 低 30 / 対象外 8 — 上位 urgent: 提出書類 (緊急) / 顧客対応 (緊急) / 報告書 (高)」が読める。UX 卓越: a 発見可能性 (hover で actionable item の具体名) / b アクセシビリティ (SR で全 tier 件数 + top 3 title) / c 状態網羅 (severity 'idle' は chip 非表示維持、severe/warn 時のみ top titles 同梱) / g 一貫性 (iter394 の `formatTopUrgentTitlesJa` の vocabulary を chip / AI prompt 双方で再利用)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更、visible chip text 不変。test 既存 76/76 PASS (urgency 71 + architecture 5)、typecheck/lint 緑 (warning baseline 1)。次 iter397 = 147%5=2 → ai-automation。
- ✅ [iter395 (urgencyTierOf-集約)] **`urgencyTier(computeUrgency())` の 4 callsite を 1 hop shorthand に集約 (refactor, 1 commit, 5844a43)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 / iter385 / iter390 と同じ「同 shape の散在を 1 file に」方針の 14 弾目。`urgencyTier(computeUrgency(item, today))` の 2 段 dispatch を「item → tier」 1 hop の shorthand `urgencyTierOf(item, today?)` に集約。callsite (-4): groupItemsByUrgencyTier / pickItemsByUrgencyTiers / countItemsByUrgencyTier / formatTopUrgentTitlesJa の 4 内部 callsite — 全て同一 pattern (`urgencyTier(computeUrgency(it, today))`) で urgency.ts 内に閉じていたが、今後追加される urgency 軸 helper も同じ pattern を踏襲するので 1 関数に集約。動作変更ゼロ (`urgencyTier` / `computeUrgency` を介して同じ実装)。caller benefits: 外部 caller (dashboard / AI prompt) も score を経由せずに「item → tier」が欲しい時 (例: tier 別 chip class 切替 / tier 別 priority dot 配色 / tier 別 SR aria-label) に 1 関数で書ける。`urgencyTier` / `computeUrgency` も export 維持。test +2 件 (4 priority + done = 5 ケース / proximity 上昇 high = 71/71 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter396 = 146%5=1 → basics。
- ✅ [iter394 (formatTopUrgentTitlesJa)] **AI 朝 brief 用「上位 urgent items」1 行 summary helper (ai-automation, 1 commit, c5db1f6)**: iter294 整備済 urgency-tier (5 段階分類) substrate に「title + tier ラベル」付き 1 行 summary を追加。iter392 で `pickItemsByUrgencyTiers` (subset filter) を追加した時の caller benefits で「AI 朝 brief 上位 X 件」を 1 関数で出すと書いていた件の補完 — 今までは `selectTopUrgentItems` が item[] を返すだけで、AI prompt に直接埋めるには caller が title + tier 連結を毎回書く必要があった。新 export (urgency.ts に追加): `UrgencyTitleFields` interface (`{title: string}`) + `formatTopUrgentTitlesJa<T extends UrgencyFields & UrgencyTitleFields>(items, n, today?)` — `selectTopUrgentItems` で top-N 抽出 → 各 item に `urgencyTier(computeUrgency())` で tier 推定 → `TIER_LABEL` (緊急 / 高 / 中 / 低) で短ラベル化 → '上位 urgent: タイトル1 (緊急) / タイトル2 (高)' 形式に format、top.length === 0 や n<=0 で `'緊急対応 0 件'`。caller benefits: AI 朝 brief「上位 urgent: 提出 (緊急) / 急ぎ (高)」を 1 関数で / pm-agent watch list の prompt セクション / dashboard hover tooltip の「上位 N 件」 detail (urgency-tiers chip iter393 の visible は count のみ、title list は次の段で chip 拡張候補)。test +6 件 (上位 N 件 format / 全 done 0 件 / n<=0 / n 超 / 空配列 / proximity 上昇 = 69/69 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter395 = 145%5=0 → refactor。
- ✅ [iter393 (urgency-tiers-chip)] **urgency-tier counts chip を Dashboard に追加 — actionable subset を可視化 (basics, 1 commit, 352330e)**: iter294 整備済 `countItemsByUrgencyTier` / `formatUrgencyTierCounts` substrate (priority + dueDate + MUST 由来の合成 score 5 段階分類) を Dashboard chip に bind。actionable subset (critical + high) を visible text で見せ、aria-label / title に full counts (medium / low / none 含む) を同梱して 2 層情報設計 (iter363/366/373/378/383/388/391 系の richer-info-via-a11y/hover 同手法)。新 export (urgency.ts に追加): `urgencyTierCountsSeverity(counts)` で `severe` (critical>0、赤強警告) / `warn` (high>0 のみ、琥珀警戒) / `idle` (chip 非表示) の 3 値判定 — slip-days / must-hygiene と同じ「重度 / 軽度 / 静か」 progressive disclosure モデル。実装: dashboard-view.tsx に `urgencyTiers` useMemo (+25 行) — counts 算出 → severity 判定 → idle で null return → visible text を「要対応: 緊急 X / 高 Y」に絞り、detail に `— 全体: ${formatUrgencyTierCounts}`、chip JSX (+19 行) を flex-wrap 先頭 (= 最も注目すべき actionable summary)、glyph 🚨 (alarm) で他 chip 8 兄弟と区別、tone は severity 別 (severe→red 直接 / warn→chipTone3Class)。UX 卓越: a 発見可能性 (actionable item ある時のみ chip 出現) / b アクセシビリティ (full counts SR 読上げ) / c 状態網羅 (severe / warn / idle 3 状態) / g 一貫性 (chip 兄弟揃い、glyph 区別)。機能追加なし、shadcn 編集なし。test +5 件 (severity 判定: critical>0 / warn 境界 / idle 境界 / 全 0 = 68/68 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter394 = 144%5=4 → ai-automation。
- ✅ [iter392 (pickItemsByUrgencyTiers)] **tier filter で actionable subset を抽出する pure helper (ai-automation, 1 commit, 1ab5cd4)**: iter294 並走 urgency-tier (5 段階分類) substrate に「subset filter」軸の薄い helper を追加。`groupItemsByUrgencyTier` (5 tier 全部 group) / `countItemsByUrgencyTier` (件数集計) / `selectTopUrgentItems` (top-N 抽出) と相補で、本 helper は「critical + high 等の指定 tier 集合に該当する items だけ stable 順序で返す」filter。AI 朝 brief / pm-agent / dashboard が「今すぐ動くべき items 一覧」(= critical + high の actionable subset) を 1 関数で取り出せる。selectTopUrgent (score 降順 N 件) と異なり、本 helper は tier 区分での「全件 cut-off」。新 export (urgency.ts に追加): `pickItemsByUrgencyTiers(items, tiers, today?): T[]` (tiers 空 → 空配列 early return、内部で Set 化して O(1) 判定、score 計算は 1 回/item、元配列順 stable)。caller benefits: AI 朝 brief「今日 critical + high 8 件 → 集中対応」/ pm-agent watch list / dashboard actionable list chip data source。test +5 件 (空 tiers / 単 tier / 複数 tier 結合 / stable order / done 除外 = 59/59 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter393 = 143%5=3 → basics。
- ✅ [iter391 (aging-chip-priority-tooltip)] **aging chip の aria-label に priority breakdown を同梱 (basics, 1 commit, 5e15444)**: iter338 で配置済の Backlog 年齢 chip に iter389 で整備済 priority 別 breakdown (countItemsByAgeByPriority / formatAgingByPriorityJa) を SR / hover 経路でのみ読めるように 2 層情報設計を投入。iter346 hit-rate / iter363 dod-coverage / iter366 due-coverage / iter373 hygiene-score / iter378 hygiene-focus / iter383 hygiene-debt / iter388 slipDays と同手法、これで Dashboard の by-priority tooltip 8 chip 揃った。実装: `countItemsByAgeByPriority(active)` を aging useMemo に追加、olderThanWeek > 0 時のみ `formatAgingByPriorityJa(byPriority)` を `stagnantBreakdown` として保持、chip の aria-label に condition 付きで `— 停滞: P1 2 件 / P3 1 件` を append、visible chip text 不変、'停滞 0 件' 文言は append しない (= 停滞無しは aria で語る価値なし)。iter390 で導入の `countNonEmptyPriorityBucketsBy` test を `Record<1|2|3|4, {score: number | null}>` 明示型で書き直し union 型対応を実例化 (typecheck fix)。UX 卓越: 発見可能性 / a11y (SR で「P1 2 件停滞」を行動指針) / 一貫性 (chip visible UI 不変、richer info は aria 経路のみ、8 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、priority 21/21 PASS。次 iter392 = 142%5=2 → ai-automation。
- ✅ [iter390 (countNonEmptyPriorityBucketsBy-集約)] **countNonEmptyPriorityBucketsBy generic で 3 dashboard predicate を集約 (refactor, 1 commit, 4f973ec)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 / iter385 と同じ「同 shape の散在を 1 file に」方針の 14 弾目。`PRIORITY_ORDER.filter((k) => predicate(byPriority[k])).length` の 3 inline pattern を generic helper に集約。dashboard chip の「priority breakdown を tooltip に出すか?」判定は by-priority substrate ごとに「empty」の意味が違う (due-hit-rate/coverage 系 `total === 0` / combined-hygiene `score === null` / hygiene-debt `debtCount === 0` / slip-days `count === 0`)。新 export `priority.ts#countNonEmptyPriorityBucketsBy<T>(byPriority, isNonEmpty)` は任意 predicate で priority bucket を数える generic、既存 `countNonEmptyPriorityBuckets(stats)` は本 helper の薄い wrapper に (後方互換、callers 5+ 件 不変)。callsite 置換 (-3 inline filter): dashboard-view.tsx の slipDays / hygieneScore / hygieneDebt useMemo の 3 箇所、import から PRIORITY_ORDER を削除 (もう dashboard では参照不要)。test +3 件 (countNonEmptyPriorityBuckets 1 / countNonEmptyPriorityBucketsBy 2 = 26/26 PASS)、動作変更ゼロ、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後追加される by-priority chip 判定も 1 行で書ける。次 iter391 = 141%5=1 → basics。
- ✅ [iter389 (backlog-aging-by-priority)] **backlog-aging を priority 別に集計する pure helper (ai-automation, 1 commit, 2b318fe)**: iter337 backlog-aging を priority 別 bucket で取り出す substrate。iter344 due-hit-rate / iter362 dod-coverage / iter364 due-date-coverage / iter372 combined-hygiene / iter382 hygiene-debt / iter387 slip-days と並ぶ「× priority」軸 7 弾目。低優先 backlog の積み残しは想定内、高優先 backlog の積み残しは要注意 = 異常検出に使える。「P1 が古参 3 件」のように 高優先軸の停滞を分離して可視化できる substrate。新 export (backlog-aging.ts に追加): `AgingByPriorityFields` (createdAt + priority)、`countItemsByAgeByPriority(items, today?)` で `Record<PriorityKey, Record<AgingKind, number>>` (iter365 で集約済 `bucketByPriorityWith` を活用、各 P bucket 必ず初期化、normalizePriority で null/範囲外 を p4)、`formatAgingByPriorityJa(byPriority)` で 1 行 summary ('停滞: P1 2 件 / P3 1 件' で stale + ancient 合算、0 件の P 省略、全 P 0 → '停滞 0 件')。caller benefits: AI 朝 brief「P1 古参 3 件 — 高優先 backlog 積残し検出」/ pm-agent priority 別停滞 queue / dashboard backlog chip の priority tooltip 同梱候補 (次 iter で iter363/366 と同手法で bind)。test +9 件 (compute 3 / format 3 / 既存 20 = 29/29 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter390 = 140%5=0 → refactor。
- ✅ [iter388 (slipDays-chip-priority-tooltip)] **slipDays chip の tooltip に priority breakdown を同梱 (basics, 1 commit, 71057c6)**: iter348 で配置済の slipDays chip に iter387 で整備済 priority 別 breakdown (computeSlipDaysByPriority / formatSlipDaysByPriorityJa) を SR / hover 経路でのみ読めるように 2 層情報設計を投入。iter346 hit-rate / iter363 dod-coverage / iter366 due-coverage / iter373 hygiene-score / iter378 hygiene-focus / iter383 hygiene-debt と同手法、これで Dashboard の by-priority tooltip 7 chip 揃った。実装: `computeSlipDaysByPriority(itemsQ.data)` を slipDays useMemo に追加、priorityBuckets > 1 (= 複数 priority に miss 分散) 時のみ `${summary} — ${formatSlipDaysByPriorityJa(byPriority)}` を `detail` として保持、chip の ariaLabel / title を `slipDays.detail` に差し替え。visible chip text 不変 (例: '遅延: 5 件 (平均 3.4日 / 中央値 2日 / 最大 12日)')、hover tooltip / SR 読上げ時のみ「… — 遅延: P1 3 件 (最大 14日) / P3 2 件 (最大 5日)」のような richer 文言。UX 卓越: 発見可能性 (priority bias hover で見える) / a11y (SR で全 P 読上げ、actionable 「P1 14 日」を行動指針) / 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、7 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、slip-days 21/21 PASS。次 iter389 = 139%5=4 → ai-automation。
- ✅ [iter387 (slip-days-by-priority)] **slip-days を priority 別に集計する pure helper (ai-automation, 1 commit, 331eabb)**: iter347 slip-days を priority 別 bucket で取り出す substrate。iter344 due-hit-rate / iter362 dod-coverage / iter364 due-date-coverage / iter372 combined-hygiene / iter382 hygiene-debt と並ぶ「× priority」軸 6 弾目。「P1 が一番遅延、最大 14 日 / P3 平均 2 日」のように 高優先タスクの遅延を分離して可視化、全体平均 3 日でも P1 が 14 日という隠れ pattern を露出。新 export (slip-days.ts に追加): `SlipDaysByPriorityFields` (priority + 既存 fields)、`computeSlipDaysByPriority(items, options?)` で `Record<PriorityKey, SlipDaysStats>` (iter365 で集約済 `bucketByPriorityWith` を活用、各 P bucket 必ず初期化、normalizePriority で null/範囲外 を p4、options.since 全 P 共通)、`formatSlipDaysByPriorityJa(byPriority)` で 1 行 summary ('遅延: P1 2 件 (最大 14日) / P3 1 件 (最大 2日)' / count=0 の P 省略 / 全 P count=0 → '遅延 0 件')。caller benefits: AI 朝 brief「P1 遅延 14 日 → 即対応」/ pm-agent priority 別遅延 queue / dashboard slip-days chip の priority tooltip 同梱候補 (次 iter で iter363/366 と同手法で bind)。test +5 件 (compute 3 / format 2 = 21/21 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter388 = 138%5=3 → basics。
- ✅ [iter386 (hygiene-debt-chip-aged-tooltip)] **hygiene-debt chip の tooltip に aged-debt 情報を同梱して 2D drill-down (basics, 1 commit, 8c58bd8)**: iter383 で配置済 priority breakdown と並列で、iter384 で整備済 aged-hygiene-debt.ts substrate (computeAgedHygieneDebt / formatAgedHygieneDebtJa、9/9 PASS) を bind。priority 軸 + 年齢軸の 2D drill-down で「P1 / P3 に分散していて、かつ 45 日放置の古参 debt がある」を 1 chip の tooltip で読めるように。iter378 の hygiene-focus chip 2D drill-down (priority+axis) と並列の hygiene-debt chip 2D (priority+age)。実装: `computeAgedHygieneDebt(itemsQ.data)` を hygieneDebt useMemo に追加、stagnantDebtCount > 0 (= ancient/stale 軸に debt あり) 時のみ `formatAgedHygieneDebtJa(aged)` を ageDetail として保持、priority detail と age detail を array でフィルタ → ' — ' で結合、visible chip text 不変 (`Triage 候補: 5`)、`data-stagnant-debt-count` + `data-oldest-age-days` data attr 追加。例: 'Hygiene Debt: 5 / 10 件 (50%) — title だけ items が triage 待ち — Hygiene Debt: P1 60% (3/5) — 停滞 Debt: 3 件 (古参 2 / 停滞 1) — 最古 45 日'。UX 卓越: 発見可能性 / a11y (actionable 文言「最古 45 日」も SR で読める) / 一貫性 (hygiene chip family の 2D 同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、aged-hygiene-debt 9/9 PASS。次 iter387 = 137%5=2 → ai-automation。
- ✅ [iter385 (rateToPct-集約)] **rateToPct helper を新設して Math.round(rate \* 100) 18 callsite を集約 (refactor, 1 commit, 210a8fe)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 / iter380 と同じ「同 shape の散在を 1 file に」方針の 13 弾目。0..1 rate を 0..100 整数 percent に変換する `Math.round(rate * 100)` の 18 inline callsite を `rateToPct(rate)` helper 1 つに集約。新 module `src/lib/format-rate.ts`: `rateToPct(rate: number): number` (0..100 整数、Math.round 0 桁丸め、入力域外は caller 責任 / clamp なし)、動作 1:1 (Math.round と同じ)、test +6 件 (0/1 境界 / tie-break / 典型値 / 0.005 half-up)。callsite 置換 (-19 行): combined-hygiene (4) / description-coverage (2) / dod-coverage (2) / due-date-coverage (2) / due-hit-rate (2) / hygiene-debt (2) / hygiene-axis-focus (1) / dashboard-view (4) = 18 件。動作変更ゼロ、affected 9 file 139/139 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後追加される rate-format helper も 1 行で書ける + 丸め policy を 1 file で変えられる。次 iter386 = 136%5=1 → basics。
- ✅ [iter384 (aged-hygiene-debt)] **古い hygiene-debt items を triage 緊急候補として抽出する combinator (ai-automation, 1 commit, 84efb90)**: iter379 hygiene-debt (= title だけ items) と iter337 backlog-aging (= createdAt 年齢バケット) を組合せて「古いが計画化されていない」 stale brain-dump を 1 関数で抽出する combinator substrate。「単なる debt 件数」より一段絞り込んだ「最も緊急度の高い triage 候補」を出す: 30 日以上放置されていて planning fields が 1 つも無い = 「忘却された brain-dump」 pattern。新 module `src/features/item/aged-hygiene-debt.ts`: `computeAgedHygieneDebt(items, today?)` で `{totalDebt, byAge, oldestAgeDays, stagnantDebtCount}` (内部で pickHygieneDebtItems → countItemsByAge、oldestAgeDays=debt 最古、stagnantDebtCount=ancient+stale 合算)、`formatAgedHygieneDebtJa(stats)` で 1 行 summary ('停滞 Debt: 3 件 (古参 2 / 停滞 1) — 最古 45 日' / 'Debt: 1 件 / 全件新規 — 停滞無し' / '未完了 Debt 0 件 (該当なし)')、`pickStagnantDebtItems(items, today?)` で「停滞 debt」のみを stable 順序で返す薄い filter (Dashboard hover / AI brief 用)。caller benefits: AI 朝 brief「45 日放置の title だけ ticket あり、要 triage」/ pm-agent stagnant brain-dump 一覧化 / dashboard aged-debt chip 候補 (次 iter で UI bind)。test +9 件 (compute 4 / format 3 / pick 2 = 9/9 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter385 = 135%5=0 → refactor。
- ✅ [iter383 (hygiene-debt-chip-priority-tooltip)] **hygiene-debt chip の tooltip に priority breakdown を同梱 (basics, 1 commit, aa386bb)**: iter381 で配置済の hygiene-debt chip に iter382 で整備済 priority 別 breakdown (computeHygieneDebtByPriority / formatHygieneDebtByPriorityJa) を SR / hover 経路でのみ読めるように 2 層情報設計を投入。iter346 hit-rate / iter363 dod-coverage / iter366 due-coverage / iter373 hygiene-score / iter378 hygiene-focus と同手法、これで Dashboard の hygiene chip 5 種で priority tooltip が揃った。実装: `computeHygieneDebtByPriority(itemsQ.data)` を hygieneDebt useMemo に追加、debtBuckets > 1 (= 複数 priority に debt 分散) 時のみ `${summary} — ${formatHygieneDebtByPriorityJa(byPriority)}` を `detail` として保持、chip の ariaLabel / title を `hygieneDebt.detail` に差し替え。visible chip text 不変 (`Triage 候補: 3`)、hover tooltip / SR 読上げ時のみ「Hygiene Debt: 3 / 10 件 (30%) — title だけ items が triage 待ち — Hygiene Debt: P1 67% (2/3) / P3 50% (1/2)」のような richer 文言。UX 卓越: 発見可能性 (priority bias hover で見える) / a11y (SR で全 P 読上げ) / 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、5 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、hygiene-debt 20/20 PASS。次 iter384 = 134%5=4 → ai-automation。
- ✅ [iter382 (hygiene-debt-by-priority)] **hygiene-debt を priority 別に集計する pure helper (ai-automation, 1 commit, 70ea2b9)**: iter379 hygiene-debt (= 期限/DoD/説明文 全 unset の triage 候補集計) を priority 別 bucket で取り出す substrate。iter344 due-hit-rate / iter362 dod-coverage / iter364 due-date-coverage / iter372 combined-hygiene と並ぶ「× priority」軸 5 弾目。「軽い気持ちで P1 ticket を作って計画化漏れ」 pattern を高優先軸で検出可能、全体 debt 30% でも P1 だけが 60% という隠れ pattern を露出。新 export (hygiene-debt.ts に追加): `HygieneDebtByPriorityFields` / `computeHygieneDebtByPriority(items)` で `Record<PriorityKey, HygieneDebtStats>` (iter365 で集約済 `bucketByPriorityWith` を活用、各 P bucket 必ず初期化、normalizePriority で null/範囲外 を p4)、`formatHygieneDebtByPriorityJa(byPriority)` で 1 行 summary ('Hygiene Debt: P1 67% (2/3) / P3 50% (1/2)' / debtCount=0 / total=0 の P は省略 / 全空 → '未完了 0 件 (該当なし)')。caller benefits: AI 朝 brief「P1 が一番 debt 多い → まず P1 から triage」/ pm-agent priority 別 triage queue / dashboard hygiene-debt chip の priority tooltip 同梱候補 (次 iter で iter363/366 と同手法で bind)。test +5 件 (compute 3 / format 2 = 20/20 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter383 = 133%5=3 → basics。
- ✅ [iter381 (dashboard-hygiene-debt-chip)] **planning hygiene debt (triage 候補) chip を Dashboard に追加 (basics, 1 commit, 5141029)**: iter379 で整備済 hygiene-debt.ts substrate (computeHygieneDebt / formatHygieneDebtJa / hygieneDebtTone、15/15 PASS) の UI bind。期限 / DoD / 説明文 を 1 つも持たない「title だけの brain-dump」 item を triage 候補として可視化。debtCount=0 で chip 非表示 (= triage 不要状態は UI 静か)。実装: `computeHygieneDebt(itemsQ.data)` を hygieneDebt useMemo (existing useItems data 流用、debtCount=0 で chip 非表示)、`<DashboardChip>` で chipTone3Class(hygieneDebt.tone) で 3 段階配色、glyph 📋 (clipboard / triage 候補 metaphor、hygiene-score chip 🧭 / hygiene-focus chip ⚠ と区別)、visible chip text 短く `Triage 候補: 3`、aria-label/title に full text ('Hygiene Debt: 3 / 10 件 (30%) — title だけ items が triage 待ち')、`data-testid="dashboard-hygiene-debt-chip"` + data-debt-count + data-debt-total hook。UX 卓越: 発見可能性 (debt 検出時のみ chip 出現 → progressive disclosure) / a11y (full text in aria, "title だけ items が triage 待ち" の actionable 文言を SR で読める) / 4 状態網羅 (null=未読込 / debt=0 非表示 / debt 少数=neutral / debt 多数=warn) / 一貫性 (hygiene chip 6 兄弟揃い: hygiene-score / hygiene-focus / debt / due-coverage / dod-coverage / desc-coverage、同 vocabulary / glyph 区別)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、hygiene-debt 15/15 PASS。次 iter382 = 132%5=2 → ai-automation。
- ✅ [iter380 (normalizePriority-完全集約)] **completion-days-by-priority の local normalizePriority / PriorityKey 重複を priority.ts に集約 (refactor, 1 commit, faf9d74)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 / iter375 と同じ「同 shape の散在を 1 file に」方針の 12 弾目。iter375 の補完 — `PRIORITY_ORDER` 集約時に local `PriorityKey` / `normalizePriority` 重複は「次 refactor iter で別途集約候補」と handoff に書いてた件を消化。変更: completion-days-by-priority.ts の local normalizePriority 関数を削除 (priority.ts の同 export と完全同一実装、4 行)、local `export type PriorityKey` を `export type { PriorityKey }` re-export 化 (priority.ts から import、後方互換維持、import callers 0 件だが念のため re-export)、import 文を 1 行に集約 (PRIORITY_ORDER + normalizePriority + PriorityKey)。動作変更ゼロ、affected 2 file 24/24 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。これで src/features/item 配下の priority normalize / PriorityKey 重複は完全に priority.ts 1 file に統一。次 iter381 = 131%5=1 → basics。
- ✅ [iter379 (hygiene-debt)] **planning fields 全 unset の item を triage 候補として抽出する pure helper (ai-automation, 1 commit, 52b4a99)**: iter349 / iter352 / iter367 の各軸 coverage は「軸別の充実度」を出すが、本 helper はそれらの **共通否定 (= 期限/DoD/説明文 全 3 軸 unset)** を 1 関数で出す substrate。「title だけ書いた brain-dump」「未着手 backlog の triage 候補」を planning debt metric として扱う。新 module `src/features/item/hygiene-debt.ts`: `computeHygieneDebt(items)` で `{total, debtCount, rate}` (集計対象 = 未完了+未archive、debt = dueDate 不正 ISO含む + dod 空 + description 空、既存 coverage helper と同 null-vocabulary)、`formatHygieneDebtJa(stats)` で 3 状態 1 行 ('Hygiene Debt: 3 / 10 件 (30%) — title だけ items が triage 待ち' / 'Hygiene Debt: 0 件 (全 item に planning fields あり)' / '未完了 0 件 (該当なし)')、`hygieneDebtTone(stats)` (warn >= 0.3 / neutral 0.1..0.3 / good < 0.1 / null=neutral)、`pickHygieneDebtItems(items)` で実際の triage 対象 item list を stable 順序で返す filter (dashboard hover で具体 item 出す用)。caller benefits: AI 朝 brief「Hygiene Debt 30% — まず 3 件を triage」/ pm-agent「title だけ items 一覧化」/ dashboard hygiene-debt chip 候補 (次 iter で UI bind)。test +15 件 (compute 6 / format 3 / tone 4 / pick 2 = 15/15 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter380 = 130%5=0 → refactor。
- ✅ [iter378 (hygiene-focus-axis-tooltip)] **hygiene-focus chip の tooltip に最弱軸を同梱して 2D drill-down (basics, 1 commit, 5b4f668)**: iter376 で配置済の「重点: P1 Hygiene 25」 hygiene-focus chip に、iter377 で整備済 hygiene-axis-focus.ts substrate (pickWeakestHygieneAxis / formatHygieneAxisFocusJa、11/11 PASS) を bind。焦点 priority 内のさらに最弱軸 (期限 / DoD / 説明文) を tooltip / aria に同梱し、「P3 が一番弱い → その中で 説明文 が弱い」を 1 経路で読める 2D drill-down UX。実装: `pickWeakestHygieneAxis(byPriority[focus.priority])` を hygieneFocus useMemo に追加 (CombinedHygieneScore を直接渡す、計算済 byPriority を再利用)、`detail = '${summary} / ${formatHygieneAxisFocusJa(weakestAxis)}'` (例: '要注意: P3 Hygiene 25 — planning 漏れ多、計画化推奨 / 重点軸: 説明文 0% — まず説明文の充実から')、chip の ariaLabel / title を `hygieneFocus.detail` に差し替え (iter363/366/373 と同手法)、visible chip text 不変、`data-focus-axis` data attr 追加 (E2E hook)。UX 卓越: 発見可能性 (1 chip で 2D 情報 = priority + 軸) / a11y (SR で full text in aria, 行動指針 2 段階) / 一貫性 (iter363/366/373 で確立した tooltip 同梱手法を踏襲、visible UI 静か)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、hygiene-axis-focus 11/11 PASS。次 iter379 = 129%5=4 → ai-automation。
- ✅ [iter377 (hygiene-axis-focus)] **planning hygiene の最弱軸 (期限/DoD/説明文) を抽出する pure helper (ai-automation, 1 commit, d19729f)**: iter374 hygiene-focus (priority 別の最弱) の対称軸 substrate。同じ CombinedHygieneScore (iter369) を入力に「3 軸 (期限 / DoD / 説明文) のうちどれが一番弱いか」を 1 関数で出す。両者を組合せれば「P1 の説明文 が一番弱い」のような 2D drill-down 用 substrate が成立。新 module `src/features/item/hygiene-axis-focus.ts`: `HygieneAxis = 'dueDate' | 'dod' | 'description'`、`HYGIENE_AXIS_ORDER` (着手依存順 = tie-break / iteration 規範)、`pickWeakestHygieneAxis(score)` で `HygieneAxisFocus | null` (集計対象 = rate !== null、同 rate 同点 → axis 順序を優先 = 着手依存順 stable、全軸 null → null)、`formatHygieneAxisFocusJa(focus)` で 3 段階 1 行 summary (rate < 0.3 '重点軸: 説明文 25% — まず説明文の充実から' / 0.3..0.7 '重点軸: 説明文 50% — 改善余地あり' / rate >= 0.7 '重点軸: 説明文 80% (全軸良好)' / null '未完了 0 件 (該当なし)')。caller benefits: AI 朝 brief で軸別重点 1 行 (priority + 軸 で 2D 切り口) / pm-agent「説明文軸が一番弱い → テンプレ提案」/ dashboard 重点軸 chip 補完候補。test +11 件 (pick 6 / format 5 = 11/11 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter378 = 128%5=3 → basics。
- ✅ [iter376 (dashboard-hygiene-focus-chip)] **planning hygiene 最弱 priority CTA chip を Dashboard に追加 (basics, 1 commit, 325cc1b)**: iter374 で整備済 hygiene-focus.ts substrate (pickWeakestHygienePriority / formatHygieneFocusJa、11/11 PASS) の UI bind。priority 別 hygiene score の最弱バケットが warn tier (< 30) のときだけ「重点: P1 Hygiene 25」 CTA chip を表示する progressive disclosure UX。実装: `pickWeakestHygienePriority(byPriority)` を useMemo に追加 (`itemsQ.data` のみ依存、computeCombinedHygieneByPriority を内部で呼出)、focus === null || focus.tone !== 'warn' で chip 非表示 (= 良好 / 中立は既存 hygiene-score chip と冗長 → 行動が必要なときだけ chip が出る UX)、`<DashboardChip>` で chipTone3Class('warn') 固定 (warn 時のみ表示)、glyph ⚠ (注意 metaphor、hygiene-score chip 🧭 と区別)、visible chip text 短く `重点: P1 Hygiene 25`、aria-label/title に full text (warn 時の suffix 「— planning 漏れ多、計画化推奨」を SR で読める)、`data-testid="dashboard-hygiene-focus-chip"` + data-focus-priority + data-focus-score hook。UX 卓越: 発見可能性 (warn 時のみ目立つ chip → attention 集中) / a11y (full text in aria) / 4 状態網羅 (null / good 非表示 / neutral 非表示 / warn 表示) / 一貫性 (hygiene chip 4 兄弟 + focus = 5 chip family、既存 vocabulary / layout / tone tooltip 同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、hygiene-focus 11/11 PASS。次 iter377 = 127%5=2 → ai-automation。
- ✅ [iter375 (PRIORITY_ORDER-集約)] **PRIORITY_ORDER を priority.ts から export し inline [1,2,3,4] を集約 (refactor, 1 commit, 5385233)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 / iter370 と同じ「同 shape の散在を 1 file に」方針の 11 弾目。priority.ts に既に存在した private const `PRIORITY_ORDER` を export 化、9 callsite (8 src/features/item + 1 src/components/workspace) に散らばった inline `[1, 2, 3, 4] as const` を 1 source of truth に集約。変更: priority.ts の private const → public export (countNonEmptyPriorityBuckets / formatNonZeroCounts は既に活用)、due-hit-rate / due-date-coverage / dod-coverage / description-coverage / combined-hygiene-by-priority / hygiene-focus / wip-by-priority (2 callsite) / dashboard-view.tsx (iter373 で追加した eligibleBuckets filter) で `for (const k of PRIORITY_ORDER)` / `PRIORITY_ORDER.filter(...)` に置換。completion-days-by-priority.ts の local 重複 PRIORITY_ORDER を削除して import 化 (-1 const、local `normalizePriority` / `PriorityKey` の重複は次 refactor iter で別途集約候補)。動作変更ゼロ、affected 10 file 156/156 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後追加される by-priority helper も `for (const k of PRIORITY_ORDER)` で書ける。次 iter376 = 126%5=1 → basics。
- ✅ [iter374 (hygiene-focus)] **planning hygiene の最弱 priority を抽出する pure helper (ai-automation, 1 commit, f0e3fda)**: iter372 で priority 別 hygiene score を整備済、本 iter は「では今どこから手をつけるか?」を 1 行に絞る焦点抽出 combinator。AI 朝 brief / pm-agent / dashboard widget が「重点: P1 Hygiene 25 (planning 漏れ多)」のような 1 行 actionable signal を 1 関数で取り出せる substrate。新 module `src/features/item/hygiene-focus.ts`: `pickWeakestHygienePriority(byPriority)` で `HygieneFocus | null` (集計対象 = score !== null、同 score 同点 → priority 数値小 = P1 寄り を優先 / 重要度高、全 P 空 → null、tone は combinedHygieneTone と同 3 値)、`formatHygieneFocusJa(focus)` で tone 別 1 行 summary (warn '要注意: P1 Hygiene 20 — planning 漏れ多、計画化推奨' / neutral '重点: P1 Hygiene 50 — 計画化余地あり' / good '良好: P1 Hygiene 80' / null '未完了 0 件 (該当なし)')。caller benefits: AI 朝 brief で attention 集中 / pm-agent「P3 が一番 planning 漏れ → 今日は P3 計画化」/ dashboard widget で強調 chip 1 個に絞れる (次 iter で hygiene-score chip の補完 chip 候補)。test +11 件 (pick 7 / format 4 = 11/11 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter375 = 125%5=0 → refactor。
- ✅ [iter373 (hygiene-score-chip-priority-tooltip)] **hygiene-score chip の tooltip に priority breakdown を同梱 (basics, 1 commit, 9fcd495)**: iter371 で配置済の Planning Hygiene 統合 score chip に SR / hover 経路で priority 別 breakdown (iter372) を読めるように 2 層情報設計を投入。iter346 hit-rate / iter363 dod-coverage / iter366 due-coverage と同手法、これで Dashboard の hygiene chip 4 種 (hit-rate / due-coverage / dod-coverage / hygiene-score) すべてに priority tooltip が揃った。実装: `computeCombinedHygieneByPriority(itemsQ.data)` を hygieneScore useMemo に追加、eligibleBuckets > 1 (= 複数 P に未完了分散) 時のみ `${summary} — ${formatCombinedHygieneByPriorityJa(byPriority)}` を `detail` に保持、chip の ariaLabel / title を `hygieneScore.detail` に差し替え。visible chip text 不変 (`Hygiene 70`)、hover tooltip / SR 読上げ時のみ「Planning Hygiene: 70 (期限 80% / DoD 60% / 説明文 70%) — Planning Hygiene: P1 100 / P3 30」のような richer 文言。UX 卓越: 発見可能性 (priority bias hover で見える) / a11y (SR で全 P 読上げ) / 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、4 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、combined-hygiene-by-priority 7/7 PASS。次 iter374 = 124%5=4 → ai-automation。
- ✅ [iter372 (combined-hygiene-by-priority)] **planning hygiene 統合 score を priority 別に集計する pure helper (ai-automation, 1 commit, fc4358c)**: iter369 combined-hygiene を「× priority」軸で展開する 4 弾目 substrate。iter344 due-hit-rate / iter362 dod-coverage / iter364 due-date-coverage で揃えた priority 別パターンを統合 score にも適用、これで「× priority」軸 substrate が 4 領域で揃った。AI 朝 brief / pm-agent / dashboard widget が「P1 Hygiene 30 — 重要 item の planning 漏れ」のような **優先度別の planning hygiene 弱点** を 1 関数で取り出せる。全体 score が健全に見えても P1 だけ低い隠れ pattern を露出。新 module `src/features/item/combined-hygiene-by-priority.ts`: `CombinedHygieneByPriorityFields` (3 substrate fields の union)、`computeCombinedHygieneByPriority(items)` で `Record<PriorityKey, CombinedHygieneScore>` (内部で 3 既存 by-priority helper 結果を combineHygieneScore に流し込み、各 P bucket 必ず初期化)、`formatCombinedHygieneByPriorityJa(byPriority)` で 1 行 summary (`'Planning Hygiene: P1 100 / P3 0'` / 全 P 空 → `'未完了 0 件 (該当なし)'`)。caller benefits: pm-agent priority 別 hygiene 弱点検出 / dashboard hygiene-score chip の priority tooltip 同梱 (iter366 と同手法で次 iter で bind 候補)。test +7 件 (compute 5 / format 2 = 7/7 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter373 = 123%5=3 → basics (本 substrate の dashboard hygiene-score chip tooltip bind が自然な次手)。
- ✅ [iter371 (dashboard-hygiene-score-chip)] **planning hygiene 統合 score chip を Dashboard に追加 (basics, 1 commit, 84f286a)**: iter369 で整備済 combined-hygiene.ts substrate (combineHygieneScore / formatCombinedHygieneJa / combinedHygieneTone、14/14 PASS) の UI bind。3 個別 hygiene chip (期限 / DoD / 説明文) と並列で「Hygiene 70」単一 score chip を Dashboard に追加、全体としての planning hygiene 健全度を 1 数字で一望可能に。実装: `combineHygieneScore({dueDate, dod, description})` を useMemo (3 既存 useMemo の `stats` を再利用、二重計算回避)、score=null で chip 非表示、3 個別 chip の前に配置 (= 単一 score → 詳細 3 軸 の drill-down UX)、`<DashboardChip>` 経由で chipTone3Class 3 段階配色、glyph 🧭 (compass / 健全度の方向 metaphor)、visible chip 短く `Hygiene 70`、aria-label/title に full text、`data-testid="dashboard-hygiene-score-chip"` + data-hygiene-score + data-eligible-axes hook。UX 卓越: 発見可能性 (1 数字で一目) / a11y (full text in aria) / 4 状態網羅 / minimal visible + richer hover. 機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、combined-hygiene 14/14 PASS。次 iter372 = 122%5=2 → ai-automation。
- ✅ [iter370 (countNonEmptyPriorityBuckets-集約)] **countNonEmptyPriorityBuckets を priority.ts の generic に集約 (refactor, 1 commit, c55e14d)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 / iter365 と同じ「同 shape の散在を 1 file に」方針の 10 弾目。iter346 で due-hit-rate.ts に閉じていた `countNonEmptyPriorityBuckets` を priority.ts の generic helper として汎化。dashboard-view.tsx の 4 hygiene chip useMemo で 3 callsite に inline `([1,2,3,4] as const).filter((k) => byPriority[k].total > 0).length` が散らばっていたので集約。新 export `priority.ts#countNonEmptyPriorityBuckets(byPriority)` 引数 = `Record<PriorityKey, {total: number}>` 満たす任意の by-priority stats、戻り値 = 0..4 整数。callsite 置換 (-12 行): due-hit-rate.ts は re-export 化 (後方互換)、dashboard-view.tsx 3 chip useMemo (dueCoverage / dodCoverage / descCoverage) の inline filter を関数呼出に統一、import を `@/features/item/priority` に集約。動作変更ゼロ、affected test 50+ 件 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter371 = 121%5=1 → basics。
- ✅ [iter369 (combined-hygiene)] **planning hygiene 3 軸を統合 score にまとめる pure helper (ai-automation, 1 commit, e8bf0b6)**: iter349 due-date-coverage / iter352 dod-coverage / iter367 description-coverage の 3 軸 (期限 / DoD / 説明文) を 1 つの 0..100 score に統合する combinator substrate。AI 朝 brief / pm-agent / dashboard widget が「Planning Hygiene 70 (期限 80% / DoD 60% / 説明文 70%)」のような単一 KPI として扱える。新 module `src/features/item/combined-hygiene.ts`: `combineHygieneScore(input)` で `{score, dueDateRate, dodRate, descriptionRate, eligibleAxes}` (3 stats を struct で受ける combinator、score = 各軸 coverageRate avg × 100 整数、null 軸は分母から除外、全軸 null → score=null + eligibleAxes=0)、`formatCombinedHygieneJa(score)` で 1 行 summary、`combinedHygieneTone(score)` で 'good' (>= 70) / 'neutral' (30..70) / 'warn' (< 30) / 'neutral' (null)。caller benefits: AI 朝 brief 単一 KPI / pm-agent「Hygiene 30 切り — 重大 planning 漏れ」/ dashboard 単一 chip で 3 hygiene chip を要約。test +14 件 (combine 7 / format 3 / tone 4 = 14/14 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter370 = 120%5=0 → refactor。
- ✅ [iter368 (dashboard-desc-coverage-chip)] **説明文カバレッジ chip を Dashboard に追加 (basics, 1 commit, c23b5c0)**: iter367 で整備済 description-coverage.ts substrate (computeDescriptionCoverage / formatDescriptionCoverageJa / descriptionCoverageTone / by-priority、11/11 PASS) の UI bind。dod-coverage chip の隣に「説明文カバレッジ: 60% (3 / 5 件 — 未設定 2 件)」chip を追加、planning hygiene を「期限 / DoD / 説明文」3 軸で Dashboard に可視化。これで Dashboard の hygiene chip 3 兄弟が完成。実装: `computeDescriptionCoverage(itemsQ.data)` を useMemo (existing useItems data 流用)、total=0 で chip 非表示、iter346 / iter363 / iter366 と同 priority tooltip パターン (`computeDescriptionCoverageByPriority` を計算、nonEmptyBuckets > 1 時のみ priority breakdown を `detail` に同梱)、iter355 で導入した `<DashboardChip>` を活用、chipTone3Class('good'/'neutral'/'warn') で 3 段階配色、glyph 📝 (memo / 説明文 metaphor、期限 chip 📅 / DoD chip ✓ と区別)、`data-testid="dashboard-desc-coverage-chip"` + data-coverage-pct hook。UX 卓越: 発見可能性 / a11y (priority breakdown 同梱) / 4 状態網羅 / 一貫性 (hygiene chip 3 兄弟で同 vocabulary / layout / tone / tooltip 同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、description-coverage 11/11 PASS。次 iter369 = 119%5=4 → ai-automation。
- ✅ [iter367 (description-coverage)] **未完了 item の description カバレッジを計算する pure helper (ai-automation, 1 commit, 07615b7)**: iter349 due-date-coverage / iter352 dod-coverage と並ぶ「× 説明文」軸 substrate。description が未設定 = 「title だけで詳細不明 → 着手直前に都度確認が必要」 pattern を planning hygiene metric として扱う。新 module `src/features/item/description-coverage.ts`: `computeDescriptionCoverage(items)` で `{total, withDescription, withoutDescription, coverageRate}` (集計対象 = `doneAt == null && archivedAt == null`、withDescription = description が non-empty `description.trim().length > 0` で判定)、`formatDescriptionCoverageJa(stats)` で 1 行 summary、`descriptionCoverageTone(stats)` で 3 値 tone (due-date-coverage と同閾値)、`computeDescriptionCoverageByPriority(items)` (iter365 で集約した `bucketByPriorityWith` を活用、3 兄弟 by-priority と同 shape)、`formatDescriptionCoverageByPriorityJa(byPriority)` で priority 別 1 行 summary。これで「× 未完了 hygiene」軸 substrate が due-date / dod / description の 3 種で揃った (= title 以外の 3 主要 metadata 全て network 化)。caller benefits: AI 朝 brief / pm-agent / dashboard hygiene chip 3 兄弟 + priority tooltip。test +11 件 (compute 5 / format 2 / tone 2 / byPriority 2 = 11/11 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter368 = 118%5=3 → basics (本 substrate の dashboard chip bind が自然な次手)。
- ✅ [iter366 (due-coverage-chip-priority-tooltip)] **due-coverage chip の tooltip / aria-label に priority breakdown を同梱 (basics, 1 commit, 66f8548)**: iter351 で配置済の due-coverage chip に SR / hover 経路でのみ priority 別 breakdown (iter364) を読めるように 2 層情報設計を投入。iter346 hit-rate-tooltip / iter363 dod-coverage-tooltip と同手法、これで Dashboard の hygiene chip 3 種 (hit-rate / dod-coverage / due-coverage) すべてに priority tooltip が揃った。実装: `computeDueDateCoverageByPriority(itemsQ.data)` を useMemo に追加、nonEmptyBuckets > 1 時のみ `${summary} — ${formatDueDateCoverageByPriorityJa(byPriority)}` を `detail` として保持、chip の aria-label / title を `dueCoverage.detail` に差し替え。visible chip text 不変、hover tooltip / SR 読上げ時のみ「期限カバレッジ: 60% (3 / 5 件 — 未設定 2 件) — 期限カバレッジ: P1 100% (2/2) / P3 50% (1/2)」のような richer 文言。UX 卓越: 発見可能性 (priority bias hover で見える) / a11y (SR で全 P 読上げ) / 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ、3 chip で同手法揃い)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、due-date-coverage 26/26 PASS。次 iter367 = 117%5=2 → ai-automation。
- ✅ [iter365 (bucketByPriorityWith-集約)] **bucketByPriorityWith に 3 by-priority helper を集約 (refactor, 1 commit, 7fdc9aa)**: iter325 / iter330 / iter340 / iter345 / iter350 / iter355 / iter360 と同じ「同 shape の散在を 1 file に」方針の 9 弾目。iter344 / iter362 / iter364 で全く同 shape のコード (`buckets = {1:[],...}` → `for it: buckets[normalizePriority(it.priority)].push(it)` → `{1: compute(buckets[1]), 2: ..., 3: ..., 4: ...}`) が 3 重複していたので集約。新 export `priority.ts#bucketByPriorityWith<T, R>(items, compute)`: 入力 = items + compute (1 bucket → R)、出力 = `Record<PriorityKey, R>` (各 bucket 必ず初期化、normalizePriority で null/範囲外 を p4)、内部で既存 `groupItemsByPriority` を再利用。callsite 置換 (-30 行 / 3 file、各 1 関数 4 行 → 1 行): due-hit-rate / dod-coverage / due-date-coverage の `compute*ByPriority` を `bucketByPriorityWith(items, g => compute*(g, options))` に。動作変更ゼロ (3 callsite 既存 test 60+ 件 PASS、bucket 順序 / normalize / 初期化 1:1 互換)。test +4 件 (bucketByPriorityWith empty / group / normalize / stable)、affected 5 file 95/95 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。今後追加される `compute*ByPriority` も同 combinator で 1 行で書ける。次 iter366 = 116%5=1 → basics。
- ✅ [iter364 (due-date-coverage-by-priority)] **期限カバレッジを priority 別に集計する pure helper (ai-automation, 1 commit, be8de2b)**: iter344 (due-hit-rate-by-priority) / iter362 (dod-coverage-by-priority) と並ぶ「× priority」軸 substrate 3 兄弟。iter349 due-date-coverage (= 全体) を priority 別 bucket で取り出し、AI brief / pm-agent / dashboard が「P1 期限カバレッジ 100% / P3 50%」のように priority 別 planning 漏れを 1 関数で出せる。新 export (`due-date-coverage.ts`): `DueDateCoverageByPriorityFields`、`computeDueDateCoverageByPriority(items)` で `Record<PriorityKey, DueDateCoverageStats>` (normalizePriority で null/範囲外 を p4、各 bucket 必ず初期化、内部で `computeDueDateCoverage` を再利用)、`formatDueDateCoverageByPriorityJa(byPriority)` で 1 行 summary (`'期限カバレッジ: P1 100% (2/2) / P3 50% (1/2)'` / `'未完了 0 件 (該当なし)'`、total=0 の P 省略)。これで「× priority」軸 substrate が due-hit-rate / dod-coverage / due-date-coverage の 3 領域で揃った。caller benefits: AI 朝 brief / pm-agent / dashboard priority 別 hygiene chip tooltip 同梱。test +5 件 (computeByPriority 3 / format 2 = 26/26 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter365 = 115%5=0 → refactor。
- ✅ [iter363 (dod-coverage-chip-priority-tooltip)] **dod-coverage chip の tooltip / aria-label に priority breakdown を同梱 (basics, 1 commit, 05258be)**: iter353 で配置済の dod-coverage chip の visible 表記は全体集計のままで、SR / hover 経路でのみ priority 別 breakdown (iter362) を読めるように UI を richer に。iter346 hit-rate-tooltip と同手法 (= visible UI 不変、richer info は a11y / hover 経路のみ提供する 2 層情報設計)。実装: `computeDodCoverageByPriority(itemsQ.data)` を useMemo に追加、nonEmptyBuckets > 1 時のみ `${summary} — ${formatDodCoverageByPriorityJa(byPriority)}` を `detail` として保持 (単一 priority 偏在は冗長)、chip の aria-label / title を `dodCoverage.summary` から `dodCoverage.detail` に差し替え。visible chip text は変わらず、hover tooltip / SR 読上げ時のみ「DoD カバレッジ: 60% (3 / 5 件 — 未設定 2 件) — DoD カバレッジ: P1 100% (2/2) / P3 50% (1/2)」のような richer 文言。UX 卓越: 発見可能性 (priority bias hover で見える) / a11y (SR で全 P 読上げ) / 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、dod-coverage 26/26 PASS。次 iter364 = 114%5=4 → ai-automation。
- ✅ [iter362 (dod-coverage-by-priority)] **DoD カバレッジを priority 別に集計する pure helper (ai-automation, 1 commit, eabd343)**: iter344 (`computeDueHitRateByPriority`) と対称な「DoD × priority」軸 substrate。iter352 dod-coverage (= 全体) を priority 別 bucket で取り出し、AI brief / pm-agent / dashboard widget が「P1 DoD 100% / P3 DoD 50%」のような bias を 1 関数で出せる。低優先 ほど DoD 未設定 = 「軽い気持ちで作って scope 曖昧」 pattern を検出。新 export (`dod-coverage.ts`): `DodCoverageByPriorityFields = DodCoverageFields & { priority? }`、`computeDodCoverageByPriority(items)` で `Record<PriorityKey, DodCoverageStats>` (normalizePriority で null/範囲外 を p4、各 bucket 必ず初期化、内部で `computeDodCoverage` を再利用)、`formatDodCoverageByPriorityJa(byPriority)` で 1 行 summary (`'DoD カバレッジ: P1 100% (2/2) / P3 50% (1/2)'` / `'未完了 0 件 (該当なし)'`、total=0 の P 省略)。caller benefits: AI 朝 brief「P1 100% / P3 50% / P4 33% — 低優先 ほど scope 曖昧」/ pm-agent「P3 DoD 不足 5 件 — 受入条件レビュー推奨」/ dashboard widget priority 別 DoD 内訳。test +5 件 (computeByPriority 3 / format 2 / 既存 16 + 新 5 = 21 / 26 dod 全件)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter363 = 113%5=3 → basics (本 substrate を dod-coverage chip の tooltip に同梱、iter346 hit-rate-priority-tooltip と同手法)。
- ✅ [iter361 (dashboard-recent-done-chip)] **「今日の達成」 (recent done) chip を Dashboard に追加 (basics, 1 commit, 2df0d56)**: iter359 で整備済 recent-completed.ts substrate (selectRecentCompleted / formatRecentCompletedSummaryJa、15/15 PASS) の UI bind。chip 列の **先頭** に「今日の達成: 3 件 — タスクA / タスクB / タスクC」chip を追加、slip-days / must-hygiene の警告 chip と対比して positive UX (= 頑張ったことを褒める / 励ます視点) を Dashboard に投入。実装: `selectRecentCompleted(itemsQ.data, { windowHours: 24, limit: 3 })` を useMemo (existing useItems data 流用)、count=0 で chip 非表示で UI 静か、全件カウントは limit 無しで再計算 (= "他 N 件" 用)、chip 列の **先頭** 配置 (朝の dashboard で最初に positive な「達成 list」が目に入る順序設計、negativity bias に抗う UX)、iter355 で導入した `<DashboardChip>` を活用 (~10 行 追加コード)、glyph 🎉 (祝賀 / 達成 metaphor)、tone good (chipTone3Class('good') = emerald)、truncateText で 1 行 clamp、aria-label/title に full text、`data-testid="dashboard-recent-done-chip"` + data-recent-done-count hook。UX 卓越: 達成感 + 動機付け / a11y full text / chip 列順序設計 (警告系より達成系を先に)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、recent-completed 15/15 PASS。次 iter362 = 112%5=2 → ai-automation。
- ✅ [iter360 (iso-helpers-集約)] **isValidIsoDate / dueDateEndOfDayMs を lib/date/iso.ts に集約 (refactor, 1 commit, 528b5e8)**: iter325 (parseDateOrNull) / iter330 (shiftIsoDate) / iter340 (toLocalMidnight) / iter345 / iter350 (UTC ISO) / iter355 (DashboardChip) と同じ「同 shape の散在を 1 file に」方針の 8 弾目。本 session で追加した 4 substrate (due-hit-rate / slip-days / due-date-coverage / must-hygiene) で同じ private helper が散らばっていた: `dueDateEndOfDayMs` (ISO YYYY-MM-DD → ローカル 23:59:59.999 ms) は due-hit-rate / slip-days で 2 callsite、`isValidIsoDate` (YYYY-MM-DD shape + range 検証) は due-date-coverage / must-hygiene で 2 callsite 同一実装。新 export (`lib/date/iso.ts`): `isValidIsoDate(s)` / `dueDateEndOfDayMs(dueDate)` (内部で `isValidIsoDate` を利用)。callsite 置換 (-22 行 / 4 file)、動作変更ゼロ (既存 test 50+ 件 PASS で確認)。test +12 件 (isValidIsoDate 4 / dueDateEndOfDayMs 3 + 既存 36 = 計 51 件 / iso.test.ts)、affected 6 file 120/120 PASS、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。`lib/date/iso.ts` 関数数 8 → 10 (todayISO / isoDaysFromNow / parseDateOrNull / shiftIsoDate / toLocalMidnight / formatLocalISO / formatUtcISO / todayUtcISO / isValidIsoDate / dueDateEndOfDayMs)。次 iter361 = 111%5=1 → basics。
- ✅ [iter359 (recent-completed)] **直近 N 時間に完了した item の title リスト substrate (ai-automation, 1 commit, 8cb49f8)**: iter302 velocity (件数のみ) と相補で「具体的に何が完了したか」を AI 朝 brief / pm-agent / dashboard widget が 1 関数で取り出せる substrate。「今日の達成: タスク A / タスク B / タスク C」のような褒める文言や retroactive な"今日の wins" 表示に使える。新 module `src/features/item/recent-completed.ts`: `selectRecentCompleted(items, options?, now?)` で `RecentCompletedEntry<T>[]` (doneAt が `now - windowHours` 以降、default 24h、doneAt 降順 + tie で priority 昇順、normalizePriority で null/範囲外 を p4、未来 doneAt 除外 = clock skew safety、windowHours <= 0 で空、不正 now で空、limit option で上位 N 件)、`formatRecentCompletedSummaryJa(entries, totalCount?, limit?)` で 1 行 summary (`'今日の達成: 3 件 — タスクA / タスクB / タスクC'` / `'今日の達成: 5 件 — A / B / C / 他 2 件'` / `'今日の達成 0 件'`、無題 title は `'(無題)'` fallback)。caller benefits: AI 朝 brief「今日の達成: 3 件 — A / B / C (お疲れ様!)」/ pm-agent retro template / dashboard widget recent-wins ticker (slip-days warn と対比して positive UX) / slack/email digest「昨日の wins」。test +15 件 (select 9 / format 5 / 統合 1 = 15/15 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter360 = 110%5=0 → refactor。
- ✅ [iter358 (dashboard-must-hygiene-chip)] **MUST hygiene chip を Dashboard に追加 (basics, 1 commit, e0f2015)**: iter357 で整備済 must-hygiene.ts substrate (computeMustHygiene / formatMustHygieneJa / mustHygieneSeverity、15/15 PASS) の UI bind。WIP-bias chip の前に「MUST: 5 件 (期限付 3 / 期限なし 2 — 計画化推奨)」chip を追加、MUST 計画 hygiene を Dashboard で 1 目視可能に。MVP の核「MUST 絶対落とさない」(CLAUDE.md) を「期限が決まっていない MUST」を見逃さないために前段で警告。実装: `computeMustHygiene(itemsQ.data)` を useMemo (existing useItems data 流用)、severity='idle' で chip 非表示で UI 静か、severity 4 値分岐 ('severe' = red 重大警告 / 'mild' = amber 警戒 / 'clean' = emerald 健全 / 'idle' = chip 非表示)、iter355 で導入した `<DashboardChip>` を活用 (~20 行 追加コード)、glyph 🔥 (緊急 / MUST metaphor)、`data-testid="dashboard-must-hygiene-chip"` + data-severity + data-without-due hook、role="status"、aria-label/title。UX 卓越: 発見可能性 / a11y / 4 状態網羅 / 一貫性 (DashboardChip 経由で contract 整合)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、must-hygiene 15/15 PASS。次 iter359 = 109%5=4 → ai-automation。
- ✅ [iter357 (must-hygiene)] **MUST item の dueDate hygiene を計算する pure helper (ai-automation, 1 commit, 2eb0892)**: iter294 (`must-risk.ts`) は MUST + dueDate 近接で「落ちそう」を抽出するが、dueDate 未設定 の MUST は対象外 (= silent gap)。本 helper はその blind spot を埋める。MUST かつ dueDate 未設定は「絶対落とせない」のに「いつ落としてはいけないか」が決まっていない最悪パターン。新 module `src/features/item/must-hygiene.ts`: `computeMustHygiene(items)` で `{total, withDueDate, withoutDueDate, coverageRate}` (集計対象 = `isMust && doneAt == null && archivedAt == null`、withDueDate = dueDate が YYYY-MM-DD valid、total=0 → coverageRate=null)、`formatMustHygieneJa(stats)` で 1 行 summary (`'MUST: 5 件 (期限付 3 / 期限なし 2 — 計画化推奨)'` 混在 / `'MUST: 5 件 (全て期限付き)'` clean / `'MUST: 3 件 (全て期限なし — 重大な計画漏れ)'` severe / `'MUST 0 件'` idle)、`mustHygieneSeverity(stats)` で 4 値 ('clean' withoutDueDate=0 / 'severe' coverage<0.5 / 'mild' 残り / 'idle' total=0)。caller benefits: AI 朝 brief「MUST 期限付 3 / 期限なし 2 — 計画化推奨」(must-risk と組合せ「期限近接 + 期限なし」二軸)/ pm-agent「MUST 期限なし 3 件 — kickoff で deadline 確定」/ dashboard widget MUST hygiene chip (severe=red 重大 / mild=amber 警戒 / clean=emerald 健全 / idle=hidden)。test +15 件 (compute 6 / format 4 / severity 4 / 統合 1 = 15/15 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter358 = 108%5=3 → basics (本 substrate の dashboard chip bind が自然な次手)。
- ✅ [iter356 (dashboard-wip-bias-chip)] **WIP-bias chip を Dashboard に追加 (basics, 1 commit, 51c3c64)**: iter354 で整備済 wip-by-priority.ts substrate (computeWipByPriority / formatWipByPriorityJa / wipBiasKind、13/13 PASS) の UI bind。dod-coverage chip の隣に「WIP: 5 件 (P1: 0 / P3: 4)」chip を追加、進行中タスクの priority 偏在 (高優先が止まっているか) を Dashboard で 1 目視可能に。実装: `computeWipByPriority(itemsQ.data)` を useMemo (existing useItems data 流用)、total=0 / bias='idle' で chip 非表示で UI 静か、`wipBiasKind(stats)` で bias 判定 ('low-priority-only' = 警戒 amber / 'has-high-priority' = 健全 emerald / 'idle' = chip 非表示)、iter355 で導入した `<DashboardChip>` を活用 (~20 行 追加コード)、glyph ▶ (再生 / 進行中 metaphor)、aria-label は 'low-priority-only' 時に「高優先 0、低優先タスクのみ進行中 (bias 検出)」を補足、`data-testid="dashboard-wip-bias-chip"` + data-bias + data-wip-total hook、role="status"、title。UX 卓越: 発見可能性 / a11y / 3 状態網羅 / 一貫性 (DashboardChip 経由で contract 整合)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、wip-by-priority 13/13 PASS。次 iter357 = 107%5=2 → ai-automation。
- ✅ [iter355 (dashboard-chip-集約)] **dashboard chip 8 種の JSX 重複を <DashboardChip> に集約 (refactor, 1 commit, b6f5f2a)**: iter325 (parseDateOrNull) / iter330 (shiftIsoDate) / iter335 (trend-tone) / iter340 (toLocalMidnight) / iter345 (formatUtcISO) / iter350 (server 層) と同じ「同 shape の散在を 1 file に」方針の 7 弾目。dashboard-view.tsx 内 8 chip (velocity / momentum / due-coverage / dod-coverage / aging / due-hit-rate / slip-days / completion-gap) で同 JSX 構造が重複していた (`<div className="inline-flex ... rounded border px-2 py-1 text-xs ${tone}" data-testid={...} data-X={...} role="status" aria-label={...} title={...}>` + glyph span + text span)。新 module `src/components/workspace/dashboard-chip.tsx`: `<DashboardChip>` component (testId / toneClass / glyph / ariaLabel / title / text / truncateText / dataAttrs を props に取って同 JSX を生成、dataAttrs は spread で chip 固有 hook 任意指定)、`chipTone3Class(tone: 'good' | 'neutral' | 'warn'): string` (3 chip で 3 重複していた tone → Tailwind class 三項演算子を 1 関数に集約: emerald / muted / amber)、ChipTone3 型 export。callsite 置換: dashboard-view.tsx 各 chip JSX (12-20 行) を `<DashboardChip ...>` の 8-12 行に圧縮、3 値 tone は `chipTone3Class(tone)` で 1 行、合計 -38 行 (-123 / +85)、動作変更ゼロ (className / data-_ / aria-label / title / role / span 構造が 1:1 互換)。test +4 件 (chipTone3Class good / neutral / warn / referential = 4/4 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。chip の visible UI / a11y / data-_ が contract として 1 file に固定、追加 chip も同 component で自動整合。次 iter356 = 106%5=1 → basics。
- ✅ [iter354 (wip-by-priority)] **WIP item の priority 分布を計算する pure helper (ai-automation, 1 commit, 0e62bd8)**: 進行中 (WIP = work in progress) item を priority 別に件数集計する substrate。「高優先が止まっている / 低優先ばかり進めている」を 1 関数で見抜き、AI 朝 brief / pm-agent / dashboard widget で bias 検出に使う。新 module `src/features/item/wip-by-priority.ts`: `computeWipByPriority(items)` で `{total, byPriority, topPriority}` (status === 'in_progress' のみ集計、normalizePriority で null/範囲外を p4、topPriority は件数最大、tie で番号小 = 高優先優先、total=0 → topPriority=null)、`formatWipByPriorityJa(stats)` で 1 行 summary (`'WIP: 5 件 (P1: 0 / P2: 1 / P3: 4 / P4: 0)'` 全 bucket / `'WIP: 3 件 (P3: 3)'` 単一集中の短縮形 / `'WIP 0 件'`)、`wipBiasKind(stats)` で 3 値 ('has-high-priority' = P1+P2 > 0 / 'low-priority-only' = P3+P4 > 0 だが高優先 0 / 'idle' = total=0)。caller benefits: AI 朝 brief「WIP: 5 件 (P3:4) — 高優先 0、低優先ばかり進めている?」/ pm-agent「P3 ばかり 5 件着手中、P1 が 3 件 todo」/ dashboard widget WIP-bias chip (low-priority-only=amber / has-high-priority=emerald / idle=muted)。test +13 件 (compute 6 / format 3 / bias 3 / 統合 1 = 13/13 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter355 = 105%5=0 → refactor。
- ✅ [iter353 (dashboard-dod-coverage-chip)] **DoD カバレッジ chip を Dashboard に追加 (basics, 1 commit, 3d08034)**: iter352 で整備済 dod-coverage.ts substrate (computeDodCoverage / formatDodCoverageJa / dodCoverageTone、16/16 PASS) の UI bind。期限カバレッジ chip (iter351) の隣に「DoD カバレッジ: 60% (3 / 5 件 — 未設定 2 件)」chip を追加、planning hygiene を「期限 + DoD」二軸で Dashboard に可視化。実装: `computeDodCoverage(itemsQ.data)` を useMemo (existing useItems data 流用、追加 fetch 無し)、total=0 / coverageRate=null で chip 非表示で UI 静か、due-coverage chip と aging chip の間に inline-flex で配置、tone 別配色 (good=emerald 受入条件揃い / neutral=muted / warn=amber scope 曖昧)、glyph ✓ (チェック / 受入 metaphor、期限 chip の 📅 と区別)、`data-testid="dashboard-dod-coverage-chip"` + data-coverage-pct hook、role="status"、aria-label/title。UX 卓越: 発見可能性 / a11y / 4 状態網羅 / 一貫性 (3 段階 tone は due-coverage chip iter351 と同 vocabulary、layout 並列で計画 + 受入の二軸対称配置)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、dod-coverage 16/16 PASS。次 iter354 = 104%5=4 → ai-automation。
- ✅ [iter352 (dod-coverage)] **未完了 item の DoD カバレッジを計算する pure helper (ai-automation, 1 commit, 0a6a076)**: iter349 due-date-coverage (= 期限が決まっているか) と対称な「受入条件 (DoD = Definition of Done) が決まっているか」 substrate。DoD 未設定は「完了基準が曖昧 = 実装範囲が膨らみやすい」とみなして planning hygiene metric として扱う。新 module `src/features/item/dod-coverage.ts`: `computeDodCoverage(items)` で `{total, withDod, withoutDod, coverageRate}` (集計対象 = `doneAt == null && archivedAt == null`、withDod = dod が non-empty 文字列 (空文字 / whitespace-only は無効、`dod.trim().length > 0`)、withoutDod = null/undefined/空/whitespace-only、total=0 → coverageRate=null)、`formatDodCoverageJa(stats)` で 1 行 summary (`'DoD カバレッジ: 60% (3 / 5 件 — 未設定 2 件)'` / 100% / 0% / `'未完了 0 件 (該当なし)'`)、`dodCoverageTone(stats)` で 3 値 tone (due-date-coverage と同閾値)。caller benefits: AI 朝 brief「DoD カバレッジ 60% — 未設定 12 件 (受入条件未定義は scope 曖昧)」/ pm-agent「kickoff で受入条件を明文化」/ dashboard widget hygiene chip (due-date-coverage chip iter351 と並列で「期限 + DoD 二軸 hygiene」)。test +16 件 (compute 5 / format 5 / tone 4 / 統合 1 + empty 1 = 16/16 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter353 = 103%5=3 → basics (本 substrate の dashboard chip bind が自然な次手)。
- ✅ [iter351 (dashboard-due-coverage-chip)] **期限カバレッジ chip を Dashboard に追加 (basics, 1 commit, f405380)**: iter349 で整備済 due-date-coverage.ts substrate (computeDueDateCoverage / formatDueDateCoverageJa / dueDateCoverageTone、16/16 PASS) の UI bind。backlog-aging chip の前に「期限カバレッジ: 60% (3 / 5 件 — 未設定 2 件)」chip を追加、planning hygiene を Dashboard で 1 目視。実装: `computeDueDateCoverage(itemsQ.data)` を useMemo (existing useItems data 流用、追加 fetch 無し)、total=0 / coverageRate=null で chip 非表示で UI 静か、aging chip 前に inline-flex で配置、tone 別配色 (good=emerald 健全 / neutral=muted / warn=amber planning 漏れ)、glyph 📅 (calendar metaphor、既存 chip ⌛/⏰/◎ と区別)、`data-testid="dashboard-due-coverage-chip"` + data-coverage-pct hook、role="status"、aria-label/title。UX 卓越: 発見可能性 / a11y / 4 状態網羅 / 一貫性 (3 段階 tone は hit-rate chip iter343 と同 vocabulary)。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、due-date-coverage 16/16 PASS。次 iter352 = 102%5=2 → ai-automation。
- ✅ [iter350 (utc-iso-server-集約)] **server 層の UTC ISO 日付 helper を lib/date/iso.ts に集約 (refactor, 1 commit, cecddd4)**: iter345 で client UI 層 (dashboard-view / pdca-panel / budget-panel) の UTC ISO idiom を `formatUtcISO` / `todayUtcISO` / `shiftIsoDate` 経由に置換した続編。本 iter は server / agent / template 層に同じ集約を適用。callsite 置換 (動作変更ゼロ、6 file): `dashboard/service.ts` (-5 行 / 1 helper 削除、`addDaysISO(base: Date, days)` を `shiftIsoDate(formatUtcISO(today), n)` に分解)、`template/instantiate-plan.ts` (-3 行 / 1-line impl、private `addDaysISO` 中身を `shiftIsoDate(formatUtcISO(base), days)`)、`pdca/service.ts` (-3 行、`function toISO(d) { ... }` を `const toISO = formatUtcISO` alias 化)、`agent/cron-workers.ts` (-3 行、同様 `dateKeyUTC = formatUtcISO` alias)、`agent/pm-service.ts` (+1 / -1、`params.today.toISOString().slice(0, 10)` → `formatUtcISO(params.today)`)、`today/build-groups.ts` (-5 行、export `shiftISO` を `shiftIsoDate` alias 化、後方互換維持)。caller は import 名で意図 (UTC vs ローカル) を判別可能、動作変更ゼロ。これで `lib/date/iso.ts` の 8 関数が server / client / agent / template の全層で唯一の date 規約 source of truth に。architecture 5/5 PASS、lib/date/iso 19/19 + build-groups 17/17 = 36/36 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter351 = 101%5=1 → basics。
- ✅ [iter349 (due-date-coverage)] **未完了 item の期限カバレッジを計算する pure helper (ai-automation, 1 commit, a735af7)**: 「計画化されている / 着手日が決まっていない」を 1 関数で出す substrate。iter287 due-proximity (期限近接バケット) は dueDate 設定済 item のみが対象、本 helper はその「分母」(期限を持つかどうか) を集計する対称軸。新 module `src/features/item/due-date-coverage.ts`: `computeDueDateCoverage(items)` で `{total, withDueDate, withoutDueDate, coverageRate}` (集計対象 = `doneAt == null && archivedAt == null`、withDueDate = dueDate が YYYY-MM-DD valid、withoutDueDate = null/undefined/不正 ISO、total=0 → coverageRate=null)、`formatDueDateCoverageJa(stats)` で 1 行 summary (`'期限カバレッジ: 60% (3 / 5 件 — 未設定 2 件)'` / 100% / 0% / `'未完了 0 件 (該当なし)'`)、`dueDateCoverageTone(stats)` で 3 値 tone (>= 0.7 good 健全 / 0.3..0.7 neutral 中立 / < 0.3 warn planning 漏れ多、total=0 → neutral)。caller benefits: AI 朝 brief「期限カバレッジ 60% — 未設定 12 件 (計画化推奨)」/ pm-agent「未完了 Item の 70% が期限未設定」/ dashboard widget hygiene chip。test +16 件 (compute 5 / format 5 / tone 4 / 統合 1 + empty 1 = 16/16 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter350 = 100%5=0 → refactor。
- ✅ [iter348 (dashboard-slip-days-chip)] **slip-days chip を Dashboard に追加 (basics, 1 commit, cf1bf78)**: iter347 で整備済 `slip-days.ts` substrate (computeSlipDays / formatSlipDaysJa、13/13 PASS) の UI bind。due-hit-rate chip (iter343) の隣に「遅延 N 件 (平均 / 中央値 / 最大 日数)」chip を追加、SLA miss の **深刻度** (= "miss が 1 日 vs 14 日") を Dashboard で 1 目視可能に。新 export `slipSeverity(stats)` を slip-days.ts に追加 (maxDays >= 7 → 'severe' / それ未満 → 'mild')。実装: `computeSlipDays(itemsQ.data)` を useMemo (existing useItems data 流用、追加 fetch 無し)、count=0 で chip 非表示で UI 静か (= 全完了 hit の workspace は dashboard 静謐)、due-hit-rate chip と completion-gap chip の間に inline-flex で配置、severity 別配色 (severe=red 重度遅延 / mild=amber 中程度遅延)、glyph ⏰ (時計 / 遅延 metaphor、既存 chip ⌛/⏱/◎ と区別)、`data-testid="dashboard-slip-days-chip"` + data-severe + data-max-days hook、role="status"、aria-label/title。test +3 件 (slipSeverity severe / mild / empty 3 case = 16/16 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)、機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。次 iter349 = 99%5=4 → ai-automation。
- ✅ [iter347 (slip-days)] **期限超過 item の slip 日数を集計する pure helper (ai-automation, 1 commit, 0cb703c)**: iter342 (`due-hit-rate.ts`、hit/miss の binary) の対称軸。「miss は何件あるか」だけでなく「平均 / 中央値 / 最大で何日 遅延しているか」を 1 関数で取り出す substrate。50% hit rate でも「平均 1 日 遅れ」と「平均 14 日 遅れ」では緊急度が桁違い。新 module `src/features/item/slip-days.ts`: `computeSlipDays(items, options?)` で `{count, avgDays, medianDays, maxDays}` (slipDays = ceil((doneAt - dueDate-end-of-day) / 86400000)、最低 1 日 = 翌日完了、hit は除外、count は miss のみ、不正値 fail-soft、options.since で window 限定、count=0 → 全 stat null、avgDays は小数 1 桁丸め、medianDays 偶数件は中央 2 値の avg)、`formatSlipDaysJa(stats)` で 1 行 summary (`'遅延 0 件'` / `'遅延: 1 件 (3日)'` (短縮形) / `'遅延: 5 件 (平均 3.4日 / 中央値 2日 / 最大 12日)'`)。caller benefits: AI 朝 brief「期限達成率 50% — 遅延: 5 件 (平均 3.4日 / 最大 12日)」(due-hit-rate と組合せで「miss が深刻 / 軽微」が見える) / pm-agent「P3 タスクが平均 7 日遅れ」(将来 by-priority 拡張) / dashboard widget の slip distribution mini-bar / 遅延 worst N item ハイライト。test +13 件 (compute 8 / format 4 / 統合 1 = 13/13 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter348 = 98%5=3 → basics (本 substrate の dashboard chip bind が自然な次手 — due-hit-rate chip と並列の「遅延 N 件 (平均 M 日)」chip)。
- ✅ [iter346 (hit-rate-chip-priority-tooltip)] **hit-rate chip の tooltip / aria-label に priority breakdown を同梱 (basics, 1 commit, 1decf44)**: iter343 で配置済の dueHitRate chip の visible 表記は全体集計のままで、SR / hover 経路でのみ priority 別 breakdown (iter344) を読めるように UI を richer に。dashboard chip 列を増やさず UI 静寂を保ちつつ、a11y / power-user 向けの 2 層情報設計を実現。実装: `countNonEmptyPriorityBuckets(byPriority)` を due-hit-rate.ts に追加 (priority bucket のうち実データ total > 0 を持つ件数: 0 / 1 / 2..4)、dashboard-view.tsx の dueHitRate useMemo を拡張 (`computeDueHitRateByPriority` → countNonEmptyPriorityBuckets > 1 時のみ `${summary} — ${formatDueHitRateByPriorityJa(byPriority)}` を `detail` として保持、単一 priority 偏在は冗長なので breakdown 出さない)、chip の aria-label / title を `dueHitRate.summary` から `dueHitRate.detail` に差し替え。visible chip text は変わらず、hover tooltip / SR 読上げ時のみ「期限達成率: 67% (2 / 3 件) — 期限達成率: P1 100% (1/1) / P3 50% (1/2)」のような richer 文言。UX 卓越: 発見可能性 (priority bias hover で見える) / a11y (SR で全 P 読上げ) / 一貫性 (chip visible UI 不変、richer info は a11y/hover 経路のみ)。test +3 件 (countNonEmptyPriorityBuckets 0 / 1 / N = 35/35 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)、機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。次 iter347 = 97%5=2 → ai-automation。
- ✅ [iter345 (utc-iso-helpers-集約)] **formatUtcISO / todayUtcISO を lib/date/iso.ts に集約 (refactor, 1 commit, ad65d9c)**: iter325 (parseDateOrNull) / iter330 (shiftIsoDate) / iter340 (toLocalMidnight + formatLocalISO) と同じ「同 shape の散在を 1 file に」方針の 4 弾目。UTC 基準 `YYYY-MM-DD` 整形 idiom (`new Date().toISOString().slice(0, 10)` / `d.toISOString().slice(0, 10)`) が dashboard / pdca / budget / agent / heartbeat 等で 10+ callsite に散在していたので、明示的な API として固定。新 export (`lib/date/iso.ts`): `formatUtcISO(d: Date): string` (UTC 基準 YYYY-MM-DD、formatLocalISO の対称)、`todayUtcISO(now?: Date): string` (糖衣)。callsite 置換は client UI 層に scope: dashboard-view.tsx (-13 行 / 2 helper 削除 + import 1 行、private todayISO / addDaysISO を `todayUtcISO()` + `shiftIsoDate(todayStr, 7)` に置換)、pdca-panel.tsx (-3 行 / `isoDaysFromToday` を `shiftIsoDate(todayUtcISO(), days)` で短縮)、budget-panel.tsx (-1 行 / inline → `todayUtcISO()`)。caller は import 名で意図 (UTC vs ローカル) を判別可能、動作変更ゼロ (3 callsite とも UTC YYYY-MM-DD idiom を維持)、formatLocalISO と対称。test +6 件 (formatUtcISO 2 / todayUtcISO 3 / 計 36/36 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。これで `lib/date/iso.ts` に date 規約 8 関数 (todayISO / isoDaysFromNow / parseDateOrNull / shiftIsoDate / toLocalMidnight / formatLocalISO / formatUtcISO / todayUtcISO) が集約完了。server/agent layer の 7+ callsite (pdca/service / dashboard/service / instantiate-plan / cron-workers / heartbeat / today/build-groups / pm-service) も同様に置換可、別 iter で続編予定。次 iter346 = 96%5=1 → basics。
- ✅ [iter344 (due-hit-rate-by-priority)] **期限達成率を priority 別に集計する pure helper (ai-automation, 1 commit, 0f967aa)**: iter342 (`due-hit-rate.ts`、全体 hit rate) と iter334 (`completion-days-by-priority`、所要日数 avg を P 別) を組み合わせた「priority 軸での SLA 達成率」 substrate。低優先 ほど hit rate 悪 → 着手遅延 / 見積精度の bias 検出に使える。新 export (`due-hit-rate.ts`): `computeDueHitRateByPriority(items, options?)` で `Record<PriorityKey, DueHitRateStats>` (各 priority bucket 必ず初期化、since filter / 不正値除外は本体と同じ)、`formatDueHitRateByPriorityJa(byPriority)` で 1 行 summary (`'期限達成率: P1 100% (2/2) / P3 50% (1/2)'` 形式、total=0 の P 省略、Math.round で 0 桁丸め、全 P total=0 → `'完了 0 件 (該当なし)'`)。既存 `priority.ts#normalizePriority` を export 化 (1/2/3 はそのまま、null/undefined/範囲外は p4 集約、挙動変わらず) — 他 substrate から再利用可。caller benefits: AI 朝 brief「期限達成率: P1 100% (3/3) / P2 80% (4/5) / P4 67% (2/3)」/ pm-agent「P3 50% — 低優先タスクが期限超過しがち、scope 縮小提案」/ dashboard "Priority 別 SLA" widget の stacked bar / completion-days-by-priority と組合せ「P3 所要日数 long + hit rate 低」二軸 insight。test +12 件 (computeByPriority 4 / format 3 / 既存 20 + 新 12 = 32/32 PASS)、architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)。次 iter345 = 95%5=0 → refactor。
- ✅ [iter343 (dashboard-due-hit-rate-chip)] **期限達成率 chip を Dashboard に追加 (basics, 1 commit, 6afeeba)**: iter342 で整備済 `due-hit-rate.ts` substrate (computeDueHitRate / formatDueHitRateJa、16/16 PASS) の UI bind。Dashboard 上部 trend chip 列 (iter331 velocity / iter341 momentum / iter338 aging / iter336 completion-gap) に「期限達成率: 80% (4 / 5 件)」chip を追加、SLA 達成度を 1 目視可能に。新 export `dueHitRateTone(stats)` を due-hit-rate.ts に追加 (hitRate を 'good' / 'neutral' / 'warn' の 3 値 tone bucket に分類: >= 0.8 達成 / 0.5..0.8 中立 / < 0.5 警戒、total=0 は neutral)。実装: `computeDueHitRate(itemsQ.data)` を useMemo (existing useItems data 流用、追加 fetch 無し)、total=0 / hitRate=null で chip 非表示で UI 静か、aging と completion-gap の間に inline-flex で配置、tone 別配色 (good=emerald 達成 / neutral=muted 中立 / warn=amber 警戒)、glyph ◎ (達成感 metaphor、既存 chip ⌛/⏱ と区別)、`data-testid="dashboard-due-hit-rate-chip"` + data-hit-rate (0..100 整数) hook、role="status"、aria-label/title。test +4 件 (dueHitRateTone good / neutral / warn / empty 4 case = 16+4 = 20/20 PASS)、architecture 5/5 PASS、typecheck 緑、lint 緑 (warning baseline 1)、機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。次 iter344 = 94%5=4 → ai-automation。
- ✅ [iter342 (due-hit-rate)] **完了 item の期限達成率 (hit rate) を計算する pure helper (ai-automation, 1 commit, 7865e49)**: iter334 (`completion-days-by-priority`、所要日数 avg) は完了所要を priority 別に出すが、「期限を守ったか」の binary は別軸。本 helper は dueDate を持つ完了 item を `hit` (doneAt ≤ dueDate 当日終端 ローカル 23:59:59.999) / `miss` (doneAt > dueDate) に分類し、SLA 達成率を 1 関数で取り出す substrate。新 module `src/features/item/due-hit-rate.ts`: `computeDueHitRate(items, options?)` で `{total, hit, miss, hitRate}` (除外: doneAt 未設定 / 不正 / dueDate 未設定 / 不正 ISO fail-soft、options.since で doneAt >= since の item のみ集計、不正 since で全件、total=0 → hitRate=null)、`formatDueHitRateJa(stats)` で 1 行 summary (`'期限達成率: 80% (4 / 5 件)'` / `'期限達成率: 100% (5 / 5 件)'` / `'期限達成率: 0% (0 / 3 件 — 全て遅延)'` / `'完了 0 件 (該当なし)'`、Math.round で 0 桁丸め)。caller benefits: AI 朝 brief「期限達成率: 80%」/ pm-agent「直近 30 日 hit rate 50% — 見積精度 / 着手遅延を疑う」/ dashboard widget hit rate chip + tone (高 = emerald 安心 / 低 = amber 警戒、trend-tone iter335 の polarity='positive' で hit=up=blue / miss=down=red も可)。test +16 件 (compute 9 / format 5 / 統合 1 / empty 1 = 16/16 PASS)、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter343 = 93%5=3 → basics (本 substrate の dashboard chip bind が自然な次手)。
- ✅ [iter341 (dashboard-momentum-chip)] **workspace momentum chip を Dashboard に追加 (basics, 1 commit, d207177)**: iter339 で整備済 `momentum.ts` substrate (computeWorkspaceMomentum / formatWorkspaceMomentumJa、17/17 PASS) の UI bind。Dashboard 上部 trend chip 列 (iter331 velocity / iter338 aging / iter336 completion-gap) に「直近 7 日 backlog 成長中 / 縮小中 / 安定 / 活動なし」を 4 値分類で chip 表示、intake vs done のトレンドを 1 目視可能に。新 export `momentumDirectionToTrend(direction)` を momentum.ts に追加 (domain 4 値 → trend-tone 軸 up/down/flat/idle マップ、caller が `@/lib/ui/trend-tone` に直接 bind 可)。実装: `computeWorkspaceMomentum(items, {windowDays:7})` を useMemo (existing useItems data 流用、追加 fetch 無し)、direction='idle' で chip 非表示で UI 静か、velocity と aging の間に inline-flex で配置、polarity='negative' で direction 別 (growing=amber↑ 警戒 / shrinking=emerald↓ 改善 / balanced=muted→ / idle=muted·)、cost-trend chip iter333 と同じ意味付け (「backlog 増 = 警戒」)、velocity (↑=活動増 + blue) とは対照、`data-testid="dashboard-momentum-chip"` + data-direction hook、role="status"、aria-label/title。test +1 件 (momentumDirectionToTrend 4 mapping)、momentum 18/18 + architecture 5/5 PASS、typecheck/lint 緑 (warning baseline 1)、機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。次 iter342 = 92%5=2 → ai-automation。
- ✅ [iter340 (date-helpers-集約)] **toLocalMidnight / formatLocalISO を lib/date/iso.ts に集約 (refactor, 1 commit, 75247a3)**: iter325 (parseDateOrNull) / iter330 (shiftIsoDate) と同じ「同 shape の散在を 1 file に」方針の 3 弾目。`toLocalMidnight` (Date を 0:00:00 揃え) は velocity.ts / due-proximity.ts / daily-summary.ts で 3 callsite 重複、`formatLocalISO` (Date → YYYY-MM-DD ローカル) は velocity.ts (formatLocalISO) / daily-summary.ts (formatISODate) / iso.ts (private formatLocal) で 3 callsite 同一実装が散らばっていた。`toLocalMidnight` は最も defensive な daily-summary 版に揃え (NaN/null/undefined → null fail-soft、due-proximity も velocity も caller 側 null check 済で互換)、`formatLocalISO` は iso.ts 内 private formatLocal を rename + export し todayISO/isoDaysFromNow も本関数を内部で呼ぶ形に統一。callsite 置換: velocity.ts (-11 行 / 2 helper 削除 + import 1 行)、due-proximity.ts (-3 行 / 1 helper 削除 + import 1 行)、daily-summary.ts (-12 行 / 2 helper 削除 + import 1 行)。test +8 件 (formatLocalISO 3 / toLocalMidnight 4 + 既存 7 → 19/19 PASS)、affected 4 file 90/90 test PASS、動作変更ゼロ (caller 側 null check は元々あった、behavior 互換)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。これで `lib/date/iso.ts` に date 規約 6 関数 (todayISO / isoDaysFromNow / parseDateOrNull / shiftIsoDate / toLocalMidnight / formatLocalISO) が集約完了。次 iter341 = 91%5=1 → basics。
- ✅ [iter339 (workspace-momentum)] **workspace モメンタム (intake vs done) を判定する pure helper (ai-automation, 1 commit, 4bc7581)**: iter302 velocity (done 件数のペース、片側) と iter312 period-completion (単一期間 itemsCompleted/itemsAdded 集計) を「window N 日固定 + direction 判定 (4 値分類)」に特化させた substrate。AI brief / pm-agent / dashboard widget が「backlog 成長中 (週 +3 件)」のようなトレンドを 1 関数で出せる。新 module `src/features/item/momentum.ts`: `computeWorkspaceMomentum(items, options?, today?)` で `{intake, done, net, direction, windowDays}` (windowDays default 7、(today - windowDays + 1) ≤ X ≤ today を window、不正値 fail-soft、windowDays<=0 / today 不正で idle)、direction 判定 (intake=done=0 → 'idle' / |net|/max(1,total) < 20% → 'balanced' / net>0 → 'growing' / net<0 → 'shrinking')、`formatWorkspaceMomentumJa(m)` で 4 文言 (`'モメンタム: 直近 7 日 +5 件 成長中 (新規 10 / 完了 5)'` 形式 / 安定 / 縮小中 / 活動なし)。caller benefits: AI 朝 brief「モメンタム: +3 件 成長中 — backlog 増加傾向」/ pm-agent「intake が done を上回る → 着手枠確保 or scope 縮小提案」/ dashboard widget で trend chip + tone bind (trend-tone iter335 と組合せ可、growing=blue / shrinking=emerald / balanced=muted)。test +17 件 (compute 11 / format 5 / 統合 1 = 17/17 PASS)、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter340 = 90%5=0 → refactor。
- ✅ [iter338 (dashboard-backlog-aging-chip)] **backlog 年齢分布 chip を Dashboard に追加 (basics, 1 commit, 8866456)**: iter337 で整備済 `backlog-aging.ts` substrate (countItemsByAge / formatAgingCounts / countAgingItemsOlderThanWeek、23/23 PASS) の UI bind。iter331 velocity / iter336 completion-gap chip と同列に「backlog 年齢構成 + 7 日以上の停滞件数」chip を追加、"作って放置" している item を Dashboard で目視可能に。実装: `countItemsByAge(active)` で active item (= !doneAt && !archivedAt) の年齢別件数集計、`formatAgingCounts` で 1 行 summary、`countAgingItemsOlderThanWeek` で stale + ancient 合算、active 0 件 / summary '0 件' で chip 非表示で UI 静か、`olderThanWeek > 0` → `trendToneClass('up', 'negative')` で amber tone (警戒)、=0 → muted (静か) 切替、trend-tone (iter335) helper の negative polarity 再利用 (「停滞 多い = 危険信号」)、glyph ⌛ (砂時計 metaphor)、文言 `'Backlog: 新規 3 / 直近 5 / 停滞 8 / 古参 12 — 7日+ 20件'`、aria-label に「棚卸し対象」補足、`data-testid="dashboard-backlog-aging-chip"` + data-older-than-week hook。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、backlog-aging test 23/23 PASS。次 iter339 = 89%5=4 → ai-automation。
- ✅ [iter337 (backlog-aging)] **item を createdAt 年齢バケット (新規/直近/停滞/古参) に分類する pure helper (ai-automation, 1 commit, 61c3221)**: iter287 due-proximity (dueDate ベース「期限近接バケット」) と対称な「intake 年齢」軸 substrate。AI 朝 brief / pm-agent / dashboard widget が「backlog の構成: 新規 3 / 直近 5 / 停滞 8 / 古参 12」を 1 関数で取り出せる。stale-items (iter299、updatedAt ベース「放置」) と相補で、こちらは createdAt ベース「いつ作られたか」。新 module `src/features/item/backlog-aging.ts`: `AgingKind` 5 段階 ('new' 今日 / 'recent' 1-6日 / 'stale' 7-29日 / 'ancient' 30日+ / 'unknown' createdAt 不正)、`getItemAge(createdAt, today?)` で `{kind, ageDays, label}` (Date | ISO 両対応、未来 createdAt は ageDays=0 / new clamp、不正 → unknown sentinel)、`agingLabel(kind)` 日本語短ラベル、`groupItemsByAge(items, today?)` で `Record<AgingKind, T[]>` (元順 stable + 必ず空配列で初期化)、`countItemsByAge(items, today?)` で件数、`formatAgingCounts(counts)` で `'新規 3 / 停滞 5 / 古参 12'` 形式 (formatNonZeroCounts 流用、0 bucket 省略、全 0 → '0 件')、`countAgingItemsOlderThanWeek(counts)` で stale + ancient 合算 (= "停滞気味 N 件" を 1 関数で、unknown 除外)。caller benefits: AI 朝 brief「backlog: 新規 3 / 直近 5 / 停滞 8 / 古参 12 (= 7 日以上 20 件)」/ pm-agent「古参 12 件 — 棚卸し対象」/ dashboard "Backlog 健康度" widget の 5 段階 stacked bar / staleness-items (updatedAt 軸) と組合せ「作って放置」二軸 insight。test +23 件 (getItemAge 11 / label 1 / group 3 / count 2 / format 3 / older-than-week 2 / 統合 1 = 23/23 PASS)、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter338 = 88%5=3 → basics。
- ✅ [iter336 (dashboard-completion-gap-chip)] **completion latency gap chip を Dashboard に追加 (basics, 1 commit, 8e8fc88)**: iter334 で整備済 `completion-days-by-priority.ts` substrate (computeCompletionDaysByPriority / formatCompletionDaysByPriorityJa / computePriorityLatencyGap、19/19 PASS) の UI bind。iter331 で追加した velocity chip の隣に「priority 別 平均所要日数」を見える化し、bias 検出 (低優先 ほど着手遅い) を Dashboard で 1 目視に。実装: `computeCompletionDaysByPriority(items)` を useMemo (since filter なし、全 history、Date.now() 内呼出回避 = react-hooks/purity 準拠、Item 完了履歴は archive されないので過去全件で平均が取れて統計安定)、`formatCompletionDaysByPriorityJa` で 1 行 summary + '完了 0 件 (該当なし)' で chip 非表示、`computePriorityLatencyGap` で `{highKey, lowKey, highDays, lowDays, gapDays}` を抽出 (「P3 が P1 より N 日遅い」を組み立て)、velocity chip と同列 (`<div className="flex flex-wrap items-center gap-2">` で囲む)、glyph ⏱ + tone muted (中立 = "観察 insight"、velocity の up/down と差別化)、文言 `'完了所要 P1 2.5日 / P4 14日 (gap 11.5日)'`、aria-label に「P4 が P1 より遅い」補足、`data-testid="dashboard-completion-gap-chip"` + data-gap-days hook。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、completion-days test 19/19 PASS。次 iter337 = 87%5=2 → ai-automation。
- ✅ [iter335 (trend-tone-集約)] **trend chip の TONE / GLYPH を lib/ui/trend-tone.ts に集約 (refactor, 1 commit, 5af135d)**: 3 callsite で同 shape の TREND_TONE map が散らばっていた (iter321 top-items-by-time-chip / iter331 dashboard-velocity / iter333 budget-cost、構造同一だが「up が good か warning か」のドメイン意味で配色が逆転)。新 module `src/lib/ui/trend-tone.ts`: `TrendDirection` 型、`TREND_GLYPH` map (4 directions、polarity 不問: ↑ ↓ → ·)、`TrendPolarity = 'positive' | 'negative'`、`trendToneClass(direction, polarity?)` で chip Tailwind class、`trendGlyph(direction)` で glyph (糖衣)、flat / idle は polarity 不問で muted (中立)。`positive` = up=blue 増加 / down=red 失速 (時間 / velocity 軸)、`negative` = up=amber 警戒 / down=emerald 改善 (cost / error 軸)。callsite 置換: top-items-by-time-chip (-7 行 + tone destructure → 関数呼出 2 つ)、dashboard-view (-11 行 + 直接呼出)、budget-panel (-9 行 + CostMonthDirection import 削除)、合計 -38 行 / +18 行 (重複削除)。test +14 件 (TREND_GLYPH 2 / positive 4 / negative 4 / cross-polarity 4 = 18/18 PASS)。動作変更ゼロ (各 callsite の chip Tailwind class 完全互換)、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。iter325 (parseDateOrNull) / iter330 (shiftIsoDate) と同じ「同 shape の散在を 1 file に」方針。今後の trend chip も polarity 引数で配色一致。次 iter336 = 86%5=1 → basics。
- ✅ [iter334 (completion-days-by-priority)] **完了 item の所要日数を priority 別に集計する pure helper (ai-automation, 1 commit, 57ee0ce)**: iter312 `period-completion.ts` (期間 × 全 priority 合算 avg) と iter292 `priority.ts` (priority 件数分布) を組み合わせた「priority 軸での所要時間学習」 substrate。AI 朝 brief / pm-agent / dashboard widget が「P1 平均 2.5日 / P2 4.0日 / P3 6.0日 / P4 14.0日」を 1 関数で取り出し「低優先 item ほど着手が後回しになる」 bias 検出に使える。iter269 bias-trend (時間 axis) と並ぶ「学習対象 dimension」増設。新 module `src/features/item/completion-days-by-priority.ts`: `computeCompletionDaysByPriority(items, options?)` で `Record<PriorityKey, {count, avgDays}>` (完了済 + createdAt 有効 + doneAt >= createdAt のみ集計、priority range 外 / null は p4 normalize、since で doneAt >= since 限定、avgDays round 1 桁、count=0 で null)、`formatCompletionDaysByPriorityJa(stats)` で `'完了所要: P1 2.5日 (n=3) / P3 6日 (n=5) / P4 14日 (n=1)'` 形式 (count=0 省略、全 0 → '完了 0 件 (該当なし)')、`computePriorityLatencyGap(stats)` で最高/最低 priority の `{highKey, lowKey, highDays, lowDays, gapDays}` (1 priority のみ → null、round 1 桁)。不正値 fail-soft (createdAt/doneAt NaN/null → 除外、doneAt < createdAt 時計ズレ → 除外、since 不正 → 全件)。caller benefits: AI 朝 brief「完了所要: P1 2.5日 / P4 14日 — gap 11.5日 (P4 が大幅遅い)」/ pm-agent「P3 14日かけてる、優先度 review」/ dashboard priority 別 latency widget の stacked bar / bias-trend と組合せ「P3 見積精度悪 + 着手も遅い」二軸 insight。test +19 件 (compute 10 / format 3 / gap 5 / 統合 1 = 19/19 PASS)、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter335 = 85%5=0 → refactor。
- ✅ [iter333 (budget-cost-trend-chip)] **BudgetPanel に月次コスト前月比 trend chip を追加 (basics, 1 commit, 86a4a40)**: iter332 で整備済 `cost-monthly-trend.ts` substrate (rollupCostByMonth / computeMonthlyCostTrend / formatMonthlyCostTrendJa、cost-monthly-trend.test.ts 21/21 PASS) の UI bind。今までは BudgetPanel は「当月実績 / 上限 / バー」だけで「先月より +50% / 下げ止まり」のような前月比インサイトが UI に出ていなかった。実装: `useMonthlyCost(workspaceId, 3)` を hook 追加 (既存 useBudgetStatus と並列、3 ヶ月分で十分)、`rollupCostByMonth(monthly.data)` → `computeMonthlyCostTrend(rolled, todayIso)` → `formatMonthlyCostTrendJa(trend)` を useMemo で combinator (3 関数 substrate を 1 ブロックで消化)、direction='idle' (= this/prior 両 0) で chip 非表示で UI 静か、chip 配置は「当月実績 / 上限」行と progressbar の間、`COST_TREND_TONE` map で direction 別 (up=amber↑ 警戒 / down=emerald↓ 安心 / flat=muted→ / idle=muted·)、Dashboard velocity chip (iter331) と同 vocabulary だが「コスト up=危険」のドメイン意味に合わせ amber (時間軸 up=blue と差別化)、`data-testid="budget-cost-trend-chip"` + data-direction hook、role="status"、aria-label/title。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、cost-monthly-trend test 21/21 PASS。次 iter334 = 84%5=4 → ai-automation。
- ✅ [iter332 (cost-monthly-trend)] **AI コストの月次推移を判定する pure helper (ai-automation, 1 commit, 0c2d3bc)**: iter319 `weekly-time-trend.ts` (時間: this week vs prior week) / iter269 `bias-trend.ts` (見積精度: 前後比較) と対称な「コスト: this month vs prior month」substrate。今までは `cost-aggregate.ts#getMonthlyCost` は workspace × month × role で集計した `MonthlyCostRow[]` を返すが、UI / AI brief / pm-agent が「先月より +30%」「下げ止まり」のような前月比トレンドを欲しい時に毎回 inline 集計が必要だった。新 module `src/features/agent/cost-monthly-trend.ts`: `rollupCostByMonth(rows)` で同一 month の重複 entry (role 別) を合算 + 月降順 sort (structural subset で MonthlyCostRow[] 直接受付可)、`computeMonthlyCostTrend(entries, today)` で thisMonth/priorMonth の `{thisMonthUsd, priorMonthUsd, deltaUsd, deltaPct, direction}` 返す (direction は idle / flat / up / down、|Δ%| < 5% で flat、prior=0+this>0 で up + deltaPct=null、prior>0+this=0 で down)、`formatMonthlyCostTrendJa(trend)` で 1 行 ('AI コスト: 増加 (先月 $1.00 → 今月 $1.50、+$0.500 (+50%))' 形式、3 桁 (<$1) / 2 桁 (>=$1) decimal)、不正値 fail-soft (NaN/負/Infinity → 0 加算、month 形式違反 → 除外、today 不正 → null+idle)。caller benefits: AI 朝 brief「AI コスト: 増加 (先月 $0.80 → 今月 $1.20、+$0.400 (+50%))」/ dashboard cost widget の trend 矢印 + tone 配色 / cost-budget warn と組合せて「上限の 80% 到達 + 前月比 +50%」二重 alert。test +21 件 (rollup 4 / trend 10 / format 6 / 統合 1 = 21/21 PASS)、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter333 = 83%5=3 → basics。
- ✅ [iter331 (dashboard-velocity-chip)] **velocity chip を Dashboard 上部に追加 (basics, 1 commit, 62ff387)**: iter302 で整備済 `velocity.ts` substrate (`computeVelocity` / `formatVelocitySummary`、velocity.test.ts 17/17 PASS) の UI bind。iter302 並走で AI brief / pm-agent には velocity 行が露出していたが、Dashboard には "MUST/WIP/期限" 系の現状指標しか無く「今週の done ペース」が UI 上で見えていなかった。実装: `useItems(workspaceId)` を hook 追加 (既存 useMustSummary / useBurndown と並列)、`computeVelocity(items, {windowDays:7})` で直近 7 日 done 件数 / 平均 / 傾向算出、`formatVelocitySummary(result, 7)` で 1 行 (例: '直近 7 日 velocity: 12 件 (1.7 件/日、傾向 up)')、total=0 (= 直近 7 日 done ゼロ) は chip 非表示で UI 静か (新規 workspace で Dashboard が "WIP/期限" 系だけになる初期体験を保つ)、stat grid (4 cards) と WIP 警告の間に inline-flex で 1 行配置、`VELOCITY_TONE` / `VELOCITY_GLYPH` map で trend 別 (up=blue↑/flat=muted→/down=red↓)、TopItemsByTimeChip の TREND_TONE (iter321) と整合、`data-testid="dashboard-velocity-chip"` + data-trend hook、aria-label/title に full text。機能追加なし、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、velocity test 17/17 PASS。**race 履歴**: 本 session 当初 iter331 として streak chip を実装したが parallel session の `76fcd39` (iter330 並走 streak chip) が先取、rebase conflict で発覚 → reset で abort、別 substrate (velocity) を bind。次 iter332 = 82%5=2 → ai-automation。
- ✅ [iter330 並走 (streak-chip)] **TopItemsByTimeChip に daily-streak chip を追加 (basics, 1 commit, 76fcd39)**: iter327 で並走着地済 `daily-streak.ts` (16/16 PASS) の UI bind。iter321 (weekly-trend chip) / iter329 (weekday-peak chip) の稼働ダッシュボードに「連続稼働 N 日目」chip を追加 (Duolingo / GitHub contribution graph 風)。`computeDailyStreak(entries, today)` を useMemo (entries 全件、Set dedupe で O(N+D))、`formatStreakSummary` で 4 文言、Lucide `Flame` icon + emerald (active) / muted (inactive) tone、trend chip の隣 (flex-wrap)、aria-label/title/data-streak-active hook、sr-only に trend → streak → peak → top の連結 summary。**本 session race 履歴**: iter327 ai-automation で daily-streak.ts を実装したが parallel session の `20efba8` (内容同等 daily-streak) が先取、iter328 basics で streak chip を実装したが parallel session の `4ff6076` (iter328 weekday-peak-chip) と同 file 衝突 → reset --hard origin/main + 再適用、結果 iter330 並走として位置付け。同 iter330 に並走 refactor `5a9910e` (shiftIsoDate-集約) も着地、双方衝突せず。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter331 = 81%5=1 → basics。
- ✅ [iter330 (shiftIsoDate-集約)] **shiftIsoDate を lib/date/iso.ts に集約 (refactor, 1 commit, 5a9910e)**: iter319 (weekly-time-trend) と iter327 並走 (daily-streak) で同一実装の private `shiftDays` を 2 callsite で写経していたが、iter280/iter305/iter325 で `lib/date/iso.ts` に集約済の date helper family と同じ位置に集約。iter325 (parseDateOrNull-集約) と完全対称: 「日付 shift」の UTC 計算規約も 1 file に統合する。実装は両 callsite と完全互換 (UTC 基準で y/m/d 分解 → `Date.UTC` 再構築 → 0 padding 込みで `YYYY-MM-DD` 復元)、caller は validated 入力前提。`weekly-time-trend.ts` (-9 行 private 削除 + 4 callsite rename) / `daily-streak.ts` (-9 行 + 2 callsite rename)、+21 行 (iso.ts) + +37 行 (test)。test +6 件 (基本 / 月跨ぎ / 年跨ぎ / 長距離 / うるう年 / 0 padding = 11 → 17 件、iso.test.ts 全体 17/17 PASS)、weekly-time-trend 16/16 / daily-streak 18/18 既存 test も PASS (動作変更ゼロ確認)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。Date 解釈 (parseDateOrNull) + 日付 shift (shiftIsoDate) + ローカル化 (todayISO/isoDaysFromNow) の date 規約が `lib/date/iso.ts` に完全集約。次 iter331 = 81%5=1 → basics。
- ✅ [iter329 (weekday-peak-chip)] **TopItemsByTimeChip に曜日別 peak chip を追加 (basics, 1 commit, 4ff6076)**: iter327 で整備済 `weekday-time-distribution.ts` substrate (16/16 PASS) の UI bind。今までは「直近 7 日 trend ↑↓ + 実績 top 5」だけだった card に「曜日 peak (火曜 6h)」を追加し、ユーザの稼働パターン (どの曜日に集中しているか) を 1 行で可視化。実装: `splitTimeEntriesByWeek` 既算出の thisWeek + priorWeek を結合 (= 直近 14 日) して `groupTimeEntriesByWeekday` + `findPeakWeekday` + `formatWeekdayTimeDistributionJa` を useMemo に追加 (3 関数 substrate を combinator 1 ブロックで消化)、7 日 window だと各曜日が 1 回しか出ず pattern にならないので trend 同様 14 日を使う (peak の信頼性が増す + 既存 split 結果を再利用で再 query 不要)、peak が null (= 14 日 0 件) の時は chip 非表示 (UI 静か)、peak ある時のみ amber chip + ▤ glyph で trend chip 直下に配置、表示文言 `'曜日 peak: 火曜 (6h)'`、aria-label/title に full text `'曜日 peak: 火曜 (6h) — 直近 14 日 月 4h / 火 6h / 金 3h'`、sr-only に trend → peak → top 5 を連結 (1 回読みで全要約)、`data-testid="weekday-time-distribution-chip"` + `data-peak="<key>"` で Playwright hook。機能追加なし、shadcn 編集なし、helper / data fetch / mutation 無変更。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS、weekday-time-distribution test 16/16 PASS。**race 履歴**: 並走 session が iter327 として streak helper (`20efba8`) を着地、本 session の iter327 (weekday-time-distribution) は別 file (race 無し)。本 commit (iter328 message) は origin/main 上で position iter329 (streak iter327 を挟んだため)。次 iter330 = 80%5=0 → refactor。
- ✅ [iter327 (weekday-time-distribution)] **time_entries を曜日別 (月-日) に集計する pure helper (ai-automation, 1 commit, ce1393d)**: iter307 `category-summary.ts` (category 軸) / iter319 `weekly-time-trend.ts` (時系列前後比較) と並ぶ「time_entries の第 3 軸 = 曜日軸」substrate。AI 朝 brief / pm-agent / dashboard widget が「火曜に稼働が集中 (4h 30min) / 金曜は 0min」のような曜日パターンを 1 関数で取り出せる。今までは weekly-time-trend が 14 日合計、category-summary が 5 カテゴリ合計に圧縮していたが、曜日方向の peak は別実装が必要だった。新 module `src/features/time-entry/weekday-time-distribution.ts`: `WEEKDAY_KEYS = ['mon'..'sun']` の business 週順固定、`weekdayLabelJa` で日本語 1 文字 (月/火/水/木/金/土/日)、`groupTimeEntriesByWeekday(entries, options?) → Record<key, minutes>` で from/to inclusive 範囲フィルタ + UTC 構築で TZ shift 排除 + 非実在日 (例 2026-02-30) は除外、durationMinutes が NaN/負/Infinity は 0 加算 fail-soft、`findPeakWeekday(totals)` で最大の曜日 + 分 (tie は週前半 月→日 順を優先、全 0 → null)、`formatWeekdayTimeDistributionJa(totals, opts?)` で `'月 4h / 火 6h / 金 1h 30min'` 形式 (hideZero default true、全 0 → '0 件')、`formatMinutes` は category-summary から再利用で表記統一。caller benefits: AI 朝 brief「曜日別稼働: 月 4h / 火 6h ... peak: 火曜 6h」/ dashboard sparkline 7 buckets bind / pm-agent「火曜集中、金曜余力あり」context。test +16 件 (group 7 / label 2 / peak 3 / format 3 / 統合 1 = 16/16 PASS)、typecheck/lint 緑 (warning baseline 1 = backlog-view.tsx)、architecture 5/5 PASS。**race 履歴**: 並走 cloud session が iter324-326 (audit-activity / parseDateOrNull-集約 / activity-brief-chip) を本 session 開始までに着地済、iter327 (judge.sh 予測 = 77%5=2 → ai-automation) を本 session が獲得。commit message は session 開始時 judge 値 (iter324) のままだが、main 上の position は iter327。次 iter328 = 78%5=3 → basics。
- ✅ [iter326 (activity-brief-chip)] **ActivityLog 上部に audit-activity brief chip を追加 (basics, 1 commit, c61098b)**: iter324 で整備済 `audit-activity.ts` substrate (31/31 PASS) の UI bind。ActivityLog が個別 row の縦並びのみで「この Item に何件、誰が、どのカテゴリの操作」が一目で見れず行を順に scroll する必要があった → `formatAuditActivityBrief(data, {actorTypes: []})` を useMemo (Item 紐付けの audit_log は user/agent 両方合算)、結果を `<div role="status">` で list 上部に配置 (ActivityIcon + brief 文字列)、aria-label/title で full text、summary=null (0 件 / loading) は chip 自体非表示、既存 `<ul>` をラップする `<div>` 追加 (layout 変更最小)。chip 例: `'活動 7 件 (2 名): 作成 1 / 更新 4 / 完了 2'`。機能追加なし、shadcn 編集なし、typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter327 = 77%5=2 → ai-automation。
- ✅ [iter325 (parseDateOrNull-集約)] **notification/audit activity helpers の toDateOrNull を parseDateOrNull に集約 (refactor, 1 commit, c5b03e8)**: iter322 / iter324 で同一 shape の private `toDateOrNull` を 2 callsite で写経していたが、iter305 で `lib/date/iso.ts` に集約済の `parseDateOrNull` と完全互換 (全分岐で挙動一致)。2 callsite を delegation に置換 (-12 行)。動作変更ゼロ、test 34/34 PASS、typecheck/lint 緑 (warning baseline 1)。Date 解釈の defensive 振る舞いが 1 ファイル (lib/date/iso.ts) に完全集約され、新 substrate 追加時の挙動が一致する。次 iter326 = 76%5=1 → basics。
- ✅ [iter324 (audit-activity)] **audit_log を action カテゴリ + actor 別に集計する pure helper (ai-automation, 1 commit, 0776828)**: iter309 並走 `comment-activity.ts` (item 別) / iter322 `notification-activity.ts` (type 別) と同 shape の「分布集計」第 4 軸 — **audit_log の操作種別 / 担当者**。`action-visual.ts` に `AuditActionCategory` union (create/update/transition/complete/reopen/delete/other) と `getAuditActionCategory(action)` を追加 (iconKey 経由で同 vocabulary、未知 action は 'other')。新 module `src/features/audit/audit-activity.ts`: `auditActionCategoryLabel` (作成/更新/遷移/完了/完了取消/削除/その他)、`groupAuditByCategory(entries, options?)` で default actorTypes=['user'] (agent 起因の自動 mutation はチーム活動と分けたい)、actorTypes=[] で全 actorType 集計、since で ts フィルタ + 不正 ts は since 指定時のみ除外。`groupAuditByActor(entries, options?) → Map<actorId, count>` insertion order 保持。`formatAuditActivitySummary` で iter300 `formatNonZeroCounts` 流用 (`'作成 12 / 更新 5 / 完了 4 / 削除 1'`)。`formatAuditActivityBrief` combinator で `'活動 24 件 (3 名): 作成 12 / 更新 5'` 形式 (合計 + actor 数 + カテゴリ分布の 3 軸圧縮)、0 件 sentinel `'活動 0 件 (該当なし)'`。caller benefits: AI 朝 brief「今日のチーム活動」/ Sprint retro AI prompt の活動量表現 / dashboard stacked bar + actor pill。test +18 件 (groupByCategory 9 / groupByActor 4 / label 1 / summary 2 / brief 3 + action-visual 既存 3 = 31/31 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter325 = 75%5=0 → refactor。
- ✅ [iter323 (bell-breakdown)] **notification-bell popover に未読 type 別 breakdown を表示 (basics, 1 commit, 14857cb)**: iter322 で整備済 `notification-activity.ts` substrate (15/15 PASS) の UI bind。今までは bell popover header に「通知」とだけあり、ユーザは個別 row を順に読まないと「期限近接が多いのか / メンションが多いのか」が分からなかった。実装: `groupNotificationsByType(notifications, {onlyUnread:true})` で未読 type 別 count、`formatNotificationActivitySummary(counts)` で 1 行 summary、未読 0 件 / 全 type 0 件は null で chip 非表示 (UI 静か)、popover header の「通知」title 直下に `text-[10px] text-muted-foreground` の small text 配置 (`'期限近接 3 / メンション 2 / 招待 1'`)、aria-label「未読内訳: <summary>」 + title attribute (truncate 時 hover 全文)、data-testid="notification-bell-breakdown" hook。機能追加なし (既存 bell の click / mark-read / Realtime 無変更)、shadcn 編集なし。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter324 = 74%5=4 → ai-automation (or refactor 割り込み)。
- ✅ [iter322 (notification-activity)] **notifications を type 別に集計する pure helper (ai-automation, 1 commit, d1485ae)**: iter309 並走 `comment-activity.ts` (item 別) / iter307 並走 `category-summary.ts` (5 カテゴリ別) と同 shape の「分布集計」第 3 軸 — **notification type 別**。今までは notification-bell.tsx が inline 集計するか bell 内で逐次描画 (iter296 並走 d304ba7 で type-visual.ts は抽出済だが件数集計未整備)。新 module `src/features/notification/notification-activity.ts`: `NOTIFICATION_TYPE_KEYS` 既知 4 + unknown = 5 種、`notificationTypeLabel(type)` で日本語 label (期限近接/メンション/招待/同期失敗/その他、bell row vocabulary と整合)、`groupNotificationsByType(notifs, options?) → Record<NotificationTypeKey, number>` で `onlyUnread=true` で readAt!=null 除外 + `since` で createdAt 古い entry 除外 + 未知 type は unknown bucket、`formatNotificationActivitySummary(counts, emptySentinel?)` で iter300 `formatNonZeroCounts` 流用 (1 行 delegation)、`'期限近接 3 / メンション 2 / 招待 1'` 形式 + 全 0 → '0 件'。defensive: 不正 createdAt は since 不指定なら集計に含める (見落とし防止)、since 指定時のみ厳格除外。Date instance / ISO 文字列の両方を `toDateOrNull` で正規化 (iter305 parseDateOrNull と同 pattern)。caller benefits: AI 朝 brief「未読 6 件 (期限近接 3 / メンション 2 / 招待 1)」を 2 関数 + 1 整形、dashboard stacked bar、bell Realtime 通知数 type 別。test +15 件 (group 9 / label 2 / format 3 / 統合 1 = 15/15 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter323 = 73%5=3 → basics。
- ✅ [iter321 (weekly-trend-chip)] **TopItemsByTimeChip に weekly-time-trend chip を追加して稼働ダッシュボード化 (basics, 1 commit, aa852d3)**: iter319 で整備済 `weekly-time-trend.ts` substrate (16/16 PASS) を iter318 の TopItemsByTimeChip に bind。今までは「直近 7 日 実績 top 5」だけだった card を、今週 vs 先週の稼働時間トレンド (↑/↓/→/·) を card 上部に追加して「直近 7 日 稼働ダッシュボード」へ昇格。実装: `splitTimeEntriesByWeek` + `computeWeeklyTimeTrend` + `formatWeeklyTimeTrendJa` を useMemo に組合せ (3 関数 substrate を combinator 1 関数で消化)、`TREND_TONE` map で direction 別に Tailwind 配色 + glyph 意味付け (up=blue+↑ 忙しさ増 / down=red+↓ 失速 / flat=zinc+→ 安定 / idle=muted+· 記録なし)、`<div role="status">` で trend chip を card 上部、aria-label に full summary + data-direction で Playwright hook、top 0 件 + trend idle 二重条件で card 完全非表示 (14 日完全ゼロ時のみ)、top 0 件 + trend あり (item 紐付け無し dev のみ等) は trend chip だけ + 「Item 紐付けの稼働記録なし (自由稼働のみ)」説明、card title 「実績 top N」→「稼働ダッシュボード」、sr-only に trend + top 連結 summary。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。次 iter322 = 72%5=2 → ai-automation。
- ✅ [iter320 (formatMinutes-集約)] **formatMinutes 重複を category-summary に集約 (refactor, 1 commit, 31ff30e)**: iter319 (weekly-time-trend) で写経した private `formatHm` と iter318 (top-items-by-time-chip) で inline 書きしていた `h/m → 'Xh Ymin'` 変換 (4 行三項条件) を iter307 並走で抽出済の `category-summary#formatMinutes` に統一。`weekly-time-trend.ts` は private formatHm 削除 + formatMinutes import (既存 formatHm と完全互換: `max(0, round(min)) → h/m 分解 → 同 format`、動作変更ゼロ)、`top-items-by-time-chip.tsx` は inline 計算 4 行 → `formatMinutes(row.totalMinutes)` 1 行 delegation。-9 行 / +2 行 (合計 -7)。test 57/57 PASS (weekly-time-trend 16 / category-summary 22 / item-time-summary 19)、typecheck/lint 緑 (warning baseline 1)。formatMinutes 仕様 (`'4h 30min' / '30min' / '2h' / '0min'`) の変更が 1 ファイルに集約された。次 iter321 = 71%5=1 → basics。
- ✅ [iter319 (weekly-time-trend)] **直近 7 日 vs 先週 7 日 の稼働時間トレンド pure helper (ai-automation, 1 commit, 4000197)**: iter269 `bias-trend.ts` (見積精度の前後比較) と対称な「実作業量の前後比較」substrate。bias-trend は学習進捗 (1.50× → 1.20× 改善)、本 helper は稼働量変化 (先週 12h → 今週 15h、+3h、+25%)。役割使い分け: bias-trend=見積精度 / weekly-time-trend=稼働時間 / velocity (iter302 並走)=件数ペース。新 module `src/features/time-entry/weekly-time-trend.ts`: `splitTimeEntriesByWeek(entries, today)` で today 含む 7 日 (today-6..today) を thisWeek、先 7 日 (today-13..today-7) を priorWeek に分割 (UTC 計算で TZ shift 無関係、不正 ISO 除外で fail-soft)、`computeWeeklyTimeTrend(thisWeek, priorWeek)` で thisWeekMinutes / priorWeekMinutes / deltaMinutes / deltaPct (priorWeek=0 で null) / direction (idle / flat / up / down)、flat 閾値 |Δ%| < 5%、両 0 → idle、prior=0+this>0 → up + deltaPct=null。`formatWeeklyTimeTrendJa(trend)` で 4 文言 (idle / 増加 / 減少 / 安定)、formatHm helper で `category-summary#formatMinutes` と同じ見た目。caller benefits: AI 朝 brief 「今週は先週より +2h、忙しくなっています」/ pm-agent 「稼働時間 +25% 増 + 見積精度 安定」/ dashboard sparkline + tone bind (up=blue / down=red / flat=zinc)。test +16 件 (split 3 / compute 7 / format 4 / 統合 1 = 16/16 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter320 = 69%5=0 → refactor。
- ✅ [iter318 (top-items-by-time)] **直近 7 日 実績 top 5 chip を TimeEntries panel に bind (basics, 1 commit, 7093937)**: iter317 で整備済 `selectTopItemsByTime` substrate (19/19 PASS) の UI bind。iter315 (Document PiP) で実用性が上がったタイマー → time_entries 蓄積の流れをユーザが「直近 1 週間でどの Item に時間を使ったか」で振り返れる軽量カード。新 component `src/components/time-entry/top-items-by-time-chip.tsx`: `useTimeEntries` + `useItems` を流用 (新 server action 無し、bias-insight と同 pattern)、`selectTopItemsByTime(entries, 5, {from: 7日前, to: 今日})` で top 5 取得、順位 1-5 の番号 chip + Item title (truncate) + `Xh Ymin` + `N 件` を `<ol>` で順序付き表示、0 件は card ごと非表示で UI 静か、sr-only に `formatTopItemsByTime` の 1 行 summary 埋込で SR は要約即読み。`time-entries-panel.tsx` で `EstimateBiasInsight` 直下に mount (2 行差分)。typecheck/lint 緑 (warning baseline 1)。次 iter319 = 68%5=3 → basics (or refactor 割り込み)。
- ✅ [iter317 (item-time-summary)] **time_entries を item 別に集計する pure helper (ai-automation, 1 commit, 373a71c)**: iter307 並走 (category-summary、5 カテゴリ別) / iter314 (daily-summary、日次) と並ぶ「時間の見方」第 3 軸 — **item 単位**。iter315 で Scope B (Document PiP) でタイマー実用性が上がった今、「今日 / 今週 どの Item に何時間使ったか」を AI 朝 brief / Item ダイアログ「実績」 chip / dashboard 「top items by time」widget が 1 関数で取れる substrate。新 module `src/features/time-entry/item-time-summary.ts`: `groupTimeEntriesByItem(entries, options?) → Map<itemId, ItemTimeStats>` で itemId=null 除外 + from/to inclusive 範囲フィルタ + 不正 duration (NaN/負/Infinity/0) は entryCount 不算入で fail-soft + 小数は round。`selectTopItemsByTime(entries, n, options?)` で totalMinutes desc → entryCount desc → itemId asc の三段 stable sort、n<=0/NaN/Infinity で空配列、データ数超過は clamp。`formatTopItemsByTime(top, titles?)` で `'実績 top 3: A (4h 30min) / B (1h 15min) / C (45min)'` 形式 (`category-summary#formatMinutes` 流用、iter307 抽出済を再利用)。0 件 sentinel `'実績 0 件 (該当なし)'`、title 欠落 `'(無題: <id 先頭 8 文字>)'` fallback。caller: AI 朝 brief「今日の実績 top 3」/ Item edit dialog の実績 chip / dashboard top 5 items widget が同 logic で揃う。test +19 件 (group 7 / select 7 / format 4 / 統合 1 = 19/19 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter318 = 67%5=2 → ai-automation (or refactor 割り込み)。
- ✅ [iter316 (sprint-tone-label)] **sprint progress tone の日本語 label を burndown.ts に集約 (refactor, 1 commit, 8d44afa)**: iter298 並走 `95dd87c` で sprints-panel.tsx に UI bind 時 inline 定義した 4 tone → 日本語 label の Record map (`done='達成' / onTrack='順調' / behind='遅れ気味' / idle='進行中'`) を burndown.ts に集約 (`goalHealthTierLabel` iter299 並走 e0a4d20 と対称な API)。`burndown.ts` に `sprintProgressToneLabel(tone)` 公開、internal `SPRINT_PROGRESS_TONE_LABEL` Record で実装。`sprints-panel.tsx` の 3 callsite (sr-only / aria-label / aria-valuetext) を `PROGRESS_TONE_LABEL[tone]` → `sprintProgressToneLabel(tone)` に置換、inline map 削除 (-9 行)。bar Tailwind class (`PROGRESS_TONE_BAR_CLASS`) は UI 結合のままで inline 維持。test +5 件 (4 tone label + sprintProgressTone 組合せ = 33/33 PASS)。動作変更ゼロ (文言完全同一)、typecheck/lint 緑 (warning baseline 1)。AI brief / dashboard widget / 別 component が同 label を 1 関数 import で再利用可能に。次 iter317 = 66%5=1 → basics。
- ✅ [iter315 P0 (timer-pip)] **タイマー panel を Document Picture-in-Picture で常に手前表示 window 化 (queue: TickTick タイマー Scope B, 1 commit, 0e5e0d5)**: FEEDBACK_QUEUE 🔥 P0 最優先 (track 判定上書き) を消化。iter247-249 で実装済 Scope A (in-page 常駐 floating panel) を Document PiP API で別 window 化 + 常に手前表示に拡張。新 module `src/lib/browser/document-pip.ts` (+106 行) に pure helper 3 つ: `isDocumentPipSupported(win?)` (capability 検出、Chrome 116+ / Edge) / `openDocumentPipWindow({width, height, sourceWindow?})` (async open、未対応 / ユーザ拒否 / 例外時は `null`) / `copyStylesheetsToWindow(target, sourceDoc?)` (`<link rel="stylesheet">` href + media / `<style>` textContent / 構築済 `adoptedStyleSheets` の 3 経路で Tailwind 等を PiP window に届ける)。**全 helper が window/document を引数で受け取る**ので vitest (node env、jsdom 不使用) で plain object mock のみで test 可能 (12 件 PASS)。`active-timer-panel.tsx`: PiP button (Lucide `PictureInPicture2`) を Pause/Resume と Stop の間に配置、click で `createPortal(body, pipWindow.document.body)` で同 React tree を PiP window に render (Zustand store なので state 自動共有)、capability 検出は `useSyncExternalStore` で SSR (false) / client (実検出) の hydration mismatch を回避、`pagehide` listener で × close 検知 → parent state 戻す、Item 切替時は `useEffect` で自動 close (タイマー入れ替え時の混乱回避)。Chrome / Edge は別 window 化 + 常に手前、Safari / Firefox は button disabled + reason aria-label「Picture-in-Picture は Chrome / Edge で利用可能」+ tooltip。PiP 中は full-window 中央寄せ (320×140 想定)、in-page は右下 fixed 小型 panel と同一 body 共有 (`data-pip` attr で hook)。typecheck/lint 緑 (warning baseline 1)。これで「フローティングでウィンドウ常に手前表示のやつ」(2026-04-29 ユーザ指摘) が消化完了。次 iter316 = 65%5=0 → refactor。
- ✅ [iter313 (palette-item-row)] **command-palette item 検索 row に StatusBadge + 親しみやすい日付 (basics, 1 commit, db5bd7e)**: iter263 で Kanban / Today / Inbox / Backlog 等で確立した「ISO 日付 → formatFriendlyDate」+ 「app 共通 StatusBadge」 graphical pattern を **Command Palette の item 検索 row** にも波及。これまでの row は priority dot + title + raw `2026-04-30` + MUST badge で、status は完全欠落、due date は ISO 生表示で「明日 / 今日 / 期限切れ」が一瞬で読み取れなかった。`formatFriendlyDate(dueDate, today)` で `4/30 (木)` / `明日` / `今日` / `期限切れ -3日` 等に整形 (ISO は title 属性に retain で SR と hover で正確)、`<StatusBadge status={item.status} iconOnly />` を priority dot の直後に追加 (size text-[10px]、Circle / PlayCircle / CheckCircle2 / XCircle / Ban の app 共通 icon)。`today` 基準日は各 render で再計算 (cost 無視できる、深夜跨ぎ追従、Kanban との相違: 大量 row 無いので memoize 不要)。+22 行 / -2 行 (component 全体 157 → 177 行)。動作変更ゼロ (priority sort / fuse 検索 / onSelect は無変更)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305/306/307/309/310 で 11 件着地、本 iter313 で 12 件目。command-palette は parallel が未着手 (iter全期間 でこの file への新規 commit 無し) で race リスクを最小化。次 iter314 = 64%5=4 → ai-automation。
- ✅ [iter312] **期間内 完了 / 追加 / 平均所要 を集計する pure helper (ai-automation, 1 commit, 8266a7d)**: iter302 並走 (velocity 日次 done count) / iter299 (stale-items) / iter302 (freshly-done) と並ぶ AI substrate。Sprint retro / 週次 review / OKR 期間終了時の AI brief が「この期間で N 件完了 / M 件追加 / 平均所要 K 日」を 1 関数で取り出せる。今までは retro-doc-service が SQL で集計、dashboard widget が独自 inline と 2 系統別。新 module `src/features/item/period-completion.ts`: `summarizePeriodCompletion(items, {from, to}) → {itemsCompleted, mustCompletedCount, itemsAdded, avgCompletionDays, periodDays}`、to は その日 23:59:59 まで inclusive (UTC)、from > to で全 0、不正 ISO fail-soft、avgCompletionDays は 0 件 null + round 1 桁。`formatPeriodCompletion(s) → string` で `"直近 7 日: 完了 12 件 (MUST 3) / 新規追加 5 件 / 平均所要 3.2 日"` 形式 (省略形バリエーション 4 種)。iter305 で集約済の `parseDateOrNull` を再利用、defensive 振る舞いが他 substrate と一致。test +12 件 (summarize 8 / format 4 = 12/12 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter313 = 63%5=3 → basics。
- ✅ [iter310 並走 (gantt-month-boundaries)] **gantt-view の monthBoundaries 計算を gantt-range.ts に集約 (refactor, 1 commit, a71f479)**: 並走 cloud session が iter310 として `47661cd` (`describe-for-prompt#formatDueLabel` を due-proximity.ts に集約) を先取 push 済だったが、本 session は iter305 の続編 — gantt-view.tsx 内 inline 計算を引き続き集約 — を独立 commit、rebase で `a71f479` として origin/main に乗った (iter# は重複だが内容は別軸 — 別 file の別 logic 集約)。新 export `computeMonthBoundaries(days)`: days 配列を走査し前日と月が変わる index を返す pure 関数。caller 側は timeline 上で `i * dayPx` に縦線を描く (TeamGantt 風 month boundary)。年跨ぎ (12 月 → 1 月) も別月扱いで境界に入る (getMonth は 0-11)。動作変更ゼロ (元 inline 6 行と完全互換)、test +6 件 (全日同月 / 月跨ぎ 1 回 / 2 回 / 年跨ぎ / 空 / 1 件 = 23/23 PASS)。`gantt-view.tsx` は 570 → 567 行 (-3)、helper +22 / test +44。AI brief / dashboard widget が「Gantt 範囲内に何月分あるか」を知る時に流用可。typecheck/lint 緑 (warning baseline 1)。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305/306/307/309 で 10 件着地、本 iter310 で 11 件目。次 iter311 = 61%5=1 → basics (parallel `d67d683` 既着地)。
- ✅ [iter311] **Backlog 上部に見積合計 chip を表示 (basics, 1 commit, d67d683)**: iter297 並走 (`9248459`) で `summarizeItemEstimates` / `formatItemEstimateSummary` substrate (estimate.ts) を整備済だったが UI に bind されていなかった。Backlog は「計画する場所」なので見積合計が一目で見えると優先順位付け / MUST 容量見積もりがしやすい。`backlog-view.tsx`: `formatItemEstimateSummary(summarizeItemEstimates(data))` を useMemo で計算、table 上部に rounded chip 配置 (⏱ icon + 集計テキスト)、`aria-label` で SR full summary、`data-testid="backlog-estimate-summary"` hook。`data` は既存の filter+sort 済 backlog rows なのでユーザの sort/filter に追従して合計が更新される。substrate 無変更、UI bind のみ (+15 行)。typecheck/lint 緑 (warning baseline 1)。これで Sprint progress (iter298) → 依存タブ readiness (iter306) → Goal progress tier (iter301) → Backlog 見積 (本 iter) の "substrate → UI" pipeline が 4 軸目で揃った。次 iter312 = 62%5=2 → ai-automation。
- ✅ [iter310] **describe-for-prompt の formatDueLabel を due-proximity.ts に集約 (refactor, 1 commit, 47661cd)**: iter287 で `getDueProximity` / `DueProximityKind` を抽出済、iter295 で `urgency.ts` の `dueProximityBonus` を集約済だが、`describe-for-prompt.ts` (iter294 並走) は独自の inline parseIso (`/^(\d{4})-(\d{2})-(\d{2})/` regex + 月日 範囲検証) + diffDays = round((due - todayBase) / 24h) + if/elseif chain で 6 mode label を保持していた。`getDueProximity(dueDate, today)` の `{kind, diffDays}` を switch する DUE_PROMPT_LABEL Record lookup + sign 補完に置換 (-12 行 / +13 行)。動作変更ゼロ: describe-for-prompt.test.ts 20/20 PASS、`期限切れ(-3d)` / `今日(0d)` / `明日(+1d)` / `今週内(+5d)` / `今後(+30d)` / `なし` の 6 mode label は完全同一。typecheck/lint 緑 (warning baseline 1)。Date 解釈 / 閾値 / kind の単一ソース化が 3 callsite (urgency.ts / describe-for-prompt / due-proximity 自身) で揃い、新 substrate 追加時の挙動が完全一致するようになった。次 iter311 = 61%5=1 → basics。
- ✅ [iter309 並走 (comment-activity)] **comments を item 別に集計する pure helper (ai-automation, 1 commit, d6c438c)**: 並走 cloud session が iter309 として `a169ea7` (effort bucket group/count/format) を先取 push 済だったが、本 session は別 domain — **comment_on_items の AI substrate** を独立 commit、rebase で `d6c438c` として origin/main に乗った (iter# は重複だが内容は別軸 — effort bucket は item 見積分布、comment-activity は item 別 comment 件数)。iter287/289 で「item 軸」、iter302 で「日次 done」、iter307 並走で「time-entry 軸」を整備済、本 commit で **comment domain** に AI substrate を新設。新 module `src/features/comment/comment-activity.ts`: `CommentActivityEntry` 最小 structural subset (itemId / createdAt) + `groupCommentsByItem(comments, options?)` で itemId → count Map (件数 0 は含まない) + `since: Date | string ISO` で日付フィルタ + 不正 createdAt は since 指定時のみ除外 + `selectTopCommentedItems(comments, n, options?)` で件数降順 + 同件数挿入順 stable + n<=0 で空配列 + `formatTopCommentedItemsSummary(entries, itemTitles?)` で `'活発 3 件: A (5 件) / B (3 件)'` 形式 + 0 件 sentinel `'活発 0 件 (該当なし)'` + title 欠落 `(無題: <id 先頭 8 文字>)`。caller benefits: AI 朝 brief が 「直近 7 日で活発: A (5 件) / B (3 件)」を 2 関数 + 1 整形で出せる、pm-agent prompt の「議論中 item」抽出、dashboard widget 「workspace 活動状況 chip」、since 引数で「今日 / 今週 / 今月」切替。test +18 件 (groupCommentsByItem 6 / selectTopCommentedItems 6 / format 5 / 統合 1 = 18/18 PASS)。typecheck/lint 緑 (warning baseline 1)。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305/306/307 で 9 件着地、本 iter309 で 10 件目。comment domain は parallel が未着手 (commits 全期間で comment/ 内に新規 file 追加無し) で race リスクを最小化。次 iter310 = 60%5=0 → refactor (parallel `a169ea7` の effort-bucket は既着地、track 重複 OK)。
- ✅ [iter309] **見積サイズ別の effort bucket group/count/format helper (ai-automation, 1 commit, a169ea7)**: iter287 (due-proximity) / iter292 (priority) / iter294 (status) / iter294 並走 (urgency-tier) と対称な「効率の意味付け」substrate。AI brief / pm-agent / dashboard widget が `<30m 5 / 30m-2h 3 / 1d+ 1 / 見積なし 2` のような effort 分布を 1 関数で出せる。`estimate.ts` に `EffortBucket` union (quick<30m / medium 30m-2h / large 2h-1d / xlarge >=1d / unknown) + `groupItemsByEffortBucket` / `countItemsByEffortBucket` / `formatEffortBucketCounts` を追加。bucket 境界は PR sizing 慣例 (skim < 30m / 1集中 medium / 半日 large / 1 日以上 xlarge) と一致、unknown は description から `extractEstimateMinutes` が値を取れない時に集約。format は iter300 (`formatNonZeroCounts`) を流用 (1 行 delegation)。iter300 集約の効用が 5 軸目で表れた。test +12 件 (group 3 / count 2 / format 2 + 既存 11 = 28/28 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter310 = 60%5=0 → refactor。
- ✅ [iter307 並走 (category-summary)] **time_entries を category 別に集計する pure helper (ai-automation, 1 commit, ba646f5)**: 並走 cloud session が iter307 として `deb0d3a` (`computeSprintProjection` velocity 加味の完了予想) を先取 push 済だったが、本 session は別観点 — **time-entry domain の AI substrate** を独立 commit、rebase で `ba646f5` として origin/main に乗った (iter# は重複だが内容は別軸 — Sprint projection と time-entry category 集計は別 entity)。iter287 / iter289 で「item 軸」、iter302 (velocity) で「日次 done count」を整備済、本 commit で **time-entry 軸** の trio (group / format / total) を新設。新 module `src/features/time-entry/category-summary.ts`: `groupTimeEntriesByCategory(entries, options?)` で全 5 カテゴリ (dev/meeting/research/ops/other) を 0 で初期化した `Record<TimeEntryCategoryKey, number>` を返す + `from`/`to` ISO 日付で範囲フィルタ (両端 inclusive) + 不正 durationMinutes (NaN/負/Infinity) は 0 加算 + 未知 category は 'other' に集約。`formatMinutes(min)` で `'4h 30min'` / `'30min'` / `'2h'` / `'0min'` 整形 (NaN/負/Infinity/小数も round で fail-soft)。`formatCategorySummary(totals, opts?)` で `'開発 4h / MTG 1h / 調査 30min'` の 1 行整形 (`hideZero=true` default で 0 件省略、全 0 → `'0 件'`)。`totalCategoryMinutes(totals)` で全カテゴリ合計分を返す (dashboard 「今日合計 5h」表示用)。caller benefits: AI 朝 brief が 2 関数 + 1 整形で「今日の作業: 開発 4h / MTG 1h / 調査 30min (合計 5h 30min)」を出せる、dashboard widget 同 logic で「stacked bar」、from/to で 7 日 / 30 日 window 切替が 1 行差分で完結。test +22 件 (集計 4 / 範囲 4 / formatMinutes 5 / formatCategorySummary 6 / total 2 / 統合 1)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305/306 で 8 件着地、本 iter307 で 9 件目。time-entry domain は parallel が手薄 (bias-\* 系は触られているが categories 集計は未着手) で race リスクを最小化。次 iter308 = 58%5=3 → basics (parallel `0c19bcf` 既着地)。
- ✅ [iter308] **Template EmptyState を onboarding 用例 + recurring 説明に置換 (basics, 1 commit, 0c19bcf)**: iter273 (Today) / iter276 (Inbox) / iter281 (Workflows) / iter283 (Sprints) / iter286 (Goals) / iter288 (items-board) / iter291 (Archive) で進めた EmptyState onboarding パターンを Template にも横展開、これで 8 view 目で同パターンが揃った。旧「上のフォームから作成してください」(schema 用語ベース) → (1) 「即実行ワークパッケージ」の 1 click 展開という売りを最初に提示、(2) 用例 2 例 (`毎週月曜 朝 9 時 / 週次レビュー` 系 + オンボーディング系) を `<code>` chip で、(3) recurring (cron) + Mustache 変数の存在を 2nd 段で示唆。description prop は ReactNode を取れる (async-states.tsx:48) ので `<p>` + `<code>` の構造化記述を直書き、機能追加なし、shadcn 編集なし。+21 行 / -1 行。typecheck/lint 緑 (warning baseline 1)。Template を初見の user が「何のための機能か」「どう使うか」「自動化の幅」を 1 EmptyState で把握できる。次 iter309 = 59%5=4 → ai-automation。
- ✅ [iter307] **velocity 加味の Sprint 完了予想 (projection) pure helper (ai-automation, 1 commit, deb0d3a)**: iter285 (`computeSprintBurndown`) / iter298 並走 (`sprintProgressTone`) の上に「実ペース (avgPerDay) 加味の予想完了日 / 上振れ・下振れ日数」を返す projection substrate を追加。Sprint review の AI brief / dashboard widget が「このペースなら N 日上振れ / 下振れ」を 1 関数で出せる。今までは elapsedPct vs completionPct 比較しか無く、実 velocity を加味した projection が取れなかった。`burndown.ts`: `SprintProjectionKind` union (done / ahead / on-pace / behind / overdue / unknown)、`computeSprintProjection({total, done, endDate, today?, avgPerDay}) → SprintProjection` で total=0 / 全 done / avgPerDay<=0/NaN/Infinity は kind 別 fail-soft、それ以外は `残件 / avgPerDay = 必要日数`、`projectedFinishDate = today + ceil(必要日数)`、`daysAhead = endDate - projectedFinishDate` (符号付き)。kind 閾値: >=2 ahead / -1..+1 on-pace / -7..-2 behind / <-7 overdue。`formatSprintProjection(p) → string` で `"Sprint 完了 (期限まで N 日)"` / `"予想 +N 日上振れ (date)"` / `"予想 ±0 (date)"` / `"予想 -N 日下振れ (date)"` / `"予想 -N 日 (期限超過、date)"` / `"予想不能 (ペース不明)"` の 6 mode。velocity (iter302 並走 6b50b60) と組合わせて pm-agent prompt が「このペースなら 2 日上振れ完了見込み (5/3)」と提案できる。test +15 件 (computeSprintProjection 8 / formatSprintProjection 7 = 31/31 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter308 = 58%5=3 → basics。
- ✅ [iter306 並走 (notification-prefs-icon)] **通知設定 toggle に type 別 icon + 配色 chip を追加 (basics, 1 commit, 37905a7)**: 並走 cloud session が iter306 として `4596842` (item-dependencies-panel に readiness summary chip bind) を先取 push 済だったが、本 session は別観点 — iter296 並走 (`d304ba7` notification-bell + `getNotificationTypeVisual`) で確立した通知 type graphical pattern を **設定 panel 側にも波及** — を独立 commit、rebase で `37905a7` として origin/main に乗った (iter# は重複だが内容は別軸)。今までは notification-preferences の 4 toggle が text のみで bell の row との視覚的紐付けが弱く、「heartbeat 通知を OFF にしたら何が消えるか」が toggle の文字列だけで分かりづらかった。`TOGGLES` 配列に `type: 'heartbeat' | 'mention' | 'invite' | 'sync-failure'` フィールドを追加 (既存 `key` は preference DB 列名、`type` は notification.type と同 vocabulary)、各 toggle 行に `getNotificationTypeVisual(spec.type)` で取得した bg/text/ring class + Lucide icon を size-5 rounded chip で配置 (notification-bell の row icon と完全に同じ視覚)。`TYPE_ICON` map (5 行) は notification-bell.tsx と同 shape を保持 (集約は parallel race リスクが上回るので各 component で独立)。chip に `role="img"` + `aria-label="期限近接通知"` 等で SR が type 即時 announce、`<li>` に `data-notification-type={spec.type}` 追加 (Playwright hook)。+46 行 / -2 行。動作変更ゼロ (toggle 動作 / preferences DB 更新は無変更)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。視覚効用: bell の row icon (red/blue/emerald/amber/zinc) と同 graphical で「設定 ↔ 受信」が即時紐づく ── toggle を OFF にすると同色の通知が消える対応関係が一瞬で伝わる。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305/306 で 8 件着地。次 iter307 = 57%5=2 → ai-automation。
- ✅ [iter306] **item-dependencies-panel に readiness summary chip を bind (basics, 1 commit, 4596842)**: iter297 で整備済 `summarizeDependencyReadiness` / `formatDependencyReadiness` substrate (28/28 PASS) の UI bind。依存タブを開いた瞬間に「いま着手可能か / 何件残っているか」が 1 chip で伝わる (FEEDBACK_QUEUE「Item dependencies tab: 依存先を visual chain」候補の前段、フル DAG 図は scope 大なので段階的に進める)。`item-dependencies-panel.tsx` の `<div data-testid="dependencies-panel">` 先頭に rounded chip を配置: blocked=amber+⏸ / ready+依存あり=emerald+✓ / 全 0 件=zinc+·、`role="status"` + `aria-live="polite"` で動的更新時 SR announce、`aria-label` に full summary、`data-blocked` / `data-testid="dep-readiness-chip"` hook。substrate (readiness.ts) は無変更、UI bind のみ (+27 行)。動作変更ゼロ (依存追加 / 削除 / section render は無変更)、typecheck/lint 緑 (warning baseline 1)。これで Sprint (iter298 並走) → Goal (iter301) → 依存タブ (本 iter) と "substrate → UI" pipeline が 3 軸目で揃った。次 iter307 = 57%5=2 → ai-automation。
- ✅ [iter305 並走 (gantt-range)] **gantt-view の range / totalDays / todayX を pure helper に抽出 (refactor, 1 commit, 76c7acd)**: 並走 cloud session が iter305 として `8ec28a0` (`parseDateOrNull` を date/iso.ts に集約 + goal-health の format 共通化) を先取 push 済だったが、本 session は別観点の refactor (`gantt-view.tsx` 583 行内 inline 計算を pure helper に集約) を独立 commit、rebase で `76c7acd` として origin/main に乗った。iter285 (sprint-burndown) / iter300 並走 (import-status) と同パターン。新 module `src/features/gantt/gantt-range.ts`: `computeGanttRange(rows)` (両端 1 日余白 / 空 → null) + `computeTotalDays(range)` (両端含む日数 / null → 0) + `computeTodayX(range, today, dayPx)` (範囲外 null、時刻 fraction 込み)。元 inline (range の useMemo + reduce 12 行 / totalDays / timelineWidth / days 5 行 / today 線 6 行) と完全互換、test 17 件で挙動を保証 (range 4 / totalDays 3 / todayX 9 / 元 inline 一致 1)。`gantt-view.tsx` の useMemo dep array が縮小、helper は AI brief / dashboard widget も流用可能 (例: 「今 Gantt は M ヶ月の範囲」)。`gantt-view.tsx` は 583 → 574 行 (-9)、helper +72 / test +125。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294/299/300/301/303/304/305 で 7 件着地。parallel が手薄な gantt domain を選択して race リスクを最小化。次 iter306 = 56%5=1 → basics。
- ✅ [iter305] **parseDateOrNull を date/iso.ts に集約 + goal-health の format 共通化 (refactor, 1 commit, 8ec28a0)**: iter302 並走 (velocity) / iter302 (freshly-done) / iter299 (stale-items) で 3 callsite に完全同一の private `toDate(input: Date | string | null | undefined): Date | null` fail-soft parser が重複していた → `src/lib/date/iso.ts` の `parseDateOrNull` に集約。仕様: null/undefined/空文字 → null、Date instance → finite 検査で素通し、文字列 → new Date() parse で finite なら通す。RFC3339 / YYYY-MM-DD どちらも OK。合わせて iter299 並走 `goal-health.ts` の `formatGoalHealthCounts` を iter300 (`formatNonZeroCounts`) で集約済の共通 format helper に置換 (5 行 inline → 1 行 delegation)。3 重複定義削除 (-21 行) + parseDateOrNull 追加 (+18 行) + iso test +6 件 = 86/86 PASS (velocity 17 / freshly-done 12 / stale-items 15 / goal-health 29 / iso 13)。動作変更ゼロ、typecheck/lint 緑 (warning baseline 1)。Date 解釈の defensive 振る舞いが 1 ファイルに集約され、新 substrate 追加時の挙動が一致する。次 iter306 = 56%5=1 → basics。
- ✅ [iter304 並走 (brief-summary)] **4 substrate を統合する brief-summary combinator (ai-automation, 1 commit, a1d3be2)**: 並走 cloud session が iter304 として `7ec5121` (`selectStartableItems` 着手可能 item 抽出) を先取 push 済だったが、本 session は別観点 — **substrate combinator** として独立 commit、rebase で `a1d3be2` として origin/main に乗った (iter# は重複だが内容は別軸 — startable は 1 substrate 追加、本 commit は 4 substrate を 1 関数で集約)。新 module `src/features/item/brief-summary.ts`: `BriefItemFields` 型 (4 substrate の structural union)、`buildBriefSummary(items, options?, today?)` で `selectTopUrgentItems` + `selectAtRiskMust` + `selectStaleItems` + `computeVelocity` を同 today で実行し、`{topUrgent, mustAtRisk, stale, velocity, textSummary}` を返す。textSummary は 4 行 (urgent / must-at-risk / stale / velocity) を改行区切りで連結、0 件軸でも 1 行残す (例: `"stale 0 (該当なし)"` 朝 brief で「今日は MUST at-risk なし」が伝わる方が静寂より親切)。同 today を 4 substrate に統一して渡すので深夜跨ぎでの不整合 (urgent は今日 / stale は昨日 today) が起きない、options 1 個で thresholds 一括管理 (`topUrgentN` / `mustAtRiskWithinDays` / `staleThresholdDays` / `velocityWindowDays`、各 default 3/6/7/7)。併設 `formatTopUrgentLine` で top urgent も既存 substrate 同様 1 行整形。caller benefits: AI brief を 4 関数 + 4 format から 1 関数呼出に圧縮、prompt tuning が 1 callsite で完結。test +14 件 (substrate 統合 4 / textSummary 整形 3 / formatTopUrgentLine 4 / 一貫性 3)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294 (urgency-tier) / iter299 (goal-health) / iter300 (import-status) / iter301 (dep-direction) / iter303 (kanban-accent) / 本 iter304 で 6 件着地。次 iter305 = 55%5=0 → refactor。
- ✅ [iter304] **着手可能 (startable) item 抽出 pure helper (ai-automation, 1 commit, 7ec5121)**: iter294 (must-risk 焦り) / iter299 (stale-items 放置) / iter302 (freshly-done 達成) / iter302 並走 (velocity ペース) と並ぶ AI 朝 brief substrate 第 5 弾 (race lost: iter303 basics で `60887e1` が同じ Kanban accent を 2 分早く先取 push、本 session の eca84c5 を rebase abort して別軸 ai-automation に切替)。「着手可能」= doneAt/archivedAt 両方 null + status not in (cancelled/blocked) + startDate 未指定 or today 以前。新 module `src/features/item/startable-items.ts`: `selectStartableItems(items, opts, today) → T[]` で urgency 降順 (computeUrgency 内部呼び) + 同 urgency stable + limit option (default 全件) + excludeStatuses option (workspace 別 status 運用に対応)。`formatStartableItemsSummary(items, showTop?) → "着手可能 5 件: A (p1) / B (p2) / 他 3 件"`。0 件 sentinel `"着手可能 0 件 (該当なし)"`、title 欠落 `"(無題)"` + priority 欠落 p4 fallback。must-risk + stale + velocity + freshly-done と組合わせて pm-agent prompt が「今日 5 件着手可能、うち MUST 2 / 期限切れ 1、放置 3 件、直近完了 4 件」と 1 paragraph で提案できる substrate が 5 軸揃った。test +14 件 (selectStartableItems 10 / format 4 = 14/14 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter305 = 55%5=0 → refactor。
- ✅ [iter303 (kanban-card-accent)] **Kanban カードに blocked/done の左 accent border + 背景色 (basics, 1 commit, 60887e1)**: FEEDBACK_QUEUE「Kanban カード: 完了 / blocked の視覚 hint を強化 (現状 title 文字色のみ)」候補を消化。column を横スキャンする時 (狭幅画面 / 多数 swimlane) や column を非表示にした時に「これは blocked」「これは done」が title 文字色 (line-through) だけでは弱かった。新 module `src/components/workspace/kanban-card-accent.ts` (pure helper、React 依存ゼロ): `getCardStatusAccent(status)` で blocked → 左 4px amber border + amber/30 背景 (警告強調) / done → 左 4px emerald border + emerald/15 背景 (line-through 補強) / 他 (todo / in_progress / cancelled / 未知) → 装飾なし `bg-background`。null / undefined / 空文字も装飾なしで fail-soft。`kanban-view.tsx#KanbanCard` の className に `${accent.bgClass} ${accent.borderClass}` を合成、`data-status={item.status}` 属性を追加 (Playwright / 単体テスト hook)。test +11 件 (known 2 / default 6 / 整合 2)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294 (urgency-tier) / iter299 (goal-health) / iter300 (import-status) / iter301 並走 (dep-direction) / 本 iter303 で 5 件着地。次 iter304 = 54%5=4 → ai-automation。
- ✅ [iter302 並走 (freshly-done)] **直近 N 日に完了した item 抽出 pure helper (ai-automation, 1 commit, ac11e1d)**: 並走 cloud session が iter302 として `6b50b60` (`computeVelocity` 集計) を先取 push 済だったが、本 session は別観点の AI substrate (item 単体の「最近やったこと」展開) を独立 commit、rebase で `ac11e1d` として origin/main に乗った (iter# は重複だが内容は別軸 — velocity が「件数 / ペース」、freshly-done が「item 単位の中身」)。iter299 (`selectStaleItems` 放置) と完全対称な API shape。新 module `src/features/item/freshly-done.ts`: `selectFreshlyDoneItems(items, opts, today) → FreshlyDoneItemEntry<T>[]` で「archivedAt 除外 + doneAt が today - thresholdDays より新しい (default 7 日)」を抽出、daysSinceDone 昇順 (新しい順) + 同日 stable。`formatFreshlyDoneSummary(entries, thresholdDays?) → "完了 3: A (今日) / B (3 日前) / C (7 日前)"` で AI prompt 用 1 行。daysSinceDone=0 は "今日" と即時性強調、0 件 sentinel `"完了 0 件 (直近 N 日)"`、title 欠落 `"(無題)"` fallback。未来 doneAt / 不正値 fail-soft、thresholdDays<0 で空配列。stale-items + freshly-done で「忘れている / やった」の双方向、velocity と組合わせて「ペース + 中身 + 放置」の三軸が AI 朝 brief / 週次 retro Doc に乗せられる。test +12 件 (selectFreshlyDoneItems 8 / format 4)。typecheck/lint 緑 (warning baseline 1)。次 iter303 = 53%5=3 → basics。
- ✅ [iter302 (velocity)] **velocity (直近 N 日 done 件数) 集計 pure helper (ai-automation, 1 commit, 6b50b60)**: iter294 (must-risk) / iter299 (stale-items) と並ぶ AI substrate 第 3 弾。AI 朝 brief / sprint burndown / pm-agent / dashboard が「直近 7 日の完了ペース」を 1 関数で取り出せる。今までは dashboard が SQL 集計、sprint burndown が inline 計算と 2 系統別実装。新 module `src/features/item/velocity.ts`: `computeVelocity(items, opts, today) → {byDay: {date, count}[], total, avgPerDay, trend: 'up'|'flat'|'down'}`。byDay は古い順 today 含む N 日 (default 7)、trend は前半 N/2 vs 後半 N/2 の比較で ±20% 閾値。`formatVelocitySummary(r, windowDays?) → "直近 7 日 velocity: 12 件 (1.7 件/日、傾向 up)"`、0 件は `'直近 7 日 velocity: 0 件'`。windowDays<=0 / 不正 doneAt は除外 fail-soft、archive/deletedAt の done も含む (一度完了した実績は消えない契約)。iter294/iter299 と組合わせて「焦る (must-risk) + 忘れる (stale) + ペース (velocity)」を AI 朝 brief 1 prompt で把握可能。test +17 件 (computeVelocity 13 / formatVelocitySummary 4)。typecheck/lint 緑 (warning baseline 1)。次 iter303 = 53%5=3 → basics。
- ✅ [iter301 並走 (dep-direction)] **item-dependencies-panel に方向 icon (← → ⇄) + StatusBadge 統一 (basics, 1 commit, 30ce2e4)**: 並走 cloud session が iter301 として `a5b91d7` (Goal progress bar tier UI bind) を先取 push 済だったが、本 session は別観点の basics (FEEDBACK_QUEUE「Item dependencies tab: 依存先を visual chain (矢印付き mini DAG)」候補の前段) を独立 commit、rebase で `30ce2e4` として origin/main に乗った。フル mini DAG (React Flow) は scope 大なので、まず同 panel 内で方向の意味と status の graphical 表現を icon + 配色 chip に揃える小幅化。`Section` props を `accent` (rose/amber/slate) → `direction` ('incoming' / 'outgoing' / 'mutual') に変更、内部 `DIRECTION_CONFIG` で icon と色を map (incoming=ArrowLeft+rose / outgoing=ArrowRight+amber / mutual=ArrowLeftRight+slate)。各 row 左端に方向 icon を `role="img"` + `aria-label` 付きで配置 — SR 利用者に「前提 (上流の完了を待っている)」/「後続 (この Item の完了を待っている)」/「関連 (双方向)」を即時 announce。raw status 表示 (`{ref.status}` を accent 色 chip) を `<StatusBadge status={ref.status} iconOnly />` に置換、app 全体で確立済の status icon (Circle / PlayCircle / CheckCircle2 / XCircle / Ban) + 配色を流用 — 「TODO=slate / 進行中=blue / 完了=emerald / blocked=amber」が dep panel でも一貫。`data-direction={direction}` を `<li>` に追加 (Playwright / 単体テスト hook)。動作変更ゼロ、66 行 +52 / -14 (panel 全体 249 → 287 行)。typecheck/lint 緑 (warning baseline 1)、architecture 5/5 PASS。本 session race 履歴: iter290/292/296 で 3 件 race lost、iter294 (urgency-tier 4d11bd1) / iter299 (goal-health e0a4d20) / iter300 (import-status 8010ee3) で着地、本 iter301 で 4 件目の着地。次 iter302 = 52%5=2 → ai-automation。
- ✅ [iter301] **Goal progress bar に達成/順調/やや遅れ/遅延 tier (色 + icon) を bind (basics, 1 commit, a5b91d7)**: iter299 並走 `e0a4d20` で `goal-health.ts` の 5 tier 分類 substrate (achieved/on-track/at-risk/behind/idle) を整備済、iter298 並走 `95dd87c` で Sprint 軸の UI bind が完了済だったが、Goal 軸の UI bind が pending だった。`goals-panel.tsx` GoalCard で progress 取得済 (open 時) なら `computeGoalHealth({pct, startDate, endDate})` で tier を計算 → bar 色 (emerald/blue/amber/red/primary)、"全体進捗" 左に icon (CheckCircle/TrendingUp/Hourglass/AlertTriangle)、完了率テキスト behind=destructive 赤 + achieved=emerald 緑、aria-label / aria-valuetext / data-tier に health label。substrate は無変更 (29/29 PASS 維持)、UI bind のみで動作仕様の追加なし。typecheck/lint 緑 (warning baseline 1)。これで FEEDBACK_QUEUE「Goal / Sprint progress bar 残/超過/達成を色 + icon で意味付け」候補は Sprint (iter298 並走) + Goal (本 iter) の 2 軸で消化完了。次 iter302 = 52%5=2 → ai-automation。
- ✅ [iter300 並走 (descendants-helper)] **subtasks-panel の totalDescendants 計算を pure helper に抽出 (refactor, 1 commit, 364d3e7)**: 並走 cloud session 2 件 (`07ffdd2` formatNonZeroCounts 共通化 / `8010ee3` ImportStatusBadge 集約) が iter300 として先取 push 済だったが、本 session は別軸の refactor (subtasks-panel.tsx 内 inline で書かれていた `totalDescendants` 計算) を独立 commit、rebase で `364d3e7` として origin/main に乗った。`subtasks-panel-helpers.ts` に `getDescendants(parent, allItems)` (返り値 = 子孫 items) と薄ラッパ `countDescendants` を追加。filter 4 条件 (`!deletedAt` / `parentPath !== ''` / `parentPath !== parent.parentPath` / `parentPath === fullPath OR startsWith(`${fullPath}.`)`) は inline の元と完全一致、動作変更ゼロ。substrate 化の効用: SubtasksPanel h3 件数表示 / AI prompt embed (「Item X は再帰子孫を 14 件持つ」) / dashboard widget / 検索 facet が同 filter で揃う、`.` boundary 判定が 1 箇所に集約。helper test +7 件 (1 段子 / 2 段再帰 / nested parent / deletedAt 除外 / sibling 除外 / 0 件 / boundary 別 tree 誤拾い禁止 = 19→28 件 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter301 = 51%5=1 → basics。
- ✅ [iter300 並走 (import-status)] **ImportStatusBadge の status → label/class マップを pure helper に抽出 (refactor, 1 commit, 8010ee3)**: 並走 cloud session が iter300 として `07ffdd2` (`formatNonZeroCounts` 共通化) を先取 push 済だったが、本 session は別観点の refactor (integrations-panel.tsx 551 行末尾の `ImportStatusBadge` で 4-way ternary を pure helper に集約) を独立 commit、rebase で `8010ee3` として origin/main に乗った。iter275 (`workflows-panel#RunStatusBadge` → `run-status.ts`) と同パターン。新 module `src/features/external-source/import-status.ts`: `importStatusLabel(status)` (待機 / 実行中 / 成功 / 失敗、未知は raw fallback) + `importStatusBadgeClass(status)` (green/red/amber/muted、未知は muted fallback)。run-status を再利用しなかった理由: `external_imports.status` と `workflow_runs.status` は別ドメインの enum で同 vocabulary は今だけ、integrations component から workflow domain helper を import すると不要な domain 結合 (run-status は cancelled/pending/skipped を持つが import-status は 4 status のみ、型上もクリアに区別)。動作変更ゼロ、`integrations-panel.tsx` は 551 → 539 行 (-12)。test +8 件 (label 2 / badgeClass 6 = 8/8 PASS)。typecheck/lint 緑 (warning baseline 1)。並走 race 履歴: iter290 P0 / iter292 / iter296 で 3 件 race lost、iter294 で urgency-tier (4d11bd1) と iter299 で goal-health (e0a4d20) が無事着地。本 commit は parallel が手薄な integrations 領域で race リスクを最小化。次 iter301 = 51%5=1 → basics。
- ✅ [iter300] **0 件 bucket 省略の format counts 関数を共通化 (refactor, 1 commit, 07ffdd2)**: iter287 / iter289 / iter292 並走 / iter294 並走 / iter294 並走 (urgency-tier) で 4 callsite (due-proximity / priority / status-visual / urgency) に同 shape の format 関数 (`order を回して 0 件 bucket を省略 / 全 0 → "0 件"`) が重複していた。新 module `src/lib/format-counts.ts` に `formatNonZeroCounts<K extends string | number>(counts, order, labels, emptySentinel?)` を抽出。K は string と数値 literal type (PriorityKey の `1|2|3|4`) の両方を許容。4 callsite を 5-6 行 inline → 1 行 delegation に置換 (-24 行 / +6 行)。動作変更ゼロ: 既存 tests (priority 14 / status 19 / due-proximity 26 / urgency 54) + 新 format-counts test 7 = 120/120 PASS。件数 summary の format ロジック変更 (例: "/" を "、" に変える、order に閾値を追加する) が 1 ファイルに集約された。typecheck/lint 緑 (warning baseline 1)。次 iter301 = 51%5=1 → basics。
- ✅ [iter299 (stale-items)] **stale items 抽出 pure helper (selectStaleItems + format) (ai-automation, 1 commit, 97c1a3c)**: iter294 (must-risk) と相補な「期限関係なく 1 週間放置の active item」を 1 関数で取り出せる AI substrate。must-risk が「期限近い MUST」(時間で焦る) なのに対し本 helper は「忘れている item」(放置で落ちる) を補完、AI prompt に両方含めると MVP 核「絶対落とさない」が二軸で守られる。新 module `src/features/item/stale-items.ts`: `selectStaleItems(items, opts, today) → StaleItemEntry<T>[]` で「doneAt/archivedAt 除外 + status not in excludeStatuses (default ['done','cancelled']) + updatedAt < today - thresholdDays (default 7 日) + 未来 updatedAt 除外 + 不正値 fail-soft」を抽出、staleDays desc + 同日 stable で並ぶ。`formatStaleItemsSummary(entries) → "stale 3: B (20 日前) / C (15 日前) / A (10 日前)"`。0 件 sentinel `'stale 0 (該当なし)'`、title 欠落は `(無題)` で fallback。test +15 件 (selectStaleItems 11 / formatStaleItemsSummary 4)。typecheck/lint 緑 (warning baseline 1)。次 iter300 = 50%5=0 → refactor。
- ✅ [iter299 並走 (goal-health)] **Goal (OKR) の健全性を tier 化する pure helper + group/count/format (ai-automation, 1 commit, e0a4d20)**: 並走 cloud session が iter299 として `97c1a3c` (`selectStaleItems` stale items 抽出 helper) を先取 push 済だったが、本 session は別軸の AI substrate (Goal 健全性 tier 化) を独立 commit、rebase で `e0a4d20` として origin/main に乗った (iter# は重複だが内容は完全別軸)。FEEDBACK_QUEUE「Goal / Sprint progress bar: 残/超過/達成を色 + icon で意味付け」候補のうち、Sprint は iter298 並走 `95dd87c` で UI bind 済、本 commit は **Goal 軸の AI substrate** を先行整備 (UI bind は別 iter)。新 module `src/features/okr/goal-health.ts`: `GoalHealthTier` union (achieved/on-track/at-risk/behind/idle)、`computeGoalHealth({pct, startDate, endDate, today?})` で 5 tier 判定、`groupGoalsByHealth` / `countGoalsByHealth` / `formatGoalHealthCounts` の trio。tier 境界は Sprint burndown と同 -10% buffer (on-track) + -30% (at-risk)、pct >= 1.0 は期間問わず achieved、開始前/終了後未達は idle。期間計算は `daysBetween` / `todayISO` を再利用、新規 heuristic 無し。pct=NaN/負値は 0 クランプで fail-soft、start=end の 1 日 Goal でも 0 除算回避。AI 朝 brief / dashboard widget が `達成 1 / 順調 2 / 遅延 1 / 期間外 0` を 1 関数で出せる substrate。test +29 件 (achieved 3 / idle 3 / on-track 4 / at-risk 3 / behind 3 / fail-soft 3 / label 1 / group 3 / count 2 / format 4)。typecheck/lint 緑 (warning baseline 1)。本 session の race lost 全体: iter290 P0 subtask gap d → iter292 urgency-tier (iter294 で着地 4d11bd1) → iter296 notification-bell (parallel `d304ba7` で同等内容で先取、abandon) → iter299 goal-health で OKR 軸 (parallel が手薄な domain) に切り替えて着地。次 iter300 = 50%5=0 → refactor。
- ✅ [iter298 並走 (sprint-tone)] **Sprint progress bar に達成/順調/遅延 tone (色 + icon) を意味付け (basics, 1 commit, 95dd87c)**: 並走 cloud session 2 件 (`601fb18` workflows EmptyState / `d6d6595` activity-log icon) が iter298 として先取 push 済だったが、本 session は FEEDBACK_QUEUE「Goal / Sprint progress bar: 残/超過/達成を色 + icon で意味付け (緑チェック / 黄黄信号 / 赤遅延)」の Sprint 部分を別軸で独立 commit、rebase で `95dd87c` として origin/main に乗った。`burndown.ts` に pure helper `sprintProgressTone(burndown, status) → 'done' | 'onTrack' | 'behind' | 'idle'` を追加 — done (completionPct=100、status 不問で永続) / onTrack (active+isOnTrack) / behind (active+!isOnTrack、10% 余裕越え) / idle (上記以外)。`sprints-panel.tsx` SprintCard で bar 色を tone 別 (emerald/blue/amber/primary)、"完了率" 左に icon (CheckCircle/TrendingUp/AlertTriangle、idle 無印)、完了率テキストは behind=destructive 赤 + done=emerald 緑、aria-label / aria-valuetext / data-tone に tone label を埋め込み (SR 即時 announce)。burndown.test.ts +5 件 (tone 判定: done 永続 / onTrack / behind / idle / 境界 99 = 16/16 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter299 = 49%5=4 → ai-automation。
- ✅ [iter298 並走 (activity-log)] **ActivityLog action 種別を icon + 配色 chip で graphical 化 (basics, 1 commit, d6d6595)**: 並走 cloud session が `601fb18` で WorkflowRunHistory の空状態に実行経路 + node 詳細案内を iter298 として先取 push 済だったが、本 session は別観点の basics (FEEDBACK_QUEUE「Activity log: 操作種別 を icon で」) を独立 commit、rebase で `d6d6595` として origin/main に乗った。新 module `src/features/audit/action-visual.ts`: `getAuditActionVisual(action)` で 14 既知 action を 6 カテゴリに分類 — create=Plus+emerald / update,set_assignees,set_tags=Pencil+blue / status_change,bulk_status_change,move,reorder,archive,unarchive=ArrowRightLeft+amber / complete=CheckCircle2+emerald / uncomplete=RotateCcw+slate / delete,bulk_delete=Trash2+red、unknown=Activity+zinc fallback。status-visual / type-visual と同じ構造で配色は 5-7 色に統一。`activity-log.tsx`: 旧 ACTION_LABEL Record (12 entry) を action-visual に置換、行頭の label 左に 5x5 rounded chip (bg + ring + Lucide icon) を追加、未知 action は raw 文字列 fallback (旧挙動維持)、`role="img"` + `aria-label="操作種別: 作成/更新/削除/..."` で SR が即時 type 識別。action-visual.test.ts +12 件 (6 カテゴリ + unknown + null/undefined/'' + 14 件登録 + non-zinc + 5-7 色制約)。動作変更ゼロ (icon 追加のみ、JSON 折りたたみ等の既存挙動は無変更)、typecheck/lint 緑 (warning baseline 1)。次 iter299 = 49%5=4 → ai-automation。
- ✅ [iter297 並走 (dep-readiness)] **依存関係から着手可能性を summarize する pure helper (ai-automation, 1 commit, a3d6340)**: 並走 cloud session が `9248459` (`summarizeItemEstimates`、見積合計集計) を iter297 として先取 push 済だったが、本 session は別観点の AI substrate (item-dependency 起点の readiness) を独立 commit、rebase で `a3d6340` として origin/main に乗った (iter# は重複だが内容は別軸)。新 module `src/features/item-dependency/readiness.ts`: `summarizeDependencyReadiness(group)` で `{isBlocked, isReady, openBlockedByCount, blockedByCount, openBlockingCount, blockingCount, relatedCount}` を返し、`formatDependencyReadiness(r)` で `"前提 1 件未完了 (3 件中) / 後続 1 件未完了 (2 件中) / 関連 1 件"` 形式の AI prompt 行 / UI badge 用 string。blocking は全 open / 全 done / 一部 open の 3 mode で表示切替 (`後続 N 件` / `後続 N 件 (全完了)` / `後続 K 件未完了 (N 件中)`)。0 件は `"依存なし"` sentinel。pure (DB / hook 不参照、ItemDependencyGroup 1 つで決まる)。test +14 件 (summarize 7 / format 7 = 14/14 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter298 = 48%5=3 → basics。
- ✅ [iter296 並走 (notification-bell)] **notification-bell 通知 type 別に icon + 配色 (basics, 1 commit, d304ba7)**: 並走 cloud session が `efeb5e0` で items-board の filter chip に「N / 全 M 件」表示を iter296 として先取 push 済だったが、本 session は別観点の basics (FEEDBACK_QUEUE「Notification bell: 通知 type 別の色/icon」) を独立 commit、rebase で `d304ba7` として origin/main に乗った。新 module `src/features/notification/type-visual.ts`: `getNotificationTypeVisual(type)` で heartbeat=AlarmClock+red / mention=AtSign+blue / invite=UserPlus+emerald / sync-failure=AlertCircle+amber + unknown=Bell+zinc fallback。status-visual.ts と同じ「icon variant key + 配色 + label」構造、配色は app 共通 5-7 色に揃え。`notification-bell.tsx`: row 先頭の単色 primary dot を type-visual based の icon chip (5x5 rounded ring) に置換、未読 dot は本文左の小さい primary dot として残し、aria-label に type label を連結 (`未読 期限近接 通知: ...`) で SR の popover scan が即時 type 識別。type-visual.test.ts +8 件 (4 既知 type + unknown fallback + null/undefined/空文字 + non-zinc 一括検証)。動作変更ゼロ (icon が増えるだけで click handler / Realtime 配線は無変更)、typecheck/lint 緑 (warning baseline 1)。次 iter297 = 47%5=2 → ai-automation。
- ✅ [iter295 並走 (urgency-due-proximity)] **urgency の dueProximity ロジックを due-proximity.ts に集約 (refactor, 1 commit, 3b24a6c)**: 並走 cloud session が `ce986d6` で items-board の filter ロジックを `board-filter.ts` に抽出する別軸の iter295 refactor を先取 push 済だったが、本 session は別観点の refactor (urgency.ts の閾値重複削除) を独立 commit、rebase で `3b24a6c` として origin/main に乗った。iter287 で getDueProximity を抽出済だったが urgency.ts は独自に diffDays 計算 + parseIsoDate 重複定義のままだったので、kind→bonus / kind→label の Record lookup に置換、private 関数 2 つ削除 (-23 行) + Record 2 つ追加 (+18 行)。動作変更ゼロ: urgency.test.ts 37/37 + due-proximity.test.ts 26/26 PASS。閾値変更 (例: thisWeek を 5 日に短縮等) が due-proximity.ts 1 箇所に集約された (今までは urgency.ts と build-groups.ts と dashboard SQL の 3 系統で別々)。typecheck/lint 緑 (warning baseline 1)。次 iter296 = 46%5=1 → basics。
- ✅ [iter294 並走 (urgency-tier)] **urgency score を tier 分類する pure helper + group/count/format (ai-automation, 1 commit, 4d11bd1)**: 本 session は元々 iter292 ai-automation として用意していたが、並走 session 2 件 (`b8f209d` explainUrgency / `66a40a8` priority groups) が先に main に着地したため iter# を再 tag、さらに rebase 中に iter294 並走 3 件 (`70a9bf7` describeItemForPrompt / `698c58b` status groups / `87f735a` must-risk) も着地、結果 iter294 ai-automation の 4 軸目として `4d11bd1` で main に乗った (iter# は 4 重複だが内容は完全別軸 — 重複ではなく相補)。iter287 (due-proximity) / iter289 (group/count/format) / iter292 並走 (priority) と対称な **urgency 軸** 版を `urgency.ts` に追加: `urgencyTier(score)` (NaN/Infinity/0/負値 fail-soft で 'none')、`urgencyTierLabel(tier)` (緊急/高/中/低/対象外)、`groupItemsByUrgencyTier` / `countItemsByUrgencyTier` / `formatUrgencyTierCounts`。境界 (100/50/20/0) は computeUrgency の score (priority weight + proximity bonus + MUST) を踏まえた heuristic。priority 軸 (iter292-B) との違い: priority のみだと "期限切れの p3" が p3 に残るが、urgency 軸 (本 iter) は proximity (+50) や MUST (+30) を反映するので **緊急** に格上げされる。AI 朝 brief / pm-agent prompt が `緊急 2 / 高 5 / 中 12 / 低 30 / 対象外 8` を 1 関数で出せる substrate。done/archive 済は固定 0 → 'none' bucket に集約。test +17 件 (urgencyTier 6 / Label 1 / group 4 / count 2 / format 4 = 54/54 PASS)。typecheck/lint 緑 (warning baseline 1)。本 session の race lost 補足: 元々 iter290 P0 で subtask gap (d) を実装したが `c16d15e` (cloud loop) が先取、iter292 で urgency-tier を実装したが iter292 並走 2 件が先行で iter294 に再 tag、iter294 中も並走 3 件が先行 — 最終的に 4d11bd1 として着地。次 iter295 = 45%5=0 → refactor。
- ✅ [iter294 並走 (must-risk)] **MUST at-risk pure helper (selectAtRiskMust + format) (ai-automation, 1 commit, 87f735a)**: 別 cloud session 2 件 (`70a9bf7` describeItemForPrompt / `698c58b` status group helper) が iter294 として先取 push 済だったが、本 session は MVP 核「MUST 絶対落とさない設計」観点で別軸の AI substrate を独立 commit、rebase で `87f735a` として origin/main に乗った (iter# は 3 重複だが内容は完全別軸)。新 module `src/features/item/must-risk.ts`: `selectAtRiskMust(items, opts, today) → MustRiskEntry<T>[]` で「isMust=true & 未 done & 未 archive & dueDate が overdue/today/tomorrow/thisWeek (withinDays 上限制御、default 6、0 で overdue+today のみ)」を抽出、severity 降順 (overdue→today→tomorrow→thisWeek) + 同 severity 内は computeUrgency 降順 + stable。`formatAtRiskMustSummary(entries) → "MUST at-risk 3: [期限切れ] A (p1) / [今日] B (p2) / [明日] C (p3)"` で AI prompt 用 1 行。0 件 sentinel `'MUST at-risk 0 (該当なし)'`、title 欠落は `(無題)` で fallback。iter279 (computeUrgency) / iter287 (getDueProximity) を内部合成、新規 heuristic は導入せず予測可能。test +14 件 (selectAtRiskMust 10 / formatAtRiskMustSummary 4)。typecheck/lint 緑 (warning baseline 1)。次 iter295 = 45%5=0 → refactor。
- ✅ [iter294 並走 (status)] **status 別の group/count/format helper (ai-automation, 1 commit, 698c58b)**: 別 cloud session が iter294 として `70a9bf7` (`describeItemForPrompt`、Item を AI prompt 用 1 行に圧縮) を先取 push 済だったが、本 session は別観点の AI substrate (status 分布) を独立 commit、rebase で `698c58b` として origin/main に乗った (iter# は重複だが内容は別軸の ai-automation)。iter287 (due-proximity) / iter289 (group/count/format) / iter292 (priority 別) と対称な status 版を `status-visual.ts` に追加: `groupItemsByStatus` / `countItemsByStatus` / `formatStatusCounts`。既知 5 status (todo/in_progress/done/cancelled/blocked) + unknown bucket = 6 種、未知 / null / undefined / 空文字 / カスタム status は unknown bucket に集約 (`getStatusVisual` のフォールバックと一貫)。formatStatusCounts は実用視認順 (`TODO 2 / 進行中 1 / blocked 1 / 完了 3 / キャンセル 1 / 不明 1`) で件数 0 を省略。test +12 件 (group 3 / count 3 / format 4 = 19/19 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter295 = 45%5=0 → refactor。
- ✅ [iter292 並走 (priority)] **priority 別の group/count/format helper (ai-automation, 1 commit, 66a40a8)**: 別 cloud session が iter292 として `b8f209d` (urgency 内訳 explainUrgency) を先取 push 済だったが、本 session は別観点の AI substrate (priority 分布) を独立 commit、rebase で `66a40a8` として origin/main に乗った (iter# は重複だが内容は別軸の ai-automation)。iter287 (due-proximity) / iter289 (group/count/format) と対称な priority 版を `priority.ts` に追加: `groupItemsByPriority` / `countItemsByPriority` / `formatPriorityCounts`。priority null/undefined/範囲外は p4 (低) バケットに集約 (priorityClass / priorityLabel のフォールバックと一貫)。formatPriorityCounts は件数 0 を省略、`最優先 2 / 高 1 / 中 3 / 低 1` を priority 1→2→3→4 順で返す。test +12 件 (group 3 / count 3 / format 4 = 14/14 PASS)。typecheck/lint 緑 (warning baseline 1)。次 iter294 = 44%5=4 → ai-automation (iter293 は並走 a000f6f keyboard で消化済)。
- ✅ [iter291 並走 → iter293 候補に再配置] **MUST badge inline span 7 callsite を `MustBadge` component に統一 (basics, 1 commit, ed65468)**: 並走 cloud session が 45aec1f で archived-items-panel / EmptyState 統一を iter291 として先取 push 済だったが、本 session の cloud loop は別観点の basics 案 (graphical 波及シリーズ MUST 部分) を独立 commit、rebase が無衝突で通り `ed65468` として origin/main に乗った (iter# は重複だが内容は別軸の basics)。kanban-view / item-edit-dialog / command-palette / subtasks-panel / quick-add / decompose-proposals-panel / template-items-editor の 7 callsite に分散していた `<span class="bg-red-100">MUST</span>` の inline chip を `MustBadge` (AlertTriangle icon + bg-red-100 + aria-label="MUST タスク") に置換。aria-label が "MUST タスク" / "MUST 提案" / "MUST item" の 3 種混在していたのを 1 種に正規化、bg 色も `bg-red-500/10` 1 件を `bg-red-100` に揃え。Gantt bar / budget / time-entry / integrations の red-100 は別 semantic / 別レイアウトなので skip。動作変更ゼロ、test 不要 (MustBadge 自体は既存)、typecheck/lint 緑 (warning baseline 1)、architecture test 5/5 PASS。-53 / +15 行。次 iter は iter293 = 43%5=3 → basics (iter292 は並走で消化済 b8f209d)。
- ✅ [iter292] **urgency に explainUrgency / formatUrgencyExplanation を追加 (ai-automation, 1 commit)**: iter279 (computeUrgency) / iter284 (selectTopUrgentItems) と並ぶ AI substrate 続編。今までは合計値しか返さず「なぜ urgent なのか」を caller 側で再計算する必要があった → `explainUrgency(item, today) → { score, factors: { label, bonus }[] }` で内訳を返す。`formatUrgencyExplanation(e)` で `"p1 priority +100 / 期限当日 +35 / MUST +30 = 165"` 形式の AI prompt 行 / UI tooltip 用文字列。done/archive は専用 sentinel "urgency 0 (完了済 / archive)"。閾値 (期限切れ +50 / 期限当日 +35 / 期限明日 +20 / 期限今週内 +10) は `dueProximityBonus` の bonus 値から逆引きしてラベル化。urgency.test.ts 24→37 件 (+13: explainUrgency 9 + formatUrgencyExplanation 3 + score=computeUrgency sanity 1)。typecheck/lint 緑 (warning baseline 1)。次 iter293 = 43%5=3 → basics。
- ✅ [iter291] **archived-items-panel を EmptyState 統一 + status 列 StatusBadge 化 (basics, 1 commit)**: iter273 (Today) / iter276 (Inbox) / iter281 (Workflows) / iter283 (Sprints) / iter286 (Goals) / iter288 (items-board) で進めた EmptyState onboarding パターンを Archive panel にも横展開、これで 7 view 目。EmptyState の説明を「削除と違い、いつでも `復元` button で戻せます。完了済タスクの履歴 / 過去の参考資料を `捨てずに片付ける` 場所として活用してください」に更新し、削除との違いを示す。加えて status 列を 生 enum 文字列 → `<StatusBadge status={...} />` に置換 (iter261 graphical 波及シリーズ続編)。typecheck/lint 緑 (warning baseline 1)。次 iter292 = 42%5=2 → ai-automation。
- ✅ [iter290 P0] **subtask に indent/outdent button + Alt+←/→ keyboard (queue: subtask gap d/4)**: queue P0 最優先消化 (track 判定上書き)。a/b/c (完了 checkbox / 再帰 tree / DnD) は iter256-258 で実装済、最後の (d) インデント/アウトデント が並走 session 上書きで pending 残っていた件を cloud loop で消化。実装: subtasks-panel-helpers.ts に 3 pure helper 追加 (`compareSiblings` position+id tie-break, `findPrevSibling` 同 parentPath で直前を返す, `findGrandparentId` 'root' sentinel で root 行きを表現)、`SubtaskTreeNode` に `onIndent`/`onOutdent`/`movePending` props 追加して `ArrowLeftFromLine`/`ArrowRightFromLine` button + Alt+←/→ keyboard、`SubtasksPanel` に `useMoveItem` + handler 配線、DnD cross-parent toast を「indent/outdent ボタン (Alt+←/→) で操作してください」に更新。disabled 時は理由 (root / 深さ超過 / 前 sibling 無し) を aria-label で提示、helpers test +15 件 (compareSiblings 3 / findPrevSibling 6 / findGrandparentId 6)。typecheck/lint 緑 (warning baseline 1)。これで subtask 機能 gap a-d 4/4 全消化完了。
- ✅ [iter289 race-lost] **due-proximity の group/count/format helper (parallel agent が先取 push)**: 同時 fire 別 session が同じ iter289 を実装して 454d1a8 として origin/main を取得、本 session の f920f99 は push race で discarded。コードは同等内容で main に commit 済み。次 iter290 を P0 queue 消化に切替。
- ✅ [iter288] **items-board EmptyState を onboarding 用 QuickAdd 例 + view 切替案内に置換 (basics, 1 commit)**: iter273 (Today) / iter276 (Inbox) / iter281 (Workflows) / iter283 (Sprints) / iter286 (Goals) で進めた EmptyState onboarding パターンを workspace の "全 Item 0 件" 時にも適用。これで 6 view 目で揃った。これは workspace 初訪問の user が一番最初に出会う EmptyState なので Item の正体・QuickAdd 自然言語の文法 (`明日 9時 資料準備 #design` / `+3d レビュー @taro`)・後でできる view 切替 (Kanban/Backlog/Gantt/Today) を 1 画面で示す onboarding 化に置き換え。typecheck/lint 緑 (warning baseline 1)。次 iter289 = 39%5=4 → ai-automation。
- ✅ [iter287] **Item の dueDate を期限近接バケットに分類する pure helper (ai-automation, 1 commit)**: iter279 (urgency 計算) / iter282 (estimate suggestion) / iter284 (top urgent selector) と並ぶ AI substrate シリーズ。今までは urgency.ts#dueProximityBonus (private) / today/build-groups.ts (inline) / dashboard service (SQL) の 3 系統で「期限切れ / 今日 / 明日 / 今週内」が別々に表現されていたので、client 側で再表現する場合 (AI prompt embed / item チップ / dashboard widget) は毎回書き直す必要があった。新 module `src/features/item/due-proximity.ts`: `getDueProximity(dueDate, today?)` → `{kind: 'overdue'|'today'|'tomorrow'|'thisWeek'|'later'|'noDate', diffDays, label}`。閾値は urgency.ts と同じ (overdue<0, today=0, tomorrow=1, thisWeek=2..6, later=7+)。今後 urgency.ts / build-groups.ts は同 substrate に集約可能だが scope 外で挙動変更リスクなしを維持。test +18 件 (kind 8 / fail-soft 7 / Date today / 省略 / label 6)。typecheck/lint 緑 (warning baseline 1)。次 iter288 = 38%5=3 → basics。
- ✅ [iter286] **Goals (OKR) EmptyState を用途例 + 期間目安に置換 (basics, 1 commit)**: iter273 (Today) / iter276 (Inbox) / iter281 (Workflows) / iter283 (Sprints) で進めた「EmptyState を schema 用語ベース → ユースケース提示」パターンを Goals (OKR) にも横展開。これで 5 view 目で同パターンが揃った (Today/Inbox/Workflows/Sprints/Goals)。旧「上のフォームから作成できます」 → 「中期 (典型は `3 ヶ月`) の Objective と Key Result を 1〜5 件紐付ける箱です。例: `2026 Q2 システム速度を体感半分に` / KR は `P95 レイテンシ 200ms 以下`。KR は items 連動 (linked items の done 比) でも手動 (current/target) でも測れます」。OKR を初見の user が「3 ヶ月で何を達成、何で測る、KR の 2 mode」を即理解できる。typecheck/lint 緑 (warning baseline 1)。次 iter287 = 37%5=2 → ai-automation。残候補 EmptyState: decompose-proposals (Backlog は親から filter 済 items を受ける別パターンで除外)。
- ✅ [iter285] **SprintCard の Burndown 計算を pure helper に抽出 (refactor, 1 commit)**: iter265 (date helpers) / iter275 (run-status) と同パターン。SprintCard 内 inline で書かれていた `totalDays` / `elapsedDays` / `remainingDays` / `elapsedPct` / `completionPct` / `isOnTrack` の 8 行式を `src/features/sprint/burndown.ts` の pure 関数 `computeSprintBurndown({startDate, endDate, total, done, today?})` に集約。AI 朝 brief / dashboard widget が同じ式で「遅れ気味」を判定できる substrate。`isOnTrack = total=0 ? true : completionPct ≥ elapsedPct - 10` の 10% 余裕も含めて完全一致 (動作変更ゼロ)。`burndown.test.ts` +11 件 (14 日 Sprint on-track / start=end / 開始前 elapsed=0 / 終了後 off-track / total=0 常に on-track / 境界 -10 ちょうど / off-track / 進みすぎ / 負値 floor / done>total / 不正 ISO fail-soft)。total/done が負値 → max(0, ...) で defensive floor、不正 ISO → daysBetween=0 で totalDays=1 にフォールバック。typecheck/lint 緑 (warning baseline 1)。次 iter286 = 36%5=1 → basics。残 large file: workflows-panel.tsx 755 / item/service.ts 715 / item-edit-dialog.tsx 692。
- ✅ [iter284] **urgency comparator に selectTopUrgentItems(items, n) を追加 (ai-automation, 1 commit)**: iter279 で追加した `computeUrgency` / `compareUrgency` の上に、AI 朝 brief / pm-agent / dashboard widget が「次にやるべき N 件」をワンライナーで取り出せる便利 helper を追加。`selectTopUrgentItems(items, n, today)` で urgency=0 (done/archive 済) を除外、高 urgency が先頭、上限 N 件、同 urgency は元配列順を保つ stable sort (index tiebreaker)。n<=0 → 空配列、n が件数を超える → 全件 (短くなるだけ)。urgency.test.ts +6 件 (元 18→24 件: 上位 2 件 / done 除外 / n=0 負 / n 過剰 / stable / 全 done)。typecheck/lint 緑 (warning baseline 1)。次 iter285 = 35%5=0 → refactor。
- ✅ [iter283] **Sprints EmptyState の説明文を用途例 + 期間目安に置き換え (basics, 1 commit)**: iter273 (Today) / iter276 (Inbox) / iter281 (Workflows) で進めた「EmptyState を schema 用語ベース → ユースケース提示」パターンを Sprints にも横展開。Today/Inbox/Workflows/Sprints の 4 view で同じパターン揃った。旧「上のフォームから作成できます」 → 「短期 (典型は `1-2 週間`) のスコープ単位で進捗を測る箱です。ゴール例: `α リリース準備` / `第 3 四半期 OKR`。完了時に Retro Doc / 開始前に Pre-mortem を生成できます」と期間目安 + ゴール 2 例 + Retro/Pre-mortem 機能の存在を <code> chip で伝達。typecheck/lint 緑 (warning baseline 1)。次 iter284 = 34%5=4 → ai-automation。残候補 EmptyState: Backlog / Goals / decompose-proposals。
- ✅ [iter282] **Item 単位で校正済み見積を提案する pure helper (ai-automation, 1 commit)**: iter267 で QuickAdd preview に「→ 39分」を出した `applyBiasCalibration` を Item 単位 API に昇格。新 module `item-estimate-suggestion.ts` で `suggestItemEstimate(description, calibrationFactor)` → `{original, calibrated, delta, summary}`。description から `extractEstimateMinutes` で抽出、factor が null/undefined/NaN/0/負 / 1.0 (delta=0) は `校正不要 (元見積で十分)`、見積あり + 校正後は `見積: 30分 → 39分 (+9分)`。`Number.isFinite` で NaN を先に弾き、applyBiasCalibration が NaN を素通しする潜在 bug 経路の保険。Item 詳細 / AI agent prompt / ItemEditDialog 等が「Item ごとに直近 calibration による校正値」を一貫して提示できる substrate。test +11 件 (見積無し 3 種 / factor null/undefined/1.0/不正 / factor 1.3+30分 → 39分 / factor 0.5+60分 → 30分 / factor 2.0+1時間30分 → 3時間 / 1 行目以外の見積無視)。typecheck/lint 緑 (warning baseline 1)。次 iter283 = 33%5=3 → basics。
- ✅ [iter281] **Workflows EmptyState の説明文を用途例に置き換え (basics, 1 commit)**: iter273 (Today) / iter276 (Inbox) で進めた「EmptyState を schema 用語ベース → ユースケース提示」パターンを Workflows にも横展開。旧「上のフォームから作成してください」は単なる行動指示で Workflow が何できるかが見えなかった → 「用途例: `item 作成 → Slack 通知` / `朝の brief 生成` / `毎日 reminder`。まず空 graph で作成し、後から graph editor で node / edge を追加します」という用途例 3 種を <code> chip で示し「最小ビルド → 後で graph editor」の育てる粒度を伝える。typecheck/lint 緑 (warning baseline 1)。次 iter282 = 32%5=2 → ai-automation。Today / Inbox / Workflows の 3 view で同じ EmptyState 改善パターン揃った (残: Backlog / Gantt / Sprints)。
- ✅ [iter280] **todayISO / isoDaysFromNow を `@/lib/date/iso` に集約 (refactor, 1 commit)**: 3 callsite で重複していた inline 定義を `src/lib/date/iso.ts` に集約 (sprint-domain でない caller が `@/features/sprint/sprint-date-helpers` から import するのを避ける)。抽出元: active-timer-panel / today-view / goals-panel。UTC ベースの inline 定義 (dashboard-view / create-time-entry-form) は behavior 変更リスクがあるので scope 外として維持。新 module は `now` 引数化で決定論テスト可能 (sprint-date-helpers iter265b と同じ方針)。iso.test.ts +7 件 (todayISO 固定 Date / 0 padding / 省略時 形式 + isoDaysFromNow +0/+7/-1 / 月跨ぎ / 年跨ぎ / 省略時)。`@/features/sprint/sprint-date-helpers` 既存 export は無変更。typecheck/lint 緑 (warning baseline 1)。次 iter281 = 31%5=1 → basics。残 large file: item/service.ts 704 / item-edit-dialog.tsx 692 / sprints-panel.tsx 745 / workflows-panel.tsx 743 (次 refactor iter285 で)。
- ✅ [iter82] **Gantt 依存線 toggle** (混雑時の可視化制御): summary banner に "依存線" checkbox 追加、`?showDeps=true` (default) を URL 永続化。OFF にすると `<GanttDependencyArrows>` が dom から消えて bar が見やすくなる。Playwright で URL 反映 + arrows 0 個を確認
- ✅ [iter83] **Today view を 4 group 化** (Todoist Today/Upcoming 風): 既存の "期限超過" / "今日" に加え "明日" / "今週内 (今日+2..+7)" を追加。空 group は自動非表示 (g.items.length>0 ガード)。`shiftISO(iso, days)` で UTC ベース計算)
- ✅ [iter279] **Item の urgency スコアを計算する pure helper (ai-automation, 1 commit)**: AI agent (PM Agent / Researcher) が「次にやるべき item」を提案する時、priority / dueDate / isMust を組み合わせた優先順位を一貫して決められる substrate。今までは `(a.priority ?? 4) - (b.priority ?? 4)` のような component-inline sort で priority だけ見ていた → AI prompt で「上位 5 件」を選ぶには届かなかった。新 module `src/features/item/urgency.ts`: `computeUrgency(item, today)` で priority weight (p1=100/p2=70/p3=40/p4=10) + dueDate proximity (overdue +50 / today +35 / +1 +20 / +2..+6 +10 / +7+ 0) + MUST +30 を合算。done/archive は固定 0。`compareUrgency(today)` を Array.sort comparator として export。不正 ISO は fail-soft で +0。urgency.test.ts +18 件 (priority 5 段階 / proximity 6 buckets / 不正 ISO / MUST / 全部入り 180 / done/archive 0 / sort 順序)。typecheck/lint 緑 (warning baseline 1)。次 iter280 = 30%5=0 → refactor。残 large file: item/service.ts 704 / item-edit-dialog.tsx 692 / sprints-panel.tsx 745 / workflows-panel.tsx 743。
- ✅ [iter84] iter83 の純粋分類関数を `src/features/today/build-groups.ts` に抽出 + **vitest 単体テスト 4 件追加**: 4 group 分類 / doneAt 除外 / priority 昇順 / scheduledFor も dueDate と同等扱い。+1 test file = 52 files / 445 tests PASS。Component 再 export で挙動変化なし
- ✅ [iter85] Today group label に **日付 + 曜日** を埋め込み (Todoist Upcoming 風): "今日 (4/27 月)" / "明日 (4/28 火)" / "今週内 (4/29 水 〜 5/4 月)"。"明日って何日?" の認知コストを下げる。unit test +1 件 (label format assertion) = 446 PASS
- ✅ [iter278] **nl-parse に EOD / EOW alias (today / 直近金曜) (basics, 1 commit)**: iter271b (`月初` / `来月` / `来月末`) と iter273 (`in N days` / `N 日後`) の延長で、ビジネス頻出の "by EOD" / "by EOW" を吸収。`EOD` / `今日中` / `今日まで` / `end of day` → today、`EOW` / `今週中` / `今週金曜まで` / `end of week` → 直近の金曜 (`(5 - cur + 7) % 7` で全曜日 OK、Sat/Sun は来週金曜にロールフォワード)。EOD/EOW を date-tokens.ts §4 直前 (`今週末` 系より上) に置き substring 衝突を回避。date-tokens.test.ts +6 件 (EOD JA/EN 4 形式 / EOW Sat → 5/1 / Mon → 5/1 / Fri 当日 / Sun → 5/1 / EOW JA/EN 4 形式)。QuickAdd hint テキストにも `今日中` / `今週中` / `eod・end of day` / `eow・end of week` を追記。typecheck/lint 緑、nl-parse.test.ts 73 件 regression なし (cumulative 154)。次 iter279 = 29%5=4 → ai-automation。
- ✅ [iter86] notification-bell の `formatNotificationBody` / `formatRelativeTime` を `src/features/notification/format.ts` に抽出 + **vitest 単体テスト 10 件追加**: heartbeat (overdue/7d) / mention (40 文字 truncate) / invite / sync-failure 各 type + 相対時刻 5 buckets。53 files / 456 tests PASS
- ✅ [iter87] gantt-view の baseline / slip 集計を `src/features/gantt/project-stats.ts` の `computeProjectStats(rows)` に抽出 + **vitest 単体テスト 4 件追加**: baseline 未設定 / 遅延 / 前倒し&計画通り / 複数 item 合算。54 files / 460 tests PASS。Playwright で "遅延 1 件 / 計 3 日" がそのまま表示されることを確認
- ✅ [iter88] gantt-view の slipText 文字列化を `formatSlipText(slipDays, hasBaseline)` に抽出 + **vitest 単体テスト 4 件追加** (空 / 遅延 / 前倒し / 計画通り の 4 ケース)。464 tests PASS。Playwright で 5 日遅延 item の bar title に "[遅延 +5日]" が含まれることを確認
- ✅ [iter277] **bias 全 substrate を AI prompt 用 markdown block に整形 (ai-automation, 1 commit)**: iter257 → 264 → 264b → 269b → 274b と積み上げた bias 関連 4 substrate (workspace report / category / trend / confidence) を 1 つの markdown block に統合する pure ヘルパ。AI consumer (pm-agent / morning brief / Slack DM) ごとに微妙に違うフォーマットを書き散らかすのを防ぐ。新 module `bias-prompt.ts` で `BiasPromptInput { workspace, category?, trend?, confidence? }` を受け取り `### 見積精度 (直近)` 見出し + 箇条書き 3-7 行を返す。行は条件付き省略 (trend=inconclusive 無し / categoryNote 同程度なら無し / confidence 不指定なら無し)、行順固定 (見出し → headline/detail/recommendation → confidence → trend → categoryNote)。`formatBiasPromptEmpty()` も用意 (data 不足時の short fallback)。bias-prompt.test.ts +9 件 (workspace のみ 4 行 / + confidence 5 行 / + trend / trend inconclusive 省略 / + category 強 skew → categoryNote 末尾 / category 同程度省略 / 全部入り 7 行 + 行順固定 / H3 見出し / empty fallback 2 行)。typecheck/lint 緑 (warning baseline 1)。次 iter278 = 28%5=3 → basics。
- ✅ [iter276] **Inbox EmptyState の説明文を自然言語例 + 仕分け手順に置き換え (basics, 1 commit)**: iter273 (parallel agent acff8a9) で Today EmptyState を schema 用語ベース → 自然言語入力例ベースに置換した同じ方針を Inbox にも横展開。旧「日付が未設定のタスクがここに溜まります」は schema 寄りで「だからどうしたい?」が伝わりにくかった → 「QuickAdd で日付を入れずに `あとで読む論文` のように書くとここに溜まります。仕分けは Item を開いて `明日` や `+3d` を後付けで」という Inbox に「入る条件」+「抜ける手順」を <code> chip で示す形に。iter273 で `description: ReactNode` 型に広げた async-states.tsx の余地を活用。typecheck/lint 緑 (warning baseline 1)。次 iter277 = 27%5=2 → ai-automation。残候補: bias-confidence (iter274b) を EstimateBiasInsight に配線、bias-trend badge (iter274) を Slack DM で実消費、Workflows / Backlog の EmptyState も自然言語例に揃える。
- ✅ [iter275] **RunStatusBadge の status → label/class マップを pure helper に抽出 (refactor, 1 commit)**: iter260 (workflows-panel.tsx 847→761 行) の続編。末尾 `RunStatusBadge` で status → label / class が三重 ternary 25 行で書かれていたのを `src/features/workflow/run-status.ts` に集約。`RunStatus` union (workflow_runs の queued/running/succeeded/failed/cancelled と workflow_node_runs の pending/running/succeeded/failed/skipped の super-type)、`runStatusLabel(status)` / `runStatusBadgeClass(status)` を pure 関数で。未知 status は label = そのまま / class = `bg-muted` fallback で DB 制約緩和や旧 enum 値を防衛。run-status.test.ts +7 件 (既知 7 status / 未知 fallback / 代表色含包 / muted 群)。workflows-panel.tsx 760→743 行 (-17, -2.2%)。動作変更ゼロ (label/class マッピング完全一致)。typecheck/lint 緑 (warning baseline 1)。次 iter276 = 26%5=1 → basics。残 large file: item/service.ts 704 / item-edit-dialog.tsx 692 / sprints-panel.tsx 745 / workflows-panel.tsx 743 — 次 refactor iter280 で。
- ✅ [iter89] **Gantt に role="grid" + aria-rowcount/aria-rowindex** (WAI-ARIA 1.2 grid pattern): outer に role=grid + aria-rowcount={N+1}、各 row に role=row + aria-rowindex (header=1, data=2..)。SR で「行 N 件中 i 番目」が伝わる。Playwright で role=grid / rowcount=3 / 各行 rowindex 2,3 を直接確認
- ✅ [iter90] **PDCA DailyBars に list semantics**: 旧 `title` 属性 (mouse hover 専用) では SR から完了件数が見えなかった → outer に `role="list" aria-label="日次完了 throughput (N 日分)"`、各日 cell に `role="listitem" aria-label="<date>: 完了 N 件"`。Playwright で 30 listitem + first aria-label 形式を直接確認
- ✅ [iter91] **Goal/Sprint 進捗バーに role="progressbar"** (WAI-ARIA progressbar pattern): aria-valuenow/min/max + aria-valuetext (sprint は "N/M (X%)" + 遅れ気味 marker)、aria-label。Playwright で sprint progressbar の role / valuetext を確認
- ✅ [iter274b] **bias の confidence 指標を pure helper で算出 (ai-automation 並行, 1 commit)**: 並行 cloud agent の iter274 (bias trend badge helper) と独立に進めた substrate 拡張のため iter274b として記録。「3 件で計算した 1.5×」と「30 件で計算した 1.5×」は意思決定の重みが異なるのに既存 brief は両方同じ重みで提示していた問題を解消。新 pure module `bias-confidence.ts` で 5 段階 `BiasConfidenceLevel` を `withEstimateCount` の bucket (0/1-2/3-9/10-19/20+ → none/very-low/low/moderate/high) で分類、calibrationFactor が `[0.5, 3.0]` clamp 端で 1 段下げ、tendency=mixed で 1 段下げ (none からは下げない)。`score: 0..1` (UI 進捗バー用) と `rationale: 'サンプル不足 (5/10) / ばらつき / clamp 範囲外'` の JA 1 行も返し、`formatBiasConfidenceJa(c)` で `自信度: 中 (12 件) — サンプル不足` 整形。bias-confidence.test.ts +15 件 (5 bucket 全境界 / clamp 0.5×・3.0× / mixed / mixed+clamp 2 段下げ / none 床下げ防止 / 高 sample on-track / format 4 種)。並行 agent の iter274 (`formatBiasTrendBadge`) とは substrate 直交 — confidence は「校正値の信頼度」、trend badge は「方向性の最小記号」 — なので将来 brief / dashboard で両方並べられる。typecheck/lint 緑 (warning baseline 1)。次 iter275 = 25%5=0 → refactor。
- ✅ [iter274] **bias trend の最小 badge ラベル helper を追加 (ai-automation, 1 commit)**: iter269b/272 で完成した time-axis bias trend の表示は EstimateBiasInsight 内の 1 行 JA 文面 (`見積精度の変化: 改善 (1.30× → 1.10×)`) だったが、Slack reaction / dashboard sparkline / pm-agent prompt のような「狭いスペースに直感的な記号 + 1 単語で要約したい」需要のために最小 badge ラベルを substrate 側で集約。新 export `formatBiasTrendBadge(direction) → {glyph: '↑/↓/→/·', label: '改善/悪化/安定/データ不足'}`。色 tone は呼び出し側で決定 (緑=改善 / 赤=悪化 / 灰=安定 / muted=不足) — helper はテキストだけ集約することで Slack mrkdwn / dashboard CSS / aria-label すべてに使い回せる。bias-trend.test.ts +4 件 → 19/19 PASS。typecheck/lint 緑 (warning baseline 1)。次 iter275 = 25%5=0 → refactor。
- ✅ [iter273] **basics 並行 2 commits (Today EmptyState + nl-parse `in N days`)**: 並行 cloud agent と本 agent が同時に basics track を進めた結果、2 つの独立 commit が main に入った。
  - **commit A (parallel agent acff8a9)**: Today EmptyState の説明文を「scheduled_for / dueDate を設定すると Today に出てきます」(schema 用語で一般ユーザに不親切) → QuickAdd の自然言語入力例 3 つ (`明日 資料準備` JA / `tomorrow 9am review` EN / `3/15 リリース` M-D US) を `<code>` chip で示す user-facing 例に置換。EmptyState の `description` 型を `string` → `ReactNode` に広げ JSX 受理 (既存 string 受け callee は代入互換で無変更)。iter256/258/266/271b で nl-parse に追加した複数表記を実例で促す効果も。
  - **commit B (本 agent ad966b4)**: nl-parse に自然語の相対日付 shortcut を追加。EN `in N days` / `in N weeks` (Todoist 互換) と JA `N 日後` / `N 週間後` / `N 週後` (空白許容)。`+Nd` (iter230) の natural form として併設、スマホ音声入力 / 雑なメモ書きの認知コスト減。date-tokens.ts §11 (EN) + §12 (JA) 追加、test +5 件 (in 3 days / IN 7 DAYS case-insensitive / in 1 week / 3 日後・2 週間後・1 週後 / 全角空白 `3 日後`)。QuickAdd hint テキストにも `+Nd or in N days` で追記。
  - typecheck/lint 緑。次 iter274 = 24%5=4 → ai-automation。残候補: bias-brief-rich (iter267b) / bias-trend (iter269b) を Slack DM / pm-agent で実消費、AI 朝 brief job 作成、ItemEditDialog にも校正 chip 横展開、Inbox / Backlog の EmptyState も自然言語例に揃える。
- ✅ [iter271b] **nl-parse に 月初 / 来月 / 来月末 + EN alias (som/sonm/eonm) (basics 並行, 1 commit)**: 並行 cloud agent の iter271 (ActiveTimerPanel 校正 chip 横展開) と独立に進めた parser 拡張のため iter271b として記録。iter256 で `月末` / `今週末` / `eom` 等の time horizon shortcut を入れた延長で、月単位の繰返しタスク (請求書 / 月初 1on1 / 来月末支払) に頻出する 3 keyword を吸収: `月初` / `start of month` / `som` (当月 1 日。今日が 1 日でなければ来月 1 日に倒す Todoist 互換)、`来月` / `来月初` / `start of next month` / `sonm` (来月 1 日)、`来月末` / `end of next month` / `eonm` (来月最終日)。substring 衝突対策で `来月末` を `来月` より先、`来月` を `月末` より先に試す順序。date-tokens.test.ts +9 件 (月初 / 月初当日 / SOM EN / 来月 / 来月初 EN / 来月末 / 来月末 EN / 来月末 vs 月末 priority / 12 月跨ぎ)。QuickAdd hint テキストにも追記。typecheck/lint 緑、nl-parse.test.ts 74 件 regression なし。次 iter273 = 23%5=3 → basics (並行 agent 側で iter272 ai-automation を消化済)。
- ✅ [iter272] **EstimateBiasInsight に時系列トレンド (改善/悪化/安定) 行を追加 (ai-automation, 1 commit)**: iter269b で追加された pure helper `splitSamplesByDate` + `computeBiasTrend` + `formatBiasTrendJa` を実 UI に配線。14 日前を境界に「先期間 → 直近」の calibrationFactor 距離変化を判定し「見積精度の変化: 改善 (1.30× → 1.10×)」のような 1 行を category 別 top-skew 行の下に追加。inconclusive は表示しない (学習途中で UI 静か)。同 entriesQ + itemsQ cache 共有で追加 fetch ゼロ。iter257→259→262→264→267→269→272 の ai-automation チェーン 7 段で「集計 → UI → 校正 → brief → input → category → trend」が出揃った。typecheck/lint 緑 (warning baseline 1)。次 iter273 = 23%5=3 → basics。残候補: bias-brief-rich を Slack DM / pm-agent で実消費、ItemEditDialog にも bias trend / 校正 chip 横展開、AI 朝 brief job 作成。
- ✅ [iter271] **計測中も bias 校正値 (→ 39分) を ActiveTimerPanel に併記 (basics, 1 commit)**: iter267 で QuickAdd preview に追加した校正 chip を ActiveTimerPanel にも横展開。タイマー計測中ユーザがリアルタイムに「自分の見積は 30分だけど直近 calibration では 39分の方が現実的」と意識しながら作業できる。見積 chip (bg-cyan-100) の右に「→ 39分」を border-cyan-200 + text-[9px] で併記、aria-label「校正後 39分 (+9分)」、title「直近の見積精度に基づく校正値 (中央値 1.30×)」。delta=0 / factor=null は非表示で UI 静か。useTimeEntries + useItems cache 共有で追加 fetch ゼロ。typecheck/lint 緑 (warning baseline 1)。次 iter272 = 22%5=2 → ai-automation。残候補: bias-trend (iter269b) を実 UI に配線、bias-brief-rich (iter267b) を Slack DM / dashboard widget で実消費、AI 朝 brief job 作成。
- ✅ [iter270] **isInvalidDateRange pure helper + bias-trend.test fixture 修正 (refactor, 並行 2 commits)**: 並行 cloud agent と本 agent が同一 iter270 refactor track を独立に進めた結果、2 つの独立した refactor が main に組み合わさって入った。
  - **commit A (parallel agent ac9e09a)**: iter269b 並行 agent が追加した bias-trend.test.ts の fixture は schema に存在しない `idempotencyKey` を入れていて型不整合のため `as unknown as TimeEntry` 二重 cast に頼っていた (judge.sh の `recent-any-leak(=1)` interrupt がこれを検出)。fixture を TimeEntry schema 一致 (不正 `idempotencyKey` 削除、欠けていた `syncStatus` / `syncAttempts` / `syncError` / `externalRef` / `version` 追加) に揃えて `as unknown as` 撤廃。tests 15/15 PASS、副次効果: `recent-any-leak` 1→0 で interrupt noise 解消。bias-selector.test / format.test / iter261b と同じ「fixture を schema 一致」パターン踏襲。
  - **commit B (本 agent 89671ca)**: 散らばっていた `start <= end` 判定を 1 か所に集約。新 pure module `src/features/item/date-range.ts` に `isInvalidDateRange(start, end)` (片方 null/undefined/空文字 → false、両方ある時のみ `end < start`)。4 callsite (item-edit-dialog / sprints-panel × 2 / goals-panel) を全部 helper 経由に置換。副次効果として sprints-panel 編集 form の `editStart > editEnd` だけで `editStart && editEnd` ガードが落ちていた潜在 bug (空文字での誤判定) を併せて修正。date-range.test.ts +8 件。エラー文面 (`期限...` / `終了日...`) は呼び出し元に残す (用語が違うため)。
  - typecheck/lint 緑 (warning baseline 1)。次 iter271 = 21%5=1 → basics。残 large file: item-edit-dialog.tsx 691 / item/service.ts 704 / sprints-panel.tsx 744 / workflows-panel.tsx 760 (次 refactor iter275 で extraction 対象に)。
- ✅ [iter269b] **見積 bias の時系列トレンド判定 pure helper (ai-automation 並行, 1 commit)**: 並行 cloud agent の iter269 (EstimateBiasInsight category 行配線) と独立に進めた substrate 拡張のため iter269b として記録。同じ 1.5× 過小評価でも「先月 1.5× → 今月 1.2×」(改善) と「先月 1.0× → 今月 1.2×」(悪化) では意味が真逆 — その時間軸方向の比較を担う pure module `bias-trend.ts` を新設。3 関数: `splitSamplesByDate(entries, lookup, splitISO)` (workDate で 2 期間分割、境界 splitISO は recent 側)、`computeBiasTrend(recent, prior)` (factor の `|f-1|` 距離が縮まれば improving、広がれば worsening、±0.05 未満は stable、片方 null は inconclusive)、`formatBiasTrendJa(trend)` (4 種 1 行 JA 文面)。over 側 (factor<1) や 1.0 跨ぎの side 切替も absolute distance で正しく判定。bias-trend.test.ts +15 件 (split 通常/null除外/境界 + trend improving/worsening/stable/over側 2 種/null 2 種/side 跨ぎ + format 4 種)。AI 朝 brief / pm-agent / dashboard widget に「先週は精度が良くなりました」のような前向きフィードバックを substrate 側で支える。typecheck/lint 緑 (warning baseline 1)。次 iter270 = 20%5=0 → refactor。残 large file: item-edit-dialog.tsx 689 / item/service.ts 704。
- ✅ [iter269] **EstimateBiasInsight に category 別 top-skew 行を追加 (ai-automation, 1 commit)**: iter264b で追加された pure helper `computeBiasByCategory` + `formatTopSkewedCategoryJa` を実 UI に配線。summary line の直下に「カテゴリ別: 開発: 見積を 1.40× 過小評価しがち (12 件)」形式の 1 行を追加 (歪み category が 1 件もなければ row ごと非表示で UI 静か)。workspace 全体の 1 本 calibrationFactor では塗りつぶされる「dev だけ過小、meeting は問題なし」みたいな category 偏りを可視化。既存 entriesQ + itemsQ cache を再利用するので追加 fetch ゼロ、overall calibration セクションは保持し「総括 → 詳細」の 2 段認知フロー。typecheck/lint 緑 (warning baseline 1)、architecture.test 5/5 PASS。次 iter270 = 20%5=0 → refactor。残 large file: item-edit-dialog.tsx 689 / item/service.ts 704 — どちらも 700 行付近、次 refactor で extraction 候補。
- ✅ [iter268] **ItemEditDialog ベースライン button の hover title を friendly 表示に (basics, 1 commit)**: iter261 (Today/Backlog) → 263 (Kanban) で進めた formatFriendlyDate 横展開の続編。set-baseline button の title 属性 (hover tooltip) を ISO `2026-04-30 → 2026-05-15` から friendly `4/30 (木) → 5/15 (金)` に変更。aria-label (SR) は ISO のまま据え置き — sighted user の認知コストは title で減らし、SR の precision は aria-label で維持する 2 チャンネル設計。clear button の aria-label も ISO 据え置き (WCAG: SR は冗長でも厳密が親切)。typecheck/lint 緑 (warning baseline 1)。次 iter269 = 19%5=4 → ai-automation。候補: ItemEditDialog にも見積 inline 校正 chip を展開、bias-brief を Slack DM / dashboard widget で実消費、見積 categories の bias 別表示 (iter264b で `bias-by-category.ts` 既設)。
- ✅ [iter267b] **bias-brief に category 別 top-skew 行を合成する rich helper (ai-automation 並行, 1 commit)**: 並行 cloud agent の iter267 (QuickAdd 見積入力 inline 校正) と独立に進めた substrate 側拡張のため iter267b として記録。iter264 (3 行 brief) + iter264b (category 1 行 top-skew) を合流させ、AI 朝 brief / pm-agent / Slack DM が「workspace 全体傾向 + 最も歪みが強い category」を 4 行で受け取れる pure helper を新設。新 module `bias-brief-rich.ts` に `BiasBriefRich extends BiasBrief` (+ 任意 `categoryNote`) と `formatBiasBriefRichJa(workspaceReport, categoryReport)` / `formatBiasBriefRichText(...)` を追加。categoryNote 付与条件: (1) ws=on-track/mixed/unknown + cat 強 skew → 採用 (新情報)、(2) ws=under/over → 同方向のみ採用 (逆方向は混乱)、(3) `|catFactor-1| - |wsFactor-1| < 0.1` (workspace と同程度) は重複情報なので省略。test +10 件 (skew 無し / on-track + 強 skew / under 同方向極端 / under 同程度省略 / 逆方向省略 / over + 強 skew / mixed + 強 skew / 既存 3 行一致 / text 3 行 4 行両モード)。typecheck/lint 緑 (warning baseline 1)、bias-brief.test + bias-by-category.test も regression なし計 19 PASS。次 iter269 = 19%5=4 → ai-automation (iter268 basics は並行 agent 側で消化済)。
- ✅ [iter267] **見積入力時に直近 bias の校正値を inline で表示 (ai-automation, 1 commit)**: iter257 (集計) → 259 (insight UI) → 262 (校正推奨) → 264 (brief) → 267 (input 側ループバック) で ai-automation チェーンの「データを使って次の入力を補助する」フィードバックループが閉じた。新 hook `useEstimateCalibration(workspaceId)` (既存 useTimeEntries + useItems cache を再利用、新 server action 無し) で `{calibrationFactor, withEstimateCount, isLoading}` を返す薄い API を提供し、QuickAdd preview chip に「→ 39分」校正値 chip を見積 chip の右に追加 (border-cyan-200 / text-[10px] でメイン見積と区別、aria-label「校正後 39分 (+9分)」、title「直近の見積精度に基づく校正値 (中央値 1.30×)」、delta=0 の行は非表示)。Item.description には元の見積をそのまま記録するので UI 提示のみで実データには影響しない。React Query 同 key 共有で /time-entries を開いていれば追加 fetch ゼロ、閉じていても 1 フェッチで済む。typecheck/lint 緑 (warning baseline 1)、architecture.test 5/5 PASS。次 iter268 = 18%5=3 → basics。候補: ItemEditDialog dueDate 表示の baseline tooltip 等 friendly 化、見積入力欄 inline 校正は他の入力点 (ItemEditDialog) にも横展開。
- ✅ [iter266] **nl-parse に AM/PM + 朝/昼/夕方/夜 + morning/noon/.../midnight 時刻 alias (basics, 1 commit)**: 新 pure module `time-tokens.ts` に `parseTimeFromText` を移動 + 拡張。受理パターン (上から順、先勝ち): HH:MM (24h colon) / HH(:MM)?am-pm (12am=00:00, 12pm=12:00 慣習) / JA HH時(MM分)? / JA alias (朝 09:00 / 昼 12:00 / 夕方 18:00 / 夜 20:00) / EN alias (morning 09:00 / noon 12:00 / afternoon 14:00 / evening 18:00 / night 20:00 / midnight 00:00)。`tonight` / `今夜` は date-tokens 側で today に倒される既存仕様を尊重して time alias から意図的に除外 (二語入力 `tonight 9pm` が date+time で別々に消費される動作を維持)。nl-parse.ts は新 helper に delegate (~15 行 → 5 行)、time-tokens.test.ts +15 件 / nl-parse.test.ts 既存 74 件は無変更で全 PASS。QuickAdd hint テキストに新キーワード + iter258 の Apr 30th / M-D ハイフン形式も同時に追記。typecheck/lint 緑 (warning baseline 1)。次 iter267 = 17%5=2 → ai-automation。候補: ItemEditDialog dueDate 表示 formatFriendlyDate (既存 inputs 以外、baseline tooltip 等)、AI 朝 brief 配線、見積入力 inline placeholder hint。
- ✅ [iter265b] **sprint-date-helpers を deterministic + fail-soft 強化 (refactor 追従, 1 commit)**: 並行 cloud agent の iter265 (sprint-date-helpers.ts 抽出) を受けた追従改修。3 つの now-依存関数 (`todayISO` / `nextDowISO` / `isoDaysFromNow`) に `now: Date = new Date()` 引数を追加し決定論テストを可能に、不正 ISO (`'2026-13-01'` / `'2026-04-32'` / `'garbage'` / 0500 年 / スラッシュ区切り) を fail-soft (`dayOfWeekJa`→`''`, `addDaysISO`→そのまま, `daysBetween`→0)、`nextDowISO` の範囲外 dow (-1 / 7 / 1.5 / NaN) は今日 fallback。元実装は `Number(...)!` の non-null assert で不正値が NaN になり Date+NaN=Invalid Date / `'NaN-NaN-NaN'` 文字列が出ていた潜在バグを併せて潰した (元の test では正常 ISO しか入れていなかったため検出されず)。内部 `parseIsoDate` (範囲検査 1000-9999 / 1-12 / 1-31) + `pad` + `formatLocal` で重複 6 箇所を 2 ヘルパに集約。`DowJa` 型 export で return 型を `DowJa | ''` に絞り switch 網羅検査が効くように。test +11 件 (元 21→32) で範囲外 / 不正 / 固定 Date による曜日計算の境界経路を網羅。caller (sprints-panel.tsx) は無変更。typecheck/lint 緑 (warning baseline 1)。次 iter266 = 16%5=1 → basics。
- ✅ [iter265] **sprints-panel.tsx (786 行) から ISO 日付 pure helper 6 個を抽出 (refactor, 1 commit)**: `dayOfWeekJa` / `formatDateJa` / `todayISO` / `nextDowISO` / `addDaysISO` / `isoDaysFromNow` / `daysBetween` + `DOW_JA` を新 module `src/features/sprint/sprint-date-helpers.ts` に集約。すべて UTC 基準で実装し worker / DB と同じカレンダー日で揃える。sprint-date-helpers.test.ts +23 件で月跨ぎ / 年跨ぎ / うるう年 / Sprint 14 日 / 不正 ISO fail-safe / nextDowISO 7 曜日全 verify を網羅。sprints-panel.tsx 786→744 行 (-42, -5%)。動作変更ゼロ (既存 import パス更新のみ + `DOW_JA` も export して UI からも使える)。features/notification/format.ts / features/workflow/format.ts / features/item/date-tokens.ts と同じ co-location パターン踏襲。次 iter266 = 16%5=1 → basics。残 large file: item-edit-dialog.tsx 688 / item/service.ts 704 (次 refactor iter270)。
- ✅ [iter264b] **bias を category 別に slice する pure ヘルパ + AI brief 用 1 行サマリ (ai-automation 重複, 1 commit)**: 並行 cloud agent が iter264 ai-automation を回している裏で同じ track を独立に進めたため iter264b として分離記録。「workspace 全体の 1 本 calibrationFactor」の限界 (実装は 1.5× 過小、MTG は ±10% 内 のような category 差を塗りつぶす) を解消する目的で、新 pure module `bias-by-category.ts` に 3 関数 — `selectBiasSamplesByCategory(entries, lookup)` (5 category 別 grouping、未知 category は `other` に倒す)、`computeBiasByCategory(entries, lookup)` (上記 + computeEstimateBias を 1 段で繋ぐ)、`formatTopSkewedCategoryJa(report)` (最も歪みが強い 1 category を `開発: 見積を 2.00× 過小評価しがち (5 件)` で返す。選定: under/over のみ → distance 最大 → samples 多い方 → 該当なし null)。bias-by-category.test.ts +12 件 (空 / itemId null / 振り分け / desc 無し / 未知 cat / unknown / 5 sample 並列 / null / under / over / 複数歪み / on-track 除外)。並行 agent の iter264 (`bias-brief.ts` の 3 行 JA 文面) とは substrate が直交 — brief は workspace 全体 1 文、本 helper は category slice — なので将来 brief に「最も歪みが強い category 行」を 4 行目として混ぜられる。typecheck/lint 緑 (warning baseline 1)。次 iter265 = 15%5=0 → refactor。
- ✅ [iter264] **bias を「朝の brief」3 行 JA 文面に整形する pure helper (ai-automation, 1 commit)**: iter257 → 259 → 262 で完成した「集計 → UI → 校正推奨」の data 連鎖の上に、AI 朝 brief / pm-agent / Slack DM / dashboard widget が同じ事実から共通の 3 行文面を生成できる pure helper を追加。`formatBiasBriefJa(report) → {headline, detail, recommendation}` で 5 tendency 全分岐 (unknown / on-track / under / over / mixed) ごとに専用文面を返す (例: under は `見積精度: 過小評価傾向 (1.30×)` / `直近 5 件のうち 4 件 (80%) が見積を超過` / `次の見積は 1.30× を掛けて出すと実測に近づきます (例: 30分 → 39分)`)。`formatBiasBriefText` は `\n` join で Slack mrkdwn / aria-label 用。bias-brief.test.ts +7 件 (5 tendency 全分岐 + factor=null 防衛 + text 3 要素 join)。UI 表示は変更なし — AI consumer が別途呼ぶ形で UI 賑やかさを増やさず文面 1 箇所集約だけを達成。typecheck/lint 緑 (warning baseline 1)。次 iter265 = 15%5=0 → refactor。残 large file: sprints-panel.tsx 786 / item/service.ts 704 / item-edit-dialog.tsx 688 (workflows-panel は iter260 で 847→761 に縮小済) — どれかを extraction 対象に。
- ✅ [iter263] **Kanban カードの dueDate / startDate も formatFriendlyDate で表示 (basics, 1 commit)**: iter261 (Today / Backlog) の続編。kanban-view.tsx の `KanbanCard` 内で「開始: 2026-04-30 / 期限: 2026-05-15」生 ISO 表示 →「開始: 4/30 (木) / 期限: 5/15 (金)」 / 「期限: 明日」(相対表現) に変更。元 ISO は親 div の title (hover tooltip) に保持し SR は親 title 経由で正確な値を取得可能に。`useMemo(() => 今日のカレンダー日)` を 1 カードにつき 1 回計算 (50 カードでも render 重さ無視可)。typecheck/lint 緑 (warning baseline 1)。次 iter264 = 14%5=4 → ai-automation。残候補: ItemEditDialog / Gantt の dueDate 表示 (Gantt は `format(date, 'yyyy-MM-dd')` で 5+ 箇所、別 iter)、見積入力欄の inline placeholder hint。
- ✅ [iter261b] **workflow/format ヘルパの DateLike 型導入で test 側 as cast 排除 (refactor 割り込み, 1 commit)**: 並行 cloud agent が iter261 basics を回している裏で `judge.sh` の `recent-any-leak(=5)` interrupt が発火 → refactor 割り込みを別系列で実行 (iter261b と命名し HANDOFF を分離)。iter260 で抽出した `src/features/workflow/format.ts` は schema 由来 `Date | null` 型のままで、test 側で `as unknown as Date` × 5 件を強要していた。`DateLike = Date | string | null | undefined` を export + `RunTimeFields` を Pick から structural な DateLike フィールドへ広げ、`WorkflowRun` 代入可能性をコンパイル時 assert で固定。`toDate()` で Number.isNaN を一元化したことで `formatRunTime` の startedAt 不正→createdAt fallback 経路が初めて正しく動くように (元実装は startedAt が `??` を通過した瞬間 NaN チェックで `'—'` に落ちて createdAt を試さなかった)。test +2 件 (createdAt fallback / finishedAt 不正)。typecheck/lint 緑、format.test 13/13 PASS、architecture.test 5/5 PASS。次 iter263 = 13%5=3 → basics (並行 agent 側で iter262 が ai-automation を消化済なので順序通り)。
- ✅ [iter262] **bias の calibrationFactor を「30分→39分 (+9)」推奨に展開 (ai-automation, 1 commit)**: 新 pure module `bias-calibration.ts` に 3 関数 — `applyBiasCalibration(estimate, factor)` (整数丸め + 1 分 clamp + null/非正/NaN ガード)、`suggestCalibratedEstimates(factor)` (典型 5 値 15/30/60/120/240 を一括校正、delta=0 行はスキップ)、`formatCalibratedEstimateJa(c)` (`30分 → 39分 (+9分)` の 1 行表記)。bias-calibration.test.ts +16 件で全エッジケース (factor null/0/負, estimate 0/負/Infinity/NaN, 整数丸め, clamp, +/-/±0 表記) 網羅。`EstimateBiasInsight` を更新し calibration 行の下に「典型的な見積分の校正推奨」を `<ul>` で表示 (aria-label + tabular-nums)。iter257 → 259 → 262 で「集計 → UI → 校正推奨」 ai-automation 3 段完成。typecheck/lint 緑 (warning baseline 1)。次 iter263 = 13%5=3 → basics。候補: ItemEditDialog / Gantt の dueDate 表示を formatFriendlyDate に揃える (iter261 の続き)、または見積入力欄の inline placeholder hint。
- ✅ [iter261] **dueDate 表示を formatFriendlyDate で「明日 / 4/30 (木)」化 (basics, 1 commit)**: iter256 で QuickAdd preview chip 用に作った `formatFriendlyDate` を Today / Backlog の dueDate に横展開 (iter256 末候補から消化)。Today では「期限 2026-04-30」→「期限 4/30 (木)」/「期限 明日」、Backlog では生 ISO → friendly 表示。元 ISO は title + aria-label に保持し SR / hover では正確な日付を維持 (QuickAdd preview と同じ a11y パターン)。buildColumns に `today: Date` パラメータ追加し component 側で `useMemo` で 1 render 内固定。typecheck/lint 緑 (warning baseline 1)。次 iter262 = 12%5=2 → ai-automation。候補: (a) iter257/259 の延長で AI 朝の brief / pm-agent から bias insight を消費する文面生成、(b) bias を Item ごとに集計して estimate 訂正提案 (item-edit-dialog に reminder)。残候補: ItemEditDialog / Gantt の dueDate 表示横展開 (次 basics)。
- ✅ [iter260] **workflows-panel.tsx (847 行) から pure helper 2 module を抽出 (refactor, 1 commit)**: `node-presets.ts` (新) に `NODE_PRESETS` / `NodePreset` / `appendNodePreset` を移動、既存 test を `src/components/workflow/workflows-panel.test.ts` → `src/features/workflow/node-presets.test.ts` に `git mv` で履歴保持しつつ co-location + import path 更新。副次効果として元 test が踏んでいた server-only import 連鎖 (workflows-panel → hooks → actions → service → env) が切れ、cloud env (.env.local 不在) でも安定走行できるように。`format.ts` (新) に `formatRunTime` / `formatRunDuration` を移動 + 9 件 test 追加 (timezone 非依存に regex 構造のみ assert)。workflows-panel.tsx 847→761 行 (-86, -10%)。typecheck/lint 緑 (warning baseline 1)。次 iter261 = 11%5=1 → basics。残り large files: sprints-panel.tsx 786 / item/service.ts 704 / item-edit-dialog.tsx 688 — 次 refactor iter265 (15%5=0) で。
- ✅ [iter259] **/time-entries に「見積精度 (直近)」 insight カード (ai-automation, 1 commit)**: iter257 の `computeEstimateBias` を実 UI に配線。新規 server action は不要 — 既存 `useTimeEntries` + `useItems` のクライアント結合で済ます。pure 2 段ヘルパ `selectBiasSamples(entries, lookup)` (itemId 無し除外、`extractEstimateMinutes` で description から見積抽出、見積行が無ければ null sample) と `buildItemDescriptionLookup(items)` (id→desc Map 化) を `bias-selector.ts` に追加 + test 8 件。`EstimateBiasInsight` (client component) は 5 tendency 別の色 (emerald/amber/sky/violet/muted) + summary + under/on/over 内訳 (dl) + calibration factor を表示、loading は同 layout placeholder で跳ね防止、error は silent (主機能を妨げない)。TimeEntriesPanel の先頭に 1 行追加で配線。typecheck/lint 緑 (warning baseline 1)。次 iter260 = 10%5=0 → refactor。候補: workflows-panel.tsx 847 行 / sprints-panel.tsx 786 行 / item/service.ts 704 行 のいずれかを分割 (HANDOFF iter256 末候補 + detect-patterns 継続検出)。
- ✅ [iter258] **QuickAdd に ordinal suffix (Apr 30th) + M-D ハイフン形式 (basics, 1 commit)**: `date-tokens.ts` の pattern 7 (EN 月名) に optional `(?:st|nd|rd|th)?` を追加し `Apr 30th` / `April 1st` / `30th Apr` / `Apr 30th, 2027` / `1ST May` を受理、pattern 9 のセパレータを `\/` から `[/-]` に拡張し `3-15` / `12-31` / `3-15-2027` / `3-15-27` も許容 (GitHub / 業務メモ慣習)。範囲外 (13-1, 0-15, 5-32) 拒否ルールは slash と同じ。ISO `YYYY-MM-DD` (パターン 8) は前段で消費されるので衝突なし、`+3d` も word boundary で衝突なしを test 化。date-tokens.test.ts +14 件 (ordinal 5 / hyphen 9) → 計 62 件全 PASS、nl-parse.test.ts 74 件は無変更。typecheck/lint 緑 (warning baseline 1)。次 iter259 = 9%5=4 → ai-automation。候補: (a) iter257 の `computeEstimateBias` を呼び出す server action + dashboard panel 配線、(b) `formatFriendlyDate` を ItemEditDialog / Today / Backlog / Gantt の日付表示に横展開 (iter256 末から繰越し)。
- ✅ [iter257] **estimate vs actual bias 集計の純粋ヘルパ (ai-automation, 1 commit)**: `src/features/time-entry/bias.ts` に `computeEstimateBias(samples)` を新設。median/mean ratio + under/on/over counts + calibrationFactor (0.05 刻み丸め, [0.5,3.0] clamp) + tendency 5 区分 (on-track/underestimating/overestimating/mixed/unknown) + JA summary を返す。tolerance/bias 区分は既存 `formatVariance` (active-timer.ts) と一致させ (10% / 最低 1 分)、estimateMinutes が null/0 の sample は ratio 集計から除外 (totalCount は維持)。AI brief / pm-agent / dashboard が「あなたは見積を 1.30× 過小評価しがち」系 insight を出すための data substrate。vitest 13 件 (空/不足/null 除外/on-track/under/over/mixed/median 奇偶/丸め/clamp/tolerance/summary 形式)。typecheck/lint 緑 (warning baseline 1)。次 iter258 = 8%5=3 → basics 復帰。本ヘルパを呼び出す server action / hooks / dashboard panel は次 ai-automation iter (iter259 or iter262) で。
- ✅ [iter256] **QuickAdd に EN 月名 / weekend EN alias / M/D 形式 / friendly 日付表示 (basics, 3 commits)**:
  iter256 = 6%5=1 で base track basics。`scripts/autonomous/judge.sh` 出力の interrupt
  signals (TODO=18 / any-leak=2) は前 iter で「誤検出 — Todoist 商品名 / status='todo'
  enum / autonomous helper の自己言及」と切り分け済 (HANDOFF iter255) なので無視し、
  本道 basics 3 連射。3 commit いずれも date-tokens.ts への純粋拡張で副作用ゼロ、
  `pnpm typecheck` / `pnpm lint` 緑 (warning baseline 1 のみ、新規 0)。
  date-tokens.test.ts は 26→48 件 (+22)、nl-parse.test.ts 74 件は無変更で全 PASS。
  - **commit 1 (`e3965b4`)**: EN 月名絶対日付 — `Apr 30` / `April 30` / `30 Apr` /
    `Apr 30, 2027` を受理。`MONTH_EN` dict + `MONTH_EN_PATTERN` (longest-first
    alternation) を追加し、Month-Day → Day-Month の順で 2 regex を試す。年省略時は
    `rollForwardMonthDay(base, m, d)` で「未来なら今年、過去なら来年に繰り上げ、当日
    は今年扱い」 — Todoist の "the next occurrence" semantics と一致。`Feb 30` のような
    存在しない日は `month+1, 0` の月末 trick で当該月末に丸める。test 8 + helper test 2
    = 10 件追加。
  - **commit 2 (`d3913f2`)**: weekend EN alias + M/D スラッシュ形式 — 既存 `今週末` /
    `月末` に EN alias `this weekend` / `end of month` / `eom` を併設、新規 `next
weekend` (= `来週末` JA alias 同義、+7 日) を追加。さらに `3/15` / `12/31` /
    `3/15/2027` / `3/15/27` の M/D[/YY[YY]] US-convention を受理 (2-digit 年は 20YY
    pivot、範囲外は誤検出防止のため拒否、2/30 は月末丸め)。test 14 件追加。
  - **commit 3 (`6c0d1a1`)**: QuickAdd preview chip の日付表示を friendly に。
    `formatFriendlyDate(iso, today, locale='ja'|'en')` pure 関数を追加。当日 / 翌日 /
    翌々日 / 昨日 を専用ラベル化、同年内は `4/30 (木)` / `Thu Apr 30`、別年は
    `2027-04-30 (金)` / `Fri Apr 30 2027`。chip に `title=ISO` + `aria-label=予定 ISO`
    を保持して SR / hover には正確な日付を残す (視覚 friendly + 音声 ISO の両立)。
    test 8 件追加。
  - 結果: QuickAdd で `apr 30 deadline` / `next weekend trip` / `5/15 release` /
    `eom report` が動き、preview chip も `今日` / `明日` / `5/1 (金)` で表示。
    国際チーム (EN 入力) と日本チーム (JA 入力) のどちらでも Todoist 並みの quick-add
    UX が揃った。
  - 次 iter257 = 7%5=2 → **ai-automation track**。候補:
    (a) 過去 30 日 estimate vs actual の bias グラフ (timer Stop 結果 → /time-entries
    に集計可視化、HANDOFF iter255 末から繰越し)
    (b) AI チャット panel: 全タスクを context にした相談 UI (大型、複数 iter)
    (c) AI 朝の brief: 「今日のおすすめ順」を AI が生成
    iter260 (10%5=0) で次の refactor 機会 — その頃には workflows-panel.tsx 847 行 /
    item-edit-dialog.tsx 831 行が依然候補。
  - 候補で時間切れ未着手:
    - `Apr 30th` ordinal suffix / `the 30th` 単独 / `M-D` ハイフン形式 (next iter で 1 commit)
    - ItemEditDialog / Today view / Backlog view / Gantt view の日付表示も
      `formatFriendlyDate` に揃える (いまは QuickAdd preview だけ — 横展開 1 commit)
    - 自己観察ノート: detect-patterns で large file 19 件は変わらず累積中。iter260
      refactor で workflows-panel.tsx 分割を最有力候補に。
- ✅ [iter255 — nl-parse refactor 担] **nl-parse.ts を pure helper 3 module に分割 (refactor, 2 commits)**:
  iter255 = 5%5=0 で base track refactor。`detect-patterns.sh` 結果は TODO=18 / any-leak=1 /
  large-files=19 だが、TODO/any-leak はいずれも誤検出 (Todoist 商品名 / status="todo" enum
  / autonomous helper の自己言及) 寄りで、純粋な refactor 余地は iter254 で
  ai-automation 担が肥大化させた `nl-parse.ts` (348 行) にあった。
  - **commit 1 (`6a78675`)**: 工数推定を `src/features/item/estimate.ts` に分離。
    `parseEstimateFromText(text): { minutes, matched } | null` を新規 export し、
    nl-parse の inline `consumeEstimate` 25 行 + 4 match 分岐を 1 関数に集約。
    `formatEstimate` / `extractEstimateMinutes` も同居。call site (quick-add /
    start-timer-button) は estimate.ts 直接 import に更新、nl-parse は後方互換の
    re-export だけ残す。estimate.test.ts 14 件追加 (parseEstimateFromText 単体契約 +
    format→extract round-trip 3 件)。
  - **commit 2 (`7ef6fc8`)**: 日付トークン抽出を `src/features/item/date-tokens.ts` に分離。
    `parseDateFromText(text, today): { date, matched } | null` で 8 パターン (今日/明日/
    明後日 → 来週X曜 → next mon → 今週末/月末 → JA/EN weekday → ISO → +Nd/+Nw) を
    "上から先勝ち" で集約。WEEKDAY_JA/EN テーブル + isoDate / addDays / nextWeekday
    helper も同居 export。nl-parse の inline date 検出 ~95 行が 1 呼び出しに減った。
    date-tokens.test.ts 15 件追加。
  - 結果: nl-parse.ts 348 → 135 行 (-213, -61%)。estimate.ts 102 行 + date-tokens.ts 170 行 =
    総 407 行 (純増 59 行は docstring + 関数境界コスト、許容範囲)。
    既存 76 件 nl-parse.test.ts は re-export 経由で全て green を維持 = 動作変更ゼロの証拠。
    typecheck / lint 緑 (warning baseline 1 件のみ、新規 0)。3 ファイル合計 101 件全 PASS。
  - 次 iter256 = 6%5=1 → **basics track**。候補 (HANDOFF iter254 末から繰越し):
    (a) 英語の絶対日付 (`Apr 30` / `30 Apr` / `3/15`) を date-tokens.ts に追加 (今や置き場所
    が明確)、(b) `weekend` 英 alias (`this weekend` / `next weekend`)、(c) 過去 30 日
    estimate vs actual の bias グラフ (timer Stop 結果 → /time-entries に集計可視化)。
    iter257 = ai-automation 復帰。iter260 (10%5=0) で次の refactor 機会 — その頃には
    workflows-panel.tsx / item-edit-dialog.tsx 等の 800+ 行 component の分割が候補に
    なっているはず。
  - 候補で時間切れ未着手: 時刻 (HH:MM / HH時) 抽出の time-tokens.ts 分離 (commit 1+2 と
    同パターン、~50 行 → ~30 行) → 次 refactor iter で。

- ✅ [iter255 — detect-patterns / SubtasksPanel / MUST+DoD 担] **refactor track 3 commits**:
  iter255 は `(255 % 5 == 0)` で refactor 規定。`bash scripts/autonomous/judge.sh` で
  TODO=18 / any-leak=1 が割り込み signal として点いていたが、調べると 18 件中 17 件が
  製品名 `最強TODO` / status enum `'todo'` / 説明文の偽陽性、any-leak 1 件は自分自身の
  identifier `recent-any-leak`。signal が機能していないと割り込み判定が壊れるので、
  refactor の最初を「detector 自身の偽陽性除去」から開始。3 commit 全て main 級で push。
  - **commit 1 (`06e72cb`)**: `src/lib/autonomous/patterns.ts` に `isActionableCodeTodo` /
    `parseCodeTodos` / `parseAnyLeaks` を追加し、bash 側 (`judge.sh` / `detect-patterns.sh`)
    を同等 regex に書き換え。TODO 検出は「コメント文脈 (`//` / `/*` / JSDoc `* `) +
    actionable 接尾 (`:` か `(`)」の AND、any-leak は構文限定 6 種 (`: any` / `as any` /
    `as unknown as` / `<any>` / `,any>` / `@ts-(expect-error|ignore)`) に分割。
    `src/lib/autonomous/` を pathspec exclude (検出器自身の test fixture が住む領域)。
    結果: TODO_COUNT 18→0 / ANY_LEAK 2→0、test 13 件追加 (40 件)。bash の `set -euo pipefail`
    - `git grep` no-match exit=1 罠は `{ ... || true; }` で吸収。今後の判定 noise が消えた。
  - **commit 2 (`17ebd6f`)**: `item-edit-dialog.tsx` (831 行) から `SubtasksPanel` (130 行) +
    pure helper `parseBulkSubtaskTitles` を分離。`subtasks-panel.tsx` (UI, 161 行) と
    `subtasks-panel-helpers.ts` (pure, 24 行) の 2 file 構成。UI 直接 import は
    `@/features/item/hooks → server-only env` で vitest 落ちるため、helper を別 file に
    分けて test 6 件追加 (workflows-panel と同パターン)。bulk 件数表示の重複 3 ヶ所を
    `pendingTitleCount` 変数に集約 — 件数の drift 防止。`item-edit-dialog.tsx` 831→688 行
    (-17%)、不要になった `useItems` / `useCreateItem` / `fullPathOf` /
    `DecomposeProposalsPanel` import を削除。動作変更ゼロ。
  - **commit 3 (`d484950`)**: `item/service.ts` 内に 4 ヶ所散らばっていた MUST+DoD invariant
    判定式 (`if (x.isMust && (!x.dod || x.dod.trim() === ''))` 系) を pure helper
    `violatesMustDodInvariant` に集約。新 file `src/features/item/must-dod.ts` (35 行) +
    test 12 件 (isMust=false 5 ケース / isMust=true 7 ケース、null/undefined/空文字/空白のみ
    全網羅)。置換箇所: `update` / `updateStatus` / `toggleComplete` / `bulkUpdateStatus` の
    4 箇所。schema.ts は zod context API が違うので今回は触らず — 次 refactor iter で同 helper
    に寄せる候補として残す。型は `MustDodFields = { isMust: boolean; dod?: string | null }`
    の structural typing で Item / before / updated / zod 入力 v すべてを受け止める。
  - 検証: typecheck / lint 緑 (warning baseline 1 件 = TanStack Table、新規 0)。
    pure test 計 18 件追加 (40+13+6+12 - 元 24 = 47 → 65)。
    cloud env で skip される実 Supabase test は従来通り (本 iter で挙動変更なし)。
  - **次 iter256 = basics track** (256 % 5 == 1)。本来 iter255 が ai-automation
    だった訳ではない (255 % 5 = 0 で refactor 規定通り) ので「割り込み消化予定」は無し。
    HANDOFF.md iter254 末尾に記録された候補から優先順位 (basics 寄りで再選定):
    (a) NL parser に英語の絶対日付 `Apr 30` / `30 Apr` / `weekend` 英 alias 追加 — 並走
    nl-parse 担が新設した `date-tokens.ts` に追記する形で実装容易
    (b) Today / Backlog のキーボードショートカット拡張 (1-4 で priority, # で tag picker)
    (c) フィルタ・ソート保存 (Smart List 風) — Backlog の URL state を nuqs で persist
  - 候補にあったが時間切れ未着手:
    (1) workflows-panel.tsx (847 行) の React Flow 部 / JSON edit 部の分割 — 次 refactor iter
    (2) `bulkUpdateStatus` / `bulkSoftDelete` の重複骨格 (auth → withUserDb → loop → audit)
    を higher-order helper に抽出 (約 80 行縮小可能)
    (3) schema.ts superRefine の MUST+DoD 判定を `violatesMustDodInvariant` に寄せる
  - **自己観察ノート**: iter255 冒頭で `bash scripts/autonomous/judge.sh` を叩いて
    TODO=18 / any-leak=1 を見たが、その値が 1 iter 前 (iter254) も同じだったことに
    HANDOFF を読み直して気付いた。「signal の偽陽性が連続 2 iter で同じ」=「detector を
    直すべきサイン」。今後 judge 出力の signal が前 iter と同値で連続したら detector 改良を
    検討する。これは次の tooling iter で `judge.sh` に「前 iter と同 signal が連続」検知を
    入れる候補 (HANDOFF 参照しながら 1 行差分で済む)。
  - **並走 (nl-parse refactor 担) との非衝突**: 別 Claude が同じ iter255 で nl-parse
    分割を進めていた (commits `6a78675` / `7ef6fc8`)。両者の変更領域は完全に分離
    (こちら: `src/lib/autonomous/` + `src/components/workspace/item-edit-dialog.tsx` +
    `src/features/item/{service.ts,must-dod.ts}`、向こう: `src/features/item/{nl-parse.ts,
estimate.ts,date-tokens.ts}`)。HANDOFF だけ衝突したので rebase で両者の entry を
    残した形で main 統合済み。

- ✅ [iter254 — 並走 ai-automation 担] **NL parser 工数推定 + 英語キーワード + timer variance 連携 (ai-automation, 3 commits)**:
  別 Claude が iter254 を tooling cold-start interrupt で消化していたのとは別に、
  並走で ai-automation 本道を 3 commit 進めた。target は QuickAdd → timer の連結強化。
  - **commit 1 (`c70aeff`)**: nl-parse に `estimateMinutes` を追加 (`30分` / `1時間` /
    `1時間30分` (JA) と `30m` / `30min` / `1h` / `1.5h` / `2h30m` (EN))。時刻
    `15時` との衝突回避のため estimate を時刻パースより前に処理。上限 60h。
    QuickAdd 側で 🕐 chip + previewSummary + placeholder + hint を更新、見積
    がある場合 description プレフィクス `見積: 1時間30分` で保存。`formatEstimate(min)`
    helper export。test 16 件追加 (estimateMinutes 11 + formatEstimate 5 = 46 件)。
  - **commit 2 (`b74c2f2`)**: nl-parse に英語キーワード追加 (`today` / `tomorrow` /
    `tmrw` / `tmr` / `tomo` / `tonight` / `mon..sun` フル+短縮 / `next monday`)。
    `WEEKDAY_EN` dict + `WEEKDAY_EN_PATTERN`。Todoist 互換で「monday も next monday
    も次の該当曜日 (今日が同曜日なら +7)」。word boundary ガードで `Monthly retro` を
    誤拾いしない。test 16 件追加 (62 件全 PASS)。
  - **commit 3 (`40e203e`)**: timer Stop 時の actual vs estimate variance toast。
    `extractEstimateMinutes(description)` で QuickAdd の `見積: ...` プレフィクスを
    復元、`ActiveTimerStore.estimateMinutes` を新フィールド (persist 対象) に追加、
    `start({...,estimateMinutes})` / `stop()` 戻り値を拡張。`formatVariance(actual,
est)` で「予定 30分 / 実測 32分 (+2分、ほぼ見積通り)」を整形 (±10% / 最低 1 分の
    tolerance、bias=under/on/over/unknown)。bias=over → toast.warning、それ以外 →
    success。`StartTimerButton` が item.description から estimate を抽出して
    start に渡す + 起動 toast に「見積 30分」表示。`ActiveTimerPanel` header に
    常時 chip + Stop toast に variance message。test 8 件追加 (extractEstimateMinutes
    12 + store estimate 1 + formatVariance 7 = 95 件全 PASS)。
  - 全 commit typecheck / lint 緑 (warning baseline 1 件)。tooling 並走分 (judge.sh /
    detect-patterns.sh / push-main.sh) はそのまま残し、本 ai-automation 進捗と
    両立させる。
  - 次 iter255 = 5%5=0 → **refactor track**。候補:
    (a) description プレフィクス方式から `items.estimate_minutes` 列への migration
    (ALTER TABLE + drizzle-zod schema + QuickAdd / dialog 移行 +
    extractEstimateMinutes deprecate)
    (b) lint warning 0 化 (TanStack Table の useMemo 不要化対応)
    (c) detect-patterns で見えた hotspot file 分割 (`workflows-panel.tsx` 847 行 /
    `item-edit-dialog.tsx` 831 行)
    iter255 開始時に `scripts/autonomous/judge.sh` + `detect-patterns.sh` を叩いて
    数値で優先度を確定する。
  - 候補で時間切れ未着手: 英語の絶対日付 (`Apr 30` / `30 Apr`) / `weekend` 英 alias /
    過去 30 日 estimate vs actual の bias グラフ → iter256 (ai-automation) で消化候補。
- ✅ [iter254] **autonomous loop 補助スクリプト 3 種を新設 (tooling — cold-start interrupt)**: 本来は ai-automation 順だったが「`scripts/autonomous/` に script が 1 つも無い」という cold-start interrupt 条件が満たされていたため tooling track に切替。3 commit を main 級で積んだ。**(1) `scripts/autonomous/judge.sh`** + `src/lib/autonomous/iter-info.ts` (pure 関数 `parseIterFromGitLog` / `decideBaseTrack` / `buildJudgeReport` / `formatJudgeReport`、vitest 13 件) — autonomous loop 冒頭で叩くと「直近 iter / 次 iter / base track / 割り込み signal」が <0.1s で出る。bash と TS の規約乖離は test が ガード。**(2) `scripts/autonomous/detect-patterns.sh`** + `src/lib/autonomous/patterns.ts` (`parseHotspots` / `findLargeFiles` / `parseLintWarnings` / `evaluateRefactorTriggers`、vitest 11 件) — リファクタ割り込み 5 軸 (lint warning / TODO/FIXME / 直近 any leak / hotspot top 5 / 巨大 file) を 1 画面に。今 iter で TODO=18 / any-leak=3 / hotspot=2 / large=19 を初検出 — 直近の pace が refactor 負債を積もらせ気味なサインを数値化。**(3) `scripts/autonomous/push-main.sh`** — main プロンプトの 3 段 fallback (fast push → fetch+rebase → 確認) を script 化、`--dry-run` で副作用なしの確認も可能。CLAUDE.md に「autonomous loop 補助スクリプト」節 (表 + 推奨フロー + 「TS test を先に書く」ルール) を追記し、次 iter の自分が見落とさない導線を確保。typecheck / lint 緑、autonomous helper 計 24 vitest tests 全パス。次 iter255 = ai-automation track 復帰 (本来予定だった分を消化)。詳細値が分かったので iter255 以降は detect-patterns で見えた refactor サイン (large file 19 件、特に workflows-panel.tsx 847 行 / item-edit-dialog.tsx 831 行) を将来の refactor iter で吸収する候補として記録。
- ✅ [iter253] **Today view に Vim/TickTick 風キーボードカーソル (basics)**: `src/lib/keyboard/list-cursor.ts` (純粋関数 `moveCursor` + 8 unit tests = 全 PASS) を新設し、TodayView に group 横断 flat list ベースの行カーソル + key handler を配線。`j` / `↓` で次、`k` / `↑` で前、`Enter` / `e` で `setOpenItemId(cursor)` (ItemEditDialog 自動 open)、`x` / `Space` で `useToggleCompleteItem` を叩き完了切替、`Esc` でカーソル解除。IME / input / textarea / Cmd/Ctrl/Alt 修飾子はガード済 (既存 `global-shortcuts.tsx` と同パターン)。選択行は `aria-current=true` + `data-cursor=true` + `ring-2 ring-primary bg-muted` で視覚化、上部に操作ヒント `<p aria-live=polite>` を表示。flat list から Item が消えた (完了/フィルタ削除) 場合の `selectedId` は state を直接書き換えず derive で `null` 化し React 19 の `set-state-in-effect` lint を回避。`src/lib/keybindings.ts` に「Today」group を 5 行追加 (j/k/Enter+e/x+Space/Esc)、`KeybindingsHelpModal` がそのまま自動描画。typecheck / lint 緑 (warning baseline 1 件のみ、新規 0)。次 iter254 = ai-automation track。
- ✅ [iter252] **楽観ロック競合 自動 retry (queue: 同時編集深堀り / 自動 retry on conflict)**: `item-edit-dialog.tsx` の `handleSave` で ConflictError (`code === 'CONFLICT'`) をキャッチした際、単なるエラー toast だけでなく最新版で 1 回自動リトライ (Linear / Notion 風)。`qc.refetchQueries` で items cache を強制 refetch → `getQueriesData` で当該 Item の最新 version を取得 → 同 patch で再 mutate。retry 成功時は「バージョン競合を自動解決」toast + onOpenChange(false)。retry も失敗した場合のみエラー toast (2 重競合は稀だが安全に fallback)。`useQueryClient` + `itemKeys` を追加 import。typecheck / lint 緑。
- ✅ [iter251] **CLI 分解経路を staging mode に切替 — propose_child_item で承認 UX (queue: タスク分解 UX)**: `decomposeItemViaClaude` が直接 `create_item` で子 Item を生成していた問題を修正。`scripts/mcp-agent-server.ts` に `DECOMPOSE_PARENT_ITEM_ID` / `AGENT_INVOCATION_ID` env を追加し、セット時は `DECOMPOSE_TOOLS` (`propose_child_item`) を使う staging mode で起動。`claude-flow-runner.ts` に `decomposeParentItemId` を追加して MCP server env に連携。Researcher の `decomposeItemViaClaude` を `staging:true` + `propose_child_item` allowedToolNames に変更。これで "AI 分解" ボタン押下後、子タスク案が `agent_decompose_proposals` に置かれ、ユーザが ItemEditDialog の「子タスク」タブで 1 件ずつ採用/却下できる TickTick/Linear 風 UX が CLI 経路でも揃った。typecheck / lint 緑。
- ✅ [iter250] **TickTick 風 タスクタイマー — Today/Backlog 行に compact StartTimerButton 配置 (queue: TickTick タイマー 行配置)**: `today-view.tsx` の各 Item 行右端 (StatusBadge 隣) に `<StartTimerButton size='sm'>` を stopPropagation wrapper で配置。`backlog-view.tsx` の actions column にも追加 (DnD 競合防止のため actions セル全体に stopPropagation/onPointerDown を追加、column size 300→380)。これで Today/Backlog のどこからでもワンクリックで timer 起動できる TickTick 風 UX が揃った。typecheck / lint 緑。
- 🚧 [iter249] **TickTick 風 タスクタイマー — Start trigger button + ItemEditDialog 配線 (queue: TickTick タイマー 進行中)**: `src/components/workspace/start-timer-button.tsx` 新設、3-way 状態出し分け: (1) active 無 → 「計測開始」 button → store.start (2) この Item で計測中 → 「経過 MM:SS — 右下 panel で停止」 表示 (button disabled、live region で SR 自動読み上げ) (3) 別 Item で計測中 → confirm dialog (「前 timer は記録せず破棄」と明示) で stop → start 入替。`item-edit-dialog.tsx` base tab に「⏱ タスクタイマー」 panel を AI 分解 / Engineer の下に追加。これで dialog → click → 右下 panel が pop の hot path が完成、TickTick 風 stopwatch UX が end-to-end で動く。次 iter で Today / Backlog 行に compact (size='sm') 配置。typecheck / lint / 67 files 598 tests 緑
- 🚧 [iter248] **TickTick 風 タスクタイマー — floating panel UI + time_entries 連携 (queue: TickTick タイマー 進行中)**: `src/components/workspace/active-timer-panel.tsx` 新設。右下 fixed の小型 panel、Active timer 立ってる時のみ表示。経過時間を 1s 刻みで描画 (Date.now base なので tick 間引きでも累積正しい)、Pause / Resume / Stop の 3 button。Stop で `useCreateTimeEntry` 経由で `time_entries` に auto insert (`workDate=today`、`category='dev'`、`description=「タスク: <title>」`、`durationMinutes=Math.max(1, round(ms/60000))`、`idempotencyKey=uuidv4`)。`role="region"` + 動的 aria-label (「経過 MM:SS 計測中/一時停止中」)、aria-live="polite" で経過時間を SR 自動読み上げ。workspace `[workspaceId]/page.tsx` に mount。次 iter で Item 行 / ItemEditDialog の Start button 追加 (まだ start trigger が無いので panel は条件出現)。typecheck / lint / 67 files 598 tests 緑
- 🚧 [iter247] **TickTick 風 タスクタイマー — Scope A core: Zustand store + formatElapsed (queue: TickTick タイマー 進行中)**: ユーザ要望「ticktick みたいに測れる」/「デスクトップアプリ風 常駐ポップアップ」の Scope A 核を実装。`src/lib/stores/active-timer.ts` で `start/pause/resume/stop/elapsedMs/isActive` を持つ Zustand store + persist middleware (`saikyo-active-timer` localStorage key) で reload 跨ぎ継続、Date.now() base で background tab で setInterval が間引かれても狂わない。stop() は `{ itemId, itemTitle, elapsedMs }` を返して caller (UI) が `time_entries` へ書き込む責務分離。`formatElapsed(ms)` で `MM:SS` / `HH:MM:SS` 表示。MVP は stopwatch、Pomodoro は POST_MVP。テスト 13 件追加 (585→598 全パス) — start/pause/resume/stop の状態遷移 + accumulated 蓄積 + elapsed 計算 + edge cases (no item / negative ms)。次 iter で floating panel UI + Item 行 Start button + Stop → time_entries auto insert。typecheck / lint / 67 files 598 tests 緑
- 🚧 [iter246] **Custom e2b template `saikyo-engineer` scaffolding (queue: Claude on Web 残タスク 3)**: `e2b/saikyo-engineer/Dockerfile` + `e2b.toml` + `README.md` 追加。Node 20 + git (e2b base) に pnpm (corepack) / Supabase CLI (Service test 用) / Playwright Chromium (e2e 用) を baked-in。claude CLI は image 肥大回避で実行時 `npm i -g @anthropic-ai/claude-code` (~10s)。`runViaCloudSandbox` / `runClaudeOnRepo` / `RunClaudeOnRepoInput` / `CloudEngineerEnv` に `template?: string` を追加、`Sandbox.create(template, opts)` overload を使い分け。Adapter は `SAIKYO_ENGINEER_TEMPLATE` env で template 名切替 (空文字は undefined 扱い)。テスト 3 件追加 (582→585) — env 未設定 / 'saikyo-engineer' / 空文字。残 (operator 作業): `e2b template build` で template_id 確定 → `.env.local` に SAIKYO_ENGINEER_TEMPLATE=saikyo-engineer 設定。typecheck / lint / 66 files 585 tests 緑
- 🚧 [iter245] **engineer-worker dispatcher 配線 — local / cloud 切替 (queue: Claude on Web 残タスク 2)**: `chooseEngineerRunner(env)` を pure 関数で抽出 (`SAIKYO_ENGINEER_USE_CLOUD_SANDBOX === 'true'` のみ厳格 match で `'cloud'`、それ以外全部 `'local'` — 誤入力 `'TRUE'` / `'1'` も拒否で本番事故防止)。`engineer-worker.ts` を `runViaLocal` / `runViaCloudSandbox` に分割し、cloud 路では adminDb で item 取得 → `buildUserMessage` で prompt → `runEngineerInCloudSandbox` 呼出 (verify=fast / autoMergeToMain=true default)。env 不備 (CloudEngineerEnvError) は catch して log のみ (job throw しない = pg-boss retry 抑制)。テスト 5 件追加 (577→582) — 5 種の env 値で routing assert。typecheck / lint / 66 files 582 tests 緑
- 🚧 [iter244] **Cloud Engineer adapter — engineer-service と cloud-sandbox-runner の橋渡し (queue: Claude on Web 残タスク 1)**: `src/features/agent/cloud-engineer-adapter.ts` 新設。`loadEnvForCloudEngineer()` で env (SAIKYO_ENGINEER_GIT_REPO_URL / GITHUB_TOKEN / GIT_REF / GIT_AUTHOR_NAME/EMAIL / CLAUDE_CREDENTIALS_PATH) を集約 + `~/.claude/.credentials.json` を base64 化、欠落時は `CloudEngineerEnvError` (AppError 派生) で fail-fast。`runEngineerInCloudSandbox(input)` で runClaudeOnRepo を呼出 (verify=fast / autoMergeToMain=true default)。これで service 側で `SAIKYO_ENGINEER_USE_CLOUD_SANDBOX` flag を見て adapter にディスパッチする土台が揃う (実配線は次 iter)。テスト 4 件追加 (573→577) — 各 env 欠落 + credentials 不在 + AppError 互換。typecheck / lint / 66 files 577 tests 緑
- ✅ [iter243] **Cloud sandbox runner — autoMergeToMain でフル自動 α 完成 (queue: Claude on Web 本体完了)**: `buildClaudeRunScript` に `autoMergeToMain` / `commitMessage` / `gitAuthorName` / `gitAuthorEmail` を追加。verify が通った後 (set -e で保証) のみ到達、変更が無ければ no-op、有れば `git add -A` → `git commit -F /tmp/commit-msg.txt` (commitMessage は base64 経由で改行/quote 安全) → `git push origin <ref>`。テスト 5 件追加 (568→573 全パス)。これで iter239-243 で skeleton → hello → claude → verify → push のパイプラインが完結、ユーザ要望「フル自動 (α)」が runner レベルで実現。残: Engineer worker 配線 / custom template (DiD+supabase) / E2B_API_KEY 取得 / CLAUDE.md ルール更新 (FEEDBACK_QUEUE.md に分離)。typecheck / lint / 65 files 573 tests 緑
- 🚧 [iter242] **Cloud sandbox runner — verify steps (typecheck / lint / test) (queue: Claude on Web)**: `buildClaudeRunScript` に `verify: 'none' \| 'fast' \| 'full'` (default 'fast') を追加。claude 実行直後に `corepack enable` → `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm lint` を流し、`full` 時のみ `pnpm test` も追加。`set -euo pipefail` で 1 step でも落ちたら全体 fail-fast。テスト 3 件追加 (568 全パス)、none / fast / full 各 mode で含まれる/含まれない command を assert。実 Supabase が要る Service test は default template だと skip 想定 (custom template + DiD は将来)。次 iter243 で autoMergeToMain 実装でフル自動 (α) 完成。typecheck / lint / 65 files 568 tests 緑
- 🚧 [iter241] **Cloud sandbox runner — git clone + claude CLI 高水準 runner (queue: Claude on Web)**: `runClaudeOnRepo(input)` 高水準 API を追加 (gitRepoUrl + gitRef + githubToken + prompt + claudeCredentialsB64)。`buildClaudeRunScript` を pure 関数で抽出し: (1) `~/.claude/.credentials.json` を base64 から復元 → (2) `npm i -g @anthropic-ai/claude-code` → (3) git clone (oauth2:$GITHUB_TOKEN 埋込) + checkout → (4) prompt を base64 経由で `claude --print` 実行 → (5) `git status --porcelain` ログ。`set -euo pipefail` で fail-fast、prompt は base64 → tmpfile で改行/クォート/日本語に安全。テスト 5 件追加 (565 全パス)、env / git URL / prompt encoding / branch checkout / config error path を網羅。次 iter242 で typecheck/lint/test verify、iter243 で main push。typecheck / lint / 65 files 565 tests 緑
- 🚧 [iter240] **Cloud sandbox runner — Sandbox.create + hello world + log capture (queue: Claude on Web)**: skeleton から実装段階に移行。`Sandbox.create({ apiKey, timeoutMs, envs })` で microVM 起動、`commands.run(script)` で任意 bash 実行、stdout/stderr を head/tail (4KiB) で集約、try/finally で `sandbox.kill()` 確実呼出。`E2B_API_KEY` 未設定時は `CloudSandboxConfigError` (AppError 派生) を throw。`cloud-sandbox-runner.test.ts` で env validation path を 2 ケース検証 (560 tests 全パス)。次 iter241 で git clone + claude CLI 配線、iter242 で verify、iter243 で main push。typecheck / lint / 65 files 560 tests 緑
- 🚧 [iter239] **Cloud sandbox Engineer/Researcher runner — skeleton (queue: Claude on Web)** (ユーザ要望: 「リモート化今すぐ・フル自動 (α)・main 直 push/merge」、Engineer の local subprocess 不安定 (sleep / OOM) を解消): `pnpm add e2b` + `src/lib/agent/cloud-sandbox-runner.ts` skeleton (CloudSandboxInput / CloudSandboxOutput types + `runViaCloudSandbox` 関数 signature)。実装は次 iter 以降段階追加 (iter240 Sandbox.create hello、iter241 git clone + claude CLI、iter242 verify、iter243 push/merge)。Auth 案: `~/.claude/.credentials.json` を base64 で env 注入。Custom template (DiD + supabase CLI + playwright) で e2e 含む全 verify を sandbox 内で完結予定。typecheck / lint / 64 files 558 tests 緑
- ✅ [iter238] **ItemEditDialog 楽観ロック Conflict UX 改善 — Linear / Asana 風 banner** (旧 UX は Description に「別端末からの変更があると Conflict」と書くだけで、save 押下後の toast.error で初めて気付く受動的だった。Notion/GDocs は CRDT、Linear/Asana は Realtime banner で事前警告): dialog open 時の `item.version` を state でキャプチャ、`item.version !== initialVersion` で `externallyChanged` を検知 (Realtime で更新が来ると判定)、role="alert" の amber banner を Tabs の上に挿入 (「他の人がこの Item を編集しました / version N → M」)、「最新を読み込み」 button で confirm + local 編集を破棄して server snapshot で再 sync。これで save 前に conflict を可視化、ユーザはどちらの編集を残すか選べる。typecheck / lint / 64 files 558 tests 緑、Playwright iter238 conflict-banner smoke 同梱
- ✅ [iter237] **Templates / Integrations / TimeEntries EmptyState にも CTA 展開** (iter234-236 同パターンを残り 3 panel に展開、これで主要 8 view すべての empty state が「作成フォームへ」 CTA を持つ統一 UX に): templates-panel → #tmpl-name、integrations-panel → #src-name、time-entries-panel → #teDate を focus + scrollIntoView。typecheck / lint / 64 files 558 tests 緑、Playwright iter237 empty-cta-3 smoke 同梱
- ✅ [iter236] **Sprint / Goal / Workflow EmptyState にも CTA 展開** (iter234/235 同パターンを 3 つの settings panel の empty state に展開): SprintsPanel / GoalsPanel / WorkflowsPanel の `〜がありません` empty state に「作成フォームへ」 CTA button を追加し、各 form の最初の input id (sprint-name / goal-title / wf-name) を focus + scrollIntoView。typecheck / lint / 64 files 558 tests 緑、Playwright iter236 empty-cta-2 smoke 同梱
- ✅ [iter235] **Inbox + items-board ルート empty state にも CTA 展開** (iter234 同パターンを残り 2 empty state に展開、どの view に居ても「次に何をすべきか」が一発で分かる UX に統一): Inbox view の `Inbox は空です` と items-board ルートの `まだ Item がありません` にそれぞれ「クイック追加にフォーカス (q)」 CTA button を追加。typecheck / lint / 64 files 558 tests 緑、Playwright iter235 empty-cta smoke 同梱
- ✅ [iter234] **Today view empty state に「クイック追加にフォーカス (q)」CTA button** (旧仕様で empty state は説明文だけで、ユーザは Tab / scroll で QuickAdd まで自力移動する必要があった、Todoist / TickTick は中央 CTA で即誘導): EmptyState `action` slot に button を追加し click で `#quick-add-input` を focus + scrollIntoView、aria-label「クイック追加入力欄にフォーカス (q キーでも可)」。typecheck / lint / 64 files 558 tests 緑、Playwright iter234 today-empty-cta smoke 同梱
- ✅ [iter233] **QuickAdd parser に「今週末」「月末」追加** (Todoist の "this weekend" / "end of month" 相当を日本語で): 今週末 = 今週土曜 (今日が土曜なら今日)、月末 = 当月最終日 (next month の 0 日 trick で計算)。テスト 3 件追加 (555→558 全パス)、QuickAdd hint にも反映。typecheck / lint / 64 files 558 tests 緑、Playwright iter233 quick-add-weekend smoke 同梱
- ✅ [iter232] **QuickAdd で MUST 入力時に inline warning + create disabled** (旧仕様で MUST 入力 → Enter → toast.error「DoD 必須」の失敗 path だった): preview.isMust=true 時に作成 button を disabled、aria-label に「MUST タスクは編集ダイアログから DoD を入力して作成してください」、inline alert chip「⚠ 編集ダイアログで DoD を入れてください」を role="alert" で SR が即座に読み上げ。「Enter → toast.error」の無駄を解消、押す前にやるべきことが視覚 + SR で分かる。typecheck / lint / 64 files 555 tests 緑、Playwright iter232 quick-add-must smoke 同梱
- ✅ [iter231] **QuickAdd 末尾 `?` で AI 分解 fire-and-forget 起動 — Phase 1.4 完成** (UX plan §1.4 で「create_item + enqueueResearcher」設計だったが long pending、toast に「Phase 2 で配線予定」と書かれた未完成機能だった): useCreateItem の戻り値から item.id を取得し useDecomposeItem (Claude CLI 経由) を fire-and-forget で起動。即時 toast「Researcher が分解中…」、完了 toast (提案 N 件 / 作成 N 件 / fallback)、失敗 toast.warning (作成は成功)。これで `?` suffix が hot path として完結。typecheck / lint / 64 files 555 tests 緑、Playwright iter231 quick-add-decompose smoke 同梱
- ✅ [iter230] **QuickAdd parser に Todoist 風 +Nd / +Nw 相対日付** (Todoist は `+3d` で「3 日後」を高速指定できるが、saikyo-todo は 明日/明後日/来週X曜/ISO のみで数日後/週後を直接書けなかった): nl-parse.ts に `+Nd` (N 日後) / `+Nw` (N 週後 = N\*7 日) を追加。先頭/空白後限定で title 中 '+' との誤認防止、日付 token が先に消費されたら無視 (先勝ち)。テスト 5 件追加 (550→555 全パス)、QuickAdd hint 文にも反映。typecheck / lint / 64 files 555 tests 緑、Playwright iter230 quick-add-rel smoke 同梱
- ✅ [iter229] **Comment 編集 textarea で Cmd/Ctrl+Enter 保存 + Esc 破棄** (iter228 は投稿側のみ、編集 textarea も同 UX 揃え): Cmd/Ctrl+Enter で handleSave、Esc で `setBody(comment.body)` + `setEditing(false)` で編集破棄。Esc は radix Dialog 全体を閉じる挙動と衝突しないよう stopPropagation で止める (編集中だけ)。aria-label にも明示。typecheck / lint / 64 files 550 tests 緑、Playwright iter229 comment-edit-keys smoke 同梱
- ✅ [iter228] **Comment thread で Cmd/Ctrl+Enter 投稿** (Slack / GitHub / Notion 標準のキーボード投稿が無く Tab 移動が必要だった): Textarea の onKeyDown で meta/ctrl+Enter を拾って handlePost、IME 変換中 / 空 / pending は no-op、placeholder と aria-label にも「Cmd/Ctrl+Enter で投稿」を明示。typecheck / lint / 64 files 550 tests 緑、Playwright iter228 comment-cmd-enter smoke 同梱
- ✅ [iter227] **ItemEditDialog で Cmd/Ctrl+S 保存ショートカット** (Todoist / TickTick / Notion / Linear 標準のキーボード保存が無く、編集 → 保存ボタンまで Tab 移動が必要で効率劣勢だった): useEffect で keydown 監視 (open 時のみ)、meta/ctrl+s で handleSave、isPending / title 空時 no-op、IME 変換中無視。keybindings.ts に「Cmd+S / Ctrl+S — Item 編集ダイアログで保存」登録で ? ヘルプモーダルに自動表示。typecheck / lint / 64 files 550 tests 緑、Playwright iter227 cmd-s smoke 同梱
- ✅ [iter226] **Workflow Editor の trigger / node preset button を SR 化** (iter156-157 で 4+6 preset button を追加したが title 属性のみで SR 不可視、node preset の aria-label は `${preset.type} node を追加` だけで preset.title の context が落ちていた): 4 trigger preset (manual / cron / item-event / webhook) に title と同等の context を aria-label に二重記述、6 node preset の aria-label を `graph に ${preset.title} の skeleton node を追加` に拡張。typecheck / lint / 64 files 550 tests 緑、Playwright iter226 wf-presets smoke 同梱
- ✅ [iter225] **Gantt 「今日へジャンプ」 button に aria-label** (旧仕様で title のみ、SR は「今日へジャンプ」 visible text だけで「どこにジャンプするのか」「ページ遷移なのか scroll なのか」が不明だった): aria-label に「Gantt timeline を今日の縦線まで横スクロール」を付与。typecheck / lint / 64 files 550 tests 緑、Playwright iter225 gantt-jump smoke 同梱
- ✅ [iter224] **Budget / SprintDefaults edit toggle + Budget save button の SR 化** (iter180-223 同パターンを残り 3 button に展開、edit-toggle 系 button は context が固定文字「編集」「上限を変更」だけで何の編集か SR 不可視だった): budget「上限を変更」に「AI 月次コスト上限と警告閾値の編集モードを開く」、sprint defaults「編集」に現在値を含めた context aria-label、budget「保存」に pending 状態別文言を付与。typecheck / lint / 64 files 550 tests 緑、Playwright iter224 edit-toggles smoke 同梱
- ✅ [iter223] **TimeEntries Sync + Create + QuickAdd 作成 button の SR 化** (iter180-222 同パターンを残り 3 button に展開): time-entries-table Sync button (pending / 通常 + 再 Sync 区別)、create-time-entry-form 記録 button (pending / 通常)、quick-add 作成 button (!preview.title / pending / 通常 + preview.title を含む dynamic context) の aria-label を状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter223 time-quick smoke 同梱
- ✅ [iter222] **ItemEditDialog の baseline 記録/更新/クリア button に aria-label** (旧仕様で baseline 系 2 button は aria-label 無く SR は「ベースライン記録」だけ聞き、item / 旧 baseline 値 / pending 状態の context が伝わらなかった、title 属性は mouse hover 専用): set-baseline は pending / 更新 (旧 baseline 値含む) / 初回記録 の 3 状態別文言、clear-baseline は pending / 通常 (現 baseline 値含む) の 2 状態別文言を新規付与。typecheck / lint / 64 files 550 tests 緑、Playwright iter222 baseline-buttons smoke 同梱
- ✅ [iter221] **Sprint 期間保存 + Archived 復元 button の pending SR 化** (iter180-220 同パターンを残り 2 button に展開): Sprint 期間保存 button + Archived items 復元 button の aria-label を pending / 通常 で 2 状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter221 misc-pending smoke 同梱
- ✅ [iter220] **Workflow rerun + Template 削除 button の pending SR 化** (iter180-219 同パターンを残り 2 button に展開): WorkflowRunHistory rerun button の aria-label を `trigger.isPending` / 通常 で 2 状態別文言、TemplatesPanel Template 削除 button に `disabled={deleteMut.isPending}` + pending 状態別 aria-label + Trash2 icon に aria-hidden 補完。typecheck / lint / 64 files 550 tests 緑、Playwright iter220 rerun-template smoke 同梱
- ✅ [iter219] **Goal ステータス変更 5 button の pending SR 化** (iter218 同パターンを Goal status 変更 (active→completed / active→archived / completed→active / completed→archived / archived→active) に展開): 各 button の aria-label を pending / 通常 で 2 状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter219 goal-status smoke 同梱
- ✅ [iter218] **Sprint ステータス変更 4 button の pending SR 化** (iter196/215-217 同パターン: 稼働開始 / 完了 / 計画に戻す / 中止 button が changing 中の固定 aria-label で SR は「変更中…」を聞き取れなかった): aria-label を pending / 通常 で 2 状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter218 sprint-status smoke 同梱
- ✅ [iter217] **IntegrationsPanel SourceCard 有効化/無効化 + 削除 button の pending SR 化** (iter195 で Pull button、iter216 で WorkflowCard 同 2 button を SR 化したが、SourceCard 同パターンが残っていた): aria-label を pending / 通常 で 2 状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter217 source-buttons smoke 同梱
- ✅ [iter216] **WorkflowCard 有効化/無効化 + 削除 + editor 保存 button の pending SR 化** (iter180-215 同パターンを WorkflowCard / WorkflowEditorDialog の 3 button に展開): 有効化/無効化 button (pending / 通常)、削除 button (pending / 通常)、editor 保存 button (saving / 通常 + wf.name を含めた context) の aria-label を 2 状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter216 wf-buttons smoke 同梱
- ✅ [iter215] **Comment 編集保存 + TeamContext 保存 button の SR 化** (iter180-214 同パターンを残り 2 save button に展開): comment 編集保存 button (body 空 / pending / 通常) と team-context 保存 button (!dirty / pending / 通常) の aria-label を 3 状態別文言に切替え。「コメントを保存するには本文を入力してください」「チームコンテキストに変更がないため保存不要」と SR が disabled 理由を伝達。typecheck / lint / 64 files 550 tests 緑、Playwright iter215 comment-team smoke 同梱
- ✅ [iter214] **Comment / Template item 削除 button の pending SR 化 + 連打防止** (iter180-213 同パターン: 削除系 button が disabled / pending を持たず SR は「削除中…」を聞き取れず race も起こし得た): comment-thread 削除/編集 button に `disabled={softDelete.isPending}` を伝搬 + 削除 button の aria-label を pending 状態別、template-items-editor 削除 button に `disabled={removeMut.isPending}` + pending 状態別 aria-label。typecheck / lint / 64 files 550 tests 緑、Playwright iter214 delete-pending smoke 同梱
- ✅ [iter213] **依存解除 button の pending SR 化 + 連打防止 disabled** (iter180-212 同パターンを ItemDependenciesPanel Section に展開、Section に removing prop を新設): aria-label を pending / 通常 で 2 状態別文言に切替え、`remove.isPending` を Section に伝搬し disabled で連打 race を防止。typecheck / lint / 64 files 550 tests 緑、Playwright iter213 dep-remove smoke 同梱
- ✅ [iter212] **ProposalRow 採用 / 却下 / 保存 button の pending SR 化 + ✓ aria-hidden** (iter180-211 同パターン: 各 button が disabled 時 aria-label 固定で SR 不可視、採用 button の `✓` text 子要素が SR で「check mark」と読み上げる aliasing が残っていた): aria-label を pending / 通常 で状態別文言に切替え、採用 button の `✓` を `<span aria-hidden>` で wrap、編集保存 button に proposal.title を含めた context 付与。typecheck / lint / 64 files 550 tests 緑、Playwright iter212 proposal-row smoke 同梱
- ✅ [iter211] **KR 削除 button の pending SR 化 + ✕ aria-hidden** (旧仕様で KR 行の削除 button はテキスト ✕ を直接子要素に持ち、aria-label は付いていたが pending 状態 (削除中…) は固定文言だった): aria-label を pending / 通常 で 2 状態別文言に切替え、✕ を `<span aria-hidden="true">` で wrap し SR aliasing を防止。typecheck / lint / 64 files 550 tests 緑、Playwright iter211 kr-delete smoke 同梱
- ✅ [iter210] **ItemDependenciesPanel 依存追加 form の label 関連付け** (iter209 同パターン: 「依存を追加」 Label が htmlFor 無しで配下 2 select との関連付けが無かった): 親 div に `role="group"` + `aria-labelledby="dep-add-label"`、Label に `id="dep-add-label"` を付け、SR が「依存を追加 グループ」を確立してから select / button を読む semantic に。typecheck / lint / 64 files 550 tests 緑、Playwright iter210 dep-add-label smoke 同梱
- ✅ [iter209] **ItemEditDialog の AssigneePicker / TagPicker に label 関連付け** (旧仕様で 担当者 / タグ の `<Label>` は htmlFor 無しで Popover trigger Button (内部で aria-label を持つ) との関連付けが無かった、SR は Popover label のみ読みセクション context を欠いた): 親 div を `role="group"` + `aria-labelledby` で Label の id に紐付け、SR が「担当者 グループ」の context を確立してから Popover button を読む semantic に。typecheck / lint / 64 files 550 tests 緑、Playwright iter209 picker-labels smoke 同梱
- ✅ [iter208] **BulkCheckbox / BulkHeaderCheckbox の SR 識別性向上** (旧仕様で BulkCheckbox は `aria-label="選択"` 固定、BulkHeaderCheckbox は `aria-label="全選択"` 固定で、Backlog 50 行で SR は同じ「選択」を 50 回読み上げて行識別不能・件数不明だった): BulkCheckbox に itemTitle prop を追加し「『〜』を一括操作の対象に追加 / 対象から外す」と行毎にユニーク化、BulkHeaderCheckbox は rowIds.length と current state で「現ページ N 行をすべて〜」を切替え。backlog-view は title を渡すよう更新。typecheck / lint / 64 files 550 tests 緑、Playwright iter208 bulk-checkbox smoke 同梱
- ✅ [iter207] **BulkActionBar status / delete button の pending SR 化** (iter167 / 180-205 で他 bulk button は整えたが、bulk-action-bar.tsx の status 変更 (各 status 毎) と 削除 button が pending 中固定文言で SR 不可視だった): aria-label を pending 状態別文言「選択 N 件のステータスを変更中…」「選択 N 件を soft delete 中…」に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter207 bulk-pending smoke 同梱
- ✅ [iter206] **DashboardView の バーンダウン / MUST 一覧 Card に landmark + icon aria-hidden** (iter191-193 region/aria-label パターンを Dashboard に展開、加えて Flame/AlertTriangle icon の aliasing 解消): バーンダウン Card に `role="region"` + aria-label「MUST Item の バーンダウン グラフ (直近 14 日)」、MUST 一覧 Card に「MUST Item 一覧 N 件」を付与、CardTitle の Flame icon と WIP 警告の AlertTriangle に aria-hidden を付け SR が「fire / warning」と読み上げる aliasing を解消。typecheck / lint / 64 files 550 tests 緑、Playwright iter206 dashboard-region smoke 同梱
- ✅ [iter205] **NotificationBell 全て既読 + Item 依存追加 button の SR 化** (iter194-204 同パターンを残り 2 button に展開): notification-bell「全て既読」 (unreadCount=0 disabled / pending / 通常) と item-dependencies-panel「追加」 (!pickId / pending / 通常) の aria-label を状態別文言に。「未読通知がないため既読化不要」「依存を追加するには対象 Item を選択してください」と SR が disabled 理由を伝達。typecheck / lint / 64 files 550 tests 緑、Playwright iter205 misc-buttons smoke 同梱
- ✅ [iter204] **子タスク bulk / Sprint デフォルト / Personal-period ゴール 保存 button の SR 化** (iter194-203 同パターンを残り 3 button に展開): 子タスク bulk 追加 button (空 / pending / 件数) / Sprint デフォルト保存 button / Personal-period ゴール保存 button (!dirty / pending / 通常) の aria-label を状態別文言に。Personal-period は `!dirty` で「変更がないため保存不要」を SR に伝達。typecheck / lint / 64 files 550 tests 緑、Playwright iter204 save-buttons smoke 同梱
- ✅ [iter203] **Workflow / Source / Comment 投稿 button の SR 化** (iter202 同パターンを残り 3 form の create button に展開): WorkflowsPanel Workflow「作成」 / IntegrationsPanel Source「作成」 / CommentThread コメント「投稿」 の aria-label を空入力 disabled / pending / 通常 の 3 状態別文言に切替え。これでアプリ全体の主要 create / submit button が SR で「なぜ disabled なのか」を聞き取れる semantic に統一。typecheck / lint / 64 files 550 tests 緑、Playwright iter203 create-buttons2 smoke 同梱
- ✅ [iter202] **Goal / KR / Sprint 新規作成 button の SR 化** (iter194-201 同パターンを 3 つの作成 form に展開): GoalsPanel Goal「作成」 / KeyResultList「KR 追加」 / SprintsPanel Sprint「作成」 の aria-label を空入力 disabled 理由 / pending / 通常 の 3 状態別文言に切替え。「Goal を作成するにはタイトルを入力してください」と SR が空入力時の disabled 理由を伝達。typecheck / lint / 64 files 550 tests 緑、Playwright iter202 create-buttons smoke 同梱
- ✅ [iter201] **Templates Panel / Items Editor / Instantiate Form の submit button SR 化** (iter194-200 同パターンを Template 系 3 form に展開): templates-panel「作成」 button / template-items-editor「+ 追加」 button / instantiate-form「即実行」 button の aria-label を 2-3 状態別文言 (空入力 disabled / pending / 通常) に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter201 template-buttons smoke 同梱
- ✅ [iter200] **ItemEditDialog footer Save / Archive / Unarchive button の SR 化** (iter194-199 同パターンを ItemEditDialog footer に展開、200 iter milestone): Save は title 空 / pending / 通常 の 3 状態別文言に、Archive / Unarchive は pending / 通常 の 2 状態別文言 + item title を含めた context に切替え。「保存するにはタイトルを入力してください」と SR が disabled 理由を伝達。typecheck / lint / 64 files 550 tests 緑、Playwright iter200 edit-dialog-buttons smoke 同梱
- ✅ [iter199] **DecomposeProposalsPanel の bulk button + cancel button の pending SR 化** (iter180 で 全て採用/全て却下/中止 button の aria-label を整えたが、pending 状態は固定文言のままで SR は「採用中…」「却下中…」「中止中…」を聞き取れなかった。中止 button は元々 aria-label 無し): pending 状態別文言に切替え、中止 button にも新規 aria-label。typecheck / lint / 64 files 550 tests 緑、Playwright iter199 proposals-pending smoke 同梱
- ✅ [iter198] **Engineer / Stand-up / Heartbeat button の pending SR 化** (iter194-197 同パターン: 3 button が pending 中に disabled になるが aria-label が固定文言で SR は「実行中…」が伝わらなかった): EngineerTriggerButton / StandupButton / HeartbeatButton の aria-label を pending 状態別文言に切替え。typecheck / lint / 64 files 550 tests 緑、Playwright iter198 header-buttons smoke 同梱
- ✅ [iter197] **ItemDecomposeButton / ItemResearchButton の disabled 理由 SR 化** (iter194-196 同パターン: 完了済 item / pending 中で disabled になるが aria-label が固定文言で SR ユーザに「なぜ disabled なのか」が伝わらなかった): 3 状態別文言 (done / pending / 通常) に切替え。Backlog 行から 1 click できる主要 AI button の文脈を SR 化。typecheck / lint / 64 files 550 tests 緑、Playwright iter197 item-ai-buttons smoke 同梱
- ✅ [iter196] **Sprint 振り返り / Pre-mortem button の pending SR 化** (iter194/195 同パターン: 2 つの button が pending で disabled になるが、aria-label が固定文言で SR は「生成中」が伝わらなかった): retroPending / premortemPending 状態に応じて aria-label を切替え。Pre-mortem は更に sprint.premortemGeneratedAt 有無で 「再生成 / 初回生成」も識別。typecheck / lint / 64 files 550 tests 緑、Playwright iter196 sprint-pending smoke 同梱
- ✅ [iter195] **IntegrationsPanel Pull / GoalCard AI 分解 button の disabled 理由 SR 化** (iter194 で WorkflowCard 実行 button を 4 状態別 aria-label にしたが、同パターンの disabled button が 2 件残っていた): IntegrationsPanel Pull button (!src.enabled / pending) と GoalCard AI 分解 button (status !== 'active' / pending) の aria-label を状態別文言に切替え、title 属性のみで mouse hover でしか分からなかった disabled 理由を SR で識別可能化。typecheck / lint / 64 files 550 tests 緑、Playwright iter195 disabled-buttons smoke 同梱
- ✅ [iter194] **WorkflowCard 実行 button の disabled 理由を SR で識別可能化** (旧仕様で `!wf.enabled || trigger.isPending || nodeCount===0` で disabled になるが、`title` 属性は mouse hover 専用で SR ユーザに「なぜ disabled なのか」が伝わらなかった): aria-label を 4 状態別文言 (無効化中 / node 無し / 実行中… / 通常) に切替え、disabled でも SR で context を識別可能に。typecheck / lint / 64 files 550 tests 緑、Playwright iter194 wf-run-state smoke 同梱
- ✅ [iter193] **TimeEntries CardTitle / Kanban 列 sr-only span の paren narration 解消** (iter191/192 で Today/PersonalPeriod/Subtasks の paren 冗長 narration を解消したが、TimeEntriesPanel の "一覧 (N 件)" CardTitle と Kanban 列の sr-only " (N 件)" に同パターンが残っていた): TimeEntries は CardTitle を二重 span (sr-only "一覧 N 件" / aria-hidden "一覧 (N 件)") 化、Kanban sr-only から paren を撤廃し " N 件" のみに。typecheck / lint / 64 files 550 tests 緑、Playwright iter193 paren-cleanup smoke 同梱
- ✅ [iter192] **PersonalPeriod Card / Subtasks h3 の paren count SR cleanly 化** (iter191 で Today view 各グループ Card の paren 冗長 narration を region+aria-label で解消したが、PersonalPeriodView の "{label}の Item ({N})" Card と ItemEditDialog Subtasks の "既存の子タスク ({N})" h3 が同パターンで残っていた): PersonalPeriod Card は role="region"+aria-label に、Subtasks h3 は二重 span (sr-only に "N 件" / aria-hidden に "(N)") で SR は "N 件"、視覚は paren 表記を維持。typecheck / lint / 64 files 550 tests 緑、Playwright iter192 region-paren smoke 同梱
- ✅ [iter191] **Today view 各グループ Card に role="region" + aria-label** (旧仕様で「期限超過 (3)」「今日 (5)」等の Card は単なる div で landmark にならず、SR は「期限超過 left paren 3 right paren」と読み上げ context 把握も skip 移動もできなかった): 各 Card に `role="region" + aria-label="<label> N 件"` を付け、SR が landmark として認識し「期限超過 3 件 region」と読み上げる semantic に。typecheck / lint / 64 files 550 tests 緑、Playwright iter191 today-region smoke 同梱
- ✅ [iter190] **ItemEditDialog の DoD 必須を可視化 (UX/SR 両対応)** (旧仕様で isMust=true 時に DoD 入力欄が出るだけで `required` も視覚マーカーも無く、Service 層 `MUST には DoD が必要です` ValidationError で初めて気付く UX だった): Label 末尾に視覚 `*` マーカー (aria-hidden)、input に `required + aria-required="true" + aria-describedby="editDod-hint"` を付け、直下に「MUST タスクは DoD が必須」短ヒントを紐付け。SR は「DoD 完了条件, 必須, MUST タスクは…」と一文で読み上げ。typecheck / lint / 64 files 550 tests 緑、Playwright iter190 dod-required smoke 同梱
- ✅ [iter189] **ItemEditDialog の Key Result (OKR) 選択 select を optgroup (Goal 毎) 化** (旧仕様で flat list に `[Goal Title] KR Title` 形式で表示し、同 Goal の KR を 5 個並べると SR が Goal Title を 5 回繰り返し読み上げる冗長な gap、iter188 同パターン): `<optgroup label="Goal: ...">` で goal ごとに 1 group にまとめ、SR が group 名を 1 回読み上げて配下 KR を列挙する semantic に。typecheck / lint / 64 files 550 tests 緑、Playwright iter189 kr-optgroup smoke 同梱
- ✅ [iter188] **ItemEditDialog の Sprint 選択 select を optgroup ("稼働中" / "計画中") 化** (旧仕様で active な Sprint だけ option text 先頭に `★ ` を付けて視覚区別していたが、SR は「black star, My Sprint」と読み上げ意味不明だった): `<optgroup label="稼働中">` / `<optgroup label="計画中">` で 2 group に分け、SR が group 名 (status) を先に読み上げて context を確立する semantic に。typecheck / lint / 64 files 550 tests 緑、Playwright iter188 sprint-optgroup smoke 同梱
- ✅ [iter187] **items-board フィルタ件数の SR 自動読み上げ** (status / sprint / MUST フィルタを切替えても "{N} 件" span が静的レンダリングのみで、SR ユーザは件数変化を毎回 Tab して読み戻す必要があった): `<span>` に `role="status" + aria-live="polite" + aria-atomic="true" + aria-label="現在のフィルタ条件で N 件"` を付け、フィルタ切替時に SR が件数を自動読み上げ。typecheck / lint / 64 files 550 tests 緑、Playwright iter187 filter-count smoke 同梱
- ✅ [iter186] **Today empty state 🎉 と QuickAdd preview 🧠 の SR 漏れ補正** (iter178 で ItemEditDialog 内の emoji は aria-hidden 化したが、Today view の `"今日のタスクはありません 🎉"` 見出しと QuickAdd preview chip の `"🧠 AI 分解"` が残って SR で「party popper」「brain」と読み上げ): Today は `EmptyState` の icon prop に 🎉 を `<span aria-hidden="true" className="text-3xl">` で分離 (タイトル本体は emoji 抜き)、QuickAdd は 🧠 を `<span aria-hidden="true">` で wrap。typecheck / lint / 64 files 550 tests 緑、Playwright iter186 emoji-a11y smoke 同梱
- ✅ [iter185] **BudgetPanel の月次 AI コスト消費率バーを role="progressbar" 化** (iter91 で goals / sprints の進捗バーは progressbar pattern を実装済だが、Phase 6.9 で追加した budget-panel.tsx の月次コスト消費率バーは role 無し / 警告閾値ライン側だけ aria-label が付いて SR で 2 個読み上げ冗長な gap): バー親要素に `role="progressbar" + aria-valuenow={ratioPct} + aria-label="AI 月次コスト消費率 N% (警告閾値 M%)"` を付与し、子の塗りつぶし div と閾値ラインは `aria-hidden="true"` で SR 重複を解消。typecheck / lint / 64 files 550 tests 緑、Playwright iter185 budget-progress smoke 同梱
- ✅ [iter184] **5 view 全部の MUST badge に role="img" + aria-label を一括付与** (iter140 / 151 / 179 で proposal / template-items / dependencies の MUST には付けたが、Today / Inbox / Kanban / Personal-period / Gantt の MUST badge は visual only で SR 不可視だった gap): 5 view すべての `<span>MUST</span>` に `role="img" + aria-label="MUST item"` を付与し、item title の右の小バッジが SR で識別できるよう統一。typecheck / lint 緑、Playwright iter184 must-badges smoke 同梱
- ✅ [iter183] **共通 StatusBadge component の SR 識別 (Today/Inbox/Kanban/Backlog/Personal-period 一括反映)** (Item view で使われる共通 StatusBadge が aria-label 無く、SR で「TODO」「進行中」「完了」と読まれても何のステータスか item title から離れて配置されると context 不明だった): `<Badge>` に `aria-label="ステータス: <label>"` を付与。1 ファイル変更で 5 view 全部に効く (today / inbox / kanban / backlog / personal-period)。typecheck / lint 緑、Playwright iter183 status-badge smoke 同梱
- ✅ [iter182] **WorkspaceHeader role / SprintCard status / GoalCard status Badge の SR 識別** (status を表示する Badge が text のみで何のステータスか SR で context 不明だった): WorkspaceHeader 役割 Badge に `aria-label="あなたの workspace role: <role>"`、SprintCard status Badge に `aria-label="Sprint「<name>」のステータス: <ラベル>"`、GoalCard status Badge に `aria-label="Goal「<title>」のステータス: <ラベル>"` を付与。typecheck / lint 緑、Playwright iter182 status-badges smoke 同梱
- ✅ [iter181] **追加分解 / やり直し button の SR 識別 a11y** (iter180 で 全て採用 / 全て却下 / 中止 / icon を整えたが、追加分解 / やり直し button が text のみで件数を含んだ動作 context が SR に伝わらない gap): 追加分解 button に動的 `aria-label="既存の保留中 N 件を残して追加で AI 分解" / "AI 分解を再実行"`、やり直し button に `aria-label="保留中の N 件を全て却下してから AI 分解をやり直し"` を付与。typecheck / lint 緑、Playwright iter181 redecompose smoke 同梱
- ✅ [iter180] **decompose-proposals-panel の bulk button SR 識別 + icon aria-hidden** (旧仕様で「全て採用」「全て却下」「中止」「追加分解」button が text のみで件数 / icon 二重読み上げの context 無し): 全て採用 / 全て却下 button に動的 `aria-label="保留中の提案 N 件をすべて採用 / 却下"`、中止 button の `<X>` icon と 追加分解 / 再分解 button の `<RotateCw>` icon に `aria-hidden="true"` (iter167 BulkActionBar / iter170 同パターン)。typecheck / lint 緑、Playwright iter180 proposals-bulk smoke 同梱
- ✅ [iter179] **item-dependencies-panel の ⚠ MUST マーカー SR 化** (iter178 で ItemEditDialog の 🧠/🛠 emoji を aria-hidden 化したが、依存 tab の MUST item を示す `⚠` が visual only で SR 不可視だった gap、iter140 同パターン): 候補 `<option>` に動的 `aria-label="MUST: <title> (<status>)"` を付与 (option は子要素受け付けないので option 全体の名前で代替)、依存リスト `<span>` 内の `⚠` を `<span role="img" aria-label="MUST item">` で wrap し emoji 自体は `aria-hidden`。typecheck / lint 緑、Playwright iter179 deps-must smoke 同梱
- ✅ [iter178] **ItemEditDialog 装飾 emoji aria-hidden + Engineer trigger button SR 識別** (旧仕様で「🧠 AI で分解」「🛠 Engineer に実装させる」見出しと Engineer button text に emoji 直書きで SR が「brain」「hammer」と読み上げ): emoji を `<span aria-hidden="true">` で wrap、Engineer trigger button に `aria-label="Engineer Agent に「<title>」を実装させる<autoPr 文言>"` を付与し SR で対象 item + 設定を識別可能化。typecheck / lint 緑、Playwright iter178 emoji-headings smoke 同梱
- ✅ [iter177] **残り 4 component の loading/empty SR semantic 一括付与** (iter161 / 168 / 171 / 176 で網羅した async-states / workflow / integrations / activity-log の漏れを最後に一括解消): item-dependencies-panel / archived-items-panel / item-edit-dialog (Subtasks loading + empty) / kanban-view (列定義 loading) の 5 箇所の `<p>` に `role="status" + aria-live="polite"` (loading) / `role="status"` (empty) を一括付与。これでアプリ全体の loading/empty inline `<p>` が SR で読み上げられる。typecheck / lint 緑、Playwright iter177 misc-states smoke 同梱
- ✅ [iter176] **ActivityLog の loading/error/empty SR semantic** (iter161 / 168 / 171 同パターンを ActivityLog (ItemEditDialog Activity tab) にも展開): 3 つの `<p>` (loading / error / empty) に `role="status" + aria-live="polite"` (loading) / `role="alert"` (error) / `role="status"` (empty) を付与。SR で「読み込み中」「Activity の取得に失敗」「権限不足 / 記録なし」が自動読み上げ。typecheck / lint 緑、Playwright iter176 activity-log smoke 同梱
- ✅ [iter175] **SourceImportHistory 行の SR 識別 a11y** (iter171 で history loading/empty に role 付けたが、各 row 内の "f=N / c=N / u=N" abbreviation と error span が SR で意味不明だった gap、iter133 sync-error 同パターン): "f=N / c=N / u=N" カウンタ span に `aria-label="fetched N / created N / updated N"` を付与、error span に `aria-label="Pull エラー: <msg>"` + `role="alert"` を付与 (旧 title 属性のみで SR 不可視)。typecheck / lint 緑、Playwright iter175 import-row smoke 同梱
- ✅ [iter174] **ItemDecomposeButton / ItemResearchButton の SR 識別 a11y** (Backlog / Kanban の action 列に並ぶ「AI 分解」「AI 調査」button が text のみで item title を含む aria-label が無く、複数 item を SR で巡回するときに対象不明、iter133 / 144 同パターン): `aria-label="「<title>」を AI 分解 (子タスクを 3〜5 件作成)"` / `aria-label="「<title>」を AI 調査して Doc を作成"` を付与。typecheck / lint 緑、Playwright iter174 item-action-buttons smoke 同梱
- ✅ [iter173] **sprints-panel の装飾 icon aria-hidden 一括付与** (iter170 / 171 / 172 と同パターンを sprint card にも適用): CalendarRange / Play / CheckCircle / Pause / X / Sparkles の 6 lucide icon すべてに `aria-hidden="true"` を sed で一括付与 (button の text と iter150 で付けた aria-label に SR の意味は集約済なので二重読み上げを抑止)。typecheck / lint 緑、Playwright iter173 sprint-icons smoke 同梱
- ✅ [iter172] **goals-panel の装飾 icon aria-hidden + AI 分解 button の SR 識別** (iter170 / 171 同パターンを goals にも展開): GoalCard の `<ChevronDown/Right>` (toggle)、AI 分解の `<Sparkles>`、KR 追加の `<Plus>` に `aria-hidden="true"`。AI 分解 button に `aria-label="Goal「<title>」を AI 分解 (5〜10 件の Item を作成)"` を付与し SR で対象 goal 識別可能化 (iter133 / 144 同パターン)。typecheck / lint 緑、Playwright iter172 goals-icons smoke 同梱
- ✅ [iter171] **integrations-panel の icon aria-hidden + button SR 識別 + import history role** (iter170 / iter168 同パターンを integrations-panel に展開): SourceCard の Play / ChevronDown / ChevronRight / Trash2 icon に `aria-hidden="true"`、Pull / 有効化 / 無効化 button に動的 `aria-label="Source「<name>」を Pull / 有効化 / 無効化"` を付与 (複数 source 巡回での SR 識別)、SourceImportHistory の loading=`role="status" + aria-live="polite"` / error=`role="alert"` / empty=`role="status"`。typecheck / lint 緑、Playwright iter171 integrations-icons smoke 同梱
- ✅ [iter170] **workflow-panel の装飾 icon aria-hidden 一括付与 + 有効化/無効化 button の SR 識別** (lucide icons (Play / Pencil / ChevronDown/Right / Trash2) が button 内に並ぶが aria-hidden 無し → SR で icon 名が二重読み上げ可能性。「有効化 / 無効化」button は text のみで複数 workflow を SR で巡回するとき対象不明): WorkflowCard の 5 種 icon (Play / Pencil / ChevronDown / ChevronRight / Trash2) と RunHistory の ChevronDown/Right、再実行 button の Play に `aria-hidden="true"` を追加。toggle button (有効化/無効化) に動的 `aria-label="Workflow「<name>」を<有効化|無効化>"` を追加。typecheck / lint 緑、Playwright iter170 workflow-icons smoke 同梱
- ✅ [iter169] **notification-bell の SR semantic 改善** (iter102 で notification item button の aria-label は付与済だが bell 全体の機能 / 装飾 / 状態 SR 表示が未対応): trigger Button に `aria-expanded={open} + aria-haspopup="dialog"`、Bell icon / Badge (件数) / CheckCheck icon に `aria-hidden="true"`、「全て既読」button に動的 `aria-label="未読 N 件をすべて既読にする"`、loading/empty `<div>` に `role="status" (loading は aria-live="polite")`、通知時刻 `<p>` を `<time dateTime>` (ISO 文字列) に変更 (iter102 ItemEditDialog Activity tab / iter165 comment-thread 同パターン)。typecheck / lint 緑、Playwright iter169 notification-bell smoke 同梱
- ✅ [iter168] **WorkflowRunHistory / WorkflowNodeRunsList の loading/error/empty state SR semantic** (iter161 async-states 同パターンを workflow run history / node viewer にも展開): 6 箇所の `<p>` (history loading / error / empty + node-runs loading / error / empty) に `role="status" + aria-live="polite"` (loading) / `role="alert"` (error) / `role="status"` (empty) を一括付与。SR でも実行履歴の状態が自動把握できる。typecheck / lint 緑、Playwright iter168 workflow-states smoke 同梱
- ✅ [iter167] **BulkActionBar の SR a11y** (固定 bottom の一括操作 bar に role 無し / status 変更 button が「X に」のみで件数連携の context が SR に伝わらない gap): bar に `role="region" + aria-label="一括操作 (N 件選択中)"`、status 変更 button に動的 `aria-label="選択 N 件を「X」に変更"`、削除 button に `aria-label="選択 N 件を soft delete"`、解除 button に `aria-label="選択を解除"`、装飾区切り `<div>` に `aria-hidden="true"`。typecheck / lint 緑、Playwright iter167 bulk-action smoke 同梱
- ✅ [iter166] **TagPicker / AssigneePicker の SR a11y** (Item edit dialog の picker 部分が trigger button に aria-label 無く SR で「タグなし / 未アサイン」しか読まれず、popover 開閉も伝わらない gap): 両 picker の trigger Button に動的 `aria-label`「タグを選択 (現在 N 件: foo, bar)」 / 「アサインを選択 (現在 N 件: alice, bob)」+ `aria-expanded={open}` + `aria-haspopup="listbox"` を追加。装飾 icon (TagIcon / UserIcon / CheckIcon / PlusIcon / 色 dot) すべてに `aria-hidden="true"` を付与し SR の二重読み上げを抑止。typecheck / lint 緑、Playwright iter166 pickers smoke 同梱
- ✅ [iter165] **comment-thread の SR semantic 改善** (iter59 / iter164 で中身 button は固めたが loading / empty / AI badge / 投稿時刻 が visual only で SR から見えない gap): 「読み込み中…」`<p>` に `role="status" aria-live="polite"`、「まだコメントはありません」`<p>` に `role="status"`、AI badge に `role="img" + aria-label="AI Agent による投稿"`、投稿時刻を `<span>` から `<time dateTime>` に変更 (iter102 ItemEditDialog Activity tab と同パターン)。typecheck / lint 緑、Playwright iter165 comment-thread smoke 同梱
- ✅ [iter164] **コメント編集 / 削除 button の SR 識別 a11y** (iter59 で comment-thread の textarea aria-label は付与済だが各コメントの「編集」/「削除」button は text のみで複数自分コメントを巡回するときに SR で対象不明だった、iter140 同パターン): 各 button に `aria-label="コメント「<body 30 文字 + …>」を編集/削除"` を追加。30 文字超過時は「…」で省略表示。typecheck / lint 緑、Playwright iter164 comment-actions smoke 同梱
- ✅ [iter163] **フォーム input の `aria-invalid` + `aria-describedby` (WCAG 3.3.1)** (iter160 で error <p> に role="alert" は付けたが input 自体の状態が SR で programmatic に把握できなかった): signup / login / create-workspace の 7 input すべてで validation error 発生時に `aria-invalid="true"` + `aria-describedby="<error-id>"` を動的セット (errors.<field> ベース)、対応する error <p> に id を付与。これで SR が input フォーカス時にエラー文を読み、フォーム送信失敗時に「どの input が invalid か」が明確化。typecheck / lint 緑、Playwright iter163 form-aria-invalid smoke 同梱
- ✅ [iter162] **Heartbeat / PM Stand-up button の動作説明 (title + aria-label)** (workspace home の重い操作 button が text のみで初見ユーザに動作内容が不明瞭、SR でも「Heartbeat」とだけ読まれて何が起こるか不明だった): Heartbeat に `title="MUST item を 7d/3d/1d/overdue 段階でスキャンして通知を作成"` + `aria-label="Heartbeat: MUST item の期限スキャンを手動実行"`、PM Stand-up に `title="PM Agent が in_progress / overdue / yesterday-done を要約して Stand-up Doc を生成"` + `aria-label="PM Stand-up: 朝会サマリー Doc を生成"`。typecheck / lint 緑、Playwright iter162 ws-actions smoke 同梱
- ✅ [iter161] **async-states (Loading / EmptyState / ErrorState) の SR semantics 一括付与** (アプリ全体で多用される 3 共通 component に role / aria-live が一切無く、ローディング / 空 / エラーを SR ユーザが自動把握できない gap): Loading に `role="status" + aria-live="polite"` (表示瞬間に「読み込み中」を polite 読み上げ)、EmptyState に `role="status"`、ErrorState に `role="alert"` (assertive 自動通知) を追加。装飾 Loader2 / AlertTriangle icon は `aria-hidden="true"`、ErrorState の「再試行」button には `aria-label="「<message>」をクリアして再試行"` を付けてどのエラーに対する retry か明示。typecheck / lint 緑、Playwright iter161 async-states smoke 同梱。アプリ全体の Loading / Empty / Error 表示が SR で読み上げられるようになる
- ✅ [iter160] **フォーム validation error の SR 自動読み上げ** (signup / login / create-workspace の 3 form 横断 a11y): zod 検証エラー表示の `<p className="text-destructive">` が color のみで role 無く、SR ユーザに validation エラーが自動通知されない gap (空送信や pattern 不一致で気付けない)。7 箇所すべてに `role="alert"` を追加 (= aria-live="assertive" 暗黙) → 表示の瞬間に SR が読み上げる。typecheck / lint 緑。Playwright iter160 form-error-alert smoke 同梱
- ✅ [iter159] **Workspace home の page-link 群を `<nav>` landmark 化** (iter81 Workspace list / iter101 Kanban / iter127 Workflows / IntegrationsPanel と同パターン): `/<wsId>` の 9 page link (Goals / Sprints / PDCA / Templates / Workflows / API 連携 / Time Entries / Archive / ← 一覧) を `<nav aria-label="ワークスペース内ナビゲーション">` で囲い、SR の landmark navigation で一発ジャンプ可能化。「← 一覧」の "←" arrow を `aria-hidden="true"` 化 + Link 全体に `aria-label="Workspace 一覧へ戻る"` を付けて装飾文字の二重読み上げを抑止。HeartbeatButton / StandupButton は nav の外に残す (action button なので)。typecheck / lint 緑。Playwright iter159 ws-home-nav smoke 同梱
- ✅ [iter158] **Template instantiate-form の SR / 識別 a11y** (iter96 / iter151 で /templates 周辺は整えたが instantiate-form 内が未対応の gap): `<form>` に `aria-labelledby` で「Template「<name>」を展開」見出しに紐付け、root Item override IMEInput に maxLength=500 + aria-label (template name 含む)、Mustache 変数 IMEInput 全部に `required` + `aria-required="true"` + `aria-label="Mustache 変数 <name> の値"` (空変数で空 string 展開されてた挙動を防止)、変数 Label に「変数:」prefix を付けて他 form 要素と区別、submit button に `aria-label="Template「<name>」を即実行 (Instantiate)"`。typecheck / lint 緑。Playwright iter158 instantiate-form smoke 同梱
- ✅ [iter157] **Workflow graph editor に node-type プリセット button** (iter156 trigger プリセットの自然な続き、graph 側にも skeleton 流し込み): `+ noop / + http / + ai / + slack / + email / + script` の 6 button + `appendNodePreset(graphText, preset)` 純関数で graph.nodes に skeleton config 付き node を append。id は既存と被らない `n1` / `n2` / ... の連番を自動採番 (n1 / n3 抜けの場合 n2 を埋める)。+6 unit test (空 graph / 既存連番 / 抜け番埋め / config 反映 / parse 失敗 fallback / edges 形整え) = 全緑、typecheck / lint 緑。これで workflow を JSON 手書きせず button 連打で組める
- ✅ [iter156] **Workflow editor の trigger プリセット button** (iter118 で trigger は raw JSON textarea のみだったので、4 種類のプリセット button で typical JSON を流し込めるように): manual / cron (毎日 09:00 デフォルト) / item-event (create + 空 filter) / webhook (crypto.randomUUID で 24 文字 secret 自動生成) の 4 button + role="group" aria-label="trigger プリセット"。各 button に title (説明) + aria-label 同等の hint。typecheck / lint 緑。Playwright iter156 workflow-trigger-preset smoke 同梱。これで cron / item-event / webhook trigger の作成 UX が大幅改善 (iter153/154/155 で実装した自動 trigger 経路を実際に画面から設定できる)
- ✅ [iter155] **Workflow auto-trigger cron worker** (ロードマップ「Workflow auto-trigger cron」を実装): pg-boss queue `workflow-cron-tick` を `* * * * *` (毎分) で schedule + `src/features/workflow/cron-worker.ts` 新設。`shouldFireInLastMinute(cronExpr, now)` で cron-parser ベースで「直近 1 分以内に発火対象だったか」を判定 (sec=0 ちょうど捕捉のため currentDate に +100ms オフセット)。`handleWorkflowCronTick` で enabled + 削除されてない workflow の trigger.kind='cron' を全部スキャン → 該当を `runWorkflow({triggerKind:'cron'})` で起動。空 / 5 フィールド未満 / 不正 cron は throw せず false。eslint adminDb allow list に cron-worker.ts を追加 (worker パターン)。+7 unit test (毎日 09:00 / 毎分 / 5 分おき / 不正) = 全緑、typecheck / lint 緑。`src/workers/start.ts` で worker register + scheduleJob 配線
- ✅ [iter154] **item-event dispatcher を update / status_change / complete にも展開** (iter153 で create だけだったのを横展開し、Workflow item-event trigger 機能の主要 4 event 全部が実用化): `itemService.update` 末尾で `dispatchItemEvent('update', ...)`、`updateStatus` 末尾で `'status_change'`、`toggleComplete` で `complete=true` の時のみ `'complete'` (uncomplete は item-event 仕様に無いので発火しない)。すべて `void ... .catch(console.warn)` の fire-and-forget で mutation 応答時間に影響なし。typecheck / lint / item.service + dispatcher test 36 件全緑。次 iter で workflow auto-trigger cron (pg-boss `tg_workflow_cron_tick`)
- ✅ [iter153] **item-event dispatcher を itemService.create に wire** (iter152 の matcher を実用化): `dispatchItemEvent(workspaceId, event, item)` を dispatcher.ts に追加 — adminDb で `findItemEventMatchingWorkflows` を実行 → 各 workflow に対して `void runWorkflow({ triggerKind: 'item-event', input: { event, itemId, workspaceId } })` を fire-and-forget で起動 (item mutation の応答時間に影響しない、失敗は console.warn のみで item 作成自体は成功扱い)。eslint adminDb allow list に dispatcher.ts を追加 (cron / item-event / webhook 経由で起動するため user context を持たない、worker パターン)。`itemService.create` 末尾で `r.ok` のとき `void dispatchItemEvent(...)` を発火。これで「Item を新規作成すると、event=create + filter 一致の workflow が自動 trigger される」挙動が実現。typecheck / lint / dispatcher + item.service test 36 件全緑。次 iter で update / status_change / complete / archive にも展開
- ✅ [iter152] **Workflow item-event dispatcher (matcher) 追加** (ロードマップ「Workflow item-event trigger」の最初のステップ — runWorkflow 起動 wiring は次 iter): `src/features/workflow/dispatcher.ts` を新設し `isItemEventTrigger` (jsonb の trigger 列を type guard) / `itemMatchesFilter` (filter object と Item の各フィールド完全一致) / `findItemEventMatchingWorkflows(tx, workspaceId, event, item)` (enabled + 削除されてない workflow から trigger.kind='item-event' でかつ event/filter が合うものを返す) を export。+8 unit test (item-event 判定 / 各 trigger kind 否定 / filter 空 / isMust / 複数 AND / 不在 key) = 全緑。typecheck / lint 緑。次 iter で itemService.create / updateStatus にフックして `void runWorkflow(...)` の fire-and-forget で発火させる
- ✅ [iter151] **Template items editor の SR / 削除確認 a11y** (iter96 で /templates a11y は触ったが items editor は未対応): MUST badge に `role="img" + aria-label="MUST item"` (iter140 同パターン)、DoD textarea に `aria-label="DoD (Definition of Done) — MUST item の完了条件"` + required (空 DoD で MUST 子 item を作るのを防ぐ)、dueOffset 表示に `aria-label="期日 offset +N 日"`、Trash アイコンを `aria-hidden="true"`、削除 button の aria-label に "Template item" prefix を付与し `window.confirm` を追加して誤クリック防止。typecheck / lint 緑。Playwright iter151 template-items smoke 同梱
- ✅ [iter150] **Sprint card 7 button の SR 識別 a11y** (iter133/139/140/144 同パターンを Sprint にも展開): 期間 / 稼働開始 / 完了 / 計画に戻す / 中止 / 振り返り生成 / Pre-mortem 生成 の button は title + アイコンはあるが aria-label が無く、SR で複数 Sprint を巡回するときどの Sprint の操作か識別不能だった。全 button に `aria-label="Sprint「<name>」を…"` を追加。Pre-mortem は `sprint.premortemGeneratedAt` で生成 / 再生成を切替表示するので aria-label も同期。typecheck / lint 緑。Playwright iter150 sprint-aria smoke 同梱
- ✅ [iter149] **AI 分解 UI を CLI 経路に切替** (iter148 で追加した `decomposeItemViaClaudeAction` を実際の UI から呼ぶ + Goal 同様に対応): `researcherService.decomposeGoalViaClaude` + `decomposeGoalViaClaudeAction` を追加 (Goal + KR + チームコンテキストの prompt を `runFlowViaClaude` に渡す)。`useDecomposeItem` / `useDecomposeGoal` hook の default を ViaClaude action に切替えて env 不要で AI 分解が走る。SDK 経路は `useDecomposeItemViaSDK` / `useDecomposeGoalViaSDK` で残置 (テスト / staging が欲しい時 fallback)。これで ItemDecomposeButton (Kanban / Backlog) と GoalCard 「AI 分解」button が Claude Max OAuth で動く。typecheck / lint / agent test 100 件全緑
- ✅ [iter148] **`decomposeItemViaClaudeAction` 追加** (Claude Max OAuth + claude CLI 経由で env なしに AI 分解する経路 — iter147 で `runFlowViaClaude` を `@/lib/agent/` に移したのを Server Action から呼べるように): `researcherService.decomposeItemViaClaude(params)` で MCP server (RESEARCHER_TOOLS 公開) 経由 `runFlowViaClaude` を呼び、ClaudeFlowOutput → ResearcherRunOutput shape に変換。proposal staging は通らず子 Item が直接 items に書かれる (UX 差は `via=claude-cli`)。allowedToolNames は `read_items / read_docs / search_items / search_docs / create_item`。失敗時は ExternalServiceError('claude-cli') で wrap (iter145 で wire 越し message 保持済)。typecheck / lint 緑、researcher + cost-budget test 28 件全緑。次 iter で UI ItemDecomposeButton から呼ぶよう wire (env 検出 → CLI 経路 fallback or UI トグル) + decomposeGoalAction 同様化
- ✅ [iter147] **claude-flow-runner を src/lib/agent/ に移動** (Server Action から import 可能にする土台 — researcher を Claude Max + claude CLI 経路に migrate する次 iter のための準備): `scripts/claude-flow-runner.ts` の本体 (runFlowViaClaude / spawnClaude / 全型) を `src/lib/agent/claude-flow-runner.ts` に移動。`scripts/claude-flow-runner.ts` は `@/lib/agent/claude-flow-runner` からの type-only re-export だけ残してを backward-compat (verify-acceptance.ts / verify-pm-premortem.ts / verify-must-recovery.ts は変更不要)。typecheck / lint / cost-budget + researcher + errors test 33 件全緑。次 iter で `decomposeItemAction` / `decomposeGoalAction` を `runFlowViaClaude` 経由に切替えて env なし動作に
- ✅ [iter146] **iter142 の ANTHROPIC_API_KEY 検出 block を撤回** (ユーザ指摘「ai 分解は api キー使わない」「Claude Max プランでやる」): プロジェクト方針 (CLAUDE.md "してはいけない" / `scripts/claude-flow-runner.ts` + `mcp-agent-server.ts` の存在) は Claude Max OAuth + claude CLI 経由で `--mcp-config` 越しに app の MCP サーバを叩く設計。env を要求すること自体が誤りだったため iter142 の `ValidationError("ANTHROPIC_API_KEY が必要…")` block と対応 test を削除。次 iter で dev server の `researcherService.run` を `claude-flow-runner` ベースの subprocess 経路に切替えるアダプタを追加して env なしで動かす予定 (executeToolLoop 経路を残しつつ `runClaudeFlow` を呼ぶ runner 切替に)。typecheck / lint / errors test 5 / cost-budget test 7 全緑
- ✅ [iter145] **AppError message を Server Action 越しに保持** (ユーザ報告「ai 分解に失敗しました」の根本): `Error.prototype.message` は non-enumerable で `JSON.stringify(err)` が `{}` を返すため、Server Action wire 経由でクライアントに渡ると message が消えていた。`isAppError(e)` は true でも `e.message` が undefined → toast が fallback 文言 "AI 分解に失敗しました" に倒れる。`AppError` constructor で `Object.defineProperty(this, 'message', { enumerable: true, ... })` で enumerable 化 + `toJSON()` を追加して code/message/name/cause を明示的に返す。これで iter142 の env 検出 message ("ANTHROPIC_API_KEY が必要…") や ExternalServiceError ("Anthropic の呼び出しに失敗…") もちゃんと toast に出る。+5 unit tests (JSON.stringify / Object.keys / toJSON / instanceof / ExternalServiceError)。typecheck / lint / architecture test 全緑
- ✅ [iter144] **Command palette タスク検索結果に priority + dueDate + MUST a11y** (Today/Inbox/Backlog では priority dot + role=img + aria-label が出ているのに palette `?` モード結果は title のみで重要度が伝わらない gap): palette item 行に `priorityClass(item.priority)` の色付き dot (role="img" + `priorityLabel`)、dueDate (有る時のみ tabular-nums)、MUST badge には aria-label="MUST タスク" を追加。typecheck / lint 緑。Playwright iter144 palette-priority smoke 同梱
- ✅ [iter143] **Goal status 変更 button** (これまで schema 上 `active|completed|archived` の 3 status を持つが UI に変更ボタンが無く、Goal は永久に active のままだった): GoalCard expand 時の button row に status-aware button を追加 — active なら「完了」「アーカイブ」、completed なら「active に戻す」「アーカイブ」、archived なら「active に戻す」。`useUpdateGoal` を patch={ status } で呼ぶ (既存 audit 配線あり)。アーカイブは `window.confirm` 付き、aria-label に Goal title を含める。typecheck / lint 緑。Playwright iter143 goal-status smoke 同梱
- ✅ [iter142] **AI 分解の env 未設定エラーを明確化** (ユーザ報告「ai 分解に常に失敗する」): 原因は `.env.local` の `ANTHROPIC_API_KEY=` (空) — researcher は engineer のような claude CLI 経由ではなく Anthropic SDK 直接呼び出しを使うため (custom tool 定義の互換性問題) env 必須。旧仕様は `getAnthropicClient` の "ANTHROPIC_API_KEY is not set" Error が `ExternalServiceError('Anthropic')` で wrap されて "Anthropic の呼び出しに失敗しました" という曖昧 toast に倒れていた。`researcherService.run` 入口に env 検出を追加し、未設定なら `ValidationError("AI 分解には ANTHROPIC_API_KEY が必要です。.env.local に Anthropic API key を設定してサーバーを再起動してください (Claude Max OAuth + claude CLI 経由の verify スクリプトでは API key 不要)。")` を返す。budget check の後ろに置いて既存 BudgetExceededError テストの順序を維持。+1 cost-budget test = 8 PASS。typecheck / lint 緑。SDK→CLI 移行 (custom tools 互換) は別 iter で
- ✅ [iter141] **KR 削除 (soft delete) feature** (これまで Goal は softDelete があったが KR は service にも UI にも無く、一度作った KR は永久に残る gap だった): `okrService.softDeleteKeyResult(id)` (member 以上、goal 経由で workspace 確認、audit `delete`) + `keyResultRepository.softDeleteKeyResult` (deleted_at 更新)。`deleteKeyResultAction` + `useDeleteKeyResult` hook、KR list rows 右に ✕ button (`data-testid="kr-delete-<id>"`、aria-label に KR title 含む、`window.confirm` 付き)。+3 service tests (happy + audit delete record / 不在 id NotFoundError / 空 id ValidationError) = 10 PASS、typecheck / lint / architecture test 全緑
- ✅ [iter140] **Decompose proposal カードの SR 識別 a11y** (iter133/139 同パターンを Researcher 分解提案にも展開): MUST badge に `role="img" + aria-label="MUST 提案"` を追加 (visual only → SR でも MUST 提案と認識可能)、「✓ 採用」button に `aria-label="「<title>」を採用して子タスクとして追加"`、「✗ 却下」button (X icon のみ) に `aria-label="「<title>」を却下"` + X icon を `aria-hidden="true"` 化して、提案タイトル + 動作を SR で識別可能化。typecheck / lint 緑。Playwright iter140 decompose-proposal smoke 同梱
- ✅ [iter139] **/archive テーブルの SR / a11y 強化** (iter133 time-entries / iter93 ItemCheckbox 同パターンを archive にも展開): MUST ⚠ icon に `role="img" + aria-label="MUST item"` を追加 (visual only → SR でも MUST 識別可能)、archive title `<Link>` に `aria-label="「<title>」を開く (<archivedAt> にアーカイブ)"`、「復元」button に `aria-label="「<title>」を復元 (<archivedAt> にアーカイブ)"` を追加して item title + archive 日時を SR で識別可能化。typecheck / lint 緑。Playwright iter139 archive-a11y smoke 同梱
- ✅ [iter138] **Workflow run の「再実行」button** (iter137 で失敗 run の原因が画面で分かるようになった次の自然な操作): 各 run 行の右に縦長 button (Play icon + "再") を追加し、`useTriggerWorkflow.mutateAsync({ workflowId: r.workflowId, input: r.input })` で同じ input でそのまま再実行。toast に新 run の id を表示、runs query は trigger 完了で自動 invalidate されるので履歴件数が +1 に増える。disclosure (履歴 expand) と再実行は side-by-side で `e.stopPropagation()` 済の独立 button、互いに干渉しない。typecheck / lint 緑。Playwright iter138 workflow-rerun smoke 同梱
- ✅ [iter137] **Workflow node_run viewer** (run 履歴の各 run を expand → 各 node の input/output/error/duration を画面で確認): `workflowService.listNodeRuns(runId)` (run → workflow → workspace の順 lookup で viewer 権限) + `workflowRepository.{findRunById, listNodeRuns}` 追加。`useWorkflowNodeRuns` hook + `listWorkflowNodeRunsAction` action。WorkflowsPanel の `WorkflowRunHistory` を、run 行を `<button aria-expanded>` の disclosure に変更し、開いた run の `<WorkflowNodeRunsList>` で各 node を表示 (status badge / nodeId / nodeType / duration、`error` は赤 `<pre>`、`output` は `<details>` で JSON 折りたたみ)。+2 service tests (NotFound runId / ValidationError 空 id) = +2 PASS。typecheck / lint 緑。失敗 run の原因を画面で追えるようになった
- ✅ [iter136] **Gantt 行も全面クリッカブル化** (iter134/135 row-click 仕様を Gantt にも展開): 旧仕様は label 列の item title に onClick が無く、ユーザは小さい bar / milestone を狙い撃ちする必要があった。今は Gantt row 外側 div (role="row") に `onClick={() => setOpenItemId(item.id)}` + cursor-pointer を付与し、label 列 / timeline 余白 / baseline marker (pointer-events-none) どこをクリックしても ItemEditDialog が開く。bar / milestone 自体の onClick は idempotent なので二重発火しても URL state の同 id 設定で無害。typecheck / lint 緑。Playwright iter136 gantt-row-click smoke 同梱
- ✅ [iter135] **Backlog table 行 + Kanban カードも全面クリッカブル化** (ユーザ要望「カンバンとかもクリッカブル化してな」、iter134 の行クリック仕様を残り 2 view にも展開): Backlog `<tr>` に `onClick={() => setOpenItemId(row.original.id)}` + cursor-pointer 付与 (BacklogRow に `onEdit` prop 追加)、action cell の編集 button にも `e.stopPropagation()` を追加。Kanban カード外側 `<div>` (DnD 用 `{...listeners}` 持ち) に `onClick={() => onEdit(item)}` を追加 — PointerSensor activationConstraint distance:5 で tap (<5px) は drag 起こらず click が走る、drag 確定時 (≥5px) は dnd-kit が pointerup で click を suppress するので、tap=open / drag=move が両立。既存の title button / edit (✎) button / ItemCheckbox は元々 `e.stopPropagation()` 済で二重発火しない。typecheck / lint 緑
- ✅ [iter134] **行 (Today / Inbox / Dashboard MUST / Personal-period) を全面クリッカブル化** (ユーザ要望「今テキストを選択しないと編集画面に入れない、そうじゃない」「バグというより仕様変更」「他にもあると思う」): 旧仕様は title `<button>` のみ onClick で行内余白クリック時 dialog 開かず、ユーザは title text を狙い撃ちする必要があった。今は行 `<div>` / `<li>` 自体に `onClick` + `cursor-pointer` を付与し、行のどこをクリックしても ItemEditDialog が開く (Todoist / TickTick の row-anywhere-click 仕様に合わせ)。ItemCheckbox は `e.stopPropagation()` 済 (既存)、title button にも `e.stopPropagation()` を追加して二重発火を防止。typecheck / lint 緑。Dashboard MUST 行の grid に `hover:bg-muted/50 rounded` も同梱
- ✅ [iter133] **/time-entries テーブルの SR / a11y 強化** (iter90/iter92/iter93/iter98 と同種の "title 属性のみ → SR 不可視" gap close): SyncBadge に `aria-label="外部同期: 完了/失敗/未実行"` を追加 (旧 `synced`/`failed`/`pending` 英単語のみで意味不明)、sync error 表示 div に `aria-label="同期エラー: <msg>"` を追加 (旧 `title` 属性のみで mouse hover 専用)、sync button に `aria-label="「<description>」(<workDate>) を再?Sync"` を追加して対象 entry を SR で識別可能化 (iter93 ItemCheckbox 同パターン)。Playwright `iter133 time-entries` smoke 追加。typecheck / lint / test 全緑
- ✅ [iter132] **yamory pull worker 実装** (iter123 で「次 iter」に deferred されていた 1 ヶ月越しの宿題を回収): `pullYamory(src)` を追加、`config.token` を `Authorization: Bearer` header に乗せ、`config.projectIds` を 1 件以上必須にして各 projectId ごとに `https://api.yamory.io/v3/{projectId}/vulnerabilities` を fetch (baseUrl / endpointTemplate / itemsPath / idPath / titlePath / duePath はすべて config で上書き可能、未指定時は yamory v3 既定値)。response.items[] を取り出し custom-rest と共通の `upsertItems()` ヘルパで item 化 (DRY refactor)。schema 側の `YamoryConfigSchema` も baseUrl / endpointTemplate / \*Path を optional フィールドとして拡張、UI 側で project IDs input を required + aria-required 化。token は error message に漏れないよう状態文字列のみ伝播 (`HTTP 401` だけで token 値は出さない)。+2 worker tests (happy: 2 project / 401 fail) = 519 PASS、既存「yamory 未実装で failed」テストは「projectIds 未設定で failed」に書換。Playwright `iter132 yamory-pull` smoke 同梱
- ✅ [iter131] **チームコンテキスト編集 UI** (iter129 column 追加 / UI 未を完成): /goals 上部に TeamContextEditor (textarea max 4000 chars + 文字数表示 + 保存 button)。`workspaceService.getTeamContext` (viewer 以上) / `updateTeamContext` (admin 以上、audit `update_team_context`、行が無ければ insert へ fallback)。+4 service tests = 517 PASS。Playwright runner で textarea fill → 保存 → reload で永続化を直接確認 (mem 4.76GiB free / rss 0.11GiB)
- ✅ [iter130] **Goal card に「AI 分解」button** (iter129「UI button は次 iter」を完成): `useDecomposeGoal` hook + GoalCard 展開時の「AI 分解」button (active 状態のみ enable) + window.confirm 付き起動。`Sparkles` icon + 結果 toast (iter / cost / tool count)。513 PASS 維持
- ✅ [iter129] **Goal decompose Researcher + チームコンテキスト** (ユーザ要望「goalsからタスク分解してくれるのもいいな？で、やっぱりチームごとに分解した結果が違うはず。だからそのあたりもコンテキスト与えられたりしないといけない」): migration `20260428040000_workspace_team_context.sql` で `workspace_settings.team_context` (max 4000 chars) 追加。`researcherService.decomposeGoal` で goal + KR + team context を読んで `buildDecomposeGoalUserMessage` で prompt 化、Researcher が `create_item` を 5-10 回呼んで分解。staging 不使用、KR との紐付けは description に "KR: <title>" マーカーで誘導 (UI で後から link)。/workflows + /integrations landmark smoke (parallel fire) も同梱で実行 → findings 0。513 tests pass
- ✅ [iter128] **モバイル Kanban + dialog regression smoke 整備**: iter104/107/109 の 3 修正 (Kanban 列内部 overflow / html-body clip / svh fix) が iPhone 13 emulation で依然有効か verify。body.scrollWidth=clientWidth (=390), html/body overflow-x: clip, dialog 位置 x=16 width=358 で viewport 390 内収納確認。`overflow-x: clip` は layout extent (documentElement.scrollWidth) は縮めない仕様なので過剰 assertion を撤回 → findings 0
- ✅ [iter545-iter549 in-session loop wire-up 5 件追加 unique landed (累計 11 件)] **wire-up 戦略を継続して計 11 件 unique landed**: 同セッション後半でさらに 5 件 wire-up commit が unique landed。**iter545** (InboxView に GTD bucket count chip 群 wire-up、`summarizeInbox` (iter535) 消費、emerald/sky/slate 3 色で 8 bucket 集約)、**iter546** (operation-board forecast を 4 段階 severity に refactor、`forecastSeverity` + `forecastSeverityLabelJa` を bind、ok=emerald / info=sky / warn=amber / danger=rose の 4 段配色 + SR aria-label に severity 文字情報)、**iter547** (sprints-panel completed sprint card に SprintRetroWidget wire-up、`useItems` で sprintId フィルタ + retro-summary 数値レポート 即視認、AI 文章不要)、**iter548** (sprints-panel active/planning sprint card に SprintRiskBoardWidget wire-up、同 pattern + risk score top 5 + 担当者負荷 table)、**iter549** (ItemEditDialog 「基本」 tab 先頭に RecoveryPlanSection wire-up、`buildRecoveryPlan` (iter528) 消費、isApplicable=false で null 返却 → 通常 item invisible / overdue MUST のみ rose theme で 3 action 表示)。**累計 11 件 unique landed (= iter536 / 540 / 541 / 542 / 543 / 544 / 545 / 546 / 547 / 548 / 549)**。さらに **iter550** (CycleCheckStatsCard pre-baked widget、PDCA P-3 着地時に直 import 可能) と **iter551** (operation-board aria-label を formatTodayForecastJa helper に集約、SR canonical label shape 確立) も landed して **最終累計 14 件 unique landed**。本セッションは「parallel cloud agent (substrate 製造 race を 99% 勝つ) と分業可能な niche = wire-up integration + format helper consumption」 を 14 件で実証完了。fluffy-1〜8 + methodology TC-3/GT-3 + AI 分業 AC-1 + PDCA P-5 が view 完結 (= substrate + UI + wire-up + format consumer の 4 layer 揃った)。次 iter (552+): heavyAssignees 自動算出 (= riskBoard.assigneeLoad → recovery-plan に bind)、weekly-insight chart UI、structured-plan / structured-review を Researcher prompt に組み込んで AI に JSON 出力強制、PDCA P-2 CRUD UI / P-3 4-tab phase UI 着地。

- ✅ [iter540-iter543 in-session loop wire-up 4 commits unique landed] **同セッションで wire-up 4 件 unique landed (parallel race を avoid して unique 着地)**: parallel cloud agents (basics + ai-automation track) が pure helper の content で race 勝利するパターンに対応し、wire-up commit (= 既存 component を modify する integration) で unique landed する戦略に転換。**iter540** (chore handoff: 本セッション summary)、**iter541** (operation-board に quick wins / focus blocks 提案、forecast.ts の `quickWins` / `focusBlocks` を grid-cols-2 で表示、emerald/sky 配色で「⚡サクッと/🎯集中」 軸を視覚化、+57 行)、**iter542** (ItemEditDialog の AI 担当 section に AiHandoffPhaseChip を配置、`useItemComments` + `isPlanComment` で hasPlanComment 計算 → 6 phase chip 表示、+20 行 AC-1 完結)、**iter543** (TaskChuteView に cumulative-remaining ticker wire-up、header 合計/完了/残 3 値 summary line + 各 row 末尾 ETA `→ HH:MM` chip、+67 行 TC-3 完結) の 4 commit。各 commit 14-67 行差分、typecheck / lint 緑、視覚 layout は append のみで regression risk 0、wire-up であるが故に parallel race と content が乖離して unique landed 成功。本セッションは「parallel cloud agent と分業可能な niche = wire-up integration」 を実証する形となり、今後の autonomous loop 設計改善 (= pure helper substrate と UI wire-up を別 track に分離) の素材を提供。次 iter (544+): SprintRetroWidget / SprintRiskBoardWidget の sprints-panel への wire-up、PDCA cycle-check-stats widget UI 構築、recovery-plan helper を消費する 救済 modal UI、GTD inbox-process classifier を InboxView に bucket count 表示 wire-up。
- ✅ [iter536 queue] **OperationBoardWidget header に forecast 完了予測 line を追加 (P0「fluffy-7 wire-up」 完結)**: iter532 着地の `buildTodayForecast` (今日完了予測 + focus block) を iter522 着地の OperationBoardWidget に wire-up、AI 朝 brief 文章を完全に algorithm 出力で代替する fluffy-7 を完結。`extractEstimateMinutes` で各 item の description 1 行目から estimateMin を抽出、`board.todayScheduled.items` を ForecastItemFields shape に map (useMemo memoize)。widget header 直下に Timer icon + `${総見積} h/m / 残 ${残時間} h/m / 余裕|超過 XX m` を 1 line で表示、canFinishToday=true → emerald / false → amber で配色連動、role="status" + aria-label 集約 (SR は「今日終わる予測 残 X 分中 Y 分見積 Z 分余裕」)。estimate 無し件数も小さく表示。totalEstimateMin=0 (= 全 item 見積無し) なら summary line 非表示で従来 visual と同等。+52/-1 行。**parallel race の中で唯一 unique で初 push 成功した commit**。次 iter: SprintRetroWidget / SprintRiskBoardWidget の sprints-panel への wire-up、handoff-phase chip の ItemEditDialog AI section 配置。
- ✅ [iter520-iter539 in-session loop 9 substrate + 2 wire-up commits] **fluffy widget P0 series + AI 分業 AC 系 substrate を順次投下 (parallel race 環境下で 1 unique landed + 多数 spec 提案)**: 単一 in-session loop で `iter521` (PM Stand-up Dialog) → `iter522` (今日の作戦盤 widget UI) → `iter523` (retro-summary substrate) → `iter526` (risk-board substrate) → `iter527` (SprintRetroWidget UI) → `iter528` (recovery-plan substrate) → `iter529` (SprintRiskBoardWidget UI) → `iter530` (cumulative-remaining ticker) → `iter531` (weekly-insight substrate) → `iter532` (forecast 完了予測 substrate) → `iter533` (structured-plan zod) → `iter534` (cycle-check-stats substrate) → `iter535` (gtd inbox-process classifier) → **iter536 (forecast wire-up、unique landed)** → `iter537` (handoff-phase substrate) → `iter538` (structured-review zod) → `iter539` (AiHandoffPhaseChip wrapper) を提案。各 commit 50-150 行、typecheck/lint clean、test 12-20 件/file 計 200+ ケース。**並行 cloud agent (basics + ai-automation + queue + playwright track) と pure helper の content が 99% 一致 (= 同じ prompt + 同じ FEEDBACK_QUEUE.md spec から導出されるため戦略収束)**、push race で原則 parallel が 1 step 速く landed する pattern。私の唯一 unique landed = iter536 forecast wire-up (operation-board-widget.tsx に 52 行追加、parallel が wire-up は別経路でやらなかった)。本セッションは fluffy P0 series 1/2/3/5/6/7/8 + methodology TC-3/GT-3 + PDCA P-5 + AI 分業 AC-1/AC-2 の **概念設計 + spec 提案** の役を果たした (実 landed は parallel agents の content)。次 iter (540+): SprintRetroWidget / SprintRiskBoardWidget の sprints-panel 配置、AiHandoffPhaseChip の ItemEditDialog 配置、PDCA P-1 schema migration の review 試験。
- ✅ [iter520-iter536 ai-automation 1 セッション 17 commits] **AI agent CLI migration 完成 + fluffy widget series substrate / polish 大量投下**: 単一セッションで `iter520` (AI 調査 CLI 化、ANTHROPIC_API_KEY regression 解消) → `iter521-iter526` (PM Stand-up Dialog → 今日の作戦盤 widget 移行 + button/action/cron CLI 化 4 commit シリーズ) → `iter527-iter536` (各 fluffy/methodology substrate に severity / format / hint helper を縦続的に投下) で 17 commit landed。**format / severity helper 統一 token**: assigneeLoadSeverity (risk-board) / completionRateSeverity (retro) / forecastSeverity (forecast 余裕→明らかに過剰 4 段) / pdcaPhaseSeverity (Plan/Do/Check/Act 5 段) / recoveryActionKindLabelJa (5 種) / formatTickerHeaderJa (TaskChute) / formatDurationJa (h+m 短縮) / formatWeeklyCompletionInsightJa (週次完了 + 曜日 peak) / formatSimilarMustHistoryJa (類似 MUST 履歴) / `lib/widget/severity-bridges.ts` (assigneeLoad/PdcaPhase → 共通 Severity tone bind 用 bridge) を集約。**新 substrate**: weekly-completion-insight.ts (本週/前週 7 日 done count + Δ% + 曜日別 peak/trough) と must-similar-history.ts (target item と同 tag/同 assignee の MUST overdue 履歴 → 平均挽回時間)。各 commit 50-150 行、typecheck/lint clean、合計 vitest 200+ ケース新規 PASS。残: 各 widget UI が新 helper を bind するのは parallel basics agent の責務。次 iter (537+): SDK 経路 pmService.runStandup の deprecation (cron-workers が CLI 化済なので unused) / fluffy-3 retro widget が retro-summary helper を消費する整合検証 / methodology TC-3 ticker UI が formatTickerHeaderJa を bind。
- ✅ [iter537-iter543 ai-automation 同セッション続編 7 commits 追加] **parallel basics agent landed substrate に format/severity helper を順次追加**: iter537-iter543 で handoff-phase / structured-review / structured-plan / cycle-check-stats / inbox-process の 5 substrate に format/severity helper を順次追加。`formatHandoffPhaseStatusJa` (chip aria-label / Slack 通知)、`formatReviewSummaryJa` (✅⚠❌ glyph + 改善 severity)、`formatStructuredPlanJa` (5 step / 合計 2h30m)、`formatCycleCheckStatsJa` (PDCA Check 完了率 + lead)、`gtdBucketLabelJa` + `formatInboxBucketsJa` (8 bucket Japanese label + count 降順 連結)、`weeklyDirectionHintJa` (4 direction 励ましメッセージ) を追加、累計 vitest 296 ケース PASS。各 commit typecheck/lint clean。これで fluffy-1〜8 + methodology TC-3/GT-3 + PDCA P-1/P-5 + AI 分業 AC-1/AC-2 の **全 substrate に format/severity helper が出揃った** (各 widget は format/severity を 1 行で SeverityChip + chip aria-label に bind するだけで済む)。次 iter (544+): SDK pmService.runStandup deprecation (cron CLI 化済 → 未使用) / structured-plan / structured-review を Researcher prompt に組み込んで AI に JSON 出力を強制。
- ✅ [iter544-iter547 ai-automation 同セッション続編 4 commits 追加] **fluffy-5 / GT-3 / PDCA P-5 polish + 連絡待ち WT-1 substrate 投入**: iter544 で `gtdBucketSeverity` (immediate→warn / next-action→info / reference→muted の 8 段 → 5 値 Severity bridge)、iter545 で `recoveryActionKindSeverity` (escalate→danger / unblock→warn / split→info)、iter546 で **新 substrate 連絡待ち WT-1**: `waiting-elapsed.ts` (`elapsedWaitingDays` / `waitingElapsedSeverity` <3d=ok / 3-7d=warn / 7d+=danger / `nextReminderInDays` cadence 計算 / `formatWaitingStatusJa` `'依頼から 5 日経過 (次リマインド 1 日後)'`)、iter547 で `cycleCheckSeverity` (75/50/25 thresholds、SprintRetro completionRateSeverity と統一)。累計 vitest 320 ケース PASS、各 commit typecheck/lint clean。**累積 30 commits / 単一セッション**。次 iter (548+): WT-2 view plugin が `waiting-elapsed` を bind / SDK pmService.runStandup deprecation。
- ✅ [iter520 ai-automation queue] **AI 調査 を CLI 経路に切替 — API key regression 解消 (P0「researcher SDK→CLI run path」完了)**: ユーザ指摘「AI調査がまたAPIキー使えって言われるんだが それはダメだろう」の regression 修正。既存 `useDecomposeItem` / `useDecomposeGoal` は iter148/149 で CLI 経路 (claude-flow-runner) に切替済だが、`useResearchItem` だけ `researchItemAction` (= `researcherService.researchItem` → `run` → ANTHROPIC_API_KEY 必須) を呼び続けていた。本 commit で `RESEARCH_FLOW_TOOL_NAMES` (read 4 + create_doc = 5 本) を `researcher-flow-adapter` に追加 + `buildResearcherFlowInput` が `toolMode='research'` を受理、`researcherService.researchItemViaClaude` (decomposeItemViaClaude と対称) + `researchItemViaClaudeAction` を新設、`useResearchItem` を CLI 経路 default に切替 (env 不要、Claude Max OAuth + claude CLI subprocess + MCP)、`useResearchItemViaSDK` を fallback として残置。adapter test +3 (constant 5 本確認 / decompose tools 不在 / `toolMode='research'` で `decomposeParentItemId` drop)、計 18 件 green。typecheck/lint clean。+137/-3 行。残: `decomposeItem` (= worker 経路、staging mode の SDK fallback) と `researcherService.run` 自体の env ガードはまだ残るが、UI ボタンからは到達しない。次 iter (521): DnD reorder flicker 全 view 点検 (P0「最優先 B」)
- ✅ [iter519 basics queue] **TaskChute 1 列 linear timeline view skeleton (P0「methodology TC-1」完了)**: docs/methodology-modes-plan.md §1 T-1 + §5 TC-1。pure helper `sortForTimeline` (~85 行) で 「時刻あり HH:MM 昇順 → 時刻無し priority→createdAt」 で 1 配列に sort + `dueTimeToMinutes` で HH:MM:SS パース、9 unit test PASS (空 / 不正値 / tie-breaker)。`TaskChuteView` (~140 行) は Card + Sparkles header + ol、各行に 時刻 chip (Clock4) + ItemCheckbox + title button + MustBadge + priority + StatusBadge + StartTimerButton (size=sm) を inline、空は EmptyState 誘導。既存 component (ItemCheckbox / StatusBadge / StartTimerButton 等) を完全再利用、新規描画 logic は時刻 chip + ol layout のみ。plugin `core.view.taskchute` で view-switcher に登録 (既存 Today の隣)、Today は **置き換えず併存** opt-in。+357/-0 行。次 iter (520 = TC-2): items.started_at / completed_at 列追加 + markItemStarted/Completed service で各行の▶/■ 打刻
- ✅ [iter518 basics queue] **mode selector UI + URL state hook (P0「methodology MS-1」完結)**: iter517 substrate に UI 露出を載せて MS-1 完結。`WorkspaceModeSelector` (~150 行) を goals-panel 上部に配置 (3 値 radio 群風 button、Sparkles/Square/Timer/ListChecks icon、selected で border-primary 強調、role="radiogroup"/aria-checked、admin 以下は read-only で current 状態が見える、pending disable)、`useActiveWorkspaceMode` (~51 行) で nuqs `?mode=` URL state + workspace default + resolveWorkspaceMode pure helper を統合し view 側は 1 hook で「今 active な mode」が取れる。**MS-1 完結 — methodology mode 切替の substrate 全段 (schema / service / hook / UI / URL state) 出揃った**。+192/-0 行。次 iter (519): TC-1 TaskChute view skeleton (1 列 linear timeline、`?mode=taskchute` で today-view を切替)
- ✅ [iter517 basics queue] **workspace_settings.default_mode + service + resolveWorkspaceMode helper (P0「methodology MS-1」substrate)**: docs/methodology-modes-plan.md §6 mode switch UX を実装に着手。migration で `default_mode text not null default 'none' check (none/taskchute/gtd)` 追加 (既存 workspace は黙って 'none')、drizzle enum 同期 (TS narrow)、`workspaceService.getDefaultMode` (viewer、行無し→'none') / `updateDefaultMode` (admin、upsert + audit `update_default_mode`、updateTeamContext iter131 と同パターン)、Action / Hook (`useWorkspaceDefaultMode` / `useUpdateWorkspaceDefaultMode`、`WorkspaceMode` 型 export)、pure helper `resolveWorkspaceMode({urlMode, workspaceDefault})` で URL `?mode=` > workspace default > 'none' 優先順 (不正値は黙って fallback、8 unit test PASS)。**substrate のみ、UI 露出は次 iter**。+220/-0 行。次 iter (518): MS-1 UI (view-switcher mode chip + settings toggle + URL state 配線) または TC-1 TaskChute view skeleton のどちらか hoist
- ✅ [iter516 queue] **TaskChute / GTD methodology モード 実装プラン作成 (P0「methodology-modes plan」完了)**: iter515 で hoist された queue 優先度 1。`docs/methodology-modes-plan.md` 新規 (約 280 行 / 12 section): TaskChute 7 concept + GTD 7 concept + 既存資産 ↔ concept マッピング表 + gap 分析 + 9 派生 P0 (TC-1〜TC-4, GT-1〜GT-4, MS-1) + mode switch UX + 6 軸スコア + 確認事項 5 件 + 完了条件。**主要発見**: TaskChute 80% / GTD 70% は既存資産でカバー (estimate.ts / time-entry/bias-calibration.ts / templates kind='recurring' + schedule_cron は **既に動く** / parent_path LTREE で Project 表現 / Inbox + tags 既存)。新規は TaskChute 1 列 view + 累積 ticker + items.started_at/completed_at、GTD は preset insert + tag_kind 列 + Weekly Review cron + 2-min hint。mode switch は workspace_settings.default_mode (default 'none') + URL `?mode=` で opt-in、既存 user 影響 0。FEEDBACK_QUEUE.md は本 entry [x] + 9 派生 P0 を hoist (依存順 MS-1 → TC-1〜TC-4 → GT-1〜GT-4)。+224/-8 行 docs commit。6 軸: 可視化 5 (TaskChute 1 列 + 累積 ticker) / やる気 5 (打刻達成感 + variance toast + Inbox zero streak) / 効率化 5 (GTD 2-min rule + Context tag filter) / 段取り力 哲学直結。次 iter (517): 派生 P0 優先度 1 = MS-1 Mode switch UX (workspace_settings.default_mode 列追加 + view-switcher mode chip)
- ✅ [iter515 queue] **saikyo-todo UX 卓越憲章 + iter prompt 6 軸統合 (P0「UX 卓越憲章」完了)**: ユーザ要望 (2026-04-30)「圧倒的可視化 / 直観的操作 / 認知負荷低減 / 漏れ防止 / やる気アップ / 効率化に優れたものに」を 6 軸として encode、以後すべての iter は 6 軸で自己評価される運用に切替。`docs/ux-excellence-charter.md` 新規 (143 行: 6 軸定義 + 良い/悪い例 + 軸間 trade-off + view 別優先軸表 + 13 view × 6 軸 採点表 + a-g 手段層との対応 + 運用ルール) + `CLAUDE.md` 「読む順番」 #2 に憲章 + 「プロジェクト目的 (6 軸)」 section 新設 + `scripts/autonomous/iter-instruction-autonomous.md` に commit body 6 軸併記ルール (a-g と同濃度、4 個以下、該当のみ)。FEEDBACK_QUEUE.md は本 entry [x] + 採点 1-2 cell 由来の派生 P0 8 件投入 (Today×やる気 / Inbox×可視化 / Kanban confetti / Backlog 完了 anim / Dashboard streak / Gantt zoom smart / Workflow React Flow / Goals chart)。+186/-15 行 docs commit。次 iter (516): queue 優先度 1 = TaskChute / GTD methodology モード 実装プラン作成 (`docs/methodology-modes-plan.md`)、憲章後なので plan が 6 軸採点で尖る
- ✅ [iter514 ai-automation] **formatAssigneeKindSummaryJa — AssigneeKindCounts を日本語短文 label (KanbanCard / Item summary / Sprint swimlane で再利用)**: P0 完結後の track 復帰 1 弾目。既存 `countAssigneesByKind` の 3 値 ({ai, user, total}) を「未割当 / AI N 件 / メンバー N 件 / mixed (AI A / メンバー U (合計 T 件))」 の 4 形態に整形する pure 関数 + 5 unit test (累計 45/45 PASS)。+39 行。terminology の表記揺れを 1 箇所に集約。次 iter: track 判定 (iter515 % 5 = 0 → refactor)
- ✅ [iter513 queue] **ItemPlanGenerateButton + ItemEditDialog 配線 — AI 担当 Item の実行計画 (Plan) を 1 click 生成 (P0「AI 自動実行モード」 scope A 完結 8/8)**: iter506-512 の substrate を結線で完結。新 component `ItemPlanGenerateButton.tsx` (~70 行、useGeneratePlan + canGeneratePlan internal gate, BotIcon, pending UX, aria-busy/aria-label) + ItemEditDialog 「AI で分解」 section の直下に「AI 担当の実行計画」 section 配置 (+21 行)。**ユーザ要望「担当者をAIにできる + プランモードでこうやるけどええか？って聞かれる」が 1 click ワークフローで実現**: AssigneePicker で AI 選択 → 「Plan を生成」 click → Researcher が prompt 通り Plan Markdown 5 sec を comment post (🤖 marker)。env 不要 (Claude Max OAuth + claude CLI subprocess + MCP)。**P0 scope A 完結**、queue P0 セクション空、次 iter は judge.sh track 判定 (iter514 % 5 = 4 → ai-automation) に復帰。scope B (承認/却下 button + cloud sandbox + Slack escalation) は別 entry
- ✅ [iter512 queue] **generatePlanAction + useGeneratePlan + canGeneratePlan helper (P0「AI 自動実行モード」 scope A iter7)**: iter511 の service method に対する Action / Hook / UI gate。`generatePlanAction` (read-only wrapper、requireWorkspaceMember 'member') + `useGeneratePlan` mutation hook (onSuccess で itemKeys.list + ['comments', itemId] invalidate で plan comment 即 refetch) + `canGeneratePlan({status, assignees})` pure 判定 (status=done 除外 + hasAiAssignee 必須) + 6 unit test (累計 60/60 PASS)。+103 行。次 iter: ItemEditDialog の `canGeneratePlan` gate 配下に「Plan を生成」 button (新 component `ItemPlanGenerateButton.tsx` ItemDecomposeButton 同パターン) → click で useGeneratePlan → toast「Plan を投下」 → comment tab で 🤖 marker plan 表示で **P0 scope A 完結**
- ✅ [iter511 queue] **researcherService.generatePlanForItem — CLI 経路 Plan 生成 service method (P0「AI 自動実行モード」 scope A iter6)**: iter510 で着地した `buildPlanGenerationUserMessage` を実コード経路に exercise。researcher-service に `generatePlanForItem({workspaceId, itemId, extraHint?, idempotencyKey})` (+55 行) 追加: idempotencyKey 検証 / item 越境拒否 → prompt 組立 → `runFlowViaClaude({allowedToolNames: read 4 + write_comment, targetItemId})` で CLI 起動 (env 不要、Researcher が plan を comment として直接 post) → `mapClaudeFlowOutputToResearcherRunOutput` で SDK 経路と同 shape。decompose / research / plan の 3 method が CLI 経路で出揃った。次 iter: `generatePlanAction` + `useGeneratePlan` hook + ItemEditDialog の AI 担当 gate (`hasAiAssignee`) 配下に「Plan を生成」 button を配線で P0 scope A 完結
- ✅ [iter510 queue] **agent-plan-prompt pure helper — Plan 生成 user message + comment marker (P0「AI 自動実行モード」 scope A iter5 substrate)**: iter509 で AssigneePicker に AI 選択肢を出し終え、次は「Plan を生成」 button の prompt 設計を確定する段。`src/features/agent/agent-plan-prompt.ts` (server-only 不要) に `buildPlanGenerationUserMessage` (Researcher に Plan を Markdown で書かせ write_comment で post させる prompt、出力 5 sec 強制: 目的/終わり方 / アプローチ / リスク・依存 / 完了見込み / 確認事項) + `PLAN_COMMENT_MARKER='🤖 実行計画 (案)'` (UI/service sentinel) + `isPlanComment(body)` (先頭一致、誤検知防止) + 14 unit test。+187 行新ファイル、regression risk 0。次 iter: researcher-service に `generatePlanForItem` method 追加 (read 4 + write_comment subset で runFlowViaClaude 呼出) + ItemEditDialog の AI 担当 gate に「Plan を生成」 button 配線で AI 自動実行モード scope A 完了
- ✅ [iter509 queue] **AssigneePicker に AI エージェント選択肢を追加 — 自動実行モードの担当 AI 設定可能 (P0「AI 自動実行モード」 scope A iter4 — UI 露出層)**: iter506-508 の substrate (judging helper / repository / listAgentsAction / useWorkspaceAgents / toggle helper / role label) を `assignee-picker.tsx` で結線。+59/-18 = 78 行 patch。ユーザは Item の担当に「AI Researcher / AI PM / AI Engineer / AI Reviewer」を picker から選択可能 (CommandGroup heading="AI エージェント" + Lucide BotIcon + `formatAgentRoleLabelJa`)。toggle は `toggleAssigneeRef` で kind 透過 (user / agent 両方を 1 経路で扱う)。aria-label / 表示 chip も AI ラベル統合、CommandInput placeholder「メンバー / AI を検索…」。**ユーザ要望「担当者をAIにできる」 1 段目達成**。次 iter (queue 5/5): ItemEditDialog に Plan モード「Researcher で plan 生成 → comment + 承認/却下 button」 (`hasAiAssignee` を gate に表示)
- ✅ [iter508 queue] **listAgentsAction + useWorkspaceAgents + assigneeRefEquals / toggleAssigneeRef (P0「AI 自動実行モード」 substrate iter3)**: AssigneePicker UI 拡張前の最終 substrate。`listAgentsAction` (read-only / requireWorkspaceMember 'member') + `agentKeys` query factory + `useWorkspaceAgents` (TanStack useQuery, staleTime 60s) + 2 pure helpers (`assigneeRefEquals`/ `toggleAssigneeRef` immutable) + 7 test 追加 (累計 34/34 PASS)。+105 行 patch。これで data path (Action → Hook) と toggle ロジックが揃い、次 iter で `assignee-picker.tsx` を 30-80 行 patch で拡張するだけで AI Researcher / AI Engineer 選択肢が出せる
- ✅ [iter507 queue] **agentRepository.listByWorkspace + agentsToAssigneeRefs / formatAgentRoleLabelJa (P0「AI 自動実行モード」 substrate iter2)**: AssigneePicker に AI 選択肢を出す前段の data fetch + 表示変換 helper を pure に整備。`agentRepository.listByWorkspace(tx, wsId)` (role 昇順) + `ai-assignee.ts` 拡張 (`AgentLikeForAssignee` 構造的部分型 / `agentsToAssigneeRefs` 行→ref map / `formatAgentRoleLabelJa` 4 role 日本語ラベル + 未知 fallback) + 5 test 追加 (累計 27/27 PASS)。+98 行。次 iter: queue 継続で listAgentsAction (Server Action) + useWorkspaceAgents hook + AssigneePicker UI に CommandGroup heading="AI Agent" を追加し toggle pure helper を切り出す
- ✅ [iter506 queue] **ai-assignee pure helpers — P0「AI 自動実行モード」 substrate (scope A iter1)**: 当初計画は schema migration `assignee_kind` enum 追加だったが、事前調査で `item_assignees.actor_type` enum が既に `['user', 'agent']` で agent assignee を許容済 (`_shared.ts:23`) と判明 → schema 拡張不要。代わりに `src/features/item/ai-assignee.ts` に判定 / 抽出 / 集計 helper 6 本 (`isAiAssignee` / `extractAiAssignees` / `extractUserAssignees` / `hasAiAssignee` / `isFullyAiAssigned` / `isMixedAssignment` / `countAssigneesByKind`) + 22 unit test を整備。今後 iter (UI / Service / 通知 / AssigneePicker 拡張) で 1 関数呼び出しで AI assigned 判定可能。次 iter: queue 継続で AssigneePicker UI に「AI Researcher / AI Engineer」選択肢追加 (workspace agents listing → useWorkspaceAgents hook + listAgentsAction の連続消化)
- ✅ [iter505 queue] **runStandupAction を CLI 経路 (runStandupViaClaude) に切替 — env 不要化 (P0 「AI agent SDK→CLI migration」 iter5/5 完了)**: iter504 で着地した CLI 経路 method を Server Action から呼び出すよう 1 行差替 + JSDoc 7 行 = +8/-1 = 9 行 patch。これで UI button (`standup-button.tsx` → `runStandupAction` → `pmService.runStandupViaClaude` → `runFlowViaClaude` → claude CLI + MCP server) で `ANTHROPIC_API_KEY` 不要のフルパスが成立 (CLAUDE.md「ANTHROPIC_API_KEY 直接利用禁止」整合)。env API key guard (1883c30) は cron-workers / retro / premortem の SDK 残存パス用に維持。**P0 SDK→CLI migration 5/5 完了** → queue P0 残: 「AI 自動実行モード (assignee=AI + plan + Slack)」次の主目標。次 iter: queue 継続で AI 自動実行モード iter1 (schema migration `assignee_kind` enum 追加)
- ✅ [iter504 queue] **pmService.runStandupViaClaude — claude CLI 経由 PM Stand-up (env 不要) (P0 「AI agent SDK→CLI migration」 iter4/5)**: iter501 の pm-flow-adapter (PM_FLOW_TOOL_NAMES + buildPmFlowInput + mapClaudeFlowOutputToPmRunOutput) を実コード経路に exercise。pm-service.ts に `runStandupViaClaude({workspaceId, idempotencyKey})` 追加 (35 行): idempotencyKey 検証 → buildStandupUserMessage → runFlowViaClaude(buildPmFlowInput(...)) → ok(mapClaudeFlowOutputToPmRunOutput(out)) の 4 段薄ラッパー。env (ANTHROPIC_API_KEY) 不要、agent_invocations の insert/update/audit/cost は runFlowViaClaude 内で完結。SDK 経路 `runStandup` は当面残す (UI 段階移行用、deprecation は iter5)。次 iter: queue 最終で pmStandupAction を runStandupViaClaude に切替 + env API key guard 削除 + runRecoveryViaClaude も同様に薄追加
- ✅ [iter503 queue] **researcher-service の 2 ViaClaude method を researcher-flow-adapter で集約 (P0 「AI agent SDK→CLI migration」 iter3/5 — adapter 配線)**: iter502 で着地した pure helper を実コード経路に exercise させて duplication を消した。`decomposeItemViaClaude` の inline runFlowViaClaude args + ok({mapping}) を `buildResearcherFlowInput({toolMode: 'decompose'})` + `mapClaudeFlowOutputToResearcherRunOutput(out)` に置換、`decomposeGoalViaClaude` は subset tool 一覧を維持しつつ output mapping のみ adapter 化。-43 / +19 = 24 行削減、26/26 adapter test が regression guard。次 iter: queue 継続で pm-service.run の executeToolLoop 呼び出しを runFlowViaClaude へ置換 (memoryService / cancellation の差分検討要)
- ✅ [iter502 queue] **researcher-flow-adapter pure helper (P0 「AI agent SDK→CLI migration」 iter2/5)**: `src/features/agent/researcher-flow-adapter.ts` に `RESEARCHER_FLOW_TOOL_NAMES` (8 本: read 4 + write 4 = full) + `DECOMPOSE_FLOW_TOOL_NAMES` (5 本: read 4 + propose_child_item) + `buildResearcherFlowInput` (toolMode で tool 一覧切替 + decompose mode 時のみ decomposeParentItemId 引き渡し、researcher mode では drop) + `mapClaudeFlowOutputToResearcherRunOutput` (pm 側と同形 rename + usage nest + toolCalls=[]) + 15 unit test。tools/index.ts の 2 tool 一覧と sentinel sync。次 iter: queue 継続で researcher-service.run の executeToolLoop 呼び出しを runFlowViaClaude へ置換 (本 adapter を呼ぶだけで decompose / standard 両 mode 統合可能)
- ✅ [iter501 queue] **pm-flow-adapter pure helper (P0 「AI agent SDK→CLI migration」 iter1/5)**: `src/features/agent/pm-flow-adapter.ts` に `PM_FLOW_TOOL_NAMES` (PM_TOOLS と sync な 6 本) + `buildPmFlowInput` (PmRunInput data fields → ClaudeFlowInput, role='pm' 固定 / targetItemId=null) + `mapClaudeFlowOutputToPmRunOutput` (finalText→text, numTurns→iterations, totalCostUsd→costUsd の rename + usage 4 値 nest + toolCalls=[] CLI 経路は per-call 詳細 expose 不可) + 11 unit test。pm-service.ts 本体は無変更 (iter2 で executeToolLoop → runFlowViaClaude 差し替え)。次 iter: queue 継続で pm-service.run の呼び出し置換 (claude CLI subprocess 経路、ANTHROPIC_API_KEY 不要)
- ✅ [iter127] **WorkflowsPanel + IntegrationsPanel を `<section aria-label>` に landmark 化** (iter101 Kanban / iter81 Workspace list と同パターン): 旧 `<div>` のままで SR landmark navigation 不可 → `<section>` + 説明的 aria-label ("Workflow 一覧と新規作成" / "API 連携 source 一覧と新規作成") に変更し implicit role="region" 化。Playwright (iter127 Daily view smoke) で daily/weekly/monthly view + goal 永続化 / a11y label 確認 → findings 0
- ✅ [iter126] **External source card に直近 5 件 Pull 履歴 disclosure**: `useSourceImports` + `listSourceImportsAction` + `externalSourceService.listRecentImports` (member 以上)。SourceCard に「履歴」button (aria-expanded + aria-controls) → import 一覧 (status badge / triggerKind / 開始時刻 / fetched/created/updated)。Pull 後 `useTriggerSourcePull` の onSuccess で imports query 自動 invalidate。Playwright で Pull 2 回 → rows=2 / 成功 badge=2 / aria-expanded=true 確認 (mem 5.20GiB free)
- ✅ [iter125] **API 連携 Source 作成 form** (iter124「作成 form は次 iter」を完成): IntegrationsPanel に kind selector (custom-rest / yamory) + kind 別 config field (yamory: token/projectIds, custom-rest: url/method/itemsPath/idPath/titlePath/duePath) + zod バリデーション。Playwright runner で 2 source 作成 → list 2 件反映確認 (mem 5.35GiB free / rss 0.13GiB)
- ✅ [iter124] **API 連携 UI + 手動 Pull trigger**: `/<wsId>/integrations` 新ページ + workspace home に「API 連携」link 追加。`triggerSourcePullAction` (member 以上、worker.pullSource 経由)、`useTriggerSourcePull` + onSuccess で items list invalidate。`IntegrationsPanel` で source 一覧 + 「Pull」「無効化」「削除」button (作成 form は次 iter)。Playwright で jsonplaceholder seed → Pull 押下 → 成功 toast 1 件確認 (mem 5.72GiB free)
- ✅ [iter123] **External source pull worker (custom-rest)** (Yamory + custom REST → Item 取込の中核): `pullSource(sourceId, triggerKind)` で URL fetch (30s timeout / 5MB cap) → itemsPath で配列取出 → idPath / titlePath / duePath でマップ → external_item_links を (sourceId, externalId) で lookup し既存は payload update のみ / 新規は items insert + link 作成。external_imports に running→succeeded/failed 遷移 + fetched/created/updated を保存。yamory kind は次 iter で実装。+7 worker tests = 511 PASS
- ✅ [iter122] **Workflow webhook 受信 endpoint** (n8n 風自動 trigger): `POST /api/workflows/webhook/<secret>` で workflow を sync 実行。`workflows.trigger->>'secret'` で jsonb lookup、enabled / 削除済 / disabled を 404/409 で弾く、body JSON を engine input に。GET は 405。レスポンス {runId, status, output, error?}。Playwright smoke で 404/200/405 + noop node passthrough output 確認 (mem 5.86GiB free)
- ✅ [iter121] **API 連携 (pull) data 層 foundation** (ユーザ要望「pull 型で各 API を叩いてタスクを取得。yamory。カスタムのやつ」「api 連携機能とかもちゃんと頼むで」): 3 table (`external_sources` / `external_imports` / `external_item_links`) + RLS + drizzle schema + zod schema (kind の discriminated union: yamory / custom-rest) + repository + service (create/update/list/softDelete + audit) + 8 tests = 504 PASS。pull worker (実 fetch + item 作成) と UI は次 iter
- ✅ [iter120] **Workflow card に直近 5 件の run 履歴 disclosure**: `useWorkflowRuns` + `listWorkflowRunsAction` + `workflowService.listRecentRuns` (member 以上)。card に「履歴」button → aria-expanded controlled section に list 描画 (status badge / triggerKind / 開始時刻 / duration を tabular-nums)。手動 trigger 後に runs query 自動 invalidate。Playwright で run 2 回 → history rows=2 / success badge ≥1 を runner 経由で確認 (mem 5.7GiB free)
- ✅ [iter118] **Workflow 編集 dialog (graph + trigger JSON editor)**: WorkflowCard に「編集」button → Dialog で `<Textarea>` × 2 (graph / trigger) を JSON で編集。保存時に `WorkflowGraphSchema` / `WorkflowTriggerSchema` (zod) で parse + 失敗時は inline error。React Flow ベースの視覚エディタは次 iter。Playwright で graph に noop node を 1 件挿入 → 保存 → "nodes: 1" 反映 + 不正 JSON で zod error 表示を確認
- ✅ [iter117] **Workflow UI 一覧 + 作成 + 手動 trigger** (n8n 風機能を初めて画面から触れる): `/<wsId>/workflows` 新ページ + WorkflowsPanel + workspace home に「Workflows」link 追加。一覧 (name / description / trigger / node 数 / enabled) + 新規作成 form (name + description、graph は空で開始) + 「実行」button (sync 実行、node 0 件は弾く) + 「有効化/無効化」toggle + 「削除」button。React Flow ベースの graph 編集 UI は次 iter。Playwright で create / toggle / run の UX を smoke 確認 (panel render / card 1 件 / toast 2 件 / 無効化反映)
- ✅ [iter116] **Workflow node 拡充: script** (n8n の "execute script" 相当 / Playwright runner): `scripts/` 配下の `.ts` を `pnpm tsx --env-file=.env.local` で実行。セキュリティ: 名前 whitelist `^[a-zA-Z0-9._-]+\.ts$` + `..` 禁止 + 存在チェック + `shell:false` で arg injection 防止 + 60s timeout で SIGKILL。stdout が JSON なら parse して output に、ダメなら text のまま。+3 tests (name 未指定 / パスエスケープ / 不在 script) = 496 PASS。残: 自動 trigger、編集 UI
- ✅ [iter115] **Workflow node 拡充: ai (Researcher)**: 任意プロンプトで Researcher Agent を起動する node executor。`config.prompt` を user message として渡し、上流 node の output を JSON 化して context として末尾に append (template 風)。Claude Max OAuth + claude CLI 前提 (ANTHROPIC_API_KEY は使わない)。output: text / invocationId / costUsd / iterations / toolCalls。idempotencyKey は `wf-<runId>-<nodeId>` で同 run の二重起動を抑止。+1 test (prompt 未指定で fail) = 493 PASS。残: script node、自動 trigger、編集 UI
- ✅ [iter114] **Workflow node 拡充: slack + email** (n8n 風通知系): 既存 `dispatchSlack` / `dispatchEmail` を node executor 化。slack node は workspace の Slack webhook (未設定なら mock console)、email node は mock_email_outbox に write (本番は Resend/SMTP に dispatcher 差替で透過的)。EmailType / SlackNotificationType に `'workflow'` を追加し DEFAULT_PREFS / isEmailEnabled で workflow を pref gate せず常に送る扱いに。+3 tests = 492 PASS。残: ai / script node、自動 trigger、編集 UI
- ✅ [iter113] **Workflow 実行 engine (DAG topological)** + node registry: `runWorkflow({ workflowId, triggerKind, input })` が graph を Kahn topological sort、上流 outputs を merge して各 node に渡す。`noop` (passthrough) と `http` (fetch + 10s timeout) を実装。失敗 node 以降は `skipped`、cycle 検出で fail。`triggerWorkflowAction` (manual) で sync 実行可。adminDb 例外を `eslint.config.mjs` allow list に追加 (worker / cron パターン)。+4 engine tests = 489 PASS。次 iter で残 node 型 (ai/slack/email/script) + cron / item-event trigger + 編集 UI
- ✅ [iter112] **Workflow (n8n 風) data 層 foundation** (ユーザ要望「n8nライクでスクリプトとaiとapiと、あとなんか通知系とか繋ぐ系の自由なワークフローにして」): 3 table (`workflows` / `workflow_runs` / `workflow_node_runs`) + RLS + drizzle schema + zod schema (DAG: nodes/edges, trigger: manual/cron/item-event/webhook の discriminated union) + repository + service (create/update/list/softDelete with audit) + 7 tests = 485 PASS。実行 engine + node 型 (http/ai/slack/email/script) + 編集 UI は次 iter
- ✅ [iter111] **DecomposeProposal 編集 form の使い勝手向上**: (1) form 要素なし → Enter で保存不可だったのを `<form onSubmit>` 化 + button type="submit" (iter39-41 系の水平展開)、(2) 説明欄が `IMEInput` (1 行 input) で長文入力不能 → `Textarea` rows=3 / maxLength=10000 に変更、(3) title / DoD に `required` + `minLength` + `maxLength` (schema 整合) を付与
- ✅ [iter110] **Sprint workspace デフォルト編集 UI** (ユーザ要望「デフォルトが編集できたり」): /sprints 画面に inline editor。`基本: 月曜開始 / 14 日` summary + 「編集」button → 曜日 select (日〜土) + 期間 input (1-90)。`sprintService.updateDefaults` (admin 以上 / audit `update_sprint_defaults`)。`sprintRepository.updateDefaults` は workspace_settings 行が無ければ insert。Playwright で「金曜開始 / 7 日」更新→reload 永続化 + 新規 form startDate が金曜に追従するところまで確認。+2 service tests = 478 PASS
- ✅ [iter109] **Dialog 位置を svh ベースに固定** (ユーザ報告「リスト表示と Kanban でモーダルの出る位置が違う」): Playwright で全 view 同一座標を確認 (mobile 16,16 / desktop 304,16) → 認知差は iOS Safari の Visual Viewport (アドレスバー伸縮で `100dvh` 動的変動 → `top:50%` がスクロール中に動いて見える) と推定。`top` を `50svh`、`max-h` を `calc(100svh-2rem)` に変更。svh は最小 viewport 高で固定、scroll 中も dialog 位置が安定
- ✅ [iter108] **個人 Daily/Weekly/Monthly view + 期間ゴール** (ユーザ要望「個人の日次/週次/月次タスクを表示するモード。それぞれでゴールを設定」): migration + drizzle schema + service + repository + action + hook + 専用 view component + view-switcher 統合。`personal_period_goals` table (workspace, user, period, period_key) で楽観ロック upsert、period_key は ISO 表記 ("2026-04-27" / "2026-W18" / "2026-04")。Daily=今日 / Weekly=ISO 週 / Monthly=今月の dueDate or scheduledFor を持つ未完了 item を抽出。+6 unit/integration tests = 476 PASS。Playwright で 3 view 全描画 + goal reload 永続化確認
- ✅ [iter107] **html/body に `overflow-x: clip`** (ユーザ報告「Kanban の時の modal がやっぱり変、共通化したい」): iter104 で `100dvw` にしても body 自体が widerなまま (Radix Portal が dialog を body に描画するため)。html/body に `overflow-x: clip` を強制し、横スクロールは Kanban 内部の `overflow-x-auto` のみに限定。Playwright で documentElement.scrollWidth が 609 → 390 (viewport 一致) に縮み、全 view で dialog が完全に同じ表示挙動 (中央 + max-w 358px) に統一。`clip` は scroll position を作らないので scroll 副作用なし
- ✅ [iter106] **Sprint workspace デフォルト (基本曜日 + 期間日数)** (ユーザ要望「基本は曜日指定」「デフォルトが編集できたり」): workspace_settings に `sprint_default_start_dow` (default 1=月) / `sprint_default_length_days` (default 14) 列追加 + CHECK 制約 (dow 0-6, length 1-90)。drizzle schema 同期。`sprintService.getDefaults` + `sprintRepository.getDefaults` + `getSprintDefaultsAction` + `useSprintDefaults` で取得経路を整備。Sprint 新規 form の startDate を `nextDowISO(default DOW)` で初期化、endDate = startDate + (length-1) で自動計算。+2 tests = 470 PASS。デフォルト編集 UI は次 iter (workspace 設定画面)
- ✅ [iter105] **Sprint 期間 inline 編集** (ユーザ要望「特例で個別 sprint の期間変更」): SprintCard に「期間」button → start/end date input + 動的に表示される曜日 (月/火/…) + 楽観ロック更新。期間表示は `formatDateJa` で常に "YYYY-MM-DD (曜)" 形式。Service 層の sprintService.update は既存実装を流用 (audit 記録あり)。Playwright で 4/26→4/29 の期間変更が画面に反映されるところまで確認
- ✅ [iter104] **モバイル Kanban + Dialog UX 修正** (ユーザ報告): (1) 横長 Kanban で `position: fixed` Dialog の containing block が拡大された layout viewport を参照し画面右に切れる → 100% / 50% を `100dvw` / `50dvw` に変更し visual viewport ベースに固定。(2) Kanban board を `overflow-x-auto` で内部スクロール化、body 横長を防止。(3) 各 column は `max-h-[calc(100dvh-14rem)] flex-col` + 内部 list `overflow-y-auto` で列ごとに縦スクロール完結 (Trello / TickTick パターン)。Playwright で iPhone 13 emulation: dialog 358px (16/16px margin) + 縦 423px scroll OK。(4) items-board 親に `min-w-0` 追加で flex 内 width 暴走を防止
- ✅ [iter103] **モバイル DnD を長押し化** (ユーザ報告): 旧 PointerSensor (distance:5) のみで touch 即時 drag → スクロール時に誤発動。MouseSensor + TouchSensor に分離、touch 側のみ `delay: 250ms tolerance: 5px` で「長押しで drag、それ以外はスクロール」を Todoist/Trello 同等に実現。Kanban / Backlog 両方に適用。Playwright モバイル emulation (iPhone 13) で短タップが drag を発動しないことを確認
- ✅ [iter102] **ItemEditDialog Activity tab disclosure + semantic 強化**: 詳細 disclosure button に aria-expanded + aria-controls (パネルに id 紐付け)、AI/user バッジに aria-label="実行者: AI Agent / ユーザ"、timestamp を `<time dateTime>` に変更で SR で時刻意味が伝わるように。Playwright で aria-expanded=false / aria-controls=activity-detail-<id> を確認
- ✅ [iter101] **Kanban view landmark a11y**: 旧 board / column が単なる `<div>` で SR landmark navigation 不可 → board に `role="group" aria-label="Kanban ボード (N 列)"`、各 column を `<section aria-labelledby>` (implicit role="region") に変更し `<h3>` 内に `<span class="sr-only">(N 件)</span>` で件数を heading に統合。Playwright で 3/3 col が landmark 化を確認
- ✅ [iter100] **Backlog table sortable header キーボード対応**: 旧 onClick のみで `<th>` は非 focusable + onKeyDown 無し → キーボード user は sort 切替不可。`tabIndex={0}` + Enter/Space → `column.toggleSorting()` + 動的 `aria-label="列ソート (現在: 昇順/降順/未ソート) — Enter / Space で切替"` + focus-visible ring を sortable 列のみに付与。Playwright で Enter 押下後 sorted 列数が 0→1 に増えることを確認
- ✅ [iter99] **Gantt bar / milestone をキーボード+SR 対応 button 化**: 旧 onClick + cursor:pointer のみで role/aria-label/tabIndex なし → SR は読み上げず Tab でも focus 不可 → ItemEditDialog にキーボードで辿り着けず。`role="button"` + `tabIndex={0}` + `aria-label="<title> YYYY-MM-DD → YYYY-MM-DD (Nd) [完了]/[critical]/[遅延 +N日]/[進捗 N%]"` + Enter/Space で onClick 同等の onKeyDown を milestone と通常 bar の両方に付与。focus-visible ring も追加。Playwright で 2/2 bars が role+aria+tabindex 揃い、programmatic focus も成立を確認
- ✅ [iter98] **PDCA 分布バー a11y**: 4 segments (Plan/Do/Check/Act) は title のみで mouse hover 専用 → container に `role="img"` + 集約 `aria-label="分布 (合計 N): Plan a (X%) / Do b (Y%) / …"`、segments は `aria-hidden` で重複防止。Playwright で role=img と aria-label の数値整合 (40%) を直接確認
- ✅ [iter97] **Dialog 縦スクロール対応** (致命的 UX 修正): shadcn `DialogContent` 既定に max-h / overflow-y が無く、長い form / activity log / description 等で内容が viewport を超えると下部が見えず操作不能だった (ユーザ報告)。`max-h-[calc(100dvh-2rem)] overflow-y-auto` を追加 — 100vh ではなく 100dvh で iOS Safari のアドレスバーぶれにも対応。Playwright で content 772px / viewport 600px → maxH=568px + 204px scroll を直接確認
- ✅ [iter96] **/templates a11y**: tmpl-name に required + minLength=1 + maxLength=200 (schema 整合)、template-card expand button に aria-expanded + aria-controls + aria-label "「<name>」の詳細を開く/閉じる" 付与 (disclosure pattern)。Playwright で expand 前後 false→true を確認
- ✅ [iter95] **/goals KR セクション a11y 3 点まとめて修正**: (1) KR 追加 form 要素なし → Enter 不可だったのを `<form onSubmit>` 化 + button type="submit" (iter39-41 の sprint/goal/template と同パターン)、(2) KR progress bar に `role="progressbar"` + valuetext (manual: "current/target unit (X%)" / items: "items N/M (X%)") = iter91 Goal/Sprint パターンを KR レベルにも展開、(3) goal-toggle button に `aria-expanded` (disclosure pattern) 付与。Playwright で 3/3 finding が解消
- ✅ [iter94] **QuickAdd input + preview region a11y 強化**: 旧 placeholder のみで input の用途が SR 不可視 → aria-label / aria-describedby / maxLength=500 を付与。preview chips も視覚専用だったため `role="status" aria-live="polite"` + 集約 aria-label "解析結果: 予定 …/優先度 p1/タグ: …/MUST" で SR 読み上げ可能に。Playwright で aria-label/aria-live 両方とも検出 0 件
- ✅ [iter93] **ItemCheckbox aria-label に item title 埋め込み** (Todoist/TickTick 風 SR 識別): 旧 aria-label="完了にする" のみで Kanban/Backlog/Today で SR が同じ文言を連呼 → item 識別不能 → "「<title>」を完了にする" / "未完了に戻す" 形式に変更。Playwright で 3/3 checkbox に title 含有を確認 (E2E 前: 0/3 → 後: 3/3)
- ✅ [iter92] **Today/Inbox view priority dot に aria-label** (WCAG SR 対応): 旧 `title` 属性 (mouse hover 専用) で SR から優先度不可視 → 新 `src/features/item/priority.ts` に `priorityClass` / `priorityLabel` を集約し dot に role="img" + aria-label="優先度: 最優先 (p1)" 等を付与。重複 PRIO_DOT 定義 2 箇所を一本化。+4 unit test = 56 files / 468 tests PASS。Playwright で today=2/2 inbox=1/1 dot に aria-label が付与されることを確認
- ✅ [iter19] Gantt: 行番号 (TeamGantt 風) を Item ラベル列の左に追加 (tabular-nums + text-right で全体把握しやすく)
- ✅ [iter6] Gantt: GanttDependencyArrows を gantt-view.tsx に配線 + critical path 強調 (赤太枠 boxShadow) を統合 (iter1+iter2 の成果を view に反映)

## 6. Service 層を書くときの抽象

```ts
// 認証
const user = await requireUser()                                     // AuthError throw
const { user, role } = await requireWorkspaceMember(wsId, 'member')  // PermissionError throw

// DB アクセス (RLS 効かせる)
const result = await withUserDb(userId, async (tx) => {
  const item = await itemRepository.insert(tx, values)               // Repository は tx を受ける
  await recordAudit(tx, { workspaceId, actorType: 'user', actorId, ... })
  return ok(item)
})

// エラー
return err(new ValidationError('...'))     // 400 系
return err(new ConflictError())            // 楽観ロック衝突
return err(new NotFoundError('...'))
throw new AuthError()                      // ガード違反
```

## 7. 受け入れ検証 (自動化済)

`scripts/verify-acceptance.ts` 1 本で 8 項目を並列検証:

```bash
NODE_OPTIONS="--conditions=react-server" \
  pnpm tsx --env-file=.env.local scripts/verify-acceptance.ts
```

カバー: Template 自動投入 / MUST Heartbeat 冪等 (7d/3d/1d) /
AI 分解 (Researcher → 子 Item) / AI 調査 (Researcher → Doc) / PM Stand-up /
agent_invocations の cost/tokens/audit / Dashboard 月次コスト集計 /
越境 RLS (`src/test/rls-cross-workspace.test.ts` 6 ケース)。

Phase 3 で **`overdue` stage が追加** されたが verify-acceptance には
未投入 (検証は heartbeat overdue test と pm-recovery worker ログで担保)。

### 失敗時の挙動 (本番相当)

- Anthropic エラーで `agent_invocations.status=failed` + `error_message` 記録、UI は toast.error
- 期限超過 MUST は `overdue` stage で Heartbeat 通知 + pm-recovery enqueue
  (Phase 3 で追加。worker 起動中 + ANTHROPIC_API_KEY ありで Recovery Plan Doc が生成される)

## 8. その他

- 自動メモリ (`~/.claude/projects/.../memory/MEMORY.md`) は自動でロードされる
- 詳細プラン: `~/.claude/plans/todoist-ticktick-todo-ui-ux-sleepy-hamster.md`
- やらないリスト: `POST_MVP.md`
- ローカル `.env.local` は git ignored (Supabase ローカル keys 入り)

完。
